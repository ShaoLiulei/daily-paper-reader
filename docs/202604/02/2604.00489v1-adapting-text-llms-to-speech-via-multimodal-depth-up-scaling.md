---
title: Adapting Text LLMs to Speech via Multimodal Depth Up-Scaling
title_zh: 通过多模态深度扩展将文本大语言模型适配至语音
authors: "Kazuki Yano, Jun Suzuki, Shinji Watanabe"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00489v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 通过深度上采样将文本大模型适配到语音
tldr: 本研究针对将预训练文本大语言模型适配为语音模型时导致的文本能力退化问题，提出了“多模态深度上扩”方法。该方法通过在冻结的文本LLM中插入新层并仅对新层进行语音数据训练，实现了高效的跨模态迁移。实验表明，使用E-Branchformer作为插入层，在保持甚至超越全量微调语音识别性能的同时，显著减少了文本能力的损失和可训练参数量，为多模态模型构建提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00489v1/fig-001.webp\", \"caption\": \"Figure 1: Proposed multimodal depth up-scaling strategy for Speech LM adaptation: trainable E-Branchformer blocks are interleaved with frozen transformer blocks to acquire speech capabilities while preserving pre-trained text representations.\", \"page\": 1, \"index\": 1, \"width\": 443, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2604.00489v1/fig-002.webp\", \"caption\": \"Table 2: Effect of added layer architecture (INTERLEAVED placement) and function preserving initialization. Top: architecture comparison. Bottom: initialization comparison for EBranchformer on SmolLM2-1.7B. WMerge is the merge projection (§3.3).\", \"page\": 3, \"index\": 2, \"width\": 464, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.00489v1/fig-003.webp\", \"caption\": \"Table 1: Results on SmolLM2-360M and SmolLM2-1.7B. WER (%) on LibriSpeech (lower is better). Text task accuracy is the average of 8 benchmarks (%, higher is better). ∆ denotes the change from the pre-trained model. Depth up-scaling results are with added layers kept at inference. Dropping added layers recovers the pre-trained performance (∆ = 0.0) by design. Italic = full fine-tuning (updates all parameters). Bold = best among depth up-scaling.\", \"page\": 3, \"index\": 3, \"width\": 477, \"height\": 358}]"
motivation: 旨在解决将文本大语言模型适配至语音模态时，因持续预训练导致的原始文本处理能力严重退化的问题。
method: 提出在冻结的文本LLM中插入新的神经层（如E-Branchformer），并仅对这些新增层进行语音数据的训练。
result: "该方法在语音识别性能上媲美全量微调，同时将文本能力退化降低了75%以上，并减少了60%的可训练参数。"
conclusion: 多模态深度上扩是一种能有效平衡语音适配性能与文本能力保留的高效模型扩展策略。
---

## 摘要
通过在语音数据上进行持续预训练，将预训练的文本大语言模型（LLMs）适配为语音语言模型（Speech LMs）具有广阔的前景，但通常会损害原始的文本能力。我们提出了多模态深度扩展（Multimodal Depth Upscaling），这是持续 LLM 预训练中一种新兴策略的延伸，即在冻结的文本 LLM 中插入新的 Transformer 层，并仅在语音数据上训练新增层。在 4.8 万小时英语自动语音识别（ASR）数据上使用 SmolLM2-360M 和 SmolLM2-1.7B 进行的实验表明，深度扩展实现了与全量微调相当的 ASR 性能，同时造成的文本能力退化远低于全量微调和低秩自适应（LoRA）。我们进一步表明，将专为语音识别设计的架构 E-Branchformer 作为插入层，在较大模型上实现了匹配或超越全量微调的 ASR 性能，同时减少了 75% 以上的文本退化，且可训练参数减少了 60%。

## Abstract
Adapting pre-trained text Large Language Models (LLMs) into Speech Language Models (Speech LMs) via continual pretraining on speech data is promising, but often degrades the original text capabilities. We propose Multimodal Depth Upscaling, an extension of an emerging strategy in continual LLM pre-training, where new transformer layers are inserted into a frozen text LLM and only the added layers are trained on speech data. Experiments with SmolLM2-360M and SmolLM2-1.7B on 48k hours of English Automatic Speech Recognition (ASR) data show that depth up-scaling achieves ASR comparable to full fine-tuning while causing far less text degradation than both full fine-tuning and Low-Rank Adaptation (LoRA). We further show that incorporating E-Branchformer, an architecture designed for speech recognition, as the inserted layers achieves ASR that matches or surpasses full fine-tuning on the larger model while reducing text degradation by over 75% with 60% fewer trainable parameters.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adapting Text LLMs to Speech via Multimodal Depth Up-Scaling》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**“灾难性遗忘”**：当研究者尝试通过持续预训练（CPT）将预训练好的文本大语言模型（LLM）适配到语音模态（转变为语音语言模型，Speech LM）时，模型在获得语音处理能力的同时，往往会严重损害其原有的文本理解和生成能力。
*   **背景**：现有的缓解策略（如文本数据回放）需要额外的训练成本且难以获取原始预训练数据；参数高效微调（如 LoRA）在跨模态适配时往往表现出表达能力不足。
*   **动机**：提出一种既能高效学习新模态（语音），又能完美保留旧模态（文本）能力的模型扩展方案。

### 2. 方法论：核心思想与关键技术
论文提出了**多模态深度扩展（Multimodal Depth Up-scaling）**方案：
*   **核心思想**：在原始冻结（Frozen）的文本 LLM 层之间插入新的可训练层。在语音适配训练期间，原始 LLM 参数保持不变，仅训练新增层和扩展的语音词表嵌入。
*   **关键技术细节**：
    *   **层插入策略**：研究了五种放置方式，包括交错（Interleaved）、顶部（Top）、中部（Middle）、底部（Bottom）和三明治式（Sandwich）。
    *   **函数保持初始化（Function Preserving Initialization）**：为了防止训练初期新增层破坏预训练表示，通过将新增层的输出投影矩阵初始化为零，使其在初始状态下表现为“恒等映射”（Identity Function），即 $Y=X$。
    *   **E-Branchformer 架构**：除了标准 Transformer 层，论文引入了专为语音设计的 E-Branchformer 作为新增层。它结合了捕获全局依赖的自注意力机制和捕获局部声学特征的卷积分支（cgMLP）。
    *   **推理时层丢弃**：由于原始层未被修改，在仅需文本任务时，可以直接移除新增层，从而 100% 恢复原始 LLM 的性能。

### 3. 实验设计
*   **基础模型**：SmolLM2-360M（32层）和 SmolLM2-1.7B（24层）。
*   **数据集**：使用 OWSM v3.2 语料库中的 4.8 万小时英语 ASR（自动语音识别）数据进行训练。
*   **Benchmark**：
    *   **语音能力**：LibriSpeech（test-clean 和 test-other）的字错率（WER）。
    *   **文本能力**：8 个标准文本基准测试（如 MMLU、ARC、HellaSwag、BoolQ 等）。
*   **对比方法**：全量微调（Full Fine-tuning）和 LoRA（设置了与深度扩展相当的可训练参数量）。

### 4. 资源与算力
*   论文提到该研究得到了日本 JST Moonshot 项目支持，并使用了北海道大学信息倡议中心的超级计算机系统或研究云系统。
*   **具体算力细节**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）、GPU 数量以及具体的训练总时长（仅提到训练步数和学习率调度）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个不同参数量级的模型（360M 和 1.7B），验证了方法的可扩展性。
*   **消融实验**：系统对比了 5 种层放置策略，并对比了标准 Transformer 与 E-Branchformer 两种架构的效果。
*   **初始化验证**：专门针对 E-Branchformer 的初始化策略进行了对比实验。
*   **定性分析**：进行了零样本（Zero-shot）指令遵循实验（翻译、简化、摘要）。
*   **评价**：实验设计较为全面，通过 ASR 性能与文本保留能力的双重维度进行评估，对比基准（全量微调和高秩 LoRA）具有说服力。

### 6. 主要结论与发现
*   **性能媲美全量微调**：在 1.7B 模型上，采用“交错放置”的深度扩展方法，其 ASR 性能达到甚至超过了全量微调。
*   **文本能力保留极佳**：相比全量微调导致的文本能力崩溃（退化 20-30% 以上），深度扩展在保留新增层的情况下仅有轻微退化，若丢弃新增层则退化为零。
*   **架构优势**：E-Branchformer 作为新增层显著优于标准 Transformer，证明了针对模态特性的架构设计在深度扩展中非常重要。
*   **指令遵循**：全量微调后的模型完全失去了遵循文本指令的能力，而深度扩展模型能够直接根据语音输入完成翻译、摘要等复杂任务。

### 7. 优点
*   **零退化保证**：提供了一种物理上可完全恢复原始模型能力的机制（层丢弃）。
*   **高效性**：相比全量微调，减少了约 60% 的可训练参数，同时获得了更好的多模态平衡。
*   **无需数据回放**：在不使用任何文本数据进行训练的情况下，依然能保持极高的文本任务水平，解决了预训练数据获取难的问题。
*   **初始化策略**：成功将“函数保持初始化”应用于复杂的 E-Branchformer 结构。

### 8. 不足与局限
*   **任务单一性**：实验主要集中在英语 ASR 任务上，尚未验证在多语言 ASR、语音翻译或文本转语音（TTS）等更广泛任务上的表现。
*   **推理开销**：虽然可以丢弃层，但在执行语音任务时，模型深度增加了 25%，这会带来额外的推理延迟和显存占用。
*   **算力细节缺失**：缺乏具体的训练资源消耗记录，不便于其他研究者评估复现成本。

（完）
