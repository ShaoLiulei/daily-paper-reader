---
title: "Attention Frequency Modulation: Training-Free Spectral Modulation of Diffusion Cross-Attention"
title_zh: 注意力频率调制：扩散交叉注意力的免训练频谱调制
authors: "Seunghun Oh, Unsang Park"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28114v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 扩散模型交叉注意力的频谱调制
tldr: 本研究探讨了潜在扩散模型中交叉注意力的时空动态，发现其在去噪过程中表现出稳定的从粗到细的频谱演变特征。基于此，提出了注意力频率调制（AFM），这是一种无需训练的推理侧干预方法。AFM 通过在傅里叶域中对交叉注意力逻辑值进行频域重加权和熵自适应门控，实现了对 Token 竞争空间尺度的连续控制。该方法无需重新训练或修改提示词，即可在保持语义一致性的同时实现显著的视觉编辑。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28114v1/fig-001.webp\", \"caption\": \"Fig. 1: Qualitative comparison on Stable Diffusion v1.5 under matched sampling settings (same prompt/seed). (a) Baseline, (b) SAG, (c) FreeU, (d) Ours (AFM).\", \"page\": 3, \"index\": 1, \"width\": 716, \"height\": 213}]"
motivation: 现有的扩散模型交叉注意力机制缺乏对其时空动态特性的深入表征，限制了对其进行原则性的免训练控制。
method: 提出 AFM 方法，在推理阶段利用傅里叶变换对交叉注意力逻辑值进行频域调制，并结合 Token 分配熵进行自适应门控。
result: 实验证明 AFM 能够可靠地重新分布注意力频谱，在不改变模型参数或提示词的情况下产生显著的图像编辑效果。
conclusion: AFM 为扩散模型提供了一种高效、连续且无需训练的手段，通过频域干预来调节 Token 竞争的空间尺度并保持良好的语义对齐。
---

## 摘要
交叉注意力是文本调节潜扩散模型的主要接口，然而其逐步的多分辨率动态特性仍未得到充分表征，这限制了有原则的免训练控制。我们将扩散交叉注意力视为潜空间网格上的时空信号，通过将 token-softmax 权重汇总为与 token 无关的集中图（concentration maps），并追踪其在去噪过程中的径向分箱傅里叶功率。在不同的提示词和种子下，编码器交叉注意力表现出一致的从粗到细的频谱演变，产生了一个稳定的 token 竞争时频指纹。基于这一结构，我们引入了注意力频率调制（AFM），这是一种即插即用的推理时干预方法，在傅里叶域中编辑 token 级别的 pre-softmax 交叉注意力 logits：在 token softmax 之前，低频和高频带根据与进度对齐的时间表进行重新加权，并可由 token 分配熵进行自适应门控。AFM 提供了一个连续的控制柄，可以在无需重新训练、提示词编辑或参数更新的情况下，偏置 token 竞争模式的空间尺度。在 Stable Diffusion 上的实验表明，AFM 能够可靠地重新分布注意力频谱，并在很大程度上保留语义对齐的同时产生显著的视觉编辑效果。最后，我们发现熵主要作为相同基于频率编辑的自适应增益，而不是一个独立的控制轴。

## Abstract
Cross-attention is the primary interface through which text conditions latent diffusion models, yet its step-wise multi-resolution dynamics remain under-characterized, limiting principled training-free control. We cast diffusion cross-attention as a spatiotemporal signal on the latent grid by summarizing token-softmax weights into token-agnostic concentration maps and tracking their radially binned Fourier power over denoising. Across prompts and seeds, encoder cross-attention exhibits a consistent coarse-to-fine spectral progression, yielding a stable time-frequency fingerprint of token competition. Building on this structure, we introduce Attention Frequency Modulation (AFM), a plug-and-play inference-time intervention that edits token-wise pre-softmax cross-attention logits in the Fourier domain: low- and high-frequency bands are reweighted with a progress-aligned schedule and can be adaptively gated by token-allocation entropy, before the token softmax. AFM provides a continuous handle to bias the spatial scale of token-competition patterns without retraining, prompt editing, or parameter updates. Experiments on Stable Diffusion show that AFM reliably redistributes attention spectra and produces substantial visual edits while largely preserving semantic alignment. Finally, we find that entropy mainly acts as an adaptive gain on the same frequency-based edit rather than an independent control axis.

---

## 论文详细总结（自动生成）

这是一份关于论文《Attention Frequency Modulation: Training-Free Spectral Modulation of Diffusion Cross-Attention》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：扩散模型中的交叉注意力（Cross-Attention）是文本引导图像生成的关键，但其在去噪过程中的**时空动态特性**（即不同去噪阶段、不同空间尺度下的表现）尚未被充分表征。
*   **研究动机**：现有的免训练控制方法（如提示词工程、引导缩放）往往是启发式的，缺乏对模型内部机制的深入理解。作者希望探索交叉注意力是否存在稳定的频谱演变规律，并利用该规律实现一种无需重新训练、无需修改提示词的精准推理侧控制手段。

### 2. 方法论：注意力频率调制 (AFM)
*   **核心思想**：将交叉注意力视为潜空间网格上的时空信号，通过在傅里叶域（频域）对注意力逻辑值（Logits）进行重加权，来干预 Token 之间的竞争模式。
*   **关键技术细节**：
    1.  **信号提取**：将 Token 相关的注意力权重通过 Top-K 均值汇总为“Token 无关的集中图”，并利用径向分箱傅里叶变换分析其频谱。
    2.  **频域干预**：在 Softmax 操作之前，对每个 Token 的 Logit 图进行 2D 傅里叶变换。
    3.  **频谱重加权**：根据去噪进度 $u(s)$，使用预设曲线对低频（LF）和高频（HF）带进行缩放。公式形式为：$\alpha_{LF} = 1 + \lambda(1-u(s))$ 和 $\alpha_{HF} = 1 + \lambda u(s)$。
    4.  **熵自适应门控**：引入 Token 分配熵作为增益控制，当注意力分布较散（高熵）时增强干预，分布集中（低熵）时减弱干预。
    5.  **即插即用**：该方法直接作用于 U-Net 的编码器交叉注意力层，不改变模型参数。

### 3. 实验设计
*   **模型与数据集**：主要在 Stable Diffusion v1.5 上进行分析，并在 v1.4 上验证鲁棒性。使用了 COCO-2017 验证集（50 个提示词）和 LAION 标题子集（100 个提示词）。
*   **Benchmark 与对比方法**：
    *   **基准**：原始 Stable Diffusion 生成结果（Baseline）。
    *   **对比方法**：**SAG**（Self-Attention Guidance）和 **FreeU**，这两者都是代表性的免训练推理侧干预方法。
*   **评估指标**：
    *   **注意力层**：高频能量比（HF ratio）、时频热图。
    *   **图像层**：LPIPS（感知相似度，用于衡量编辑强度）、带通 LPIPS（区分结构与细节变化）。
    *   **语义层**：CLIP 文本-图像余弦相似度（衡量语义对齐保持情况）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总计算时长。
*   **实现细节**：由于是免训练（Training-Free）方法，其计算开销主要集中在推理时的 FFT（快速傅里叶变换）和 iFFT。作者提到该方法是“即插即用”的，暗示其对推理延迟的影响在可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：总计生成了约 600 组配对图像（COCO 200 组，LAION 400 组），每组包含 4 个随机种子。
*   **消融与鲁棒性**：
    *   进行了针对不同频率截止半径 ($r_c$) 的鲁棒性测试。
    *   对比了“仅曲线控制”与“曲线+熵控制”的效果。
    *   分析了 AFM 在 U-Net 不同阶段（编码器、中间块、解码器）的下游影响。
*   **评价**：实验设计较为客观，采用了配对采样（相同种子/提示词）来隔离变量，且通过定量指标（LPIPS/CLIP）和定性对比共同支撑结论，实验充分性较高。

### 6. 主要结论与发现
*   **频谱规律**：编码器交叉注意力在去噪过程中表现出极其稳定的“从粗到细”的频谱演变特征。
*   **AFM 效果**：AFM 能够有效重新分布注意力频谱，抑制后期的高频碎片化，从而在图像层面产生显著的视觉编辑。
*   **语义保持**：尽管视觉变化显著（LPIPS 较高），但 CLIP 分数显示 AFM 能够很好地保留原始文本的语义对齐。
*   **熵的作用**：注意力熵并非独立的控制轴，而是作为频率编辑的“自适应增益”。

### 7. 优点
*   **理论支撑**：基于对模型内部动态（频谱演变）的实证观察，而非纯粹的经验性调参。
*   **无需训练**：极大地降低了用户定制生成风格的门槛，且不依赖于复杂的提示词工程。
*   **连续可控**：通过调节强度参数 $\lambda$ 和时间表，可以实现对图像细节/结构连续的干预。

### 8. 不足与局限
*   **代理指标风险**：作者承认注意力图的频谱统计量只是图像特征的“代理”，并不等同于图像本身的傅里叶内容或真实的物体布局。
*   **应用限制**：目前主要在 Stable Diffusion 系列上验证，对于更新的架构（如基于 Transformer 的 DiT）的适用性尚未讨论。
*   **计算开销**：虽然无需训练，但在每一层、每一步进行 FFT/iFFT 仍会增加推理时间，文中缺乏具体的推理延迟量化分析。

（完）
