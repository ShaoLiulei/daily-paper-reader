---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于内存增强型大语言模型智能体的强化学习
tldr: "针对记忆增强型大模型智能体开发中存在的实现碎片化、缺乏统一框架的问题，本文推出了MemFactory。这是一个首创的统一、高度模块化的训练与推理框架，它将记忆的提取、更新和检索等生命周期抽象为“乐高式”的可插拔组件。该框架原生支持GRPO算法，通过多维环境奖励优化内部记忆管理策略，并兼容Memory-R1等前沿范式。实验证明，MemFactory能显著提升智能体性能，最高增幅达14.8%，为未来记忆驱动的AI创新铺平了道路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v2/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2603.29493v2/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强型智能体实现高度碎片化且任务特定，缺乏统一的基础设施来简化集成、训练和评估流程。
method: 提出MemFactory框架，通过模块化架构将记忆生命周期抽象为原子组件，并集成GRPO算法以微调记忆管理策略。
result: "在MemAgent架构上的实验表明，该框架在域内和分布外测试中均优于基础模型，性能提升最高达14.8%。"
conclusion: MemFactory为记忆驱动的AI智能体提供了标准化且易用的基础设施，显著降低了研究门槛并促进了该领域的创新。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管记忆增强型大语言模型（LLM）智能体在长期上下文处理上具有潜力，且利用强化学习（RL）优化记忆操作（提取、更新、检索）已成为趋势，但目前的实现方式高度**碎片化**且**任务特定**。
*   **研究动机**：缺乏一个像 LLaMA-Factory 那样统一、模块化且易用的基础设施，导致研究人员在复现、组合或改进不同的记忆策略（如 Memory-R1, MemAgent, RMM）时面临巨大的工程障碍。

### 2. 方法论：MemFactory 框架
MemFactory 采用四层解耦架构，实现“乐高式”的智能体构建：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：从原始上下文中解析事实和经验。
    *   **Updater**：执行增删改（ADD, DEL, UPDATE, NONE）操作维护记忆库。
    *   **Retriever**：基于语义或重排序（Rerank）检索相关记忆。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent）。
*   **智能体层 (Agent Layer)**：作为策略执行器，通过组合上述模块构建自定义智能体，支持 `generate`（训练中间态）、`rollout`（获取奖励的轨迹）和 `inference`（纯推理）接口。
*   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器。提供 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短期长文本）两种环境，支持格式奖励和 LLM-as-a-Judge 等多维评价。
*   **训练层 (Trainer Layer)**：核心优化引擎。
    *   **核心算法**：原生集成 **GRPO (Group Relative Policy Optimization)**。该算法通过组内奖励归一化计算优势（Advantage），无需额外的 Critic 网络，显著降低了显存占用。
    *   **公式原理**：第 $i$ 个候选响应的优势 $\hat{A}_i = \frac{r_i - \text{mean}(R)}{\text{std}(R)}$，其中 $R$ 为同组样本的奖励集合。

### 3. 实验设计
*   **实验对象**：基于开源的 **MemAgent** 架构进行验证。
*   **基座模型**：Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集/Benchmark**：使用 MemAgent 公开的数据集，包括两个主任务测试集（`eval_50`, `eval_100`）和一个分布外（OOD）测试集（`eval_fwe_16384`）。
*   **对比方法**：对比了各基座模型在经过 MemFactory RL 微调前后的性能表现。

### 4. 资源与算力
*   **硬件环境**：整个训练和评估流水线可以在**单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步骤（valid steps）。为了提高效率，训练时将上下文长度简化为原始长度的约 1/3（每样本 50-80 个文档）。

### 5. 实验数量与充分性
*   **实验规模**：针对两种不同规模的模型（1.7B 和 4B）进行了对比实验。
*   **客观性保障**：所有评分均采用 4 次独立试验的平均值（avg@4），以减少生成随机性带来的偏差。
*   **充分性**：实验覆盖了域内（In-domain）和分布外（OOD）场景，验证了框架的泛化能力。虽然实验主要集中在 MemAgent 范式上，但框架本身已内置了对 Memory-R1 和 RMM 的支持。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 平均提升 **14.8%**，Qwen3-4B-Instruct 平均提升 **7.3%**。
*   **模型规模效应**：较大的模型（4B）在 OOD 任务上表现更稳健，能够更好地将学到的循环记忆策略迁移到未见过的长文本设置中。
*   **框架有效性**：证明了即使在有限的算力（单 GPU）和简化数据下，通过统一的 RL 框架也能有效优化复杂的记忆策略。

### 7. 优点
*   **高度模块化**：通过原子化抽象，研究人员可以像搭积木一样更换检索器或更新策略。
*   **显存友好**：集成 GRPO 算法，解决了长上下文 RL 训练中显存极度紧张的问题。
*   **开箱即用**：预置了多种前沿算法实现，极大降低了记忆增强型智能体的研究门槛。
*   **标准化**：为碎片化的记忆研究提供了统一的接口和评估流程。

### 8. 不足与局限
*   **范式覆盖有限**：目前仅深入验证了 MemAgent 范式，对其他复杂记忆架构（如多级缓存、分层记忆）的覆盖有待加强。
*   **训练效率**：虽然支持单卡运行，但在处理极长序列时的训练吞吐量仍有优化空间。
*   **评估维度**：实验主要依赖于任务最终得分，对于记忆操作过程中的中间决策质量（如提取的准确率、更新的冗余度）缺乏更细粒度的消融分析。

（完）
