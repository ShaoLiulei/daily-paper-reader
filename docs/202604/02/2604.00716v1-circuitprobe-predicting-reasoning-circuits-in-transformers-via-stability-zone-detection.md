---
title: "CircuitProbe: Predicting Reasoning Circuits in Transformers via Stability Zone Detection"
title_zh: CircuitProbe：通过稳定性区域检测预测 Transformer 中的推理电路
authors: Rajkiran Panuganti
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00716v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 预测 Transformer 语言模型中的推理电路
tldr: 本研究针对 Transformer 中推理电路（重复特定层块以增强推理能力）搜索成本高昂的问题，提出了 CircuitProbe。该方法通过分析激活统计量（早期层的稳定性与后期层的量级异常），将搜索时间从 25 小时缩短至 5 分钟以内。实验涵盖 9 种模型架构，证明了其在预测最优电路位置上的准确性。研究还发现层重复技术对 3B 以下的小模型有显著提升，为轻量化模型性能优化提供了高效路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00716v1/fig-001.webp\", \"caption\": \"Figure 1: Stability zone detection across four architectures. Green bars show the detected stability zone (lowest derivative of representation change). In Phi-4 (top left), this aligns with the known ground-truth reasoning circuit at layers 6–11 (blue shading). The same pattern appears in all four models despite their different architectures, sizes, and training procedures.\", \"page\": 6, \"index\": 1, \"width\": 881, \"height\": 627}, {\"url\": \"assets/figures/arxiv/2604.00716v1/fig-002.webp\", \"caption\": \"Table 1: CircuitProbe predictions across 9 models. All show the same two-circuit pattern. “L” denotes total layer count. DS-R1 abbreviates DeepSeek-R1-Distill.\", \"page\": 7, \"index\": 2, \"width\": 560, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.00716v1/fig-003.webp\", \"caption\": \"Table 2: Scaling experiment across model sizes. Layer duplication consistently benefits models under 3B but degrades 7B+ models on broad benchmarks.\", \"page\": 8, \"index\": 3, \"width\": 664, \"height\": 215}]"
motivation: 现有的暴力搜索推理电路的方法计算成本极高，限制了通过层重复技术优化模型推理能力的实际应用。
method: 提出 CircuitProbe 算法，利用激活统计量中的表征变化导数和异常得分，分别检测早期稳定性电路和后期量级电路。
result: 在多种架构上实现了 3 到 4 个数量级的加速，且预测位置与最优电路高度吻合，同时发现该技术对 3B 以下模型效果显著。
conclusion: CircuitProbe 是一种高效、跨语言且低样本需求的电路预测工具，为提升小参数语言模型的推理能力提供了实用的扩展方案。
---

## 摘要
Transformer 语言模型包含局部推理电路，即在推理阶段进行复制时能够提升推理能力的连续层块。目前，寻找这些电路需要进行暴力扫描，每个模型需耗费 25 个 GPU 小时。我们提出了 CircuitProbe，它能在 5 分钟内通过 CPU 上的激活统计数据预测电路位置，实现了三到四个数量级的加速。我们发现推理电路分为两类：位于早期层、通过表示变化的导数检测到的稳定性电路，以及位于后期层、通过异常评分检测到的量级电路。我们在涵盖 6 种架构的 9 个模型（包括 2025 年的模型）上进行了验证，结果证实 CircuitProbe 的首选预测在所有验证案例中均与最优电路匹配，或误差在 2 层以内。针对 Qwen 2.5 系列的缩放实验表明，层复制能持续使 3B 参数以下的模型受益，但会降低 7B 以上模型的性能，这使其成为小型语言模型的一种实用缩放技术。CircuitProbe 仅需 10 个校准样本，且其预测在英语、印地语、中文和法语中均表现稳定。

## Abstract
Transformer language models contain localized reasoning circuits, contiguous layer blocks that improve reasoning when duplicated at inference time. Finding these circuits currently requires brute-force sweeps costing 25 GPU hours per model. We propose CircuitProbe, which predicts circuit locations from activation statistics in under 5 minutes on CPU, providing a speedup of three to four orders of magnitude. We find that reasoning circuits come in two types: stability circuits in early layers, detected through the derivative of representation change, and magnitude circuits in late layers, detected through anomaly scoring. We validate across 9 models spanning 6 architectures, including 2025 models, confirming that CircuitProbe top predictions match or are within 2 layers of the optimal circuit in all validated cases. A scaling experiment across the Qwen 2.5 family reveals that layer duplication consistently benefits models under 3B parameters but degrades performance in 7B+ models, making this a practical scaling technique for small language models. CircuitProbe requires as few as 10 calibration examples and its predictions are stable across English, Hindi, Chinese, and French.

---

## 论文详细总结（自动生成）

以下是对论文《CircuitProbe: Predicting Reasoning Circuits in Transformers via Stability Zone Detection》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：Transformer 模型中存在特定的“推理电路”（即连续的层块），在推理时通过“层手术”（Layer Surgery）重复执行这些层，可以显著提升模型的推理能力。然而，目前定位这些电路的方法依赖于**暴力搜索**（对每一组可能的层块进行修改并跑分测试），每个模型需要耗费 7-25 个 GPU 小时，成本极高且缺乏理论指导。
*   **研究动机**：作者旨在寻找一种无需运行下游任务评测、仅通过分析模型内部激活统计特性就能快速定位推理电路的方法，从而将搜索成本降低到实用水平。

### 2. 论文提出的方法论：CircuitProbe
*   **核心思想**：推理关键层在处理信息时会表现出独特的统计特征。研究发现推理电路分为两类：
    1.  **稳定性电路（早期层，10-25% 深度）**：模型表征从混乱转向稳定（结晶化）的过渡区。
    2.  **量级电路（后期层，85-100% 深度）**：表现为极大的表征变化和高输入依赖性。
*   **关键技术细节**：
    *   **数据采集**：使用极小规模（10-50个样本）的校准集进行一次前向传播，收集残差流的五种统计量：表征变化量、自相似性、范数增长比、跨样本方差、表征秩（Representation Rank）。
    *   **评分机制**：
        *   **稳定性得分 ($S_{stability}$)**：通过计算表征变化量的**导数**来捕捉“稳定性区域”，导数越小（变化越平稳）得分越高。
        *   **异常得分 ($S_{anomaly}$)**：通过 Z-score 检测后期层中变化量和方差异常大的块。
    *   **最终决策**：取两类得分归一化后的最大值，输出候选电路的排名。

### 3. 实验设计
*   **模型与架构**：涵盖 9 个模型、6 种架构，包括 Qwen2.5 (0.5B-7B)、Qwen3-8B、DeepSeek-R1-Distill (Qwen/Llama)、Mistral-7B、TinyLlama-1.1B、StableLM-2、Phi-3/4。
*   **数据集/Benchmark**：
    *   **校准集**：仅需 10-50 个任意文本样本。
    *   **验证集**：GSM8K（数学推理）、BBH（因果判断、逻辑推理、日期理解），共 250 个标准化问题。
*   **对比方法**：
    *   **Ground Truth**：通过暴力扫描（Brute-force sweep）获得的最优电路位置。
    *   **替代方案消融**：对比了单纯的异常检测、对比分析（推理 vs 普通文本）和边界检测方法。

### 4. 资源与算力
*   **CircuitProbe 耗时**：在 **CPU** 上运行（float16 精度）。0.5B 模型约 15 秒，14B 模型约 5 分钟。
*   **暴力搜索耗时**：每个模型需要 **7-25 个 GPU 小时**（取决于模型大小和扫描步长）。
*   **加速比**：CircuitProbe 实现了 **3 到 4 个数量级** 的加速。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 9 个不同年份、不同训练目标（含 RL 强化的 DeepSeek-R1）的模型上验证了预测准确性。
    *   进行了**缩放实验**（Scaling Experiment），观察层复制在不同参数量（0.5B 到 7B）下的表现。
    *   进行了**鲁棒性测试**，验证了校准样本数量、内容类型以及多语言（英、汉、印、法）对预测结果的影响。
*   **充分性评价**：实验设计较为全面，不仅验证了算法的快，还通过实际的“层手术”验证了预测位置的有效性。跨语言和跨架构的稳定性测试增强了结论的说服力。

### 6. 论文的主要结论与发现
*   **两类电路共存**：所有测试模型均存在早期稳定性电路和后期量级电路。
*   **小模型红利**：层复制技术对 **3B 参数以下** 的模型效果显著（提升 2%~10%），但对 7B 以上模型在综合任务上可能导致性能下降。
*   **架构决定论**：推理电路的位置由预训练架构决定。DeepSeek-R1 等经过强化学习的模型，其电路位置与对应的 Base 模型完全一致，说明 RL 只是利用了既有结构。
*   **多语言一致性**：推理电路是结构性的，不随输入语言的改变而漂移。

### 7. 优点
*   **极高效率**：将原本需要昂贵 GPU 集群数小时的工作缩短至普通笔记本 CPU 几分钟完成。
*   **低数据依赖**：仅需 10 个无标签样本即可实现稳定预测。
*   **实用性强**：为边缘侧部署的小模型提供了一种“免费”的性能提升手段（无需重新训练，仅增加约 15% 延迟）。
*   **理论洞察**：提出了“稳定性区域”这一新视角，解释了为何早期推理电路此前难以被量级指标检测到。

### 8. 不足与局限
*   **精度偏差**：预测位置虽然接近最优，但有时仍有 1-2 层的误差，可能需要微调。
*   **大模型负迁移**：在 7B 及以上模型中，层复制虽然提升了特定推理能力，但可能损害指令遵循或格式化能力。
*   **覆盖范围**：未测试 20B 以上的大规模模型，尚不清楚该规律在超大规模参数下是否依然成立。
*   **组合效应未知**：尚未研究同时复制早期和后期两个电路是否具有叠加效应。

（完）
