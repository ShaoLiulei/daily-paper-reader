---
title: "MoE-GRPO: Optimizing Mixture-of-Experts via Reinforcement Learning in Vision-Language Models"
title_zh: MoE-GRPO：通过视觉语言模型中的强化学习优化混合专家模型
authors: "Dohwan Ko, Jinyoung Park, Seoung Choi, Sanghyeok Lee, Seohyun Lee, Hyunwoo J. Kim"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24984v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于混合专家模型路由的强化学习
tldr: 针对视觉语言模型（VLM）中混合专家（MoE）架构存在的专家选择单一及过拟合问题，本文提出MoE-GRPO框架。该方法将专家路由建模为序列决策过程，利用群体相对策略优化（GRPO）算法，结合模态感知路由引导，通过强化学习探索更优的专家组合。实验证明，该方法在多模态图像和视频基准测试中显著提升了性能，实现了更具多样性和任务专业化的专家分配。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24984v2/fig-001.webp\", \"caption\": \"Figure 1. Comparison between top-K routing and our MoEGRPO. (a) While the top-K routing deterministically selects K experts based on gating scores, (b) MoE-GRPO stochastically samples K experts across multiple rollouts and optimizes the expert selection policy through reward-based feedback.\", \"page\": 1, \"index\": 1, \"width\": 491, \"height\": 414}, {\"url\": \"assets/figures/arxiv/2603.24984v2/fig-002.webp\", \"caption\": \"Figure 2. Overall pipeline of MoE-GRPO. Given an input image (or video) and a question, denoted as x, the rollout module gold samples G expert routing policies, i.e., {Ei}Gi=1 ∼ gold(E|x), where each policy Ei represents a sequence of expert selections across layers. Under each rollout Ei, the model generates an output token sequence yi, and a corresponding reward Ri is computed by the reward function. The relative reward of each rollout is evaluated within its group to derive the advantage value Âi, which guides the policy update toward higher-reward expert combinations. To jointly optimize token-level generation and layer-wise expert routing, the overall training objective of MoE-GRPO consists of two sub-objectives: Token-GRPO, which optimizes token-level generation quality, and Gate-GRPO, which refines layer-wise expert selection through the gating network.\", \"page\": 3, \"index\": 2, \"width\": 1036, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2603.24984v2/fig-003.webp\", \"caption\": \"Figure 6. A qualitative example and its routing probabilities. (a) illustrates the expert routing probabilities, with the selected experts highlighted in red boxes. (b) presents a qualitative example demonstrating that the learned expert selection policy of MoEGRPO yields a correct prediction, whereas the baseline Det-FT model produces an incorrect one.\", \"page\": 8, \"index\": 3, \"width\": 496, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2603.24984v2/fig-004.webp\", \"caption\": \"Table 1. Results on multi-modal understanding benchmarks. # activated and # total denote the number of activated and total parameters. The last column reports the average accuracy across all benchmarks, excluding MME.\", \"page\": 5, \"index\": 4, \"width\": 1026, \"height\": 325}]"
motivation: 传统的确定性Top-K路由机制容易导致专家选择陷入局部最优并引发过拟合，限制了MoE模型在多模态任务中的表达能力。
method: 提出基于强化学习的MoE-GRPO框架，通过GRPO算法优化路由策略，并引入模态感知引导以增强训练稳定性和效率。
result: 在多个图像和视频多模态基准测试中，MoE-GRPO均优于标准的Top-K路由及其变体，有效提升了专家选择的多样性。
conclusion: 通过强化学习优化MoE路由能有效缓解专家过拟合，并促进模型在任务层面的专家专业化协作。
---

## 摘要
混合专家模型（MoE）已成为降低 Transformer 架构计算开销的一种有效方法，它通过为每个 token 稀疏地激活参数子集，同时保持较高的模型容量。这一范式最近已被扩展到视觉语言模型（VLM），从而在降低计算成本的同时实现了可扩展的多模态理解。然而，广泛采用的确定性 top-K 路由机制可能会忽略更优的专家组合，并导致专家过拟合。为了解决这一局限性并提高专家选择的多样性，我们提出了 MoE-GRPO，这是一个基于强化学习（RL）的框架，用于优化基于 MoE 的 VLM 中的专家路由。具体而言，我们将专家选择建模为一个序列决策问题，并使用组相对策略优化（GRPO）对其进行优化，使模型能够通过探索和基于奖励的反馈学习自适应专家路由策略。此外，我们引入了一种模态感知路由引导，通过阻止路由器探索针对特定模态极少激活的专家，从而增强训练的稳定性和效率。在多模态图像和视频基准测试上的广泛实验表明，MoE-GRPO 通过促进更多样化的专家选择，始终优于标准的 top-K 路由及其变体，从而缓解了专家过拟合，并实现了任务级的专家专业化。

## Abstract
Mixture-of-Experts (MoE) has emerged as an effective approach to reduce the computational overhead of Transformer architectures by sparsely activating a subset of parameters for each token while preserving high model capacity. This paradigm has recently been extended to Vision-Language Models (VLMs), enabling scalable multi-modal understanding with reduced computational cost. However, the widely adopted deterministic top-K routing mechanism may overlook more optimal expert combinations and lead to expert overfitting. To address this limitation and improve the diversity of expert selection, we propose MoE-GRPO, a reinforcement learning (RL)-based framework for optimizing expert routing in MoE-based VLMs. Specifically, we formulate expert selection as a sequential decision-making problem and optimize it using Group Relative Policy Optimization (GRPO), allowing the model to learn adaptive expert routing policies through exploration and reward-based feedback. Furthermore, we introduce a modality-aware router guidance that enhances training stability and efficiency by discouraging the router from exploring experts that are infrequently activated for a given modality. Extensive experiments on multi-modal image and video benchmarks show that MoE-GRPO consistently outperforms standard top-K routing and its variants by promoting more diverse expert selection, thereby mitigating expert overfitting and enabling a task-level expert specialization.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MoE-GRPO** 的新型强化学习框架，旨在优化视觉语言模型（VLM）中混合专家（MoE）架构的路由策略。以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：虽然 MoE 架构通过稀疏激活参数显著提升了 Transformer 的计算效率，但主流的 **Top-K 确定性路由机制**存在局限性。这种“贪婪选择”限制了对专家组合的探索，容易导致模型过拟合到少数专家，忽略了潜在的最优组合。
*   **核心问题**：如何在多模态场景下，超越简单的启发式或确定性路由，学习一种能够自适应、多样化且针对任务专业化的专家选择策略？

### 2. 方法论：MoE-GRPO 框架
*   **核心思想**：将 MoE 的专家选择建模为一个**序列决策问题**，并利用**群体相对策略优化（GRPO）**算法进行训练。模型不再仅仅学习生成正确的 Token，还学习如何选择最优的专家路径。
*   **关键技术细节**：
    *   **双重优化目标**：
        1.  **Token-GRPO**：优化 Token 级别的生成质量，确保输出符合预期。
        2.  **Gate-GRPO**：直接优化每一层 gating 网络（路由器）的决策，通过奖励信号强化高回报的专家路径。
    *   **模态感知路由引导（Modality-Aware Router Guidance）**：为了缩小 RL 的搜索空间并提高稳定性，预先计算专家对视觉和文本模态的亲和力得分，在探索过程中屏蔽掉该模态下极少激活的专家（底部 25%），从而引导路由器进行更有效的探索。
    *   **奖励机制**：采用基于准确性的可验证奖励（正确为 1，错误为 0），通过组内相对优势（Advantage）将信号传播到每一层的路由决策中。

### 3. 实验设计
*   **模型基础**：将 InternVL3.5-1B 转换为 MoE 架构（8 选 2 专家），激活参数 1.3B，总参数 2.9B。
*   **数据集与 Benchmark**：
    *   **图像理解**：MME, MMBench, MMStar, MMT-Bench, AI2D。
    *   **视频理解**：VideoMME, MLVU, LongVideoBench, MVBench。
    *   **泛化性测试**：在 ImageNet 上训练，并在 10 个跨数据集（如 Caltech101, OxfordPets 等）和 4 个领域泛化数据集（ImageNet-V2/S/A/R）上评估。
*   **对比方法**：
    *   **确定性微调（Det-FT）**：标准的 Top-K 路由。
    *   **随机微调（Stoch-FT）**：包括多项式采样（V-MoE 变体）和高斯噪声扰动。
    *   **其他路由算法**：Expert Choice 路由、基于 Sinkhorn 的最优传输路由。

### 4. 资源与算力
*   **硬件设备**：使用了 **4 张 NVIDIA RTX Pro 6000 Blackwell Max-Q GPU**。
*   **训练配置**：
    *   训练步数：25,000 步。
    *   样本量：从 One-Thinker 中采样 100K 条多模态指令微调数据。
    *   训练时长：总训练时间约为 **1 天**。
    *   超参数：学习率 1e-6，Rollout 组大小 $G=8$。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 9 个主流多模态基准测试，并额外进行了跨数据集和领域泛化的深入评估。
*   **消融实验**：针对 Token-GRPO 与 Gate-GRPO 的必要性、模态感知引导的有效性、以及不同 RL 算法（DAPO, SAPO）的鲁棒性进行了详细对比。
*   **客观性**：实验对比了多种随机化基准和先进路由算法，并提供了路由分布的可视化分析（如 Jensen-Shannon 散度），证明了性能提升并非偶然，而是源于专家利用率的优化。

### 6. 主要结论与发现
*   **性能超越**：MoE-GRPO 在 7/9 的基准测试中优于所有基线，平均准确率比标准 Top-K 提升了约 2.0%。
*   **缓解过拟合**：在领域泛化实验中，传统 Det-FT 往往在分布外数据上性能下降，而 MoE-GRPO 保持了更强的鲁棒性。
*   **专家专业化**：分析显示，MoE-GRPO 显著增加了路由分布的熵（从 1.05 升至 1.82），并诱导出了明显的**任务级专家专业化**（不同任务倾向于调用不同的专家组合）。

### 7. 优点与亮点
*   **首创性**：首次将专家路由显式建模为序列决策问题并使用 RL（特别是 GRPO）进行端到端优化。
*   **训练效率**：引入模态感知引导，有效解决了 RL 在大规模参数空间中探索效率低下的痛点。
*   **即插即用**：该框架不改变推理时的计算量（依然是 Top-2 激活），仅通过训练策略的改进提升了模型上限。

### 8. 不足与局限
*   **奖励函数单一**：目前主要依赖二元准确性奖励（0/1），对于更复杂的开放式生成任务，可能需要更精细的奖励模型（Reward Model）。
*   **模型规模限制**：实验主要集中在 1B-3B 级别的小规模模型上，尚未在大规模（如 70B+）MoE 模型上验证其扩展性。
*   **训练开销**：虽然推理成本不变，但 RL 训练阶段需要多次 Rollout（$G=8$），相比传统监督微调（SFT）需要更多的显存和计算时间。

（完）
