---
title: "Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition"
title_zh: 通过联邦学习优化多语言大语言模型：客户端语言组成的研究
authors: "Aleix Sant, Jordi Luque, Carlos Escolano"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24242v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大模型的多语言指令微调与联邦学习
tldr: 本研究针对多语言环境下联邦学习（FL）优化大语言模型（LLM）时面临的语言分布不均和资源差异挑战，扩展了FederatedScope-LLM框架并提出LDES-FL动态早停机制。通过系统研究客户端语言组成对模型性能的影响，发现增加客户端内的语言多样性能显著提升全局模型的质量与公平性，尤其对低资源语言有显著增益，尽管这会带来更高的计算成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24242v2/fig-001.webp\", \"caption\": \"Table 4: Training cost normalized by the higheststep configuration, Local FT (multilingual), which requires 1.83× 105 total optimization steps. Each value is reported as a fraction of this reference.\", \"page\": 7, \"index\": 1, \"width\": 415, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2603.24242v2/fig-002.webp\", \"caption\": \"Table 3: Results for the multilingual test set of the server. R and Fb stand for ROUGE-L (Lin, 2004) and Fbert (Zhang et al., 2020) respectively. The language in columns corresponds to the monolingual part of the test set. Mean reports the average score across all languages, whereas σ denotes the corresponding standard deviation, used here as an indicator of multilingual fairness. A bootstrap test with 100 sets (p-value < 0.05) confirms that all federated models achieve statistically significant improvements over the Base Model on all evaluated metrics and languages, as indicated by an asterisk (*).\", \"page\": 7, \"index\": 2, \"width\": 943, \"height\": 250}]"
motivation: 旨在解决多语言联邦学习中由于客户端语言分布异构和资源不平等导致的大模型微调难题。
method: 扩展了FederatedScope-LLM框架，并引入一种允许客户端根据本地验证表现暂停或恢复训练的动态早停机制（LDES-FL）。
result: 实验表明，客户端内部语言越多样，全局模型的性能和公平性越好，且能有效缩小与中心化微调的差距。
conclusion: 客户端语言组成是多语言联邦学习的关键设计变量，决定了模型在性能、公平性与训练效率之间的权衡。
---

## 摘要
多语言环境下大语言模型（LLMs）的联邦学习（FL）面临着重大挑战，这些挑战源于客户端之间异构的语言分布以及语言资源可用性的差异。为了应对这些挑战，我们扩展了 FederatedScope-LLM 框架，以支持 LLMs 的多语言指令微调实验。我们还引入了一种新型的客户端特定早停机制——局部动态早停（LDES-FL），该机制允许客户端根据客户端侧的验证性能暂停和恢复局部训练，从而提高训练效率和可持续性。通过一系列实验，我们研究了客户端语言组成（从完全单语言到日益增加的多语言客户端）如何影响多语言质量、公平性和训练成本。研究发现，单语言局部微调对于单语言专业化仍然最为有效，而联邦训练则更适合学习单一的平衡多语言模型。在联邦学习中，增加客户端内部的多语言性会产生更强大且更公平的全局模型，缩小与集中式多语言微调的差距，并为低资源语言带来最大的收益，尽管代价是需要更多的优化步骤。总之，我们的研究结果表明，客户端语言组成是多语言联邦学习中的一个关键设计变量，它影响着性能、公平性和效率。

## Abstract
Federated Learning (FL) of Large Language Models (LLMs) in multilingual environments presents significant challenges stemming from heterogeneous language distributions across clients and disparities in language resource availability. To address these challenges, we extended the FederatedScope-LLM framework to support multilingual instruction-tuning experiments with LLMs. We also introduced a novel client-specific early stopping mechanism, Local Dynamic Early Stopping (LDES-FL), which allows clients to pause and resume local training based on client-side validation performance, enhancing training efficiency and sustainability. Through a series of experiments, we studied how client language composition - from fully monolingual to increasingly multilingual clients - affects multilingual quality, fairness and training cost. Monolingual local fine-tuning remains the most effective for single-language specialization, whereas federated training is better suited to learning a single balanced multilingual model. In FL, increasing within-client multilinguality leads to stronger and fairer global models, narrows the gap to centralized multilingual fine-tuning, and yields the largest gains for lower-resource languages, albeit at the cost of more optimization steps. Overall, our results identify client language composition as a key design variable in multilingual FL, shaping performance, fairness and efficiency.

---

## 论文详细总结（自动生成）

### 论文总结：通过联邦学习优化多语言大语言模型

#### 1. 论文的核心问题与整体含义
本文探讨了在**联邦学习（FL）**环境下，如何通过微调**大语言模型（LLM）**来应对多语言场景中的挑战。核心问题在于：客户端之间的数据往往是高度异构的（Non-IID），即每个客户端可能只拥有特定语言的数据，这会导致“客户端漂移（Client Drift）”，损害全局模型的收敛和泛化。研究重点分析了**“客户端语言组成”**（即每个客户端内部包含多少种语言）这一变量对模型性能、公平性及训练效率的影响。

#### 2. 论文提出的方法论
*   **核心思想**：通过调整客户端内部的语言多样性，减轻非独立同分布（Non-IID）带来的负面影响，并引入动态早停机制提升训练效率。
*   **关键技术细节**：
    *   **框架扩展**：基于 `FederatedScope-LLM` 进行了多语言适配，支持语言感知的样本处理和灵活的 Prompt 集成。
    *   **LDES-FL（局部动态早停）**：这是一种新型算法。不同于传统的全局早停，LDES-FL 允许客户端根据其私有验证集的损失函数自主决定暂停或恢复训练。如果全局模型在某轮更新后改善了该客户端的本地表现，已停止的客户端可以“重新加入”训练。
    *   **参数高效微调（PEFT）**：采用 **LoRA** 技术，仅训练约 1%-2% 的参数，以降低通信开销和计算需求。
*   **算法流程**：服务器分发全局 LoRA 参数 $\rightarrow$ 客户端进行本地训练 $\rightarrow$ 服务器聚合更新（FedAvg） $\rightarrow$ 客户端通过 LDES-FL 监控本地验证集并反馈状态。

#### 3. 实验设计
*   **数据集**：使用 **Alpaca Cleaned** 的多语言版本，涵盖 8 种语言（英语、西班牙语、德语、加泰罗尼亚语、丹麦语、塞尔维亚语、克罗地亚语、巴斯克语）。
*   **实验场景（Benchmark）**：
    *   **100% Mono**：每个客户端仅含一种语言（极端 Non-IID）。
    *   **混合比例**：设置了 85%、70%、50%、30%、15% Mono 等梯度，逐渐增加客户端内的语言多样性（趋向 IID）。
*   **对比方法**：
    *   **Base Model**：原始的 `salamandra-2b-instruct` 模型。
    *   **Local FT (lang)**：在单一语言上进行中心化本地微调。
    *   **Local FT (multilingual)**：在所有语言的并集上进行中心化微调（作为性能上限）。
    *   **FedAvg**：在不同语言组成配置下的联邦学习表现。
*   **评估指标**：ROUGE-L 和 BERTScore ($F_{bert}$)，并以标准差 ($\sigma$) 衡量多语言公平性。

#### 4. 资源与算力
*   **模型规模**：使用 `salamandra-2b-instruct`（约 20 亿参数）。
*   **训练细节**：本地微批次大小为 2，梯度累积 16 步，有效批次大小为 32。使用 OneBitAdam 优化器。
*   **算力说明**：论文**未明确列出具体的 GPU 型号和数量**。但通过“归一化优化步数”对比了成本：中心化多语言微调成本最高（$1.83 \times 10^5$ 步），而 100% Mono 的联邦学习成本仅为其 6.1%。

#### 5. 实验数量与充分性
*   **实验规模**：针对 6 种不同的客户端语言组成比例进行了完整的联邦学习实验，并对 8 种语言分别做了本地微调对比。
*   **充分性与客观性**：
    *   实验设计了严谨的对照组（中心化 vs 联邦，单语言 vs 多语言）。
    *   使用了 **Bootstrap test**（100 组采样，p-value < 0.05）进行显著性检验，确保结果不是偶然产生的。
    *   控制了每个客户端的总样本量一致，排除了数据量差异的干扰，实验设计较为公平。

#### 6. 主要结论与发现
*   **语言组成的关键性**：客户端内部的语言越多样（即越接近 IID），全局模型的平均性能越好，且多语言之间的表现越均衡（公平性更高）。
*   **低资源语言获益最大**：增加客户端的多语言组成对低资源语言（如塞尔维亚语、巴斯克语）的提升最为显著（ROUGE-L 提升高达 16.11%）。
*   **性能与成本权衡**：虽然多语言客户端能带来更好的模型，但由于模型在更多轮次中持续改进，其收敛所需的优化步数也更多（15% Mono 的成本是 100% Mono 的 6.8 倍）。
*   **联邦学习的优势**：相比于为每种语言单独训练和维护本地模型，联邦学习能以更低的总计算成本（约 1/6 到 1/43）获得一个高质量的多语言通用模型。

#### 7. 优点
*   **LDES-FL 机制**：解决了联邦学习中客户端训练冗余的问题，支持动态退出和重新加入，非常符合实际应用中客户端资源受限的场景。
*   **系统性分析**：首次深入探讨了“客户端内部语言比例”这一细粒度变量，为多语言 FL 的系统设计提供了理论依据。
*   **公平性视角**：不仅关注平均分，还通过标准差分析了模型在不同资源等级语言之间的公平性。

#### 8. 不足与局限
*   **数据偏差风险**：实验数据源于机器翻译（Alpaca 翻译版），可能存在翻译质量不一或文化偏见问题。
*   **数据重叠问题**：论文提到多语言 split 并非严格平行，某些翻译实例可能在不同客户端的训练/验证集中交叉出现，这可能导致对跨语言迁移能力的评估存在偏差。
*   **算力信息缺失**：未提供具体的硬件环境和实际训练时长，不便于其他研究者评估复现所需的物理成本。
*   **语言覆盖有限**：虽然涵盖了 8 种语言，但主要集中在欧洲语系，对于语系差异更大的语言（如中、日、韩、阿拉伯语）的泛化性尚待验证。

（完）
