---
title: "QUEST: A robust attention formulation using query-modulated spherical attention"
title_zh: QUEST：一种使用查询调制球面注意力的鲁棒注意力机制
authors: "Hariprasath Govindarajan, Per Sidén, Jacob Roll, Fredrik Lindsten"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00199v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: Transformer模型的新注意力机制公式
tldr: Transformer的核心是注意力机制，但Query和Key的范数增长会导致训练不稳定。本文提出QUEST，通过将Key约束在超球面上并利用Query调节注意力分布的锐度，解决了这一问题。QUEST作为即插即用的替代方案，在视觉等多个领域表现出更强的训练稳定性、更高的性能以及对数据损坏和对抗攻击的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00199v1/fig-001.webp\", \"caption\": \"Table 1: Ablation of different QK normalization methods for ImageNet classification (ViT-Tiny model trained using DeiT for 300 epochs) [†Liu et al. (2022), ‡Dehghani et al. (2023)]. The Top-1 validation accuracies for ImageNet are the means over 3 independent training runs. The detailed statistics of these results are reported in Table A1.\", \"page\": 6, \"index\": 1, \"width\": 799, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2604.00199v1/fig-002.webp\", \"caption\": \"Figure 1: Class-activation maps for an image from the Macaw class in ImageNet, generated using AG-CAM (Leem & Seo, 2024). Standard attention concentrates on few bird instances (see first row) and mis-classifies the image if the region containing those instances is noised (see third row). This indicates that the birds in the bottom half of the image do not contribute to the correct prediction in standard attention. Hence, when the top part of the image is noised, the model focuses on the birds in the bottom part of the image since they are the most salient object in the image then, but results in a misclassification. QUEST attention attends evenly to different bird instances and classifies the image correctly even if some of the bird instances are noised. A more diverse attention can make the models more robust to input data variations, which can be observed in the improved model robustness in 4.1.2.\", \"page\": 2, \"index\": 2, \"width\": 450, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2604.00199v1/fig-003.webp\", \"caption\": \"Table 2: ImageNet Classification using DeiT and DeiT-3 training recipes [‡Dehghani et al. (2023)]. For longer trainings with larger ViT models (denoted as †), the batch size and input image resolution in the first phase of training are changed compared to Touvron et al. (2022) in order to train on a limited compute infrastructure, see A.5.1 for details.\", \"page\": 7, \"index\": 3, \"width\": 790, \"height\": 466}, {\"url\": \"assets/figures/arxiv/2604.00199v1/fig-004.webp\", \"caption\": \"Table 10: Language Modeling on WikiText-103\", \"page\": 10, \"index\": 4, \"width\": 801, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2604.00199v1/fig-005.webp\", \"caption\": \"Table A13: Self-supervised pre-training on ImageNet with iBOT and evaluating on transfer tasks\", \"page\": 28, \"index\": 5, \"width\": 790, \"height\": 203}, {\"url\": \"assets/figures/arxiv/2604.00199v1/fig-006.webp\", \"caption\": \"Table 4: Robustness to adversarial attacks using ViT-Ti/16 model trained using DeiT\", \"page\": 8, \"index\": 6, \"width\": 720, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.00199v1/fig-007.webp\", \"caption\": \"Figure 3: Success rates of learning the correct solution to the toy example. Models are trained with different hyperparameter combinations with 5 different weight initializations and 5 different realizations of the data. The QKNorm methods obtained ∼0% overall success rate and their results are available in Figure A1.\", \"page\": 4, \"index\": 7, \"width\": 752, \"height\": 216}, {\"url\": \"assets/figures/arxiv/2604.00199v1/fig-008.webp\", \"caption\": \"Table A11: GPS hyperparameter setup for long-range graph benchmarks (Dwivedi et al., 2022)\", \"page\": 27, \"index\": 8, \"width\": 771, \"height\": 520}, {\"url\": \"assets/figures/arxiv/2604.00199v1/fig-009.webp\", \"caption\": \"Table 7: UEA Multivariate Time Series Classification using Transformers\", \"page\": 9, \"index\": 9, \"width\": 409, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.00199v1/fig-010.webp\", \"caption\": \"Figure 4: Norms of answer key tokens for biased and unbiased samples: A common failure case for standard and QNorm attention involves the key norms of the biased answer token increasing as the training progresses. The model relies on looking up the bias vector to identify the answer.\", \"page\": 5, \"index\": 10, \"width\": 827, \"height\": 273}]"
motivation: 标准注意力机制中Query和Key的范数不受控增长会导致训练不稳定，且容易受到数据中伪模式的影响。
method: 提出QUEST方法，将Key向量约束在超球面潜空间，同时允许Query动态调节注意力分布的锐度。
result: QUEST在消除训练不稳定的同时提升了模型性能，并显著增强了模型对数据损坏和对抗攻击的鲁棒性。
conclusion: QUEST是一种稳健、高效且通用的注意力机制改进方案，可作为标准注意力的即插即用替代品。
---

## 摘要
Transformer 模型架构已成为深度学习中最广泛使用的架构之一，而注意力机制是其核心。标准注意力公式对查询（query）和键（key）向量之间的缩放点积应用 softmax 操作。我们探讨了查询和键的范数所起的作用，当这些范数任意增大时，可能会导致训练不稳定。我们证明了即使在简单的 Transformer 模型中，当数据中存在易于学习的伪模式（spurious patterns）时，也会发生这种情况。我们提出了一种新的注意力公式——查询调制球面注意力（QUEST），它将键约束在超球面潜空间中，同时仍允许单个标记（token）灵活地控制注意力分布的锐度。QUEST 可以作为标准注意力的直接替代方案轻松使用。我们专注于视觉应用，同时也探索了其他领域，以突出该方法的通用性。我们表明：(1) QUEST 在训练过程中没有不稳定性；(2) 产生的模型性能有所提高；(3) 对数据损坏和对抗性攻击具有鲁棒性。

## Abstract
The Transformer model architecture has become one of the most widely used in deep learning and the attention mechanism is at its core. The standard attention formulation uses a softmax operation applied to a scaled dot product between query and key vectors. We explore the role played by norms of the queries and keys, which can cause training instabilities when they arbitrarily increase. We demonstrate how this can happen even in simple Transformer models, in the presence of easy-to-learn spurious patterns in the data. We propose a new attention formulation, QUEry-modulated Spherical aTtention (QUEST), that constrains the keys to a hyperspherical latent space, while still allowing individual tokens to flexibly control the sharpness of the attention distribution. QUEST can be easily used as a drop-in replacement for standard attention. We focus on vision applications while also exploring other domains to highlight the method's generality. We show that (1) QUEST trains without instabilities and (2) produces models with improved performance (3) that are robust to data corruptions and adversarial attacks.

---

## 论文详细总结（自动生成）

### 论文总结：QUEST——基于查询调制球面注意力的鲁棒注意力机制

#### 1. 核心问题与整体含义（研究动机和背景）
Transformer 的核心是**缩放点积注意力机制（SDPA）**。然而，研究发现查询（Query, Q）和键（Key, K）向量的范数（Norm）在训练过程中可能会无限制增长，导致注意力 Logit 爆炸，进而引发训练不稳定或注意力坍缩（即模型只关注极少数 token）。
*   **动机**：现有的稳定化方法（如 QKNorm）虽然能解决稳定性问题，但往往通过强制归一化 Q 和 K 来限制表达能力，导致小模型性能下降。
*   **背景**：作者通过一个“伪模式”玩具实验证明，标准注意力容易被数据中简单但非鲁棒的特征（Spurious patterns）误导，导致 Key 范数异常增大以“窃取”全局注意力。

#### 2. 方法论：核心思想与技术细节
论文提出了 **QUEST (QUEry-modulated Spherical aTtention)**，其核心公式为：
$$A = \text{softmax}(Q \bar{K}^T)$$
其中 $\bar{K}$ 是经过 $\ell_2$ 归一化后的键向量（即被约束在超球面上）。
*   **核心思想**：
    *   **Key 归一化**：防止单个 token 通过增大范数来强行获取全局关注，确保注意力竞争基于向量方向（余弦相似度）。
    *   **Query 保留范数**：允许每个查询 token 独立控制其 Softmax 分布的“锐度”（Sharpness）。范数大的 Query 产生尖锐的注意力，范数小的则产生平滑的注意力。
*   **算法流程**：QUEST 移除了传统的 $1/\sqrt{d}$ 缩放因子，直接计算未归一化的 $Q$ 与归一化后的 $K$ 的点积，随后进行 Softmax。它是一个**即插即用**的模块，无需改变模型架构。

#### 3. 实验设计
作者在多个领域进行了广泛验证：
*   **计算机视觉**：使用 ViT (Tiny, Small, Base, Large, Huge, 2B) 架构，在 ImageNet-1K 上进行分类，并在 ADE20K 上进行语义分割。
*   **语言建模**：在 WikiText-103 数据集上测试 Transformer-Small/Medium 和 Transformer-XL。
*   **其他领域**：包括 UEA 多变量时间序列分类、GraphGPS 图神经网络基准、以及 PointTransformer-V3 点云分割。
*   **对比方法（Benchmark）**：标准注意力（Standard）、QKNorm-DS（维度缩放）、QKNorm-HS（头缩放）、QNorm（仅归一化查询）以及 Elliptical Attention。
*   **鲁棒性评估**：使用 ImageNet-C（损坏）、ImageNet-A（对抗样本）以及多种对抗攻击（FGSM, PGD, AutoAttack）。

#### 4. 资源与算力
论文明确提到了实验使用的硬件资源：
*   **GPU 型号**：主要使用 **NVIDIA A100 (40GB)**。
*   **算力分配**：
    *   ViT-Base 训练使用 4 张 A100。
    *   CrossViT 和 iBOT 预训练使用 8 张 A100。
    *   点云分割和部分小模型实验在单张 A100 上完成。
*   **训练时长**：涵盖了从 100 epoch 到 400+20 epoch（DeiT-3 方案）的不同周期。

#### 5. 实验数量与充分性
实验设计非常**充分且客观**：
*   **多维度验证**：不仅关注准确率，还深入分析了训练稳定性（Logit 变化曲线）、可解释性（AG-CAM 激活图）和对抗鲁棒性。
*   **消融实验**：对比了 Q 和 K 各种归一化组合（如仅归一化 Q、同时归一化 QK 等），证明了“仅归一化 K”的优越性。
*   **统计显著性**：部分实验（如 ViT-Tiny）进行了 3 次独立运行并报告了均值和标准差。
*   **模型规模**：实验覆盖了从 5M 参数到 **2.4B 参数**（ViT-2B）的巨大跨度，验证了方法的扩展性。

#### 6. 主要结论与发现
*   **训练稳定性**：QUEST 有效防止了 Logit 爆炸。在标准注意力导致训练崩溃的场景下（如大模型、高学习率），QUEST 依然稳定。
*   **性能提升**：在视觉、语言、图和时间序列任务中，QUEST 普遍优于标准注意力和 QKNorm。
*   **鲁棒性增强**：QUEST 模型在面对图像损坏和对抗攻击时表现更稳健。
*   **注意力分布**：可解释性分析显示，QUEST 产生的注意力图更均匀地覆盖目标物体，而不是像标准注意力那样只锁定在局部极小区域。

#### 7. 优点
*   **简洁高效**：实现极其简单（仅需几行代码修改），且不增加额外的计算参数。
*   **理论支撑**：通过梯度更新公式（Reverse Attention）解释了 QK 范数互锁增长的原理，并证明了归一化 K 能打破这种恶性循环。
*   **通用性强**：在视觉、文本、图、点云等多种模态下均表现出一致的改进。

#### 8. 不足与局限
*   **语言模型深度不足**：虽然在 WikiText-103 上有测试，但尚未在超大规模预训练语言模型（如 Llama 级别的 LLM）上进行长文本或大规模指令微调的验证。
*   **线性注意力扩展**：论文主要讨论 Softmax 注意力，虽然提到了线性注意力的潜力，但未给出具体的实验数据。
*   **几何优化探索**：作者提到未来可以结合黎曼梯度下降等几何优化方法，目前 QUEST 仍使用标准的 AdamW 等优化器。

（完）
