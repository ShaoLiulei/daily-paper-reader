---
title: Large Language Model as Token Compressor and Decompressor
title_zh: 大语言模型作为 Token 压缩器与解压器
authors: "Wenbing Li, Zikai Song, Jielei Zhang, Tianhao Zhao, Junkai Lin, Yiran Wang, Wei Yang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25340v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 使用LoRA微调将大模型作为Token压缩器
tldr: 本研究提出将预训练大语言模型（LLM）作为高效的Token压缩器与解压器。通过自表达自动编码学习框架，利用轻量化LoRA适配器将长文本转换为离散且变长的潜在编码（Z-tokens），并能精确重建原文本。该方法实现了内容自适应压缩，在保持高保真度的同时，在多个数据集上达到最高18倍的压缩率，为长上下文推理和提示词压缩提供了高效的新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25340v1/fig-001.webp\", \"caption\": \"Figure 3. BLEU-4 at different input length and compression ratio. The solid line represents our method, and the dashed line represents ICAE.\", \"page\": 7, \"index\": 1, \"width\": 496, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2603.25340v1/fig-002.webp\", \"caption\": \"Figure 2. Here we demonstrat the flexible use of Z-tokens. The left side shows that the same sentence can be compressed into different Z-tokens but decompressed to represent the same meaning. The right side shows, from three perspectives, that the same Z-tokens can be decoded into different content in different contexts. This illustrates that Z-tokens are controllable and interpretable, rather than chaotic.\", \"page\": 6, \"index\": 2, \"width\": 948, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2603.25340v1/fig-003.webp\", \"caption\": \"Figure 1. Our framework consists of three components: a compressor, an inference module, and a decompressor. It supports two usage paradigms. In the first, we directly feed the compressed representation to the decompressor to perform the downstream task. In the second, we first run the inference module in the Z-token space and then invoke the decompressor to recover task outputs at the token level.\", \"page\": 3, \"index\": 3, \"width\": 1025, \"height\": 378}]"
motivation: 旨在探索如何利用现有的预训练大语言模型实现高效的长文本Token压缩，以解决长上下文处理中的计算开销问题。
method: 提出一种自表达自动编码框架，通过LoRA微调LLM将文本映射为内容自适应的离散变长潜在编码（Z-tokens）。
result: 在Wikipedia和CNN/DailyMail等多个数据集上实现了高达18倍的Token减量，且能保持极高的重建精度和下游任务性能。
conclusion: 该设计证明了LLM在压缩空间的自回归生成潜力，为实现Token高效的长上下文推理提供了简单有效的方案。
---

## 摘要
在本文中，我们提出了一个新颖的见解：现成的大语言模型（LLM）可以作为出色的 Token 压缩器和解压器。为了证明这一点，我们设计了一个自表达自动编码学习框架，通过微调预训练 LLM，将长文本转换为由离散、变长潜码（称为 Z-token）组成的紧凑内部语言，并能从中精确重建原始文本。所生成的表示具有内容自适应性：通过轻量级的基于 LoRA 的适配器头，语义密集的片段会分配到更多 Z-token，而冗余或可预测的区域则会被大幅压缩。实验表明，我们的方法在 Wikipedia、CNN/DailyMail、HotpotQA 和 Qulac 风格的长查询数据集上实现了高达 18 倍的 Token 缩减，同时保持了重建保真度和下游任务性能。这种简单而有效的设计支持包括提示词压缩和直接在 Z-token 空间进行自回归生成在内的多种应用，为实现 Token 高效的长上下文推理提供了一条潜在路径。

## Abstract
In this paper, we establish the novel insight that an off-the-shelf LLM can function as an excellent token compressor and decompressor. To demonstrate, we design a self-expressive autoencoding learning framework fine-tunes a pretrained LLM to translate long texts into a compact internal language of discrete, variable-length latent codes, termed Z-tokens, and to reconstruct the original text exactly from them. The resulting representation is content-adaptive: semantically dense segments receive more Z-tokens, while redundant or predictable regions are aggressively compressed, via lightweight LoRA-based adapter heads. Empirically, our method achieves up to 18 times token reduction on Wikipedia, CNN/DailyMail, HotpotQA, and Qulac-style long-query datasets, while preserving reconstruction fidelity and downstream performance. This simple yet effective design supports applications including prompt compression and autoregressive generation directly in the Z-token space, offering a potential pathway toward token-efficient long-context reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《Large Language Model as Token Compressor and Decompressor》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）在处理长上下文时面临注意力机制平方级复杂度的瓶颈，导致计算和内存开销巨大。
*   **研究动机**：现有的 Token 压缩方法（如启发式合并或固定长度嵌入）存在局限性：要么容易丢失关键语义，要么生成的连续向量不可解释且缺乏灵活性。
*   **核心见解**：LLM 本身具备强大的语义抽象能力。论文提出将预训练 LLM 转化为“压缩器”和“解压器”，通过一种内部的离散语言（Z-tokens）来实现高效、变长且可恢复的文本压缩。

### 2. 论文提出的方法论
*   **核心思想**：构建一个自表达的自动编码框架。压缩器将原始文本 $X$ 翻译成离散的、变长的潜在编码 $Z$（Z-tokens），解压器则负责从 $Z$ 中还原文本或直接进行下游推理。
*   **关键技术细节**：
    *   **Z-token 空间**：扩展原有的词表，引入专门用于内部表示的离散 Token。
    *   **自回归压缩**：压缩器以自回归方式生成 Z-tokens，允许根据内容密度动态调整压缩长度（语义复杂处多分配，冗余处少分配）。
    *   **离散化训练**：使用 **Gumbel-Softmax** 估计器和直通估计器（Pass-through estimator）解决离散采样不可导的问题，实现端到端训练。
    *   **滑动窗口压缩**：针对超长文本，采用带重叠的滑动窗口，并通过一致性约束（Loverlap）确保语义衔接。
    *   **双路径解压**：
        1.  **作为推理器**：直接从 Z-tokens 生成答案（如摘要、QA）。
        2.  **作为重建器**：先还原成原始文本，或在 Z-token 空间进行纯符号推理后再还原。
    *   **计划采样（Schedule Sampling）**：在训练中逐步引入模型自身生成的 Token，以缓解自回归解码中的暴露偏差（Exposure Bias）。

### 3. 实验设计
*   **数据集**：
    *   **重建与续写**：Wikipedia。
    *   **长文本问答（Long QA）**：HotpotQA、QuALITY、NarrativeQA、QASPER。
    *   **摘要生成**：CNN/DailyMail。
*   **Benchmark 与对比方法**：
    *   对比了 **ICAE**（上下文自动编码器）、**Gist-Token**、**AutoCompressor** 和 **LLOCO** 等主流学习型压缩方法。
    *   基准模型采用 Qwen3 系列（0.4B, 1.7B, 4B）。
*   **评估指标**：BLEU-4、PPL（困惑度）、Rouge-L、F1 分数及推理耗时/显存占用。

### 4. 资源与算力
*   **硬件环境**：使用了 6 台 NVIDIA GPU，具体包括 **2 张 A800-80G** 和 **4 张 V100-32G**。
*   **软件环境**：Ubuntu 22.04, PyTorch 2.5.1, CUDA 12.4。
*   **训练细节**：使用了 LoRA 微调技术（秩 r=128），针对 Qwen3 不同参数规模的模型进行了适配。文中未明确给出总训练时长，但提到了推理速度的提升数据。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础的文本重建到复杂的多步推理 QA，共 6 个大型数据集。
*   **消融实验**：
    *   验证了 **计划采样** 对自由解码性能的提升。
    *   研究了 **Z-token 数量**（1k 到 10k）对性能的影响，找出了压缩率与准确率的平衡点。
    *   对比了不同模型规模（0.6B 到 4B）下的表现。
*   **客观性**：每组实验独立重复 3 次取平均值，对比基准涵盖了近年来的 SOTA 方法，实验设计较为客观公平。

### 6. 主要结论与发现
*   **高效压缩**：在保持高性能的同时，实现了最高 **18 倍** 的 Token 缩减。
*   **性能领先**：在所有测试的长文本 QA 和摘要任务中，该方法均优于现有的 ICAE 和 AutoCompressor 等基准。
*   **推理加速**：推理速度提升了 **4.6 倍**（在特定配置下为 2.1x-3.4x），并显著降低了显存占用。
*   **Z-token 的多义性**：发现 Z-tokens 具有类似自然语言的“多义性”，即同一个 Z-token 在不同上下文中代表不同含义，这种特性使其能以极高的密度编码语义。

### 7. 优点
*   **内容自适应性**：不同于固定长度压缩，变长压缩更符合语言本身的逻辑。
*   **高保真度**：通过 LLM 自身的语义先验，实现了极高的重建精度（BLEU-4 接近完美）。
*   **可解释性与灵活性**：离散的 Z-tokens 使得压缩过程更具透明度，且支持直接在压缩空间进行推理。
*   **轻量化**：基于 LoRA 的微调方式使得该方法易于迁移到不同的预训练模型上。

### 8. 不足与局限
*   **训练依赖**：虽然使用了 LoRA，但仍需要针对特定任务或领域进行微调，并非完全的“即插即用”。
*   **长程依赖协调**：在滑动窗口模式下，如何更好地处理跨窗口的全局长程依赖仍有优化空间。
*   **任务感知能力**：目前的压缩过程是通用的，未来若能根据下游任务（如只关注事实或只关注逻辑）进行任务感知型压缩，效果可能更佳。
*   **暴露偏差**：尽管引入了计划采样，但在极长序列的自由解码中，误差累积风险依然存在。

（完）
