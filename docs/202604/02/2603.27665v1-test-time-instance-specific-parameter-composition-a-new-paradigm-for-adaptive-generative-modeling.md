---
title: "Test-Time Instance-Specific Parameter Composition: A New Paradigm for Adaptive Generative Modeling"
title_zh: 测试时实例特定参数组合：一种自适应生成建模的新范式
authors: "Minh-Tuan Tran, Xuan-May Le, Quan Hung Tran, Mehrtash Harandi, Dinh Phung, Trung Le"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27665v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 通过参数组合实现的自适应生成模型
tldr: 现有的生成模型（如扩散模型和自回归网络）通常使用固定的预训练参数，缺乏对不同输入的灵活性。受人类感知适应能力的启发，本文提出了 Composer 范式。该方法在推理阶段根据输入动态生成特定实例的参数调整，并将其注入预训练模型中，无需微调或重新训练。Composer 在多步生成前仅需一次适配，能以极低的计算和内存开销显著提升生成质量和上下文感知能力，为动态生成模型设计开辟了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27665v1/fig-001.webp\", \"caption\": \"Figure 1. Comparison of static versus adaptive parameterization. Composer dynamically composes instance-specific parameter updates, allowing per-input adaptation without fine-tuning.\", \"page\": 1, \"index\": 1, \"width\": 480, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2603.27665v1/fig-002.webp\", \"caption\": \"Table 2. Comparison of different generative models and methods in class-conditional image generation on ImageNet 256× 256. Inference time shows absolute and percentage increase relative to Standard.\", \"page\": 6, \"index\": 2, \"width\": 718, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2603.27665v1/fig-003.webp\", \"caption\": \"Figure 5. Qualitative comparison on additional text-to-image examples. For each prompt, we show images produced by baseline methods and by our approach.\", \"page\": 11, \"index\": 3, \"width\": 1022, \"height\": 834}, {\"url\": \"assets/figures/arxiv/2603.27665v1/fig-004.webp\", \"caption\": \"Figure 2. Overview of Composer. Given any weight matrix W from the backbone, Composer generates a low-rank update W ′ = W + AB conditioned on the input. Specifically, the query and value matrices WQ and WV from the pretrained model are linearly projected from Rd×d to R2r×dmodel . The projected representations are then separated to initialize tokens A0 i and B0 i ∈ R1×dmodel . During training, these tokens are combined with prompt tokens Pi ∈ R1×dmodel and processed by a transformer to produce W ∗ = AB. The adapted parameters W ′ = W +W ∗ are used for generation. At inference, the first projection layers are removed, while A0 i and B0 i are stored for fast instance-specific adaptation.\", \"page\": 2, \"index\": 4, \"width\": 493, \"height\": 642}, {\"url\": \"assets/figures/arxiv/2603.27665v1/fig-005.webp\", \"caption\": \"Table 6. Comparison of SD2.1 variants on COCO 2014 (30K samples). Metrics include FID, CLIP similarity, inference time (s), and peak GPU memory.\", \"page\": 7, \"index\": 5, \"width\": 446, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2603.27665v1/fig-006.webp\", \"caption\": \"Figure 3. Illustration of the attention scheme. Component tokens attend to prompt tokens for context, maintain local block-wise attention, and the first token of each block captures inter-block correlations.\", \"page\": 4, \"index\": 6, \"width\": 375, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2603.27665v1/fig-007.webp\", \"caption\": \"Table 13. Ablation on modified attention layers for VAR d-16 and VAR d-24 on ImageNet 256× 256.\", \"page\": 12, \"index\": 7, \"width\": 487, \"height\": 258}]"
motivation: 传统生成模型采用静态参数处理所有输入，无法像人类一样根据特定语境灵活调整内部表示。
method: 提出 Composer 范式，在推理时根据输入生成特定参数适配并注入预训练权重，实现无需微调的实例级专业化。
result: 实验证明该方法在多种生成模型（包括轻量化和量化模型）上均显著提升了性能，且计算开销极小。
conclusion: Composer 通过输入感知的参数组合，打破了静态参数化的限制，确立了自适应生成建模的新范式。
---

## 摘要
现有的生成模型（如扩散模型和自回归网络）本质上是静态的，依赖于一组固定的预训练参数来处理所有输入。相比之下，人类能够根据每个感知或想象的情境灵活地调整其内部生成表征。受此能力的启发，我们提出了 Composer，这是一种基于测试时实例特定参数组合的自适应生成建模新范式。Composer 在推理时生成以输入为条件的参数自适应，并将其注入预训练模型的权重中，从而在无需微调或重新训练的情况下实现针对每个输入的专门化。自适应在多步生成之前发生一次，能够以极小的计算和内存开销产生更高质量、上下文感知的输出。实验表明，Composer 在多种生成模型和用例中显著提升了性能，包括轻量级/量化模型和测试时缩放。通过利用输入感知的参数组合，Composer 为设计能够动态适应每个输入的生成模型建立了一种新范式，超越了静态参数化。

## Abstract
Existing generative models, such as diffusion and auto-regressive networks, are inherently static, relying on a fixed set of pretrained parameters to handle all inputs. In contrast, humans flexibly adapt their internal generative representations to each perceptual or imaginative context. Inspired by this capability, we introduce Composer, a new paradigm for adaptive generative modeling based on test-time instance-specific parameter composition. Composer generates input-conditioned parameter adaptations at inference time, which are injected into the pretrained model's weights, enabling per-input specialization without fine-tuning or retraining. Adaptation occurs once prior to multi-step generation, yielding higher-quality, context-aware outputs with minimal computational and memory overhead. Experiments show that Composer substantially improves performance across diverse generative models and use cases, including lightweight/quantized models and test-time scaling. By leveraging input-aware parameter composition, Composer establishes a new paradigm for designing generative models that dynamically adapt to each input, moving beyond static parameterization.

---

## 论文详细总结（自动生成）

### 论文总结：Test-Time Instance-Specific Parameter Composition (Composer)

#### 1. 核心问题与整体含义（研究动机和背景）
现有的生成模型（如扩散模型和视觉自回归模型）在预训练完成后，其参数是**静态固定**的。这意味着无论输入什么样的提示词（Prompt）或场景，模型都使用同一套权重进行处理。这种“一刀切”的方法限制了模型对复杂、模糊或特定语境的适应能力，导致生成结果有时会出现平滑过度或逻辑不一致。
虽然现有的“测试时训练”（Test-Time Training, TTT）可以实现适配，但其推理成本极高（需要梯度优化）；而“混合专家模型”（MoE）则需要改变架构并重新训练。
**研究动机**：受人类大脑能根据情境灵活调整表征的启发，本文提出 **Composer** 范式，旨在推理阶段为每个输入实例动态生成特定的参数补偿，实现“即插即用”的自适应生成。

#### 2. 方法论：核心思想与技术细节
Composer 的核心思想是**在推理时通过一个轻量级的元生成器（Meta-Generator）为预训练模型生成低秩参数更新**。

*   **核心公式**：将预训练权重 $W$ 调整为 $W' = W + AB$，其中 $A \in \mathbb{R}^{d \times r}$ 和 $B \in \mathbb{R}^{r \times d}$ 是针对特定输入生成的低秩矩阵（秩 $r \ll d$）。
*   **Transformer 元生成器**：
    *   **Token 初始化**：从预训练的 Query ($W_Q$) 和 Value ($W_V$) 矩阵中投影提取初始 Token。
    *   **上下文注入**：将输入条件（如文本 Embedding 或类别 Embedding）作为 Prompt Token，与权重 Token 一起输入 Transformer。
    *   **全局-局部注意力机制**：设计了一种特殊的注意力机制，让组件 Token 关注 Prompt Token 以获取上下文，同时在组件内部保持局部块注意力以降低计算量，并通过首个 Token 进行跨块通信。
*   **算法流程**：
    1.  **推理前适配**：输入 Prompt，Composer 一次性生成 $A$ 和 $B$。
    2.  **权重融合**：将 $AB$ 注入原权重 $W$。
    3.  **执行生成**：使用更新后的 $W'$ 进行多步扩散或自回归生成。
*   **上下文感知训练**：在训练 Composer 时，通过控制比例 $\alpha$ 混合“同类/相似 Prompt”和“异类/不同语境”的样本，确保模型既能学到一致性适配，又能保持区分度。

#### 3. 实验设计
*   **数据集**：ImageNet（256x256, 512x512）用于类条件生成；MS-COCO 2014 用于文生图生成。
*   **Benchmark 与模型**：
    *   **自回归模型**：VAR (d-16, d-24, d-30, d-36)。
    *   **扩散模型**：DiT (L/2, XL/2)、Stable Diffusion 2.1。
*   **对比方法**：
    *   **Standard**：原始静态预训练模型。
    *   **Test-time Training (TTT)**：在推理时对输入进行梯度下降微调的方法。
*   **评估指标**：FID（质量）、IS（多样性）、Precision/Recall、CLIP Score（图文对齐度）、推理时间与内存占用。

#### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100 80GB GPU** 进行训练。
*   **训练配置**：使用 AdamW 优化器，学习率 1e-4，权重衰减 0.05。
*   **训练时长**：所有实验均训练了 **50 个 Epoch**。
*   **说明**：论文未明确提及具体使用的 GPU 数量，但从模型规模（最高 2.2B 参数）来看，通常需要多卡并行。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从类条件生成到文生图、从全精度到量化模型（PTQ）、从基础生成到测试时缩放（Test-time Scaling）的多个维度。
*   **消融实验**：非常详尽，包括低秩维度 $r$ 的影响、采样比例 $\alpha$ 的选择、注意力机制的对比、元生成器架构（CNN vs MLP vs Transformer）的对比，以及适配层位置的选择。
*   **充分性评价**：实验设计非常全面且客观。通过在不同架构（VAR, DiT, SD）上的一致性提升，证明了该方法的通用性。同时，它不仅关注质量提升，还详细对比了推理开销，体现了公平性。

#### 6. 主要结论与发现
*   **性能提升**：Composer 在所有基准测试中均优于 Standard 和 TTT。例如在 VAR-d30 上，FID 从 1.97 降至 1.79。
*   **极低开销**：相比 TTT 动辄 100 倍以上的推理时间增加，Composer 的额外时间开销通常小于 **0.1% - 7%**，内存增加约 **3% - 5%**。
*   **量化修复**：在极低比特（如 2-bit）量化下，Composer 能显著修复量化带来的精度损失（IS 分数从 49 提升至 78）。
*   **即插即用**：无需修改原始模型架构，无需对主干网络进行昂贵的重新训练。

#### 7. 优点
*   **高效性**：通过一次性生成参数更新，规避了测试时迭代优化的巨大开销。
*   **灵活性**：作为插件（Plug-in），可以轻松集成到现有的各种 Transformer 架构生成模型中。
*   **上下文感知**：相比 LoRA 等固定适配器，Composer 实现了真正的“因输入而异”的动态适配。
*   **多场景适用**：在模型压缩（量化）和性能增强（测试时缩放）方面展现了强大的潜力。

#### 8. 不足与局限
*   **推理延迟**：虽然开销极小，但在极少步数（如 1-step）的生成模型中，Composer 生成参数的时间占比会相对增加。
*   **训练依赖**：虽然推理不需要梯度，但 Composer 本身仍需要针对特定的预训练模型进行 50 个 Epoch 的辅助训练。
*   **参数量增加**：虽然不改变主干，但引入了额外的元生成器参数（如 VAR-d16 增加了约 100M 参数）。
*   **泛化边界**：目前主要验证了图像生成任务，在视频生成或更广泛的视觉任务上的表现尚待进一步探索。

（完）
