---
title: Corruption-robust Offline Multi-agent Reinforcement Learning From Human Feedback
title_zh: 抗污染的离线多智能体人类反馈强化学习
authors: "Andi Nika, Debmalya Mandal, Parameswaran Kamalaruban, Adish Singla, Goran Radanović"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28281v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 基于人类反馈的离线多智能体强化学习
tldr: 本文研究了离线多智能体强化学习从人类反馈中学习（MARLHF）在数据受损情况下的鲁棒性。针对 ε 比例数据被恶意篡改的情景，研究者在线性马尔可夫博弈框架下，分别在均匀覆盖和单边覆盖假设下提出了鲁棒估计方法。研究不仅给出了纳什均衡间隙的理论界限，还针对计算效率问题提出了准多项式时间的粗相关均衡（CCE）算法，填补了该领域在对抗性数据污染处理上的空白。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28281v1/fig-001.webp\", \"caption\": \"Table 1: Summary of our bounds for: (i) NE gap minimization under uniform coverage, (ii) NE gap minimization under unilateral coverage, and (iii) CCE gap minimization under unilateral coverage. Here, Õ hides any poly-logarithmic factors, n denotes the number of agents, H denotes the horizon and d denotes the dimension. Moreover, ξR and ξP denote the uniform coverage coefficients, while CR and CP denote the unilateral coverage coefficients; ϵ denotes the corruption parameter, m is the data size, T1 is the number of gradient steps while T2 is the number of steps for which OptimisticHedge is run. It is worth mentioning that our bounds have optimal dependence on ϵ in the uniform coverage setting, while maintaining the same dependence as the single-agent (and two-player zero-sum Markov game) settings under non-uniform coverage. Moreover, note that the algorithm for CCE approximation is also computationally efficient.\", \"page\": 3, \"index\": 1, \"width\": 810, \"height\": 211}]"
motivation: 旨在解决离线多智能体强化学习中，人类反馈数据可能存在恶意篡改或噪声，从而影响学习策略鲁棒性的问题。
method: 在线性马尔可夫博弈框架下，针对不同数据覆盖度设计了鲁棒估计器，并引入准多项式时间算法以优化计算效率。
result: 证明了在不同覆盖条件下算法均能达到亚线性的误差界限，并首次实现了计算可行的鲁棒 CCE 求解。
conclusion: 该研究为离线 MARLHF 提供了首个系统性的对抗性数据污染处理方案，具有重要的理论和实践意义。
---

## 摘要
我们研究了在强污染模型下，离线多智能体人类反馈强化学习（MARLHF）对数据污染的鲁棒性。给定一个由轨迹-偏好元组组成的数据集 $D$（每个偏好是一个 $n$ 维二进制标签向量，代表 $n$ 个智能体中每一个的偏好），其中 $ε$ 比例的样本可能被任意污染。我们使用线性马尔可夫博弈框架对该问题进行建模。首先，在均匀覆盖假设下（即所有感兴趣的策略在污染前的干净数据中都有充分体现），我们引入了一种鲁棒估计器，保证纳什均衡间隙具有 $O(ε^{1 - o(1)})$ 的上界。接下来，我们转向更具挑战性的单边覆盖设定，即仅覆盖纳什均衡及其单玩家偏离。在这种情况下，我们提出的算法实现了 $O(\sqrtε)$ 的纳什间隙上界。然而，这两项程序都存在计算不可行的问题。为了解决这一问题，我们将解概念放宽为粗相关均衡（CCE）。在相同的单边覆盖机制下，我们推导出一个准多项式时间算法，其 CCE 间隙为 $O(\sqrtε)$。据我们所知，这是对离线 MARLHF 中对抗性数据污染的首次系统性研究。

## Abstract
We consider robustness against data corruption in offline multi-agent reinforcement learning from human feedback (MARLHF) under a strong-contamination model: given a dataset $D$ of trajectory-preference tuples (each preference being an $n$-dimensional binary label vector representing each of the $n$ agents' preferences), an $ε$-fraction of the samples may be arbitrarily corrupted. We model the problem using the framework of linear Markov games. First, under a uniform coverage assumption - where every policy of interest is sufficiently represented in the clean (prior to corruption) data - we introduce a robust estimator that guarantees an $O(ε^{1 - o(1)})$ bound on the Nash equilibrium gap. Next, we move to the more challenging unilateral coverage setting, in which only a Nash equilibrium and its single-player deviations are covered. In this case, our proposed algorithm achieves an $O(\sqrtε)$ bound on the Nash gap. Both of these procedures, however, suffer from intractable computation. To address this, we relax our solution concept to coarse correlated equilibria (CCE). Under the same unilateral coverage regime, we derive a quasi-polynomial-time algorithm whose CCE gap scales as $O(\sqrtε)$. To the best of our knowledge, this is the first systematic treatment of adversarial data corruption in offline MARLHF.

---

## 论文详细总结（自动生成）

这是一份关于论文《Corruption-robust Offline Multi-agent Reinforcement Learning From Human Feedback》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：人类反馈强化学习（RLHF）已成为微调大语言模型（LLM）的主流方法。然而，现实应用（如自动驾驶、分布式系统、战略市场）往往涉及多个智能体的交互，即多智能体人类反馈强化学习（MARLHF）。
*   **核心问题**：离线数据集极易受到**对抗性数据污染（Data Poisoning）**的攻击。攻击者可以通过篡改一小部分（$\epsilon$ 比例）的偏好标签，诱导模型产生偏见或有害输出。
*   **研究意义**：本文旨在解决在离线、多智能体且存在恶意数据污染的复杂环境下，如何稳健地学习到纳什均衡（Nash Equilibrium, NE）或其近似解。这是首个系统性探讨 MARLHF 对抗性鲁棒性的理论研究。

### 2. 论文提出的方法论
论文基于**线性马尔可夫博弈（Linear Markov Games）**框架，针对不同的数据覆盖强度提出了三种主要的算法思路：

*   **核心思想**：遵循“鲁棒奖励估计 $\Rightarrow$ 鲁棒 Q 函数估计 $\Rightarrow$ 估计间隙最小化”的流水线。
*   **关键技术细节**：
    1.  **均匀覆盖（Uniform Coverage）下的 NE 学习**：
        *   使用 **TrimmedMLE**（修剪极大似然估计）从受损偏好数据中恢复每个智能体的奖励参数。
        *   通过**鲁棒值迭代（Backward Induction）**结合鲁棒均值估计器（RobEst）来处理 Bellman 算子的污染。
        *   证明了纳什间隙（Nash Gap）可以达到 $O(\epsilon^{1-o(1)})$ 的近线性误差界。
    2.  **单边覆盖（Unilateral Coverage）下的 NE 学习**：
        *   这是更现实的假设，即数据仅覆盖纳什均衡及其单方面偏离。
        *   **挑战**：无法直接获得准确的奖励梯度。
        *   **对策**：引入**投影梯度上升（PGA）**，利用行为策略的特征差异作为“有偏但可用”的梯度代理，实现了 $O(\sqrt{\epsilon})$ 的误差界。
    3.  **计算高效的 CCE 学习**：
        *   由于一般博弈中寻找 NE 是计算不可行的（PPAD-hard），论文将解概念放宽为**粗相关均衡（CCE）**。
        *   在每个阶段博弈中使用 **Optimistic Hedge**（一种无悔学习算法）来高效求解，在准多项式时间内达到 $O(\sqrt{\epsilon})$ 的 CCE 间隙。

### 3. 实验设计
*   **性质说明**：本论文是一篇**纯理论证明型论文（Theoretical Paper）**。
*   **数据集/场景**：文中没有使用真实世界的数据集或模拟环境进行实证测试。
*   **Benchmark**：其对比基准是单智能体鲁棒 RLHF 的现有理论界限（如 Mandal et al. 2025）以及非鲁棒的多智能体离线学习界限。
*   **验证方式**：通过严密的数学推导和引理证明（附录长达 20 余页）来验证算法的收敛性和鲁棒性界限。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**使用了任何 GPU 或大规模计算资源。
*   **原因**：由于不涉及深度神经网络的训练或大规模实证实验，其核心贡献在于算法复杂度的理论分析和误差上界的推导。算法 5 提到的准多项式时间复杂度主要用于理论上的计算可行性论证。

### 5. 实验数量与充分性
*   **充分性评价**：从理论研究的角度看，论文非常充分。它涵盖了从强假设（均匀覆盖）到弱假设（单边覆盖）的过渡，并兼顾了统计效率（误差界）与计算效率（CCE 算法）。
*   **客观性**：论文客观地指出了在弱覆盖条件下误差界从 $\epsilon$ 退化到 $\sqrt{\epsilon}$ 的现象，这与单智能体鲁棒学习的理论预期一致。

### 6. 主要结论与发现
*   **误差界限**：在均匀覆盖下，可以实现近乎最优的线性依赖 $\epsilon$ 的误差界；在单边覆盖下，误差界为 $\sqrt{\epsilon}$。
*   **计算可行性**：证明了虽然精确 NE 难以计算，但通过 Optimistic Hedge 可以高效地找到鲁棒的 CCE。
*   **理论统一**：首次将鲁棒统计学中的 TrimmedMLE 和 RobMean 技术成功引入到多智能体偏好学习的动态博弈场景中。

### 7. 优点
*   **理论开创性**：填补了 MARLHF 在对抗性鲁棒性领域的空白。
*   **处理梯度偏差的创新**：在单边覆盖下，巧妙地利用特征期望差异作为梯度代理，解决了无法直接优化未知奖励函数的问题。
*   **解概念的灵活性**：通过引入 CCE，为实际应用中处理大规模多智能体博弈提供了计算上的可能性。

### 8. 不足与局限
*   **缺乏实证验证**：完全依赖理论推导，没有在实际的多智能体环境（如星际争霸模拟器或多智能体对话系统）中验证算法的实际表现。
*   **线性假设限制**：模型假设奖励和转移函数是线性的，这在处理具有高度非线性特征的复杂任务（如现代 LLM）时可能过于简化。
*   **界限紧凑性**：虽然达到了 $O(\sqrt{\epsilon})$，但论文提到在非均匀覆盖下该界限是否为理论下界（即是否最紧）仍是一个开放性问题。
*   **应用限制**：算法中涉及的某些鲁棒估计步骤在超高维空间下的实现细节仍具挑战性。

（完）
