---
title: "Parallelized Hierarchical Connectome: A Spatiotemporal Recurrent Framework for Spiking State-Space Models"
title_zh: 并行化分层连接组：一种用于脉冲状态空间模型的时空循环框架
authors: Po-Han Chiang
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01295v1"
tags: ["query:snn"]
score: 10.0
evidence: 脉冲状态空间模型的时空循环框架
tldr: 本研究提出PHC框架，将仅具时间递归的状态空间模型（SSM）升级为时空递归网络。通过将SSM核心映射为神经元层并引入多传输循环，PHC在保持O(logT)并行性的同时实现了层内空间交互，并整合了LIF动力学、戴尔法则等生物物理约束。其实例化模型PHCSSM在生理基准测试中表现优异，且将参数复杂度从Theta(D^2 L)降至Theta(D^2)，实现了生物启发式的高效并行脉冲神经网络训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01295v1/fig-001.webp\", \"caption\": \"Figure 2: Structural isomorphism between stacked SSMs and the PHC framework. Left: A conventional L-layer stacked SSM, where L independent diagonal state-transition matrices (Layer 0, Layer 1) are interleaved with L independent dense MLPs (MLP1, MLP2), each with non-shared parameters, forming a one-direction forward connection. Right: The PHC framework collapses this vertical stack into a single spatial plane. Each diagonal layer maps to a region (R0, R1) within a shared Neuron Layer whose parameters are reused across all regions (Parallelized State Update). Each inter-layer MLP maps to a sub-block of the Hierarchical Connectome Matrix, which consolidates all inter-neuronal communication into a single shared Synapse Layer with biologically constrained connectivity (Dale’s Law, topology mask).\", \"page\": 6, \"index\": 1, \"width\": 814, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2604.01295v1/fig-002.webp\", \"caption\": \"Table 7: Run time in seconds for the considered models for 1000 training steps. All measurements were performed on an NVIDIA RTX4090 GPU using JAX. Values for all baseline models are from † Rusch and Rus (2025) and ‡ Farsang et al. (2025). Only LrcSSM value from Farsang et al. (2025) was performed on the NVIDIA A100.\", \"page\": 25, \"index\": 2, \"width\": 800, \"height\": 382}, {\"url\": \"assets/figures/arxiv/2604.01295v1/fig-003.webp\", \"caption\": \"Table 8: Ablation study. Test accuracy (mean ± std over 5 seeds) on Heartbeat, SCP2, and MotorImagery under systematic removal of individual bio constraints. “LIF only” replaces ALIF with a standard non-adaptive LIF neuron; “no Dale’s Law” removes E/I sign-clamping from the Synapse Layer; “no STP” sets synaptic efficacy to static weights by disabling the Tsodyks–Markram mechanism; “no STDP” disables the R-STDP dualupdate, retaining gradient-only training. Each ablation variant uses the best-performing Topology Matrix configuration per dataset (Heartbeat: Mtopo 0; SCP2 and MotorImagery: Mtopo 1).\", \"page\": 25, \"index\": 3, \"width\": 664, \"height\": 226}, {\"url\": \"assets/figures/arxiv/2604.01295v1/fig-004.webp\", \"caption\": \"Figure 3: Detailed signal flow of the PHCSSM forward pass. The input sequence is projected via a linear encoder and gated by an input mask restricting sensory drive to designated populations. Within the Multi-Transmission Loop, the NL performs three sequential diagonal parallel scans (membrane potential, adaptive threshold, and refractory suppression) followed by pointwise spike generation (ALIF). The SL applies a synaptic delay buffer, modulates spikes via Tsodyks–Markram STP (two additional parallel scans), and transmits the result through the biologically constrained weight matrix Wstruct (= Wsyn ⊙Mtopo). Convergence is assessed via the Cauchy criterion after each transmission; upon exit, R-STDP updates synaptic weights using genuine binary spike timing. The output membrane voltage is projected to logits via a linear readout.\", \"page\": 7, \"index\": 4, \"width\": 814, \"height\": 561}, {\"url\": \"assets/figures/arxiv/2604.01295v1/fig-005.webp\", \"caption\": \"Table 4: Test accuracy (mean ± std over 5 seeds) on UEA physiological benchmarks. † Results from Rusch and Rus (2025). ‡ Results from Farsang et al. (2025). *Results from Terzić et al. (2025). Best per column in bold. PHCSSM is reported under two topology matrix configurations (Mtopo 0 and Mtopo 1); the best result per dataset across both configurations is underlined and used in all per-dataset comparisons.\", \"page\": 22, \"index\": 5, \"width\": 802, \"height\": 493}, {\"url\": \"assets/figures/arxiv/2604.01295v1/fig-006.webp\", \"caption\": \"Table 5: Number of parameters for every considered model on all long-range datasets. GPU memory reflects peak activation during backpropagation. All measurements were performed on an NVIDIA RTX4090 GPU using JAX. Values for all baseline models are from † Rusch and Rus (2025).\", \"page\": 23, \"index\": 6, \"width\": 800, \"height\": 328}, {\"url\": \"assets/figures/arxiv/2604.01295v1/fig-007.webp\", \"caption\": \"Figure 1: Comparison of conventional stacked SSMs and the Parallelized Hierarchical Connectome (PHC) framework. (A) Conventional SSMs process sequences through stacked layers connected via unidirectional feedforward MLPs, corresponding to L independent diagonal state-transition matrices and L dense inter-layer weight matrices, supporting only temporal recurrence with no lateral or feedback interactions. (B) The PHC framework partitions neurons into hierarchical regions (R0, R1) with biologically constrained E/I populations. Within each region, neurons interact via full local microcircuits; across regions, both feedforward (R0→R1) and feedback (R1→R0) projections are supported through the Hierarchical Connectome Matrix (Wsyn ⊙Mtopo), illustrated here under the bidirectional configuration.\", \"page\": 4, \"index\": 7, \"width\": 814, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.01295v1/fig-008.webp\", \"caption\": \"Table 6: Architectural complexity comparison of SSM variants. PHCSSM’s NL is analogous to the diagonal SSM core in LRU, S5, Mamba, LinOSS, and LrcSSM, while the SL is analogous to the inter-layer MLP. Because NL and SL are shared across all M transmission steps, total parameter count is Θ(D2) rather than Θ(D2L) for L-layer stacked architectures.\", \"page\": 24, \"index\": 8, \"width\": 800, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2604.01295v1/fig-009.webp\", \"caption\": \"Table 3: Optimized hyperparameters used for training.\", \"page\": 20, \"index\": 9, \"width\": 798, \"height\": 216}, {\"url\": \"assets/figures/arxiv/2604.01295v1/fig-010.webp\", \"caption\": \"Table 2: Topology Matrix Setting\", \"page\": 20, \"index\": 10, \"width\": 800, \"height\": 301}]"
motivation: 传统SSM仅限于时间维度递归，缺乏单时间步内神经元间的空间交互与复杂的生物物理动力学模拟。
method: 提出PHC框架，利用多传输循环实现层内空间递归，并将生物连接组拓扑与多种神经物理约束整合进可并行化的训练流水线。
result: PHCSSM在UEA多变量时间序列基准测试中达到了与最先进SSM相当的性能，同时显著降低了模型参数量。
conclusion: 生物学归纳偏置为构建参数高效且支持完全并行训练的脉冲状态空间模型提供了一种原则性的新路径。
---

## 摘要
本研究提出了并行化分层连接组（PHC），这是一个将仅限时间维度的状态空间模型（SSMs）升级为时空循环网络的通用框架。传统的 SSMs 通过并行扫描实现高速序列处理，但仅限于时间维度的循环，在单个时间步内缺乏横向或反馈交互。PHC 将对角 SSM 核心映射到共享神经元层，将神经元间通信映射到共享突触层，其中神经元根据连接组拓扑结构被划分为分层区域。多传输循环（Multi-Transmission Loop）实现了切片内的空间循环，允许信号在每个时间窗口内跨分层连接组传播，同时保持 O(logT) 的并行性。该框架能够集成标准 SSMs 通常难以处理的神经物理先验，包括自适应漏电整合发放（LIF）动力学、戴尔法则（Dale's Law）、短期可塑性以及奖励调节的脉冲时间依赖可塑性（R-STDP）。该框架被实例化为 PHCSSM，这是首个在完全可并行化的训练流水线中，将循环脉冲神经网络动力学与对角 SSM 并行性统一起来的模型，同时强制执行了所有五种生物约束和可学习的横向连接。在 UEA 多变量时间序列存档的生理基准测试上的实验结果表明，PHCSSM 达到了与最先进 SSMs 相当的性能，同时将参数复杂度从 L 层堆叠架构的 Theta(D^2 L) 降低到 Theta(D^2)。这些发现表明，基于生物学的归纳偏置为参数高效的序列建模提供了一条原则性路径，为对角 SSMs 引入了时空循环，并实现了完全可并行化的循环脉冲神经网络训练。

## Abstract
This work presents the Parallelized Hierarchical Connectome (PHC), a general framework that upgrades temporal-only State-Space Models (SSMs) into spatiotemporal recurrent networks. Conventional SSMs achieve high-speed sequence processing through parallel scans, yet are limited to temporal recurrence without lateral or feedback interactions within a single timestep. PHC maps the diagonal SSM core to a shared Neuron Layer and inter-neuronal communication to a shared Synapse Layer, where neurons are partitioned into hierarchical regions governed by the connectome topology. A Multi-Transmission Loop enables intra-slice spatial recurrence, allowing signals to propagate across the hierarchical connectome within each temporal window while preserving O(logT) parallelism. This framework enables integration of neuro-physical priors typically intractable for standard SSMs, including adaptive leaky integrate-and-fire dynamics, Dale's Law, short-term plasticity, and reward-modulated spike-timing-dependent plasticity. The framework is instantiated as PHCSSM, the first model to unify recurrent spiking neural network dynamics with diagonal SSM parallelism while enforcing all five biological constraints and learnable lateral connections within a fully parallelizable training pipeline. Empirical results on physiological benchmarks from the UEA multivariate time-series archive demonstrate that PHCSSM achieves performance competitive with state-of-the-art SSMs while reducing parameter complexity from Theta(D^2 L) for L-layer stacked architectures to Theta(D^2). These findings suggest that biologically grounded inductive biases offer a principled route to parameter-efficient sequence modeling, opening diagonal SSMs to spatiotemporal recurrence and enabling fully parallelizable recurrent spiking neural network training.

---

## 论文详细总结（自动生成）

### 论文总结：Parallelized Hierarchical Connectome (PHC)

#### 1. 核心问题与整体含义（研究动机和背景）
现代序列建模中，状态空间模型（SSM，如 Mamba、S4）通过将状态转移矩阵限制为**对角阵**，利用并行关联扫描实现了 $O(\log T)$ 的训练效率。然而，这种计算上的飞跃是以牺牲**生物真实性**为代价的：
*   **缺乏空间交互**：对角化约束使得同一时间步内的神经元彼此解耦，无法实现生物大脑中常见的横向抑制、反馈交互和复杂的空间递归。
*   **参数冗余**：为了补偿空间表达能力的不足，现有 SSM 依赖于“层堆叠”（Layer Stacking），导致参数量随深度 $L$ 线性增长（$\Theta(D^2 L)$）。
*   **脉冲神经网络（SNN）的训练瓶颈**：传统的循环脉冲神经网络（RSNN）虽然具有丰富的时空动力学，但受限于 BPTT 的串行依赖，难以在长序列上高效训练。

本论文的核心动机是打破“并行效率”与“空间递归/生物约束”之间的互斥关系，提出一种既能保持并行扫描效率，又能引入可学习横向连接和多种生物物理特性的新框架。

#### 2. 方法论：核心思想与关键技术
论文提出了 **PHC (Parallelized Hierarchical Connectome)** 框架，其核心思想是**“步内时空解耦”（Intra-Step Spatiotemporal Decoupling）**。

*   **结构同构映射**：将传统堆叠 SSM 的对角核心映射为共享的**神经元层（NL）**，将层间混合矩阵映射为共享的**突触层（SL）**。
*   **多传输循环（Multi-Transmission Loop）**：在单个时间步内，信号在 NL 和 SL 之间多次循环（$M$ 次）。这种设计允许神经元在时间推进前达成空间上的动态平衡，用“空间循环”替代“物理层堆叠”，将参数复杂度降至 $\Theta(D^2)$。
*   **并行化生物动力学**：
    *   **ALIF 神经元**：将自适应阈值和漏电整合发放动力学重新表述为仿射递归，利用对数域并行前缀和实现 $O(\log T)$ 处理。
    *   **短期可塑性（STP）**：实现了 Tsodyks-Markram 模型，通过两个连续的并行扫描层动态调节突触权重。
    *   **戴尔法则（Dale’s Law）与拓扑约束**：强制执行兴奋性/抑制性神经元分离，并引入分层连接组掩码（如前馈、双向拓扑）。
    *   **奖励调节 STDP（R-STDP）**：利用真实的二值脉冲进行在线局部学习，作为梯度下降的补充。
*   **收敛判定**：引入 Cauchy 收敛准则进行动态早停（Early Exit），根据输入复杂度自适应调整空间处理深度。

#### 3. 实验设计
*   **数据集**：使用了 UEA 多变量时间序列分类存档中的 6 个生理信号基准数据集：Heartbeat、SCP1、SCP2、EthanolConcentration、MotorImagery 和 EigenWorms（序列长度从 405 到 17,984 不等）。
*   **Benchmark**：遵循 Walker 等人 (2024) 建立的标准评估协议。
*   **对比方法**：涵盖了主流的 SSM 变体（S5, S6, Mamba, LRU, LinOSS, LrcSSM）、神经常微分方程（NCDE, Log-NCDE）以及 Transformer 变体（RFormer）。

#### 4. 资源与算力
*   **硬件**：所有实验均在单张 **NVIDIA RTX 4090 GPU** 上完成，使用 JAX 框架。
*   **训练时长**：1000 个训练步的耗时在 27 到 129 秒之间（视数据集长度而定）。
*   **内存占用**：峰值 GPU 显存保持在 10MB 到 48MB 之间，极具轻量化优势。
*   **参数量**：PHCSSM 的参数量极小（约 1,700 到 9,500 个），仅为对比方法的 1/10 甚至 1/100。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同特性的数据集上进行了测试，每个结果均基于 5 个固定随机种子的平均值和标准差。
*   **消融实验**：针对 ALIF、戴尔法则、STP 和 R-STDP 分别做了系统性消融，验证了每个生物约束的独立贡献。
*   **客观性**：直接引用了最新文献（2025年）中的基准数据，对比环境公平。实验覆盖了从短序列到超长序列（EigenWorms），证明了模型的鲁棒性。

#### 6. 主要结论与发现
*   **性能领先**：PHCSSM 在 SCP2 数据集上达到了 59.3% 的准确率，创下了该基准的新纪录；在长序列任务 MotorImagery 上显著超过 Mamba（+6.0%）。
*   **参数极其高效**：通过权重共享和空间递归，模型在参数量极低的情况下依然保持了极强的竞争力。
*   **生物约束即正则化**：消融实验显示，戴尔法则和 R-STDP 等约束不仅提升了准确率，还显著降低了训练的方差，起到了结构正则化的作用。
*   **并行化可行性**：证明了复杂的非线性生物动力学（如 STP）可以被有效地并行化，而不损失 $O(\log T)$ 的扩展性。

#### 7. 优点
*   **理论创新**：首次在 SSM 框架内实现了支持可学习横向连接且保持并行性的时空递归。
*   **生物真实性**：高度整合了神经科学先验（ALIF, STP, Dale's Law, STDP），使模型具有天然的可解释性和神经形态硬件友好性。
*   **极低开销**：参数量和显存占用极低，适合资源受限的边缘计算场景。

#### 8. 不足与局限
*   **任务覆盖面**：目前仅在生理信号分类任务上验证，尚未在自然语言处理（NLP）或大规模视觉任务上进行测试。
*   **拓扑设计**：分层连接组的掩码目前仍是人工设计的，而非从真实大脑连接组数据中自动学习或提取。
*   **硬件适配**：虽然理论上适合神经形态芯片，但由于 ALIF 和 STP 的复杂性，目前尚缺乏在真实类脑芯片上的能效实测数据。
*   **规模扩展性**：实验中的神经元维度较小（D=16~64），在大规模隐藏维度下的表现尚待观察。

（完）
