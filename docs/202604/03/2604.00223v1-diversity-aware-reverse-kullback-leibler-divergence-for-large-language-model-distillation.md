---
title: Diversity-Aware Reverse Kullback-Leibler Divergence for Large Language Model Distillation
title_zh: 针对大语言模型蒸馏的感知多样性逆向 KL 散度
authors: "Hoang-Chau Luong, Dat Ba Tran, Lingwei Chen"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00223v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于大语言模型蒸馏的反向KL散度
tldr: 针对大语言模型蒸馏中逆向KL散度（RKL）导致的模型过度自信和输出多样性不足问题，本文提出了多样性感知逆向KL（DRKL）。通过分析RKL梯度结构，研究者发现其非目标梯度会持续推高目标概率，且对长尾类别监督不足。DRKL通过消除这种梯度偏差并增强非目标类别监督，在保持RKL优化优势的同时，显著提升了模型的生成质量和多样性，在多个数据集上优于现有主流蒸馏方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00223v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of distillation losses for GPT-2 and OPT teacher–student pairs. We report ROUGE-L, with mean and standard deviation computed over five random seeds. The best results are boldfaced and the second-best are underlined.\", \"page\": 8, \"index\": 1, \"width\": 822, \"height\": 725}, {\"url\": \"assets/figures/arxiv/2604.00223v1/fig-002.webp\", \"caption\": \"Figure 1: FKL and RKL when fitting a teacher distribution under different output sizes. The x-axis shows the class index, and the y-axis shows the corresponding output probability. As the number of classes increases, the optimization difficulty grows substantially.\", \"page\": 4, \"index\": 2, \"width\": 825, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.00223v1/fig-003.webp\", \"caption\": \"Figure 5: (a) Validation performance of different distillation losses. (b) DRKL performance across different values of γ. (c) Efficiency analysis.\", \"page\": 9, \"index\": 3, \"width\": 818, \"height\": 193}, {\"url\": \"assets/figures/arxiv/2604.00223v1/fig-004.webp\", \"caption\": \"Figure 2: (a, b) Fidelity vs. diversity: RKL reduces diversity (Negative Self-BLEU and Distinct-2), while DRKL achieves a better balance across methods. (c) ROUGE-L vs. prediction confidence: RKL produces overconfident predictions without improving ROUGE-L, while DRKL is better calibrated.\", \"page\": 5, \"index\": 4, \"width\": 819, \"height\": 201}]"
motivation: 传统的RKL蒸馏会导致学生模型过度自信并降低输出多样性，且对非目标类别的监督较弱。
method: 提出DRKL方法，通过分解并修正RKL梯度，消除对目标logit的过度推高，并加强对非目标类别的对齐。
result: 实验证明DRKL在多个模型家族和数据集上均优于FKL、RKL及其他SOTA蒸馏目标，实现了更好的保真度与多样性平衡。
conclusion: DRKL有效解决了RKL在LLM蒸馏中的结构性缺陷，是提升蒸馏模型性能和多样性的有力工具。
---

## 摘要
逆向 Kullback-Leibler (RKL) 散度近期已成为大语言模型 (LLM) 蒸馏的首选目标函数，其表现持续优于正向 KL (FKL)，特别是在词表较大且教师与学生模型容量存在显著差异的情况下。在这些场景中，RKL 将学习重点放在主导模式上，而非强制进行密集对齐。然而，RKL 引入了一种结构性限制，会促使学生模型产生过度自信的预测。我们首先通过将 RKL 的梯度分解为目标和非目标分量对其进行了分析，并表明即使在学生模型已经与教师模型匹配的情况下，非目标梯度仍会持续推高目标 logit，从而降低了输出的多样性。此外，RKL 对非目标类别的监督较弱，导致长尾对齐效果不佳。为了解决这些问题，我们提出了感知多样性的 RKL (DRKL)，它在保留 RKL 优化优势的同时，消除了这种梯度效应并加强了非目标监督。在不同数据集和模型系列上的广泛实验表明，DRKL 的表现持续优于 FKL、RKL 以及其他最先进的蒸馏目标，实现了更好的性能以及更优的保真度-多样性权衡。

## Abstract
Reverse Kullback-Leibler (RKL) divergence has recently emerged as the preferred objective for large language model (LLM) distillation, consistently outperforming forward KL (FKL), particularly in regimes with large vocabularies and significant teacher-student capacity mismatch, where RKL focuses learning on dominant modes rather than enforcing dense alignment. However, RKL introduces a structural limitation that drives the student toward overconfident predictions. We first provide an analysis of RKL by decomposing its gradients into target and non-target components, and show that non-target gradients consistently push the target logit upward even when the student already matches the teacher, thereby reducing output diversity. In addition, RKL provides weak supervision over non-target classes, leading to poor tail alignment. To address these issues, we propose Diversity-aware RKL (DRKL), which removes this gradient effect and strengthens non-target supervision while preserving the optimization benefits of RKL. Extensive experiments across datasets and model families demonstrate that DRKL consistently outperforms FKL, RKL, and other state-of-the-art distillation objectives, achieving better performance and a superior fidelity-diversity trade-off.

---

## 论文详细总结（自动生成）

这是一份关于论文《Diversity-Aware Reverse Kullback-Leibler Divergence for Large Language Model Distillation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在大型语言模型（LLM）的知识蒸馏中，**逆向 KL 散度（RKL）**因其“模式寻求（Mode-seeking）”特性，在处理大词表和教师-学生模型容量严重不匹配时，表现优于传统的正向 KL（FKL）。
*   **研究动机**：尽管 RKL 效果好，但存在两个严重缺陷：
    1.  **过度自信（Overconfidence）**：学生模型预测的概率分布往往比教师更尖锐，甚至在目标概率已匹配时仍持续推高目标 logit。
    2.  **多样性缺失**：RKL 倾向于压缩非目标类别的概率空间，导致生成文本的词汇多样性（如 Distinct-2 指标）显著下降。
*   **核心发现**：作者通过梯度分解发现，RKL 的非目标梯度项会产生一种“捷径”，通过不断增加目标项的置信度来降低损失，而非真正对齐非目标分布。

### 2. 论文提出的方法论：DRKL
作者提出了**感知多样性的逆向 KL（Diversity-aware RKL, DRKL）**，其核心思想是解耦目标类别与非目标类别的优化过程。

*   **梯度分解分析**：将 RKL 分解为目标项（$T_{RKL}$，处理目标 token 的二元分布）和非目标项（$N_{RKL}$，处理剩余 token 的归一化分布）。
*   **关键技术细节**：
    *   **原始 RKL 的缺陷**：在标准 RKL 中，$N_{RKL}$ 项被权重 $(1 - q_m)$ 缩放（$q_m$ 是学生对目标项的预测概率）。当 $q_m$ 增大时，非目标项的监督信号会变弱。
    *   **DRKL 公式**：$L_{DRKL} = D_{KL}(\tilde{q}_m \parallel \tilde{p}_m) + \gamma D_{KL}(\hat{q} \parallel \hat{p})$。
    *   **改进点**：
        1.  **消除干扰梯度**：通过将 $N_{RKL}$ 的权重替换为固定超参数 $\gamma$，消除了非目标分布对目标 logit 的负向梯度贡献，防止了置信度的盲目扩增。
        2.  **强化长尾监督**：通过独立的 $\gamma$ 权重，确保即使在目标项概率很高时，模型依然能接收到来自非目标类别（暗知识）的强监督信号，从而提升多样性。

### 3. 实验设计
*   **数据集/场景**：
    *   训练集：使用基于 `databricks-dolly-15k` 构造的指令遵循数据集。
    *   测试集（Benchmark）：Dolly 验证集、Self-Instruct、Vicuna Eval、Super-Natural Instructions (Super-NI) 和 Unnatural Instructions (UnNI)。
*   **模型架构**：
    *   教师模型：GPT-2 XL (1.5B)、OPT-6.7B。
    *   学生模型：GPT-2 Base (120M)、GPT-2 Medium (340M)、OPT-1.3B。
*   **对比方法**：包括 FKL、RKL、Sym-KL（对称 KL）、DKD（解耦 KL）、JS 散度、SFKL/SRKL（偏斜 KL）、AKL（自适应 KL）和 AB 散度。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100 或 H100）及具体数量。
*   **效率分析**：作者提供了一个“效率-性能权衡图”，显示 DRKL 的每步训练耗时约为 **0.58 秒/步**，与标准 RKL（0.57 秒/步）几乎持平，证明该方法在不显著增加计算开销的前提下提升了性能。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个主流模型家族（GPT-2 和 OPT）和三种不同规模的学生模型。
    *   在 5 个不同的指令遵循 Benchmark 上进行了测试。
    *   所有实验均基于 **5 个随机种子**取平均值并标注了标准差，确保了结果的统计可靠性。
*   **消融与分析**：
    *   进行了超参数 $\gamma$ 的敏感性分析（范围 0.5 到 4.0）。
    *   进行了保真度（ROUGE-L）与多样性（Distinct-2, Self-BLEU）的权衡分析。
    *   提供了定性的 Case Study（案例分析）来直观展示生成质量。
*   **评价**：实验设计非常充分且客观，对比了几乎所有近年来主流的蒸馏改进方案，具有很强的说服力。

### 6. 主要结论与发现
1.  **性能领先**：DRKL 在所有测试的 Benchmark 上均取得了最高或次高的 ROUGE-L 分数，显著优于 FKL 和 RKL。
2.  **解决过度自信**：DRKL 生成的预测概率分布与教师模型更接近，有效缓解了 RKL 导致的置信度虚高问题。
3.  **多样性提升**：在保持高保真度的同时，DRKL 的 Distinct-2 等多样性指标明显优于 RKL，证明了强化非目标监督的有效性。
4.  **收敛更快**：验证集曲线显示 DRKL 在训练早期就能达到较高的性能。

### 7. 优点：亮点总结
*   **理论深度**：不仅提出了方法，还通过数学推导（Proposition 4.1 & 4.2）揭示了 RKL 导致过度自信的底层梯度机制。
*   **即插即用**：DRKL 结构简单，可以轻松集成到现有的蒸馏框架中，且与 SRKL 等其他改进方法具有良好的兼容性。
*   **平衡性好**：成功解决了蒸馏中“准确性”与“多样性”难以兼得的经典矛盾。

### 8. 不足与局限
*   **超参数依赖**：虽然实验显示 $\gamma$ 在 0.5-2.0 之间较稳定，但在不同规模或不同领域的模型上，可能仍需手动调优 $\gamma$ 以获得最佳效果。
*   **模型规模限制**：实验最高只做到了 6.7B 到 1.3B 的蒸馏。在目前主流的 70B 甚至更大型模型的蒸馏场景下，该方法的表现和计算开销尚未得到验证。
*   **离线蒸馏局限**：本文主要关注离线（Off-policy）蒸馏，未深入探讨在在线（On-policy）采样蒸馏中，DRKL 是否能持续保持这种优势。

（完）
