---
title: "Switch Attention: Towards Dynamic and Fine-grained Hybrid Transformers"
title_zh: Switch Attention：迈向动态且细粒度的混合 Transformer
authors: "Yusheng Zhao, Hourun Li, Bohan Wu, Jingyang Yuan, Meng Zhang, Yichun Yin, Lifeng Shang, Ming Zhang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26380v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 具有动态路由的新型混合Transformer架构
tldr: 针对标准注意力机制在长文本建模中计算复杂度高的问题，本文提出Switch Attention (SwiAttn)。该方法打破了传统混合模型中静态、启发式的注意力分配模式，通过动态路由机制，在每个Token和每一层上灵活选择全注意力或滑动窗口注意力。结合自适应正则化目标和持续预训练技术，SwiAttn在保持全局信息聚合能力的同时显著提升了效率，在23个基准测试中表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26380v1/fig-001.webp\", \"caption\": \"Figure 1: Conventional hybrid transformers (a) often adopt manually designed, static hybrid schemes. In contrast, the proposed SwiAttn (b) dynamically selects the type of attention for each input token at each transformer layer, enabling fine-grained allocation of computation.\", \"page\": 1, \"index\": 1, \"width\": 431, \"height\": 433}, {\"url\": \"assets/figures/arxiv/2603.26380v1/fig-002.webp\", \"caption\": \"Table 1: Performance on common sense reasoning datasets. Best results are in bold and second-best underline.\", \"page\": 6, \"index\": 2, \"width\": 939, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2603.26380v1/fig-003.webp\", \"caption\": \"Figure 2: During the continual pretraining stage (a), we compute both the full attention branch and the SWA branch separately. Subsequently, the router is applied to select the attention output from different branches for each token’s hidden representation. The selection result is then processed by a feed-forward network (FFN Layer). During the decoding stage (b), the router is used to decide the branch for attention computation. The full attention branch and the SWA branch share a unified KV cache for better efficiency.\", \"page\": 3, \"index\": 3, \"width\": 919, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2603.26380v1/fig-004.webp\", \"caption\": \"Figure 8: The changes of full attention ratio during the continual pretraining stage. The ratio is averaged within each transformer layer.\", \"page\": 17, \"index\": 4, \"width\": 915, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2603.26380v1/fig-005.webp\", \"caption\": \"Figure 7: Language modeling loss curves of (a) pretraining of the full attention transformer on 4K-length sequences, (b) continual pretraining of SwiAttn on 4K-length sequences, (c) pretraining of the full attention transformer on 32K-length sequences, and (d) continual pretraining of SwiAttn on 32K-length sequences.\", \"page\": 16, \"index\": 5, \"width\": 939, \"height\": 644}]"
motivation: 现有的混合注意力模型多采用静态、启发式的交替模式，无法根据不同场景灵活且高效地分配计算资源。
method: 提出一种动态路由机制，使每个Token在每层能自主选择全注意力或滑动窗口注意力分支，并配合自适应正则化进行优化。
result: 在23个涵盖常规（4K）和长文本（32K）长度的基准数据集上，SwiAttn均展现出卓越的性能与效率平衡。
conclusion: SwiAttn通过细粒度的动态计算分配，为构建高效且具备长文本处理能力的混合Transformer架构提供了新思路。
---

## 摘要
注意力机制一直是现代 Transformer 架构的核心组件。然而，标准全注意力（full attention）的计算量随序列长度呈二次方增长，成为长上下文语言建模中的主要瓶颈。滑动窗口注意力通过限制上下文长度来提高效率，但代价是感受野变窄。虽然现有研究尝试通过构建混合模型来兼顾两者的优点，但它们通常采用静态的、基于启发式设计的交替模式，这限制了在各种场景下计算资源的有效分配。在本文中，我们提出了 Switch Attention (SwiAttn)，这是一种新型混合 Transformer，能够在全注意力与滑动窗口注意力之间实现动态且细粒度的路由。对于每个 Transformer 层中的每个 token，SwiAttn 动态地将计算路由至用于全局信息聚合的全注意力分支，或用于高效局部模式匹配的滑动窗口分支。我们设计了一种自适应正则化目标，以鼓励模型提高效率。此外，我们采用持续预训练（continual pretraining）来优化模型，将全注意力架构转换为混合架构。我们在 23 个基准数据集上进行了广泛实验，涵盖了常规（4K）和长（32K）上下文长度，证明了所提方法的有效性。

## Abstract
The attention mechanism has been the core component in modern transformer architectures. However, the computation of standard full attention scales quadratically with the sequence length, serving as a major bottleneck in long-context language modeling. Sliding window attention restricts the context length for better efficiency at the cost of narrower receptive fields. While existing efforts attempt to take the benefits from both sides by building hybrid models, they often resort to static, heuristically designed alternating patterns that limit efficient allocation of computation in various scenarios. In this paper, we propose Switch Attention (SwiAttn), a novel hybrid transformer that enables dynamic and fine-grained routing between full attention and sliding window attention. For each token at each transformer layer, SwiAttn dynamically routes the computation to either a full-attention branch for global information aggregation or a sliding-window branch for efficient local pattern matching. An adaptive regularization objective is designed to encourage the model towards efficiency. Moreover, we adopt continual pretraining to optimize the model, transferring the full attention architecture to the hybrid one. Extensive experiments are conducted on twenty-three benchmark datasets across both regular (4K) and long (32K) context lengths, demonstrating the effectiveness of the proposed method.

---

## 论文详细总结（自动生成）

### 论文总结：Switch Attention (SwiAttn) —— 迈向动态且细粒度的混合 Transformer

#### 1. 核心问题与整体含义（研究动机和背景）
现代 Transformer 架构的核心瓶颈在于标准全注意力（Full Attention）的计算复杂度随序列长度呈二次方增长（$O(T^2)$），这极大限制了长文本的处理效率。虽然滑动窗口注意力（SWA）能将复杂度降为线性，但会牺牲长程依赖的建模能力。
*   **研究动机**：现有的“混合注意力”方案（如一层全注意力交替三层滑动窗口）大多是**静态且启发式**的，即在训练前就固定了层级分配。这种方式无法根据输入内容（Token 级别）或任务需求动态调整计算资源，导致在不需要全局信息的场景下浪费算力，或在关键时刻缺乏全局视野。

#### 2. 论文提出的方法论
论文提出了 **Switch Attention (SwiAttn)**，其核心思想是在每一层为每个 Token 引入一个动态路由器（Router），实时决定该 Token 走“全注意力”分支还是“滑动窗口”分支。

*   **双分支路由机制**：
    *   **全注意力分支**：负责精确聚合全序列的全局信息。
    *   **滑动窗口分支**：负责高效处理局部模式匹配。
    *   **路由逻辑**：通过一个线性层和 Sigmoid 函数生成软门控值，再通过直通估计器（STE）转换为硬门控信号（0 或 1）。
*   **自适应正则化（Adaptive Regularization）**：
    *   为了平衡性能与效率，设计了损失函数 $L_{reg}$。
    *   **关键细节**：正则化权重是动态的，受两个因素调节：1. **NLL（负对数似然）**：如果模型预测当前 Token 感到困难（NLL 高），则降低正则化，倾向于使用更强的全注意力；2. **MSE（分支差异）**：如果两个分支的输出差异很大，说明全局信息至关重要，同样倾向于全注意力。
*   **持续预训练（Continual Pretraining, CPT）**：
    *   SwiAttn 不需要从零训练，而是从现有的全注意力预训练模型初始化，通过少量的持续预训练（如 20B tokens）即可完成架构转换。
*   **推理优化**：在解码阶段，两个分支共享同一个 KV Cache，路由器决定计算路径，从而显著减少内存访问。

#### 3. 实验设计
*   **数据集与场景**：
    *   **常识推理**：Lambada, Wikitext, BoolQ, PIQA 等 10 个数据集。
    *   **上下文检索**：SWDE, FDA, TriviaQA, SQuAD 等 6 个数据集（4K 长度）。
    *   **长文本理解**：LongBench-E 中的 GovReport, MultiNews, Qasper 等 7 个数据集（最高 32K 长度）。
*   **对比方法（Baselines）**：
    *   **FullAttn**：标准全注意力模型。
    *   **SWA-ZS / SWA-CPT**：滑动窗口注意力的零样本版本和持续预训练版本。
    *   **StaticHybrid**：工业界常用的静态混合模式（1:3 比例交替）。

#### 4. 资源与算力
*   **模型规模**：1.5B 参数量，22 层。
*   **算力平台**：实验在 **Ascend 910C NPUs**（华为昇腾）上完成。
*   **训练细节**：
    *   4K 基础模型预训练使用了 240B tokens。
    *   SwiAttn 的持续预训练（CPT）阶段使用了 20B tokens（4K 场景）或 8B tokens（32K 场景）。
    *   32K 场景下使用了 YaRN 技术进行长度扩展。
*   **说明**：文中未明确给出具体的 NPU 数量和总训练小时数，但提供了详细的迭代步数和 Token 总量。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 23 个主流基准数据集，测试了从短文本（4K）到长文本（32K）的跨度。
*   **消融实验**：针对自适应正则化中的 NLL 项、MSE 项以及超参数 $\alpha, \epsilon$ 进行了 7 组对比实验，验证了各组件的必要性。
*   **深度分析**：
    *   **大海捞针（NIAH）测试**：验证了在 32K 长度下的完美检索能力。
    *   **效率分析**：对比了 Prefill 和 Decode 阶段的计算量（GFLOPs）和内存访问。
    *   **路由分布分析**：统计了不同层对全注意力的偏好，发现平均全注意力比例仅为 13%（低于静态混合的 25%）。
*   **评价**：实验设计全面且客观，通过多维度的指标（准确率、困惑度、推理速度、路由比例）证明了方法的优越性。

#### 6. 主要结论与发现
*   **性能持平或领先**：SwiAttn 在长文本任务上超过了静态混合模型，并达到了与全注意力模型相当的水平。
*   **显著提速**：在 32K 上下文解码时，SwiAttn 相比全注意力模型实现了 **4 倍至 12 倍** 的加速。
*   **动态性验证**：实验发现，当答案（Needle）位于滑动窗口外时，模型会自动提高全注意力的使用比例；当答案在窗口内时，则自动切换到高效的 SWA。
*   **层级差异**：底层和顶层更倾向于使用 SWA，而中间层（如第 8, 11, 14 层）更依赖全注意力来整合信息。

#### 7. 优点（亮点）
*   **细粒度控制**：首次将混合注意力的选择精确到 Token 级别和层级别，实现了真正的动态计算分配。
*   **自适应正则化**：巧妙地利用模型自身的预测不确定性（NLL）和分支分歧（MSE）来指导路由，避免了盲目追求效率而损失精度。
*   **高兼容性**：支持从现有全注意力模型通过 CPT 快速转换，极大地降低了开发成本。

#### 8. 不足与局限
*   **硬件算子支持**：目前在 Prefill（预填充）阶段，由于需要分别计算两个分支再合并，缺乏专门优化的硬件内核（Kernel），实际加速效果受限。
*   **模型规模验证**：实验主要集中在 1.5B 模型上，尚未在百亿（7B+）或千亿级参数模型上验证其路由坍塌风险或扩展性。
*   **训练复杂度**：相比静态模型，动态路由增加了训练时的计算开销（需计算双分支输出以计算 MSE 损失）。

（完）
