---
title: Finding Distributed Object-Centric Properties in Self-Supervised Transformers
title_zh: 在自监督 Transformer 中发现分布式以物体为中心的属性
authors: "Samyak Rawlekar, Amitabh Swain, Yujun Cai, Yiwei Wang, Ming-Hsuan Yang, Narendra Ahuja"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26127v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 自监督视觉Transformer与注意力图
tldr: "本研究针对自监督视觉Transformer（如DINO）中[CLS]标记在物体定位上的局限性，发现物体中心信息实际上分布在所有层的patch级查询、键、值（q, k, v）相似度中。据此提出Object-DINO，一种无需训练的方法，通过聚类跨层注意力头来提取分布式的物体特征。该方法显著提升了无监督物体发现的准确性，并能有效缓解多模态大模型的物体幻觉问题。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26127v1/fig-001.webp\", \"caption\": \"Figure 4. Mitigating MLLM Hallucination with Object-DINO Visual Guidance. Our training-free decoding strategy computes two separate logit distributions. The standard branch uses the original image u and a general prompt Tu (e.g., \\\"describe this image\\\"). The guidance branch uses the Object-DINO’s object map v and a prompt Tv (e.g., \\\"describe the highlighted regions\\\"). Here, R is the text prompt generated so far. We then add these logits, Logits(y|Tu, R, u) + α · Logits(y|Tv, R, v), to amplify tokens consistent with the visual evidence, thus correcting the hallucinations (e.g., \\\"Two\\\" → \\\"Three\\\" dogs)\", \"page\": 6, \"index\": 1, \"width\": 496, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-002.webp\", \"caption\": \"Figure 8. Evaluation of optimal number of clusters K using DaviesBouldin Score (lower is better). We select K = 5 for all our experiments.\", \"page\": 11, \"index\": 2, \"width\": 487, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-003.webp\", \"caption\": \"Figure 14. Object-centric information in patch-level interactions from MAE [11]. We visualize the inter-patch similarity maps (Aq, Ak, Av) computed from the Query, Key, and Value representations of patch tokens in a MAE ViT-B/16 model. For visualization, we invert the similarity maps so objects appear bright.\", \"page\": 15, \"index\": 3, \"width\": 1052, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-004.webp\", \"caption\": \"Figure 5. Experimental results of MME on the hallucination subset\", \"page\": 7, \"index\": 4, \"width\": 487, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-005.webp\", \"caption\": \"Figure 13. Analysis of the object-centric head distribution in ViT-L, computed over 4,000 images from the COCO dataset. (a) The heatmap shows the frequency of heads (across all 24 layers) belonging to the object-centric cluster. The red boxes highlight that numerous heads in later layers (e.g., Layers 20-23) are consistently selected, demonstrating that object-centric information is a distributed phenomenon and not confined to the final layer. (b) The plot details the final layer’s contribution, showing that several heads (circled in black) have low frequency, confirming that some final-layer heads are \\\"noisy\\\" (non-object-centric). (c) The histogram shows the number of strongly active object-centric heads per layer, with Layer 23 containing 10 heads, Layer 22 containing 5 heads, and Layer 21 containing 1 head.\", \"page\": 14, \"index\": 5, \"width\": 923, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-006.webp\", \"caption\": \"Figure 2. Analysis of the object-centric head distribution, computed over 4,000 images from the COCO dataset. (a) The heatmap shows the frequency of heads (across all layers) belonging to the object-centric cluster. The red boxes highlight that numerous heads in intermediate layers (e.g., Layers 8–10) are consistently selected, proving object-centric information is a distributed phenomenon and not confined to the final layer. (b) The plot details the final layer’s contribution. It shows that several heads (circled in black) have a low frequency, confirming that some final-layer heads are \\\"noisy\\\" (non-object-centric). (c) The histogram shows the number of strongly active heads per layer.\", \"page\": 3, \"index\": 6, \"width\": 969, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-007.webp\", \"caption\": \"Figure 12. Final Layer Head Visualization. Ensemble similarity maps Aens for all 12 heads in Layer 11 of DINO-V3. For visualization, we invert the maps to show bright colors for objects.\", \"page\": 13, \"index\": 7, \"width\": 840, \"height\": 709}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-008.webp\", \"caption\": \"Figure 11. Evolution of localizations with temperature. For visualization, we invert the maps to show bright colors for objects.\", \"page\": 13, \"index\": 8, \"width\": 840, \"height\": 468}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-009.webp\", \"caption\": \"Figure 6. Impact of Attention Components on Head Selection. CorLoc performance across three datasets (VOC2007, VOC2012, COCO20k) when using different attention components (Q,K, V ) versus their ensemble to identify object-centric heads via ObjectDINO. The performance ordering is Q < V < K < Ensemble across all datasets. This demonstrates that Ensemble leads to identification of robust object-centric heads.\", \"page\": 8, \"index\": 9, \"width\": 481, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-010.webp\", \"caption\": \"Figure 7. Qualitative comparison of captions generated by LLava using regular decoding (red box) vs our method (green box).\", \"page\": 8, \"index\": 10, \"width\": 494, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-011.webp\", \"caption\": \"Figure 3. Object-DINO Overview. Our training-free algorithm identifies a distributed set of object-centric heads from a pre-trained models such as DINO. First, for every head across all layers, we compute the patch similarity maps from its query (Aq), key (Ak), and value (Av) representations. Second, these three maps are ensembled and flattened to create a vector representing each head’s localization pattern. Third, all heads are clustered based on these pattern. Fourth, guided by the established observation that object-centric information is most prevalent in the final layer [4, 23, 28], we automatically identify the object Cluster (cobj) using the criterion that it contains the highest prevalence of final-layer heads. Finally, aggregating the similarity maps from only the object-centric heads (Hobj) produces a high-fidelity object localization, effectively filtering noise from non-object-centric heads. For visualization, we invert the maps to show bright colors for objects.\", \"page\": 4, \"index\": 11, \"width\": 1036, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-012.webp\", \"caption\": \"Figure 9. Guidance weight ablation for MLLM hallucination mitigation. We observe that the Cs and Ci scores are lowest for α = 0.4, making it our optimal choice\", \"page\": 12, \"index\": 12, \"width\": 486, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-013.webp\", \"caption\": \"Figure 10. Tau ablation. We show the optimal choice of τ for object centric signal is ∈ (10, 100)\", \"page\": 12, \"index\": 13, \"width\": 487, \"height\": 303}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-014.webp\", \"caption\": \"Figure 15. Analysis of the object-centric head distribution in MAE [11], computed over 4,000 random images from the COCO dataset. (a) The heatmap shows the frequency of heads (across all 12 layers) belonging to the object-centric cluster identified by our k-means clustering (K=5). (b) The plot shows the final layer’s contribution, showing that some heads (circled in black) have low frequency. (c) The histogram shows the number of strongly active object-centric heads per layer.\", \"page\": 16, \"index\": 14, \"width\": 923, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2603.26127v1/fig-015.webp\", \"caption\": \"Figure 16. Qualitative comparison of captions generated by Regular decoding (Red) and Ours (Green)\", \"page\": 16, \"index\": 15, \"width\": 1022, \"height\": 400}]"
motivation: "现有的自监督ViT主要依赖[CLS]标记进行物体发现，但其全局聚合特性常导致定位不准且包含伪激活。"
method: 提出Object-DINO，通过计算所有层patch间的q/k/v相似度并对注意力头进行聚类，自动识别并提取分布式的物体中心信息。
result: 在无监督物体发现任务中获得3.6至12.4的CorLoc增益，并成功通过视觉定位缓解了多模态大模型的物体幻觉。
conclusion: 视觉Transformer内部蕴含着分布式的物体中心属性，充分利用这些跨层patch信息无需额外训练即可显著增强下游视觉任务。
---

## 摘要
DINO 等自监督视觉 Transformer (ViT) 展现出一种涌现的物体发现能力，这种能力通常在最后一层的 [CLS] 标记（token）注意力图中被观察到。然而，这些注意力图往往包含伪激活，导致物体定位效果不佳。这是因为基于图像级目标训练的 [CLS] 标记总结了整幅图像，而非专注于物体。这种聚合稀释了存在于局部 patch 级交互中的以物体为中心的信息。我们通过计算所有层中 patch 级注意力组件（查询 q、键 k 和值 v）之间的 patch 间相似度来分析这一现象。我们发现：(1) 以物体为中心的属性编码在源自所有三个组件（q, k, v）的相似度图中，这与以往仅使用键（key）特征或 [CLS] 标记的工作不同；(2) 这种以物体为中心的信息分布在整个网络中，而不仅仅局限于最后一层。基于这些见解，我们提出了 Object-DINO，这是一种无需训练的方法，用于提取这种分布式的以物体为中心的信息。Object-DINO 根据 patch 的相似性对所有层的注意力头进行聚类，并自动识别出对应于所有物体的以物体为中心的聚类。我们在两个应用上证明了 Object-DINO 的有效性：增强无监督物体发现（CorLoc 提升 +3.6 至 +12.4）以及通过提供视觉定位来缓解多模态大语言模型中的物体幻觉。我们的结果表明，利用这种分布式的以物体为中心的信息可以在无需额外训练的情况下改进下游任务。

## Abstract
Self-supervised Vision Transformers (ViTs) like DINO show an emergent ability to discover objects, typically observed in [CLS] token attention maps of the final layer. However, these maps often contain spurious activations resulting in poor localization of objects. This is because the [CLS] token, trained on an image-level objective, summarizes the entire image instead of focusing on objects. This aggregation dilutes the object-centric information existing in the local, patch-level interactions. We analyze this by computing inter-patch similarity using patch-level attention components (query, key, and value) across all layers. We find that: (1) Object-centric properties are encoded in the similarity maps derived from all three components ($q, k, v$), unlike prior work that uses only key features or the [CLS] token. (2) This object-centric information is distributed across the network, not just confined to the final layer. Based on these insights, we introduce Object-DINO, a training-free method that extracts this distributed object-centric information. Object-DINO clusters attention heads across all layers based on the similarities of their patches and automatically identifies the object-centric cluster corresponding to all objects. We demonstrate Object-DINO's effectiveness on two applications: enhancing unsupervised object discovery (+3.6 to +12.4 CorLoc gains) and mitigating object hallucination in Multimodal Large Language Models by providing visual grounding. Our results demonstrate that using this distributed object-centric information improves downstream tasks without additional training.

---

## 论文详细总结（自动生成）

### 论文总结：在自监督 Transformer 中发现分布式以物体为中心的属性

#### 1. 核心问题与整体含义（研究动机和背景）
自监督视觉 Transformer（如 DINO）展现出了无需人工标注即可发现物体的“涌现”能力。传统方法通常依赖模型最后一层的 `[CLS]` 标记（token）的注意力图。然而，由于 `[CLS]` 标记的训练目标是图像级的全局特征提取，它往往会聚合过多的背景上下文，导致物体定位不准或产生伪激活。
**核心动机**：作者认为以物体为中心的信息并非仅存在于 `[CLS]` 标记中，而是更直接地编码在局部 patch 之间的交互中。本研究旨在探索这些信息在网络内部的分布规律，并提出一种更精确、无需训练的物体提取方法。

#### 2. 方法论：Object-DINO
Object-DINO 是一种**无需训练（Training-free）**的方法，其核心思想是利用全网络所有层的注意力头信息。
*   **多组件相似度计算**：对于每个注意力头，分别计算 patch 级 Query (q)、Key (k) 和 Value (v) 的自相似度矩阵（$A_q, A_k, A_v$）。
*   **集成表示（Ensemble）**：将这三个矩阵加权平均，形成一个统一的集成相似度矩阵 $A_{ens}$。实验发现，q 揭示了“寻找相似信息”的 patch，k 揭示了“提供相似上下文”的 patch，而 v 揭示了“内容相似”的 patch，三者结合能有效滤除噪声。
*   **跨层注意力头聚类**：将全网络所有层的所有注意力头作为样本，根据其 $A_{ens}$ 特征进行 K-means 聚类。
*   **自动识别物体头**：基于“物体信息在最后一层最集中”的先验知识，自动选择包含最后一层注意力头比例最高的聚类作为“物体中心聚类”（$H_{obj}$）。
*   **应用流程**：在推理时，仅聚合属于 $H_{obj}$ 的注意力头的相似度图，从而生成高保度的物体定位掩码。

#### 3. 实验设计
研究在两个主要下游任务上验证了方法的有效性：
*   **无监督物体发现**：
    *   **数据集**：PASCAL VOC 2007、VOC 2012、COCO 20k。
    *   **基准方法**：以 TokenCut 为基础框架，对比了原始 DINO-v2 和 DINO-v3 的表现。
    *   **指标**：CorLoc（正确定位率）。
*   **缓解多模态大模型（MLLM）幻觉**：
    *   **数据集**：CHAIR（句子级/实例级幻觉）、POPE（物体存在性问答）、MME（幻觉子集）。
    *   **模型**：LLaVA-1.5、Instruct-BLIP、Qwen-VL。
    *   **对比方法**：VCD、M3ID、RITUAL、DeGF、MARINE 等先进的解码策略。

#### 4. 资源与算力
*   **算力说明**：论文提到获得了 NVIDIA 学术资助计划和 UIUC NCSA-Delta 集群的支持。
*   **训练/推理时长**：由于该方法是 **Training-free** 的，不需要重新训练模型。在 MLLM 任务中，作者提供了效率对比：Object-DINO 的推理延迟约为基准模型的 2.06 倍，远低于 DeGF（4.04 倍）或 HALC（6.51 倍），在显存占用上也仅有轻微增加（约 1.07 倍）。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多模型验证**：涵盖了 DINO-v2/v3 的不同规模（ViT-B, ViT-L），甚至测试了基于重构目标的 MAE 模型。
*   **消融实验**：详细探讨了 q/k/v 组件的贡献、集成权重、温度系数 $\tau$、聚类数 $K$ 以及不同层对性能的影响。
*   **数据规模**：分析基于 COCO 数据集中的 4000 张图像进行，确保了统计意义上的可靠性。
*   **公平性**：在相同框架（如 TokenCut）下仅更换特征提取方式，直接证明了分布式特征的优越性。

#### 6. 主要结论与发现
*   **分布式特性**：以物体为中心的信息并非只存在于最后一层，而是分布在网络的多个中间层（如 Layer 8-10）。
*   **组件互补性**：$A_q, A_k, A_v$ 均包含物体信息，且集成后的效果显著优于单一组件（尤其是以往常用的 Key 特征）。
*   **最后一层并非全优**：最后一层中约有 1/3 的注意力头实际上是“噪声头”，不包含物体定位信息，剔除它们能显著提升性能。
*   **性能提升**：在无监督物体发现中，CorLoc 最高提升了 12.4%；在 MLLM 任务中，显著降低了物体幻觉率。

#### 7. 优点
*   **无需训练**：作为一种后处理/分析工具，它可以直接应用于任何预训练的 ViT，极具实用价值。
*   **深入的机理分析**：揭示了 Transformer 内部注意力机制如何分工协作来表征物体。
*   **高效性**：相比于调用外部目标检测器（如 Grounding DINO）来缓解幻觉，Object-DINO 利用模型原生特征，速度更快且保留了空间连续性。

#### 8. 不足与局限
*   **实例分割限制**：目前的方法主要实现前景与背景的分离，尚无法在无监督条件下准确区分同一类别下的多个不同实例（Instance-level segmentation）。
*   **模型依赖**：虽然在 MAE 上也有发现，但该方法在 DINO 系列模型上的表现最为突出，说明其效果仍受限于预训练目标的质量。
*   **超参数敏感性**：虽然论文给出了推荐值，但聚类数 $K$ 和温度系数 $\tau$ 在不同架构的模型上可能需要微调。

（完）
