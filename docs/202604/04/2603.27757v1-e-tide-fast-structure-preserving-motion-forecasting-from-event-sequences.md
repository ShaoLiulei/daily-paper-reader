---
title: "E-TIDE: Fast, Structure-Preserving Motion Forecasting from Event Sequences"
title_zh: E-TIDE：基于事件序列的快速、结构保持运动预测
authors: "Biswadeep Sen, Benoit R. Cottereau, Nicolas Cuperlier, Terence Sim"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27757v1"
tags: ["query:snn"]
score: 9.0
evidence: 基于事件相机的序列运动预测
tldr: 针对事件相机捕捉的高速动态数据，本文提出了E-TIDE，一种轻量级且端到端可训练的事件张量预测架构。为了解决现有方法计算开销大、依赖大规模预训练的问题，E-TIDE引入了TIDE模块，通过大卷积核混合和活动感知门控机制实现高效的时空交互。该方法在保持低计算复杂度的同时，能有效捕捉时间依赖性，在标准数据集上实现了与SOTA相当的性能，且显著降低了模型参数量和训练需求，非常适合资源受限的实时部署场景。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27757v1/fig-001.webp\", \"caption\": \"TABLE I ETRAM COMPARISON ON 10→10 EVENT PREDICTION (BATCH SIZE 1). LOWER IS BETTER FOR MSE/LPIPS; HIGHER IS BETTER FOR SSIM/MIOU/AIOU. PEAK VRAM REPORTS MAX MEMORY ALLOCATED DURING INFERENCE ON RTX 3090. TYPE: RGB INDICATES RGB-ORIGIN VIDEO PREDICTORS RETRAINED ON EVENT OCCURRENCE MAPS; EVENT INDICATES EVENT-NATIVE PREDICTORS DESIGNED FOR EVENT DATA.\", \"page\": 6, \"index\": 1, \"width\": 1054, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2603.27757v1/fig-002.webp\", \"caption\": \"TABLE II E-3DTRACK COMPARISON ON 10→10 EVENT PREDICTION (BATCH SIZE 1). SAME METRICS AND MEASUREMENT PROTOCOL AS TABLE I.\", \"page\": 6, \"index\": 2, \"width\": 1054, \"height\": 207}, {\"url\": \"assets/figures/arxiv/2603.27757v1/fig-003.webp\", \"caption\": \"Fig. 3. Qualitative comparison on ETRAM. Example 10→10 forecasts showing input event frames and future predictions at t+1, t+5, and t+10. ETIDE produces motion-aligned, temporally consistent event structure that better preserves thin boundaries compared to the event-native baseline E-Motion. Red/blue indicate ON/OFF polarities.\", \"page\": 6, \"index\": 3, \"width\": 1052, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2603.27757v1/fig-004.webp\", \"caption\": \"Fig. 4. Accuracy–efficiency Pareto trade-offs. We plot aIoU (higher is better) against model size, inference latency, and peak VRAM (log-scale where indicated). Across all 3 plots, E-TIDE lies in the top-left frontier, ie. it beats the other models in size, latency and memory usage.\", \"page\": 7, \"index\": 4, \"width\": 1052, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2603.27757v1/fig-005.webp\", \"caption\": \"Fig. 5. Downstream tasks Qualitative results for micro-mobility, cars, and pedestrians. Predicted event frames remain structurally faithful to the ground truth, enabling reliable segmentation and tracking across diverse object categories and forecast horizons.\", \"page\": 7, \"index\": 5, \"width\": 1052, \"height\": 519}, {\"url\": \"assets/figures/arxiv/2603.27757v1/fig-006.webp\", \"caption\": \"Fig. 1. E-TIDE overview. Polarity-separated event occurrence maps X = {xt}Tin t=1 are encoded with shared weights to obtain per-step features et, which are packed along time into channels to form Z ∈ RB×D×H′×W ′ with D = TinCs for fully-parallel spatiotemporal processing. The interaction core C(·) is a stack of TIDE blocks: within each block we denote the input as U and output as U+ (with U = Z for the first block), and the stacked core maps Z to Z+ = C(Z). Unstack denotes a reshape/view of the packed tensor (time-in-channels) for visualization; the decoder operates on Z+. The decoder upsamples to future logits Ŝ ∈ RTout×2×H×W and probabilities Ŷ = σ(Ŝ). We detail the internal TIDE block design in Fig. 2.\", \"page\": 3, \"index\": 6, \"width\": 1010, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2603.27757v1/fig-007.webp\", \"caption\": \"Fig. 2. TIDE block (used in Fig. 1). Given packed features U, static mixing performs large-kernel depthwise spatial aggregation followed by a pointwise projection to produce mixed features F, while dynamic gating computes an activity mask (quantile q), pools only active regions, and generates a channel gate g via an MLP and sigmoid. The two paths fuse through the multiplicative residual interaction U+ = g ⊙ F⊙ (1+U).\", \"page\": 3, \"index\": 7, \"width\": 1052, \"height\": 227}]"
motivation: 现有的事件预测方法通常依赖沉重的骨干网络或大规模预训练，难以满足资源受限环境下的实时性需求。
method: 提出TIDE模块，利用大卷积核混合和活动感知门控机制，在保持低复杂度的前提下实现高效的时空特征交互。
result: 在标准事件数据集上的实验表明，该方法在显著减少模型尺寸和训练成本的情况下，达到了极具竞争力的预测性能。
conclusion: E-TIDE为实时、低功耗的事件序列运动预测提供了一种高效且结构保留的解决方案。
---

## 摘要
事件相机将视觉信息捕获为像素级亮度变化的异步流，从而生成稀疏且时间精确的数据。与传统的基于帧的传感器相比，它们在捕获高速动态方面具有显著优势，同时功耗大幅降低。从过去的观测中预测未来的事件表示是一个重要问题，它能够在无需获取未来传感器测量值的情况下，实现诸如未来语义分割或目标跟踪等下游任务。虽然最近的最先进方法取得了强大的性能，但它们通常依赖于计算量巨大的骨干网络，且在某些情况下需要大规模预训练，这限制了它们在资源受限场景中的适用性。在这项工作中，我们提出了 E-TIDE，这是一种用于事件张量预测的轻量级、端到端可训练架构，旨在无需大规模预训练即可高效运行。我们的方法采用了 TIDE 模块（动态事件的时间交互），该模块受稀疏事件张量的高效时空交互设计启发，通过大核混合和活动感知门控来捕获时间依赖性，同时保持较低的计算复杂度。在标准事件数据集上的实验表明，我们的方法在显著降低模型大小和训练要求的情况下，实现了具有竞争力的性能，使其非常适合在严格的延迟和内存预算下进行实时部署。

## Abstract
Event-based cameras capture visual information as asynchronous streams of per-pixel brightness changes, generating sparse, temporally precise data. Compared to conventional frame-based sensors, they offer significant advantages in capturing high-speed dynamics while consuming substantially less power. Predicting future event representations from past observations is an important problem, enabling downstream tasks such as future semantic segmentation or object tracking without requiring access to future sensor measurements. While recent state-of-the-art approaches achieve strong performance, they often rely on computationally heavy backbones and, in some cases, large-scale pretraining, limiting their applicability in resource-constrained scenarios. In this work, we introduce E-TIDE, a lightweight, end-to-end trainable architecture for event-tensor prediction that is designed to operate efficiently without large-scale pretraining. Our approach employs the TIDE module (Temporal Interaction for Dynamic Events), motivated by efficient spatiotemporal interaction design for sparse event tensors, to capture temporal dependencies via large-kernel mixing and activity-aware gating while maintaining low computational complexity. Experiments on standard event-based datasets demonstrate that our method achieves competitive performance with significantly reduced model size and training requirements, making it well-suited for real-time deployment under tight latency and memory budgets.

---

## 论文详细总结（自动生成）

这是一份关于论文《E-TIDE: Fast, Structure-Preserving Motion Forecasting from Event Sequences》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：事件相机（Event Cameras）以微秒级精度记录像素亮度变化，具有低延迟、高动态范围和低功耗的优势，非常适合高速运动场景。
*   **核心问题**：如何从过去的事件序列预测未来的事件张量（Motion Forecasting）。这对于自动驾驶、机器人避障等需要“预判”的实时任务至关重要。
*   **研究动机**：现有的事件预测方法（如基于扩散模型的 E-Motion）计算开销巨大、推理延迟高，且往往依赖大规模预训练；而传统的 RGB 视频预测模型直接迁移到稀疏的事件数据上效果不佳。本文旨在提出一种**轻量级、确定性、单次推理**的架构，以满足实时部署的需求。

### 2. 方法论：E-TIDE 架构
E-TIDE 采用“编码器-交互核心-解码器”的结构，核心思想是将时间维度压缩进通道维度，实现全并行处理。
*   **时间-通道打包（Time-to-Channel Packing）**：避免了循环神经网络（RNN）的串行更新，将 $T_{in}$ 个时间步的特征拼接在通道维度，使用 2D 卷积进行高效时空交互。
*   **TIDE 模块（核心创新）**：
    *   **大核深度卷积混合**：使用大尺寸深度卷积（如 $5\times5$ 和 $7\times7$ 扩张卷积）扩大感受野，捕捉空间上下文。
    *   **活动感知门控（Activity-aware Gating）**：针对事件数据的稀疏性，通过分位数阈值筛选出高活跃区域进行全局池化，生成通道门控信号，避免背景噪声主导特征。
    *   **乘法残差交互**：采用 $g \odot F \odot (1 + U)$ 的形式，增强活跃区域的梯度流动，同时保持不活跃区域的稳定。
*   **损失函数**：
    *   **极性感知加权 Focal Loss**：针对 ON/OFF 事件的极端不平衡，引入极性权重 $\lambda_c$。
    *   **时间差分正则化（DDR）**：约束预测帧与真实帧之间的一阶差分分布一致性，减少预测中的伪影和震荡。

### 3. 实验设计
*   **数据集**：
    *   **ETram**：长时、静态视角的城市交通监控数据。
    *   **E-3DTrack**：包含高速、非均匀运动的异构物体轨迹数据。
*   **实验设置**：输入 10 帧预测未来 10 帧（10 → 10）。
*   **对比方法（Benchmark）**：
    *   **事件原生方法**：E-Motion（基于扩散模型）。
    *   **RGB 迁移方法**：PredRNN, SimVP, MIMO-VP, MMVP, MGVP（均在事件数据上重新训练）。
*   **评价指标**：像素级指标（MSE, SSIM, LPIPS）和结构重叠指标（mIoU, aIoU）。

### 4. 资源与算力
*   **硬件平台**：单张 **NVIDIA RTX 3090 GPU**。
*   **训练细节**：Batch Size 为 4，使用 Adam 优化器。
*   **推理效率**：E-TIDE 的推理延迟仅为 **3.1 ms**，峰值显存占用仅 **0.12 GB**。
*   **训练时长**：文中未明确给出具体的总训练小时数，但强调了其无需大规模预训练。

### 5. 实验数量与充分性
*   **实验规模**：在两个具有代表性的不同场景数据集上进行了全面测试。
*   **消融实验**：针对极性感知损失（PAFL）、活动感知池化、乘法残差和 DDR 正则化项分别做了消融，验证了各组件的有效性。
*   **下游任务验证**：将预测结果输入到分割（Segmentation）和跟踪（Tracking）算法中，验证了预测结果的实用价值。
*   **评价客观性**：实验对比了多种不同架构（RNN、CNN、Transformer、Diffusion），且使用了自动阈值处理（Otsu）来确保 IoU 计算的公平性。

### 6. 主要结论与发现
*   **性能领先**：E-TIDE 在结构保持指标（aIoU/mIoU）上显著优于现有方法，尤其在 ETram 数据集上比 E-Motion 提升了约 18% 的 mIoU。
*   **极致效率**：相比于 E-Motion，参数量减少了约 **3800倍**（0.4M vs 1521M），推理速度快了约 **2700倍**。
*   **结构保持**：实验证明，对于稀疏事件预测，保持运动结构的完整性比单纯追求像素级 MSE 更重要。

### 7. 优点与亮点
*   **部署友好**：是目前唯一能满足严格实时延迟和内存预算的事件预测模型。
*   **针对性设计**：TIDE 模块中的活动感知门控和乘法残差设计精准捕捉了事件数据的稀疏特性。
*   **多功能性**：预测结果在下游分割和跟踪任务中表现优异，证明了其作为感知前端的潜力。

### 8. 不足与局限
*   **实例融合问题**：在极端情况下（如两个车辆靠得非常近），模型可能会将两个实例的预测结果融合在一起（见文中 Failure Case）。
*   **场景限制**：目前主要针对固定相机视角（如交通监控），对于剧烈相机自运动（Ego-motion）场景的泛化能力尚待进一步验证。
*   **分辨率限制**：实验主要在 $128\times128$ 分辨率下进行，更高分辨率下的表现未充分展示。

（完）
