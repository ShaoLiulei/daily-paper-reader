---
title: "Stochastic Attention: Connectome-Inspired Randomized Routing for Expressive Linear-Time Attention"
title_zh: 随机注意力：受连接组学启发的随机化路由，用于高表达能力的线性时间注意力
authors: "Zehao Jin, Yanan Sui"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00754v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 受连接组启发的线性时间注意力随机路由
tldr: 受果蝇脑连接组启发，本文提出随机注意力（SA），通过在滑动窗口注意力前对序列进行随机排列，将局部窗口转化为随机全局窗口。该方法在保持线性时间复杂度的同时，使感受野随深度指数级增长，显著提升了长程通信效率。实验证明 SA 在语言模型预训练和免训练推理任务中均优于传统滑动窗口方法，是提升线性时间注意力表达能力的有效手段。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00754v1/fig-001.webp\", \"caption\": \"Table 3: Comparison of attention mechanisms. n: sequence length, w: window size, ℓ: number of layers.\", \"page\": 15, \"index\": 1, \"width\": 826, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.00754v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of Stochastic Attention (SA). (a) A standard SWA Transformer layer. (b) The fruit fly whole-brain connectome: the adjacency matrix, shown after Reverse Cuthill– McKee reordering to expose block structure, lacks clear diagonal blocks, indicating that connections are broadly distributed across brain regions rather than confined to local modules. (c) An SA layer: token sequences are randomly permuted before windowed attention and restored afterward, producing stochastic long-range shortcuts analogous to the cross-regional connections in (b).\", \"page\": 2, \"index\": 2, \"width\": 826, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2604.00754v1/fig-003.webp\", \"caption\": \"Figure 3: Attention weight visualization (Layer 11, Head 0) on a 27-token sequence with window size w=8. Gray regions are masked (structurally invisible). Blue intensity indicates attention weight. Full Attention exhibits the complete lower-triangular pattern. SWA shows a strict diagonal band with all out-of-window positions masked. Stochastic Attention introduces scattered non-zero entries beyond the diagonal band. These are distant tokens that became local neighbors after random permutation, enabling direct long-range information flow within the same O(nw) budget. SA + SWA combines both patterns: the SWA path provides the dense diagonal band for local coherence, while the SA path adds stochastic long-range connections, with the learned gate adaptively balancing the two.\", \"page\": 7, \"index\": 3, \"width\": 829, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2604.00754v1/fig-004.webp\", \"caption\": \"Table 4: Training-free inference on Qwen3-8B. We report accuracy on 7 benchmarks at selected window sizes. Best result per column among efficient methods in bold, underlined denotes second best.\", \"page\": 18, \"index\": 4, \"width\": 823, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2604.00754v1/fig-005.webp\", \"caption\": \"Figure 8: Per-task accuracy vs. effective window size for Qwen3-30B-A3B across all evaluated benchmarks.\", \"page\": 18, \"index\": 5, \"width\": 829, \"height\": 469}, {\"url\": \"assets/figures/arxiv/2604.00754v1/fig-006.webp\", \"caption\": \"Figure 4: Average accuracy across 7 benchmarks as a function of effective window size for Qwen3-8B (left) and Qwen3-30B-A3B (right). Stochastic Attention (red) recovers the full-attention baseline (dashed gray) most rapidly as window size increases, consistently outpacing SWA (blue) and matching or exceeding MoBA (green) at comparable compute budgets.\", \"page\": 8, \"index\": 6, \"width\": 664, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2604.00754v1/fig-007.webp\", \"caption\": \"Figure 2: Left: Receptive field coverage as a function of depth (n=2048, w=32). SA achieves full sequence coverage in O(logw n) layers via exponential growth, while SWA requires O(n/w) layers with linear growth. Right: Computational cost scaling with sequence length (w=256). Both SA and SWA maintain O(nw) linear scaling, while full attention grows quadratically.\", \"page\": 4, \"index\": 7, \"width\": 664, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.00754v1/fig-008.webp\", \"caption\": \"Table 5: Training-free inference on Qwen3-30B-A3B. Same setup as Table 4.\", \"page\": 19, \"index\": 8, \"width\": 827, \"height\": 917}, {\"url\": \"assets/figures/arxiv/2604.00754v1/fig-009.webp\", \"caption\": \"Figure 6: Per-task accuracy vs. window size on Qwen3-30B-A3B for four representative benchmarks.\", \"page\": 9, \"index\": 9, \"width\": 829, \"height\": 259}]"
motivation: 借鉴生物脑连接组中随机长程连接实现高效全局通信的特性，解决滑动窗口注意力感受野增长缓慢的问题。
method: 在滑动窗口注意力前后分别执行随机排列和逆排列，以线性复杂度实现随机化的全局信息路由。
result: SA 在预训练任务中取得了最佳的零样本准确率，并在 Qwen3 模型上的免训练推理中显著优于 SWA 等基准方法。
conclusion: 随机路由是一种受生物启发的实用原语，能有效增强高效注意力机制的表达能力，且与现有线性或稀疏方法互补。
---

## 摘要
果蝇的全脑连接组包含超过 13 万个神经元，连接概率仅为 0.02%，但平均最短路径仅为 4.4 跳。尽管在回路层面具有高度结构化，但该网络的远距离连接广泛分布在各个脑区，充当随机捷径，从而实现高效的全局通信。受此启发，我们提出了随机注意力（Stochastic Attention, SA），这是一种针对滑动窗口注意力（SWA）的即插即用增强方案。它在进行窗口注意力计算前对 token 序列应用随机排列，并在计算后恢复原始顺序。这在相同的每层 $O(nw)$ 预算内，将固定的局部窗口转变为随机的全局窗口。随着深度增加，独立采样的排列会产生指数级增长的感受野，在 $O(\log_w n)$ 层内即可实现全序列覆盖，而 SWA 则需要 $O(n/w)$ 层。我们在两种设置下验证了 SA：一是从零开始预训练语言模型，其中门控 SA + SWA 组合实现了最佳的平均零样本准确率；二是在 Qwen3-8B 和 Qwen3-30B-A3B 上进行免训练推理，在同等计算预算下，SA 的表现始终优于 SWA，并达到或超过了混合块注意力（Mixture of Block Attention）。这些结果表明，受连接组启发的随机路由是提高高效注意力表达能力的实用原语，可与现有的线性及稀疏方法互补。

## Abstract
The whole-brain connectome of a fruit fly comprises over 130K neurons connected with a probability of merely 0.02%, yet achieves an average shortest path of only 4.4 hops. Despite being highly structured at the circuit level, the network's long-range connections are broadly distributed across brain regions, functioning as stochastic shortcuts that enable efficient global communication. Inspired by this observation, we propose Stochastic Attention (SA), a drop-in enhancement for sliding-window attention (SWA) that applies a random permutation to the token sequence before windowed attention and restores the original order afterward. This transforms the fixed local window into a stochastic global one within the same $O(nw)$ per-layer budget. Through depth, independently sampled permutations yield exponentially growing receptive fields, achieving full sequence coverage in $O(\log_w n)$ layers versus $O(n/w)$ for SWA. We validate SA in two settings: pre-training language models from scratch, where a gated SA + SWA combination achieves the best average zero-shot accuracy, and training-free inference on Qwen3-8B and Qwen3-30B-A3B, where SA consistently outperforms SWA and matches or exceeds Mixture of Block Attention at comparable compute budgets. These results suggest that connectome-inspired stochastic routing is a practical primitive for improving the expressivity of efficient attention, complementary to existing linear and sparse approaches.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stochastic Attention: Connectome-Inspired Randomized Routing for Expressive Linear-Time Attention》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保持线性时间复杂度（$O(nw)$）的同时，解决滑动窗口注意力（SWA）感受野增长缓慢（线性增长 $O(n/w)$）的问题，从而实现高效的全局信息路由。
*   **研究背景**：
    *   **生物学启发**：果蝇脑连接组虽然连接极度稀疏（0.02%），但通过分布式的“随机捷径”实现了极短的平均路径长度（4.4跳）。
    *   **现有技术局限**：SWA 虽然计算高效，但其确定性的局部性限制了长程通信；而全注意力（Full Attention）虽具备全局视野，但计算成本随序列长度呈平方级增长。

### 2. 方法论：随机注意力 (SA)
*   **核心思想**：在执行滑动窗口注意力之前，对 token 序列进行随机排列（Permutation），计算后再恢复原始顺序。
*   **关键技术细节**：
    *   **三阶段流程**：
        1.  **排列 (Permute)**：使用随机排列矩阵 $P_\sigma$ 重排 Query、Key、Value。
        2.  **窗口注意力**：在排列后的空间执行标准的 $O(nw)$ 滑动窗口注意力。
        3.  **逆排列 (Undo Permute)**：使用 $P_{\sigma^{-1}}$ 恢复原始 token 顺序。
    *   **感受野扩展**：通过数学证明，独立采样的随机排列使感受野随深度呈指数级增长，仅需 $O(\log_w n)$ 层即可覆盖全序列。
    *   **门控组合 (SA + SWA)**：提出一种双路径架构，通过学习到的门控权重融合 SA（提供全局捷径）和 SWA（保持局部相干性），模拟生物脑的“小世界”特性。
    *   **因果掩码适配**：在排列空间中应用因果约束，确保自回归生成的合法性。

### 3. 实验设计
*   **实验场景**：
    1.  **从零预训练**：训练 360M 参数的 Transformer 模型，使用 SlimPajama 数据集（15B tokens）。
    2.  **免训练推理 (Training-free Inference)**：将 SA 直接应用于预训练好的 Qwen3-8B 和 Qwen3-30B-A3B 模型，测试其作为 SWA 替代方案的效果。
*   **对比方法 (Benchmarks)**：
    *   Full Attention（全注意力基准）
    *   SWA（滑动窗口注意力）
    *   MoBA（Mixture of Block Attention，混合块注意力）
*   **评估指标**：WikiText/LAMBADA 困惑度，以及 MMLU、HellaSwag、ARC、PIQA、BoolQ、HumanEval 等下游任务的准确率。

### 4. 资源与算力
*   **硬件使用**：预训练阶段使用了 **4 张 NVIDIA A100 80GB GPU**。
*   **训练细节**：序列长度 2048，batch size 16，梯度累积 30 步。
*   **效率分析**：在 A100 上进行了推理延迟测试，证明 SA 在 32k 序列长度下比全注意力快 28 倍，且与 SWA 耗时基本持平。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 360M 小模型预训练到 30B 大模型推理的跨度。
*   **消融与对比**：对比了纯 SA、纯 SWA 以及 SA+SWA 组合；在推理实验中对窗口大小 $w$ 进行了详尽的扫描（16 到 512）。
*   **充分性评价**：实验设计较为充分且客观。通过在不同规模模型和多种性质任务（知识、推理、代码生成）上的验证，证明了该方法的普适性。

### 6. 主要结论与发现
*   **性能优越**：SA + SWA 组合在预训练任务中取得了最佳的平均零样本准确率，甚至在某些任务上超过了全注意力模型。
*   **推理增强**：在对 Qwen3 进行免训练替换时，SA 恢复全注意力性能的速度远快于 SWA，且在同等计算预算下优于 MoBA。
*   **理论验证**：实验结果验证了 $O(\log_w n)$ 的感受野覆盖理论，证明随机路由是实现全局信息交换的高效手段。

### 7. 优点与亮点
*   **参数高效**：SA 本身不增加任何可学习参数，仅通过索引重排实现。
*   **即插即用**：可以作为现有 SWA 实现（如 Mistral, Gemma 2）的直接升级方案，无需改变底层内核。
*   **生物启发**：将复杂的连接组学原理简化为简洁的数学原语（随机排列），具有很强的跨学科解释力。

### 8. 不足与局限
*   **局部性损失**：纯 SA 会破坏语言建模所需的局部相干性（导致困惑度上升），必须依赖 SWA 补足。
*   **双路径开销**：SA + SWA 组合虽然复杂度仍为线性，但实际计算量和显存占用约为单路径 SWA 的两倍。
*   **随机性风险**：虽然实验中未观察到显著方差，但推理时的随机排列可能在极端长序列或特定任务下引入不稳定性。

（完）
