---
title: Improving Sparse Memory Finetuning
title_zh: 改进稀疏记忆微调
authors: "Satyam Goyal, Anirudh Kanchi, Garv Shah, Prakhar Gupta"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05248v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于LLM持续适配的稀疏记忆微调
tldr: 针对大语言模型在持续学习中面临的灾难性遗忘问题，本文提出了一种改进的稀疏记忆微调（SMF）方法。研究团队开发了一个开源流水线，将稀疏记忆模块集成到预训练模型中，并引入了基于KL散度的槽位选择机制，优先更新信息量大的“惊奇”标记。该方法在消费级硬件上实现了高效的知识获取，同时显著减少了对已有能力的干扰，验证了稀疏更新在实际应用中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05248v1/fig-001.webp\", \"caption\": \"Figure 2: Recovery phase training loss on the instruction dataset (using TF-IDF).\", \"page\": 4, \"index\": 1, \"width\": 498, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-002.webp\", \"caption\": \"Figure 3: Sparse update mechanism: only retrieved key/value entries receive gradients.\", \"page\": 4, \"index\": 2, \"width\": 582, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-003.webp\", \"caption\": \"Figure 4: Finetuning performance across datasets. (a) TriviaQA training F1, (b) Natural Questions evaluation F1 during recovery, and (c) GSM8K evaluation loss for full finetuning and sparse memory variants.\", \"page\": 6, \"index\": 3, \"width\": 826, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-004.webp\", \"caption\": \"Figure 5: Finetuning performance when adapting to SimpleQA. (a) TriviaQA training F1, (b) Natural Questions evaluation F1 during recovery, and (c) GSM8K evaluation loss for full finetuning and sparse finetuning variants.\", \"page\": 6, \"index\": 4, \"width\": 826, \"height\": 229}]"
motivation: 传统的全量微调或参数高效微调方法在更新模型知识时，容易因修改共享的稠密表示而导致严重的灾难性遗忘。
method: 提出一种开源流水线，通过在预训练模型中嵌入稀疏记忆模块，并利用基于KL散度的机制来选择性地更新包含新信息的特定参数槽位。
result: 实验证明，该方法使模型能够在获取新事实知识的同时，极大地保留了原有的各项能力，且支持在消费级硬件上运行。
conclusion: 改进后的稀疏记忆微调通过局部化参数更新，为解决大语言模型的持续学习与知识更新挑战提供了一种高效且实用的方案。
---

## 摘要
大语言模型（LLMs）在训练后通常是静态的，但实际应用要求模型能够持续适应新知识，且不降低现有能力。标准的模型更新方法，如全量微调或参数高效方法（如 LoRA），面临着一个根本性的权衡：灾难性遗忘。这些方法修改了共享的稠密表示，从而导致任务间的干扰。稀疏记忆微调（SMF）提供了一种极具前景的替代方案，它通过将更新定位到显式记忆层中的一小部分参数子集来实现。在本研究中，我们提出了一个开源流水线，用于为现有的预训练模型（Qwen-2.5-0.5B）改造稀疏记忆模块，从而在消费级硬件上实现有效的持续学习。我们通过引入一种基于 Kullback-Leibler (KL) 散度的、具有理论依据的插槽选择机制扩展了先前的工作，该机制优先针对相对于背景分布而言具有信息“惊奇性”的标记进行记忆更新。我们的实验表明，改造后的模型能够获取新的事实知识，同时对保留能力的遗忘极小，从而在实际场景中验证了稀疏更新假设。

## Abstract
Large Language Models (LLMs) are typically static after training, yet real-world applications require continual adaptation to new knowledge without degrading existing capabilities. Standard approaches to updating models, like full finetuning or parameter-efficient methods (e.g., LoRA), face a fundamental trade-off: catastrophic forgetting. They modify shared dense representations, causing interference across tasks. Sparse Memory Finetuning (SMF) offers a promising alternative by localizing updates to a small subset of parameters in explicit memory layers. In this work, we present an open-source pipeline to retrofit existing pretrained models (Qwen-2.5-0.5B) with sparse memory modules, enabling effective continual learning on consumer hardware. We extend prior work by introducing a theoretically grounded slot-selection mechanism based on Kullback-Leibler (KL) divergence, which prioritizes memory updates for informationally "surprising" tokens relative to a background distribution. Our experiments demonstrate that our retrofitted models can acquire new factual knowledge with minimal forgetting of held-out capabilities, validating the sparse update hypothesis in a practical setting.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Sparse Memory Finetuning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）在部署后通常是静态的，但现实世界的信息是动态变化的。传统的更新方法（如全量微调或 LoRA）在引入新知识时，会修改模型共享的稠密表示（Dense Representations），导致**灾难性遗忘**——即新知识的获取会干扰并破坏模型原有的能力。
本文旨在解决这一“稳定性-塑性”权衡问题。研究者基于**稀疏记忆微调（SMF）**的概念，探索如何通过局部化更新（仅修改极少数特定参数）来让模型在不丢失旧能力的前提下学习新事实。

### 2. 方法论：核心思想与关键技术
论文提出了一套将标准 Transformer 改造为稀疏记忆增强模型的流水线，核心思想是将前馈网络（FFN）替换为**键值对（Key-Value）记忆层**。

*   **核心架构：** 将 Qwen-2.5-0.5B 的部分 FFN 层替换为记忆层。每个记忆层包含一个查询投影矩阵 $W_q$、一组可训练的键 $K$ 和值 $V$。前向传播时，仅检索与查询向量最匹配的 $k$ 个槽位（Top-k），实现计算的稀疏性。
*   **三阶段改造流水线：**
    1.  **改造（Retrofitting）：** 移除特定层的 FFN，插入随机初始化的记忆模块。
    2.  **恢复（Healing）：** 在通用指令数据集上微调记忆参数，使模型恢复基础的语言生成能力。
    3.  **任务特定微调：** 在目标任务上进行稀疏更新。
*   **信息论槽位选择机制（关键创新）：** 
    *   为了决定更新哪些记忆槽位，作者对比了传统的 **TF-IDF 启发式方法**和新提出的 **KL 散度评分法**。
    *   **KL 散度法：** 计算当前批次数据对槽位的调用分布与背景分布之间的 KL 散度。优先更新那些在当前任务中表现出“惊奇性”（即调用频率显著偏离背景分布）的槽位，从而更精准地定位任务相关参数。
*   **梯度掩码（Gradient Masking）：** 在更新时，通过钩子函数（Hook）将未选中槽位的梯度清零，确保只有选中的稀疏参数被修改。

### 3. 实验设计
*   **基础模型：** Qwen-2.5-0.5B-Instruct。
*   **数据集：**
    *   **恢复阶段：** OpenAssistant (oasst1) 抽取 2 万条样本。
    *   **目标任务（新知识学习）：** TriviaQA 和 SimpleQA（各取 1k 样本）。
    *   **保留能力评估（遗忘测试）：** GSM8K（数学推理）和 NaturalQuestions（通用问答）。
*   **对比基准（Baselines）：**
    *   **全量微调（Full Finetuning）：** 更新所有稠密权重。
    *   **稀疏记忆微调（SMF）：** 包含基于 TF-IDF 和基于 KL 散度的两种变体。

### 4. 资源与算力
*   **硬件环境：** 论文明确提到该流水线旨在“消费级硬件”上运行。
*   **具体算力：** 文中未明确给出具体的 GPU 型号、数量或总训练时长。但考虑到模型规模（0.5B）和样本量（1k-20k），该实验在单张中高端消费级显卡（如 RTX 3090/4090）上即可完成。

### 5. 实验数量与充分性
*   **实验规模：** 论文针对两个主要的目标任务（TriviaQA, SimpleQA）进行了完整的对比实验，并监控了两个不同维度的保留能力指标。
*   **充分性评价：** 实验设计较为合理，涵盖了从模型改造、能力恢复到最终任务适配的全流程。通过对比全量微调，清晰地展示了稀疏更新在抑制遗忘方面的优势。然而，实验主要集中在 0.5B 的小模型上，且目标任务的样本量较小（1k），对于更大规模模型或更复杂任务的泛化性验证略显不足。

### 6. 主要结论与发现
*   **稀疏更新的优越性：** 相比全量微调，SMF 能在更短的步数内实现对新任务的快速适配（高塑性），同时在 GSM8K 等推理任务上的性能下降远小于全量微调（高稳定性）。
*   **KL 评分的作用：** KL 散度评分机制在处理噪声较大或需要强正则化的任务（如 SimpleQA）时表现更优，能有效稳定学习过程。
*   **灾难性遗忘的证实：** 实验再次证明，全量微调会导致模型推理能力（GSM8K）的严重退化。

### 7. 优点与亮点
*   **开源与实用性：** 提供了一套可复现的开源流水线，允许开发者在不依赖闭源架构的情况下改造现有模型。
*   **理论支撑：** 引入 KL 散度作为槽位选择依据，比传统的 TF-IDF 更有信息论基础，能够动态识别任务特异性信息。
*   **高效性：** 证明了在极小参数更新量下（仅更新选中的记忆槽位）依然能获得强大的知识获取能力。

### 8. 不足与局限
*   **模型规模局限：** 实验仅限于 0.5B 参数的模型，尚未在大规模模型（如 7B 或 70B）上验证其扩展性。
*   **性能损失：** 改造初期的“恢复阶段”虽然能找回大部分能力，但相比原始稠密模型，改造后的模型在某些基准上可能仍存在微小的性能缺口。
*   **超参数敏感：** 槽位选择的数量 $T$ 和背景分布的计算方式可能对最终效果有较大影响，文中未深入讨论这些超参数的敏感度。

（完）
