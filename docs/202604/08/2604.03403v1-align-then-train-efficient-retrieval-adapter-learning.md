---
title: "Align then Train: Efficient Retrieval Adapter Learning"
title_zh: 先对齐后训练：高效检索适配器学习
authors: "Seiji Maekawa, Moin Aminnaseri, Pouya Pezeshkpour, Estevam Hruschka"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03403v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于指令遵循的高效检索适配器学习
tldr: 针对复杂查询与简单文档之间的检索不匹配问题，本文提出了一种高效检索适配器（ERA）框架。该框架采用“先对齐后训练”的两阶段策略，通过自监督对齐和监督适配，将强大的大型查询编码器与轻量级文档编码器结合。ERA在无需重新索引语料库的情况下，显著提升了低标注样本场景下的检索性能，在126个检索任务中表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03403v1/fig-001.webp\", \"caption\": \"Table 2: nDCG@10 (%) on the MAIR benchmark. We use 20% of labeled query-document pairs. Bold indicates the best result per model and domain. Emb. Adapter indicates Embedding Adapter. All models use the same model for both query and document encoding.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 440}, {\"url\": \"assets/figures/arxiv/2604.03403v1/fig-002.webp\", \"caption\": \"Table 7: nDCG@10 (%) on the MAIR benchmark with 5% of labeled query-document pairs.\", \"page\": 15, \"index\": 2, \"width\": 826, \"height\": 440}, {\"url\": \"assets/figures/arxiv/2604.03403v1/fig-003.webp\", \"caption\": \"Table 8: nDCG@10 (%) on the MAIR benchmark with 10% of labeled query-document pairs.\", \"page\": 15, \"index\": 3, \"width\": 736, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2604.03403v1/fig-004.webp\", \"caption\": \"Figure 4: nDCG@10 of zero-shot, ERA w/o adaptation, Embedding Adapter, and ERA retrieval at varying train ratios, where we use Qwen3-8B as the query embedder.\", \"page\": 7, \"index\": 4, \"width\": 816, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2604.03403v1/fig-005.webp\", \"caption\": \"Table 5: Recall@100 (%) on the MAIR benchmark with 20% of labeled query-document pairs.\", \"page\": 14, \"index\": 5, \"width\": 826, \"height\": 440}, {\"url\": \"assets/figures/arxiv/2604.03403v1/fig-006.webp\", \"caption\": \"Table 6: MAP@100 (%) on the MAIR benchmark with 20% of labeled query-document pairs.\", \"page\": 14, \"index\": 6, \"width\": 736, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2604.03403v1/fig-007.webp\", \"caption\": \"Table 1: Comparison of the proposal, ERA, with existing methods.\", \"page\": 3, \"index\": 7, \"width\": 826, \"height\": 232}, {\"url\": \"assets/figures/arxiv/2604.03403v1/fig-008.webp\", \"caption\": \"Figure 2: The overview of Efficient Retrieval Adapter.\", \"page\": 4, \"index\": 8, \"width\": 744, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.03403v1/fig-009.webp\", \"caption\": \"Table 9: nDCG@10 (%) on the MAIR benchmark with 40% of labeled query-document pairs.\", \"page\": 16, \"index\": 9, \"width\": 826, \"height\": 440}, {\"url\": \"assets/figures/arxiv/2604.03403v1/fig-010.webp\", \"caption\": \"Figure 7: nDCG@10 of zero-shot, symmetric ERA and asymmetric ERA retrieval with varying train ratios, where we use Qwen3-8B as the query embedder.\", \"page\": 16, \"index\": 10, \"width\": 821, \"height\": 198}]"
motivation: 现有的稠密检索系统在处理复杂指令查询时，面临大型模型微调成本高昂与轻量级模型理解能力不足之间的矛盾。
method: 提出ERA框架，通过自监督阶段对齐不同规模模型的嵌入空间，再通过监督阶段利用少量标签适配查询表示。
result: 在MAIR基准测试的126个任务中，ERA在低标注环境下超越了依赖大量数据的传统方法，实现了跨域的高效检索。
conclusion: ERA证明了通过适配器连接异构编码器是解决复杂检索任务的一种资源高效且性能强大的方案。
---

## 摘要
稠密检索系统日益需要处理复杂的查询。在许多现实场景中，用户通过长指令或特定任务描述来表达意图，而目标文档则保持相对简单且静态。这种不对称性导致了检索失配：理解查询可能需要强大的推理和指令遵循能力，而高效的文档索引则更倾向于轻量级编码器。现有的检索系统通常通过直接改进嵌入模型来解决这种失配，但微调大型嵌入模型以更好地遵循此类指令在计算上是昂贵的，且耗费内存，操作负担重。为了应对这一挑战，我们提出了高效检索适配器（ERA），这是一个标签高效的框架，分两个阶段训练检索适配器：自监督对齐和有监督适配。受大语言模型（LLM）预训练和有监督微调阶段的启发，ERA 首先对齐大型查询嵌入器和轻量级文档嵌入器的嵌入空间，然后利用有限的标注数据来适配查询侧表示，在无需重新索引语料库的情况下，弥合了嵌入模型之间的表示差距以及复杂查询与简单文档之间的语义差距。在包含 6 个领域、126 个检索任务的 MAIR 基准测试上的实验表明，ERA 提升了低标签设置下的检索性能，优于依赖大量标注数据的方法，并能跨领域有效地将更强的查询嵌入器与较弱的文档嵌入器相结合。

## Abstract
Dense retrieval systems increasingly need to handle complex queries. In many realistic settings, users express intent through long instructions or task-specific descriptions, while target documents remain relatively simple and static. This asymmetry creates a retrieval mismatch: understanding queries may require strong reasoning and instruction-following, whereas efficient document indexing favors lightweight encoders. Existing retrieval systems often address this mismatch by directly improving the embedding model, but fine-tuning large embedding models to better follow such instructions is computationally expensive, memory-intensive, and operationally burdensome. To address this challenge, we propose Efficient Retrieval Adapter (ERA), a label-efficient framework that trains retrieval adapters in two stages: self-supervised alignment and supervised adaptation. Inspired by the pre-training and supervised fine-tuning stages of LLMs, ERA first aligns the embedding spaces of a large query embedder and a lightweight document embedder, and then uses limited labeled data to adapt the query-side representation, bridging both the representation gap between embedding models and the semantic gap between complex queries and simple documents without re-indexing the corpus. Experiments on the MAIR benchmark, spanning 126 retrieval tasks across 6 domains, show that ERA improves retrieval in low-label settings, outperforms methods that rely on larger amounts of labeled data, and effectively combines stronger query embedders with weaker document embedders across domains.

---

## 论文详细总结（自动生成）

这是一份关于学术论文《Align then Train: Efficient Retrieval Adapter Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是稠密检索系统中的**“查询-文档不对称性”**问题。在实际应用中，用户查询往往包含复杂的长指令或任务描述，需要强大的推理能力（通常由大型 LLM 嵌入模型提供）；而文档库通常规模巨大且相对静态，为了检索效率，往往使用轻量级编码器进行索引。
*   **痛点**：直接微调大型嵌入模型成本极高，且如果更换模型，需要对海量文档重新进行向量化索引（Re-indexing），这在工程上难以接受。
*   **目标**：在不更新骨干模型参数、不重新索引语料库的前提下，如何高效地弥合不同模型间的表示差距以及复杂查询与简单文档间的语义差距。

### 2. 方法论：核心思想与技术流程
论文提出了 **ERA (Efficient Retrieval Adapter)** 框架，借鉴了 LLM “预训练+微调”的思想，将适配器的训练分为两个阶段：
*   **核心思想**：通过一个轻量级的线性适配器 $W$（矩阵），将查询侧的嵌入空间映射到文档侧的嵌入空间。
*   **阶段一：自监督对齐 (Self-supervised Alignment)**
    *   **流程**：从目标语料库中提取无标注文档，分别通过查询编码器 $E_q$ 和文档编码器 $E_d$ 生成向量。
    *   **目标**：训练 $W$ 使得 $E_q(d)W$ 与 $E_d(d)$ 的余弦相似度最大化。这一步旨在消除不同模型架构或维度带来的“表示鸿沟”。
*   **阶段二：有监督适配 (Supervised Adaptation)**
    *   **流程**：使用极少量（如每个任务少于 100 个）的标注查询-文档对。
    *   **目标**：使用对比损失函数（如 InfoNCE），进一步微调 $W$，使其能够捕捉复杂指令与相关文档之间的深层语义关联。
*   **技术细节**：采用 **TopK-PercPos** 负采样策略，即在对齐阶段后，选择检索得分高但实际不相关的文档作为硬负样本。

### 3. 实验设计
*   **数据集**：使用 **MAIR 基准测试**，包含 126 个检索任务，涵盖学术、代码、金融、法律、医疗和 Web 六大领域。
*   **模型选择**：
    *   查询/文档编码器包括：Qwen3 (8B/0.6B)、OpenAI (text-embedding-3-large/small)、BGE-M3。
*   **对比方法**：
    1.  **Zero-shot Retrieval**：直接使用原始模型。
    2.  **Embedding Adapter**：传统的单阶段监督适配器。
    3.  **ERA w/o adapt**：仅进行自监督对齐的消融版本。
*   **评估指标**：nDCG@10（主指标）、Recall@100、MAP@100。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练配置**：提到了使用 AdamW 优化器，对齐阶段训练 100 个 epoch，适配阶段最多 1000 个 epoch（带早停机制），Batch Size 为 256。由于适配器仅为线性层（矩阵乘法），推测其训练对算力要求极低，可在单张消费级 GPU 上快速完成。

### 5. 实验数量与充分性
*   **实验规模**：实验非常充分。涵盖了 126 个任务，并进行了不同标注比例（5% 到 40%）的对比。
*   **消融与分析**：
    *   **非对称检索实验**：验证了“强查询模型 + 弱文档模型”组合的有效性。
    *   **领域泛化性实验**：测试了在一个领域训练的适配器在其他领域的表现（Out-of-domain）。
    *   **超参数敏感度**：对负采样策略、负样本数量、学习率和权重衰减进行了详细分析。
*   **客观性**：通过跨模型家族（如 Qwen 到 OpenAI）的测试，证明了方法的通用性，实验设计公平且具有说服力。

### 6. 主要结论与发现
*   **性能提升**：在低标签（20% 标注）设置下，ERA 将平均 nDCG@10 提升了高达 **8%**（对称模型）和 **12%**（非对称模型）。
*   **标签高效性**：即使只有 5% 的标注数据（约 500 条查询），ERA 依然能显著优于 Zero-shot，而传统适配器在此时往往失效。
*   **非对称优势**：ERA 证明了可以使用 8B 的大模型处理查询，同时匹配 0.6B 小模型索引的文档，性能接近甚至超过全对称的大模型检索，且无需重排索引。
*   **领域通用性**：训练一个通用的全局适配器比为每个领域单独训练适配器更具性价比，且具有良好的跨领域迁移能力。

### 7. 优点（亮点）
*   **无需重新索引**：这是该方法最大的工程价值，极大地降低了检索系统升级的成本。
*   **两阶段设计的巧妙性**：通过先对齐空间再学习语义，解决了小样本下监督学习容易过拟合或难以收敛的问题。
*   **黑盒兼容性**：不需要访问模型内部参数，仅需嵌入向量，因此适用于 OpenAI 等闭源 API 模型。
*   **极简架构**：仅使用线性层即可实现显著提升，推理延迟几乎可以忽略不计。

### 8. 不足与局限
*   **线性限制**：适配器被限制为线性变换，虽然保证了泛化性，但在处理极度复杂的语义映射时，表达能力可能不如非线性 MLP。
*   **依赖查询编码器质量**：如果查询侧的骨干模型本身无法理解指令，适配器的提升空间会受限。
*   **负采样依赖**：实验表明性能对负采样策略（如 TopK-PercPos）有一定依赖，在完全没有标注数据的冷启动场景下，硬负样本的获取质量可能影响最终效果。

（完）
