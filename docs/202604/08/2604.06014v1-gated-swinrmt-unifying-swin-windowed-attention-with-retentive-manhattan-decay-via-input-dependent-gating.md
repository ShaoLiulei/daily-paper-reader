---
title: "Gated-SwinRMT: Unifying Swin Windowed Attention with Retentive Manhattan Decay via Input-Dependent Gating"
title_zh: Gated-SwinRMT：通过输入依赖门控统一 Swin 窗口注意力与 Retentive 曼哈顿衰减
authors: "Dipan Maity, Suman Mondal, Arindam Roy"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06014v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 结合窗口注意力的混合视觉Transformer
tldr: 本研究提出Gated-SwinRMT，一种结合了Swin Transformer移位窗口注意力和Retentive Networks（RMT）曼哈顿距离空间衰减的混合视觉Transformer。该模型通过输入依赖的门控机制增强特征表达，并将自注意力分解为窗口内的行列保留传递，利用指数衰减掩码提供二维局部先验。实验表明，该方法在Mini-ImageNet上显著优于RMT基准，有效提升了视觉任务的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06014v1/fig-001.webp\", \"caption\": \"Table 2: Shared training hyper-parameters applied identically to all models on both datasets.\", \"page\": 6, \"index\": 1, \"width\": 535, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2604.06014v1/fig-002.webp\", \"caption\": \"Figure 1: Architecture of the proposed Gated-SwinRMT variants. (a) Gated-SwinRMT-SWAT: sigmoid-based normalized attention with SwiGLU-gated values, balanced ALiBi positional bias, and multiplicative spatial decay γ|i−j| applied post-sigmoid. (b) GatedSwinRMT-Retention: softmax-normalized retention with additive log-decay mask Mij=|i−j| log γh applied pre-softmax, and a learned G1 sigmoid gate applied after local context enhancement (LCE) and before the output projection WO. Both variants share DWConv 3×3 positional encoding, LayerScale (γ1, γ2) with DropPath, an LCE module on V , adaptive window partitioning with optional cyclic shift, and convolutional patch merging. Best viewed in colour.\", \"page\": 2, \"index\": 2, \"width\": 1102, \"height\": 969}]"
motivation: 旨在解决现有视觉Transformer在结合局部窗口注意力和长程保留机制时，缺乏输入依赖的抑制能力及有效的局部先验引导问题。
method: 通过将自注意力分解为行列保留传递，并引入曼哈顿衰减偏置与输入依赖的门控单元（如SwiGLU或G1门控）来优化特征提取过程。
result: "在Mini-ImageNet数据集上，Gated-SwinRMT-SWAT变体达到了80.22%的准确率，相比RMT基准提升了6.48个百分点。"
conclusion: 结合移位窗口、曼哈顿衰减和输入依赖门控的混合架构能显著增强视觉Transformer的建模能力，尤其在处理具有复杂空间结构的图像时表现优异。
---

## 摘要
我们引入了 Gated-SwinRMT，这是一个混合视觉 Transformer 系列，它结合了 Swin Transformer 的移动窗口注意力与 Retentive Networks (RMT) 的曼哈顿距离空间衰减，并辅以输入依赖门控。在每个移动窗口内，自注意力被分解为连续的宽度方向和高度方向的保留（retention）传递，其中每头指数衰减掩码提供了二维局部性先验，无需学习位置偏置。本文提出了两种变体：Gated-SwinRMT-SWAT 使用 sigmoid 激活替代 softmax，实现了具有乘法激活后空间衰减的平衡 ALiBi 斜率，并通过 SwiGLU 对值投影进行门控，其归一化输出隐式地抑制了无信息的注意力分数；Gated-SwinRMT-Retention 则保留了带有加性对数空间衰减偏置的 softmax 归一化保留机制，并引入了一个显式的 G1 sigmoid 门——该门由块输入投影生成，在局部上下文增强（LCE）之后但在输出投影 $W_O$ 之前应用——以缓解低秩 $W_V \!\cdot\! W_O$ 瓶颈，并实现对注意力输出的输入依赖抑制。我们在相同的训练协议下，在 Mini-ImageNet ($224{\times}224$，100 类) 和 CIFAR-10 ($32{\times}32$，10 类) 上评估了这两种变体。在约 77–79M 参数量下，Gated-SwinRMT-SWAT 和 Gated-SwinRMT-Retention 在 Mini-ImageNet 上的 top-1 测试准确率分别达到 80.22% 和 78.20%，而 RMT 基准模型为 73.74%。在 CIFAR-10 上，由于较小的特征图导致自适应窗口机制将注意力坍缩至全局范围，准确率优势从 +6.48 个百分点压缩至 +0.56 个百分点。

## Abstract
We introduce Gated-SwinRMT, a family of hybrid vision transformers that combine the shifted-window attention of the Swin Transformer with the Manhattan-distance spatial decay of Retentive Networks (RMT), augmented by input-dependent gating. Self-attention is decomposed into consecutive width-wise and height-wise retention passes within each shifted window, where per-head exponential decay masks provide a two-dimensional locality prior without learned positional biases.   Two variants are proposed. \textbf{Gated-SwinRMT-SWAT} substitutes softmax with sigmoid activation, implements balanced ALiBi slopes with multiplicative post-activation spatial decay, and gates the value projection via SwiGLU; the Normalized output implicitly suppresses uninformative attention scores. \textbf{Gated-SwinRMT-Retention} retains softmax-normalized retention with an additive log-space decay bias and incorporates an explicit G1 sigmoid gate -- projected from the block input and applied after local context enhancement (LCE) but prior to the output projection~$W_O$ -- to alleviate the low-rank $W_V \!\cdot\! W_O$ bottleneck and enable input-dependent suppression of attended outputs.   We assess both variants on Mini-ImageNet ($224{\times}224$, 100 classes) and CIFAR-10 ($32{\times}32$, 10 classes) under identical training protocols, utilizing a single GPU due to resource limitations. At ${\approx}77$--$79$\,M parameters, Gated-SwinRMT-SWAT achieves $80.22\%$ and Gated-SwinRMT-Retention $78.20\%$ top-1 test accuracy on Mini-ImageNet, compared with $73.74\%$ for the RMT baseline. On CIFAR-10 -- where small feature maps cause the adaptive windowing mechanism to collapse attention to global scope -- the accuracy advantage compresses from $+6.48$\,pp to $+0.56$\,pp.

---

## 论文详细总结（自动生成）

这是一份关于论文《Gated-SwinRMT: Unifying Swin Windowed Attention with Retentive Manhattan Decay via Input-Dependent Gating》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决视觉 Transformer（ViT）中的三个核心局限：
*   **计算效率与局部先验的矛盾**：标准 ViT 具有平方级计算复杂度且缺乏空间局部性诱导偏置。
*   **窗口 Softmax 的病态问题**：Swin Transformer 虽然通过窗口化降低了复杂度，但 Softmax 强制每个窗口内的注意力权重总和为 1，导致模型即使在无信息区域也必须分配注意力。
*   **长程建模的衰减缺失**：现有的窗口注意力缺乏像 RetNet 那样基于物理距离（如曼哈顿距离）的显式衰减机制。

### 2. 方法论：核心思想与关键技术
Gated-SwinRMT 结合了 Swin 的**移位窗口（Shifted Window）**架构、RMT 的**曼哈顿距离空间衰减**以及**输入依赖的门控机制**。其核心技术包括：

*   **分解式保留机制（Decomposed Retention）**：将 2D 窗口注意力分解为连续的 1D 宽度方向和高度方向传递，降低计算量。
*   **两种变体设计**：
    *   **Gated-SwinRMT-SWAT**：使用 Sigmoid 激活代替 Softmax，引入平衡 ALiBi 斜率（正负斜率各半）和 SwiGLU 门控值投影。其特点是“激活后乘法衰减”，允许模型隐式抑制不重要的分数。
    *   **Gated-SwinRMT-Retention**：保留 Softmax 归一化，但在 Log 空间引入加性衰减偏置。为了解决 Softmax 的局限，在输出投影前增加了一个显式的 **G1 Sigmoid 门控**，用于根据输入动态抑制无用信息。
*   **自适应窗口缩放**：当特征图尺寸小于设定窗口时，自动调整窗口大小，使模型能平滑退化为全局注意力。
*   **局部上下文增强（LCE）**：在注意力分支中加入深度卷积（DWConv），补偿保留机制可能忽略的微观结构。

### 3. 实验设计
*   **数据集**：
    *   **Mini-ImageNet**（224x224）：作为主要基准，模拟真实窗口化操作场景。
    *   **CIFAR-10**（32x32）：作为对照组，由于分辨率低，窗口机制会退化为全局注意力，用于验证“窗口 Softmax 病态”假设。
*   **Benchmark 与对比方法**：
    *   核心基准是 **RMT**（Retentive Vision Transformer）。
    *   在相同参数规模（约 77M-79M）和训练协议下进行公平对比。

### 4. 资源与算力
*   **硬件**：使用了单台 **NVIDIA H100 80GB GPU**。
*   **精度**：采用 `bfloat16` 混合精度训练。
*   **时长**：Mini-ImageNet 每轮（epoch）训练约 240-300 秒；CIFAR-10 每轮约 10 秒。总训练时长分别为 40 轮和 50 轮。

### 5. 实验数量与充分性
*   **实验规模**：进行了跨数据集对比（Mini-ImageNet vs CIFAR-10）和组件级消融实验。
*   **充分性评价**：
    *   **客观性**：所有模型在完全相同的超参数（学习率、优化器、增强策略）下训练，保证了比较的公平性。
    *   **局限性**：实验主要集中在分类任务，且受限于算力，未能在完整的 ImageNet-1k 上验证，也缺乏下游任务（检测、分割）的测试。消融实验是通过完整模型对比进行的间接消融，而非单一变量控制。

### 6. 主要结论与发现
*   **性能提升**：在 Mini-ImageNet 上，SWAT 变体比 RMT 基准高出 **6.48 个百分点**（80.22% vs 73.74%）。
*   **病态假设验证**：在 CIFAR-10（无窗口效应）上，性能优势从 6.48pp 缩减至 0.56pp，有力证明了所提门控机制主要解决了窗口化带来的负面影响。
*   **收敛速度**：Sigmoid 基础的 SWAT 变体在训练早期（前 5 轮）表现出更快的收敛速度。

### 7. 优点
*   **理论联系实际**：针对 Softmax 在局部窗口中的数学缺陷提出了针对性的门控和 Sigmoid 替代方案。
*   **架构融合**：成功将 RetNet 的衰减特性引入移位窗口架构，且无需学习位置编码。
*   **自适应性**：自适应窗口机制增强了模型处理不同分辨率输入的鲁棒性。

### 8. 不足与局限
*   **实验覆盖面**：缺乏在大规模数据集（ImageNet-1k）和密集预测任务（如目标检测）上的验证。
*   **过拟合风险**：SWAT 变体在训练集和验证集之间存在约 7% 的准确率差距，表明仍存在残余过拟合。
*   **效率分析缺失**：虽然提到了训练时长，但未提供详细的 FLOPs（浮点运算量）或推理延迟（Latency）的帕累托分析。
*   **参数开销**：相比 RMT，新组件引入了约 1.6% 的参数增量和约 20% 的计算耗时。

（完）
