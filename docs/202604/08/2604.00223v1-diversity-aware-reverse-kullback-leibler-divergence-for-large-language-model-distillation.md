---
title: Diversity-Aware Reverse Kullback-Leibler Divergence for Large Language Model Distillation
title_zh: 面向大语言模型蒸馏的多样性感知逆向 Kullback-Leibler 散度
authors: "Hoang-Chau Luong, Dat Ba Tran, Lingwei Chen"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00223v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于大语言模型蒸馏的反向KL散度
tldr: 针对大语言模型蒸馏中逆向KL散度（RKL）导致的预测过度自信和输出多样性不足问题，本文提出了多样性感知逆向KL（DRKL）。通过分析RKL梯度结构，DRKL消除了导致目标概率异常升高的负面影响，并加强了对非目标类别的监督。实验证明，该方法在保持RKL优化优势的同时，显著提升了模型的性能和保真度-多样性权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00223v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of distillation losses for GPT-2 and OPT teacher–student pairs. We report ROUGE-L, with mean and standard deviation computed over five random seeds. The best results are boldfaced and the second-best are underlined.\", \"page\": 8, \"index\": 1, \"width\": 822, \"height\": 725}, {\"url\": \"assets/figures/arxiv/2604.00223v1/fig-002.webp\", \"caption\": \"Figure 1: FKL and RKL when fitting a teacher distribution under different output sizes. The x-axis shows the class index, and the y-axis shows the corresponding output probability. As the number of classes increases, the optimization difficulty grows substantially.\", \"page\": 4, \"index\": 2, \"width\": 825, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.00223v1/fig-003.webp\", \"caption\": \"Figure 5: (a) Validation performance of different distillation losses. (b) DRKL performance across different values of γ. (c) Efficiency analysis.\", \"page\": 9, \"index\": 3, \"width\": 818, \"height\": 193}, {\"url\": \"assets/figures/arxiv/2604.00223v1/fig-004.webp\", \"caption\": \"Figure 2: (a, b) Fidelity vs. diversity: RKL reduces diversity (Negative Self-BLEU and Distinct-2), while DRKL achieves a better balance across methods. (c) ROUGE-L vs. prediction confidence: RKL produces overconfident predictions without improving ROUGE-L, while DRKL is better calibrated.\", \"page\": 5, \"index\": 4, \"width\": 819, \"height\": 201}]"
motivation: 传统的RKL蒸馏会导致学生模型产生过度自信的预测并降低输出多样性，且对非目标类别的监督较弱。
method: 提出DRKL方法，通过分解并修正RKL梯度中的非目标分量，在抑制过度自信的同时增强对长尾分布的对齐。
result: 在多个数据集和模型系列上的实验表明，DRKL在性能和多样性平衡上均优于FKL、RKL及其他先进的蒸馏目标。
conclusion: DRKL有效解决了RKL在LLM蒸馏中的结构性缺陷，是实现高质量大模型知识迁移的更优选择。
---

## 摘要
逆向 Kullback-Leibler (RKL) 散度近期已成为大语言模型 (LLM) 蒸馏的首选目标函数，其表现持续优于正向 KL (FKL)，特别是在词表较大且教师与学生模型容量显著不匹配的情况下，RKL 倾向于将学习重点放在主导模式上，而非强制进行密集对齐。然而，RKL 引入了一种结构性限制，会导致学生模型产生过度自信的预测。我们首先通过将 RKL 的梯度分解为目标和非目标分量对其进行了分析，并表明即使在学生模型已经与教师模型匹配的情况下，非目标梯度仍会持续推高目标 logit，从而降低输出的多样性。此外，RKL 对非目标类别的监督较弱，导致尾部对齐效果不佳。为了解决这些问题，我们提出了多样性感知 RKL (DRKL)，它在保留 RKL 优化优势的同时，消除了这种梯度效应并加强了非目标监督。在不同数据集和模型系列上的广泛实验表明，DRKL 持续优于 FKL、RKL 以及其他最先进的蒸馏目标，实现了更好的性能和更优的保真度-多样性权衡。

## Abstract
Reverse Kullback-Leibler (RKL) divergence has recently emerged as the preferred objective for large language model (LLM) distillation, consistently outperforming forward KL (FKL), particularly in regimes with large vocabularies and significant teacher-student capacity mismatch, where RKL focuses learning on dominant modes rather than enforcing dense alignment. However, RKL introduces a structural limitation that drives the student toward overconfident predictions. We first provide an analysis of RKL by decomposing its gradients into target and non-target components, and show that non-target gradients consistently push the target logit upward even when the student already matches the teacher, thereby reducing output diversity. In addition, RKL provides weak supervision over non-target classes, leading to poor tail alignment. To address these issues, we propose Diversity-aware RKL (DRKL), which removes this gradient effect and strengthens non-target supervision while preserving the optimization benefits of RKL. Extensive experiments across datasets and model families demonstrate that DRKL consistently outperforms FKL, RKL, and other state-of-the-art distillation objectives, achieving better performance and a superior fidelity-diversity trade-off.

---

## 论文详细总结（自动生成）

这是一份关于论文《Diversity-Aware Reverse Kullback-Leibler Divergence for Large Language Model Distillation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在大型语言模型（LLM）的知识蒸馏中，**逆向 KL 散度（RKL）**因其“模式寻求（Mode-seeking）”特性，在处理大词表和教师-学生模型容量极度不匹配时，表现优于传统的正向 KL（FKL）。
然而，RKL 存在两个严重的结构性缺陷：
*   **过度自信（Overconfidence）：** RKL 会导致学生模型对目标 Token 的预测概率异常升高，甚至超过教师模型。
*   **多样性缺失（Low Diversity）：** 由于概率质量过度集中在主导模式上，学生生成的文本缺乏多样性。
*   **非目标对齐差：** RKL 对非目标类别的监督信号较弱，导致长尾分布的学习效果不佳。

### 2. 方法论：核心思想与技术细节
论文提出了 **DRKL（Diversity-aware RKL）**，其核心逻辑如下：

*   **梯度分解分析：** 作者通过数学证明，将 RKL 分解为**目标项（TRKL）**和**非目标项（NRKL）**。分析发现，NRKL 的梯度会持续产生一个负值，作用于目标 logit，这意味着即使学生模型已经匹配了教师的目标概率，非目标项的误差仍会不断推高目标 logit，造成过度自信。
*   **解耦与重构：** DRKL 的核心思想是切断非目标项对目标 logit 的梯度贡献。
    *   **公式调整：** 在 RKL 的分解式中，非目标项通常被权重 $(1 - q_m)$ 缩放（$q_m$ 为学生预测目标类的概率）。DRKL 将该权重替换为一个固定的超参数 $\gamma$。
    *   **效果：** 这种修改消除了非目标类错位对目标类 logit 的干扰，防止了信心膨胀；同时，通过调节 $\gamma$，增强了对非目标类别（即“暗知识”）的监督强度。
*   **优化优势：** DRKL 保留了 RKL 在大词表下的计算效率，即只关注学生模型分配了概率的活跃 Token，而不需要像 FKL 那样对整个词表进行密集匹配。

### 3. 实验设计
*   **数据集/场景：** 采用指令微调（Instruction Following）场景。训练集使用 `databricks-dolly-15k`。
*   **Benchmark（测试集）：** Dolly 验证集、Self-Instruct、Vicuna Eval、Super-Natural Instructions (Super-NI) 和 Unnatural Instructions (UnNI)。
*   **对比方法：** 涵盖了 9 种主流蒸馏目标函数，包括 FKL、RKL、Sym-KL（对称 KL）、DKD（解耦 KL）、JS 散度、SFKL/SRKL（偏斜 KL）、AKL（自适应 KL）和 AB 散度。
*   **模型组合：** 
    *   GPT-2 XL (1.5B) $\rightarrow$ GPT-2 Base (120M) / Medium (340M)
    *   OPT-6.7B $\rightarrow$ OPT-1.3B

### 4. 资源与算力
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号和数量。
*   **效率数据：** 论文提供了一份效率分析图，显示 DRKL 的每步训练耗时约为 **0.58 秒**，与 RKL（0.57s）和 FKL（0.56s）基本持平，证明该方法在引入性能提升的同时几乎没有增加计算负担。

### 5. 实验数量与充分性
*   **实验规模：** 论文在两个不同的模型家族（GPT-2 和 OPT）上进行了验证，涵盖了从 120M 到 1.3B 不同规模的学生模型。
*   **充分性：** 
    *   每组实验均运行了 **5 个随机种子**并报告了均值和标准差，具有统计说服力。
    *   进行了**超参数敏感度分析**（针对 $\gamma$）。
    *   包含了**多样性指标测试**（Distinct-2, Negative Self-BLEU）和**校准度分析**（预测置信度）。
    *   提供了**定性案例研究**（Case Study），直观展示了生成质量。
*   **公平性：** 所有方法在相同的硬件环境、数据集和超参数设置下运行，对比具有客观性。

### 6. 主要结论与发现
*   **性能领先：** DRKL 在所有测试的 Benchmark 上均取得了最高或次高的 ROUGE-L 分数，显著优于 RKL 和 FKL。
*   **解决过度自信：** 实验证明 DRKL 生成的概率分布比 RKL 更接近教师模型，有效抑制了学生模型的盲目自信。
*   **保真度-多样性平衡：** DRKL 在保持高保真度（Fidelity）的同时，显著提升了生成文本的多样性（Distinct-2 指标明显优于 RKL）。
*   **收敛更快：** 验证集曲线显示 DRKL 在训练早期就能达到较高的性能，且稳定性优于其他变体。

### 7. 优点（亮点）
*   **理论扎实：** 论文不仅提出了新方法，还通过梯度分解深入揭示了 RKL 导致过度自信的数学本质。
*   **简单高效：** 方法实现简单（仅需修改损失函数权重），且不增加额外的推理或训练开销。
*   **通用性强：** 实验证明 DRKL 可以与 SRKL 等其他改进方法结合使用，进一步提升效果。

### 8. 不足与局限
*   **模型规模限制：** 实验最高仅验证到 6.7B 到 1.3B 的蒸馏，尚未在当前主流的百亿或千亿级参数模型（如 Llama-3 70B）上进行验证。
*   **数据集规模：** 训练仅使用了 14k 条指令数据，在大规模预训练阶段的蒸馏效果尚待观察。
*   **超参数依赖：** 虽然 $\gamma$ 在 0.5 到 2.0 之间表现稳定，但仍需针对不同任务进行微调以获得最优性能。

（完）
