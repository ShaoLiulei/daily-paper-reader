---
title: "ALTO: Adaptive LoRA Tuning and Orchestration for Heterogeneous LoRA Training Workloads"
title_zh: ALTO：针对异构 LoRA 训练负载的自适应 LoRA 调优与编排
authors: "Jingwei Zuo, Xinze Feng, Zien Liu, Kaijian Wang, Fanjiang Ye, Ye Cao, Zhuang Wang, Yuke Wang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05426v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型微调中的自适应 LoRA 调优
tldr: 针对大模型LoRA微调中超参数敏感导致的多任务并发训练效率低、资源浪费问题，本文提出ALTO系统。ALTO通过协同设计训练与编排，利用共享冻结主干网络的特性，引入早期停止机制、融合分组GEMM及秩局部适配器并行技术，并结合多层级调度策略，在保证模型质量的同时显著提升了异构LoRA任务的训练速度和资源利用率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05426v1/fig-001.webp\", \"caption\": \"Figure 10: Model quality of the best configuration found by ALTO on single-GPU 7B–8B models, compared against expertrecommended hyperparameters from Unsloth and Tinker. (a) GSM8K test accuracy (higher is better). Completion loss on the evaluation split of (b) Tulu-3 and (c) OpenThoughts3 (lower is better). For ALTO, each bar shows the best result found across batched runs with per-adapter batch sizes in {1,2,4,8}.\", \"page\": 11, \"index\": 1, \"width\": 1042, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2604.05426v1/fig-002.webp\", \"caption\": \"Figure 2: System Overview of ALTO.\", \"page\": 2, \"index\": 2, \"width\": 1052, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2604.05426v1/fig-003.webp\", \"caption\": \"Figure 9: End-to-end training speedup of ALTO across single-GPU and multi-GPU configurations. From left to right: Llama-3.18B and Qwen2.5-7B on a single H100 GPU, Qwen2.5-32B on 2× H100 and Llama-3.1-70B on 4× H100. Each configuration trains 60 (single-GPU) or 64 (multi-GPU) heterogeneous LoRA adapters with varied ranks, batch sizes, and learning rates across three datasets. ALTO achieves up to 9.5× speedup on single-GPU and up to 13.8× speedup on multi-GPU settings, consistently outperforming all baselines. Speedup is evaluated relative to LoRAFusion.\", \"page\": 10, \"index\": 3, \"width\": 1047, \"height\": 220}, {\"url\": \"assets/figures/arxiv/2604.05426v1/fig-004.webp\", \"caption\": \"Figure 15: Training samples saved by each early-exit pattern across seven model–dataset combinations. Stacked bars decompose savings by detector: underperformance filtering dominates in SFT (∼66%), while overfitting and divergence contribute more in DPO. Green diamonds show the ratio of best validation loss w/ vs. w/o early exit; values at or near 1.0 confirm that early termination preserves model quality.\", \"page\": 13, \"index\": 4, \"width\": 489, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.05426v1/fig-005.webp\", \"caption\": \"Figure 8: Two executor modes in ALTO’s batched execution engine. (a) Multi-GPU executor with adapter parallelism: base model weights are sharded across ranks and synchronized via all-gather, while each rank trains a disjoint set of LoRA jobs locally via grouped GEMM, therefore there is no adapter gradients cross rank boundaries. (b) Single-GPU executor: the full base model and multiple LoRA jobs reside on a single device, trained concurrently through grouped GEMM batching.\", \"page\": 8, \"index\": 5, \"width\": 498, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.05426v1/fig-006.webp\", \"caption\": \"Figure 14: Single-GPU ablation on Llama-3.1-8B / GSM8K across per-adapter batch sizes. Left: GSM8K evaluation accuracy (strict parsing). Gray dots show individual adapter accuracies from the full sweep without selection. Colored markers compare the best accuracy found by Multi-LoRA batching alone vs. Multi-LoRA with early exit. Right: Best validation loss. Early exit preserves or improves quality by concentrating resources on promising configurations.\", \"page\": 12, \"index\": 6, \"width\": 497, \"height\": 253}, {\"url\": \"assets/figures/arxiv/2604.05426v1/fig-007.webp\", \"caption\": \"Figure 3: LoRA fine-tuning prefer small batch size training. SFT validation loss for Llama-3.1-8B on (a) GSM8K, (b) Tulu-3, and (c) OpenThoughts3; DPO reward accuracy for Qwen2.5-32B on (d) UltraFeedback across five learning rates. Performance peaks at small batch sizes (≤16) and degrades beyond 32 for these settings.\", \"page\": 5, \"index\": 7, \"width\": 488, \"height\": 290}]"
motivation: 现有的LoRA训练系统通常独立处理并发任务，导致在低质量候选配置上浪费大量计算资源且GPU利用率较低。
method: ALTO通过监控损失轨迹提前终止差配置，并利用融合分组GEMM、秩局部并行及跨任务调度实现高效的资源共享与任务编排。
result: 实验结果显示，ALTO在不牺牲适配器质量的前提下，相比现有最先进技术实现了高达13.8倍的训练加速。
conclusion: ALTO证明了通过深度融合超参数优化与底层系统调度，可以极大地提升多租户环境下异构LoRA微调任务的整体吞吐量和效率。
---

## 摘要
低秩自适应（LoRA）是目前大语言模型参数高效微调的主流方法，但由于 LoRA 的性能对配置选择高度敏感，获得高质量的适配器（adapter）通常需要系统的超参数调优。在实践中，这导致了许多并发的 LoRA 任务，这些任务通常跨越多租户环境中的异构任务。现有系统在很大程度上独立处理这些任务，这既浪费了在弱候选配置上的计算资源，又导致 GPU 利用率不足。我们提出了 ALTO（自适应 LoRA 调优与编排），这是一个协同设计的训练系统，它在加速 LoRA 超参数调优的同时，实现了异构任务间的高效集群共享。ALTO 的核心见解是，当多个调优任务在共享的冻结主干网络（backbone）上并发运行时，它们会暴露出单任务设计无法利用的优化机会。基于此，ALTO 监控损失轨迹以提前终止无望的配置，使用融合分组 GEMM（fused grouped GEMM）结合一种新的秩局部适配器并行（rank-local adapter parallelism）来共同部署幸存的适配器并回收释放的 GPU 容量，并结合任务内和任务间调度，利用 LoRA 任务可预测的时长来改进多任务放置。广泛的评估表明，在不牺牲适配器质量的前提下，ALTO 与现有最先进技术相比实现了高达 13.8 倍的加速。

## Abstract
Low-Rank Adaptation (LoRA) is now the dominant method for parameter-efficient fine-tuning of large language models, but achieving a high-quality adapter often requires systematic hyperparameter tuning because LoRA performance is highly sensitive to configuration choices. In practice, this leads to many concurrent LoRA jobs, often spanning heterogeneous tasks in multi-tenant environments. Existing systems largely handle these jobs independently, which both wastes computation on weak candidates and leaves GPUs underutilized. We present ALTO (Adaptive LoRA Tuning and Orchestration), a co-designed training system that accelerates LoRA hyperparameter tuning while enabling efficient cluster sharing across heterogeneous tasks. The central insight behind ALTO is that when multiple tuning jobs run concurrently over a shared frozen backbone, they expose optimization opportunities that single-job designs cannot exploit. Building on this, ALTO monitors loss trajectories to terminate unpromising configurations early, uses fused grouped GEMM together with a new rank-local adapter parallelism to co-locate surviving adapters and reclaim freed GPU capacity, and combines intra-task and inter-task scheduling to improve multi-task placement by leveraging the predictable duration of LoRA jobs. Extensive evaluation shows that ALTO achieves up to $13.8\times$ speedup over state-of-the-art without sacrificing adapter quality.

---

## 论文详细总结（自动生成）

### ALTO: 针对异构 LoRA 训练负载的自适应 LoRA 调优与编排

#### 1. 核心问题与整体含义（研究动机和背景）
LoRA 已成为大语言模型（LLM）参数高效微调（PEFT）的主流方法。然而，LoRA 的性能对超参数（如学习率、Batch Size、秩 $r$）高度敏感，开发者通常需要进行大规模的超参数搜索（Grid Search）。
*   **现有痛点**：目前的框架（如 PEFT, LlamaFactory）通常将每个超参数配置视为独立的任务顺序执行，导致：
    1.  **计算浪费**：在明显无法收敛或表现较差的配置上浪费了大量算力。
    2.  **硬件利用率低**：LoRA 训练通常偏好小 Batch Size 以获得更好的泛化性能，但这会导致 GPU 计算单元（SM）和内存带宽严重闲置。
    3.  **调度低效**：在多租户、多任务环境下，缺乏对异构任务（不同模型、不同数据集）的有效编排。

#### 2. 方法论：核心思想与关键技术
ALTO 提出了一个协同设计的系统，旨在将 LoRA 调优视为一个统一的系统负载，而非独立任务。其核心包含三个关键技术：
*   **损失感知早期退出（Loss-Aware Early Exit）**：
    *   **在线模式检测**：实时监控训练/验证损失曲线。通过线性回归斜率检测“发散（Divergence）”，通过验证损上升检测“过拟合（Overfitting）”。
    *   **热身过滤（Warmup-Based Exiting）**：在训练初期的热身阶段（如前 5% 步数）后，仅保留表现前 25% 的配置，剔除低质量候选者。
*   **动态批处理执行引擎（Dynamically Batched Executor）**：
    *   **解耦的分组 GEMM**：利用所有 LoRA 任务共享冻结主干网络的特性，将主干网络的计算合并，而对不同的 LoRA 适配器使用 Triton 编写的融合分组 GEMM 内核，减少内核启动开销并提升 SM 利用率。
    *   **秩局部适配器并行（Adapter Parallelism, AP）**：在多 GPU 场景下，不同于 FSDP 复制适配器，AP 将不同的适配器分配到不同的 GPU 秩（Rank）上。这样每个 GPU 只负责计算本地适配器的梯度，消除了适配器参数的跨卡通信开销。
*   **层级调度策略（Hierarchical Scheduling）**：
    *   **任务内调度**：根据 GPU 显存自动分析并动态填充适配器槽位。
    *   **任务间调度**：利用 LoRA 任务时长可预测的特性，将其建模为约束规划问题（CP-SAT），最小化集群的总完工时间（Makespan）。

#### 3. 实验设计
*   **数据集/场景**：
    *   **SFT（监督微调）**：GSM8K（数学）、Tulu-3（指令遵循）、OpenThoughts3（通用推理）。
    *   **RLHF（强化学习）**：使用 DPO 算法在 UltraFeedback 数据集上进行对齐训练。
*   **基准模型**：Llama-3.1-8B/70B, Qwen2.5-7B/32B。
*   **对比方法**：
    *   **Sequential**：顺序执行每个配置。
    *   **LoRAFusion**：最先进的融合内核多 LoRA 系统。
    *   **mLoRA**：基于流水线并行的多 LoRA 框架。
    *   **分布式策略**：FSDP、Tensor Parallelism (TP)、Pipeline Parallelism (PP)。

#### 4. 资源与算力
*   **硬件**：使用了 NVIDIA H100 SXM5 80GB GPU 集群。
*   **配置**：
    *   单卡实验：1 × H100（针对 7B/8B 模型）。
    *   多卡实验：2 × H100（针对 32B 模型），4 × H100（针对 70B 模型）。
    *   集群实验：8 × H100（用于验证多任务调度）。
*   **训练规模**：每个任务搜索 60-64 个超参数配置。

#### 5. 实验数量与充分性
论文进行了非常详尽的实验：
*   **端到端性能**：覆盖了从 7B 到 70B 不同规模的模型，以及 SFT 和 DPO 两种主流训练范式。
*   **消融实验**：分别验证了内核优化、早期退出机制、适配器并行策略以及调度算法对性能的贡献。
*   **质量验证**：对比了 ALTO 找到的最佳适配器与专家推荐配置（如 Unsloth, Tinker）的下游任务准确率，证明了加速并未牺牲模型质量。
*   **充分性评价**：实验设计客观、公平，通过 Spearman 秩相关分析验证了早期退出机制的统计合理性，具有较强的说服力。

#### 6. 主要结论与发现
*   **显著加速**：在单卡上实现高达 9.5 倍加速，在多卡（70B 模型）上实现高达 **13.8 倍**加速。
*   **资源节省**：早期退出机制平均节省了 **72% - 83%** 的训练样本计算量。
*   **质量保证**：ALTO 自动搜索到的适配器在 GSM8K 等任务上的表现优于或等同于手动调优的专家配置。
*   **并行优势**：提出的 Adapter Parallelism 在小 Batch Size 下比 FSDP 快 4.7 倍，解决了传统并行策略在 LoRA 任务中的不匹配问题。

#### 7. 优点
*   **协同设计**：不仅优化了底层算子（GEMM），还结合了上层的算法特性（损失曲线）和集群调度，是一个完整的系统解决方案。
*   **实用性强**：直接解决了云端微调服务（LoRA-as-a-Service）中高成本、低效率的痛点。
*   **泛化性好**：算法和阈值在 SFT 和 DPO 任务间通用，无需针对特定任务手动调整。

#### 8. 不足与局限
*   **硬件依赖**：高性能的融合内核（Triton 实现）主要针对 NVIDIA H100 等现代 GPU 优化，在旧型号或其他架构硬件上的表现未提及。
*   **模型架构限制**：目前主要针对 Transformer 架构的线性层 LoRA，对于其他类型的 PEFT 方法（如 Prefix Tuning）或非线性层的适配可能需要额外开发。
*   **超参数空间假设**：实验假设用户提供了一个预定义的搜索空间，如果搜索空间本身设计不合理，系统虽能加速但仍无法获得高质量模型。

（完）
