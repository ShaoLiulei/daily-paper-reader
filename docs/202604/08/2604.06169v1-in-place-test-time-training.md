---
title: In-Place Test-Time Training
title_zh: 原位测试时训练 (In-Place Test-Time Training)
authors: "Guhao Feng, Shengjie Luo, Kai Hua, Ge Zhang, Di He, Wenhao Huang, Tianle Cai"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06169v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型推理时的权重自适应
tldr: 传统的“先训练后部署”模式限制了大型语言模型（LLM）动态适应新信息的能力。本文提出 In-Place TTT 框架，将 MLP 层的最终投影矩阵视为可更新的“快速权重”，实现了无需从头训练的即插即用式增强。该方法引入了与下文预测任务对齐的优化目标和高效的分块更新机制，显著提升了模型在长文本处理中的表现，为 LLM 的持续学习提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06169v1/fig-001.webp\", \"caption\": \"Figure 1 The overall framework of our In-Place Test-Time Training. The module operates sequentially on input chunks. For each chunk, the current fast weights are first applied to the intermediate activations Z to produce the output. Then, these weights are updated using the activations Z and a value V derived from the token embeddings. This \\\"apply-then-update\\\" cycle allows the model to dynamically adapt to incoming context in a strictly causal manner.\", \"page\": 6, \"index\": 1, \"width\": 980, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2604.06169v1/fig-002.webp\", \"caption\": \"Table 1 Evaluation results on RULER [28]. Average accuracy (%) is reported, with the best results in bold.\", \"page\": 7, \"index\": 2, \"width\": 785, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2604.06169v1/fig-003.webp\", \"caption\": \"Figure 3 Ablation studies on the key design choices of the In-Place TTT framework, evaluated on the RULER benchmark with a 1.7B parameter model. The plots illustrate the impact of: (a) State size, showing that performance improves as the state size scales; (b) Chunk size, demonstrating a performance trade-off where intermediate sizes (e.g., 512, 1024) are optimal; and (c) The LM-Aligned Value objective, confirming that both the convolution (w Conv) and the projection (w Proj) are crucial.\", \"page\": 10, \"index\": 3, \"width\": 970, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2604.06169v1/fig-004.webp\", \"caption\": \"Table 4 Training hyperparameters for 500M and 1.5B models.\", \"page\": 18, \"index\": 4, \"width\": 468, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.06169v1/fig-005.webp\", \"caption\": \"Table 7 Hyperparameters for continual pre-training of LLaMA-3.1-8B and Qwen3-14B-Base.\", \"page\": 19, \"index\": 5, \"width\": 514, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2604.06169v1/fig-006.webp\", \"caption\": \"Table 6 Hyperparameters for two-stage continual pre-training.\", \"page\": 19, \"index\": 6, \"width\": 627, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.06169v1/fig-007.webp\", \"caption\": \"Table 3 Evaluation results of 4B models on common sense reasoning and long-context evaluation benchmarks. Best performance is in bold. “SWA” is Sliding-Window Attention, “Full Attn.” is Full Attention, and “I.P. TTT” is our In-Place TTT.\", \"page\": 9, \"index\": 7, \"width\": 971, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2604.06169v1/fig-008.webp\", \"caption\": \"Figure 2 Sliding Window Perplexity at varying context lengths on the Pile dataset for 500M (left) and 1.5B (right) parameter models. Our In-Place TTT consistently achieves lower perplexity than all competitive baselines.\", \"page\": 9, \"index\": 8, \"width\": 975, \"height\": 325}]"
motivation: 静态预训练模型无法在推理阶段根据实时信息流动态调整权重，限制了其在处理长上下文和动态任务时的适应性。
method: 将 MLP 块的投影矩阵作为可调权重，并结合与自回归语言建模对齐的理论目标及高效的分块更新机制。
result: 该框架使 4B 参数模型在 128k 上下文任务中表现优异，且在从头预训练时性能一致优于现有的 TTT 相关方法。
conclusion: In-Place TTT 是一种高效、可扩展且兼容现有架构的方案，标志着 LLM 向推理阶段持续学习迈出了重要一步。
---

## 摘要
静态的“先训练后部署”范式从根本上限制了大语言模型（LLMs）根据现实任务中固有的持续新信息流动态调整其权重的能力。测试时训练（TTT）通过在推理阶段更新模型参数的一个子集（快权重）提供了一种极具吸引力的替代方案，但其在当前 LLM 生态系统中的潜力受到关键障碍的阻碍，包括架构不兼容、计算效率低下以及语言建模中快权重目标的不匹配。在这项工作中，我们引入了原位测试时训练（In-Place TTT），这是一个无缝赋予 LLMs 测试时训练能力的框架。In-Place TTT 将普遍存在的 MLP 模块的最终投影矩阵视为其可自适应的快权重，从而实现了对 LLMs 的“即插即用”式增强，而无需昂贵的从头开始重新训练。此外，我们将 TTT 的通用重构目标替换为一个定制的、具有理论依据的目标，该目标明确与主导自回归语言建模的下一标记预测（Next-Token-Prediction）任务相对齐。这一原则性目标结合高效的分块更新机制，产生了一种与上下文并行性兼容的高可扩展算法。广泛的实验验证了我们框架的有效性：作为一种原位增强，它使一个 4B 参数模型在上下文高达 128k 的任务上实现了卓越的性能；当从头开始预训练时，它始终优于具有竞争力的 TTT 相关方法。消融研究结果进一步提供了关于我们设计选择的更深层见解。总的来说，我们的结果确立了 In-Place TTT 是迈向 LLMs 持续学习范式的有希望的一步。

## Abstract
The static ``train then deploy" paradigm fundamentally limits Large Language Models (LLMs) from dynamically adapting their weights in response to continuous streams of new information inherent in real-world tasks. Test-Time Training (TTT) offers a compelling alternative by updating a subset of model parameters (fast weights) at inference time, yet its potential in the current LLM ecosystem is hindered by critical barriers including architectural incompatibility, computational inefficiency and misaligned fast weight objectives for language modeling. In this work, we introduce In-Place Test-Time Training (In-Place TTT), a framework that seamlessly endows LLMs with Test-Time Training ability. In-Place TTT treats the final projection matrix of the ubiquitous MLP blocks as its adaptable fast weights, enabling a ``drop-in" enhancement for LLMs without costly retraining from scratch. Furthermore, we replace TTT's generic reconstruction objective with a tailored, theoretically-grounded objective explicitly aligned with the Next-Token-Prediction task governing autoregressive language modeling. This principled objective, combined with an efficient chunk-wise update mechanism, results in a highly scalable algorithm compatible with context parallelism. Extensive experiments validate our framework's effectiveness: as an in-place enhancement, it enables a 4B-parameter model to achieve superior performance on tasks with contexts up to 128k, and when pretrained from scratch, it consistently outperforms competitive TTT-related approaches. Ablation study results further provide deeper insights on our design choices. Collectively, our results establish In-Place TTT as a promising step towards a paradigm of continual learning in LLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《In-Place Test-Time Training》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统大语言模型（LLM）遵循“先训练后部署”的静态范式，一旦部署，权重便不再更新。这导致模型在处理超长上下文、动态演进的任务或需要持续学习新知识的场景时存在局限性。虽然“测试时训练”（Test-Time Training, TTT）允许模型在推理时更新部分“快权重”（Fast Weights）以适应新信息，但现有的 TTT 方法面临三大障碍：
*   **架构不兼容**：通常需要引入特殊层，无法直接应用于现有的预训练模型。
*   **计算效率低**：逐标记（Per-token）更新机制难以利用现代 GPU 的并行能力。
*   **目标不匹配**：通用的重构损失（Reconstruction Loss）与自回归语言建模的“下一标记预测”（NTP）目标不一致。

### 2. 方法论：核心思想与关键技术
本文提出了 **In-Place TTT（原位测试时训练）** 框架，其核心思想是“不增减架构，原地升级”：
*   **原位设计（In-Place Design）**：不引入新层，而是直接复用 Transformer 中现有的 MLP 块。将 MLP 的最终投影矩阵 $W_{down}$ 视为可更新的“快权重”，而将输入投影 $W_{up}$ 和 $W_{gate}$ 保持冻结。
*   **分块更新机制（Chunk-wise Update）**：为了提升效率，将序列划分为非重叠的块（如 512 或 1024 个标记）。模型先应用当前快权重处理该块，然后利用该块的信息通过梯度下降更新快权重。
*   **语言模型对齐目标（LM-Aligned Objective）**：摒弃了传统的重构目标，设计了一个与 NTP 任务一致的目标。通过 1D 卷积和投影层从输入中提取包含“未来信息”的特征作为更新目标，使快权重能够学习预测下一个标记所需的上下文。
*   **上下文并行（Context Parallelism）**：利用更新规则的关联性，通过并行前缀和（Parallel Scan）算法实现跨块的同步计算，确保在保持因果性的同时实现高吞吐量。

### 3. 实验设计
研究团队在多个维度验证了方法的有效性：
*   **即插即用增强**：以 Qwen3-4B-Base 为基座，通过持续预训练（Continual Training）对比了原始模型与增强版在长文本任务上的表现。
*   **从头预训练对比**：在 500M、1.5B 和 4B 规模下，将 In-Place TTT 与 SWA（滑动窗口注意力）、GLA（门控线性注意力）、DeltaNet 和 LaCT 等竞争方法进行对比。
*   **Benchmark**：
    *   **长文本**：RULER（测试 4k 到 256k 上下文）、滑动窗口困惑度（PPL）。
    *   **通用能力**：HellaSwag、ARC、MMLU、PIQA 等常识推理任务。

### 4. 资源与算力
*   **硬件**：所有模型均在 **NVIDIA H800 GPU** 上训练。
*   **训练规模**：
    *   500M/1.5B 模型分别训练了 20B 和 60B 标记。
    *   4B 模型训练了 120B 标记。
    *   持续预训练阶段：第一阶段 20B 标记（32k 长度），第二阶段 15B 标记（128k 长度）。
*   **推理效率**：实验证明 In-Place TTT 在 128k 上下文下的预训练吞吐量和内存占用与基座模型相比几乎没有额外开销。

### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多尺度验证**：覆盖了从 500M 到 14B（补充实验中包含 LLaMA-3.1-8B 和 Qwen3-14B）的不同参数规模。
*   **消融实验**：深入探讨了状态大小（TTT 层数）、分块大小（Chunk Size）以及 LM 对齐目标中各组件（卷积、投影）的影响。
*   **公平性**：在对比实验中，确保了所有模型使用相同的训练数据和超参数设置，仅改变核心架构组件。

### 6. 主要结论与发现
*   **性能卓越**：In-Place TTT 显著提升了模型处理长上下文的能力，4B 模型在 RULER 128k 任务上表现优于多个更大规模的基座模型。
*   **可扩展性**：随着快权重状态（TTT 层数）的增加，模型性能持续提升。
*   **理论一致性**：理论分析证明，LM 对齐目标能有效增加正确标记的对数几率（Logits），而重构目标对此几乎无贡献。
*   **高效性**：通过分块更新和并行算法，该方法在现代硬件上实现了极高的计算效率。

### 7. 优点
*   **极佳的兼容性**：这是首个能直接作为“插件”应用于现有预训练 LLM 的 TTT 方案，无需从头训练。
*   **理论与实践结合**：不仅提出了算法，还通过数学证明解释了为什么 NTP 对齐目标优于重构目标。
*   **硬件友好**：通过分块设计解决了 TTT 长期以来的并行化难题。

### 8. 不足与局限
*   **持续训练成本**：虽然不需要从头训练，但要获得最佳效果，仍需要对现有模型进行一定规模（如 35B 标记）的持续预训练。
*   **超参数敏感性**：推理时的更新步长（学习率）和梯度裁剪阈值可能需要针对不同任务进行微调。
*   **任务覆盖**：目前主要集中在语言建模和长文本任务，对于需要极强逻辑推理或特定领域知识的实时更新效果尚待更多验证。

（完）
