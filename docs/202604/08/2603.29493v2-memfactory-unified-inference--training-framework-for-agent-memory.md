---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 记忆增强型大语言模型智能体的统一训练框架
tldr: "MemFactory是首个针对记忆增强型智能体设计的统一推理与训练框架。针对现有记忆管理研究碎片化且难以复用的问题，它借鉴LLaMA-Factory的模块化理念，将记忆生命周期抽象为可插拔的原子组件。该框架原生集成GRPO算法，支持通过环境奖励优化记忆策略，并兼容Memory-R1等前沿范式。实验证明，MemFactory能显著提升智能体性能，最高增幅达14.8%，极大降低了记忆驱动型AI的开发门槛。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v2/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2603.29493v2/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强型大模型研究缺乏统一的基础设施，导致记忆操作的集成、训练和评估过程高度碎片化且难以复用。
method: 提出模块化的“乐高式”架构，将记忆生命周期抽象为原子组件，并集成GRPO算法以实现基于环境奖励的记忆管理策略微调。
result: "在MemAgent架构上的实验显示，该框架在领域内和分布外评估集中均优于基座模型，性能提升最高达14.8%。"
conclusion: MemFactory通过提供标准化且可扩展的基础设施，为记忆驱动型AI智能体的未来创新与开发铺平了道路。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近年来，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均有一致的性能提升，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MemFactory**，一个旨在统一记忆增强型大语言模型（LLM）智能体训练与推理的开源框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：为了让 AI 智能体具备长期记忆和个性化能力，研究者开始利用强化学习（RL）来优化记忆操作（如提取、更新、检索）。
*   **核心问题**：现有的记忆强化学习（Memory-RL）研究高度碎片化，代码实现通常与特定任务深度耦合，缺乏统一的基础设施。这导致研究者难以复用、组合或对比不同的记忆策略（如 Memory-R1、MemAgent 等）。
*   **整体含义**：MemFactory 借鉴了 LLaMA-Factory 的成功经验，通过模块化设计降低了开发记忆智能体的门槛，为该领域的标准化和快速迭代提供了工具基础。

### 2. 方法论：核心思想与技术细节
MemFactory 采用“乐高式”的模块化架构，分为四个核心层：
*   **模块层 (Module Layer)**：将记忆生命周期拆解为原子操作。
    *   **Extractor**：从原始上下文中解析事实和经验。
    *   **Updater**：执行增删改查（ADD/DEL/UPDATE/NONE）操作维护记忆库。
    *   **Retriever**：基于语义或推理模型（LRM）进行重排序检索。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：作为策略执行器，通过组合上述原子模块构建复杂的记忆工作流，并生成交互轨迹（Rollout）。
*   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器。支持 `MemoryBankEnv`（显式记忆库）和 `LongcontextEnv`（长上下文循环状态），并提供格式奖励和 LLM-as-a-Judge 等多维奖励信号。
*   **训练层 (Trainer Layer)**：核心优化引擎。
    *   **关键算法**：原生集成 **GRPO (Group Relative Policy Optimization)**。该算法通过组内奖励归一化估计基准，无需额外的 Critic 网络，显著降低了长上下文训练时的显存开销。

### 3. 实验设计
*   **实验对象**：以 **MemAgent** 架构为基础，验证框架的有效性。
*   **数据集/场景**：
    *   使用 MemAgent 公开的训练和评估数据。
    *   **测试集**：包含两个主任务测试集（`eval_50`, `eval_100`）和一个分布外（OOD）测试集（`eval_fwe_16384`）。
*   **对比基准**：对比了微调前后的基座模型（Qwen3-1.7B 和 Qwen3-4B-Instruct）。
*   **评估指标**：采用 `avg@4`（4 次独立试验的平均得分）以减少生成随机性的影响。

### 4. 资源与算力
*   **硬件设备**：论文明确指出，整个训练和评估流程可以在 **单台 NVIDIA A800 80GB GPU** 上完成。
*   **训练细节**：每个模型训练 250 个有效步（steps）。为了提高效率，训练时将上下文长度简化为原始长度的三分之一（每样本 50-80 个文档）。
*   **算力评价**：该框架表现出极高的计算效率，证明了其在有限资源下的可复用性。

### 5. 实验数量与充分性
*   **实验规模**：针对两种不同规模的基座模型（1.7B 和 4B）进行了对比实验，涵盖了主任务和 OOD 场景。
*   **充分性评价**：
    *   **优点**：引入了 OOD 测试，验证了记忆策略的泛化能力；采用了多次试验取平均的方法，保证了结果的客观性。
    *   **局限**：实验主要集中在 MemAgent 这一种范式上，虽然框架支持 Memory-R1 和 RMM，但文中未给出这两类智能体在 MemFactory 下的详细对比训练数据。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**，Qwen3-4B-Instruct 提升了 **7.3%**。
*   **泛化能力**：较大规模的模型（4B）在 OOD 测试集上表现出更稳健的提升，说明其能更好地习得通用的循环记忆策略。
*   **框架有效性**：验证了通过 GRPO 优化记忆策略的可行性，且证明了统一框架能有效简化复杂的 Memory-RL 实验流程。

### 7. 优点与亮点
*   **高度模块化**：通过原子化的接口设计，研究者可以像搭积木一样组合不同的提取器、更新器和检索器。
*   **显存友好**：原生支持 GRPO 和 FlashAttention-2，使得在单卡上训练长上下文记忆模型成为可能。
*   **开箱即用**：预置了多种前沿算法（Memory-R1, RMM, MemAgent）的实现，极大方便了基准测试。

### 8. 不足与局限
*   **范式覆盖有限**：目前仅涵盖了几种代表性的记忆范式，对于更复杂的分布式记忆或多模态记忆支持尚待观察。
*   **训练效率空间**：虽然支持单卡训练，但在处理极长序列时的训练吞吐量仍有优化空间。
*   **评估深度**：实验部分主要验证了框架“能跑通且有效”，但缺乏针对不同记忆模块组合（如不同检索算法对 RL 效果的影响）的深度消融研究。

（完）
