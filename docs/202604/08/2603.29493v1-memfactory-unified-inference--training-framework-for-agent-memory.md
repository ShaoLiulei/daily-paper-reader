---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 记忆增强型大模型智能体的统一训练框架
tldr: "MemFactory是首个针对记忆增强型智能体设计的统一推理与训练框架。针对现有记忆管理研究碎片化且难以复用的问题，它借鉴LLaMA-Factory的模块化理念，将记忆生命周期抽象为可插拔的原子组件。该框架原生集成GRPO算法，支持通过多维环境奖励优化记忆策略，并兼容Memory-R1等前沿范式。实验证明，MemFactory能显著提升智能体性能，最高增幅达14.8%，为记忆驱动的AI研究提供了标准化的基础设施。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v1/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v1/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 221}]"
motivation: 现有的记忆增强大模型研究缺乏统一的基础设施，导致记忆操作的集成、训练和评估过程高度碎片化且任务特定。
method: 提出一种模块化的“乐高式”架构，将记忆生命周期抽象为原子组件，并利用GRPO算法实现基于环境奖励的记忆管理策略微调。
result: "在MemAgent架构上的实验表明，该框架在域内和分布外评估中均优于基础模型，相对性能提升最高可达14.8%。"
conclusion: MemFactory通过提供标准化、可扩展且易用的基础设施，显著降低了记忆增强智能体的开发门槛，为未来AI智能体的记忆创新铺平了道路。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管记忆增强型大语言模型（LLM）在构建长期 AI 智能体方面至关重要，且利用强化学习（RL）优化记忆操作（提取、更新、检索）已成为趋势，但目前该领域的研究高度**碎片化**。
*   **背景**：现有的记忆 RL 实现（如 Memory-R1, MemAgent, RMM）通常与特定任务深度耦合，缺乏统一的基础设施。研究人员在复现或组合不同记忆策略时面临巨大的工程障碍。
*   **整体含义**：MemFactory 旨在成为记忆智能体领域的“LLaMA-Factory”，通过提供一个统一、模块化且易用的训练与推理框架，降低记忆驱动型 AI 智能体的开发门槛。

### 2. 方法论：核心思想与关键技术
MemFactory 采用了高度模块化的“乐高式”架构，分为四个核心层：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：从原始上下文中解析事实和经验。
    *   **Updater**：执行增删改（ADD, DEL, UPDATE, NONE）操作维护记忆库。
    *   **Retriever**：负责语义检索或利用推理模型（LRM）进行重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：负责模块集成与策略执行，支持通过配置文件灵活更换底层模块。
*   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器。提供 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短期长文本记忆）两种环境，并支持格式奖励和 LLM-as-a-Judge 等多维奖励信号。
*   **训练层 (Trainer Layer)**：核心优化引擎。
    *   **关键算法**：原生集成 **GRPO (Group Relative Policy Optimization)**。该算法通过组内奖励归一化估计基准，无需额外的 Critic 网络，显著降低了显存占用，适合长上下文的记忆训练。

### 3. 实验设计
*   **数据集/场景**：使用了 MemAgent 发布的公开数据集。
*   **Benchmark**：
    *   **主任务评估**：`eval_50` 和 `eval_100`（包含不同数量的文档上下文）。
    *   **分布外 (OOD) 评估**：`eval_fwe_16384`（测试模型的泛化能力）。
*   **对比方法**：以 Qwen3-1.7B 和 Qwen3-4B-Instruct 为基座模型，对比了其原始 Checkpoint 与经过 MemFactory RL 微调后的性能差异。

### 4. 资源与算力
*   **硬件设备**：整个训练和评估流水线可以在**单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步骤（steps）。
*   **算力评价**：论文强调了该框架的低硬件门槛，证明了即使在有限的计算资源下也能进行复杂的记忆策略优化。

### 5. 实验数量与充分性
*   **实验规模**：针对两种不同规模的基座模型（1.7B 和 4B）进行了实验。
*   **客观性保障**：所有评分均基于 **4 次独立试验的平均值 (avg@4)**，以减少生成随机性带来的偏差。
*   **充分性**：实验覆盖了主任务和 OOD 场景，验证了框架在提升性能和泛化性方面的有效性。虽然实验主要集中在 MemAgent 架构上，但框架本身提供了对 Memory-R1 和 RMM 的开箱即用支持。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均得分提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **泛化能力**：较大规模的模型（4B）在 OOD 测试集上表现出更稳健的正向增益，表明 RL 微调能帮助模型学习到可迁移的记忆策略。
*   **框架有效性**：验证了通过 GRPO 优化内部记忆管理策略（如循环状态更新）的可行性。

### 7. 优点
*   **高度模块化**：解耦了记忆的提取、更新和检索，允许研究人员像搭乐高一样组合不同的记忆策略。
*   **统一性**：首次填补了记忆增强智能体缺乏统一训练框架的空白，支持多种 SOTA 范式。
*   **资源友好**：通过集成 GRPO 和 FlashAttention-2，使得在单卡上训练长上下文记忆模型成为可能。
*   **开源贡献**：提供了标准化的接口和开箱即用的基准实现。

### 8. 不足与局限
*   **范式覆盖有限**：虽然支持了三种代表性范式，但对于更复杂的分布式记忆或多模态记忆系统的支持尚待扩展。
*   **训练效率**：论文提到在训练效率方面仍有提升空间。
*   **评估维度**：目前的实验主要集中在性能得分上，对于记忆操作的解释性（例如模型为何选择删除某条记忆）缺乏深度的定性分析。
*   **模型规模限制**：实验主要在较小规模模型（<7B）上进行，在大规模模型上的表现和资源消耗情况尚不明确。

（完）
