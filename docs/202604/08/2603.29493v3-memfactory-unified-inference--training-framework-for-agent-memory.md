---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v3"
tags: ["query:ai-llm"]
score: 9.0
evidence: 记忆增强型大语言模型的训练框架
tldr: "针对长时AI智能体中记忆管理实现碎片化且缺乏统一框架的问题，本文提出了MemFactory。这是首个统一且高度模块化的记忆增强智能体训练与推理框架，采用类乐高架构实现记忆生命周期的原子化组件化。该框架原生集成GRPO算法以优化记忆管理策略，并支持Memory-R1等前沿范式。实验表明，MemFactory在MemAgent架构上相比基座模型平均性能提升显著，最高达14.8%，为记忆驱动的AI研究提供了标准化基础设施。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v3/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v3/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强大模型实现方案过于碎片化且特定于任务，缺乏统一的架构来简化记忆流水线的集成、训练和评估。
method: 提出一种模块化的“类乐高”架构，将记忆生命周期抽象为原子组件，并集成GRPO算法通过多维环境奖励优化内部记忆管理策略。
result: "在MemAgent架构上的实验验证显示，该框架使模型性能平均得到提升，相对基座模型的最高增益达到14.8%。"
conclusion: MemFactory通过提供标准化且易用的基础设施，显著降低了开发门槛，为未来记忆驱动的AI智能体创新铺平了道路。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各评估集上，MemFactory 的平均性能优于相应的基座模型，相对提升最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管记忆增强型大语言模型（LLM）在长时交互中至关重要，且利用强化学习（RL）优化记忆操作（提取、更新、检索）已成为趋势，但现有的研究实现高度碎片化。
*   **研究背景**：目前的 Memory-RL 实现通常与特定任务深度耦合，缺乏统一的基础设施。研究人员在复现或组合不同记忆策略（如将 Memory-R1 的更新模块与 RMM 的重排序模块结合）时，面临巨大的工程障碍。
*   **整体含义**：MemFactory 旨在填补这一空白，通过提供一个类似 LLaMA-Factory 的、高度模块化的统一框架，简化记忆增强型智能体的构建、训练和推理流程。

### 2. 方法论：核心思想与关键技术
MemFactory 采用了“类乐高”的四层架构设计：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作，包括：
    *   **Extractor**：从原始上下文中解析事实和经验。
    *   **Updater**：对比新旧记忆，执行 ADD、DEL、UPDATE 或 NONE 操作。
    *   **Retriever**：执行语义检索或基于推理模型的重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：作为策略执行器，通过组合原子模块构建复杂的记忆策略，支持统一的 `generate`、`rollout` 和 `inference` 接口。
*   **环境层 (Environment Layer)**：负责数据加载与奖励管理。提供 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短期上下文记忆），并支持格式奖励和 LLM-as-a-Judge 等多维奖励信号。
*   **训练层 (Trainer Layer)**：核心优化引擎，原生集成 **GRPO (Group Relative Policy Optimization)** 算法。该算法通过组内奖励归一化估计基准，无需额外的 Critic 网络，显著降低了长上下文训练时的显存开销。

### 3. 实验设计
*   **数据集/场景**：使用了 MemAgent 发布的公开训练和评估数据集。
*   **Benchmark**：
    *   `eval_50` 和 `eval_100`：主任务测试集（分别包含 50 和 100 个文档上下文）。
    *   `eval_fwe_16384`：分布外（OOD）测试集，用于验证泛化能力。
*   **对比方法**：以 Qwen3-1.7B 和 Qwen3-4B-Instruct 作为基座模型（Base Checkpoint），对比其在经过 MemFactory RL 流程微调前后的性能表现。

### 4. 资源与算力
*   **硬件设备**：论文明确指出，整个训练和评估流水线可以在 **单张 NVIDIA A800 80GB GPU** 上执行。
*   **训练细节**：每个模型训练了 250 个有效步数（valid steps）。为了提高效率，训练时将上下文长度简化为原始长度的三分之一（约 50-80 个文档）。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的模型（1.7B 和 4B）在三个测试集上进行了验证。
*   **客观性保证**：所有评分均基于 **4 次独立实验的平均值 (avg@4)**，以减少生成随机性带来的偏差。
*   **充分性评价**：实验涵盖了主任务和 OOD 任务，验证了框架在性能提升和泛化性方面的表现。虽然实验组数不算庞大，但针对其“统一框架”的定位，通过复现 SOTA 范式（MemAgent）已足以证明其有效性。

### 6. 主要结论与发现
*   **性能显著提升**：MemFactory 成功优化了记忆管理策略。Qwen3-1.7B 的平均性能提升了 **14.8%**，Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模影响泛化性**：较小的模型（1.7B）在主任务上进步明显，但在 OOD 任务上有所下降；而较大的模型（4B）在所有测试集（包括 OOD）上均表现出稳健的增长，说明大模型能更好地吸收复杂的循环记忆策略。
*   **低门槛可行性**：证明了在有限算力（单 GPU）下，通过 GRPO 优化长上下文记忆策略是切实可行的。

### 7. 优点
*   **高度模块化**：解耦了记忆操作，支持“乐高式”快速组装，极大地降低了开发门槛。
*   **显存友好**：通过集成 GRPO 算法，解决了记忆增强智能体在长序列训练时显存易溢出的痛点。
*   **开箱即用**：内置了对 Memory-R1、MemAgent 和 RMM 等前沿算法的支持，具有很强的实用价值。

### 8. 不足与局限
*   **范式覆盖有限**：目前仅涵盖了几种代表性的记忆范式，对于更复杂的分布式记忆或多模态记忆支持尚待补充。
*   **训练效率**：虽然支持单卡训练，但在处理极长上下文时的吞吐量和训练效率仍有优化空间。
*   **实验深度**：论文重点在于框架的展示，缺乏针对不同记忆模块组合（如不同 Extractor 与 Retriever 配对）的深度消融研究。

（完）
