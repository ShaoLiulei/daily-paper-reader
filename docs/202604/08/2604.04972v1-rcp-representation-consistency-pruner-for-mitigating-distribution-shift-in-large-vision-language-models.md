---
title: "RCP: Representation Consistency Pruner for Mitigating Distribution Shift in Large Vision-Language Models"
title_zh: RCP：用于缓解大型视觉语言模型中分布偏移的表示一致性剪枝器
authors: "Jianwei Zhang, Chaoning Zhang, Sihan Cao, Wang Liu, Pengcheng Zheng, Jiaxin Huang, Caiyan Qin, Yalan Ye, Wei Dong, Yang Yang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.04972v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大型视觉语言模型的剪枝方法
tldr: RCP是一个针对大型视觉语言模型（LVLM）的剪枝框架，旨在解决视觉标记过多导致的推理成本高及剪枝引发的分布偏移问题。它通过交叉注意力剪枝器实现累积标记减少，并结合延迟修复适配器（DRA）补偿信息损失。该方法仅需训练轻量级插件，在大幅减少计算开销的同时，有效保持了模型的预测性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04972v1/fig-001.webp\", \"caption\": \"TABLE I PERFORMANCE COMPARISON ON MULTIPLE BENCHMARKS WITH LLAVA-1.5-7B BACKBONE. WE REPORT ABSOLUTE SCORES AND THE COMPRESSION RETENTION RATES TO UPPER BOUND MODEL. ACC. MEANS THE AVERAGE OF NORMALIZED RELATIVE PERCENTAGES ACCURACY ACROSS 7 BENCHMARKS.\", \"page\": 6, \"index\": 1, \"width\": 708, \"height\": 1042}, {\"url\": \"assets/figures/arxiv/2604.04972v1/fig-002.webp\", \"caption\": \"Fig. 1. Layer-wise representation drift between the original and pruned models, measured by W 2 2 .\", \"page\": 2, \"index\": 2, \"width\": 502, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2604.04972v1/fig-003.webp\", \"caption\": \"TABLE II PERFORMANCE COMPARISON ON MULTIPLE BENCHMARKS WITH LLAVA-1.5-13B BACKBONE.\", \"page\": 7, \"index\": 3, \"width\": 520, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2604.04972v1/fig-004.webp\", \"caption\": \"Fig. 3. Comparison of the repair loss between the baseline variant using only the pruner and the full RCP framework to demonstrate the stabilization effect provided by the repair adapter and the repair loss.\", \"page\": 7, \"index\": 4, \"width\": 493, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.04972v1/fig-005.webp\", \"caption\": \"Fig. 2. Overview of the proposed Representation Consistency Pruner framework. (a) Overall Architecture illustrates the global system where one or more pruning modules precede a delayed repair adapter separated by multiple language model decoder layers. (b) Pruner Layer & Delayed Repair Adapter provides the structural details for the internal components of both the pruning and repair modules.\", \"page\": 3, \"index\": 5, \"width\": 1076, \"height\": 392}]"
motivation: 现有的视觉标记剪枝方法会导致隐藏层状态发生分布偏移，从而造成严重的模型性能下降。
method: 提出RCP框架，利用交叉注意力剪枝器预测累积掩码，并配合延迟修复适配器（DRA）通过FiLM调制和统计匹配来补偿被剪枝的信息。
result: "在多个基准测试中，RCP成功移除了高达88.9%的视觉标记，减少了85.7%的FLOPs，且准确率仅有轻微下降。"
conclusion: RCP通过维持表示一致性，在不微调原始模型的情况下，实现了高效且低损耗的LVLM推理加速。
---

## 摘要
由于语言解码器需要处理海量的视觉标记（tokens），大型视觉语言模型（LVLMs）面临着极高的推理成本。现有的剪枝方法往往会导致显著的性能下降，因为视觉标记的不可逆移除会引起隐藏状态的分布偏移，使其偏离预训练的全标记模式。为了解决这一问题，我们提出了表示一致性剪枝器（Representation Consistency Pruner，简称 RCP），这是一个将累积视觉标记剪枝与延迟修复机制相结合的新颖框架。具体而言，我们引入了一种交叉注意力剪枝器，利用大语言模型（LLM）的内在注意力作为基准来预测累积掩码，从而确保跨层标记减少的一致性和单调性。为了补偿由此产生的信息损失，我们设计了一个名为延迟修复适配器（DRA）的模块，该模块缓存被剪枝标记的精华，并专门对答案生成标记应用基于 FiLM 的调制。我们采用修复损失来使剪枝后的表示与全标记教师模型的一阶和二阶统计量相匹配。RCP 非常高效，因为它仅训练轻量级插件模块，同时允许在推理时进行物理标记丢弃。在 LVLM 基准测试上的广泛实验表明，RCP 在仅有轻微平均准确率下降的情况下，移除了高达 88.9% 的视觉标记，并减少了高达 85.7% 的 FLOPs，在多个广泛使用的基准测试中优于以往无需微调原始模型的方法。

## Abstract
Large Vision-Language Models (LVLMs) suffer from prohibitive inference costs due to the massive number of visual tokens processed by the language decoder. Existing pruning methods often lead to significant performance degradation because the irreversible removal of visual tokens causes a distribution shift in the hidden states that deviates from the pre-trained full-token regime. To address this, we propose Representation Consistency Pruner, which we refer to as RCP, as a novel framework that integrates cumulative visual token pruning with a delayed repair mechanism. Specifically, we introduce a cross-attention pruner that leverages the intrinsic attention of the LLM as a baseline to predict cumulative masks, ensuring consistent and monotonic token reduction across layers. To compensate for the resulting information loss, we design a delayed repair adapter denoted as DRA, which caches the essence of pruned tokens and applies FiLM-based modulation specifically to the answer generation tokens. We employ a repair loss to match the first and second-order statistics of the pruned representations with a full-token teacher. RCP is highly efficient because it trains only lightweight plug-in modules while allowing for physical token discarding at inference. Extensive experiments on LVLM benchmarks demonstrate that RCP removes up to 88.9\% of visual tokens and reduces FLOPs by up to 85.7\% with only a marginal average accuracy drop, and outperforms prior methods that avoid fine-tuning the original model on several widely used benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《RCP: Representation Consistency Pruner for Mitigating Distribution Shift in Large Vision-Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
大型视觉语言模型（LVLM）在推理时，语言解码器需要处理大量的视觉标记（Visual Tokens），导致计算开销（FLOPs）和内存占用（KV Cache）巨大。现有的剪枝方法虽然能减少标记数量，但往往导致严重的性能下降。作者发现其根本原因在于：**不可逆的标记移除会导致隐藏层状态（Hidden States）产生分布偏移（Distribution Shift）**，这种偏移会随层数加深而累积放大，最终偏离预训练时的全标记表示模式。

### 2. 方法论：核心思想与关键技术
RCP 框架的核心思想是**“累积剪枝 + 延迟修复”**，通过轻量级插件在不微调原始模型的前提下缓解分布偏移。
*   **残差交叉注意力剪枝器（Residual Cross-Attention Pruner）：**
    *   结合了 LLM 固有的注意力权重（作为基准）、可学习的查询向量（Query）以及视觉标记自身的 MLP 分数。
    *   采用**累积掩码（Cumulative Mask）**机制，确保被丢弃的标记在后续层中不会重新出现，维持单调递减。
    *   使用 Gumbel-Sigmoid 和直通估计器（Straight-Through Estimator）实现端到端的离散掩码训练。
*   **延迟修复适配器（Delayed Repair Adapter, DRA）：**
    *   **信息缓存：** 在剪枝层提取被丢弃标记的语义精华（掩码嵌入和剪枝特征嵌入）。
    *   **FiLM 调制：** 在较深层（修复层）利用缓存信息，通过特征线性调制（FiLM）对“答案生成标记”的隐藏状态进行缩放（Scale）和偏移（Shift）修正。
*   **分布对齐损失（Repair Loss）：**
    *   基于 **2-Wasserstein 距离**的思想，强制剪枝后的学生模型在隐藏层的一阶（均值）和二阶（方差）统计量上与全标记教师模型匹配，实现分布级的对齐。

### 3. 实验设计
*   **数据集：** 在 7 个主流基准测试上进行了评估，包括 GQA（推理）、MME（感知/认知）、POPE（幻觉）、ScienceQA、VQAv2、MM-Bench 和 VizWiz。
*   **基准模型（Backbone）：** LLaVA-1.5-7B（主要）和 LLaVA-1.5-13B（验证扩展性）。
*   **对比方法：** 包括 ToMe、FastV、PDrop、HiRED、VisionZip 和 DART 等多种 SOTA 剪枝/压缩方法。

### 4. 资源与算力
*   **硬件：** 使用了 **2 张 NVIDIA RTX 4090 GPU**。
*   **训练时长：** 在 VQAv2 的 10,000 个样本子集上训练 **1 个 epoch**，耗时约 **40 分钟**。
*   **参数量：** 仅训练轻量级插件。对于 7B 模型，剪枝器和适配器分别约有 16.56M 和 20.15M 参数。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 192、128 到 64 个标记（压缩率最高达 88.9%）的多个梯度。
*   **消融实验：** 针对组件有效性（剪枝器、适配器、修复损失）、剪枝器数量与位置、适配器数量与位置、保留率演变等进行了详细测试。
*   **评价：** 实验设计较为充分且客观。通过在不同规模（7B/13B）和不同任务（零样本/微调）上验证，证明了方法的通用性。对比实验均在相同硬件和基准线下进行，具有较高的公平性。

### 6. 主要结论与发现
*   **高效压缩：** RCP 能移除高达 **88.9%** 的视觉标记，减少 **85.7%** 的 FLOPs，而平均准确率下降极小（约 5%）。
*   **分布对齐的重要性：** 实验证明，仅匹配均值（一阶）是不够的，匹配二阶统计量能显著稳定模型在高压缩率下的表现。
*   **位置策略：** 早期剪枝（如第 2 层开始）结合后期修复（靠近输出层）的效果最佳。

### 7. 优点（亮点）
*   **无需微调主模型：** 保持 LVLM 骨干参数冻结，仅训练极小规模的插件，极大降低了部署成本。
*   **解决本质问题：** 首次明确指出并量化了剪枝导致的“表示漂移”现象，并提出了针对性的统计对齐方案。
*   **推理友好：** 支持物理上的标记丢弃，能直接减少 KV Cache 占用和计算延迟。

### 8. 不足与局限
*   **任务局限：** 修复机制主要针对“答案生成标记”，对于需要密集视觉输出的任务（如图像分割、目标检测）可能不完全适用。
*   **超参数敏感性：** 剪枝和修复层的位置选择（如文中提到的 2, 14, 26 层）可能需要针对不同架构的模型进行手动调优。
*   **静态压缩：** 虽然剪枝是动态的，但目标保留率（Target Rate）在推理前需预设，缺乏完全自适应的按需压缩机制。

（完）
