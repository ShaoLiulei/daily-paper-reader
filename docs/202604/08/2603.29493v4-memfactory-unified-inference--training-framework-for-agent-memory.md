---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v4"
tags: ["query:ai-llm"]
score: 9.0
evidence: 记忆增强型大语言模型智能体的统一训练框架
tldr: "MemFactory是首个针对记忆增强型智能体设计的统一、模块化训练与推理框架。针对现有记忆管理研究碎片化且缺乏通用基础设施的问题，该框架借鉴LLaMA-Factory的理念，将记忆生命周期抽象为可插拔组件，并集成GRPO算法以优化记忆策略。它支持多种前沿范式，显著降低了开发门槛，并在实验中实现了最高14.8%的性能提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v4/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v4/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强型大模型研究缺乏统一的基础设施，导致记忆操作的集成、训练和评估过程高度碎片化且难以复用。
method: 提出MemFactory框架，采用模块化“乐高式”架构抽象记忆生命周期，并原生集成GRPO算法通过环境奖励优化记忆管理策略。
result: "在MemAgent架构上的实验表明，MemFactory相比基础模型平均性能有所提升，最高相对增益达到14.8%。"
conclusion: MemFactory为记忆驱动的AI智能体提供了标准化、可扩展且易用的基础设施，有效降低了研究门槛并推动了该领域的创新。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为近期前沿范式（包括 Memory-R1、RMM 和 MemAgent）提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各评估集上，MemFactory 相比相应的基座模型平均提升了性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管记忆增强型大语言模型（LLM）在长期交互和个性化方面至关重要，且利用强化学习（RL）优化记忆操作（提取、更新、检索）已成为趋势，但该领域目前处于**高度碎片化**的状态。
*   **研究背景**：现有的记忆管理研究（如 Memory-R1, MemAgent 等）通常与特定任务深度耦合，缺乏统一的基础设施。研究人员在复现或组合不同记忆模块时面临巨大的工程负担。
*   **整体含义**：MemFactory 旨在填补这一空白，通过借鉴 LLaMA-Factory 的模块化理念，提供首个统一的、针对记忆增强型智能体的训练与推理框架，旨在降低研究门槛并推动记忆驱动型 AI 的标准化。

### 2. 论文提出的方法论
MemFactory 采用“乐高式”的四层模块化架构：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作，包括：
    *   **Extractor (提取器)**：从原始上下文解析结构化事实。
    *   **Updater (更新器)**：执行 ADD/DEL/UPDATE/NONE 等操作维护记忆库。
    *   **Retriever (检索器)**：基于语义或推理模型（LRM）进行重排序检索。
    *   **Agent Module (智能体模块)**：支持端到端的循环记忆策略。
*   **智能体层 (Agent Layer)**：作为策略执行器，通过组合上述原子模块构建复杂的记忆策略，并生成交互轨迹（Rollout）。
*   **环境层 (Environment Layer)**：负责数据加载与奖励管理。提供 `MemoryBankEnv`（长期记忆）和 `LongcontextEnv`（短期长文本）两种环境，并支持格式奖励和 LLM-as-a-Judge 等多维奖励信号。
*   **训练层 (Trainer Layer)**：核心优化引擎，原生集成 **GRPO (Group Relative Policy Optimization)** 算法。该算法通过组内奖励归一化估计优势函数，无需额外的 Critic 网络，显著降低了长文本训练时的显存开销。

### 3. 实验设计
*   **数据集与场景**：使用了 MemAgent 发布的公开数据集，涵盖长文本处理任务。
*   **Benchmark**：
    *   `eval_50` 和 `eval_100`：主任务测试集（分别包含 50 和 100 个文档上下文）。
    *   `eval_fwe_16384`：分布外（OOD）测试集，用于验证泛化能力。
*   **对比方法**：以 Qwen3-1.7B 和 Qwen3-4B-Instruct 作为基座模型（Base Checkpoint），对比经过 MemFactory 强化学习训练后的性能提升。
*   **验证范式**：框架内置了对 Memory-R1、MemAgent 和 RMM 三种前沿范式的支持。

### 4. 资源与算力
*   **硬件设备**：实验明确指出整个训练和评估流水线可以在**单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步（steps）。
*   **优化技术**：集成了 FlashAttention-2 以处理长上下文并减少显存占用。
*   **效率**：通过简化训练样本（将上下文长度缩减至原有的约 1/3，即 50-80 个文档）来提高训练效率。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型（1.7B 和 4B）在三个测试集上进行了实验。
*   **客观性保障**：所有评分均采用 **avg@4**（即每个问题进行 4 次独立试验后取平均值），以减少生成随机性带来的偏差。
*   **充分性评价**：实验涵盖了主任务和 OOD 任务，验证了框架在不同规模模型上的有效性。虽然实验组数不算庞大，但作为框架类论文，其重点在于验证“框架的可行性”和“对现有算法的优化能力”，从这个角度看实验是充分且客观的。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均得分相对提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模效应**：较大的模型（4B）在 OOD 任务上表现出更强的鲁棒性和泛化能力，而较小模型（1.7B）虽然在主任务提升明显，但在 OOD 任务上略有下降。
*   **框架有效性**：证明了通过 GRPO 算法在单卡环境下优化复杂的循环记忆策略是切实可行的。

### 7. 优点
*   **高度模块化**：通过原子操作的抽象，实现了记忆策略的“乐高式”组装，极大地提高了代码的复用性。
*   **资源友好**：原生支持 GRPO，去除了 Critic 网络，使得在有限算力（单卡）下训练长文本记忆智能体成为可能。
*   **开箱即用**：集成了多种 SOTA 算法（Memory-R1 等）和监控工具（SwanLab），对开发者非常友好。

### 8. 不足与局限
*   **范式覆盖有限**：目前仅涵盖了三种代表性范式，对于更复杂的、涉及多智能体协作的记忆管理尚待扩展。
*   **训练效率仍有提升空间**：尽管使用了 GRPO，但在处理极长序列时的吞吐量仍是瓶颈。
*   **实验深度**：论文更多侧重于框架的“集成”能力，对于不同记忆模块（如不同检索算法）之间的消融对比实验相对较少。

（完）
