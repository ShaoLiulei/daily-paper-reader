---
title: Efficient Equivariant Transformer for Self-Driving Agent Modeling
title_zh: 用于自动驾驶智能体建模的高效等变 Transformer
authors: "Scott Xu, Dian Chen, Kelvin Wong, Chris Zhang, Kion Fallah, Raquel Urtasun"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01466v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于智能体建模的Transformer架构
tldr: 针对自动驾驶中智能体建模的SE(2)等变性需求，本文提出DriveGATr架构。传统方法依赖高成本的成对相对位置编码，导致计算量随智能体数量呈二次方增长。DriveGATr利用2D射影几何代数将场景元素编码为多重向量，通过标准注意力机制实现等变性，在保持SOTA性能的同时显著降低了计算开销，在Waymo数据集上展现了优异的性能与效率平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01466v1/fig-001.webp\", \"caption\": \"Table 2. Results on WOSAC 2024 validation set. RMM is the Realism Meta Metric used for ranking. DriveGATr achieves the best RMM among its baselines. Compared to publicly available state-of-the-art methods, DriveGATr achieves comparable realism without top-k sampling and closed-loop fine-tuning.\", \"page\": 7, \"index\": 1, \"width\": 944, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2604.01466v1/fig-002.webp\", \"caption\": \"Figure 2. Training curves. We compare the envelope of minimal training loss per FLOP (left), compute efficiency as the number of agents scale (middle), and sample efficiency as the training dataset grows (right). Compared to the baselines, DriveGATr establishes a superior Pareto front in performance and computation cost thanks to its compute and sample efficiency.\", \"page\": 7, \"index\": 2, \"width\": 939, \"height\": 230}, {\"url\": \"assets/figures/arxiv/2604.01466v1/fig-003.webp\", \"caption\": \"Figure 1. The DriveGATr architecture. Figure 1a provides an overview. The poses and features of Nactor agents and Nmap nodes in each scene are encoded as multivectors in R∗ 2,0,1 and scalars. These tensors are processed by N transformer blocks, each consisting of agent and map cross attention, temporal causal self-attention, equivariant MLPs and invariant adapters. Each of these modules contain skip connections, closely mimicking a standard transformer. Figure 1b describes the attention block. For all attention blocks, the query inputs are agent of interests. The key and value inputs are agents and map nodes for the cross attentions, attending across elements per-timestep. For self-attention, attention spans the temporal axis per-agent. Figure 1c describes the equivariant MLP block. Table 1 provides details on R∗ 2,0,1 encodings. Section 3.4 provides details on each of the equivariant primitive layers. Section 3.5 describes the invariant adapter block.\", \"page\": 3, \"index\": 3, \"width\": 1035, \"height\": 480}, {\"url\": \"assets/figures/arxiv/2604.01466v1/fig-004.webp\", \"caption\": \"Figure 3. Robustness to roto-translations. We compare Transformer (left), Transformer + DRoPE (middle), and DriveGATr (right)’s robustness to roto-translations. In each figure, we overlay rollouts from the original coordinate frame vs one rotated by 90°and translated by 100m forward. Blue trajectories visualize model predictions in the original input, and red visualize predictions in the transformed scene. DriveGATr produces consistent trajectories despite closed-loop execution, demonstrating its robustness to roto-translations.\", \"page\": 8, \"index\": 4, \"width\": 898, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2604.01466v1/fig-005.webp\", \"caption\": \"Figure 5. Visualizations of multi-modal DriveGATr rollouts. In each figure, we overlay trajectories from multiple (k = 8) rollouts. Different actors of the same color corresponds to the same rollout.\", \"page\": 17, \"index\": 5, \"width\": 1010, \"height\": 1302}, {\"url\": \"assets/figures/arxiv/2604.01466v1/fig-006.webp\", \"caption\": \"Table 1. 2D encodings of geometries and transforms.\", \"page\": 5, \"index\": 6, \"width\": 698, \"height\": 182}, {\"url\": \"assets/figures/arxiv/2604.01466v1/fig-007.webp\", \"caption\": \"Figure 4. Robustness to roto-translations. Additional visualizations showing Transformer (left), Transformer + DRoPE (middle), and DriveGATr (right)’s robustness to roto-translations. In each figure, we overlay rollouts from the original coordinate frame vs one rotated by 90°and translated by 100m forward. Blue trajectories visualize model predictions in the original input, and red visualize predictions in the transformed scene.\", \"page\": 16, \"index\": 7, \"width\": 1018, \"height\": 614}]"
motivation: 现有的等变Transformer在处理自动驾驶场景时，因依赖高成本的成对相对位置编码而难以扩展至大规模场景。
method: 提出DriveGATr架构，利用2D射影几何代数将场景元素表示为多重向量，并通过等变Transformer块直接处理几何关系。
result: 在Waymo开放运动数据集上，该方法在交通仿真任务中达到了领先水平，并建立了更优的性能与计算成本帕累托前沿。
conclusion: DriveGATr证明了通过几何代数实现高效SE(2)等变建模的可行性，为大规模自动驾驶智能体建模提供了高效方案。
---

## 摘要
准确建模智能体行为是自动驾驶中的一项重要任务。这也是一个具有多种对称性的任务，例如对场景中智能体和物体顺序的等变性，或者对整个场景任意旋转平移的等变性，即 SE(2) 等变性。Transformer 架构是建模这些对称性的普遍工具。虽然标准自注意力机制本质上具有置换等变性，但显式的成对相对位置编码一直是引入 SE(2) 等变性的标准做法。然而，这种方法引入了与智能体数量呈平方关系的额外开销，限制了其在大规模场景和批量大小下的可扩展性。在这项工作中，我们提出了 DriveGATr，这是一种新型的基于 Transformer 的智能体建模架构，它在不产生现有方法计算开销的情况下实现了 SE(2) 等变性。受几何深度学习最新进展的启发，DriveGATr 将场景元素编码为 2D 射影几何代数 $\mathbb{R}^*_{2,0,1}$ 中的多重向量，并使用一堆等变 Transformer 块对其进行处理。至关重要的是，DriveGATr 使用多重向量之间的标准注意力来建模几何关系，从而消除了对昂贵的显式成对相对位置编码的需求。在 Waymo Open Motion 数据集上的实验表明，DriveGATr 在交通仿真方面与现有最先进技术相当，并在性能与计算成本之间建立了更优的帕累托前沿。

## Abstract
Accurately modeling agent behaviors is an important task in self-driving. It is also a task with many symmetries, such as equivariance to the order of agents and objects in the scene or equivariance to arbitrary roto-translations of the entire scene as a whole; i.e., SE(2)-equivariance. The transformer architecture is a ubiquitous tool for modeling these symmetries. While standard self-attention is inherently permutation equivariant, explicit pairwise relative positional encodings have been the standard for introducing SE(2)-equivariance. However, this approach introduces an additional cost that is quadratic in the number of agents, limiting its scalability to larger scenes and batch sizes. In this work, we propose DriveGATr, a novel transformer-based architecture for agent modeling that achieves SE(2)-equivariance without the computational cost of existing methods. Inspired by recent advances in geometric deep learning, DriveGATr encodes scene elements as multivectors in the 2D projective geometric algebra $\mathbb{R}^*_{2,0,1}$ and processes them with a stack of equivariant transformer blocks. Crucially, DriveGATr models geometric relationships using standard attention between multivectors, eliminating the need for costly explicit pairwise relative positional encodings. Experiments on the Waymo Open Motion Dataset demonstrate that DriveGATr is comparable to the state-of-the-art in traffic simulation and establishes a superior Pareto front for performance vs computational cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Equivariant Transformer for Self-Driving Agent Modeling》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶领域，准确建模交通智能体（车辆、行人等）的行为至关重要。该任务具有天然的几何对称性，即**SE(2)等变性**：如果整个场景发生旋转或平移，模型的预测结果也应相应地旋转或平移。
*   **现有痛点**：目前主流方法（如 SMART, BehaviorGPT）通过“成对相对位置编码（RPE）”来实现等变性，但这会导致计算复杂度随智能体数量呈**二次方增长**（$O(N^2)$），且无法利用 FlashAttention 等高度优化的标准算子，限制了模型向大规模场景扩展的能力。
*   **研究目标**：设计一种既能保证严格 SE(2) 等变性，又能保持计算效率（不依赖显式成对编码）的 Transformer 架构。

### 2. 方法论：核心思想与关键技术
论文提出了 **DriveGATr** 架构，其核心思想是将几何深度学习中的**几何代数（Geometric Algebra）**引入自动驾驶建模。
*   **几何编码**：利用 2D 射影几何代数 $\mathbb{R}^*_{2,0,1}$，将场景元素（点、线、姿态、旋转、平移）统一编码为 8 维的**多重向量（Multivectors）**。
*   **等变原语**：开发了一系列 SE(2) 等变神经层，包括：
    *   **等变线性层**：通过特定的基向量投影保持等变性。
    *   **几何双线性层**：利用几何积（Geometric Product）和连接算子（Join Operator）增强表达力。
    *   **等变注意力机制**：这是核心创新。通过多重向量的内积计算注意力权重，并引入“距离感知（Distance-awareness）”特征，使标准注意力机制在不使用 RPE 的情况下捕捉几何关系。
*   **不变适配器（Invariant Adapter）**：设计了一个新模块，将等变的几何特征转换到智能体局部坐标系下，变为不变特征，以便最终解码动作逻辑。

### 3. 实验设计
*   **数据集**：使用 **Waymo Open Motion Dataset (WOMD)**，遵循 Waymo Open Sim Agents Challenge (WOSAC) 协议。
*   **任务**：交通仿真（Traffic Simulation），生成 8 秒的闭环轨迹。
*   **Benchmark**：Realism Meta-Metric (RMM) 综合指标、minADE 等。
*   **对比方法**：
    *   **SOTA 模型**：BehaviorGPT, SMART, CAT-K。
    *   **基准变体**：标准 Transformer（靠数据增强）、Transformer + DRoPE（旋转位置编码）、Transformer + RPE（显式相对位置编码）。

### 4. 资源与算力
*   **硬件**：论文在延迟测试中明确提到了使用 **NVIDIA A5000 GPU**。
*   **训练细节**：训练步数为 250,000 步，学习率为 $10^{-3}$，采用余弦退火。
*   **模型规模**：提供了 3M（2.7M 参数）和 30M（29.4M 参数）两个版本。
*   **效率对比**：文中给出了训练延迟、峰值内存和推理延迟的具体数值。例如，DriveGATr-3M 的训练内存（6.35 GB）远低于 Transformer+RPE（15.19 GB）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在全量 Waymo 数据集上进行了 SOTA 对比。
    *   进行了**扩展性分析（Scaling Analysis）**，对比了不同 FLOPs 下的训练损失曲线。
    *   进行了**样本效率实验**，测试了在 1%、10%、50%、100% 数据量下的表现。
    *   **消融实验**：验证了不变适配器和距离感知特征的有效性。
    *   **鲁棒性测试**：可视化验证了模型在输入场景旋转/平移时预测结果的一致性。
*   **评价**：实验设计非常充分且客观。通过固定计算资源对比“性能-计算成本”前沿（Pareto Front），有力证明了其效率优势。

### 6. 主要结论与发现
*   **性能领先**：DriveGATr 在 RMM 指标上优于同等规模的基准模型，并达到了与当前最强 SOTA（如 SMART）相当的水平。
*   **效率卓越**：DriveGATr 建立了更优的帕累托前沿。在相同计算预算下，其训练损失更低；随着智能体数量增加，其计算开销增长远慢于 RPE 方法。
*   **严格等变**：数学证明和实验均证实模型具有严格的 SE(2) 等变性，对坐标系变换具有极强的鲁棒性。

### 7. 优点（亮点）
*   **理论严谨**：将复杂的几何代数理论成功落地到自动驾驶任务，并提供了详尽的数学证明。
*   **计算友好**：通过多重向量编码，将几何关系建模转化为标准矩阵运算，能够直接利用 FlashAttention 等硬件加速技术。
*   **高样本效率**：由于内置了等变性归纳偏置，模型在小数据量下的表现显著优于非等变模型。

### 8. 不足与局限
*   **维度限制**：目前主要针对 2D 平面（SE(2)），虽然文中提到可以扩展到 2.5D 或 3D，但尚未在复杂的 3D 地形或多层立交桥场景中验证。
*   **实现复杂度**：几何代数的引入增加了底层代码实现的复杂度，对于不熟悉该领域的开发者有一定的门槛。
*   **闭环微调**：相比于 CAT-K 等方法，DriveGATr 尚未充分探索闭环微调（Closed-loop fine-tuning）带来的额外增益。

（完）
