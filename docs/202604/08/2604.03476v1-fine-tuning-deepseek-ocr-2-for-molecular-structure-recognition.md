---
title: Fine-tuning DeepSeek-OCR-2 for Molecular Structure Recognition
title_zh: 微调 DeepSeek-OCR-2 用于分子结构识别
authors: "Haocheng Tang, Xingyu Dang, Junmei Wang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03476v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 视觉语言模型的监督微调
tldr: 本研究针对化学结构识别（OCSR）任务，基于DeepSeek-OCR-2开发了MolSeek-OCR模型。通过将分子图像转换为SMILES序列，并采用两阶段渐进式微调策略（从LoRA过渡到带分层学习率的选择性全参数微调），结合大规模合成与真实专利数据集进行训练。该模型在图像到序列任务中表现出色，达到了与顶尖模型相当的精确匹配率，为利用视觉语言模型处理复杂科学符号提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03476v1/fig-001.webp\", \"caption\": \"Figure 1. The overall architecture of DeepSeek-OCR-2.\", \"page\": 3, \"index\": 1, \"width\": 1002, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2604.03476v1/fig-002.webp\", \"caption\": \"Table 2. Exact matching accuracy (%) on different datasets.\", \"page\": 4, \"index\": 2, \"width\": 1008, \"height\": 397}]"
motivation: 视觉语言模型在直接应用于化学结构识别时面临训练不稳定及全参数微调难以收敛的挑战。
method: 提出一种两阶段渐进式监督微调策略，将任务建模为图像条件下的SMILES生成，并结合合成与真实专利数据进行大规模训练。
result: MolSeek-OCR在精确匹配准确率上达到了与最先进图像到序列模型相当的水平，但仍略逊于专门的图像到图结构模型。
conclusion: 研究证明了通用OCR模型经适配后在分子识别领域的潜力，并发现强化学习等后训练手段对提升SMILES序列的精确匹配效果有限。
---

## 摘要
光学化学结构识别（OCSR）对于将印刷文献中的二维分子图转换为机器可读格式至关重要。虽然视觉语言模型在端到端 OCR 任务中展现出潜力，但将其直接应用于 OCSR 仍具挑战性，且直接进行全参数监督微调往往会失败。在这项工作中，我们将 DeepSeek-OCR-2 适配于分子光学识别，将该任务表述为以图像为条件的 SMILES 生成。为了克服训练不稳定性，我们提出了一种两阶段渐进式监督微调策略：从参数高效的 LoRA 开始，逐步过渡到带有分层学习率的选择性全参数微调。我们在结合了来自 PubChem 的合成渲染图和来自 USPTO-MOL 的真实专利图像的大规模语料库上训练模型，以提高覆盖范围和鲁棒性。我们微调后的模型 MolSeek-OCR 展示了具有竞争力的能力，实现了与性能最佳的图像到序列模型相当的精确匹配准确率。然而，它仍逊于最先进的图像到图模型。此外，我们探索了强化学习风格的后训练和基于数据策划的精炼，发现它们未能提高精确 SMILES 匹配所需的严格序列级保真度。

## Abstract
Optical Chemical Structure Recognition (OCSR) is critical for converting 2D molecular diagrams from printed literature into machine-readable formats. While Vision-Language Models have shown promise in end-to-end OCR tasks, their direct application to OCSR remains challenging, and direct full-parameter supervised fine-tuning often fails. In this work, we adapt DeepSeek-OCR-2 for molecular optical recognition by formulating the task as image-conditioned SMILES generation. To overcome training instabilities, we propose a two-stage progressive supervised fine-tuning strategy: starting with parameter-efficient LoRA and transitioning to selective full-parameter fine-tuning with split learning rates. We train our model on a large-scale corpus combining synthetic renderings from PubChem and realistic patent images from USPTO-MOL to improve coverage and robustness. Our fine-tuned model, MolSeek-OCR, demonstrates competitive capabilities, achieving exact matching accuracies comparable to the best-performing image-to-sequence model. However, it remains inferior to state-of-the-art image-to-graph modelS. Furthermore, we explore reinforcement-style post-training and data-curation-based refinement, finding that they fail to improve the strict sequence-level fidelity required for exact SMILES matching.

---

## 论文详细总结（自动生成）

这是一份关于论文《Fine-tuning DeepSeek-OCR-2 for Molecular Structure Recognition》的结构化深入分析报告：

### 1. 核心问题与整体含义
*   **研究背景**：光学化学结构识别（OCSR）是将印刷文献中的二维分子图转化为机器可读格式（如 SMILES）的关键技术。
*   **核心问题**：虽然视觉语言模型（VLM）在通用 OCR 任务中表现出色，但直接将其应用于分子结构识别面临两大挑战：一是训练极不稳定，全参数监督微调（SFT）容易失败；二是分子结构的严谨性要求极高的序列保真度。
*   **研究意义**：本研究旨在探索如何通过有效的微调策略，将通用的文档理解模型（DeepSeek-OCR-2）转化为专业的化学结构识别工具。

### 2. 方法论
论文提出了 **MolSeek-OCR**，其核心思想是将 OCSR 建模为图像条件下的 SMILES 序列生成任务，并采用**两阶段渐进式微调策略**：
*   **第一阶段：LoRA SFT**
    *   使用参数高效的 LoRA 技术，在注意力层、前馈网络及视觉-语言投影接口上进行微调。
    *   目的：初步建立视觉特征与化学字符串（SMILES）之间的对齐关系，稳定训练初期。
*   **第二阶段：渐进式全参数微调**
    *   冻结底层的视觉 Tokenizer 和输入嵌入层，仅优化高层视觉编码器、投影接口和自回归解码器。
    *   **分层学习率（Split Learning Rates）**：为视觉分支设置较低的学习率（$5 \times 10^{-6}$），为语言分支设置较高的学习率（$2 \times 10^{-5}$），以平衡感知与生成的适配速度。
*   **数据策略**：结合了 PubChem 的合成数据（模拟不同绘图风格）和 USPTO-MOL 的真实专利图像，以增强模型对真实文档噪声的鲁棒性。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：LoRA 阶段使用 192k 样本，全参数阶段扩展至 800k 样本。
    *   **测试集（Benchmark）**：涵盖合成集（Indigo, ChemDraw）、真实集（USPTO, CLEF, Staker, UOB, ACS）以及针对真实集的扰动版本（Perturbed sets）。
*   **对比方法**：
    *   **规则类**：MolVec, OSRA。
    *   **图像到序列类**：Img2Mol, DECIMER, SwinOCSR。
    *   **图像到图类**：MolScribe, Image2Graph, ChemGrapher。
*   **评价指标**：SMILES 字符串的精确匹配准确率（Exact Matching Accuracy）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及总训练时长。
*   **训练细节**：
    *   Batch Size：每台设备为 4，梯度累积步数为 8。
    *   优化器：AdamW。
    *   步数：LoRA 阶段 3000 步，全参数阶段 2500 步。

### 5. 实验数量与充分性
*   **实验规模**：模型在 11 个不同的基准数据集上进行了测试，涵盖了从理想合成图到带有噪声的期刊/专利图的多种场景。
*   **消融与探索**：
    *   对比了 LoRA 与全参数微调的效果（全参数微调在所有指标上均有显著提升）。
    *   **负面实验探索**：详细记录了强化学习（GSPO）和数据精炼（ReFT）在 OCSR 任务中的失败尝试，证明了这些方法虽能提升图一致性，但会破坏 SMILES 的精确匹配。
*   **充分性评价**：实验设计较为全面，对比了不同技术路线的主流模型，结果具有客观性。

### 6. 主要结论与发现
*   **性能表现**：MolSeek-OCR 达到了与目前最强的“图像到序列”模型（如 DECIMER）相当的水平。
*   **架构对比**：VLM 方案虽然强大，但目前仍**逊于**专门的“图像到图”模型（如 MolScribe），因为后者能显式预测原子和键的几何布局。
*   **后训练局限**：强化学习等手段在提升分子图逻辑正确性的同时，往往会改变 SMILES 的标准序列格式，导致精确匹配率下降。

### 7. 优点
*   **稳定性方案**：成功解决了通用 VLM 在专业科学领域微调时的不收敛问题。
*   **鲁棒性强**：通过混合合成与真实数据，模型在处理模糊、低质量的专利扫描件时表现稳健。
*   **开源贡献**：提供了完整的代码、数据和模型权重，便于社区复现和改进。

### 8. 不足与局限
*   **序列依赖**：SMILES 是一种线性表示，对微小字符差异敏感，模型缺乏对分子拓扑结构的显式几何理解。
*   **性能上限**：在处理手性（Chirality）和极其复杂的环系结构时，准确率仍有待提高。
*   **推理成本**：作为基于 3B 参数规模的 VLM，其推理开销可能高于轻量级的专用 OCSR 工具。

（完）
