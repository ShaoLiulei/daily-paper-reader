---
title: "Attention Editing: A Versatile Framework for Cross-Architecture Attention Conversion"
title_zh: Attention Editing：一种通用的跨架构注意力转换框架
authors: "Zhen Cheng, Hao-Bo Yang, Wan-Yi Huang, Jin-Long Li"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05688v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型的跨架构注意力转换
tldr: 本研究针对大语言模型在长文本推理中KV缓存开销巨大的问题，提出了Attention Editing框架。该框架无需从头预训练，即可将已训练模型的注意力机制转换为更高效的架构（如MLA或GateSWA）。通过层级教师强制优化和模型级蒸馏，在Qwen3系列模型上实现了显著的推理效率提升，并保持了竞争力性能，同时在国产昇腾硬件上验证了其可行性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05688v1/fig-001.webp\", \"caption\": \"Figure 4: Performance under different concurrency levels for three input lengths. The upper row shows the variation in throughput , while the lower row shows the variation in TTFT. The experimental setup is as follows: inference is conducted on a single H800 GPU using vLLM version 0.16.0, with gpu-memory-utilization set to 0.8. Missing data in the figure indicate that the model is unable to handle that level of concurrency simultaneously.\", \"page\": 11, \"index\": 1, \"width\": 960, \"height\": 515}, {\"url\": \"assets/figures/arxiv/2604.05688v1/fig-002.webp\", \"caption\": \"Figure 5: Throughput under different concurrency levels. The experimental setup is as follows: inference is conducted on a single Ascend 910B using vLLM/vLLM-Ascend version 0.16.0. Missing data in the figure indicate that the model is unable to handle that level of concurrency simultaneously. The results about Qwen3-8B-MLA are missing, because the MLA kernels in higher-version vLLM-Ascend impose customized requirements on specific parameter configuration.\", \"page\": 11, \"index\": 2, \"width\": 882, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.05688v1/fig-003.webp\", \"caption\": \"Table 4: Accuracy (%) of different attention-variant models on GSM8K and C-Eval in thinking-mode.\", \"page\": 10, \"index\": 3, \"width\": 483, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2604.05688v1/fig-004.webp\", \"caption\": \"Table 3: Performance (%) of different attention variants under few-shot evaluation. The model is required to directly output the answer, and accuracy is computed by matching the predicted answer against the ground truth.\", \"page\": 9, \"index\": 4, \"width\": 864, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.05688v1/fig-005.webp\", \"caption\": \"Figure 2: Illustration of the forward propogation in two stages of progressive distillation. (a) Block-wise teacher forcing distillation: the inputs to each layer comes from the original model; (b) Model-level distillation: each architecture maintain its own inputs. The gray shading indicates that these two modules can be merged into a single model and trained efficiently using sharding strategies such as FSDP or DeepSpeed.\", \"page\": 5, \"index\": 5, \"width\": 577, \"height\": 402}]"
motivation: 旨在解决长上下文推理中KV缓存带来的内存和带宽瓶颈，通过架构转换提升现有大模型的推理效率。
method: 采用渐进式蒸馏框架，结合层级中间激活监督和模型级下一标记分布蒸馏，将原始注意力模块替换为可学习的目标模块。
result: 在Qwen3-8B和30B模型上成功应用，显著降低了推理成本并保持了模型性能，且在昇腾910B集群上完成了实证。
conclusion: Attention Editing证明了大规模注意力架构转换的稳健性与可行性，为模型高效部署提供了实用方案。
---

## 摘要
在长上下文和长生成场景下，键值（KV）缓存内存和带宽日益成为大语言模型推理成本的主要瓶颈。诸如多头潜变量注意力（MLA）和混合滑动窗口注意力（SWA）等架构可以缓解这一限制，但将它们集成到现有模型中仍然具有挑战性。先前的方法对源注意力模块和目标注意力模块都提出了细粒度的结构要求，无法满足实际部署中的可行性需求。我们提出了 Attention Editing，这是一个实用的框架，用于在无需从头开始重新预训练的情况下，为已训练的大语言模型（LLM）转换新的注意力架构。Attention Editing 将原始注意力替换为可学习的目标模块，并使用渐进式蒸馏进行训练，该过程包括：（1）带有中间激活监督的逐层教师强制优化，以防止冷启动误差累积；（2）针对下一标记（next-token）分布的模型级蒸馏，并可选择性地通过弱特征匹配进行正则化。我们在两种不同的目标架构——MLA 和 GateSWA（一种门控混合 SWA 设计）上实例化了该框架，并将其应用于 Qwen3-8B 和 Qwen3-30B-A3B。所得模型在保持竞争性能的同时，实现了显著的效率提升，证明了大规模注意力转换既可行又稳健。值得注意的是，实验是在昇腾 910B 集群上进行的，为国产硬件上的训练提供了实际案例研究。

## Abstract
Key-Value (KV) cache memory and bandwidth increasingly dominate large language model inference cost in long-context and long-generation regimes. Architectures such as multi-head latent attention (MLA) and hybrid sliding-window attention (SWA) can alleviate this bound, but integrating them into existing models remains difficult. Prior methods impose fine-grained structural requirements on both source and target attention modules, which cannot meet the feasible requirement in practical deployment. We present Attention Editing, a practical framework for converting already-trained large language models (LLMs) with new attention architectures without re-pretraining from scratch. Attention editing replaces the original attention with a learnable target module and trains it using progressive distillation, consisting of (1) layer-wise teacher-forced optimization with intermediate activation supervision to prevent cold-start error accumulation, and (2) model-level distillation on next-token distributions, optionally regularized by weak feature matching. We instantiate the framework on two different target--MLA and GateSWA, a gated hybrid SWA design, and apply it to Qwen3-8B and Qwen3-30B-A3B. The resulting models maintain competitive performance while delivering substantial efficiency improvements, demonstrating that large-scale attention conversion is both feasible and robust. Notably, experiments are conducted on an Ascend 910B clusters, offering a practical training case study on domestic hardware.

---

## 论文详细总结（自动生成）

### 论文总结：Attention Editing - 通用的跨架构注意力转换框架

#### 1. 核心问题与研究背景
*   **研究动机**：大语言模型（LLM）在处理长文本和复杂 Agent 任务时，KV Cache（键值缓存）的内存占用和带宽需求成为推理效率的主要瓶颈。
*   **核心问题**：虽然 MLA（多头潜变量注意力）和 SWA（滑动窗口注意力）等架构能显著降低开销，但将现有已训练好的模型（尤其是经过 SFT/RL 的 Chat 模型）转换为这些架构通常需要从头预训练，或者依赖极其复杂的“权重手术”（如 SVD 分解），这在实际部署中缺乏通用性和灵活性。
*   **整体含义**：本文提出了一种名为 **Attention Editing** 的框架，旨在通过一种结构不敏感、数据驱动的方法，将已训练模型的注意力机制无缝转换为更高效的变体。

#### 2. 方法论：渐进式蒸馏（Progressive Distillation）
该框架的核心思想是将目标注意力模块视为“可学习的替代品”，通过两个阶段的蒸馏完成转换：
*   **阶段 I：层级教师强制蒸馏（Layer-wise Teacher-forcing）**：
    *   **操作**：将学生模型中随机初始化的新注意力模块嵌入教师模型。
    *   **细节**：输入固定为教师模型的中间激活值（Hidden States），训练目标是让学生模块的输出（经过 `o_proj` 后）匹配教师模块的对应输出。
    *   **目的**：解决冷启动问题，防止随机初始化的模块在深层网络中产生误差累积。
*   **阶段 II：模型级蒸馏（Model-level Distillation）**：
    *   **操作**：学生模型进行完整的端到端前向传播。
    *   **细节**：使用 KL 散度匹配教师和学生的下一标记（Next-token）概率分布，并辅以中间层特征的余弦相似度作为正则化项。
    *   **课程学习**：训练数据从通用领域逐渐过渡到高难度的数学、代码和推理数据。

#### 3. 实验设计
*   **基础模型**：Qwen3-8B（稠密模型）和 Qwen3-30B-A3B（MoE 模型）。
*   **目标架构**：
    1.  **MLA**：压缩 KV 状态至低维潜空间。
    2.  **GateSWA**：一种新型混合架构，结合了滑动窗口（窗口大小 128）和全注意力层（比例 5:1），并引入元素级门控机制以消除“注意力汇”（Attention Sink）。
*   **Benchmark**：
    *   **基础能力**：ARC-Easy/Challenge, MMLU, C-Eval（Few-shot 模式）。
    *   **推理能力**：GSM8K, C-Eval（Thinking 模式，即开启思维链）。
    *   **推理效率**：首字延迟（TTFT）、吞吐量（Throughput）、KV Cache 占用率。

#### 4. 资源与算力
*   **硬件平台**：全流程在 **华为昇腾（Ascend）910B 集群** 上完成，展示了国产硬件在大模型后训练阶段的实用性。
*   **算力消耗**：
    *   **数据量**：阶段 I 使用 2B tokens，阶段 II 使用 6B tokens，总计约 8B tokens。
    *   **对比**：该数据量不到 Qwen3 原始预训练数据量（36T tokens）的千分之一。
    *   **时长**：文中未明确给出具体的训练总时长（小时/天），但强调了其数据高效性。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8B 和 30B 两种参数量级，以及 Dense 和 MoE 两种主流架构，验证了框架的普适性。
*   **对比维度**：不仅对比了模型精度，还详细测试了在不同并发（Concurrency）下的推理性能提升。
*   **客观性**：实验采用了第三方评估工具（如 AIS-Bench）和主流推理框架（vLLM），对比基准清晰，实验设计较为客观。

#### 6. 主要结论与发现
*   **转换可行性**：证明了无需复杂的权重初始化，仅靠随机初始化+渐进式蒸馏即可实现注意力架构的深度重构。
*   **性能保持**：转换后的模型在 ARC 和 MMLU 等任务上几乎无损，在 GSM8K 数学推理上达到了原模型 98% 以上的水平。
*   **效率飞跃**：GateSWA 架构将 KV Cache 占用降低了约 80%，在高并发场景下显著提升了吞吐量；MLA 在保持表达力的同时大幅优化了内存带宽。

#### 7. 优点
*   **通用性极强**：不依赖特定的矩阵分解技术，理论上支持任何形式的注意力变体转换。
*   **数据效率高**：利用极少量的数据即可找回原模型的大部分能力，适合私有化部署和快速架构迭代。
*   **工程落地性**：提供了在国产昇腾芯片上的完整训练案例，对国内开发者具有重要参考价值。

#### 8. 不足与局限
*   **数据规模限制**：实验仅使用了 8B tokens，尚未探索更大规模数据是否能完全抹平与原模型的微小差距。
*   **Agent 能力验证不足**：虽然提及了 Agent 趋势，但实验主要集中在 Chat 和推理 Benchmark，缺乏对工具调用、长程规划等核心 Agent 能力的深度测试。
*   **知识损失风险**：在某些通用知识类任务（如 C-Eval）上仍存在性能下滑，说明注意力机制的改变对模型内置知识的检索仍有一定负面影响。

（完）
