---
title: "ExFusion: Efficient Transformer Training via Multi-Experts Fusion"
title_zh: ExFusion：通过多专家融合实现高效的 Transformer 训练
authors: "Jiacheng Ruan, Daize Dong, Xiaoye Qu, Tong Zhu, Ting Liu, Yuzhuo Fu, Yu Cheng, Suncheng Xiang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27965v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 通过多专家融合实现高效的Transformer训练
tldr: 针对混合专家模型（MoE）在训练、存储和部署中存在的高昂成本问题，本文提出了一种名为 ExFusion 的高效 Transformer 训练方法。该方法在初始化阶段将前馈网络（FFN）扩展为多专家配置，并在训练过程中通过权重将多个专家融合为单个等效专家进行前向计算。ExFusion 在保持与标准稠密模型相当的计算开销下，引入了多专家特性，并在训练后通过参数融合消除了额外的存储和部署负担，在 CV 和 NLP 任务中均表现出色。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27965v1/fig-001.webp\", \"caption\": \"Fig. 1: (a) Illustration of the vanilla Top-k MoE. Given an input, the MoE layer employs a router to identify and select the top k most pertinent experts from N . In this paper, we refer to this configuration of MoE as MoE-N -k. (b) Comparison of the performance and training GPU hours among the baseline dense model (ViT-B/16), vanilla MoE models, and our ExFusion. All models are pre-trained on the ImageNet-1k dataset and share the same number of activated parameters.\", \"page\": 1, \"index\": 1, \"width\": 448, \"height\": 654}, {\"url\": \"assets/figures/arxiv/2603.27965v1/fig-002.webp\", \"caption\": \"TABLE III: Results on the ADE20K.\", \"page\": 6, \"index\": 2, \"width\": 500, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2603.27965v1/fig-003.webp\", \"caption\": \"TABLE IV: Comparison results on the COCO dataset.\", \"page\": 6, \"index\": 3, \"width\": 454, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2603.27965v1/fig-004.webp\", \"caption\": \"TABLE II: Results on the ImageNet-1k.\", \"page\": 6, \"index\": 4, \"width\": 442, \"height\": 475}, {\"url\": \"assets/figures/arxiv/2603.27965v1/fig-005.webp\", \"caption\": \"Fig. 4: Comparison results on C4.\", \"page\": 7, \"index\": 5, \"width\": 503, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2603.27965v1/fig-006.webp\", \"caption\": \"Fig. 5: Visualization of expert weight variations in DeiT-T†.\", \"page\": 8, \"index\": 6, \"width\": 1063, \"height\": 520}, {\"url\": \"assets/figures/arxiv/2603.27965v1/fig-007.webp\", \"caption\": \"Fig. 2: The overall architecture of ExFusion-sw and ExFusion-dw. The number of experts is set to four for demonstration. The dashed lines represent the multi-experts fusion process.\", \"page\": 4, \"index\": 7, \"width\": 1020, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2603.27965v1/fig-008.webp\", \"caption\": \"TABLE I: Comparison results of Dense ViT-B/16, the corresponding MoE ViT-B/16 models, and ExFusion-mb ViT-B/16 on the ImageNet-1k dataset.\", \"page\": 5, \"index\": 8, \"width\": 532, \"height\": 301}, {\"url\": \"assets/figures/arxiv/2603.27965v1/fig-009.webp\", \"caption\": \"Fig. 3: The overall architecture of ExFusion-mb. During training, the router and memory bank control the importance of experts depending on the input data. After training, the final weights in the memory bank is employed to fuse experts. The number of experts is set to four for demonstration. The dashed lines represent the multi-experts fusion process.\", \"page\": 5, \"index\": 9, \"width\": 968, \"height\": 365}]"
motivation: 旨在解决 MoE 模型在提升性能的同时带来的巨大计算资源消耗、存储开销及部署困难等挑战。
method: 在训练期间将 FFN 扩展为多专家结构，并通过学习到的权重将其动态融合为单个统一的专家进行计算，实现高效训练。
result: 在多种计算机视觉和自然语言处理任务上的实验证明，该方法能以极低的额外成本显著提升模型性能。
conclusion: ExFusion 成功地将多专家模型的性能优势与稠密模型的部署效率相结合，为高效 Transformer 训练提供了新方案。
---

## 摘要
混合专家（MoE）模型通过增加稠密架构的容量显著提升了性能。然而，直接训练 MoE 模型需要大量的计算资源，并在参数存储和部署方面引入了额外开销。因此，开发一种既能利用 MoE 的多专家能力来增强性能，又只需极小额外成本的方法至关重要。为此，我们提出了一种名为 ExFusion 的新型预训练方法，通过多专家融合提高 Transformer 训练的效率。具体而言，在初始化阶段，ExFusion 将 Transformer 的前馈网络（FFN）升级为多专家配置，并为每个专家分配一个权重以便后续进行参数融合。在训练过程中，这些权重允许将多个专家融合为一个与原始 FFN 等效的统一专家，随后将其用于前向计算。因此，与标准的稠密训练相比，ExFusion 在训练过程中引入了多专家特性，且仅产生微小的计算成本。训练完成后，利用学习到的权重将多专家整合为一个统一的专家，从而消除了存储和部署方面的额外开销。在多种计算机视觉和自然语言处理任务上的广泛实验证明了所提方法的有效性。

## Abstract
Mixture-of-Experts (MoE) models substantially improve performance by increasing the capacity of dense architectures. However, directly training MoE models requires considerable computational resources and introduces extra overhead in parameter storage and deployment. Therefore, it is critical to develop an approach that leverages the multi-expert capability of MoE to enhance performance while incurring minimal additional cost. To this end, we propose a novel pre-training approach, termed ExFusion, which improves the efficiency of Transformer training through multi-expert fusion. Specifically, during the initialization phase, ExFusion upcycles the feed-forward network (FFN) of the Transformer into a multi-expert configuration, where each expert is assigned a weight for later parameter fusion. During training, these weights allow multiple experts to be fused into a single unified expert equivalent to the original FFN, which is subsequently used for forward computation. As a result, ExFusion introduces multi-expert characteristics into the training process while incurring only marginal computational cost compared to standard dense training. After training, the learned weights are used to integrate multi-experts into a single unified expert, thereby eliminating additional overhead in storage and deployment. Extensive experiments on a variety of computer vision and natural language processing tasks demonstrate the effectiveness of the proposed method.

---

## 论文详细总结（自动生成）

这是一份关于论文《ExFusion: Efficient Transformer Training via Multi-Experts Fusion》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不显著增加计算开销、存储空间和推理延迟的前提下，利用混合专家（MoE）架构的强大表达能力来提升标准 Transformer 模型的性能？
*   **研究背景**：传统的 MoE 虽然能通过增加参数量提升模型容量，但面临三大痛点：
    1.  **训练低效**：在标准训练配置下，MoE 往往比稠密模型更难收敛且耗时更长。
    2.  **存储压力**：MoE 引入了海量参数（如 64 专家版本参数量增加数十倍），难以在显存受限设备上部署。
    3.  **推理开销**：尽管是稀疏激活，但路由逻辑和多专家权重管理增加了部署复杂性。

### 2. 方法论：ExFusion 核心思想
ExFusion 的核心逻辑是**“训练时多专家，推理时单专家”**。它通过参数融合（Parameter Fusion）技术，将多个专家的权重在训练前向传播前合并为一个等效的 FFN 层。

*   **核心流程**：
    1.  **初始化**：将 FFN 中的线性层扩展为 $N$ 个专家（线性层副本）。
    2.  **加权融合**：在每次前向计算前，根据分配的权重 $w_i$ 将 $N$ 个专家的参数矩阵线性组合：$\bar{E} = \sum w_i E_i$。
    3.  **等效计算**：使用融合后的单一专家 $\bar{E}$ 处理输入。由于线性性质，这在数学上等同于分别计算再加权求和，但计算量大幅降低。
    4.  **部署**：训练结束后，利用最终权重永久融合专家，模型结构回归为标准的稠密 Transformer。

*   **三种变体**：
    *   **ExFusion-sw (静态权重)**：所有专家权重固定（如 $1/N$），利用集成学习原理降低方差。
    *   **ExFusion-dw (动态权重)**：权重设为可学习参数，自动调节各专家的贡献。
    *   **ExFusion-mb (记忆库权重)**：引入路由层（Router）感知输入数据，并使用带有动量更新的记忆库（Memory Bank）来平滑和锁定最终融合权重。

### 3. 实验设计与 Benchmark
*   **计算机视觉 (CV)**：
    *   **任务**：图像分类（ImageNet-1k）、语义分割（ADE20K）、目标检测（COCO）。
    *   **基准模型**：ViT, DeiT, PVT, Swin Transformer。
*   **自然语言处理 (NLP)**：
    *   **任务**：语言模型预训练（C4 数据集）、下游任务微调（GLUE 榜单）。
    *   **基准模型**：T5-base。
*   **对比方法**：标准稠密模型（Dense）、传统的 Top-k MoE 模型（如 MoE-8-1 到 MoE-64-1）。

### 4. 资源与算力消耗
*   **硬件环境**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练时长示例**：
    *   **ViT-B 稠密模型**：240 GPU 小时。
    *   **ExFusion-mb (ViT-B)**：264 GPU 小时（仅比稠密模型增加约 **10%**）。
    *   **对比 MoE-64-1**：需要 720 GPU 小时（是 ExFusion 的近 3 倍）。
*   **显存占用**：ExFusion-mb 在训练时的显存占用远低于传统 MoE（25GB vs 37GB），推理时则与稠密模型完全一致。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从基础分类到下游密集预测任务的全面验证，并跨越了 CV 和 NLP 两个领域。
*   **消融实验**：针对专家数量（4, 8, 16）、动量值、路由设置、MoE 层放置位置等进行了详细测试。
*   **客观性**：实验对比了相同激活参数量下的性能，并展示了训练曲线（Loss 变化），证明了 ExFusion 在收敛速度上的优势（在 T5 预训练中比稠密模型快 3.3 倍）。

### 6. 主要结论与发现
*   **性能提升**：在不增加推理参数的前提下，ExFusion 显著提升了基准模型。例如，ViT-B 在 ImageNet-1k 上提升了 **3.2%**，DeiT-T 提升了 **1.9%**。
*   **训练效率**：ExFusion 解决了传统 MoE 在小数据集或标准训练周期下表现不佳的问题，实现了“低成本换高收益”。
*   **专家特化**：可视化证明，即使最终融合，不同专家在训练过程中也表现出了明显的路由偏好和特化现象。

### 7. 优点与亮点
*   **部署零成本**：这是该方法最大的亮点。它生成的模型与标准 Transformer 完全兼容，无需任何特殊的推理引擎或算子支持。
*   **即插即用**：可以轻松集成到现有的各种 Transformer 变体中。
*   **理论支撑**：利用集成学习中的方差缩减理论解释了为何融合多个“弱专家”能提升泛化能力。

### 8. 不足与局限
*   **专家扩展性限制**：实验发现专家数量增加（如从 4 增加到 16）反而会导致性能下降，这可能是因为过多的专家导致参数更新被过度稀释（Over-smoothing）。
*   **牺牲动态特化**：由于推理时融合为单专家，它无法像传统 MoE 那样针对特定输入动态调用特定专家，在处理极大规模多任务场景时可能不如传统 MoE 灵活。
*   **超大规模验证缺失**：目前实验主要集中在 Base 级别的模型（如 ViT-B, T5-base），在千亿级参数（LLM）上的表现尚需进一步验证。

（完）
