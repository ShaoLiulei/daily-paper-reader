---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v3"
tags: ["query:ai-llm"]
score: 9.0
evidence: 记忆增强型大语言模型智能体的统一训练框架
tldr: 针对记忆增强型大语言模型在长期智能体应用中存在的实现碎片化问题，本文推出了首个统一且高度模块化的训练与推理框架MemFactory。该框架借鉴LLaMA-Factory的成功经验，将记忆的提取、更新和检索等生命周期抽象为“乐高式”原子组件，并原生集成GRPO算法，支持通过多维环境奖励优化记忆管理策略。MemFactory兼容Memory-R1等前沿范式，实验证明其能显著提升智能体性能，为记忆驱动的AI研究提供了标准化的基础设施。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v3/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v3/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有记忆增强型智能体的训练与推理实现过于碎片化且缺乏统一框架，阻碍了复杂记忆管理策略的优化与集成。
method: 提出首个模块化框架MemFactory，将记忆生命周期抽象为原子组件，并集成GRPO算法以支持基于环境奖励的记忆策略微调。
result: "在MemAgent架构上的实验显示，该框架能显著提升模型性能，平均相对增益最高达14.8%。"
conclusion: MemFactory为记忆驱动的AI智能体提供了标准化、可扩展的基础设施，有效降低了研究门槛并推动了该领域的创新。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们推出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各评估集上，MemFactory 的平均性能优于相应的基座模型，相对提升最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：为了让大语言模型（LLM）进化为具备长期能力的 AI 智能体，记忆增强技术至关重要。传统的启发式记忆管理（如简单的 RAG）在处理长期对话时容易出现冗余和冲突。
*   **核心问题**：近期研究（如 Memory-R1, MemAgent）证明利用强化学习（RL）优化记忆操作（提取、更新、检索）效果显著，但这些实现通常是**高度碎片化、任务特定且代码耦合严重**的。
*   **整体含义**：本文推出了 **MemFactory**，这是首个模仿 LLaMA-Factory 成功经验的、统一且模块化的记忆智能体训练与推理框架。它旨在通过标准化基础设施，降低研究人员开发和优化“记忆-强化学习（Memory-RL）”流水线的门槛。

### 2. 核心方法论
MemFactory 采用四层解耦架构，支持“乐高式”构建智能体：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：从原始上下文解析事实。
    *   **Updater**：执行 ADD/DEL/UPDATE/NONE 等操作维护记忆库。
    *   **Retriever**：支持语义检索及基于推理模型（LRM）的重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：负责模块组装与策略执行，支持训练模式（生成轨迹）和推理模式（API/vLLM 加速）。
*   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器。提供 `MemoryBankEnv`（长期记忆）和 `LongcontextEnv`（短期上下文）两种环境，并集成格式奖励和 LLM-as-a-Judge 评估。
*   **训练层 (Trainer Layer)**：核心优化引擎。
    *   **关键算法**：原生集成 **GRPO (Group Relative Policy Optimization)**。该算法通过组内奖励归一化估计基准，无需额外的 Critic 模型，极大地节省了显存，适合长上下文的记忆训练。

### 3. 实验设计
*   **实验对象**：以 **MemAgent** 架构为基础进行验证。
*   **基座模型**：Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集/场景**：
    *   使用 MemAgent 公开的训练数据（简化了上下文长度以提高效率）。
    *   **Benchmark**：包含两个主任务测试集（`eval_50`, `eval_100`）和一个分布外（OOD）测试集（`eval_fwe_16384`）。
*   **对比方法**：对比了经过 MemFactory RL 微调后的模型与原始基座模型（Base Checkpoint）的性能差异。

### 4. 资源与算力
*   **硬件设备**：整个训练和评估流水线可以在**单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步（valid steps）。
*   **优化技术**：集成了 FlashAttention-2 以处理长上下文并减少显存占用。

### 5. 实验数量与充分性
*   **实验规模**：针对两种不同规模的模型（1.7B 和 4B）在三个不同难度的测试集上进行了实验。
*   **客观性保障**：所有评分均采用 **avg@4**（即每个问题进行 4 次独立实验后取平均值），以减少生成随机性带来的偏差。
*   **充分性评价**：实验涵盖了主任务和 OOD 任务，验证了框架的有效性和泛化能力。虽然主要针对 MemAgent 范式进行了深入验证，但框架本身已内置了对 Memory-R1 和 RMM 的支持，展示了较好的覆盖面。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆处理能力。Qwen3-1.7B 平均提升 **14.8%**，Qwen3-4B-Instruct 平均提升 **7.3%**。
*   **模型规模效应**：较大的模型（4B）在 OOD 任务上表现更稳健，能够更好地将学到的循环记忆策略迁移到未见过的设置中；而较小模型（1.7B）虽然在主任务提升大，但在 OOD 任务上略有下降。
*   **框架有效性**：证明了即使在有限的算力（单 GPU）和简化数据下，通过统一框架进行 Memory-RL 训练也能取得实质性进展。

### 7. 优点
*   **高度模块化**：通过“原子操作”抽象，研究人员可以像搭乐高一样更换检索器或更新策略。
*   **显存友好**：集成 GRPO 算法，解决了记忆增强任务中长上下文与 RL 训练显存开销巨大的矛盾。
*   **开箱即用**：预置了 Memory-R1、RMM 等前沿算法的实现，具有很强的实用价值。

### 8. 不足与局限
*   **范式覆盖有限**：虽然支持多种 SOTA 算法，但目前实证部分主要集中在 MemAgent 范式上。
*   **训练效率**：论文提到在训练效率方面仍有进一步优化的空间。
*   **小模型泛化风险**：实验显示小模型在经过特定记忆任务微调后，在分布外（OOD）任务上的泛化能力可能受损。

（完）
