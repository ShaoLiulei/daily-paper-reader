---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 记忆增强型大模型智能体的统一训练框架
tldr: 针对记忆增强型大模型智能体在记忆管理方面缺乏统一框架的问题，本文推出MemFactory。它是首个高度模块化的训练与推理框架，将记忆的提取、更新和检索等生命周期抽象为“乐高式”原子组件。该框架原生支持GRPO强化学习算法，并集成了Memory-R1等前沿范式，旨在通过标准化的基础设施降低开发门槛，提升智能体的长期记忆处理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v1/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v1/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 221}]"
motivation: 现有的记忆增强智能体实现方案高度碎片化且缺乏通用性，亟需一个统一的框架来简化复杂记忆流水线的集成、训练与评估。
method: MemFactory采用模块化架构实现记忆操作的即插即用，并利用组相对策略优化（GRPO）算法根据环境奖励微调内部记忆管理策略。
result: "在MemAgent架构上的实验显示，该框架在域内和分布外测试中均显著优于基础模型，性能相对提升最高达14.8%。"
conclusion: MemFactory为记忆驱动型AI智能体提供了标准化且易用的基础设施，为未来长短期记忆智能体的创新研究铺平了道路。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为包括 Memory-R1、RMM 和 MemAgent 在内的近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管记忆增强型大语言模型（LLM）在长期对话和复杂任务中至关重要，且利用强化学习（RL）优化记忆操作（提取、更新、检索）已成为趋势，但目前的实现方案高度**碎片化**且**任务特定**。
*   **研究动机**：缺乏一个像 LLaMA-Factory 那样统一、模块化且易用的基础设施，导致研究人员在复现、组合或改进不同的记忆策略（如 Memory-R1, MemAgent 等）时面临巨大的工程障碍。
*   **整体含义**：MemFactory 旨在通过提供一个标准化的“乐高式”框架，统一记忆智能体的训练、推理和评估流程，降低记忆驱动型 AI 智能体的开发门槛。

### 2. 论文提出的方法论
MemFactory 采用四层模块化架构：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：从原始上下文解析事实和经验。
    *   **Updater**：对比新旧记忆，执行 ADD、DEL、UPDATE 或 NONE 操作。
    *   **Retriever**：执行语义检索或基于推理模型（LRM）的重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent）。
*   **智能体层 (Agent Layer)**：作为策略执行器，允许用户像搭乐高一样组合上述模块，支持训练模式（generate/rollout）和纯推理模式。
*   **环境层 (Environment Layer)**：负责数据加载和奖励管理。提供 `MemoryBankEnv`（长期记忆）和 `LongcontextEnv`（短期上下文）两种环境，并支持格式奖励和 LLM-as-a-Judge 等多维评价。
*   **训练层 (Trainer Layer)**：核心优化引擎。
    *   **关键算法**：原生集成 **GRPO (Group Relative Policy Optimization)**。该算法通过组内奖励归一化估计基准，无需额外的 Critic 网络，显著降低了显存占用，适合长上下文场景。

### 3. 实验设计
*   **实验对象**：以开源的 **MemAgent** 架构为基础进行验证。
*   **基座模型**：Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集/场景**：
    *   **训练集**：基于 MemAgent 公开数据，为提高效率将上下文长度简化至原有的 1/3（每样本 50-80 个文档）。
    *   **评估集 (Benchmark)**：包含两个主任务测试集（`eval_50`, `eval_100`）和一个分布外（OOD）测试集（`eval_fwe_16384`）。
*   **对比方法**：将经过 MemFactory RL 微调后的模型与原始基座模型（Base Checkpoint）进行对比。

### 4. 资源与算力
*   **硬件设备**：整个训练和评估流水线可以在**单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步骤（steps）。
*   **算力评价**：论文强调了该框架的低硬件门槛，证明了在有限计算资源下进行复杂记忆策略优化的可行性。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的模型（1.7B 和 4B）在三个测试集上进行了实验。
*   **客观性保障**：所有评分均基于 **4 次独立试验的平均值 (avg@4)**，以减少生成随机性带来的偏差。
*   **充分性评价**：实验涵盖了域内和分布外（OOD）场景，验证了策略的泛化能力。虽然主要针对 MemAgent 架构进行了深入验证，但框架本身预置了对 Memory-R1 和 RMM 的支持，展示了较好的覆盖面。

### 6. 论文的主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均得分相对提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **泛化能力**：较大规模的模型（4B）在 OOD 测试集上表现出更稳健的提升，说明通过 RL 学习到的循环记忆策略具有良好的迁移性。
*   **框架有效性**：验证了通过统一框架和 GRPO 算法，即使使用简化数据和单卡资源，也能有效优化复杂的记忆策略。

### 7. 优点
*   **高度模块化**：解耦了记忆的提取、更新和检索，极大地增强了框架的灵活性和可扩展性。
*   **资源友好**：通过集成 GRPO 算法，解决了记忆增强任务中长上下文与显存占用之间的矛盾。
*   **开箱即用**：预置了多种 SOTA 记忆算法（Memory-R1, RMM 等）的实现，具有很强的实用价值。
*   **标准化**：填补了智能体记忆研究领域缺乏统一基础设施的空白。

### 8. 不足与局限
*   **范式覆盖有限**：虽然支持几种代表性算法，但尚未涵盖所有新兴的记忆管理范式。
*   **训练效率**：论文提到在训练效率方面仍有提升空间。
*   **实验深度**：实验主要集中在 MemAgent 这一种架构的微调上，对于其他预置架构（如 Memory-R1）的跨架构对比实验较少。
*   **数据依赖**：性能提升仍受限于训练数据的质量和多样性。

（完）
