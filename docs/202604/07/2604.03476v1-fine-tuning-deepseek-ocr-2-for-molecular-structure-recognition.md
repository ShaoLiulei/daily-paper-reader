---
title: Fine-tuning DeepSeek-OCR-2 for Molecular Structure Recognition
title_zh: 微调 DeepSeek-OCR-2 用于分子结构识别
authors: "Haocheng Tang, Xingyu Dang, Junmei Wang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03476v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 视觉语言模型的监督微调
tldr: 本研究针对化学结构识别（OCSR）任务，基于DeepSeek-OCR-2开发了MolSeek-OCR模型。为解决视觉语言模型在分子识别中微调不稳定的问题，提出了一种从LoRA到选择性全参数微调的两阶段渐进式策略，并结合合成与真实专利数据进行训练。该模型在SMILES生成任务上表现出色，达到了顶尖序列生成模型的水平，为利用通用多模态大模型解决专业领域OCR问题提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03476v1/fig-001.webp\", \"caption\": \"Figure 1. The overall architecture of DeepSeek-OCR-2.\", \"page\": 3, \"index\": 1, \"width\": 1002, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2604.03476v1/fig-002.webp\", \"caption\": \"Table 2. Exact matching accuracy (%) on different datasets.\", \"page\": 4, \"index\": 2, \"width\": 1008, \"height\": 397}]"
motivation: 旨在解决视觉语言模型在直接应用于化学结构识别时存在的训练不稳定及全参数微调难以收敛的挑战。
method: 提出一种两阶段渐进式微调策略，通过LoRA初始化并衔接带分层学习率的选择性全参数微调，将分子识别建模为图像条件下的SMILES序列生成。
result: MolSeek-OCR在精确匹配率上达到了与最先进图像到序列模型相当的水平，但仍略逊于专门的图像到图结构模型。
conclusion: 研究证明了通用OCR大模型经适配后可胜任分子识别，但强化学习等后训练手段对提升SMILES序列的精确匹配效果有限。
---

## 摘要
光学化学结构识别（OCSR）对于将印刷文献中的二维分子图转换为机器可读格式至关重要。虽然视觉语言模型在端到端 OCR 任务中展现出潜力，但将其直接应用于 OCSR 仍具挑战性，且直接进行全参数监督微调往往会失败。在这项工作中，我们将 DeepSeek-OCR-2 适配于分子光学识别，将该任务表述为以图像为条件的 SMILES 生成。为了克服训练不稳定性，我们提出了一种两阶段渐进式监督微调策略：从参数高效的 LoRA 开始，逐步过渡到带有分层学习率的选择性全参数微调。我们在结合了来自 PubChem 的合成渲染图和来自 USPTO-MOL 的真实专利图像的大规模语料库上训练模型，以提高覆盖范围和鲁棒性。我们微调后的模型 MolSeek-OCR 展示了具有竞争力的能力，实现了与性能最佳的图像到序列模型相当的精确匹配准确率。然而，它仍逊于最先进的图像到图模型。此外，我们探索了强化学习风格的后训练和基于数据策划的精炼，发现它们未能提高精确 SMILES 匹配所需的严格序列级保真度。

## Abstract
Optical Chemical Structure Recognition (OCSR) is critical for converting 2D molecular diagrams from printed literature into machine-readable formats. While Vision-Language Models have shown promise in end-to-end OCR tasks, their direct application to OCSR remains challenging, and direct full-parameter supervised fine-tuning often fails. In this work, we adapt DeepSeek-OCR-2 for molecular optical recognition by formulating the task as image-conditioned SMILES generation. To overcome training instabilities, we propose a two-stage progressive supervised fine-tuning strategy: starting with parameter-efficient LoRA and transitioning to selective full-parameter fine-tuning with split learning rates. We train our model on a large-scale corpus combining synthetic renderings from PubChem and realistic patent images from USPTO-MOL to improve coverage and robustness. Our fine-tuned model, MolSeek-OCR, demonstrates competitive capabilities, achieving exact matching accuracies comparable to the best-performing image-to-sequence model. However, it remains inferior to state-of-the-art image-to-graph modelS. Furthermore, we explore reinforcement-style post-training and data-curation-based refinement, finding that they fail to improve the strict sequence-level fidelity required for exact SMILES matching.

---

## 论文详细总结（自动生成）

以下是对论文《Fine-tuning DeepSeek-OCR-2 for Molecular Structure Recognition》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何有效地将通用的视觉语言模型（VLM）适配到高度专业化的光学化学结构识别（OCSR）任务中，即将分子图像准确转化为机器可读的 SMILES 字符串。
*   **研究背景**：传统的 OCSR 方法（如基于规则或专门的编码器-解码器架构）在处理复杂或低质量图像时存在局限。虽然 VLM 在通用 OCR 领域表现优异，但直接对分子结构进行全参数微调（SFT）极易导致训练不稳定或失败。

### 2. 方法论：两阶段渐进式微调策略
论文将 OCSR 建模为“图像条件下的 SMILES 生成”任务，并针对 DeepSeek-OCR-2 提出了以下核心技术：
*   **第一阶段：LoRA SFT**
    *   使用参数高效的 LoRA 技术对模型进行初步适配。
    *   不仅微调解码器，还将 LoRA 模块应用于视觉-语言投影层（Alignment Interface）和注意力层，以建立视觉特征与化学序列之间的初步映射。
*   **第二阶段：渐进式全参数微调（Progressive Full SFT）**
    *   在 LoRA 权重的基础上，冻结底层的视觉分词器（Visual Tokenizer）和输入嵌入层。
    *   **分层学习率（Split Learning Rates）**：对视觉分支使用较小的学习率（$5 \times 10^{-6}$），对语言生成分支使用较大的学习率（$2 \times 10^{-5}$），以平衡感知稳定性和生成灵活性。
*   **多尺度视觉处理**：保留了 DeepSeek-OCR-2 的原始管线，通过全局视图和局部切片（Local Crops）捕捉分子的整体拓扑和局部化学键细节。

### 3. 实验设计与 Benchmark
*   **数据集**：
    *   **训练集**：结合了来自 PubChem 的合成数据（模拟 MolScribe 和 ChemDraw 风格）以及来自 USPTO-MOL 的真实专利图像，规模从 192k（LoRA 阶段）扩展到 800k（全参数阶段）。
    *   **测试集**：涵盖合成数据集（Indigo, ChemDraw）、真实专利/期刊数据集（CLEF, UOB, USPTO, Staker, ACS）以及相应的扰动（Perturbed）数据集。
*   **对比方法**：
    *   **规则/传统方法**：MolVec, OSRA。
    *   **图像到序列（Img2Seq）**：Img2Mol, DECIMER, SwinOCSR。
    *   **图像到图（Img2Graph）**：MolScribe, ChemGrapher 等。
*   **评价指标**：SMILES 序列的**精确匹配准确率（Exact Matching Accuracy）**。

### 4. 资源与算力
*   **算力细节**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：
    *   LoRA 阶段：Batch Size 为 4（每设备），梯度累积为 8，训练 3000 步。
    *   全参数阶段：Batch Size 为 4（每设备），梯度累积为 8，训练 2500 步。
    *   使用了 AdamW 优化器。

### 5. 实验数量与充分性
*   **实验规模**：在 11 个不同的基准测试集（包含合成、真实和扰动场景）上进行了评估。
*   **消融与探索**：对比了 LoRA 与全参数微调的效果，并额外探索了强化学习风格的后训练（GSPO）和数据精炼（ReFT）。
*   **充分性评价**：实验设计较为客观，覆盖了 OCSR 领域的主流 Benchmark。通过对比不同架构（序列 vs 图），清晰地界定了 VLM 在该领域的当前位置。

### 6. 主要结论与发现
*   **MolSeek-OCR 表现优异**：通过两阶段微调，模型在精确匹配率上达到了与 DECIMER（最强 Img2Seq 模型）相当的水平。
*   **VLM 仍有差距**：尽管表现不俗，但仍逊于以 MolScribe 为代表的“图像到图”模型，说明直接生成序列在处理复杂拓扑时不如显式预测原子和键。
*   **强化学习效果有限**：GSPO 和 ReFT 虽然能提升图层面的相似度，但往往会破坏 SMILES 序列的严格一致性，导致精确匹配率下降。

### 7. 优点
*   **策略有效性**：提出的“LoRA 到分层全参数微调”路径解决了通用大模型在专业 OCR 任务中难以收敛的问题。
*   **鲁棒性强**：通过混合合成与真实数据，模型在面对模糊、扫描质量差的专利图像时表现出较好的泛化能力。
*   **开源贡献**：提供了完整的训练数据集、代码和参数，具有很高的复现价值。

### 8. 不足与局限
*   **序列建模局限**：SMILES 字符串具有非唯一性（尽管有规范化），且微小的序列错误会导致整个分子识别失败，这在本质上对自回归模型具有挑战性。
*   **性能瓶颈**：相比于专门针对化学结构设计的图生成模型，通用 VLM 缺乏对化学几何约束的显式建模。
*   **算力信息缺失**：未披露训练所需的具体资源消耗，不便于其他研究者评估微调成本。

（完）
