---
title: "CircuitProbe: Predicting Reasoning Circuits in Transformers via Stability Zone Detection"
title_zh: CircuitProbe：通过稳定性区域检测预测 Transformer 中的推理电路
authors: Rajkiran Panuganti
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00716v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 通过激活统计预测Transformer架构中的推理电路
tldr: Transformer模型中存在能提升推理能力的局部“推理电路”，但传统暴力搜索方法耗时极长。本文提出CircuitProbe，通过分析激活统计量在5分钟内即可预测电路位置，实现数千倍提速。研究发现电路分为早期稳定性电路和后期量级电路，该方法在多种架构和语言上表现稳健，证实了层复制技术是提升3B以下小模型推理性能的有效手段。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00716v1/fig-001.webp\", \"caption\": \"Figure 1: Stability zone detection across four architectures. Green bars show the detected stability zone (lowest derivative of representation change). In Phi-4 (top left), this aligns with the known ground-truth reasoning circuit at layers 6–11 (blue shading). The same pattern appears in all four models despite their different architectures, sizes, and training procedures.\", \"page\": 6, \"index\": 1, \"width\": 881, \"height\": 627}, {\"url\": \"assets/figures/arxiv/2604.00716v1/fig-002.webp\", \"caption\": \"Table 1: CircuitProbe predictions across 9 models. All show the same two-circuit pattern. “L” denotes total layer count. DS-R1 abbreviates DeepSeek-R1-Distill.\", \"page\": 7, \"index\": 2, \"width\": 560, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.00716v1/fig-003.webp\", \"caption\": \"Table 2: Scaling experiment across model sizes. Layer duplication consistently benefits models under 3B but degrades 7B+ models on broad benchmarks.\", \"page\": 8, \"index\": 3, \"width\": 664, \"height\": 215}]"
motivation: 传统的暴力搜索推理电路方法计算成本极高，单个模型需耗费25个GPU小时，严重限制了该技术的实用性。
method: 提出CircuitProbe算法，通过表示变化的导数检测早期稳定性电路，并利用异常评分检测后期量级电路。
result: 在9种模型架构上实现了3到4个数量级的加速，预测位置与最优电路高度吻合，且仅需10个样本即可在多语言环境下稳定运行。
conclusion: 推理电路预测为优化小参数模型提供了高效路径，证明了层复制在3B以下模型中具有显著的性能提升潜力。
---

## 摘要
Transformer 语言模型包含局部推理电路，即在推理阶段进行复制时能够增强推理能力的连续层块。目前，寻找这些电路需要进行暴力扫描，每个模型需耗费 25 个 GPU 小时。我们提出了 CircuitProbe，它能根据激活统计数据在 5 分钟内（CPU 环境）预测电路位置，实现了三到四个数量级的加速。我们发现推理电路分为两类：早期层中的稳定性电路（通过表示变化的导数检测）和后期层中的量级电路（通过异常评分检测）。我们在涵盖 6 种架构的 9 个模型（包括 2025 年发布的模型）上进行了验证，结果证实 CircuitProbe 的首选预测在所有验证案例中均与最优电路匹配，或误差在 2 层以内。针对 Qwen 2.5 系列的缩放实验表明，层复制能持续提升参数量在 3B 以下模型的性能，但会降低 7B 以上模型的性能，这使其成为小型语言模型的一种实用缩放技术。CircuitProbe 仅需 10 个校准样本，且其预测在英语、印地语、中文和法语中均表现稳定。

## Abstract
Transformer language models contain localized reasoning circuits, contiguous layer blocks that improve reasoning when duplicated at inference time. Finding these circuits currently requires brute-force sweeps costing 25 GPU hours per model. We propose CircuitProbe, which predicts circuit locations from activation statistics in under 5 minutes on CPU, providing a speedup of three to four orders of magnitude. We find that reasoning circuits come in two types: stability circuits in early layers, detected through the derivative of representation change, and magnitude circuits in late layers, detected through anomaly scoring. We validate across 9 models spanning 6 architectures, including 2025 models, confirming that CircuitProbe top predictions match or are within 2 layers of the optimal circuit in all validated cases. A scaling experiment across the Qwen 2.5 family reveals that layer duplication consistently benefits models under 3B parameters but degrades performance in 7B+ models, making this a practical scaling technique for small language models. CircuitProbe requires as few as 10 calibration examples and its predictions are stable across English, Hindi, Chinese, and French.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CircuitProbe** 的方法，旨在高效定位 Transformer 模型中的“推理电路”（Reasoning Circuits），并通过层复制（Layer Duplication）技术在推理阶段提升模型性能。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：最近的研究发现，Transformer 模型内部存在特定的“推理电路”（连续的层块）。在推理时重复执行这些层（层复制）可以显著提升模型的推理能力（5-16%）。
*   **核心痛点**：目前寻找这些电路的方法依赖于“暴力搜索”（Brute-force sweep），即逐个尝试不同的层组合并运行基准测试。这种方法极其耗时（每个模型需 7-25 GPU 小时），且不同架构的电路位置各异，缺乏通用规律。
*   **整体含义**：本文旨在开发一种无需运行下游任务、仅通过分析模型内部激活统计特性即可快速预测推理电路位置的方法，将搜索时间从数小时缩短至数分钟。

### 2. 论文提出的方法论
CircuitProbe 的核心思想是：**推理关键层具有独特的统计特征，可以通过激活值的稳定性或异常值来识别。**

*   **核心发现：两类推理电路**
    1.  **稳定性电路（Stability Circuits）**：位于模型早期（深度 10-25%）。这是模型表示从混沌转为稳态的“结晶点”。
    2.  **量级电路（Magnitude Circuits）**：位于模型后期（深度 85-100%）。表现为极大的表示变化和高跨样本方差。
*   **关键技术细节**：
    *   **激活统计收集**：在少量校准数据（10-50 个样本）上运行前向传播，收集残差流的五个指标：表示变化幅度、自相似性、范数增长率、跨样本方差和表示秩（Representation Rank）。
    *   **评分函数**：
        *   **稳定性评分 ($S_{stability}$)**：利用表示变化幅度的**导数**。导数越低（变化越趋于平稳），得分越高。
        *   **异常评分 ($S_{anomaly}$)**：基于 Z-score 检测激活量级异常大的层。
    *   **综合排序**：最终得分取两类评分归一化后的最大值 $C(s, e) = \max(\tilde{S}_{stability}, \tilde{S}_{anomaly})$。

### 3. 实验设计
*   **实验对象**：涵盖 6 种架构的 9 个模型，包括 Qwen2.5 系列（0.5B 到 7B）、Qwen3-8B（2025年模型）、DeepSeek-R1-Distill 系列、Mistral-7B、Phi-4 (14B) 等。
*   **基准测试（Benchmark）**：
    *   使用 250 个问题的标准化测试集：包含 GSM8K（数学）、BBH（因果判断、逻辑推理、日期理解）。
    *   使用 GGUF 格式进行层手术（Layer Surgery），通过 `llama.cpp` 验证预测电路的实际效果。
*   **对比方法**：对比了暴力搜索结果（Ground Truth）、单纯的异常检测、对比分析（Contrastive Analysis）以及 ShortGPT 使用的 Block Influence 指标。

### 4. 资源与算力
*   **CircuitProbe 耗时**：在 **CPU** 上运行，0.5B 模型约 15 秒，14B 模型约 5 分钟。
*   **暴力搜索耗时**：每个模型需要 **7-25 GPU 小时**（取决于模型大小和搜索空间）。
*   **算力节省**：CircuitProbe 实现了 **3 到 4 个数量级** 的加速，且无需 GPU 即可完成预测。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 9 个不同年份、不同规模的模型上验证了电路模式的普遍性。
    *   针对 Qwen2.5 全系列进行了缩放实验（Scaling Experiment）。
    *   进行了多语言稳健性测试（英语、中文、法语、印地语）。
    *   进行了校准样本量（10-50个）和样本类型的消融实验。
*   **充分性评价**：实验设计较为全面，不仅验证了预测的准确性，还通过实际的层复制实验验证了性能提升。跨架构和跨语言的测试增强了结论的说服力。

### 6. 论文的主要结论与发现
1.  **高效性**：CircuitProbe 能在 5 分钟内准确预测推理电路，预测结果与最优位置误差通常在 2 层以内。
2.  **电路分类**：首次明确了早期“稳定性电路”和后期“量级电路”的双重存在。
3.  **缩放效应**：层复制技术对 **3B 参数以下** 的小模型效果显著（提升 2-10%），但对 7B 以上模型可能导致通用能力下降。
4.  **结构决定论**：推理电路的位置在预训练阶段就已确定，强化学习（如 DeepSeek-R1）或不同语言的输入不会改变电路的物理位置。

### 7. 优点
*   **极高的实用价值**：为小模型开发者提供了一种“免费”提升推理能力的手段，无需重训。
*   **深刻的洞察**：提出了“表示结晶”（Crystallization）的概念，解释了为什么早期层对推理至关重要。
*   **稳健性强**：方法对校准数据不敏感，仅需 10 个任意文本样本即可工作，且具有多语言通用性。

### 8. 不足与局限
*   **预测精度限制**：虽然接近最优，但并非百分之百精确（如在 TinyLlama 上最优解排在第 4 位）。
*   **大模型负效果**：在 7B 及以上模型中，层复制虽然提升了特定推理任务，但可能损害指令遵循和格式化能力。
*   **模型覆盖范围**：尚未在 20B-32B 及更大规模的模型上验证，不确定性能下降的趋势是否会逆转。
*   **任务局限**：主要关注逻辑和数学推理，对创意写作或长文本总结等任务的影响尚未深入探讨。

（完）
