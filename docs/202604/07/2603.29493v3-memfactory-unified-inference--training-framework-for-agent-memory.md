---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v3"
tags: ["query:ai-llm"]
score: 9.0
evidence: 记忆增强大语言模型智能体的统一训练框架
tldr: "针对记忆增强型大模型智能体实现碎片化的问题，本文推出MemFactory，这是首个统一且高度模块化的智能体记忆训练与推理框架。该框架借鉴LLaMA-Factory的成功经验，将记忆生命周期抽象为“乐高式”组件，并原生集成GRPO算法以优化记忆管理策略。MemFactory支持Memory-R1等前沿范式，在MemAgent任务中实现高达14.8%的性能提升，显著降低了开发高性能记忆智能体的门槛。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v3/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v3/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强智能体研究缺乏统一的基础设施，导致复杂记忆流水线的集成、训练和评估过程高度碎片化。
method: 采用模块化架构将记忆操作抽象为可插拔组件，并利用组相对策略优化（GRPO）根据环境奖励微调记忆管理策略。
result: "在公开数据集上的实验表明，MemFactory能有效提升模型性能，在MemAgent架构上最高实现14.8%的相对增益。"
conclusion: MemFactory为记忆驱动的AI智能体提供了标准化且易用的基础设施，将有力推动长短期记忆智能体技术的创新与普及。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备强大能力的长期 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们推出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为近期前沿范式（包括 Memory-R1、RMM 和 MemAgent）提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各个评估集中，MemFactory 的性能平均优于相应的基座模型，相对提升最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的记忆增强型大语言模型（LLM）智能体研究存在严重的**碎片化**问题。虽然利用强化学习（RL）优化记忆操作（如提取、更新、检索）已成为趋势，但不同研究（如 Memory-R1, MemAgent, RMM）的实现高度耦合特定任务，缺乏统一的基础设施。
*   **研究背景**：传统的静态 RAG 或启发式记忆规则在处理长程对话时容易产生冗余和冲突。通过 RL 将记忆管理视为序列决策过程是解决之道，但高昂的工程门槛阻碍了该领域的快速创新。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：借鉴 LLaMA-Factory 的成功经验，MemFactory 提出了一个**高度模块化、乐高式**的架构，将复杂的记忆生命周期抽象为原子化操作，并统一了训练、推理和评估流程。
*   **四层架构设计**：
    *   **模块层 (Module Layer)**：定义了提取器 (Extractor)、更新器 (Updater)、检索器 (Retriever) 和智能体模块 (Agent Module) 四类原子组件。
    *   **智能体层 (Agent Layer)**：负责将上述模块组装成具体的策略执行器，支持“即插即用”的模块替换。
    *   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器，提供格式奖励、LLM-as-a-Judge 等多维反馈。
    *   **训练层 (Trainer Layer)**：核心优化引擎。
*   **关键算法 (GRPO)**：原生集成**群体相对策略优化 (Group Relative Policy Optimization)**。该算法通过组内奖励归一化估计基准，无需额外的 Critic 网络，极大地节省了显存，适合长上下文的记忆训练任务。

### 3. 实验设计
*   **数据集与场景**：使用了 MemAgent 发布的公开数据集，包含训练集和三个测试集：`eval_50`、`eval_100`（主任务）以及 `eval_fwe_16384`（分布外 OOD 任务）。
*   **基准模型 (Benchmark)**：对比了 Qwen3-1.7B 和 Qwen3-4B-Instruct 的原始 Checkpoint。
*   **对比范式**：框架内置并支持了 Memory-R1、MemAgent 和 RMM 三种前沿记忆范式的复现。

### 4. 资源与算力
*   **硬件配置**：实验明确指出整个训练和评估流水线可以在**单台 NVIDIA A800 80GB GPU** 上执行。
*   **训练细节**：每个模型训练了 250 个有效步骤（valid steps）。这种轻量化的配置证明了该框架的高可复现性和对中小型研究团队的友好性。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型（1.7B 和 4B）在三个不同性质的测试集上进行了实验。
*   **客观性保障**：所有评分均基于 **4 次独立试验的平均值 (avg@4)**，以减少 LLM 生成随机性带来的偏差。
*   **充分性评价**：实验涵盖了主任务和 OOD 任务，验证了模型的泛化能力。虽然消融实验较少，但作为框架类论文，其重点在于验证“通过该框架进行 RL 训练确实能提升性能”，这一点已得到充分证明。

### 6. 主要结论与发现
*   **性能提升**：使用 MemFactory 进行 RL 微调后，模型性能普遍提升。Qwen3-1.7B 在平均分上实现了 **14.8%** 的相对增益；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **泛化能力**：较大规模的模型（4B）在 OOD 任务上表现出更强的鲁棒性，能够有效迁移学习到的循环记忆策略。
*   **框架有效性**：证明了统一框架可以简化 Memory-RL 的开发流程，且在有限算力下也能取得显著效果。

### 7. 优点与亮点
*   **高度模块化**：通过标准化的 `generate`、`rollout` 和 `inference` 接口，实现了记忆策略的自由组合。
*   **显存友好**：集成 GRPO 算法，解决了记忆增强智能体在长上下文训练时显存容易溢出的痛点。
*   **开箱即用**：预置了多种 SOTA 算法实现，极大降低了研究人员复现和改进前人工作的难度。

### 8. 不足与局限
*   **范式覆盖有限**：目前仅涵盖了三种代表性范式，对于更复杂的分布式记忆或多模态记忆支持尚待观察。
*   **训练效率**：论文提到在训练效率方面仍有提升空间。
*   **任务偏差**：实验主要集中在 MemAgent 任务上，对于其他类型的记忆任务（如极长文本的 Key-Value 检索）验证相对较少。

（完）
