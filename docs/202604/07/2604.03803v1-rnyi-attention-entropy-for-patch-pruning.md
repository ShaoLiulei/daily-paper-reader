---
title: Rényi Attention Entropy for Patch Pruning
title_zh: 基于 Rényi 注意力熵的 Patch 剪枝
authors: "Hiroaki Aizawa, Yuki Igaue"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03803v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: Transformer中用于补丁剪枝的注意力熵
tldr: 针对Transformer模型自注意力机制计算开销随Token数量平方增长的问题，本文提出了一种基于注意力熵的补丁剪枝方法。研究者利用Shannon熵衡量注意力分布，保留注意力集中、熵值低的补丁，剔除冗余的高熵补丁。进一步引入Rényi熵以增强对注意力峰值的捕捉，并支持灵活的剪枝策略。在细粒度图像识别实验中，该方法在大幅降低计算量的同时保持了高精度，优化了性能与效率的权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03803v1/fig-001.webp\", \"caption\": \"Fig. 3. Visualization of Rényi attention entropy (α = 2.0) for each Transformer block in DeiT-S. This visualization shows that attention entropy depends on Transformer layer depth, and lower entropy corresponds to foreground regions.\", \"page\": 6, \"index\": 1, \"width\": 645, \"height\": 466}, {\"url\": \"assets/figures/arxiv/2604.03803v1/fig-002.webp\", \"caption\": \"Fig. 5. Visualization of Shannon and Rényi attention entropies. For each DeiT-S block, the figure shows histograms of Shannon attention entropy (α = 1.0) and Rényi attention entropy at different α orders. Blue indicates informative patches that are kept, and red indicates redundant patches that are pruned. The results show that the Rényi order controls peak emphasis and allows the characterization of the attention distribution to be adapted to the task.\", \"page\": 11, \"index\": 2, \"width\": 725, \"height\": 203}, {\"url\": \"assets/figures/arxiv/2604.03803v1/fig-003.webp\", \"caption\": \"Fig. 4. Visualizations of patch pruning results for EViT and Rényi attention entropybased approach on ImageNet-100, FGVC Aircraft, and Oxford Flowers102. From left to right: input image, pruning results at Blocks 4, 7, and 10. The keep rate is r = 0.7, and for our method we show the results with the tuned α.\", \"page\": 10, \"index\": 3, \"width\": 716, \"height\": 700}, {\"url\": \"assets/figures/arxiv/2604.03803v1/fig-004.webp\", \"caption\": \"Table 1. Top-1 accuracy(%) with DeiT-S on ImageNet-100 (IN-100), FGVC Aircraft (Aircraft), and Oxford Flowers102 (Flowers) under different keep rates r. The DeiTS column reports scores without patch pruning. The bold scores indicate the best performance at each keep rate.\", \"page\": 8, \"index\": 4, \"width\": 747, \"height\": 443}, {\"url\": \"assets/figures/arxiv/2604.03803v1/fig-005.webp\", \"caption\": \"Fig. 2. Overall pipeline of the Rényi attention entropy pruning. The pruning procedure is described in Section 3.3.\", \"page\": 4, \"index\": 5, \"width\": 498, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2604.03803v1/fig-006.webp\", \"caption\": \"Table 2. Top-1 accuracy(%) with DeiT-B on ImageNet-100 (IN-100), FGVC Aircraft (Aircraft), and Oxford Flowers102 (Flowers) under different keep rates r. The DeiTB column reports scores without patch pruning. The bold scores indicate the best performance at each keep rate.\", \"page\": 9, \"index\": 6, \"width\": 750, \"height\": 443}]"
motivation: Transformer的自注意力机制计算成本随Token数量呈平方级增长，亟需通过补丁剪枝来减少冗余计算并提升效率。
method: 提出基于Shannon和Rényi注意力熵的准则，通过识别并保留注意力分布集中、信息量大的低熵补丁来实现高效剪枝。
result: 在细粒度图像识别任务中，该方法在显著减少计算量的同时维持了模型准确率，并提供了更优的性能与功耗权衡。
conclusion: Rényi注意力熵为补丁剪枝提供了一种灵活且高效的衡量标准，能够根据任务需求优化Transformer模型的推理效率。
---

## 摘要
Transformer 在视觉和语言领域都是强大的基准模型，因为自注意力机制能够捕捉 token 之间的长程依赖关系。然而，自注意力的计算成本随 token 数量呈平方级增长。Patch 剪枝通过估计每个 patch 的重要性并移除冗余 patch 来降低这一成本。为了识别用于剪枝的信息丰富的 patch，我们引入了一种基于注意力分布 Shannon 熵的标准。低熵 patch 接收到的是选择性且集中的注意力，因此被视为重要的并予以保留；而高熵 patch 的注意力分散在多个位置，被视为冗余。我们还将该标准从 Shannon 熵扩展到 Rényi 熵，后者强调尖锐的注意力峰值，并支持能够适应任务需求和计算限制的剪枝策略。在 patch 选择至关重要的细粒度图像识别实验中，我们的方法在保持准确性的同时减少了计算量。此外，通过 Rényi 熵度量调整剪枝策略可以获得进一步的收益，并改善准确性与计算量之间的权衡。

## Abstract
Transformers are strong baselines in both vision and language because self-attention captures long-range dependencies across tokens. However, the cost of self-attention grows quadratically with the number of tokens. Patch pruning mitigates this cost by estimating per-patch importance and removing redundant patches. To identify informative patches for pruning, we introduce a criterion based on the Shannon entropy of the attention distribution. Low-entropy patches, which receive selective and concentrated attention, are kept as important, while high-entropy patches with attention spread across many locations are treated as redundant. We also extend the criterion from Shannon to Rényi entropy, which emphasizes sharp attention peaks and supports pruning strategies that adapt to task needs and computational limits. In experiments on fine-grained image recognition, where patch selection is critical, our method reduced computation while preserving accuracy. Moreover, adjusting the pruning policy through the Rényi entropy measure yields further gains and improves the trade-off between accuracy and computation.

---

## 论文详细总结（自动生成）

### 论文总结：Rényi Attention Entropy for Patch Pruning

#### 1. 核心问题与整体含义（研究动机和背景）
Vision Transformer (ViT) 在计算机视觉领域表现卓越，但其核心机制——自注意力（Self-Attention）的计算复杂度随输入 Patch（补丁）数量的增加呈平方级增长。这限制了其在高分辨率图像或实时任务中的应用。
**研究动机**：现有的 Patch 剪枝方法（如 EViT）主要依赖注意力权重的“绝对数值大小”来衡量重要性。然而，这种方法无法有效区分模型的不确定性。例如，一个 Patch 可能获得较高的总权重，但如果这些权重均匀分散在大量背景 Patch 上，其信息价值其实很低。本文旨在引入**信息论中的“熵”**来量化注意力分布的集中程度，从而更精准地识别并保留关键信息区域（通常为前景），剔除冗余区域（通常为背景）。

#### 2. 论文提出的方法论
核心思想是利用**注意力熵（Attention Entropy）**作为 Patch 重要性的评分准则。
*   **核心逻辑**：
    1.  **低熵 = 重要**：当某个 Patch 的注意力分布非常集中（即只关注少数几个关键 Patch）时，其熵值较低，表明模型对其特征提取具有高度确定性，应予以保留。
    2.  **高熵 = 冗余**：当注意力分布弥散（均匀分布在大量 Patch 上）时，熵值较高，表明该 Patch 缺乏显著特征或属于背景，可被剪枝。
*   **关键技术细节**：
    *   **Shannon 熵**：基础度量方式，计算注意力分布的平均不确定性。
    *   **Rényi 熵**：Shannon 熵的广义形式，引入了阶数参数 $\alpha$。通过调节 $\alpha$，可以控制对注意力分布中“峰值”的敏感度。当 $\alpha > 1$ 时，更强调高概率事件（即更关注最强的注意力连接），从而允许根据任务需求（如细粒度识别）灵活调整剪枝策略。
*   **算法流程**：在预训练 ViT 的微调阶段，在特定层（如第 4、7、10 层）计算每个 Patch 的注意力熵，按熵值升序排列，保留前 $r$ 比例的低熵 Patch，直接丢弃高熵 Patch。

#### 3. 实验设计
*   **数据集**：
    *   **通用分类**：ImageNet-100。
    *   **细粒度分类**：FGVC Aircraft（飞机）、Oxford Flowers102（花卉）。
*   **基准模型（Backbone）**：DeiT-S 和 DeiT-B。
*   **对比方法**：主要对比了经典的 Patch 剪枝方法 **EViT**（基于注意力权重大小）。
*   **评估指标**：Top-1 准确率、FLOPs（计算量）、Throughput（推理吞吐量）。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX A6000 GPU**。
*   **训练配置**：所有模型微调 100 个 epoch，Batch size 为 256。
*   **优化器**：AdamW，权重衰减 0.05。
*   **输入分辨率**：224 × 224。

#### 5. 实验数量与充分性
实验设计较为充分且具有客观性：
*   **多维度对比**：涵盖了从轻量级（DeiT-S）到中量级（DeiT-B）的模型。
*   **参数敏感性分析**：对保留率 $r$（从 0.3 到 0.9）和 Rényi 熵阶数 $\alpha$（1.0, 2.0, 5.0, 10.0）进行了详尽的网格搜索。
*   **消融与可视化**：提供了剪枝过程的可视化图，直观展示了低熵准则如何锁定前景物体；同时对比了注意力熵与注意力距离（Receptive Field）的相关性。
*   **公平性**：在相同的训练协议和硬件环境下对比了 EViT，确保了结果的可比性。

#### 6. 主要结论与发现
*   **性能提升**：在 ImageNet-100 上，该方法在减少 30% Token 的情况下，准确率仅下降 0.02%，且在相同计算量下优于 EViT。
*   **细粒度任务优势**：在飞机和花卉识别任务中，Rényi 熵表现尤为突出。通过调大 $\alpha$ 值，模型能更好地捕捉局部关键零件特征，甚至在大幅剪枝时准确率超过了无剪枝的基准模型。
*   **效率权衡**：在 DeiT-S 上，该方法实现了约 55% 的推理速度提升和 35% 的计算量削减。
*   **分布特性**：发现注意力熵随网络深度增加而变化，后期层通常呈现更极端的分布，这为未来开发动态剪枝率提供了依据。

#### 7. 优点
*   **理论优雅**：将信息论中的熵引入视觉任务，比单纯的权重求和更具解释性。
*   **灵活性高**：Rényi 熵的 $\alpha$ 参数提供了一个可调的“杠杆”，使模型能适应从通用物体到细粒度零件的不同识别需求。
*   **即插即用**：不需要引入额外的可学习子网络（如预测头），计算开销极低，易于集成到现有 ViT 架构中。

#### 8. 不足与局限
*   **吞吐量略低**：虽然 FLOPs 降低了，但由于计算对数（log）和幂运算（Rényi 熵所需）比简单的加权求和稍慢，其实际推理吞吐量（images/s）略逊于 EViT。
*   **超参数依赖**：最佳的 $\alpha$ 值在不同数据集和不同保留率 $r$ 下并不固定，需要一定的调优成本。
*   **任务覆盖**：目前主要集中在图像分类任务，尚未在目标检测或语义分割等更复杂的下游任务中验证其有效性。

（完）
