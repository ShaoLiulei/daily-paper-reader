---
title: Universal YOCO for Efficient Depth Scaling
title_zh: Universal YOCO：实现高效深度扩展
authors: "Yutao Sun, Li Dong, Tianzhu Ye, Shaohan Huang, Jianyong Wang, Furu Wei"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01220v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型的高效深度扩展与解码器-解码器架构
tldr: 针对大语言模型在推理扩展时面临的计算开销大和KV缓存随深度膨胀的问题，本文提出Universal YOCO (YOCO-U)。该架构将YOCO的解码器-解码器结构与递归计算相结合，通过在浅层高效注意力层进行参数共享的多次迭代，实现了恒定的全局KV缓存和线性预填充。YOCO-U在提升模型表达深度的同时保持了极高的推理效率，在通用及长文本基准测试中表现优异，为可扩展LLM提供了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01220v1/fig-001.webp\", \"caption\": \"Figure 3: Accuracy comparison on 11 math benchmarks. YOCO-U consistently outperforms the YOCO baseline across all tasks, achieving a significant boost in average accuracy.\", \"page\": 6, \"index\": 1, \"width\": 825, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2604.01220v1/fig-002.webp\", \"caption\": \"Figure 8: Angular distances between consecutive layers.\", \"page\": 11, \"index\": 2, \"width\": 610, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.01220v1/fig-003.webp\", \"caption\": \"Table 6: Hyperparamters used for the model in Section 4.1.\", \"page\": 15, \"index\": 3, \"width\": 344, \"height\": 436}, {\"url\": \"assets/figures/arxiv/2604.01220v1/fig-004.webp\", \"caption\": \"Figure 4: Long sequence perplexity decreases along with the increasing input length on Book (top) and Code (bottom) data. YOCO-U consistently achieves lower perplexity compared to non-recursive baselines, i.e., Transformer and YOCO. YOCO-U also maintains parity with the heavier recursive baseline (RINS), indicating effective utilization of long-range context.\", \"page\": 7, \"index\": 4, \"width\": 824, \"height\": 330}, {\"url\": \"assets/figures/arxiv/2604.01220v1/fig-005.webp\", \"caption\": \"Table 3: Performance comparison among different architectures. RINS is a standard decoder-only Transformer with early-layer recursion. YOCO-U achieves comparable results to RINS while maintaining significantly lower KV cache and inference cost.\", \"page\": 7, \"index\": 5, \"width\": 818, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2604.01220v1/fig-006.webp\", \"caption\": \"Figure 7: Overall efficiency comparison across varying sequence lengths. The models have the same number of parameters, where YOCO-U achieves better performance. YOCO-U achieves competitive prefilling and decoding throughput (i.e., tokens/second), as well as KV cache memory consumption, compared with Transformer and vanilla loop Transformer (RINS). The curves of YOCO-U and YOCO in Figure 7c are overlapped.\", \"page\": 10, \"index\": 6, \"width\": 814, \"height\": 647}, {\"url\": \"assets/figures/arxiv/2604.01220v1/fig-007.webp\", \"caption\": \"Figure 1: Overview of the YOCO-U architecture. Universal Self-Decoder (bottom) performs recursive computation (T iterations, indicated by the red dashed line) using efficient self-attention (local windows) to refine representations. Crucially, the global KV cache produced for cross-attention is generated once and remains constant regardless of T , while only the local window-based KV caches grow with iterations, keeping overall cache overhead negligible. Cross-Decoder (top) reuses this shared global KV cache via cross-attention for autoregressive token prediction.\", \"page\": 3, \"index\": 7, \"width\": 498, \"height\": 572}, {\"url\": \"assets/figures/arxiv/2604.01220v1/fig-008.webp\", \"caption\": \"Table 5: Performance comparison among different design choices. “Deep (Instead of Wide)”: double depth while using the same model size. “Upper Loop”: loop cross-decoder instead of self-decoder. “Upper Loop w/o Shared KV”: loop cross-decoder without using shared KV cache, i.e., employing self attention for loop cross-decoder.\", \"page\": 8, \"index\": 8, \"width\": 822, \"height\": 201}, {\"url\": \"assets/figures/arxiv/2604.01220v1/fig-009.webp\", \"caption\": \"Table 1: Inference complexity comparison. N,L,D, T denote sequence length, number of layers, hidden dimension, and loop iterations. W is the local window size in efficient self-attention. In YOCO-U, the global KV cache (O(ND)) is independent of T , and only the local efficient-attention cache (O(WTLD)) scales with iterations. Since W ≪ N , the additional overhead is negligible for long sequences.\", \"page\": 4, \"index\": 9, \"width\": 829, \"height\": 195}, {\"url\": \"assets/figures/arxiv/2604.01220v1/fig-010.webp\", \"caption\": \"Table 10: KV Cache Occupancy (MB) across different sequence lengths.\", \"page\": 17, \"index\": 10, \"width\": 829, \"height\": 189}, {\"url\": \"assets/figures/arxiv/2604.01220v1/fig-011.webp\", \"caption\": \"Figure 5: Parameter scaling properties. We keep the training steps the same. Left: YOCO-U achieves comparable performance with 50% fewer parameters than YOCO. Right: YOCO-U demonstrates scalable parameter utility as the activated parameter count increases.\", \"page\": 9, \"index\": 11, \"width\": 809, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.01220v1/fig-012.webp\", \"caption\": \"Figure 2: Scaling behavior of language modeling loss with the same model size. (Left) Loss versus training FLOPs: YOCO-U achieves competitive or lower loss (∆L=0.033) at the same FLOPs budget, while incurring negligible KV cache overhead. (Right) Loss versus training tokens: YOCOU also improves data efficiency, requiring approximately 62% fewer tokens to reach comparable performance.\", \"page\": 5, \"index\": 12, \"width\": 822, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2604.01220v1/fig-013.webp\", \"caption\": \"Table 7: Model size and hyperparameters used in Section 4.4.\", \"page\": 16, \"index\": 13, \"width\": 829, \"height\": 264}]"
motivation: 传统Transformer架构在进行推理时间扩展时，面临计算开销过高以及KV缓存随模型深度增加而膨胀的挑战。
method: 提出YOCO-U架构，利用YOCO的恒定KV缓存特性，并在其浅层高效注意力层引入参数共享的递归计算以增强表示深度。
result: 实验证明YOCO-U在通用任务和长文本基准测试中保持了高度竞争力，同时显著提升了推理效率和Token效用。
conclusion: 将高效注意力架构与递归计算相结合，是实现大语言模型高效深度扩展和推理性能提升的一种极具前景的方案。
---

## 摘要
测试时扩展（test-time scaling）的兴起显著提升了大语言模型（LLMs）的推理能力和智能体水平。然而，标准 Transformer 架构难以高效地扩展推理时计算，因为传统的循环策略面临高昂的计算开销，且 KV 缓存会随模型深度增加而膨胀。我们提出了 Universal YOCO (YOCO-U)，它将 YOCO 的 decoder-decoder 架构与递归计算相结合，实现了优于两者单独应用时的协同效应。基于 YOCO 框架，YOCO-U 实现了一个通用自解码器（Universal Self-Decoder），通过参数共享执行多次迭代，同时将迭代过程限制在浅层的高效注意力层中。这种结合产生了一种优异的能力-效率权衡，这是 YOCO 或递归单独无法实现的。YOCO 架构提供了恒定的全局 KV 缓存和线性预填充，而部分递归则以有限的开销增强了表示深度。综上所述，YOCO-U 在保持高效推理的同时，提高了 token 效用和扩展表现。实验结果证实，YOCO-U 在通用和长文本基准测试中保持了极强的竞争力，表明高效注意力架构与递归计算的结合是可扩展 LLM 的一个极具前景的方向。

## Abstract
The rise of test-time scaling has remarkably boosted the reasoning and agentic proficiency of Large Language Models (LLMs). Yet, standard Transformers struggle to scale inference-time compute efficiently, as conventional looping strategies suffer from high computational overhead and a KV cache that inflates alongside model depth. We present Universal YOCO (YOCO-U), which combines the YOCO decoder-decoder architecture with recursive computation to achieve a synergistic effect greater than either alone. Built on the YOCO framework, YOCO-U implements a Universal Self-Decoder that performs multiple iterations via parameter sharing, while confining the iterative process to shallow, efficient-attention layers. This combination yields a favorable capability-efficiency tradeoff that neither YOCO nor recursion achieves independently. The YOCO architecture provides a constant global KV cache and linear pre-filling, while partial recursion enhances representational depth with limited overhead. Together, YOCO-U improves token utility and scaling behavior while maintaining efficient inference. Empirical results confirm that YOCO-U remains highly competitive in general and long-context benchmarks, demonstrating that the integration of efficient-attention architectures and recursive computation is a promising direction for scalable LLMs.

---

## 论文详细总结（自动生成）

### 论文总结：Universal YOCO (YOCO-U) 实现高效深度扩展

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）进入“测试时扩展”（Test-time Scaling）时代，通过增加推理计算量来提升逻辑推理能力成为主流。然而，传统的 Transformer 架构在扩展计算深度时面临两大瓶颈：
*   **计算开销巨大**：简单的循环（Looping）策略会导致计算量线性增加。
*   **KV 缓存膨胀**：在标准 Transformer 中，KV 缓存的大小随模型深度线性增长，导致长文本推理时的内存压力剧增。
**YOCO-U** 的提出旨在结合 **YOCO（You Only Cache Once）** 架构的内存优势与 **递归计算（Recursive Computation）** 的表达能力，实现一种既能通过增加迭代次数提升性能，又不会导致 KV 缓存爆炸的高效深度扩展方案。

#### 2. 方法论：核心思想与技术细节
YOCO-U 继承了 YOCO 的“解码器-解码器”架构，并引入了局部递归机制：
*   **架构组成**：
    *   **通用自解码器（Universal Self-Decoder）**：位于模型浅层，使用高效注意力机制（如滑动窗口注意力 SWA）。其核心创新在于**递归计算**——输入在 $L/2$ 层模块中循环迭代 $T$ 次，且这些迭代共享同一组参数。
    *   **交叉解码器（Cross-Decoder）**：位于模型深层，使用标准交叉注意力机制。它复用自解码器生成的单一全局 KV 缓存。
*   **核心公式与流程**：
    1.  **递归处理**：$USD(X) = \text{Self-Decoder}^{T}(X)$。通过 $T$ 次迭代增强表示深度，而不增加参数量。
    2.  **KV 缓存生成**：仅在自解码器完成 $T$ 次迭代后，生成一组全局 KV 缓存（$\hat{K}, \hat{V}$）。
    3.  **高效推理**：交叉解码器的所有层均共享这组 KV 缓存，复杂度维持在 $O(N)$，而非标准 Transformer 的 $O(NL)$。
*   **技术优势**：实现了线性复杂度的预填充（Pre-filling）和恒定大小的全局 KV 缓存，使得递归带来的额外开销仅限于计算，而非内存。

#### 3. 实验设计
*   **数据集与场景**：
    *   **通用任务**：ARC-C, MMLU, GSM8K, Hellaswag 等。
    *   **长文本建模**：Book 和 Code 数据集，以及 Needle In-A-Haystack（大海捞针）测试。
    *   **数学推理**：11 个数学基准测试（如 MATH, SVAMP, OlympiadBench 等）。
*   **Benchmark 与对比方法**：
    *   **基准模型**：标准 Transformer、原始 YOCO。
    *   **扩展变体**：Universal Transformer (UT)、RINS（早期递归 Transformer）、ParScale（并行扩展）。
*   **评估维度**：Token 效用、模型参数扩展性、循环次数扩展性、推理吞吐量及内存占用。

#### 4. 资源与算力
*   **训练算力**：使用了 **AMD MI300X GPU** 集群。
*   **训练细节**：模型规模从 300M 扩展至 10.8B。主实验（1.3B 激活参数，10B 总参数的 MoE 结构）训练了 300B tokens，共 75k steps。
*   **推理测试**：在 **NVIDIA H100-80GB** 上进行，基于 Nano-vLLM 框架，应用了 Flash-Decoding 和 Paged Attention 等优化技术。

#### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **扩展性实验**：覆盖了从 300M 到 10.8B 的参数规模，验证了 Scaling Law 的稳定性。
*   **消融实验**：对比了不同的循环位置（浅层 vs 深层）、KV 共享策略以及模型布局（深窄 vs 浅宽）。
*   **效率对比**：在不同上下文长度（8K 到 256K）下测试了吞吐量和内存。
*   **客观性**：实验在相同的 FLOPs 预算下对比了多种架构，确保了对比的公平性。结果显示 YOCO-U 在相同计算量下显著优于非递归模型。

#### 6. 主要结论与发现
*   **计算效率高**：在相同 FLOPs 预算下，YOCO-U 的 Loss 低于标准模型；达到相同性能所需 Token 数减少了约 **62%**。
*   **推理优势巨大**：相比标准 Transformer，YOCO-U 在 256K 上下文下的预填充吞吐量提升了 **10倍**，解码速度提升了 **2.21倍**。
*   **内存节省**：在长文本场景下，YOCO-U 的 KV 缓存占用仅为递归 Transformer（如 RINS）的 **1/38**。
*   **能力增强**：递归计算显著提升了模型的逻辑推理能力，在数学基准测试中平均准确率提升了 **24.4%**。

#### 7. 优点：亮点总结
*   **协同效应**：成功将“参数共享递归”与“Decoder-Decoder 架构”结合，解决了递归模型在 Transformer 中 KV 缓存随深度膨胀的顽疾。
*   **推理友好**：保持了 YOCO 的线性预填充和极低内存占用，是目前最适合长文本推理扩展的架构之一。
*   **参数效用**：通过递归显著提升了单位参数的表达能力，实验证明其能以更小的参数量匹配更大模型的性能。

#### 8. 不足与局限
*   **边际收益递减**：实验显示，随着循环次数 $T$ 的增加，表示向量的角距离逐渐减小，意味着递归带来的性能增益会趋于饱和。
*   **架构复杂性**：虽然推理高效，但训练时的递归逻辑相比标准 Transformer 增加了实现复杂度。
*   **规模验证**：虽然测试到了 10B 规模，但在超大规模（如 100B+）模型上的表现及稳定性仍需进一步验证。
*   **任务偏差**：递归主要提升了推理和语言建模能力，对于某些对浅层特征敏感的任务，其提升幅度可能有限。

（完）
