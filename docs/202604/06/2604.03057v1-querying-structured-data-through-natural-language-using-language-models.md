---
title: Querying Structured Data Through Natural Language Using Language Models
title_zh: 使用语言模型通过自然语言查询结构化数据
authors: "Hontan Valentin-Micu, Bunea Andrei-Alexandru, Tantaroudas Nikolaos Dimitrios, Popovici Dan-Matei"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03057v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 使用 QLoRA 对大模型进行微调以实现结构化数据查询
tldr: 本研究针对检索增强生成（RAG）在处理数值和结构化数据时的局限性，提出了一种将自然语言转换为可执行查询的开源方法。通过构建规范的合成数据生成流水线，并利用QLoRA技术对DeepSeek R1 Distill 8B小型模型进行4位量化微调，实现了在消费级硬件上的高效部署。实验证明，该模型在多语言及未知地理场景下均能保持高精度，为资源受限环境下的结构化数据查询提供了可靠的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03057v1/fig-001.webp\", \"caption\": \"Fig. 4. Web application for model queries\", \"page\": 11, \"index\": 1, \"width\": 588, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2604.03057v1/fig-002.webp\", \"caption\": \"Fig. 3. Evaluation metrics during fine-tuning.\", \"page\": 7, \"index\": 2, \"width\": 616, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.03057v1/fig-003.webp\", \"caption\": \"Table 2. Model accuracy\", \"page\": 9, \"index\": 3, \"width\": 675, \"height\": 288}]"
motivation: 传统的RAG技术在处理高度结构化和数值型信息时存在困难，需要一种能精准理解用户意图并生成可执行查询的替代方案。
method: 开发了一套合成数据生成流程来捕捉数据集语义，并采用QLoRA量化微调技术训练小型语言模型生成结构化查询语句。
result: 在西班牙Durangaldea服务可及性数据集上的测试显示，微调后的模型在单语言、多语言及未见区域场景下均表现出极高的查询准确率。
conclusion: 小型领域特定模型在处理结构化数据查询任务时可达到甚至超越大型商业模型的效果，具有极强的泛化能力和部署灵活性。
---

## 摘要
本文提出了一种开源方法，允许用户通过自然语言查询结构化的非文本数据集。与在处理数值和高度结构化信息时表现不佳的检索增强生成（RAG）不同，我们的方法训练大语言模型（LLM）生成可执行查询。为了支持这一功能，我们引入了一个规范的合成训练数据生成流水线，产生多样化的问答对，以捕捉用户意图和底层数据集的语义。我们使用带有 4 位量化的 QLoRA 微调了一个紧凑型模型 DeepSeek R1 Distill 8B，使该系统适用于在通用硬件上部署。我们在描述西班牙杜兰加尔德亚（Durangaldea）地区基本服务可及性的数据集上评估了该方法。微调后的模型在单语言、多语言和未见地点场景下均实现了高准确率，展示了鲁棒的泛化能力和可靠的查询生成能力。研究结果表明，小型领域特定模型可以在不依赖大型专有 LLM 的情况下，在此任务中实现高精度，使该方法适用于资源受限的环境，并可适配更广泛的多数据集系统。

## Abstract
This paper presents an open source methodology for allowing users to query structured non textual datasets through natural language Unlike Retrieval Augmented Generation RAG which struggles with numerical and highly structured information our approach trains an LLM to generate executable queries To support this capability we introduce a principled pipeline for synthetic training data generation producing diverse question answer pairs that capture both user intent and the semantics of the underlying dataset We fine tune a compact model DeepSeek R1 Distill 8B using QLoRA with 4 bit quantization making the system suitable for deployment on commodity hardware We evaluate our approach on a dataset describing accessibility to essential services across Durangaldea Spain The fine tuned model achieves high accuracy across monolingual multilingual and unseen location scenarios demonstrating both robust generalization and reliable query generation Our results highlight that small domain specific models can achieve high precision for this task without relying on large proprietary LLMs making this methodology suitable for resource constrained environments and adaptable to broader multi dataset systems We evaluate our approach on a dataset describing accessibility to essential services across Durangaldea Spain The fine tuned model achieves high accuracy across monolingual multilingual and unseen location scenarios demonstrating both robust generalization and reliable query generation Our results highlight that small domain specific models can achieve high precision for this task without relying on large proprietary LLMs making this methodology suitable for resource constrained environments and adaptable to broader multi dataset systems.

---

## 论文详细总结（自动生成）

### 论文总结：使用语言模型通过自然语言查询结构化数据

#### 1. 核心问题与整体含义
论文针对**检索增强生成（RAG）在处理高度结构化和数值型数据时的局限性**提出了挑战。传统的 RAG 依赖向量相似度搜索，难以处理如“查找开车 10 分钟内能到达医院的小镇”这类需要精确数值逻辑和结构化查询的问题。虽然顶级商业大模型（如 GPT-4）具备此类能力，但其高昂的成本、隐私风险以及对超大规模算力的依赖限制了其在资源受限环境下的应用。因此，本研究旨在开发一种**完全开源、可在消费级硬件上运行、且能精准生成可执行查询**的轻量化系统。

#### 2. 方法论
核心思想是**将自然语言转换为特定的 API 调用或数据库查询语句**，通过执行查询获取结果后再生成最终答案。
*   **合成数据生成流水线**：由于领域特定数据集缺乏标注对，作者提出了一种基于数据库“投影（Projection）”的生成方法。
    *   从数据库表中提取语义相关的属性组合（投影）。
    *   利用 SOTA 模型（Gemini 2.5 Pro 和 DeepSeek R1）根据投影属性生成问题模板。
    *   结合 Overpass API 获取真实地理坐标，填充模板生成 44,849 个高质量问答对。
*   **模型与微调技术**：
    *   **基座模型**：选用 **DeepSeek R1-Distill-8B**，因其具备较强的推理能力和结构化输出稳定性。
    *   **微调算法**：采用 **QLoRA（4位量化低秩适配）** 技术，仅训练约 1000 万个参数，大幅降低显存占用。
*   **推理流程**：用户提问 -> 模型生成 `<API>...</API>` 标签内的查询 -> 系统拦截并执行查询 -> 将查询结果反馈给模型 -> 模型生成最终自然语言回答。

#### 3. 实验设计
*   **数据集**：使用西班牙 Durangaldea 地区的服务可及性数据集（由德国航空航天中心 DLR 提供），包含 10 万+条关于医院、超市、药店的坐标及多种交通方式（步行、骑行、驾车）的通行时间。
*   **测试场景**：
    1.  **未见位置（Unseen Locations）**：测试模型对训练集以外地理坐标的泛化能力。
    2.  **语义变体（Semantic Variants）**：测试对同义不同表达问题的鲁棒性。
    3.  **多语言查询**：涵盖西班牙语、加泰罗尼亚语、巴斯克语、加利西亚语和法语。
*   **Benchmark 与对比方法**：
    *   **指标**：精确匹配率（Exact Match, EM）、BLEU-4、ROUGE-L。
    *   **对比对象**：在零样本（Zero-shot）设置下对比了 GPT-4、Gemini、Grok 以及原始 DeepSeek R1。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 3090 (24GB)** 显卡。
*   **训练细节**：使用 4-bit 量化，Batch Size 为 32，采用余弦学习率调度器，训练 4 个 epoch，每个 epoch 耗时数小时。
*   **推理性能**：平均推理延迟约为 3 秒，主要耗时在模型生成阶段，数据库查询和后端逻辑耗时极短。

#### 5. 实验数量与充分性
作者进行了多维度的实验评估：
*   **样本量**：测试集包含 2800 条单语言查询和 500 条多语言查询，以及 500 条针对未见位置的专项测试。
*   **充分性**：实验设计较为全面，不仅验证了基础的查询准确性，还通过“未见位置”实验证明了模型并非单纯死记硬背，而是学会了 API 的调用逻辑。
*   **客观性**：通过与多种 SOTA 商业模型对比，客观展示了 8B 小模型在特定任务上通过微调可以达到甚至逼近千亿级模型的水平。

#### 6. 主要结论与发现
*   **高精度**：微调后的 8B 模型在语义变体测试中达到了 **94.2% 的精确匹配率**，接近 GPT-4 的 97.2%。
*   **强泛化性**：在完全未见的地理位置上，模型仍能保持 89% 的查询准确率。
*   **多语言能力**：模型继承了底座的语言能力，在西班牙语等主流语言上表现优异（EM > 80%），但在巴斯克语等小众语言上表现较差（EM 仅 24%）。
*   **可行性**：证明了在消费级 GPU 上部署高性能、领域特定的结构化数据查询系统是完全可行的。

#### 7. 优点
*   **开源与低成本**：不依赖闭源 API，适合对成本和隐私敏感的应用场景。
*   **自动化程度高**：提出的合成数据生成流水线减少了对人工标注的依赖，易于迁移到其他结构化数据集。
*   **模块化扩展**：基于 LoRA 的架构允许为不同数据集训练不同的适配器（Adapter），实现单模型支持多数据库。

#### 8. 不足与局限
*   **语言偏差**：对训练数据中未充分代表的语言（如巴斯克语）支持不足。
*   **错误恢复机制缺失**：目前系统缺乏自动纠错功能，如果模型生成的查询语法错误，系统无法通过迭代反馈进行自我修正。
*   **扩展性挑战**：当面对极其复杂、包含数百个表或复杂本体的多数据库系统时，手动定义模板和投影的方法可能面临规模化瓶颈。

（完）
