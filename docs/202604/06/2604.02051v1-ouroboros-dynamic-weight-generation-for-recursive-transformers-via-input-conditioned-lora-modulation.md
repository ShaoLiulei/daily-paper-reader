---
title: "Ouroboros: Dynamic Weight Generation for Recursive Transformers via Input-Conditioned LoRA Modulation"
title_zh: Ouroboros：通过基于输入条件的 LoRA 调制实现递归 Transformer 的动态权重生成
authors: "Jaber Jaber, Osama Jaber"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02051v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 递归Transformer的动态权重生成
tldr: "针对递归Transformer在深度迭代中重复应用相同变换的局限性，本文提出Ouroboros系统。该系统通过轻量级Controller超网络，根据当前隐藏状态动态生成对角调制向量，并作用于冻结的LoRA基矩阵，实现输入相关的逐步权重调整。结合门控循环和逐步层归一化，Ouroboros在大幅减少参数量的同时，在Qwen2.5-3B实验中成功恢复了超过50%因层数削减导致的性能损失，显著提升了递归模型的表达能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02051v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of recursive transformer approaches. OUROBOROS is the only system where per-step weight modulation depends on the current hidden state (input-conditioned).\", \"page\": 3, \"index\": 1, \"width\": 556, \"height\": 224}, {\"url\": \"assets/figures/arxiv/2604.02051v1/fig-002.webp\", \"caption\": \"Figure 1: OUROBOROS architecture. A pretrained model is split into frozen Prelude and Coda sections. The middle Recurrent Block loops a single transformer layer N times. At each step, the Controller observes the mean-pooled hidden state h̄ and generates a diagonal modulation vector applied to frozen SVD-initialized LoRA bases. A gated recurrence (initialized to 88% retention) mixes the new and previous hidden states.\", \"page\": 3, \"index\": 2, \"width\": 640, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2604.02051v1/fig-003.webp\", \"caption\": \"Table 4: Controller ablation across depths, ranks, and learning rates. All runs: 300K steps on a single H100. The system converges to nearly identical loss (≈ 5.08) across all configurations, demonstrating strong robustness to hyperparameter choices.\", \"page\": 6, \"index\": 3, \"width\": 490, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2604.02051v1/fig-004.webp\", \"caption\": \"Table 3: Controller vs. static per-step LoRA on Qwen2.5-3B (Prelude/Recurrent/Coda). All runs: 300K steps, rank 32, FineWeb-edu. The 17-layer baseline without any recurrence has loss 8.975. Lower is better. The Controller wins in every configuration, with the largest margin at depth 1.\", \"page\": 6, \"index\": 4, \"width\": 614, \"height\": 265}]"
motivation: 递归Transformer因每步应用完全相同的变换而限制了其在深度方向上组合不同操作的能力。
method: 引入Controller超网络动态生成输入相关的LoRA调制向量，并结合门控循环机制与逐步层归一化以增强迭代稳定性。
result: "在Qwen2.5-3B上，该方法比17层基线降低了43.4%的训练损失，且在多个深度和秩的设定下均优于静态LoRA。"
conclusion: 动态权重生成和门控循环是提升递归Transformer性能的关键，尽管目前在未见文本上的泛化性仍需进一步优化。
---

## 摘要
递归 Transformer 在多个深度步骤中重用共享权重块，以计算量换取参数量。其核心局限性在于：每一步都应用相同的变换，阻碍了模型在深度方向上组合不同的操作。我们提出了 Ouroboros，该系统将一个紧凑的控制器（Controller）超网络附加到递归 Transformer 块上。控制器观察当前的隐藏状态，生成逐步的对角调制向量，并将其应用于冻结的、经 SVD 初始化的 LoRA 基矩阵，从而使每个递归步骤都具有输入依赖性。我们将此方法与门控递归（偏置初始化为 88% 的保留率）和逐步层归一化（LayerNorm）相结合，以实现稳定的深度迭代。在将 Qwen2.5-3B 拆分为 Prelude/Recurrent/Coda 架构（保留 36 层中的 17 层）的实验中，Ouroboros 相比未经修改的 17 层基线降低了 43.4% 的训练损失，弥补了因移除层而导致的 51.3% 的性能差距。整个系统仅增加了 920 万个可训练参数（包括控制器、门控和逐步归一化），但在深度为 1 时，其性能比同等规模的静态逐步 LoRA 高出 1.44 个损失点，并在所有测试深度（1, 4, 8, 16）和秩（8, 32, 64）下均保持领先。我们还发现门控递归至关重要：若无此机制，递归层的应用会导致模型性能显著下降。这些提升是在训练分布上测得的；在预留文本上，控制器尚未表现出优于基线的改进，我们将这一局限性归因于冻结的下游层，并对此进行了详细讨论。

## Abstract
Recursive transformers reuse a shared weight block across multiple depth steps, trading parameters for compute. A core limitation: every step applies the same transformation, preventing the model from composing distinct operations across depth. We present Ouroboros, a system that attaches a compact Controller hypernetwork to a recursive transformer block. The Controller observes the current hidden state, produces a per-step diagonal modulation vector, and applies it to frozen SVD-initialized LoRA bases, making each recurrence step input-dependent. We combine this with gated recurrence (bias-initialized to 88% retention) and per-step LayerNorm for stable deep iteration. On Qwen2.5-3B split into a Prelude/Recurrent/Coda architecture (17 of 36 layers retained), Ouroboros reduces training loss by 43.4% over the unmodified 17-layer baseline, recovering 51.3% of the performance gap caused by layer removal. The full system adds only 9.2M trainable parameters (Controller, gate, and per-step norms) yet outperforms equivalently-sized static per-step LoRA by 1.44 loss points at depth 1 and remains ahead across all tested depths (1, 4, 8, 16) and ranks (8, 32, 64). We also find that gated recurrence is essential: without it, recursive layer application makes the model strictly worse. These gains are measured on the training distribution; on held-out text, the Controller does not yet improve over the baseline, a limitation we attribute to frozen downstream layers and discuss in detail. Code: https://github.com/RightNow-AI/ouroboros

---

## 论文详细总结（自动生成）

以下是对论文《Ouroboros: Dynamic Weight Generation for Recursive Transformers via Input-Conditioned LoRA Modulation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
递归 Transformer（Recursive Transformers）通过在深度方向上重用相同的权重块，显著减少了模型参数量，但其核心局限在于**权重均匀性**：每一层执行完全相同的变换，无法像标准模型那样在不同深度学习不同的抽象表示。
**Ouroboros 的动机**是打破这种均匀性。研究者提出：能否通过一个微小的超网络，根据模型当前的“思考进度”（隐藏状态）动态地调整递归层的权重？其目标是在保持极低参数增量的同时，赋予递归模型输入依赖的动态表达能力。

### 2. 方法论：核心思想与技术细节
Ouroboros 系统由以下四个关键组件构成：
*   **Prelude/Recurrent/Coda 架构**：将预训练模型（如 Qwen2.5-3B）拆分。保留前 8 层（Prelude）和后 8 层（Coda）并冻结，中间仅保留 1 层作为递归块（Recurrent Block）循环运行 $N$ 次。
*   **Controller（控制器）超网络**：这是一个仅 0.7M 参数的轻量级 MLP。它接收当前步骤的平均池化隐藏状态和步骤嵌入，动态生成对角调制向量 $\delta$。
*   **SVD 初始化的冻结 LoRA 基矩阵**：利用被移除层的权重残差进行 SVD 分解，初始化 LoRA 的 $A$ 和 $B$ 矩阵并冻结。控制器生成的 $\delta$ 仅用于调制这些预存“知识方向”的强度（$\Delta W = B \cdot \text{diag}(\delta) \cdot A$）。
*   **门控递归（Gated Recurrence）与逐步 LayerNorm**：
    *   引入带有恒等偏置（初始化为 88% 保留率）的门控机制，防止深度迭代中的表示漂移。
    *   每一步递归使用独立的 RMSNorm 缩放参数，以区分不同步骤的特征尺度。

### 3. 实验设计
*   **基础模型与数据集**：使用 **Qwen2.5-3B**（36 层），缩减至 17 层进行实验。训练数据采用 **FineWeb-edu**，序列长度 2048。
*   **Benchmark 与对比方法**：
    *   **17-layer Baseline**：直接移除层后的静态模型。
    *   **Full Qwen 3B**：原始 36 层模型（性能上限）。
    *   **Static Per-step LoRA**：与 Ouroboros 参数量相同，但调制向量是静态学习的，不随输入改变。
*   **测试场景**：涵盖了不同的递归深度（1, 4, 8, 16）、LoRA 秩（8, 32, 64）以及学习率设定。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA H100 80GB** GPU 上运行。
*   **训练细节**：每个配置训练 **300,000 个步骤**。
*   **精度与框架**：使用 PyTorch 2.11，BF16 混合精度训练，AdamW 优化器。
*   **代码实现**：项目包含约 7000 行 Python 代码，经过 66 项单元测试，确保了实现的严谨性。

### 5. 实验数量与充分性
论文进行了较为充分的消融实验和对比分析：
*   **13 种配置实验**：跨越了 5 种深度、3 种秩、2 种学习率以及 6 个静态基线。
*   **消融研究**：专门验证了“门控递归”的必要性。
*   **客观性**：作者不仅展示了成功的训练结果，还诚实地报告了在**预留测试集（Held-out text）**上泛化失败的情况，并进行了深入的根因分析，这种透明度在学术论文中值得肯定。

### 6. 主要结论与发现
*   **性能提升**：在训练分布上，Ouroboros 相比 17 层基线降低了 **43.4%** 的损失，恢复了因删层导致性能损失的 **51.3%**。
*   **动态胜过静态**：在所有测试深度下，动态控制器均优于静态 LoRA。在深度为 1 时，领先优势高达 **1.44 个损失点**。
*   **深度不变性**：令人惊讶的是，增加递归深度（从 1 到 16）对最终损失影响极小。这表明单次动态调制的递归层已能捕捉大部分增量信息。
*   **门控是必须的**：如果没有门控机制，递归应用层会导致模型性能绝对下降。

### 7. 优点（亮点）
*   **极高的参数效率**：仅增加 9.2M 可训练参数（占原模型 0.6%），实现了显著的性能修复。
*   **创新的初始化**：通过 SVD 捕获被移除层的残差知识，为控制器提供了高质量的搜索空间。
*   **输入依赖性**：成功证明了在递归模型中引入“动态权重”比增加“静态参数”更有效。

### 8. 不足与局限
*   **泛化性挑战**：模型在训练集（FineWeb-edu）上表现优异，但在未见过的文本上未能超越基线。作者分析这是因为**下游 Coda 层被冻结**，无法适应控制器产生的分布偏移。
*   **深度利用不足**：实验显示增加迭代次数并没有带来预期的性能线性增长，暗示目前的架构尚未完全挖掘“思考更久（Think longer）”的潜力。
*   **规模验证有限**：目前仅在 3B 规模模型上验证，尚未确认该结论是否能无缝迁移至 70B 等更大规模的模型。

（完）
