---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 存储增强型大语言模型智能体的训练框架
tldr: 针对记忆增强型大模型智能体开发中存在的实现碎片化问题，本文推出了首个统一且高度模块化的训练与推理框架MemFactory。该框架借鉴LLaMA-Factory的成功经验，将记忆生命周期抽象为原子化组件，支持类似“乐高”式的灵活构建。通过原生集成GRPO强化学习算法，MemFactory能够优化智能体的记忆管理策略。实验证明，该框架在MemAgent等架构上显著提升了性能，为未来记忆驱动型AI智能体的创新提供了标准化的基础设施。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v2/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2603.29493v2/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强型智能体实现高度碎片化且缺乏统一框架，限制了记忆管理策略（如提取、更新、检索）的优化与集成。
method: 提出MemFactory框架，通过原子化组件和插件式架构实现记忆生命周期的模块化管理，并原生支持利用GRPO算法进行强化学习微调。
result: "在MemAgent架构上的实验表明，MemFactory在域内和分布外评估中均显著提升了模型性能，相对增益最高达14.8%。"
conclusion: MemFactory降低了记忆驱动型智能体的开发门槛，为长短期记忆系统的标准化研究与策略优化提供了高效的基础设施。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近年来，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们推出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：为了让大语言模型（LLM）从简单的对话接口进化为全能的 AI 智能体，必须具备长期记忆、经验积累和个性化行为的能力。
*   **核心问题**：现有的记忆增强型智能体研究（如 Memory-R1, MemAgent, RMM）虽然证明了利用强化学习（RL）优化记忆操作（提取、更新、检索）的有效性，但其实现方式高度碎片化、任务耦合度高，且缺乏统一的基础设施。
*   **研究意义**：本文提出了 **MemFactory**，这是首个专为记忆增强型智能体设计的统一、模块化训练与推理框架。它旨在像 LLaMA-Factory 简化微调一样，通过“乐高式”的架构简化记忆智能体的构建、训练和评估流程。

### 2. 方法论：核心思想与关键技术
MemFactory 的核心思想是将复杂的记忆生命周期解构为原子化的模块，并通过四层架构实现统一管理：
*   **模块层 (Module Layer)**：定义了记忆操作的原子组件，包括：
    *   **Extractor（提取器）**：从原始上下文中解析事实和经验。
    *   **Updater（更新器）**：执行增删改（ADD, DEL, UPDATE, NONE）操作维护记忆库。
    *   **Retriever（检索器）**：基于语义或推理模型（LRM）重排序获取相关记忆。
    *   **Agent Module（智能体模块）**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：作为策略执行器，将上述模块组合成完整的记忆策略，支持训练模式（生成/回放接口）和推理模式（API/vLLM 接口）。
*   **环境层 (Environment Layer)**：负责数据加载和奖励管理。支持 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短文境记忆），并提供格式奖励和 LLM-as-a-Judge 等多维奖励信号。
*   **训练层 (Trainer Layer)**：核心优化引擎，原生集成 **GRPO（群体相对策略优化）** 算法。GRPO 通过组内奖励归一化估计基准，无需额外的 Critic 网络，显著降低了长上下文训练时的显存开销。

### 3. 实验设计
*   **实验场景**：验证框架对现有前沿算法的复现能力及优化效果。
*   **数据集/Benchmark**：使用了 MemAgent 发布的公开数据集，包括：
    *   **主任务评估集**：`eval_50` 和 `eval_100`（分别包含 50 和 100 个文档的上下文）。
    *   **分布外 (OOD) 评估集**：`eval_fwe_16384`（测试模型的泛化能力）。
*   **对比方法**：以 Qwen3-1.7B 和 Qwen3-4B-Instruct 作为基座模型（Base Checkpoint），对比经过 MemFactory RL 优化后的性能提升。
*   **内置范式**：框架预装了 Memory-R1、MemAgent 和 RMM 三种经典智能体架构。

### 4. 资源与算力
*   **硬件设备**：论文明确指出，整个训练和评估流水线可以在 **单台 NVIDIA A800 80GB GPU** 上执行。
*   **训练细节**：每个模型训练 250 个有效步（steps）。为了提高效率，训练时将原始上下文长度缩减至约三分之一（每样本 50-80 个文档）。
*   **推理设置**：每个问题进行 4 次独立试验并取平均分（avg@4），以减少生成方差。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型（1.7B 和 4B）在三个测试集上进行了对比实验。
*   **充分性评价**：
    *   **客观性**：使用了公开的第三方数据集和标准的 RL 算法（GRPO），实验结果具有可重复性。
    *   **局限性**：实验主要集中在 MemAgent 这一种架构的优化上，虽然框架支持 Memory-R1 和 RMM，但文中未详细展示这两类架构在 MemFactory 下的完整训练对比数据。
    *   **公平性**：通过 avg@4 的多次采样评估，一定程度上抵消了 LLM 生成的随机性。

### 6. 主要结论与发现
*   **性能提升显著**：MemFactory 成功提升了基座模型的记忆管理能力。Qwen3-1.7B 在平均得分上实现了 **14.8%** 的相对增益；Qwen3-4B-Instruct 实现了 **7.3%** 的提升。
*   **泛化能力**：较大规模的模型（4B）在 OOD 数据集上表现更稳健，证明了通过 RL 优化的记忆策略具有一定的迁移性。
*   **资源友好**：证明了在有限算力（单 GPU）下，通过合理的框架设计和算法选择（如 GRPO），也能有效优化长上下文记忆策略。

### 7. 优点与亮点
*   **高度模块化**：借鉴 LLaMA-Factory 的成功经验，将记忆操作“原子化”，极大降低了研究人员开发新记忆算法的工程门槛。
*   **算法集成先进**：原生支持 GRPO，解决了记忆增强型智能体在长上下文训练中显存爆炸的痛点。
*   **开箱即用**：集成了多种 SOTA 记忆范式，为社区提供了标准化的 Baseline。

### 8. 不足与局限
*   **覆盖范围有限**：目前的实验主要验证了循环记忆（Recurrent Memory）范式，对于复杂的显式记忆库（Memory Bank）更新策略的 RL 优化效果缺乏更深入的实证数据。
*   **训练效率仍有提升空间**：虽然支持单卡训练，但在处理极长上下文或大规模参数模型时，训练吞吐量仍是瓶颈。
*   **评估维度单一**：主要依赖于任务最终得分，对于记忆操作过程中的中间状态（如提取的准确率、更新的冗余度）缺乏细粒度的量化分析。

（完）
