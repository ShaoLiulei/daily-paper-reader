---
title: "CircuitProbe: Predicting Reasoning Circuits in Transformers via Stability Zone Detection"
title_zh: CircuitProbe：通过稳定性区域检测预测 Transformer 中的推理电路
authors: Rajkiran Panuganti
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00716v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 通过激活统计预测Transformer中的推理电路
tldr: 本研究针对Transformer模型中推理电路（即通过推理时复制可提升性能的层块）搜索效率低下的问题，提出了CircuitProbe。该方法通过分析激活统计量，在5分钟内即可预测电路位置，比暴力搜索快三到四个数量级。研究发现推理电路分为早期稳定电路和后期量级电路，并在多种模型和语言上验证了其有效性，特别适用于提升3B以下小模型的推理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00716v1/fig-001.webp\", \"caption\": \"Figure 1: Stability zone detection across four architectures. Green bars show the detected stability zone (lowest derivative of representation change). In Phi-4 (top left), this aligns with the known ground-truth reasoning circuit at layers 6–11 (blue shading). The same pattern appears in all four models despite their different architectures, sizes, and training procedures.\", \"page\": 6, \"index\": 1, \"width\": 881, \"height\": 627}, {\"url\": \"assets/figures/arxiv/2604.00716v1/fig-002.webp\", \"caption\": \"Table 1: CircuitProbe predictions across 9 models. All show the same two-circuit pattern. “L” denotes total layer count. DS-R1 abbreviates DeepSeek-R1-Distill.\", \"page\": 7, \"index\": 2, \"width\": 560, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.00716v1/fig-003.webp\", \"caption\": \"Table 2: Scaling experiment across model sizes. Layer duplication consistently benefits models under 3B but degrades 7B+ models on broad benchmarks.\", \"page\": 8, \"index\": 3, \"width\": 664, \"height\": 215}]"
motivation: 现有的推理电路搜索方法依赖于耗时的暴力扫描，限制了该技术在模型优化中的快速应用。
method: 提出CircuitProbe，通过检测早期层的表示变化导数和后期层的异常得分来快速定位推理电路。
result: 在9种模型架构上实现了数千倍的加速，预测位置与最优电路高度吻合，且仅需10个样本即可在多语言环境下稳定运行。
conclusion: 推理电路复制是提升3B参数以下小模型性能的有效手段，而CircuitProbe为其提供了极低成本的自动化定位方案。
---

## 摘要
Transformer 语言模型包含局部推理电路，即在推理阶段通过复制可以提升推理能力的连续层块。目前，寻找这些电路需要进行暴力扫描，每个模型耗费约 25 个 GPU 小时。我们提出了 CircuitProbe，它能根据激活统计量在 5 分钟内（仅需 CPU）预测电路位置，实现了三到四个数量级的加速。我们发现推理电路分为两类：早期层中的稳定性电路（通过表示变化的导数检测）和后期层中的量级电路（通过异常评分检测）。我们在涵盖 6 种架构的 9 个模型（包括 2025 年发布的模型）上进行了验证，确认在所有验证案例中，CircuitProbe 的首选预测结果与最优电路完全匹配或误差在 2 层以内。针对 Qwen 2.5 系列的规模扩展实验表明，层复制能持续提升 3B 参数以下模型的性能，但会降低 7B 以上模型的性能，这使其成为一种适用于小语言模型的实用扩展技术。CircuitProbe 仅需 10 个校准样本，且其预测结果在英语、印地语、中文和法语中均保持稳定。

## Abstract
Transformer language models contain localized reasoning circuits, contiguous layer blocks that improve reasoning when duplicated at inference time. Finding these circuits currently requires brute-force sweeps costing 25 GPU hours per model. We propose CircuitProbe, which predicts circuit locations from activation statistics in under 5 minutes on CPU, providing a speedup of three to four orders of magnitude. We find that reasoning circuits come in two types: stability circuits in early layers, detected through the derivative of representation change, and magnitude circuits in late layers, detected through anomaly scoring. We validate across 9 models spanning 6 architectures, including 2025 models, confirming that CircuitProbe top predictions match or are within 2 layers of the optimal circuit in all validated cases. A scaling experiment across the Qwen 2.5 family reveals that layer duplication consistently benefits models under 3B parameters but degrades performance in 7B+ models, making this a practical scaling technique for small language models. CircuitProbe requires as few as 10 calibration examples and its predictions are stable across English, Hindi, Chinese, and French.

---

## 论文详细总结（自动生成）

这是一份关于论文《CircuitProbe: Predicting Reasoning Circuits in Transformers via Stability Zone Detection》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：Transformer 模型中存在特定的“推理电路”（连续的层块），在推理时通过“层复制”（Layer Duplication）技术重复运行这些层，可以显著提升模型的推理能力。然而，目前寻找这些电路的方法依赖于**暴力搜索**（对每一组可能的层块进行基准测试），每个模型需耗费 7-25 个 GPU 小时，效率极低且缺乏理论预测手段。
*   **研究背景**：推理电路的位置随架构而异（如 Qwen2.5 在 7-9 层，而 Devstral 在 12-14 层），无法通过模型规格直接推断。论文旨在开发一种极速、低成本的方法，通过分析模型内部激活统计量来精准定位这些电路。

### 2. 方法论：CircuitProbe
CircuitProbe 的核心思想是利用**激活统计特征**而非下游任务表现来识别推理关键层。它将推理电路分为两类：
*   **稳定性电路（Stability Circuits）**：位于模型早期（深度 10-25%）。特征是表示变化（Representation Change）的导数趋于平稳，即模型内部表示从“混沌”转为“结晶”的过渡区。
    *   **评分公式**：结合了表示变化导数的 Z 分数、增长率、转换奖励和表示秩（Rank）。
*   **量级电路（Magnitude Circuits）**：位于模型后期（深度 85-100%）。特征是极大的表示变化幅度和高跨样本方差。
    *   **评分公式**：结合了变化量、自相似性（负相关）、方差和表示秩的 Z 分数。
*   **算法流程**：
    1.  使用极小规模校准集（10-50 个样本）进行一次前向传播。
    2.  收集残差流的五类统计量：变化幅值、自相似性、范数增长比、跨样本方差、表示秩。
    3.  计算每个候选块的稳定性得分和异常得分，取两者最大值作为最终排名依据。

### 3. 实验设计与基准测试
*   **实验对象**：涵盖 9 个模型、6 种架构（包括 Phi-4, Qwen2.5/3, DeepSeek-R1-Distill, Mistral, TinyLlama 等），参数量从 0.5B 到 14B。
*   **基准测试（Benchmark）**：
    *   **Ground Truth 获取**：使用已有的暴力搜索数据（如 Phi-4）或通过 GGUF 手术进行的自定义扫描。
    *   **评估任务**：250 题标准化测试集，包含 GSM8K（数学）、BBH（因果判断、逻辑推理、日期理解）。
*   **对比方法**：对比了纯异常检测（Anomaly Detection）、对比分析（Contrastive Analysis）和边界检测（Boundary Detection）等替代方案。

### 4. 资源与算力
*   **CircuitProbe 耗时**：在 **CPU**（float16 精度）上运行，0.5B 模型约 15 秒，14B 模型约 5 分钟。
*   **暴力搜索耗时（对比项）**：每个模型需要 **7-25 个 GPU 小时**。
*   **算力优势**：CircuitProbe 实现了 3-4 个数量级的加速，且无需 GPU 资源。

### 5. 实验数量与充分性
*   **实验规模**：
    *   跨 9 个模型验证了电路预测的准确性。
    *   在 Qwen2.5 全系列上进行了规模扩展（Scaling）实验。
    *   进行了多语言鲁棒性测试（英、汉、法、印地语）。
    *   进行了校准集规模（10-50 样本）和内容类型的消融实验。
*   **充分性评价**：实验设计较为全面，不仅验证了预测的准确性，还通过实际的“层手术”验证了性能提升。多语言和多架构的覆盖增强了结论的普适性。

### 6. 主要结论与发现
*   **双电路模式**：所有测试模型均表现出早期“稳定性电路”和后期“量级电路”的结构。
*   **小模型红利**：层复制技术对 **3B 参数以下** 的模型效果显著（提升 0.4% - 10%），但对 7B 以上模型可能导致通用性能下降。
*   **结构决定论**：推理电路的位置在预训练阶段就已确定。DeepSeek-R1 等经过 RL 强化推理的模型，其电路位置与对应的基座模型完全一致。
*   **语言无关性**：推理电路是架构特征，不随输入语言改变。

### 7. 优点与亮点
*   **极高效率**：将原本需要数天的搜索过程缩短至几分钟，且仅需 CPU。
*   **发现新机制**：首次识别并量化了早期层的“稳定性区域”作为推理关键区，这是以往基于量级的分析方法所忽略的。
*   **实用性强**：为边缘侧小模型提供了一种“零成本”的性能提升方案。

### 8. 不足与局限
*   **精度偏差**：预测位置虽然接近最优，但并非总是 100% 精确（有时存在 1-2 层的偏差）。
*   **大模型负收益**：在 7B 及以上模型中，层复制虽然提升了特定推理能力，但可能损害指令遵循或格式化等通用能力。
*   **覆盖范围**：未测试 20B-32B 以上的大规模模型，尚不清楚性能下降趋势是否会逆转。
*   **组合效应未知**：尚未研究同时复制稳定性电路和量级电路是否具有叠加效应。

（完）
