---
title: "CAWN: Continuous Acoustic Wave Networks for Autoregressive Language Modeling"
title_zh: CAWN：用于自回归语言建模的连续声波网络
authors: "Dejan Čugalj, Aleksandar Jevremovic"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04250v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: Transformer自注意力的替代架构
tldr: 针对Transformer计算复杂度高和SSM长文本信号衰减问题，本文提出连续声波网络（CAWN）。该架构采用复数域相量表示隐藏状态，通过O(L)复杂度的相位累积机制实现序列混合，并引入选择性相位共振与时间语法缓存以增强长短期依赖捕捉。实验表明，1.5亿参数模型在200万超长文本检索中表现优异，且显存占用恒定，成功突破了上下文内存瓶颈。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04250v1/fig-001.webp\", \"caption\": \"Table 4: Targeted Semantic Retrieval results for CAWN-150M. The model maintains 100% retrieval accuracy up to 1,000,000 tokens. At the 2,000,000 token mark (Phase Boundary), a selective failure occurs due to harmonic phase cancellation.\", \"page\": 11, \"index\": 1, \"width\": 706, \"height\": 186}, {\"url\": \"assets/figures/arxiv/2604.04250v1/fig-002.webp\", \"caption\": \"Figure 3: Validation perplexity descent of the CAWN-150M prototype compared to O(L2) baselines. CAWN demonstrates strict, monotonic convergence, successfully crossing below the parameter-matched standard Transformer baseline (Pythia-160M) as training progresses (where 800k micro-batch steps equates to ∼5.7 Billion tokens).\", \"page\": 10, \"index\": 2, \"width\": 765, \"height\": 441}, {\"url\": \"assets/figures/arxiv/2604.04250v1/fig-003.webp\", \"caption\": \"Figure 1: CAWN-150M architecture. Quadratic attention is supplanted by complex-domain phasor synthesis with Temporal Cache and Depth-wise Harmonic Convolution, alongside GELU [10] feed-forward layers. Residual connections are restricted to block-local scope, with historical states retrieved via depth-wise attention residuals [2].\", \"page\": 3, \"index\": 3, \"width\": 713, \"height\": 636}, {\"url\": \"assets/figures/arxiv/2604.04250v1/fig-004.webp\", \"caption\": \"Figure 2: Empirical VRAM scaling trajectory for the CAWN-150M architecture evaluated on an NVIDIA H100. Extended bounds testing demonstrates predictable linear growth until the 32k chunk limit, after which the O(1) phase caching mechanism strictly caps peak VRAM at 8.72 GB. This allows the model to continuously process up to 2 Million tokens without memory degradation or expansion.\", \"page\": 8, \"index\": 4, \"width\": 847, \"height\": 403}]"
motivation: 旨在解决Transformer自注意力机制的平方级复杂度以及状态空间模型在长上下文中信号衰减的局限性。
method: 提出一种基于复数相量相位累积的连续序列混合架构，结合选择性相位共振、深度谐波卷积及时间语法缓存。
result: 模型在50亿token训练节点下，实现了200万长度的语义检索，且峰值显存稳定在8.72GB，展现了极佳的扩展性。
conclusion: CAWN通过线性复杂度的相位机制，为超长上下文语言建模提供了一种高效且能克服内存墙的全新替代方案。
---

## 摘要
现代大语言模型 (LLM) 依赖于 Transformer 的自注意力机制，其计算复杂度随序列长度呈平方级增长。最近出现的线性时间替代方案，如状态空间模型 (SSM)，在长上下文环境下往往面临信号衰减的问题。我们提出了连续声波网络 (CAWN)，这是一种全连续的序列混合架构。CAWN 并非采用基于离散矩阵的注意力机制，而是将隐藏状态投影到多头复数域相量中，通过因果性的 $O(L)$ 相位累积机制实现序列混合。为了防止超长上下文中的信号衰减，我们引入了一种双门控选择性相位共振机制，该机制结合了频率相关保留、通过直通估计 (Straight-Through Estimation) 实现的硬阈值门控，以及用于捕捉短期局部依赖的临时语法缓存。我们还使用深度谐波卷积取代了标准的稠密线性投影，以实现最优的空间频率混合，并辅以块注意力残差进行深度状态路由。在扩展至 1.5 亿参数规模的模型中，CAWN 利用自定义 Triton 内核，在 float32 精度下实现了硬件高效的真复数相位累积。该原型在 1000 亿 token 的语料库上通过连续流式循环进行训练，并在 50 亿 token 的里程碑处进行了评估。通过定向语义检索协议进行的实证评估表明，该模型具有稳健的词汇获取能力和扩展的显式学习上下文去噪能力。通过利用分块预填充实现的 $O(1)$ 状态传递，该模型能够在 2,000,000 个 token 中检索目标信息，同时峰值显存严格稳定在 8.72 GB，从实证上克服了 $O(L^2)$ 的上下文内存墙。

## Abstract
Modern Large Language Models (LLMs) rely on Transformer self-attention, which scales quadratically with sequence length. Recent linear-time alternatives, like State Space Models (SSMs), often suffer from signal degradation over extended contexts. We introduce the Continuous Acoustic Wave Network (CAWN), a fully continuous sequence-mixing architecture. Instead of discrete matrix-based attention, CAWN projects hidden states into multi-headed complex-domain phasors, achieving sequence mixing through a causal, $O(L)$ Phase Accumulation mechanism. To prevent signal degradation over ultra-long contexts, we introduce a dual-gated Selective Phase Resonance mechanism incorporating Frequency-Dependent Retention, Hard-Threshold Gating via Straight-Through Estimation, and a Temporal Syntax Cache to capture short-term local dependencies. We also replace standard dense linear projections with Depth-wise Harmonic Convolutions for optimal spatial frequency mixing, augmented by Block Attention Residuals for depth-wise state routing. Scaled to a 150M-parameter model, CAWN utilizes custom Triton kernels for hardware-efficient, true-complex phase accumulation in float32. Trained via a continuous streaming loop on a 100-Billion-token corpus, the prototype is evaluated at a 5-Billion-token milestone. Empirical evaluations via a Targeted Semantic Retrieval protocol demonstrate robust vocabulary acquisition and extended explicitly learned contextual denoising. By leveraging $O(1)$ state-passing via chunked prefill, the model retrieves targeted information across 2,000,000 tokens while strictly plateauing at 8.72 GB of Peak VRAM, empirically overcoming the $O(L^2)$ context memory wall.

---

## 论文详细总结（自动生成）

这是一份关于论文《CAWN: Continuous Acoustic Wave Networks for Autoregressive Language Modeling》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心动机是解决大语言模型（LLM）在处理超长文本时的**计算瓶颈**。
*   **Transformer 的局限性**：自注意力机制的计算和内存需求随序列长度 $L$ 呈平方级增长（$O(L^2)$），形成了“内存墙”。
*   **现有替代方案的不足**：状态空间模型（SSM，如 Mamba）虽然实现了线性复杂度，但在极长上下文中容易出现信号衰减和信息丢失。
*   **核心假设**：作者假设语言的语法和语义可以被建模为复数域中连续声波的**相干与干涉**。通过将离散 Token 映射为连续波形，可以利用物理上的相位累积来高效编码历史信息。

### 2. 方法论：核心思想与关键技术
CAWN 抛弃了矩阵注意力，构建了一个基于**共振层（Resonance Layer）**的架构：
*   **复数域相量投影**：将隐藏状态投影为复数域的相量（Phasor），包含振幅（Amplitude）和相位（Phase）。
*   **因果相位累积（Causal Phase Accumulation）**：利用欧拉公式实现 $O(L)$ 复杂度的序列混合。每个 Token 的波形会与之前累积的共振状态动态交互，通过固定的复数旋转（类似 RoPE 的逻辑）来编码相对位置。
*   **选择性相位共振机制**：
    *   **频率相关保留（$\gamma$ 门控）**：低频波充当“全局记忆”，高频波充当“局部草稿本”。
    *   **硬阈值门控（$\beta$ 门控）**：使用直通估计器（STE）将微弱信号强制归零，防止数值噪声污染相位状态。
*   **时间语法缓存（Temporal Syntax Cache）**：在波形投影前使用 1D 卷积捕捉局部语法依赖，让全局共振专注于长期语义。
*   **块注意力残差（Block Attention Residuals）**：为了防止深层网络中的信号稀释，每 4 层切断一次残差流，通过深度维度的 Softmax 注意力从早期块中提取“纯净”的波形状态。
*   **硬件优化**：开发了自定义 **Triton 内核**，在 SRAM 中直接进行复数运算，极大提升了硬件效率。

### 3. 实验设计
*   **数据集**：使用 1000 亿 Token 的混合英语语料库（FineWeb-Edu, DCLM 等），采用无限流式训练模式。
*   **基准测试（Benchmarks）**：
    *   **语言建模**：WikiText-103 上的困惑度（Perplexity）。
    *   **语义推理**：PIQA（物理常识）、ARC-Easy（科学逻辑）。
    *   **长文本检索**：定向语义检索协议（Targeted Semantic Retrieval），在海量噪声中检索特定目标。
*   **对比方法**：
    *   **标准 Transformer**（$O(L^2)$ 复杂度基准）。
    *   **Pythia-160M**（参数匹配的成熟模型）。
    *   **GPT-2 Small** 和 **SmolLM-135M**（作为性能上限参考）。

### 4. 资源与算力
*   **硬件环境**：主要在 **NVIDIA RTX 3090 (24GB)** 上进行训练，并在 **NVIDIA H100 (80GB)** 上进行了超长上下文的压力测试。
*   **训练细节**：模型规模为 1.5 亿参数（150M）。采用了梯度累积（36步），微批次大小为 7。
*   **训练时长**：论文展示了训练至 50 亿 Token（约 75.2 万步）时的阶段性成果，总训练计划为 1000 亿 Token。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了内存缩放测试、推理吞吐量测试、零射（Zero-shot）推理评估以及极长上下文检索实验。
*   **充分性评价**：
    *   **客观性**：通过与参数量相近的 Pythia 模型在相同 Token 数下的对比，证明了架构的学习效率。
    *   **公平性**：内存测试在相同硬件和精度（bfloat16）下进行，对比直观。
    *   **局限**：消融实验相对较少，例如尚未完全隔离“数据增强”与“架构设计”对长文本检索能力的各自贡献。

### 6. 主要结论与发现
*   **突破内存墙**：CAWN 在处理 200 万 Token 时，显存占用稳定在 **8.72 GB**，而标准 Transformer 在 4000 Token 时即发生显存溢出（OOM）。
*   **推理效率**：实现了 $O(1)$ 的状态传递，生成速度不随上下文长度增加而减慢。
*   **性能表现**：在 50 亿 Token 节点，CAWN 的推理准确率（PIQA 60.23%）超过了训练 21 亿 Token 的 Pythia-160M，证明了波形干涉建模语义的有效性。
*   **长文本极限**：模型能完美检索 100 万 Token 距离的信息，但在 200 万 Token 处因“声波周期性”或数值精度问题出现部分失效。

### 7. 优点（亮点）
*   **创新的物理隐喻**：将复杂的注意力机制简化为波形干涉，理论优雅且具有高度可解释性。
*   **极致的扩展性**：通过复数域建模和 Triton 内核优化，真正实现了超长上下文的工业级可行性。
*   **深度路由优化**：引入块注意力残差解决了深层网络信号衰减的顽疾。

### 8. 不足与局限
*   **数值稳定性依赖精度**：在极长序列下，bfloat16 的舍入误差会累积，必须在核心路径使用 float32，这增加了一定的计算开销。
*   **吞吐量峰值较低**：由于依赖自定义 Triton 内核而非高度成熟的 FlashAttention 库，目前在短文本上的绝对生成速度略慢于优化后的 Transformer。
*   **模型规模尚小**：目前仅验证了 150M 规模，尚未在大规模参数（如 7B 以上）和万亿级 Token 上验证其收敛稳定性。

（完）
