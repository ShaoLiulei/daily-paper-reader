---
title: Adapting Text LLMs to Speech via Multimodal Depth Up-Scaling
title_zh: 通过多模态深度扩展将文本大语言模型适配至语音
authors: "Kazuki Yano, Jun Suzuki, Shinji Watanabe"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00489v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 通过深度上采样将文本大模型适配至语音
tldr: 本研究针对将预训练文本大语言模型适配为语音模型时导致的文本能力退化问题，提出了“多模态深度上扩”方法。该方法通过在冻结的文本LLM中插入新层并仅对新层进行语音数据训练，实现了与全量微调相当的语音识别性能。实验证明，该方法在显著减少文本能力损失的同时，利用E-Branchformer架构进一步提升了效率，为构建高性能且保留文本能力的语音大模型提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00489v1/fig-001.webp\", \"caption\": \"Figure 1: Proposed multimodal depth up-scaling strategy for Speech LM adaptation: trainable E-Branchformer blocks are interleaved with frozen transformer blocks to acquire speech capabilities while preserving pre-trained text representations.\", \"page\": 1, \"index\": 1, \"width\": 443, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2604.00489v1/fig-002.webp\", \"caption\": \"Table 2: Effect of added layer architecture (INTERLEAVED placement) and function preserving initialization. Top: architecture comparison. Bottom: initialization comparison for EBranchformer on SmolLM2-1.7B. WMerge is the merge projection (§3.3).\", \"page\": 3, \"index\": 2, \"width\": 464, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.00489v1/fig-003.webp\", \"caption\": \"Table 1: Results on SmolLM2-360M and SmolLM2-1.7B. WER (%) on LibriSpeech (lower is better). Text task accuracy is the average of 8 benchmarks (%, higher is better). ∆ denotes the change from the pre-trained model. Depth up-scaling results are with added layers kept at inference. Dropping added layers recovers the pre-trained performance (∆ = 0.0) by design. Italic = full fine-tuning (updates all parameters). Bold = best among depth up-scaling.\", \"page\": 3, \"index\": 3, \"width\": 477, \"height\": 358}]"
motivation: 旨在解决将文本大语言模型适配为语音模型过程中，因持续预训练导致的原始文本处理能力严重退化的问题。
method: 提出在冻结的文本LLM中插入新的神经层（如E-Branchformer），并仅对这些新增层进行语音数据的训练。
result: "该方法在语音识别任务上达到了与全量微调相当的水平，同时将文本能力退化降低了75%以上，且训练参数量减少了60%。"
conclusion: 多模态深度上扩是一种能有效平衡语音能力获取与文本能力保留的高效模型适配策略。
---

## 摘要
通过在语音数据上进行持续预训练，将预训练文本大语言模型（LLMs）适配为语音语言模型（Speech LMs）具有广阔前景，但通常会损害原始的文本能力。我们提出了多模态深度扩展（Multimodal Depth Upscaling），这是持续 LLM 预训练中一种新兴策略的延伸，即在冻结的文本 LLM 中插入新的 Transformer 层，且仅在语音数据上训练新增层。在 4.8 万小时英语自动语音识别（ASR）数据上使用 SmolLM2-360M 和 SmolLM2-1.7B 进行的实验表明，深度扩展实现了与全量微调相当的 ASR 性能，同时造成的文本能力退化远低于全量微调和低秩自适应（LoRA）。我们进一步证明，将专为语音识别设计的架构 E-Branchformer 作为插入层，在较大模型上实现了匹配或超越全量微调的 ASR 性能，同时在可训练参数减少 60% 的情况下，将文本退化降低了 75% 以上。

## Abstract
Adapting pre-trained text Large Language Models (LLMs) into Speech Language Models (Speech LMs) via continual pretraining on speech data is promising, but often degrades the original text capabilities. We propose Multimodal Depth Upscaling, an extension of an emerging strategy in continual LLM pre-training, where new transformer layers are inserted into a frozen text LLM and only the added layers are trained on speech data. Experiments with SmolLM2-360M and SmolLM2-1.7B on 48k hours of English Automatic Speech Recognition (ASR) data show that depth up-scaling achieves ASR comparable to full fine-tuning while causing far less text degradation than both full fine-tuning and Low-Rank Adaptation (LoRA). We further show that incorporating E-Branchformer, an architecture designed for speech recognition, as the inserted layers achieves ASR that matches or surpasses full fine-tuning on the larger model while reducing text degradation by over 75% with 60% fewer trainable parameters.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adapting Text LLMs to Speech via Multimodal Depth Up-Scaling》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：将预训练的文本大语言模型（LLM）适配为语音语言模型（Speech LM）时，通常采用在语音数据上进行持续预训练（CPT）的方法。然而，这种跨模态适配往往会导致严重的“灾难性遗忘”，即模型在获得语音能力的同时，原始的文本理解和生成能力大幅退化。
*   **核心问题**：如何在不依赖大规模文本数据回放（Replay）且不显著增加计算成本的前提下，高效地将文本 LLM 适配至语音模态，并完美保留其原始文本能力？

### 2. 核心方法论
论文提出了**多模态深度扩展（Multimodal Depth Up-scaling）**策略：
*   **核心思想**：在原始 $n$ 层的文本 LLM 中插入 $m$ 个新的神经层。在训练期间，**冻结所有原始参数**，仅训练新增层和扩展的语音词表嵌入层。
*   **关键技术细节**：
    *   **层插入策略**：研究了五种放置方式（交错式 Interleaved、底部、中部、顶部、三明治式）。实验发现“交错式”在语音识别（ASR）性能上表现最佳。
    *   **函数保留初始化（Function Preserving Initialization）**：为了防止训练初期新增层破坏预训练表示，通过将新增层的输出投影矩阵初始化为零（或特定恒等映射），使新增层在初始状态下等效于“恒等函数”（Identity Function）。
    *   **E-Branchformer 架构**：除了标准的 Transformer 层，论文引入了专为语音设计的 E-Branchformer 块作为新增层，利用其并行分支分别捕捉全局上下文（自注意力）和局部声学特征（卷积）。
    *   **推理时的灵活性**：由于原始权重未动，推理时若只需文本能力，可直接丢弃新增层，100% 恢复原始 LLM 性能。

### 3. 实验设计
*   **数据集**：使用 OWSM v3.2 集合中的 4.8 万小时英语 ASR 数据进行训练。
*   **基座模型**：SmolLM2-360M（32层）和 SmolLM2-1.7B（24层）。
*   **Benchmark**：
    *   **语音能力**：LibriSpeech（test-clean 和 test-other）的字错率（WER）。
    *   **文本能力**：8 项标准文本基准测试（ARC-e/c, BoolQ, HellaSwag, MMLU 等）的平均准确率。
*   **对比方法**：
    1.  **全量微调（Full Fine-tuning）**：更新所有参数。
    2.  **LoRA**：低秩自适应，设置了与深度扩展相当的可训练参数量。

### 4. 资源与算力
*   **训练配置**：使用 AdamW 优化器，峰值学习率 $1 \times 10^{-4}$，2.5万步预热，线性衰减。最大上下文长度 8192 标记。
*   **算力说明**：文中提到了使用了北海道大学信息倡议中心的超级计算机系统或研究云系统，但**未明确给出具体的 GPU 型号、数量以及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同参数量级（360M 和 1.7B）的模型上验证了通用性。
    *   对比了 5 种不同的层放置策略。
    *   进行了消融实验，验证了 E-Branchformer 架构和初始化策略的有效性。
    *   进行了定性分析，测试了模型在语音驱动下的翻译、简化和摘要等指令遵循能力。
*   **充分性评价**：实验设计较为客观、公平。特别是将 LoRA 的参数量提升到与新增层一致的水平进行对比，增强了结论的说服力。

### 6. 主要结论与发现
*   **性能匹配**：在 1.7B 模型上，采用交错式放置的深度扩展方法，其 ASR 性能达到甚至超过了全量微调。
*   **文本保留**：相比全量微调和 LoRA 导致的文本能力大幅崩溃（如 1.7B 模型得分下降 >30%），深度扩展在保留新增层的情况下仅有轻微下降（<7%），且可通过丢弃新增层实现零退化。
*   **架构优势**：E-Branchformer 作为新增层比标准 Transformer 更有效，能以更少的可训练参数（减少 60%）获得更好的 ASR 结果。
*   **指令遵循**：全量微调后的模型完全丧失了遵循文本指令的能力，而深度扩展模型仍能根据语音输入完成翻译和摘要任务。

### 7. 优点
*   **架构优雅**：通过“恒等初始化”和“层插入”实现了模态适配与能力保留的解耦。
*   **实用性强**：解决了开源模型在缺乏原始预训练文本数据时无法进行“数据回放”的痛点。
*   **推理灵活**：支持在语音模式（全模型）和纯文本模式（丢弃新增层）之间无缝切换。

### 8. 不足与局限
*   **任务覆盖有限**：实验主要集中在英语 ASR 任务上，尚未验证在多语言、语音翻译或复杂音频推理任务上的表现。
*   **推理开销**：虽然文本模式可以丢弃层，但在语音模式下，由于增加了层数，推理时的计算量和显存占用会比原始模型略高（增加了约 25% 的层）。
*   **算力细节缺失**：缺乏具体的训练效率数据（如每小时训练成本），难以评估其在资源受限环境下的可行性。

（完）
