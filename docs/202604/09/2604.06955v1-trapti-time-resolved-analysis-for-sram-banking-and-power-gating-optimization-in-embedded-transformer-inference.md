---
title: "TRAPTI: Time-Resolved Analysis for SRAM Banking and Power Gating Optimization in Embedded Transformer Inference"
title_zh: TRAPTI：嵌入式 Transformer 推理中 SRAM 分体与电源门控优化的时间分辨分析
authors: "Jan Klhufek, Alberto Marchisio, Vojtech Mrazek, Lukas Sekanina, Muhammad Shafique"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06955v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: Transformer推理优化与KV缓存管理
tldr: 针对Transformer模型在嵌入式硬件部署中面临的KV缓存内存压力，本文提出TRAPTI分析框架。该框架通过周期级仿真获取内存占用的时间分辨轨迹，并据此优化SRAM分块与电源门控策略。研究对比了MHA与GQA架构，证明了GQA在降低峰值内存占用及提升能效方面的显著潜力，为嵌入式加速器设计提供了量化指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06955v1/fig-001.webp\", \"caption\": \"Fig. 5. Time-resolved SRAM occupancy for two workloads executed on the same accelerator with a 128 MiB shared SRAM. Left: GPT-2 XL with MHA (48 layers). Right: DS-R1D Q-1.5B with GQA (28 layers). DS-R1D Q-1.5B exhibits lower peak SRAM requirement with more room for optimization.\", \"page\": 6, \"index\": 1, \"width\": 1076, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2604.06955v1/fig-002.webp\", \"caption\": \"Fig. 8. Inferred bank activity timeline for DS-R1D Q-1.5B at 64 MiB with B=4 under different values of α. The black curve shows total SRAM occupancy over time. The red region represents the effective data-placement overhead induced by α, which reduces the usable per-bank capacity and models conservative assumptions about non-ideal data packing. Blue segments indicate banks that must remain active. Smaller α values increase the modeled overhead, resulting in more active banks and fewer gate-eligible intervals.\", \"page\": 6, \"index\": 2, \"width\": 529, \"height\": 433}, {\"url\": \"assets/figures/arxiv/2604.06955v1/fig-003.webp\", \"caption\": \"TABLE II ENERGY/AREA TRADE-OFFS FOR SRAM BANKING CANDIDATES AT α=0.9. ENERGY (E) IN MJ, AREA (A) IN MM2 , VARYING THE NUMBER OF BANKS (B); ∆ VALUES ARE RELATIVE TO B=1 FOR EACH MEMORY CAPACITY (C). FOR DS-R1D Q-1.5B, BANKING REDUCES SRAM ENERGY ACROSS ALL CAPACITIES, WITH THE STRONGEST REDUCTIONS AT B ∈ {8, 16} (BOLD), WHILE FURTHER BANKING YIELDS DIMINISHING ENERGY RETURNS AND AREA OVERHEAD. GPT-2 XL SHOWS SIMILAR TRENDS BUT IS EVALUATED ONLY AT LARGER CAPACITIES DUE TO ITS HIGHER SRAM REQUIREMENT.\", \"page\": 7, \"index\": 3, \"width\": 1076, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2604.06955v1/fig-004.webp\", \"caption\": \"Fig. 3. Two-stage workflow for on-chip memory sizing, banking, and power-gating analysis. Stage I (blue) uses TransInferSim to simulate inference for a given workload and accelerator configuration, producing performance statistics and a time-resolved on-chip memory occupancy trace; the blue loop indicates iterative adjustment of the memory configuration until a target SRAM capacity is reached. Stage II (orange) reuses the Stage-I trace and statistics to evaluate banked SRAM organizations and power-gating policies using CACTI 7 characterization, yielding energy trends across bank counts. The power-gating policies include (i) a baseline without power gating, (ii) an aggressive policy that gates idle-eligible banks using minimal headroom (α≈1), and (iii) a conservative policy that reserves per-bank headroom (α<1) and avoids gating across short idle intervals to reduce wake-up overhead.\", \"page\": 4, \"index\": 4, \"width\": 1076, \"height\": 218}]"
motivation: 旨在解决Transformer推理过程中KV缓存动态增长导致的片上存储资源紧张及功耗优化难题。
method: 提出一种结合周期级推理仿真与离线优化分析的两阶段方法，用于探索最优的SRAM分块组织和电源门控配置。
result: 实验显示DeepSeek-R1（采用GQA）相比GPT-2 XL（采用MHA）在相同配置下实现了2.72倍的峰值片上内存占用削减。
conclusion: 通过时间分辨的内存占用分析，可以有效利用GQA等机制带来的存储红利，进一步挖掘嵌入式Transformer推理的电源门控优化空间。
---

## 摘要
Transformer 神经网络在语言和视觉任务中取得了最先进的准确率，但其在嵌入式硬件上的部署受到严格的面积、延迟和功耗约束的阻碍。在推理过程中，性能和效率日益受到键值（KV）缓存的主导，其内存占用随序列长度增加而增长，给片上内存利用率带来了压力。尽管与多头注意力（MHA）相比，分组查询注意力（GQA）等现有机制减少了 KV 缓存需求，但要有效利用这种减少，需要了解片上内存需求随时间演变的情况。本研究提出了 TRAPTI，这是一种结合了周期级推理仿真与片上内存占用时间分辨分析的两阶段方法论，用以指导设计决策。在第一阶段，该框架通过仿真获取内存占用轨迹和内存访问统计数据。在第二阶段，该框架利用这些轨迹在离线优化流程中探索分体内存组织和电源门控配置。我们在相同的加速器配置下将该方法应用于 GPT-2 XL 和 DeepSeek-R1-Distill-Qwen-1.5B，从而实现了 MHA 和 GQA 内存特性的直接比较。分析表明，在此设置下，与 GPT-2 XL 相比，DeepSeek-R1-Distill-Qwen-1.5B 的峰值片上内存利用率降低了 2.72 倍，为电源门控优化释放了更多机会。

## Abstract
Transformer neural networks achieve state-of-the-art accuracy across language and vision tasks, but their deployment on embedded hardware is hindered by stringent area, latency, and energy constraints. During inference, performance and efficiency are increasingly dominated by the Key--Value (KV) cache, whose memory footprint grows with sequence length, straining on-chip memory utilization. Although existing mechanisms such as Grouped-Query Attention (GQA) reduce KV cache requirements compared to Multi-Head Attention (MHA), effectively exploiting this reduction requires understanding how on-chip memory demand evolves over time. This work presents TRAPTI, a two-stage methodology that combines cycle-level inference simulation with time-resolved analysis of on-chip memory occupancy to guide design decisions. In the first stage, the framework obtains memory occupancy traces and memory access statistics from simulation. In the second stage, the framework leverages the traces to explore banked memory organizations and power-gating configurations in an offline optimization flow. We apply this methodology to GPT-2 XL and DeepSeek-R1-Distill-Qwen-1.5B under the same accelerator configuration, enabling a direct comparison of MHA and GQA memory profiles. The analysis shows that DeepSeek-R1-Distill-Qwen-1.5B exhibits a 2.72x reduction in peak on-chip memory utilization in this setting compared to GPT-2 XL, unlocking further opportunities for power-gating optimization.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《TRAPTI: Time-Resolved Analysis for SRAM Banking and Power Gating Optimization in Embedded Transformer Inference》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
Transformer 模型（如 LLM）在嵌入式设备上的部署面临严峻挑战，主要矛盾在于 **KV 缓存（Key-Value Cache）** 随序列长度线性增长，极大地消耗了有限的片上 SRAM 资源。
*   **研究动机**：虽然新型注意力机制（如 GQA）能减少 KV 缓存，但现有的设计空间探索（DSE）工具通常只关注峰值内存需求或静态统计，忽略了内存占用随时间动态变化的特性。
*   **核心问题**：如何系统地量化 Transformer 推理过程中内存占用的“时间分辨”特性，并利用这些动态空闲区间，通过硬件手段（如 SRAM 分块和电源门控）来降低静态功耗（漏电功耗）。

### 2. 方法论：TRAPTI 两阶段框架
论文提出了 **TRAPTI** 框架，将周期级仿真与离线内存优化解耦：
*   **阶段 I：仿真与轨迹提取**
    *   使用 **TransInferSim** 仿真器对目标 Transformer 工作负载进行周期级仿真。
    *   **关键技术**：提取“时间分辨的片上内存占用轨迹”（Time-resolved Occupancy Trace）。该轨迹记录了每一时刻 SRAM 中“必需数据”（Needed）、“过时数据”（Obsolete）和“空闲空间”（Free）的分布。
*   **阶段 II：SRAM 分块与电源门控优化**
    *   **核心思想**：将 SRAM 划分为 $B$ 个独立可控的存储体（Banks）。根据阶段 I 的轨迹，计算每一时刻必须保持开启的存储体数量 $B_{act}(t)$。
    *   **关键公式**：$B_{act}(t) = \lceil \frac{o(t)}{\alpha \cdot (C/B)} \rceil$。其中 $o(t)$ 是 $t$ 时刻的内存占用，$\alpha$ 是预留系数（Headroom Factor，用于模拟非理想数据打包），$C$ 是总容量。
    *   **电源门控策略**：当某个存储体的空闲时间超过“盈亏平衡点”（Break-even duration，即节省的漏电功耗大于开关切换开销）时，对其执行电源门控（关断）。

### 3. 实验设计
*   **对比模型（Workloads）**：
    1.  **GPT-2 XL**：代表传统的多头注意力机制（MHA）。
    2.  **DeepSeek-R1-Distill-Qwen-1.5B**：代表现代的分组查询注意力机制（GQA）。
*   **硬件模板**：包含 4 个 128×128 的脉动阵列（Systolic Arrays），主频 1GHz，配有共享 SRAM 和离线 DRAM。
*   **Benchmark/对比维度**：
    *   内存占用轨迹对比（MHA vs. GQA）。
    *   不同 SRAM 容量（48MiB 至 128MiB）下的能效表现。
    *   不同分块数量（B=1, 2, 4, 8, 16, 32）的面积与功耗权衡。

### 4. 资源与算力
*   **仿真工具**：使用了 **TransInferSim**（周期级推理仿真器）和 **CACTI 7**（SRAM 建模工具，基于 45nm 工艺节点）。
*   **算力说明**：论文**未提及**模型训练所需的 GPU 算力，因为该研究聚焦于**推理阶段的硬件仿真分析**。实验是在通用的 CPU 服务器上运行仿真软件完成的。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对两个主流模型进行了全序列（2048 token）的推理仿真。
    *   对 SRAM 容量进行了步进式扫掠（16MiB 增量）。
    *   对分块数量和电源门控策略（激进 vs. 保守）进行了消融实验。
    *   扩展实验：测试了多级存储层次（Dedicated Memory + Shared SRAM）的复杂架构。
*   **充分性评价**：实验设计较为充分，通过直接对比 MHA 和 GQA 在相同硬件底座上的表现，客观地证明了 GQA 在硬件层面的优势。引入 $\alpha$ 系数考虑了实际部署中的非理想情况，增强了实验的公平性和现实意义。

### 6. 主要结论与发现
*   **GQA 的巨大优势**：在相同配置下，采用 GQA 的 DeepSeek 模型比采用 MHA 的 GPT-2 XL 峰值内存占用降低了 **2.72 倍**，推理速度快 **1.89 倍**。
*   **能效提升**：通过 TRAPTI 优化，SRAM 的总能耗最高可降低 **78%**（在 $\alpha=0.9$ 的保守设定下）。
*   **分块收益递减**：增加存储体数量（B）能显著降低功耗，但当 B > 16 后，面积开销增加而能效收益趋于平缓。
*   **动态性利用**：GQA 模型由于内存需求波动大，比 MHA 模型拥有更多的电源门控机会，从而在硬件层面获得更高的能效红利。

### 7. 优点与亮点
*   **时间分辨分析**：突破了传统 DSE 只看静态峰值的局限，能够捕捉推理过程中的瞬态空闲机会。
*   **两阶段解耦**：仿真与优化分离，使得设计者可以在不重新运行耗时仿真的情况下，快速评估成百上千种硬件配置。
*   **实用性强**：集成了 TransInferSim 工具，支持复现，且对当前流行的 GQA 架构提供了量化的硬件设计指导。

### 8. 不足与局限
*   **数据放置假设**：实验假设数据可以完美或接近完美地（$\alpha=0.9$）在存储体间连续打包，实际硬件中的内存碎片和地址映射可能会降低这种利用率。
*   **模型覆盖面**：虽然对比了 MHA 和 GQA，但未涉及更复杂的注意力变体（如线性注意力或稀疏注意力）。
*   **应用限制**：目前主要针对确定性的推理调度，对于动态批处理（Dynamic Batching）等更复杂的服务器端场景，内存轨迹的预测会更加困难。

（完）
