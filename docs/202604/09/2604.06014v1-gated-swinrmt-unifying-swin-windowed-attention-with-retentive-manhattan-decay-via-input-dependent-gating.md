---
title: "Gated-SwinRMT: Unifying Swin Windowed Attention with Retentive Manhattan Decay via Input-Dependent Gating"
title_zh: Gated-SwinRMT：通过输入依赖的门控机制统一 Swin 窗口注意力与 Retentive Manhattan 衰减
authors: "Dipan Maity, Suman Mondal, Arindam Roy"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06014v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 结合窗口注意力和空间衰减的混合视觉Transformer
tldr: 本研究提出Gated-SwinRMT，一种结合了Swin Transformer移位窗口注意力和Retentive Networks（RMT）曼哈顿距离空间衰减的混合视觉Transformer。该模型通过输入依赖的门控机制增强特征表达，将自注意力分解为窗口内的行列双向保留路径，并利用指数衰减掩码提供二维局部先验。实验表明，该方法在Mini-ImageNet等数据集上显著优于RMT基准模型，证明了门控机制与空间衰减结合在视觉任务中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06014v1/fig-001.webp\", \"caption\": \"Table 2: Shared training hyper-parameters applied identically to all models on both datasets.\", \"page\": 6, \"index\": 1, \"width\": 535, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2604.06014v1/fig-002.webp\", \"caption\": \"Figure 1: Architecture of the proposed Gated-SwinRMT variants. (a) Gated-SwinRMT-SWAT: sigmoid-based normalized attention with SwiGLU-gated values, balanced ALiBi positional bias, and multiplicative spatial decay γ|i−j| applied post-sigmoid. (b) GatedSwinRMT-Retention: softmax-normalized retention with additive log-decay mask Mij=|i−j| log γh applied pre-softmax, and a learned G1 sigmoid gate applied after local context enhancement (LCE) and before the output projection WO. Both variants share DWConv 3×3 positional encoding, LayerScale (γ1, γ2) with DropPath, an LCE module on V , adaptive window partitioning with optional cyclic shift, and convolutional patch merging. Best viewed in colour.\", \"page\": 2, \"index\": 2, \"width\": 1102, \"height\": 969}]"
motivation: 旨在通过结合窗口注意力、空间衰减和输入依赖门控，解决现有视觉Transformer在局部建模能力和特征抑制灵活性上的不足。
method: 提出了SWAT和Retention两种变体，利用行列分解的保留机制、曼哈顿衰减偏置以及SwiGLU或Sigmoid门控实现输入相关的特征增强。
result: "在Mini-ImageNet上，Gated-SwinRMT-SWAT达到了80.22%的准确率，相比RMT基准（73.74%）提升了约6.48个百分点。"
conclusion: 实验证明，引入输入依赖的门控机制和空间衰减先验能显著增强窗口化视觉Transformer的特征提取能力和分类性能。
---

## 摘要
我们引入了 Gated-SwinRMT，这是一系列混合视觉 Transformer，它结合了 Swin Transformer 的移动窗口注意力（shifted-window attention）与 Retentive Networks (RMT) 的曼哈顿距离空间衰减，并辅以输入依赖的门控机制。自注意力在每个移动窗口内被分解为连续的宽度方向和高度方向的保留（retention）传递，其中每个头的指数衰减掩码提供了二维局部性先验，而无需学习位置偏置。本文提出了两种变体：Gated-SwinRMT-SWAT 使用 sigmoid 激活函数替代了 softmax，实现了具有乘法激活后空间衰减的平衡 ALiBi 斜率，并通过 SwiGLU 对值投影（value projection）进行门控，其归一化输出隐式地抑制了无信息的注意力分数；Gated-SwinRMT-Retention 保留了带有加性对数空间衰减偏置的 softmax 归一化保留机制，并引入了一个显式的 G1 sigmoid 门——该门从块输入投影而来，应用于局部上下文增强 (LCE) 之后、输出投影 $W_O$ 之前——以缓解低秩 $W_V \cdot W_O$ 瓶颈，并实现对注意力输出的输入依赖抑制。我们在相同的训练协议下，在 Mini-ImageNet ($224{\times}224$, 100 类) 和 CIFAR-10 ($32{\times}32$, 10 类) 上评估了这两种变体。在约 77–79M 参数量下，Gated-SwinRMT-SWAT 在 Mini-ImageNet 上达到了 80.22% 的 top-1 测试准确率，Gated-SwinRMT-Retention 达到了 78.20%，而 RMT 基准模型为 73.74%。在 CIFAR-10 上——由于较小的特征图导致自适应窗口机制将注意力坍缩至全局范围——准确率优势从 +6.48 个百分点压缩至 +0.56 个百分点。

## Abstract
We introduce Gated-SwinRMT, a family of hybrid vision transformers that combine the shifted-window attention of the Swin Transformer with the Manhattan-distance spatial decay of Retentive Networks (RMT), augmented by input-dependent gating. Self-attention is decomposed into consecutive width-wise and height-wise retention passes within each shifted window, where per-head exponential decay masks provide a two-dimensional locality prior without learned positional biases.   Two variants are proposed. \textbf{Gated-SwinRMT-SWAT} substitutes softmax with sigmoid activation, implements balanced ALiBi slopes with multiplicative post-activation spatial decay, and gates the value projection via SwiGLU; the Normalized output implicitly suppresses uninformative attention scores. \textbf{Gated-SwinRMT-Retention} retains softmax-normalized retention with an additive log-space decay bias and incorporates an explicit G1 sigmoid gate -- projected from the block input and applied after local context enhancement (LCE) but prior to the output projection~$W_O$ -- to alleviate the low-rank $W_V \!\cdot\! W_O$ bottleneck and enable input-dependent suppression of attended outputs.   We assess both variants on Mini-ImageNet ($224{\times}224$, 100 classes) and CIFAR-10 ($32{\times}32$, 10 classes) under identical training protocols, utilizing a single GPU due to resource limitations. At ${\approx}77$--$79$\,M parameters, Gated-SwinRMT-SWAT achieves $80.22\%$ and Gated-SwinRMT-Retention $78.20\%$ top-1 test accuracy on Mini-ImageNet, compared with $73.74\%$ for the RMT baseline. On CIFAR-10 -- where small feature maps cause the adaptive windowing mechanism to collapse attention to global scope -- the accuracy advantage compresses from $+6.48$\,pp to $+0.56$\,pp.

---

## 论文详细总结（自动生成）

这是一份关于论文《Gated-SwinRMT: Unifying Swin Windowed Attention with Retentive Manhattan Decay via Input-Dependent Gating》的结构化深入总结：

### 1. 核心问题与研究动机
论文旨在解决视觉 Transformer（ViT）中的三个核心局限：
*   **计算效率与全局建模的矛盾**：标准 ViT 具有二次计算复杂度，而 Swin Transformer 虽然通过窗口化降低了复杂度，但缺乏显式的空间先验。
*   **空间先验缺失**：Swin 仅依靠位置编码，而 Retentive Networks (RMT) 引入的曼哈顿距离衰减（Manhattan Decay）能提供更强的局部性归纳偏置。
*   **窗口化 Softmax 的病态问题**：在小窗口内，Softmax 强制权重归一化，导致模型即使在无信息区域也必须分配注意力。论文试图通过引入**输入依赖的门控机制（Gating）**来允许模型抑制无信息的局部输出。

### 2. 方法论：Gated-SwinRMT
论文提出了两种将 Swin 的移位窗口机制与 RMT 的衰减机制结合的变体：

*   **核心思想**：在 Swin 的每个移位窗口内，将 2D 注意力分解为连续的宽度方向（Width-wise）和高度方向（Height-wise）的 1D 保留（Retention）操作，并应用基于曼哈顿距离的指数衰减掩码。
*   **关键技术细节**：
    *   **自适应窗口调整**：当特征图尺寸小于预设窗口时，自动切换为全局注意力，确保模型在不同分辨率下平滑过渡。
    *   **Gated-SwinRMT-SWAT 变体**：
        *   使用 **Sigmoid** 替代 Softmax，避免了概率分布的强制约束。
        *   引入**平衡 ALiBi 斜率**（正负斜率各半）和 **1-D RoPE**。
        *   在 Value 投影前使用 **SwiGLU** 门控，增强特征表达。
    *   **Gated-SwinRMT-Retention 变体**：
        *   保留 Softmax，但在 Log 空间添加加性衰减偏置。
        *   引入**显式 G1 门控**：在局部上下文增强（LCE）后、输出投影前应用 Sigmoid 门控，打破低秩瓶颈。
    *   **局部上下文增强 (LCE)**：通过 5x5 深度卷积（DWConv）补偿保留机制可能忽略的极细微局部结构。

### 3. 实验设计
*   **数据集**：
    *   **Mini-ImageNet**：224x224 分辨率，100 类，用于模拟标准窗口化工作模式。
    *   **CIFAR-10**：32x32 分辨率，用于测试“窗口失效/全局模式”下的性能。
*   **基准模型 (Benchmark)**：原始的 **RMT (Retentive Vision Transformer)**。
*   **对比实验**：在相同参数规模（约 77-79M）和训练协议下，对比 RMT、Retention 变体和 SWAT 变体。

### 4. 资源与算力
*   **硬件**：使用了 **1 台 NVIDIA H100 80GB GPU**。
*   **训练细节**：采用 bfloat16 混合精度训练。
*   **时长**：Mini-ImageNet 每轮（Epoch）约 240-300 秒，总计 40 轮；CIFAR-10 每轮约 10 秒，总计 50 轮。
*   **软件**：基于 PyTorch 和 `torch.cuda.amp`。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同分辨率的数据集上进行了对比，并提供了 5 轮/10 轮间隔的收敛曲线分析。
*   **消融实验**：通过对比 RMT -> Retention -> SWAT 的增量性能，间接分析了 DWConv、LCE、G1 门控和 Sigmoid 激活的贡献。
*   **充分性评价**：实验设计逻辑清晰，特别是通过 CIFAR-10 的“窗口坍缩”现象反向验证了门控机制在窗口化场景下的必要性。但由于资源限制，**缺乏在 ImageNet-1K 大规模数据集上的验证**，且消融实验是基于完整模型的对比，而非严格的单变量控制。

### 6. 主要结论与发现
*   **性能提升显著**：在 Mini-ImageNet 上，SWAT 变体比 RMT 基准高出 **6.48 个百分点**（80.22% vs 73.74%）。
*   **门控机制的价值**：在窗口化模式下，门控机制能有效抑制无信息窗口，缓解 Softmax 的局限性。
*   **收敛速度**：SWAT 变体由于使用了 Sigmoid 和 SwiGLU，在训练初期（前 5 轮）的收敛速度明显快于其他模型。
*   **泛化性**：提出的变体在验证集和测试集之间的差距更小，表现出更好的泛化能力。

### 7. 优点与亮点
*   **理论结合紧密**：成功将 NLP 中的 RetNet 思想（衰减、门控）与 CV 中的 Swin 窗口机制融合。
*   **自适应设计**：自适应窗口大小的设计解决了小尺寸特征图下的退化问题。
*   **分析深入**：对“为什么窗口化 Softmax 会导致性能下降”给出了直观的解释，并通过跨数据集对比进行了验证。

### 8. 不足与局限
*   **数据规模限制**：未在 ImageNet-1K 或更大型数据集上测试，结论的普适性有待进一步验证。
*   **下游任务缺失**：未在目标检测、语义分割等需要密集预测的下游任务中评估。
*   **计算开销**：虽然参数量增加不多（约 1.6%），但由于引入了 LCE 和额外的门控投影，每轮训练时间增加了约 20%-24%。
*   **过拟合风险**：SWAT 变体在训练集和验证集之间仍存在约 7% 的准确率差距，提示正则化策略仍有优化空间。

（完）
