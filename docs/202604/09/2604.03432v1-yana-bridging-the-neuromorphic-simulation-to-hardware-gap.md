---
title: "YANA: Bridging the Neuromorphic Simulation-to-Hardware Gap"
title_zh: YANA：弥合类脑计算仿真到硬件的鸿沟
authors: "Brian Pachideh, Sven Nitzsche, Moritz Neher, Jann Krausse, Carmen Weigelt, Klaus Knobloch, Victor Pazmino Betancourt, Juergen Becker"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03432v1"
tags: ["query:snn"]
score: 10.0
evidence: 基于FPGA的数字SNN加速器，弥合仿真与硬件差距
tldr: 脉冲神经网络（SNN）在处理稀疏数据时具有低功耗优势，但硬件匮乏导致仿真与硬件部署之间存在鸿沟。本文提出YANA，一个基于FPGA的开源数字SNN加速器。它采用五级事件驱动流水线，支持任意拓扑并充分利用时空稀疏性。通过在AMD Kria平台上的验证，YANA实现了推理时间随稀疏度的近线性缩放，为SNN的训练、优化和部署提供了端到端开源解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03432v1/fig-001.webp\", \"caption\": \"Fig. 3. Overview of the YANA software framework\", \"page\": 8, \"index\": 1, \"width\": 627, \"height\": 195}, {\"url\": \"assets/figures/arxiv/2604.03432v1/fig-002.webp\", \"caption\": \"Fig. 1. High-level block diagram of the YANA core.\", \"page\": 4, \"index\": 2, \"width\": 686, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2604.03432v1/fig-003.webp\", \"caption\": \"Fig. 4. Scaling of inference time of SNNs with different spatial and temporal sparsity levels after deployment on YANA. In the case of sweeping Sspat, Stemp cannot be fixed and has to be given in a small range, since the pruning level influences hidden layer sparsity, ultimately changing the total Stemp.\", \"page\": 10, \"index\": 3, \"width\": 674, \"height\": 244}]"
motivation: 针对神经形态硬件稀缺导致的仿真与硬件部署脱节问题，旨在提供一个易于获取且高效的硬件软件框架以促进算法创新。
method: 设计并实现了一个基于FPGA的五级事件驱动处理流水线，利用查找表进行高效神经元更新，并支持通过点对点连接实现任意SNN拓扑。
result: 在SHD数据集上展示了推理时间随空间和时间稀疏度的近线性缩放，且在极低资源占用的情况下支持多达2^17个突触和2^10个神经元。
conclusion: YANA作为一个集成了神经形态中间表示（NIR）的开源框架，成功弥合了SNN仿真与硬件之间的差距，为软硬件协同设计提供了有力工具。
---

## 摘要
脉冲神经网络 (SNN) 在严格功耗约束下实时处理时间稀疏数据流的应用中，相比传统人工神经网络 (ANN) 具有显著优势，这一概念被称为“类脑优势” (Neuromorphic Advantage)。然而，类脑硬件的有限可用性造成了巨大的仿真到硬件鸿沟，阻碍了算法创新、软硬件协同设计以及成熟开源生态系统的发展。为应对这一挑战，我们推出了 YANA (Yet Another Neuromorphic Accelerator)，这是一款基于 FPGA 的数字 SNN 加速器，旨在通过提供易于获取的类脑计算软硬件框架来弥合这一鸿沟。YANA 实现了五级事件驱动处理流水线，在通过点对点神经元连接支持任意 SNN 拓扑的同时，充分利用了时间和空间稀疏性。该架构采用了一种输入预处理方案，可在无缓冲区溢出风险的情况下保持每周期处理一个事件的稳定速度，并利用查找表进行泄漏计算，实现了硬件高效的事件驱动神经元更新。我们通过在 Spiking Heidelberg Digits 数据集上的实验展示了 YANA 的稀疏性利用能力，结果表明推理时间随空间和时间稀疏度水平呈近线性缩放。在易于获取的 AMD Kria KR260 平台上部署时，单个 YANA 核心使用 740 个 LUT、918 个寄存器、7 个 BRAM 和 24 个 URAM，支持多达 $2^{17}$ 个突触和 $2^{10}$ 个神经元。我们将 YANA 框架作为开源项目发布，提供了一个用于训练、优化和部署 SNN 的端到端解决方案，并通过类脑中间表示 (NIR) 与现有的类脑计算工具集成。

## Abstract
Spiking Neural Networks (SNNs) promise significant advantages over conventional Artificial Neural Networks (ANNs) for applications requiring real-time processing of temporally sparse data streams under strict power constraints -- a concept known as the Neuromorphic Advantage. However, the limited availability of neuromorphic hardware creates a substantial simulation-to-hardware gap that impedes algorithmic innovation, hardware-software co-design, and the development of mature open-source ecosystems. To address this challenge, we introduce Yet Another Neuromorphic Accelerator (YANA), an FPGA-based digital SNN accelerator designed to bridge this gap by providing an accessible hardware and software framework for neuromorphic computing. YANA implements a five-stage, event-driven processing pipeline that fully exploits temporal and spatial sparsity while supporting arbitrary SNN topologies through point-to-point neuron connections. The architecture features an input preprocessing scheme that maintains steady event processing at one event per cycle without buffer overflow risks, and implements hardware-efficient event-driven neuron updates using lookup tables for leak calculations. We demonstrate YANA's sparsity exploitation capabilities through experiments on the Spiking Heidelberg Digits dataset, showing near-linear scaling of inference time with both spatial and temporal sparsity levels. Deployed on the accessible AMD Kria KR260 platform, a single YANA core utilizes 740 LUTs, 918 registers, 7 BRAMS and 24 URAMs, supporting up to $2^{17}$ synapses and $2^{10}$ neurons. We release the YANA framework as an open-source project, providing an end-to-end solution for training, optimizing, and deploying SNNs that integrates with existing neuromorphic computing tools through the Neuromorphic Intermediate Representation (NIR).

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **YANA (Yet Another Neuromorphic Accelerator)** 的开源框架，旨在解决脉冲神经网络（SNN）在学术研究与实际硬件部署之间的脱节问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管 SNN 在理论上具有低功耗和实时处理稀疏数据的“类脑优势”，但目前存在严重的**“仿真到硬件鸿沟” (Simulation-to-Hardware Gap)**。
*   **研究动机**：
    *   现有的类脑硬件（如 Loihi）大多不可公开获取或编程受限。
    *   现有的 FPGA 加速器多为时钟驱动而非事件驱动，且往往仅支持顺序分层结构，无法处理复杂的递归拓扑。
    *   缺乏成熟的开源生态系统，导致算法创新难以在真实硬件上验证。

### 2. 方法论：核心思想与技术细节
YANA 是一个基于 FPGA 的数字 SNN 仿真器，其核心特征包括：
*   **五级事件驱动流水线**：
    1.  **输入级 (RX)**：接收外部或内部反馈事件。
    2.  **突触级 (Synapse)**：预处理输入事件，计算每个神经元的权重和，并识别“活跃神经元”（Hot Neurons）。
    3.  **神经元级 (Neuron)**：实现 Forward-Euler LIF 模型。采用**延迟更新机制**，仅在神经元接收到事件时才更新状态。
    4.  **轴突级 (Axon)**：根据映射表加载并发出脉冲。
    5.  **输出级 (TX)**：路由脉冲至内部反馈路径或外部互连。
*   **关键技术细节**：
    *   **LUT 泄漏计算**：为了避免在硬件中直接计算复杂的指数幂函数，使用查找表（LUT）预存泄漏项 $(1 - 1/\tau_{mem})^n$ 的解，实现高效的定点运算。
    *   **任意拓扑支持**：通过点对点连接（Point-to-Point）和独立的预/后突触存储结构，支持任意 SNN 图结构（包括递归连接）。
    *   **软件框架**：集成了 **YANA train**（基于 Norse 的硬件感知训练）、**YANA deploy**（利用 NIR 中间表示进行模型转换）和 **YANA runtime**（基于 PYNQ 的硬件控制）。

### 3. 实验设计
*   **数据集**：使用 **Spiking Heidelberg Digits (SHD)** 语音数据集。
*   **实验场景**：
    *   部署具有 100 个隐藏层神经元的 SNN。
    *   通过均匀丢弃事件来改变**时间稀疏度**。
    *   通过剪枝（Pruning）来改变**空间稀疏度**。
*   **Benchmark**：主要衡量**推理延迟（Inference Latency）**随稀疏度变化的缩放特性。
*   **对比对象**：虽然未直接与其他硬件进行横向跑分对比，但重点展示了其相对于传统时钟驱动架构在处理稀疏数据时的效率提升。

### 4. 资源与算力
*   **硬件平台**：AMD Kria KR260 机器人开发套件（低成本 SoC FPGA）。
*   **资源占用**（单个核心）：
    *   740 LUTs, 918 Registers。
    *   7 BRAMs, 24 URAMs（支持 $2^{17}$ 个突触和 $2^{10}$ 个神经元）。
*   **算力/训练时长**：文中未明确给出训练阶段的具体 GPU 型号或时长，但提到训练是基于 PyTorch Lightning 和 Norse 框架在主机端完成的。硬件运行频率为 **100 MHz**。

### 5. 实验数量与充分性
*   **实验规模**：针对不同的空间和时间稀疏度组合进行了多组实验，每组配置取 20 个样本的平均推理时间。
*   **充分性评价**：实验设计较为客观，清晰地展示了推理时间与稀疏度之间的**近线性缩放关系**。然而，目前的实验主要集中在推理延迟上，尚未涵盖多数据集的大规模准确率验证或多核心扩展性实验。

### 6. 主要结论与发现
*   **稀疏性即速度**：YANA 的推理时间随空间和时间稀疏度的增加而显著下降。在 SHD 任务中，推理时间从 16ms 缩短至 1ms，远快于实时处理要求。
*   **硬件效率**：通过延迟更新和 LUT 方案，YANA 在极低的 FPGA 资源占用下实现了复杂的 LIF 动力学模拟。
*   **开源价值**：通过支持 NIR（类脑中间表示），YANA 能够无缝对接现有的类脑软件生态。

### 7. 优点
*   **完全事件驱动**：不同于许多伪脉冲加速器，YANA 真正实现了按事件处理，空闲时无计算开销。
*   **灵活性高**：支持任意 SNN 拓扑，不局限于简单的多层感知机结构。
*   **易于获取**：基于廉价的 FPGA 开发板并完全开源，极大地降低了研究门槛。
*   **端到端工具链**：提供了从训练到部署的完整软件支持。

### 8. 不足与局限
*   **功耗验证缺失**：虽然理论上事件驱动更省电，但论文尚未给出在 FPGA 上的实际功耗测量数据。
*   **层类型支持有限**：目前主要支持全连接层，对卷积层（Weight Reuse）的支持尚在开发中。
*   **规模限制**：单核支持的神经元数量（1024个）对于大规模深度 SNN 来说较小，需进一步验证多核互连（NoC）的性能。
*   **应用场景单一**：目前的评估仅限于离线数据集处理，尚未展示在实时传感器（如 DVS 相机）上的闭环应用。

（完）
