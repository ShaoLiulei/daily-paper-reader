---
title: "LanteRn: Latent Visual Structured Reasoning"
title_zh: LanteRn：潜空间视觉结构化推理
authors: "André G. Viveiros, Nuno Gonçalves, Matthias Lindemann, André Martins"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25629v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大型多模态模型中的潜空间视觉推理
tldr: 针对当前大模型在视觉推理中过度依赖文本描述而缺乏细粒度空间理解的问题，本文提出 LanteRn 框架。该框架允许模型在推理过程中交替使用语言和紧凑的潜空间视觉表示，直接在潜空间进行视觉推理。通过监督微调和强化学习两阶段训练，LanteRn 在多个感知基准测试中显著提升了视觉定位和细粒度推理能力，为高效多模态推理提供了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25629v1/fig-001.webp\", \"caption\": \"Table 1: SFT evaluation results on V ⋆, Blink and a subset of VisCoT datasets. DA = Direct Attribution, RP = Relative Position, OL = Object Localization.\", \"page\": 7, \"index\": 1, \"width\": 827, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2603.25629v1/fig-002.webp\", \"caption\": \"Table 2: RL evaluation results on VStar, Blink and a subset of Viscot datasets. DA = Direct Attribution, RP = Relative Position, OL = Object Localization.\", \"page\": 8, \"index\": 2, \"width\": 827, \"height\": 191}]"
motivation: 现有大模型通常将视觉内容转化为文本进行推理，这限制了其对细粒度空间和视觉信息的理解效率与准确性。
method: 提出 LanteRn 框架，使模型能够生成并关注连续的视觉思维嵌入，通过监督微调和强化学习实现潜空间内的视觉推理。
result: "在 VisCoT、V* 和 Blink 等多个感知基准测试中，LanteRn 在视觉定位和细粒度推理方面均表现出显著且持续的改进。"
conclusion: 研究证明了利用内部潜空间表示进行多模态推理比直接在像素空间或纯文本空间推理更具效率和潜力。
---

## 摘要
尽管语言推理模型在许多任务中表现出色，但视觉推理对于当前的大型多模态模型（LMMs）而言仍具挑战性。因此，大多数 LMM 默认将感知内容转化为文本，这对于需要细粒度空间和视觉理解的任务来说是一个严重的局限。虽然最近的方法通过调用工具或生成中间图像在“以图思考”方面取得了进展，但它们要么依赖外部模块，要么因直接在像素空间进行推理而产生不必要的计算开销。在本文中，我们介绍了 LanteRn，这是一个使 LMM 能够将语言与紧凑的潜视觉表示交织在一起的框架，从而允许视觉推理直接在潜空间中进行。LanteRn 增强了视觉-语言 Transformer 的能力，使其在推理过程中能够生成并关注连续的视觉思维嵌入。我们分两个阶段训练该模型：首先通过监督微调将视觉特征锚定在潜状态中，随后通过强化学习使潜推理与任务级效用对齐。我们在三个以感知为中心的基准测试（VisCoT、V* 和 Blink）上评估了 LanteRn，观察到在视觉定位和细粒度推理方面有一致的提升。这些结果表明，内部潜表示为更高效的多模态推理提供了一个极具前景的方向。

## Abstract
While language reasoning models excel in many tasks, visual reasoning remains challenging for current large multimodal models (LMMs). As a result, most LMMs default to verbalizing perceptual content into text, a strong limitation for tasks requiring fine-grained spatial and visual understanding. While recent approaches take steps toward thinking with images by invoking tools or generating intermediate images, they either rely on external modules, or incur unnecessary computation by reasoning directly in pixel space. In this paper, we introduce LanteRn, a framework that enables LMMs to interleave language with compact latent visual representations, allowing visual reasoning to occur directly in latent space. LanteRn augments a vision-language transformer with the ability to generate and attend to continuous visual thought embeddings during inference. We train the model in two stages: supervised fine-tuning to ground visual features in latent states, followed by reinforcement learning to align latent reasoning with task-level utility. We evaluate LanteRn on three perception-centric benchmarks (VisCoT, V*, and Blink), observing consistent improvements in visual grounding and fine-grained reasoning. These results suggest that internal latent representations provide a promising direction for more efficient multimodal reasoning.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **LanteRn**（Latent Visual Structured Reasoning）的框架，旨在提升大型多模态模型（LMMs）的细粒度视觉推理能力。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的 LMM 在处理视觉任务时，通常先将视觉信息“语言化”（Verbalization），即用文字描述图像内容后再进行推理。这种方式被称为“思考图像”（Thinking about images），其局限性在于文字描述的带宽极低，难以捕捉细粒度的空间结构和复杂的视觉细节。
*   **研究背景**：现有的改进方案要么依赖外部工具（如调用检测器），要么在像素空间生成中间图像（计算开销巨大）。LanteRn 提出“用图像思考”（Thinking with images），即在模型的潜空间（Latent Space）中直接交织文本和视觉表示，实现更高效、更精准的推理。

### 2. 方法论
LanteRn 将推理过程建模为文本标记和连续潜向量的混合序列。
*   **核心思想**：在 Transformer 的推理链中插入“视觉思维”块。模型可以自主决定何时进入“潜空间推理模式”，输出一组连续的嵌入向量而非离散文本。
*   **关键技术细节**：
    *   **架构扩展**：基于 Qwen2.5-VL-3B，引入了三个特殊控制标记：`<|lvr_start|>`、`<|lvr_sep|>` 和 `<|lvr_end|>`。
    *   **潜空间模式**：当模型生成开始标记后，接下来的 $K$ 个步骤将跳过语言预测头，直接输出 Transformer 最后一层的隐藏状态（Hidden States）作为潜视觉表示。
*   **两阶段训练流程**：
    1.  **监督微调 (SFT)**：利用模型自身的视觉编码器作为“教师”。将图像特定区域的特征图经过池化后作为目标，训练 LLM 生成与之匹配的潜向量（使用 MSE 损失）。这实现了潜状态与视觉特征的初步锚定。
    2.  **强化学习 (RL)**：采用 **GRPO**（Group Relative Policy Optimization）算法。通过最终答案的正确性（奖励信号）来优化模型，使其生成的潜表示不再仅仅是视觉特征的重建，而是服务于任务解决的“有用思维”。为了稳定训练，引入了**潜状态重放（Latent State Replay）**机制。

### 3. 实验设计
*   **数据集**：
    *   **SFT 阶段**：基于 Visual-CoT 构建合成数据，利用 Qwen3-VL-235B 生成带有视觉定位（Bounding Boxes）的推理轨迹。
    *   **RL 阶段**：使用 VIRL-39k 数据集，包含多样的视觉推理问题。
*   **基准测试 (Benchmarks)**：
    *   **VisCoT**：评估多步推理能力。
    *   **V\***：评估视觉搜索和空间关系理解（包括直接属性 DA 和相对位置 RP 子集）。
    *   **Blink**：评估核心感知技能（如物体定位 OL 和相对位置 RP）。
*   **对比方法**：
    *   **Qwen2.5-VL-3B**：原始基座模型。
    *   **LantErn-NTP**：一个关键的基准线，它使用相同的推理链结构，但将中间过程全部设为普通文本标记（Next Token Prediction），用于证明“潜向量”本身而非仅仅是“推理步数”带来了提升。

### 4. 资源与算力
*   **模型规模**：主要在 3B 参数规模的模型上进行实验。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到了使用 AdamW 优化器、学习率为 $1 \times 10^{-5}$（SFT）和 $5 \times 10^{-6}$（RL），并使用了 TRL 库进行强化学习训练。

### 5. 实验数量与充分性
*   **实验规模**：针对不同潜向量长度 $K \in \{4, 8, 16, 32\}$ 做了消融实验。在三个主流视觉推理基准上进行了全面测试。
*   **充分性与公平性**：
    *   实验设计较为严谨，特别是引入了 NTP 基准线，排除了“推理链变长”带来的干扰变量。
    *   对比了 SFT 和 RL 两个阶段的效果，证明了 RL 在提升复杂关系推理（如 Blink RP 从 0.68 升至 0.81）中的关键作用。
    *   由于算力限制，未与 7B 以上的大模型直接对比，但在 3B 规模下展现了极具竞争力的性能。

### 6. 主要结论与发现
*   **潜空间推理有效性**：LanteRn 在感知密集型任务上显著优于纯文本推理模型。
*   **RL 的转变作用**：SFT 阶段主要让模型学会“重建”视觉特征，而 RL 阶段则促使模型学会“利用”这些特征进行逻辑推导，显著提升了处理空间关系的能力。
*   **容量权衡**：潜向量长度 $K$ 并非越大越好，存在一个性能拐点（实验中 $K=8$ 表现较稳健），过大的 $K$ 可能导致推理效率下降或性能退化。

### 7. 优点
*   **效率高**：避免了昂贵的像素级图像生成，直接在特征空间操作。
*   **端到端**：不需要依赖外部插件或手工设计的工具调用，推理过程更加内化。
*   **训练策略科学**：结合了“视觉特征蒸馏（SFT）”和“任务效用对齐（RL）”，解决了潜表示缺乏直观监督信号的问题。

### 8. 不足与局限
*   **固定容量限制**：目前使用固定的潜向量长度 $K$，无法根据任务难度动态调整推理深度。
*   **领域局限性**：训练数据主要集中在特定的视觉推理领域，在更广泛的多模态任务上的泛化性有待验证。
*   **可解释性挑战**：潜空间中的“视觉思维”对人类来说是不可读的，难以像文本 Chain-of-Thought 那样进行直观的错误分析。

（完）
