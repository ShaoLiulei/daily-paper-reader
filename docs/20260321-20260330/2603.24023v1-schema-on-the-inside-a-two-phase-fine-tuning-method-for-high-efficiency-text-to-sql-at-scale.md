---
title: "Schema on the Inside: A Two-Phase Fine-Tuning Method for High-Efficiency Text-to-SQL at Scale"
title_zh: 模式内化：一种用于大规模高效 Text-to-SQL 的两阶段微调方法
authors: "Chinmay Soni, Shivam Chourasia, Gaurav Kumar, Hitesh Kapoor"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24023v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于Text-to-SQL的两阶段微调
tldr: "针对大规模Text-to-SQL应用中API调用成本高、延迟大的挑战，本文提出了一种创新的两阶段监督微调方法，使8B参数的本地模型能够内化完整的数据库模式。该方法在拥有2.5亿用户的板球统计平台CriQ中应用，将输入Token减少了99%以上，且在执行成功率和语义准确率上均优于Gemini Flash 2.0，为工业级Text-to-SQL应用提供了高效、低成本的路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24023v1/fig-001.webp\", \"caption\": \"Figure 1: Conceptual overview of the conversational AI feature. A user asks a question in natural language, and the system returns a data-driven answer by generating and executing a SQL query.\", \"page\": 1, \"index\": 1, \"width\": 492, \"height\": 897}, {\"url\": \"assets/figures/arxiv/2603.24023v1/fig-002.webp\", \"caption\": \"Figure 5: Performance comparison of the final deployed model against the prompt-engineered Gemini baseline. The left chart shows our model achieved higher execution success (98.4% vs 95.6%) and semantic accuracy (92.5% vs 89.4%). The right chart illustrates the dramatic (> 99%) reduction in prompt token length.\", \"page\": 6, \"index\": 2, \"width\": 500, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2603.24023v1/fig-003.webp\", \"caption\": \"Figure 6: Execution and semantic accuracy for different Qwen 3 model sizes fine-tuned with our two-phase methodology. The 8B parameter model shows the highest accuracy, justifying its selection.\", \"page\": 6, \"index\": 3, \"width\": 500, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2603.24023v1/fig-004.webp\", \"caption\": \"Figure 2: A visual comparison of the two paradigms : Left side : “The External API Paradigm”, where a large prompt is sent to a third-party, pay-per-token service. Right side : “The Internalized Paradigm”, showing a small prompt with just the question going to a fine-tuned, selfhosted model with the schema “baked in”.\", \"page\": 2, \"index\": 4, \"width\": 412, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2603.24023v1/fig-005.webp\", \"caption\": \"Figure 3: The iterative process for generating the groundtruth dataset. User queries are processed by a self-hosted reasoning LLM to generate SQL. The query is executed, and if successful, the question-SQL pair is stored. If it fails, the prompt is updated with the error, and the process is retried up to a maximum number of attempts.\", \"page\": 4, \"index\": 5, \"width\": 500, \"height\": 773}, {\"url\": \"assets/figures/arxiv/2603.24023v1/fig-006.webp\", \"caption\": \"Figure 4: An overview of the two-phase fine-tuning methodology. Phase 1 combines full-context examples with explicit schema memorization tasks to create a schema-aware LoRA adapter. Phase 2 continues training using this adapter but with a minimal prompt, forcing the model to rely on its internalized knowledge of the schema.\", \"page\": 5, \"index\": 6, \"width\": 1052, \"height\": 264}]"
motivation: 旨在解决依赖大型API模型处理复杂数据库模式时，因冗长Prompt导致的极高成本和响应延迟问题。
method: 通过两阶段监督微调使模型内化数据库模式，从而在推理时无需在Prompt中提供详细的模式信息。
result: "输入Token从1.7万个骤减至不足100个，同时实现了98.4%的执行成功率和92.5%的语义准确率，性能超越了Gemini Flash 2.0。"
conclusion: 研究证明了利用领域专用的自托管模型内化模式，是实现大规模、高性能且低成本Text-to-SQL生产部署的可行方案。
---

## 摘要
将大型专有 API 语言模型应用于 Text-to-SQL 任务面临着重大的行业挑战：依赖包含大量模式（schema）的冗长提示词会导致极高的单 token API 成本和高延迟，从而阻碍大规模生产部署。我们展示了一个专门的、自托管的 8B 参数模型，该模型专为 CriQ 中的对话机器人设计。CriQ 是印度最大的幻想体育平台 Dream11（拥有超过 2.5 亿用户）的姐妹应用，用于回答用户关于板球统计数据的查询。我们提出了一种新颖的两阶段有监督微调方法，使模型能够内化整个数据库模式，从而消除了对长上下文提示词的需求。这将输入 token 减少了 99% 以上，从 17k token 的基准减少到不足 100 个，并用高效的本地推理取代了昂贵的外部 API 调用。该系统实现了 98.4% 的执行成功率和 92.5% 的语义准确率，显著优于使用 Google Gemini Flash 2.0 的提示工程基准（95.6% 执行成功率，89.4% 语义准确率）。这些结果展示了在大型生产环境中使用领域专门化、自托管语言模型实现高精度、低延迟 Text-to-SQL 应用的切实可行路径。

## Abstract
Applying large, proprietary API-based language models to text-to-SQL tasks poses a significant industry challenge: reliance on massive, schema-heavy prompts results in prohibitive per-token API costs and high latency, hindering scalable production deployment. We present a specialized, self-hosted 8B-parameter model designed for a conversational bot in CriQ, a sister app to Dream11, India's largest fantasy sports platform with over 250 million users, that answers user queries about cricket statistics. Our novel two-phase supervised fine-tuning approach enables the model to internalize the entire database schema, eliminating the need for long-context prompts. This reduces input tokens by over 99%, from a 17k-token baseline to fewer than 100, and replaces costly external API calls with efficient local inference. The resulting system achieves 98.4% execution success and 92.5% semantic accuracy, substantially outperforming a prompt-engineered baseline using Google's Gemini Flash 2.0 (95.6% execution, 89.4% semantic accuracy). These results demonstrate a practical path toward high-precision, low-latency text-to-SQL applications using domain-specialized, self-hosted language models in large-scale production environments.

---

## 论文详细总结（自动生成）

以下是对论文《Schema on the Inside: A Two-Phase Fine-Tuning Method for High-Efficiency Text-to-SQL at Scale》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：在工业级 Text-to-SQL 应用中，主流做法是调用大型专有 API（如 Gemini、GPT-4）。为了让模型理解数据库，必须在 Prompt 中加入完整的数据库模式（Schema），这导致 Prompt 极其冗长（本案例中达 17,000 tokens）。
*   **核心痛点**：
    1.  **高昂成本**：API 按 token 计费，长 Prompt 导致运营成本不可控。
    2.  **高延迟**：处理长上下文会显著增加推理时间，影响用户体验。
    3.  **性能瓶颈**：通用模型在处理特定领域（如复杂的板球统计数据）时，即便有长 Prompt，准确率仍难以达到生产要求。
*   **研究目的**：开发一种能够“内化”数据库模式的本地化小参数模型（8B），在不提供 Schema 的情况下实现高精度、低延迟的 SQL 生成。

### 2. 方法论
核心思想是**模式内化（Schema Internalization）**，通过两阶段监督微调（SFT）将数据库结构编码进模型参数。
*   **数据准备**：使用高性能推理模型（Qwen/QwQ-32B）作为教师模型，通过“生成-执行-报错-自纠错”的闭环迭代，生成高质量的“问题-SQL”对。
*   **两阶段微调流程**：
    *   **第一阶段：模式学习与关联（Schema Learning）**。采用混合任务训练：
        *   *SQL 生成任务*（8万条）：提供完整 Schema 上下文，让模型学习 SQL 逻辑和领域知识。
        *   *显式记忆任务*（1.5万条）：输入数据库 ID，要求模型完整输出整个 Schema 文本，强迫模型将模式编码进权重。
    *   **第二阶段：模式召回与应用（Schema Recall）**。
        *   使用第一阶段的 LoRA 权重继续训练（15万条数据）。
        *   **关键变化**：移除 Prompt 中的 Schema，仅提供“用户问题 + 数据库 ID”。迫使模型在生成 SQL 时完全依赖其内部记忆的模式信息。

### 3. 实验设计
*   **应用场景**：CriQ（Dream11 姐妹应用）的板球统计对话机器人。
*   **数据集**：
    *   训练集：基于 1.1 万个真实用户查询扩展而来的合成数据集。
    *   测试集：3 万个真实世界用户查询（Holdout set）。
*   **Benchmark 与对比方法**：
    *   **基准 1**：初始原型（Gemini 2.0 Flash + 10k token Prompt）。
    *   **基准 2**：深度提示工程后的 Gemini 2.0 Flash（17k token Prompt，包含复杂约束和示例）。
    *   **对比模型**：LLaMA 3 8B、CodeLLAMA 7B、Qwen 2.5 7B、Qwen 3 8B。

### 4. 资源与算力
*   **硬件环境**：使用了一个包含 **8x NVIDIA H100 80GB GPU** 的集群。
*   **训练细节**：
    *   采用分布式数据并行（DDP）加速。
    *   使用 LoRA 技术（r=64, alpha=128）。
    *   第一阶段上下文长度设为 20,480，第二阶段缩减至 1,024。
*   **推理部署**：使用 vLLM 框架和 Paged-Attention 技术进行自托管服务。

### 5. 实验数量与充分性
*   **实验规模**：进行了多模型横向对比、不同参数规模（0.5B 到 8B）的纵向对比，以及针对 3 万条真实数据的大规模验证。
*   **消融实验**：
    1.  **两阶段必要性**：对比单阶段训练（直接用简短 Prompt 训练），证明了先进行全上下文预热的必要性（准确率从 74.96% 提升至 98.4%）。
    2.  **显式记忆任务**：证明了专门训练模型“背诵”Schema 能显著提升召回稳定性。
*   **评价**：实验设计非常贴近工业实战，样本量大且覆盖了消融验证，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能反超**：经过两阶段微调的 8B 本地模型，在执行成功率（98.4%）和语义准确率（92.5%）上均**优于**拥有 17k token 提示词的 Gemini Flash 2.0。
*   **效率飞跃**：输入 Token 数量从 17,000+ 骤降至不足 100，**降幅超过 99%**。
*   **规模效应**：在 Text-to-SQL 任务中，8B 规模的模型是实现模式内化的“甜点位”，更小的模型（如 0.5B/1.5B）在内化复杂模式时准确率下降明显。

### 7. 优点
*   **极高的实用价值**：为企业摆脱昂贵的 API 依赖、实现数据隐私保护和极低延迟提供了清晰的路线图。
*   **创新的训练策略**：通过“显式记忆任务”解决模型在长程依赖下的幻觉问题，这一思路可推广至其他需要内化长文档的场景。
*   **成本结构转型**：将变动的 API 流量成本转化为固定的基础设施成本，适合高并发的大规模生产环境。

### 8. 不足与局限
*   **模式静态性限制**：该方法假设数据库 Schema 是相对静态的。如果数据库频繁增删表或字段，需要重新触发微调流程，维护成本较高。
*   **领域泛化性**：论文主要针对板球统计这一垂直领域，未讨论在多数据库、跨领域动态切换场景下的表现。
*   **训练资源门槛**：虽然推理成本低，但前期需要 H100 集群进行大规模合成数据生成和微调，对小型开发者仍有门槛。

（完）
