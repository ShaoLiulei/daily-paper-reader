---
title: Aggregation Alignment for Federated Learning with Mixture-of-Experts under Data Heterogeneity
title_zh: 数据异构下基于混合专家模型的联邦学习聚合对齐
authors: "Zihan Fang, Qianru Wang, Haonan An, Zheng Lin, Yiqin Deng, Xianhao Chen, Yuguang Fang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21276v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 混合专家大语言模型的联邦学习
tldr: 针对联邦学习中微调混合专家模型（MoE）面临的数据异构性挑战，本文提出了FedAlign-MoE框架。该框架通过路由一致性加权和分布正则化来对齐各客户端的门控网络行为，并利用语义一致性量化实现专家的选择性聚合。实验证明，该方法有效解决了门控网络失效和专家语义模糊问题，在非独立同分布环境下显著提升了模型的收敛速度和准确率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21276v1/fig-001.webp\", \"caption\": \"Fig. 1: Federated MoE-based LLM fine-tuning workflow.\", \"page\": 1, \"index\": 1, \"width\": 538, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2603.21276v1/fig-002.webp\", \"caption\": \"Table II: The test accuracy on the AGNews and MMLU datasets under non-IID data distributions using Switch-base-16 and DeepSeek-MoE-16B models.\", \"page\": 11, \"index\": 2, \"width\": 848, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2603.21276v1/fig-003.webp\", \"caption\": \"Fig. 7: The semantic-aware expert aggregation mechanism, which selectively aggregates experts based on semantic consistency, with adaptive thresholding to dynamically calibrate alignment sensitivity.\", \"page\": 7, \"index\": 3, \"width\": 535, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2603.21276v1/fig-004.webp\", \"caption\": \"Fig. 14: Semantic-aware expert aggregation on AGNews and MMLU dataset using Switch-base-16 (Fig. 14(a)-(b)) and DeepSeek-MoE-16B (Fig. 14(c)-(d)).\", \"page\": 12, \"index\": 4, \"width\": 529, \"height\": 359}]"
motivation: 数据异构导致联邦学习中MoE模型的门控网络难以统一，且同索引专家在不同客户端间产生了语义角色偏移。
method: 提出FedAlign-MoE框架，通过一致性加权对齐路由分布，并基于语义一致性量化进行专家的选择性聚合。
result: 实验表明，FedAlign-MoE在非IID联邦环境下比现有基准方法具有更快的收敛速度和更高的模型准确率。
conclusion: 通过联合对齐路由行为与专家语义，FedAlign-MoE有效解决了异构数据下MoE模型的聚合挑战。
---

## 摘要
大语言模型（LLMs）越来越多地采用混合专家（MoE）架构，以在减少计算量的同时扩展模型容量。微调这些基于 MoE 的 LLM 通常需要访问分布式的隐私敏感数据，这使得集中式微调变得不切实际。因此，联邦学习（FL）提供了一种协同微调基于 MoE 的 LLM 的范式，使每个客户端能够在不损害数据隐私的情况下整合多样化的知识。然而，由于客户端之间固有的数据异构性，将基于 MoE 的 LLM 微调集成到 FL 中面临两个关键的聚合挑战：(i) 不同的本地数据分布促使客户端为本地化的专家选择形成独特的门控偏好，导致直接的参数聚合产生一个“无一适用”的全局门控网络；(ii) 相同索引的专家在不同客户端之间形成了迥异的语义角色，导致专家语义模糊和专家专业化程度下降。为了应对这些挑战，我们提出了 FedAlign-MoE，这是一个联合强制执行路由一致性和专家语义对齐的联邦聚合对齐框架。具体而言，FedAlign-MoE 通过一致性加权对齐路由分布来聚合门控行为，并通过分布正则化优化本地门控网络，在不覆盖具有辨别性的本地偏好的情况下保持跨客户端的稳定性。同时，FedAlign-MoE 显式地量化了跨客户端相同索引专家之间的语义一致性，并选择性地聚合来自语义对齐客户端的更新，确保全局专家具有稳定且专业化的功能角色。广泛的实验表明，FedAlign-MoE 优于最先进的基准方法，在非独立同分布（non-IID）联邦环境中实现了更快的收敛速度和更高的准确率。

## Abstract
Large language models (LLMs) increasingly adopt Mixture-of-Experts (MoE) architectures to scale model capacity while reducing computation. Fine-tuning these MoE-based LLMs often requires access to distributed and privacy-sensitive data, making centralized fine-tuning impractical. Federated learning (FL) therefore provides a paradigm to collaboratively fine-tune MoE-based LLMs, enabling each client to integrate diverse knowledge without compromising data privacy. However, the integration of MoE-based LLM fine-tuning into FL encounters two critical aggregation challenges due to inherent data heterogeneity across clients: (i) divergent local data distributions drive clients to develop distinct gating preference for localized expert selection, causing direct parameter aggregation to produce a ``one-size-fits-none'' global gating network, and (ii) same-indexed experts develop disparate semantic roles across clients, leading to expert semantic blurring and the degradation of expert specialization. To address these challenges, we propose FedAlign-MoE, a federated aggregation alignment framework that jointly enforces routing consistency and expert semantic alignment. Specifically, FedAlign-MoE aggregates gating behaviors by aligning routing distributions through consistency weighting and optimizes local gating networks through distribution regularization, maintaining cross-client stability without overriding discriminative local preferences. Meanwhile, FedAlign-MoE explicitly quantifies semantic consistency among same-indexed experts across clients and selectively aggregates updates from semantically aligned clients, ensuring stable and specialized functional roles for global experts. Extensive experiments demonstrate that FedAlign-MoE outperforms state-of-the-art benchmarks, achieving faster convergence and superior accuracy in non-IID federated environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Aggregation Alignment for Federated Learning with Mixture-of-Experts under Data Heterogeneity》的结构化深度总结：

### 1. 核心问题与研究背景
*   **研究动机**：随着大语言模型（LLM）转向混合专家（MoE）架构以实现高效扩展，如何在保护隐私的联邦学习（FL）框架下微调这些模型成为关键。
*   **核心挑战**：由于客户端数据存在严重的**异构性（Non-IID）**，直接应用传统的联邦聚合方法面临两大困境：
    1.  **门控偏好错位**：不同客户端对专家的选择偏好迥异，直接聚合门控参数会产生一个对任何客户端都不适用的“平庸”全局门控。
    2.  **专家语义发散**：相同索引的专家在不同客户端上处理的语义区域不同，聚合会导致专家功能模糊，破坏其专业化特性。

### 2. 方法论：FedAlign-MoE 框架
该论文提出了 **FedAlign-MoE**，核心思想是从“参数空间聚合”转向“功能与语义空间对齐”。

*   **基于一致性的门控分布对齐**：
    *   **行为对齐而非参数聚合**：服务器不直接平均门控参数，而是收集客户端的平均路由分布。
    *   **路由一致性加权**：引入“专家重叠度”（外部一致性）和“路由决策边际”（内部置信度）来计算聚合权重，确保全局参考分布能反映共性规律。
    *   **分布正则化**：客户端在本地微调时，通过 KL 散度约束本地门控输出靠近全局参考分布，同时利用自适应权重保护本地特有的专家偏好。
*   **语义感知专家聚合**：
    *   **语义量化**：利用“输入空间分配”（专家处理的隐藏状态均值）和“权重更新方向”（余弦相似度）来定义专家的语义角色。
    *   **区域条件聚合**：只有当两个客户端的同索引专家在语义区域和更新方向上高度一致时，才进行聚合。
    *   **自适应阈值**：根据专家语义的统计离散程度动态调整对齐敏感度，防止语义模糊。

### 3. 实验设计
*   **数据集**：涵盖了分类与推理任务，包括 **AGNews**（新闻分类）、**PIQA**（常识推理）、**HellaSwag**（上下文预测）和 **MMLU**（综合多任务理解）。
*   **模型架构**：采用了 **Switch-base-16**（Top-1 路由）和 **DeepSeek-MoE-16B**（细粒度 Top-2 路由）。
*   **基准方法（Benchmarks）**：
    *   通用 FL 方法：**FedAvg**, **FedProx**。
    *   针对 MoE 的 FL 方法：**PFL-MoE**, **FedMoE**。
*   **数据划分**：使用 Dirichlet 分布（$\alpha=0.1$）模拟高度非独立同分布（Non-IID）的场景。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **NVIDIA GeForce RTX 4090 GPU** 的服务器上运行。
*   **训练细节**：
    *   设置了 10 个同步通信的客户端。
    *   针对 16B 参数的 DeepSeek-MoE，使用了 **8-rank QLoRA** 技术以实现高效微调。
    *   总计进行 25 轮通信，每轮本地训练 1 个 Epoch。
    *   学习率设为 $1 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的对比，包括 4 个数据集在 IID 和 Non-IID 下的性能测试。
*   **消融实验**：分别验证了“门控分布对齐”和“语义感知聚合”中各组件（如一致性加权、自适应阈值等）的有效性。
*   **鲁棒性与扩展性**：测试了不同异构程度（$\alpha$ 从 0.1 到 1.0）以及不同客户端数量（10 到 50 个）对模型性能的影响。
*   **评价**：实验设计全面，涵盖了从模型精度到收敛速度的指标，对比实验选取了最新的 MoE 联邦学习方案，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能提升**：在 Non-IID 环境下，FedAlign-MoE 的准确率比 FedAvg 提升了约 7%，比专门的 FedMoE 提升了约 4%。
*   **收敛加速**：相比基准方法，该框架在 Switch-base-16 上实现了 1.3 倍、在 DeepSeek-MoE 上实现了 1.7 倍的收敛加速。
*   **专家专业化**：通过语义对齐，成功保持了全局专家的功能锐度，避免了因异构数据导致的“语义模糊”现象。

### 7. 优点与亮点
*   **创新视角**：跳出了传统的参数平均逻辑，通过路由分布和语义指标在功能层面解决 MoE 的聚合难题。
*   **自适应性强**：引入的自适应阈值和权重机制，使其能在保持全局一致性的同时，容忍并利用客户端的本地特性。
*   **高效性**：结合 QLoRA 和轻量级统计量传输，使其在大规模 MoE 模型（如 16B）上具有实际可操作性。

### 8. 不足与局限
*   **超参数依赖**：框架引入了 $\lambda$（正则化系数）、$\eta$（重叠阈值）等超参数，虽然文中做了敏感性分析，但在实际复杂场景下可能需要较多调优工作。
*   **通信开销**：虽然传输的是统计量而非全量参数，但相比 FedAvg，每个专家额外的语义指标和分布数据仍会增加一定的通信负担。
*   **应用限制**：目前主要验证了自然语言处理任务，在多模态或视觉 MoE 模型上的泛化能力尚待进一步验证。

（完）
