---
title: "CurvZO: Adaptive Curvature-Guided Sparse Zeroth-Order Optimization for Efficient LLM Fine-Tuning"
title_zh: CurvZO：用于高效大语言模型微调的自适应曲率引导稀疏零阶优化
authors: "Shuo Wang, Ziyu Chen, Ming Tang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21725v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 利用零阶优化进行高效大模型微调
tldr: 针对大语言模型微调中反向传播内存开销巨大的挑战，本文提出CurvZO。该方法利用零阶优化的标量反馈在线追踪曲率信号，并据此构建参数采样分布以实现稀疏更新。通过动态调整扰动预算，CurvZO在保持内存效率的同时，显著降低了梯度估计方差，提升了微调性能和收敛速度，为资源受限环境下的模型优化提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21725v1/fig-001.webp\", \"caption\": \"Figure 2. Accuracy (%) on fine-tuning Llama2-7B (top) and Llama2-13B (bottom) with 1,000 training samples.\", \"page\": 8, \"index\": 1, \"width\": 463, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2603.21725v1/fig-002.webp\", \"caption\": \"Table 1. Accuracy (%) on OPT-2.7B fine-tuning with 1,000 training samples. FT and LoRA are first-order (backpropagation-based) oracle baselines. Better results among MeZO, DiZO, and CurvZO are highlighted in bold.\", \"page\": 7, \"index\": 2, \"width\": 1017, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2603.21725v1/fig-003.webp\", \"caption\": \"Table 4. Fine-tuning results on OPT-2.7B across tasks. CurvZO combines curvature-guided sampling with adaptive budget selection. US+AB (Uniform Sampling + Adaptive Budget) uses the same adaptive budget selection as CurvZO, but samples parameters uniformly at random. CurvZO (B = ρd) uses curvature-guided sampling with a fixed perturbation budget B = ρd (e.g., ρ ∈ {0.1, 0.4, 0.7}). The best results in each block are highlighted.\", \"page\": 17, \"index\": 3, \"width\": 1017, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2603.21725v1/fig-004.webp\", \"caption\": \"Table 5. Hyperparameters used in our experiments. CurvZO and CurvZO+LoRA use the same settings as MeZO and MeZO+LoRA, respectively.\", \"page\": 17, \"index\": 4, \"width\": 492, \"height\": 308}]"
motivation: 传统的零阶优化在微调大模型时因梯度估计方差大而导致收敛缓慢，且难以在仅有标量反馈的情况下高效选择关键参数进行稀疏更新。
method: 提出一种自适应曲率引导的稀疏零阶优化算法，通过在线追踪曲率信号来构建参数采样分布，并动态调整扰动预算以平衡更新的专注度与探索性。
result: "在OPT和Llama模型上的实验显示，CurvZO相比现有零阶优化基线提升了高达4.4%的准确率，并实现了最高2倍的训练加速。"
conclusion: CurvZO通过曲率引导的自适应稀疏策略，在维持极低内存占用的前提下，显著增强了零阶优化在大语言模型微调任务中的有效性和效率。
---

## 摘要
使用反向传播微调大语言模型（LLMs）虽然能获得高性能，但会产生巨大的显存开销，限制了其在资源受限硬件上的可扩展性。零阶（ZO）优化仅依赖前向传播，提供了一种显存高效的替代方案，但由于梯度估计的高方差，通常面临收敛缓慢或不稳定的问题。稀疏零阶更新通过仅扰动参数子集在一定程度上缓解了这一问题，但其有效性取决于对信息丰富参数的选择，而这在零阶优化中具有挑战性，因为每次查询仅产生标量反馈。我们提出了自适应曲率引导的稀疏零阶优化（CurvZO），它从标量零阶反馈中在线跟踪曲率信号，并利用这些信号构建逐参数的采样分布，以便在每次更新时选择坐标，从而降低稀疏零阶梯度估计器的方差。此外，CurvZO 根据不断演化的曲率信号分布动态调整扰动预算，使稀疏零阶更新既能保持聚焦，又具备充分的探索性。在 OPT 和 Llama 上针对多种 NLP 任务进行的广泛实验表明，与零阶基准方法相比，CurvZO 持续提升了微调性能并缩短了训练时间。它在保持显存效率的同时，将准确率最高提升了 4.4 个百分点，并实现了高达 2 倍的加速。

## Abstract
Fine-tuning large language models (LLMs) with backpropagation achieves high performance but incurs substantial memory overhead, limiting scalability on resource-constrained hardware. Zeroth-order (ZO) optimization provides a memory-efficient alternative by relying solely on forward passes, yet it typically suffers from slow or unstable convergence due to high-variance gradient estimates. Sparse ZO updates partially address this issue by perturbing only a subset of parameters, but their effectiveness hinges on selecting informative parameters, which is challenging in ZO optimization because each query yields only scalar feedback. We propose \textbf{Adaptive Curvature-Guided Sparse Zeroth-Order Optimization (CurvZO)}, which tracks curvature signals online from scalar ZO feedback and leverages these signals to construct a parameter-wise sampling distribution for selecting coordinates at each update, reducing the variance of the sparse ZO gradient estimator. Moreover, CurvZO dynamically adapts the perturbation budget to the evolving curvature signal distribution, yielding sparse ZO updates that remain both focused and sufficiently exploratory. Extensive experiments on OPT and Llama across diverse NLP tasks show that CurvZO consistently improves fine-tuning performance and reduces training time over ZO baselines. It improves accuracy by up to 4.4 points and achieves up to a $2\times$ speedup, while preserving memory efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《CurvZO: Adaptive Curvature-Guided Sparse Zeroth-Order Optimization for Efficient LLM Fine-Tuning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）的全参数微调依赖反向传播，产生巨大的显存开销（激活值、梯度、优化器状态）。零阶（ZO）优化（如 MeZO）虽能显著降低显存，但由于梯度估计方差极高，导致收敛缓慢且不稳定。
*   **研究动机**：现有的稀疏零阶优化尝试通过仅扰动部分参数来降低方差，但如何选择“最有信息量”的参数子集是一个难题。现有方法要么依赖预计算的静态统计量（增加开销），要么随机选择（效率低下）。本文旨在设计一种**无需预计算、在线自适应**的机制，识别并优先扰动对损失函数最敏感（高曲率）的参数。

### 2. 方法论：CurvZO 核心思想与技术细节
CurvZO 提出了一套在线追踪曲率并引导稀疏采样的框架，包含以下关键技术：
*   **在线曲率信号追踪（Online Curvature Tracking）**：利用零阶优化的标量反馈 $\Delta$（两次前向传播的损失差）和扰动向量 $v$，定义曲率得分 $s_i = \Delta^2 v_i^2$。通过指数移动平均（EMA）和归一化处理，在训练过程中动态更新每个参数（或参数块）的曲率估计。
*   **曲率引导的采样规则**：根据曲率得分构建采样分布 $\pi$，证明了当采样概率 $\pi_i \propto \sqrt{s_i}$ 时，可以最小化梯度估计器的方差上限。
*   **偏差修正（Bias Correction）**：引入 Horvitz-Thompson 重写机制，对采样后的梯度估计进行逆概率加权，确保稀疏更新下的梯度估计依然是无偏的。
*   **自适应扰动预算（Adaptive Budget）**：引入“有效支撑大小”和“得分锐度（熵）”两个指标，动态调整每步扰动的参数数量。在训练初期（曲率分布平坦）增加预算以加强探索，在后期（曲率集中）减少预算以精细优化。
*   **块级实现（Block-wise Implementation）**：为降低计算和存储开销，将参数按张量或层进行分组（Blocks），在块级别应用上述逻辑。

### 3. 实验设计
*   **模型与任务**：
    *   **模型**：OPT 系列（2.7B, 6.7B）和 Llama2 系列（7B, 13B）。
    *   **任务**：SuperGLUE 评测集（RTE, CB, BoolQ, WiC, WSC）、SST-2 分类任务，以及 SQuAD、DROP 等生成式问答任务。
*   **Benchmark 与对比方法**：
    *   **零阶基线**：MeZO（全参数扰动）、DiZO（散度驱动优化）。
    *   **一阶基线（Oracle）**：全参数微调（FT）、LoRA。
    *   **设置**：对比了标准微调和结合 LoRA 的参数高效微调两种场景。

### 4. 资源与算力
*   **硬件型号**：实验在 **NVIDIA L40** 和 **A100** GPU 上完成。
*   **训练时长**：所有实验固定为 20,000 个更新步数。
*   **算力表现**：CurvZO 显著减少了达到相同精度所需的 GPU 小时数。例如，在 BoolQ 任务上比 MeZO 减少了 **59%** 的 GPU 耗时，在 RTE 上减少了 **51%**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 个模型家族、4 种模型尺寸、8 个 NLP 任务。
*   **消融实验**：详细对比了“均匀采样+自适应预算”、“曲率引导+固定预算”等变体，验证了曲率引导和自适应预算的协同作用。
*   **客观性**：所有实验均基于 3 次独立运行取平均值，并提供了收敛曲线对比。实验设计覆盖了分类与生成、全量与 LoRA 等多种维度，论证较为充分、公平。

### 6. 主要结论与发现
*   **性能提升**：CurvZO 在多个任务上持续优于 MeZO 和 DiZO，准确率最高提升 **4.4 个百分点**。
*   **收敛加速**：达到相同精度所需的迭代步数比 MeZO 少 **2.0x 到 2.4x**。
*   **显存效率**：在保持与 MeZO 相当的极低显存占用的同时（仅需存储额外的块级曲率得分，开销可忽略），实现了接近一阶微调的性能。
*   **扩展性**：在 Llama2-13B 等更大规模模型上依然保持领先优势。

### 7. 优点与亮点
*   **即插即用**：无需像 SensZOQ 那样预计算 Fisher 信息矩阵，完全在线运行，不改变现有零阶优化框架。
*   **理论支撑**：提供了严谨的收敛性证明，并推导出了方差最小化的最优采样分布。
*   **动态性**：自适应预算机制平衡了优化过程中的“专注（Exploitation）”与“探索（Exploration）”。

### 8. 不足与局限
*   **局部性限制**：曲率得分是基于坐标或块的对角近似，无法捕捉参数之间的耦合关系（非对角 Hessian 信息）。
*   **块粒度权衡**：虽然块级操作节省了显存，但可能损失了参数内部的细粒度敏感性信息。
*   **任务范围**：主要集中在 NLP 任务，尚未在多模态或强化学习等更广泛的零阶优化应用场景中验证。

（完）
