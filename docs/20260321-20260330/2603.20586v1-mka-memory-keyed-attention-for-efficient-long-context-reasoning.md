---
title: "MKA: Memory-Keyed Attention for Efficient Long-Context Reasoning"
title_zh: MKA：用于高效长上下文推理的记忆键控注意力机制
authors: "Dong Liu, Yanxuan Yu, Ben Lengerich, Ying Nian Wu"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20586v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于长文本语言建模的记忆键注意力机制
tldr: 针对长文本语言模型中KV缓存带来的存储与计算瓶颈，本文提出Memory-Keyed Attention (MKA)。该机制通过分层KV缓存（局部、会话、长期）和动态路由实现高效注意力。进一步提出的FastMKA变体通过融合内存源提升效率。实验表明，FastMKA在保持与MLA相当的困惑度时，训练吞吐量提升达5倍，推理延迟降低1.8倍，为长文本推理提供了高效且可扩展的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.20586v1/fig-001.webp\", \"caption\": \"Table 4: Prefill vs Decode latency breakdown on Qwen2.5-7B (batch=1, bf16). Prefill: total time to process input tokens. Decode: latency per generated token. Measurements use contiguous KV layout for ≤32K and paged layout for longer contexts.\", \"page\": 6, \"index\": 1, \"width\": 502, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2603.20586v1/fig-002.webp\", \"caption\": \"Table 6: Comparison across different model architectures on WikiText-2 (sequence length 32K, batch=2 for training, batch=1 for inference). Qwen2.5-14B experiments use 4 A800 GPUs with tensor parallelism (TP=4); all other models use a single A800 GPU. FastMKA maintains efficiency advantages across architectures.\", \"page\": 6, \"index\": 2, \"width\": 502, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2603.20586v1/fig-003.webp\", \"caption\": \"Table 11: Per-token computational cost breakdown across different model architectures (128K context, batch=1, bf16). FLOPs and memory access measured via Nsight Compute. FastMKA achieves speedup through algorithmic reduction of attention paths rather than implementation differences.\", \"page\": 8, \"index\": 3, \"width\": 502, \"height\": 252}]"
motivation: 随着长文本建模需求增加，维护大规模KV缓存的成本成为训练和推理的主要瓶颈。
method: 提出一种集成多级KV缓存的分层注意力机制MKA，并引入融合内存源的FastMKA变体以优化计算效率。
result: FastMKA在保持与MLA相当精度的前提下，实现了最高5倍的训练吞吐量提升和1.8倍的推理延迟降低。
conclusion: MKA是一个兼顾效率与性能的实用框架，为长文本注意力机制的优化提供了新方向。
---

## 摘要
随着长上下文语言建模变得日益重要，维护和处理大规模键/值（KV）缓存的成本迅速增长，成为训练和推理中的主要瓶颈。虽然先前的工作（如多查询注意力 MQA 和多潜变量注意力 MLA）通过共享或压缩 KV 特征来减少内存占用，但它们往往会牺牲表示质量或产生额外的运行时开销。我们提出了记忆键控注意力（Memory-Keyed Attention, MKA），这是一种分层注意力机制，它集成了多级 KV 缓存（局部、会话和长期），并学习在这些缓存之间动态路由注意力。我们进一步引入了路由融合 MKA（FastMKA），这是一种广播路由变体，在注意力计算之前融合记忆源以提高效率。在不同序列长度上的实验表明，FastMKA 实现了良好的准确率与效率权衡：在困惑度与 MLA 相当的情况下，实现了高达 5 倍的训练吞吐量提升和 1.8 倍的评估延迟降低。这些结果表明，MKA 是一个用于高效长上下文注意力的实用且可扩展的框架。

## Abstract
As long-context language modeling becomes increasingly important, the cost of maintaining and attending to large Key/Value (KV) caches grows rapidly, becoming a major bottleneck in both training and inference. While prior works such as Multi-Query Attention (MQA) and Multi-Latent Attention (MLA) reduce memory by sharing or compressing KV features, they often trade off representation quality or incur runtime overhead. We propose Memory-Keyed Attention (MKA), a hierarchical attention mechanism that integrates multi-level KV caches (local, session, and long-term) and learns to route attention across them dynamically. We further introduce Route-Fused MKA (FastMKA), a broadcast-routed variant that fuses memory sources before attention computation for improved efficiency. Experiments on different sequence lengths show that FastMKA achieves a favorable accuracy-efficiency trade-off: comparable perplexity to MLA while achieving up to 5x faster training throughput and 1.8x lower evaluation latency. These results highlight MKA as a practical and extensible framework for efficient long-context attention.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MKA (Memory-Keyed Attention)** 的新型注意力机制，旨在解决大语言模型（LLM）在处理长文本时 KV 缓存（Key/Value Cache）带来的内存与计算瓶颈。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **背景：** 现代 LLM 支持的上下文长度已扩展至 128K 甚至 1M 标记，但传统的自注意力机制在推理时需要存储和检索庞大的 KV 缓存，导致内存带宽受限和推理延迟激增。
*   **核心问题：** 现有的优化方案（如 MQA、GQA、MLA）虽然减少了 KV 缓存大小，但往往在表示精度（Representation Fidelity）上有所妥协，或者在处理超长文本时仍面临计算效率瓶颈。
*   **动机：** 模拟计算机系统的多级缓存架构，通过分层存储和动态路由，在不牺牲过多精度的前提下，大幅提升长文本处理的效率。

### 2. 提出的方法论
*   **分层记忆设计 (Hierarchical Memory)：**
    *   **L1 (局部记忆)：** 存储当前窗口内的原始标记，用于捕捉即时上下文。
    *   **L2 (会话记忆)：** 对历史前缀进行低秩摘要或门控汇总，捕捉中程语义。
    *   **L3 (长期记忆)：** 采用向量化哈希（Vectorized Hashing）和分块检索（Chunk-based Recall），从动态记忆库中提取相关历史信息。
*   **动态路由机制 (Dynamic Routing)：**
    *   为每个查询标记（Query Token）学习一个轻量级的路由门控 $\lambda \in \mathbb{R}^3$，动态决定对 L1、L2、L3 三层记忆的关注权重。
*   **FastMKA (路由融合变体)：**
    *   为了避免多次注意力计算的开销，FastMKA 在投影前对三层记忆进行“路由融合”（Route-Fusion），将多条注意力路径合并为一条，显著降低了计算复杂度和内核启动次数。
*   **技术细节：** 引入了递归 MKA 公式和分层 Max-Shift 技巧，确保在低精度（如 bf16）和超长序列下的数值稳定性；支持类似 FlashAttention 的分块执行和 GPU 内核融合。

### 3. 实验设计
*   **数据集：**
    *   **WikiText-2：** 用于评估基础语言建模能力（困惑度 PPL）。
    *   **LongBench：** 评估长文本理解任务（问答、摘要、代码）。
    *   **RULER：** 专门测试长距离检索能力（如 Passkey 检索）。
*   **对比基准 (Benchmarks)：**
    *   **MHA (多头注意力)：** 标准基准。
    *   **GQA (分组查询注意力)：** Llama 3 等模型采用的工业标准。
    *   **MLA (多潜变量注意力)：** DeepSeek-V2/V3 采用的先进压缩方案。
*   **模型架构：** 在 Qwen2.5 (7B/14B)、Llama 3.1-8B 和 DeepSeek-V3 上进行了验证。

### 4. 资源与算力
*   **硬件：** 使用了 NVIDIA A800 80GB GPU 集群。
    *   7B 模型（≤32K 序列）：单张 A800。
    *   14B 模型及超长序列（64K-256K）：4 到 8 张 A800，配合张量并行（TP=2-4）。
*   **训练细节：** 采用混合精度（bf16）和 FlashAttention-2。每个模型仅微调一个 Epoch，以对比在相同计算预算下的收敛速度和效率。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 4K 到 256K 的多种序列长度，测试了训练吞吐量、预填充延迟、解码延迟、内存带宽利用率等多个维度。
*   **消融实验：** 针对记忆层级（L1/L2/L3）的必要性、路由策略（软路由 vs 硬路由 vs 固定权重）进行了详细对比。
*   **充分性评价：** 实验设计较为全面，不仅对比了学术指标（PPL），还深入到了系统底层指标（Nsight Compute 分析），证明了提速并非来自简单的工程调优，而是算法级的路径削减。

### 6. 主要结论与发现
*   **效率飞跃：** 在 256K 上下文下，FastMKA 的训练吞吐量比 MLA 快 **5.01 倍**，解码延迟降低了 **1.86 倍**。
*   **精度保持：** 在 Qwen2.5-7B 上，FastMKA 以仅 1.2% 的困惑度微增为代价，换取了数倍的效率提升，且在 LongBench 等长文本任务上表现与 MLA 相当。
*   **内存优势：** 相比 MHA，KV 缓存内存占用减少了约 **66.8%**，且由于内存访问模式更连续，HBM 带宽利用率更高。

### 7. 优点
*   **实用性强：** FastMKA 作为一个“掉入式”（Drop-in）替代方案，能直接集成到现有的 Transformer 流水中。
*   **理论支撑：** 提出了递归注意力公式，证明了其在亚二次方复杂度下的可行性。
*   **硬件友好：** 专门针对 GPU 内核启动和内存对齐进行了优化，解决了长文本推理中常见的“小算子过多”问题。

### 8. 不足与局限
*   **精度微损：** 相比于 MLA，MKA 在某些任务上存在轻微的性能下降（PPL 略高），这可能是由于路由融合过程中的信息损失。
*   **微调依赖：** MKA 需要对预训练模型进行一定程度的微调才能发挥最佳效果，对于完全不具备微调条件的场景受限。
*   **L3 检索开销：** 虽然 L3 检索是亚线性的，但在极端超长序列下，哈希冲突和检索延迟的边际效应仍需进一步在大规模生产环境中验证。

（完）
