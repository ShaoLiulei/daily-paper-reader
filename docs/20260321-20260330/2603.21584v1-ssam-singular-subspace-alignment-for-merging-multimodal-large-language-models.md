---
title: "SSAM: Singular Subspace Alignment for Merging Multimodal Large Language Models"
title_zh: SSAM：用于合并多模态大语言模型的奇异子空间对齐
authors: "Md Kaykobad Reza, Ameya Patil, Edward Ayrapetian, M. Salman Asif"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21584v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 利用子空间对齐合并多模态大语言模型
tldr: 本研究针对多模态大模型（MLLM）训练成本高昂的问题，提出了一种名为 SSAM 的无训练模型合并框架。该方法旨在将多个独立训练的专家级 MLLM（如视觉-语言和音频-语言模型）统一为一个多模态模型。SSAM 通过识别语言相关参数更新的共享低秩子空间，并在该空间内进行对齐与合并，有效解决了参数干扰问题。实验表明，SSAM 在无需任何多模态训练数据的情况下，在多个基准测试中达到了 SOTA 性能，证明了参数空间对齐在模型扩展中的高效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21584v1/fig-001.webp\", \"caption\": \"Table 2: Performance comparison on MCUB benchmark [3]. SSAM achieves the best average performance across all modality combinations and consistently outperforms both training-free merging baselines and models finetuned on paired multimodal data.\", \"page\": 11, \"index\": 1, \"width\": 710, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2603.21584v1/fig-002.webp\", \"caption\": \"Table 1: Performance comparison on MUSIC-AVQA [17] and AVQA [45] datasets. SSAM achieves the best average performance across all modality combinations and consistently outperforms both training-free model merging baselines and models finetuned on paired multimodal data.\", \"page\": 10, \"index\": 2, \"width\": 719, \"height\": 483}, {\"url\": \"assets/figures/arxiv/2603.21584v1/fig-003.webp\", \"caption\": \"Fig. 2: Overview of the proposed SSAM method. (a) Subspace projection: each language vector ∆i is projected onto a shared low-rank subspace prior to merging. (b) SSAM constructs covariance matrices A and B from all ∆i, extracts the shared left and right bases Uc and Vc, projects language vectors, and merge them into ∆merged.\", \"page\": 3, \"index\": 3, \"width\": 688, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2603.21584v1/fig-004.webp\", \"caption\": \"Fig. 3: Qualitative examples on commonality understanding on the MCUB-4 dataset.\", \"page\": 12, \"index\": 4, \"width\": 725, \"height\": 223}]"
motivation: 针对多模态大模型训练资源消耗大以及不同模态专家模型合并时存在的参数干扰和表示差异挑战。
method: 提出 SSAM 框架，通过在共享低秩子空间中对齐并合并语言相关的参数更新，实现无训练的多模态模型集成。
result: 在四个数据集上均取得 SOTA 表现，优于现有的无训练合并方法及部分联合训练的多模态模型。
conclusion: 证明了在参数空间进行模型对齐是构建多模态模型的一种资源高效且极具扩展性的新途径。
---

## 摘要
多模态大语言模型（MLLMs）通过联合处理来自视觉、音频和语言等多种模态的输入，实现了强大的性能。然而，构建此类模型或将其扩展到新模态通常需要大规模的配对数据集和大量的计算资源。鉴于许多预训练的 MLLMs（例如视觉-语言或音频-语言模型）已经公开可用，我们探究是否可以将它们合并为一个能够处理多种模态的单一 MLLM？合并具有不同输入模态的 MLLMs 仍然具有挑战性，部分原因是学习到的表示存在差异以及它们参数空间之间的干扰。为了应对这些挑战，我们提出了奇异子空间对齐与合并（SSAM），这是一种无需训练的模型合并框架，它将独立训练的专家级 MLLMs 统一为一个能够处理任何输入模态组合的单一模型。SSAM 分别保留特定模态的参数更新，并为语言相关的参数更新识别出一个共享的低秩子空间，在该子空间内对齐它们并进行合并，从而在最小化参数干扰的同时保留互补知识。在不使用任何多模态训练数据的情况下，SSAM 在四个数据集上均达到了最先进的性能，超越了先前的无需训练的合并方法，甚至优于联合训练的多模态模型。这些结果表明，在参数空间中对齐模型为传统的联合多模态训练提供了一种可扩展且资源高效的替代方案。

## Abstract
Multimodal large language models (MLLMs) achieve strong performance by jointly processing inputs from multiple modalities, such as vision, audio, and language. However, building such models or extending them to new modalities often requires large paired datasets and substantial computational resources. Since many pretrained MLLMs (e.g., vision-language or audio-language) are publicly available, we ask whether we can merge them into a single MLLM that can handle multiple modalities? Merging MLLMs with different input modalities remains challenging, partly because of differences in the learned representations and interference between their parameter spaces. To address these challenges, we propose Singular Subspace Alignment and Merging (SSAM), a training-free model merging framework that unifies independently trained specialist MLLMs into a single model capable of handling any combination of input modalities. SSAM maintains modality-specific parameter updates separately and identifies a shared low-rank subspace for language-related parameter updates, aligns them within this subspace, and merges them to preserve complementary knowledge while minimizing parameter interference. Without using any multimodal training data, SSAM achieves state-of-the-art performance across four datasets, surpassing prior training-free merging methods and even jointly trained multimodal models. These results demonstrate that aligning models in parameter space provides a scalable and resource-efficient alternative to conventional joint multimodal training.

---

## 论文详细总结（自动生成）

### SSAM：用于合并多模态大语言模型的奇异子空间对齐——论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：构建能够处理多种模态（视觉、音频、视频、点云等）的统一多模态大语言模型（MLLM）通常面临两大障碍：一是需要海量的多模态配对数据，二是训练成本（算力和时间）极其高昂。
*   **核心问题**：既然社区已经存在许多针对单一模态对（如视觉-语言、音频-语言）训练好的专家模型，能否在**无需额外训练数据**且**无需重新训练**的情况下，将这些专家模型合并为一个能够同时处理多种模态组合的统一模型？
*   **挑战**：直接合并不同模态的模型会导致参数空间干扰（Interference）和表示空间不一致，简单的权重平均往往会显著降低模型性能。

#### 2. 方法论：SSAM 框架
SSAM（Singular Subspace Alignment and Merging）的核心思想是**在共享的低秩子空间中对齐并合并语言相关的参数更新**。
*   **参数解耦**：将 MLLM 分为模态特定参数（编码器和投影层）和语言相关参数（解码器）。合并时保留各自的模态编码器，仅对语言解码器参数进行合并。
*   **语言向量（Language Vectors）计算**：定义每个专家模型相对于原始预训练语言模型（$W_0$）的参数增量为语言向量 $\Delta_i = W_i - W_0$。
*   **共享奇异子空间构建**：
    *   通过计算所有专家模型语言向量的行向和列向内积矩阵（协方差矩阵 $A$ 和 $B$），捕捉跨模态的一致性方向。
    *   利用**奇异值分解（SVD）**从这些矩阵中提取前 $k$ 个特征向量，构建一个“共享共识子空间”。这一步旨在过滤掉各专家模型中特有的噪声或相互冲突的更新方向。
*   **投影与合并**：将每个专家的语言向量投影到该共享子空间中进行对齐，然后进行加权合并，最后将合并后的增量重新加回 $W_0$。
*   **算法流程**：计算增量 -> 构建协方差矩阵 -> SVD 提取基向量 -> 投影对齐 -> 线性合并 -> 恢复模型权重。

#### 3. 实验设计
*   **数据集与场景**：
    *   **AVQA 基准**：MUSIC-AVQA 和 AVQA（评估音视频问答能力）。
    *   **MCUB 基准**：MCUB-3 和 MCUB-4（评估跨模态共同性理解，涉及图像、音频、视频、点云的复杂组合）。
    *   **领域特定基准**：MMLU（语言理解）、OCR-Bench（视觉文本）、MMAU（音频理解），用于验证合并后是否保留了专家能力。
*   **对比方法（Baselines）**：
    *   **联合训练模型**：ImageBind-LLM, OneLLM, X-InstructBLIP 等。
    *   **无训练合并方法**：Task Arithmetic, TIES-Merging, WUDI, OptMerge, DAMC 等。

#### 4. 资源与算力
*   **算力需求**：论文强调该方法是 **Training-free（无需训练）** 的，因此不需要大规模的 GPU 集群进行梯度下降训练。
*   **模型规模**：实验主要基于 Vicuna-7B-v1.5 语言解码器。
*   **具体细节**：文中未详细列出合并过程消耗的具体 GPU 小时数，但由于仅涉及矩阵运算（SVD 和投影），其计算开销远小于任何微调过程。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主要多模态数据集和 3 个领域基准上进行了测试，涵盖了从单模态到四模态（I+A+V+P）的多种组合。
*   **消融实验**：详细探讨了子空间秩 $k$（取值 64 到 512）对性能的影响，并对比了简单的算术平均与子空间投影的效果。
*   **客观性与公平性**：实验采用了相同的专家模型底座进行对比，确保了合并算法本身改进的有效性。通过定性分析（可视化案例）进一步验证了模型在未见过的模态组合上的推理能力。

#### 6. 主要结论与发现
*   **性能领先**：SSAM 在所有测试数据集上均达到了 SOTA 性能，显著优于现有的无训练合并方法。
*   **超越联合训练**：在某些指标上，SSAM 甚至超过了使用大量配对数据联合训练的多模态模型（如 Proj-Only 提升了约 8%-19%）。
*   **零样本泛化**：合并后的模型能够处理专家模型在训练时从未见过的模态组合（例如同时输入音频、视频和点云），表现出强大的跨模态推理能力。
*   **能力保留**：在 MMLU 等单模态基准上，SSAM 不仅没有出现性能退化，反而略有提升，证明其有效缓解了灾难性遗忘。

#### 7. 优点
*   **高效扩展**：为 MLLM 扩展新模态提供了一种极低成本的方案，无需收集昂贵的对齐数据。
*   **抗干扰性强**：通过奇异子空间对齐，巧妙地解决了模型合并中常见的“参数冲突”问题。
*   **即插即用**：适用于 LoRA 等低秩适配器，能够直接应用于开源社区的各种微调模型。

#### 8. 不足与局限
*   **架构限制**：目前仅支持具有**相同语言解码器架构**（如同为 Vicuna-7B）的模型合并，无法直接合并异构架构（如 Llama 合并 Qwen）。
*   **超参数依赖**：子空间秩 $k$ 的选择对性能有一定影响，虽然论文给出了经验值（$k=128$），但在不同规模的模型上可能需要重新调整。
*   **模型规模局限**：实验主要集中在 7B 规模，尚未在大规模模型（如 70B+）或混合专家模型（MoE）上验证其扩展性。

（完）
