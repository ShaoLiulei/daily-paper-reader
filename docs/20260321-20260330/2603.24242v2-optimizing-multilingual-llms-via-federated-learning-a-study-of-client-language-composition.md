---
title: "Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition"
title_zh: 通过联邦学习优化多语言大语言模型：客户端语言构成的研究
authors: "Aleix Sant, Jordi Luque, Carlos Escolano"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24242v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 多语言大模型指令微调的联邦学习
tldr: 本研究探讨了多语言环境下大语言模型的联邦学习优化，针对客户端语言分布不均和资源差异的挑战，扩展了FederatedScope-LLM框架并提出局部动态早停机制（LDES-FL）。通过实验分析了客户端语言组成对模型质量、公平性和成本的影响。研究发现，增加客户端内的语言多样性能显著增强全局模型的性能与公平性，尤其对低资源语言增益最大，揭示了语言组成是多语言联邦学习中的关键设计变量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24242v2/fig-001.webp\", \"caption\": \"Table 4: Training cost normalized by the higheststep configuration, Local FT (multilingual), which requires 1.83× 105 total optimization steps. Each value is reported as a fraction of this reference.\", \"page\": 7, \"index\": 1, \"width\": 415, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2603.24242v2/fig-002.webp\", \"caption\": \"Table 3: Results for the multilingual test set of the server. R and Fb stand for ROUGE-L (Lin, 2004) and Fbert (Zhang et al., 2020) respectively. The language in columns corresponds to the monolingual part of the test set. Mean reports the average score across all languages, whereas σ denotes the corresponding standard deviation, used here as an indicator of multilingual fairness. A bootstrap test with 100 sets (p-value < 0.05) confirms that all federated models achieve statistically significant improvements over the Base Model on all evaluated metrics and languages, as indicated by an asterisk (*).\", \"page\": 7, \"index\": 2, \"width\": 943, \"height\": 250}]"
motivation: 针对多语言联邦学习中客户端语言分布异构和资源不平衡导致的训练难题。
method: 扩展FederatedScope-LLM框架并引入局部动态早停机制（LDES-FL）以提升训练效率。
result: 客户端内语言越趋向多样化，全局模型的性能和公平性越好，且低资源语言获益最明显。
conclusion: 客户端语言组成是影响多语言联邦学习性能、公平性和效率的核心设计因素。
---

## 摘要
多语言环境下大语言模型（LLMs）的联邦学习（FL）面临着重大挑战，这些挑战源于客户端之间异构的语言分布以及语言资源可用性的差异。为了应对这些挑战，我们扩展了 FederatedScope-LLM 框架，以支持 LLMs 的多语言指令微调实验。我们还引入了一种新型的客户端特定早停机制，即局部动态早停（LDES-FL），该机制允许客户端根据客户端侧的验证性能暂停和恢复局部训练，从而提高训练效率和可持续性。通过一系列实验，我们研究了客户端语言构成——从完全单语言到日益多语言的客户端——如何影响多语言质量、公平性和训练成本。单语言局部微调对于单语言专业化仍然最为有效，而联邦训练则更适合学习单个平衡的多语言模型。在联邦学习中，增加客户端内部的多语言性可以产生更强大、更公平的全局模型，缩小与集中式多语言微调的差距，并为低资源语言带来最大的收益，尽管代价是需要更多的优化步骤。总之，我们的结果表明，客户端语言构成是多语言联邦学习中的一个关键设计变量，它塑造了性能、公平性和效率。

## Abstract
Federated Learning (FL) of Large Language Models (LLMs) in multilingual environments presents significant challenges stemming from heterogeneous language distributions across clients and disparities in language resource availability. To address these challenges, we extended the FederatedScope-LLM framework to support multilingual instruction-tuning experiments with LLMs. We also introduced a novel client-specific early stopping mechanism, Local Dynamic Early Stopping (LDES-FL), which allows clients to pause and resume local training based on client-side validation performance, enhancing training efficiency and sustainability. Through a series of experiments, we studied how client language composition - from fully monolingual to increasingly multilingual clients - affects multilingual quality, fairness and training cost. Monolingual local fine-tuning remains the most effective for single-language specialization, whereas federated training is better suited to learning a single balanced multilingual model. In FL, increasing within-client multilinguality leads to stronger and fairer global models, narrows the gap to centralized multilingual fine-tuning, and yields the largest gains for lower-resource languages, albeit at the cost of more optimization steps. Overall, our results identify client language composition as a key design variable in multilingual FL, shaping performance, fairness and efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition》的结构化总结：

### 1. 核心问题与整体含义
论文探讨了在**多语言环境**下，如何通过**联邦学习（FL）**优化大语言模型（LLMs）的指令微调。研究背景在于：在现实的联邦学习场景中，客户端之间的数据往往是高度异构的（非独立同分布，Non-IID），特别是语言分布的不均会导致“客户端漂移（Client Drift）”，从而损害全局模型的性能。论文的核心研究问题是：**客户端内部的语言构成（从纯单语言到多种语言混合）如何影响联邦学习的效果、公平性以及训练效率？**

### 2. 方法论
*   **框架扩展**：基于 `FederatedScope-LLM` 框架进行了扩展，支持多语言指令微调、语言感知的样本处理和灵活的 Prompt 集成。
*   **局部动态早停机制（LDES-FL）**：这是本文提出的核心算法创新。
    *   **核心思想**：允许客户端根据其私有验证集的损失值自主决定暂停或恢复训练。
    *   **流程**：客户端监控局部验证损失，若无提升则停止训练并向服务器发送其历史最佳模型；若后续下载的全局模型在局部验证集上表现提升，客户端可重新加入训练。
*   **参数高效微调（PEFT）**：采用 **LoRA** 技术，仅优化约 1%-2% 的参数，以降低通信开销和计算负担。
*   **聚合算法**：使用标准的 `FedAvg` 算法进行模型参数聚合。

### 3. 实验设计
*   **模型**：使用 `salamandra-2b-instruct`（支持 35 种欧洲语言的 20 亿参数模型）。
*   **数据集**：多语言版本的 `Alpaca Cleaned` 数据集，涵盖 8 种语言（英语、西班牙语、德语、加泰罗尼亚语、丹麦语、塞尔维亚语、克罗地亚语、巴斯克语）。
*   **对比场景（Benchmark）**：
    1.  **Base Model**：原始预训练指令模型。
    2.  **Local FT (Monolingual)**：在单个语言上进行本地微调（代表单语言专业化上限）。
    3.  **Local FT (Multilingual)**：集中式多语言微调（代表全局性能上限）。
    4.  **Federated Settings**：设置了不同比例的客户端语言构成，包括 100% mono（纯单语言客户端）、85%、70%、50%、30% 及 15% mono（逐渐增加客户端内的语言多样性）。
*   **评估指标**：ROUGE-L 和 BERTScore ($F_{bert}$)，并以标准差 ($\sigma$) 衡量多语言公平性。

### 4. 资源与算力
*   **优化器参数**：使用 `OneBitAdam`，学习率 0.001，梯度裁剪阈值 1.0。
*   **批处理设置**：微批次（micro-batch）大小为 2，梯度累积 16 步，每轮每个 GPU 处理 320 个样本。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了训练成本的量化对比，例如集中式多语言微调需要约 $1.83 \times 10^5$ 个优化步骤，而联邦学习设置下的步骤数约为其 6% 到 41% 不等。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从完全非 IID 到接近 IID 的 6 种联邦学习配置，以及 8 种单语言本地微调和 1 种集中式微调。
*   **充分性**：实验设计较为系统，通过控制变量法隔离了“语言组成”这一变量。
*   **客观性**：引入了 Bootstrap 显著性检验（p-value < 0.05）来验证改进的有效性，并同时考虑了性能（均值）和公平性（标准差），实验结果较为客观。

### 6. 主要结论与发现
*   **语言构成的关键性**：增加客户端内部的语言多样性（降低单语言比例）能显著提升全局模型的平均性能，并缩小不同语言间的表现差距（提高公平性）。
*   **低资源语言获益最大**：随着客户端变得更加多语言化，低资源语言（如巴斯克语、塞尔维亚语）的性能提升最为显著。
*   **效率与性能的权衡**：虽然增加客户端多语言性会提升模型质量，但也会导致优化步骤增加（训练时间变长），因为模型需要更多轮次才能达到更好的全局收敛点。
*   **LDES-FL 的有效性**：相比标准联邦早停，LDES-FL 在保持性能的同时，减少了约 22%-32% 的不必要计算。
*   **专业化 vs. 通用性**：单语言本地微调在特定语言上最强，但联邦学习在构建平衡的多语言模型方面更具优势且成本更低。

### 7. 优点
*   **机制创新**：LDES-FL 解决了联邦学习中客户端训练步数不一致的问题，提高了训练的可持续性。
*   **现实意义**：系统地量化了数据异构性（语言组成）对多语言 LLM 联邦学习的具体影响，为实际部署提供了设计参考。
*   **开源贡献**：扩展了 FederatedScope 框架并开源了代码和数据处理流程。

### 8. 不足与局限
*   **数据对齐偏差**：实验使用的多语言 Alpaca 数据集是通过翻译获得的，不同语言的样本可能存在内容重叠，这可能导致对“跨语言迁移能力”的评估存在偏差。
*   **模型规模限制**：实验仅在 2B 参数模型上进行，尚不清楚相关结论是否能完美推广到超大规模模型（如 70B+）。
*   **语言覆盖面**：仅涵盖了 8 种欧洲语言，未涉及语系差异更大的语言（如亚洲语系或非洲语系），其结论的普适性有待进一步验证。
*   **硬件细节缺失**：缺乏具体的硬件环境描述，不利于其他研究者评估实际的计算成本。

（完）
