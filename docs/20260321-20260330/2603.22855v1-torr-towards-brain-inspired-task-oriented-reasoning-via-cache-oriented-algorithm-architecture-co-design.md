---
title: "TorR: Towards Brain-Inspired Task-Oriented Reasoning via Cache-Oriented Algorithm-Architecture Co-design"
title_zh: TorR：通过面向缓存的算法-架构协同设计迈向类脑启发式任务导向推理
authors: "Hyunwoo Oh, SungHeon Jeong, Suyeon Jang, Hanning Chen, Sanggeon Yun, Tamoghno Das, Mohsen Imani"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22855v1"
tags: ["query:snn"]
score: 9.0
evidence: 类脑算法-架构协同设计与超维计算
tldr: 针对面向任务的目标检测（TOOD）在边缘端部署时面临的计算密集和内存开销大的挑战，本文提出了TorR，一种受大脑启发的算法-架构协同设计方案。TorR利用超维计算（HDC）关联推理器取代传统的CLIP密集对齐，并通过查询缓存、δ更新和负载门控等机制实现部分相似性复用。实验表明，TorR在保持竞争力的检测精度的同时，显著降低了能耗并实现了实时处理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22855v1/fig-001.webp\", \"caption\": \"Figure 1: From CLIP/ViT to TorR. Top: dense token aligner with fixed per-frame cost. Bottom: an event-driven encoder paired with an HDC associative aligner and a lightweight reasoner. Query caching turns temporal coherence into reuse so TorR updates on change.\", \"page\": 1, \"index\": 1, \"width\": 500, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2603.22855v1/fig-002.webp\", \"caption\": \"Figure 2: Bottleneck shift from CLIP/ViT to TorR. Left: TaskCLIP is dominated by the ViT backbone. Right: after ViT→event encoder, cost shifts to HDC associative search + graph reasoning, which are memory-bound.\", \"page\": 2, \"index\": 2, \"width\": 499, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2603.22855v1/fig-003.webp\", \"caption\": \"Figure 4: Cache-gated HDC reasoner with 𝛿-alignment. A query cache (depth 𝐾) supplies the nearest prior query q(𝑡−1) . Partial similarity 𝜌 = cos(q(𝑡 ) , q(𝑡−1) ) = 1 − 2 |Δ | 𝐷′ selects 𝛿- update (update only flipped indices Δ) or full. Under high load, if 𝜌 ≥ 𝜏byp∧𝐻 (𝑁,𝑞), cached scores/outputs are reused. Otherwise the aligner computes 𝑠 𝑗 = cos(q, h𝑗 ) and the reasoner (for fixed task) applies cached weights ?̃? 𝑗 = cos(g𝑃 , h𝑗 ) with g𝑃 = t ⊙ 𝑟ℓ1 ⊙ · · · ⊙ 𝑟ℓ𝑘 , producing 𝑠 𝑗 = 𝑠 𝑗?̃? 𝑗 . The FPS/QoS controller gates 𝐷 ′ (bank gating).\", \"page\": 3, \"index\": 3, \"width\": 500, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2603.22855v1/fig-004.webp\", \"caption\": \"Figure 3: TorR overview. Images are used only to transfer CLIP semantics to events. At run time, partial-similarity reuse (𝛿-updates, caches) and FPS/QoS control align cost with scene dynamics.\", \"page\": 3, \"index\": 4, \"width\": 438, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2603.22855v1/fig-005.webp\", \"caption\": \"Figure 5: Similarity-gated top-level accelerator. PSU computes inter-query similarity 𝜌 . The FPS/QoS controller selects bypass/𝛿/full, gates 𝐷 ′ (bank enables) and precision, and programs the associative aligner. In 𝛿-mode the aligner uses a Δ-index FIFO for sparse reads from the banked item memory𝑀 × 𝐷; scores are accumulated, top-𝑘 pooled, optionally reasoned (HDC), cached, and returned via host/DMA. Solid arrows = data, dashed = control.\", \"page\": 4, \"index\": 5, \"width\": 500, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2603.22855v1/fig-006.webp\", \"caption\": \"Table 1: Hardware specifications (TSMC 28nm, 1GHz). Logic areas in mm2, powers in mW.\", \"page\": 5, \"index\": 6, \"width\": 402, \"height\": 374}]"
motivation: 传统的基于CLIP的面向任务目标检测因计算量大和内存访问频繁，难以在功耗受限的边缘设备上实现实时部署。
method: 提出TorR协同设计，在算法上将对齐重构为HDC相似度计算并引入缓存复用机制，在架构上实现位切片项存储器和轻量级控制器。
result: "在TSMC 28nm工艺下，TorR实现了60 FPS的实时吞吐量，能耗仅为毫焦级别，且在多个任务提示下保持了约44.27%的平均精度。"
conclusion: TorR证明了通过HDC与软硬件协同设计，可以在极低功耗下实现高效、可配置的边缘端面向任务推理。
---

## 摘要
基于 CLIP 的任务导向目标检测（TOOD）提供了开放词汇、提示驱动的语义，但密集的逐窗口计算和沉重的内存流量阻碍了其在功耗受限的边缘设备上的实时部署。我们提出了 TorR，这是一种类脑启发的算法-架构协同设计，它用超维（HDC）关联推理器取代了 CLIP 风格的密集对齐，并将时间相干性转化为重用。在算法方面，TorR 将对齐重新表述为 HDC 相似度和图组合，通过以下方式引入部分相似度重用：(i) 具有逐类得分累积的查询缓存，(ii) 仅当少量超向量位发生变化时的精确 δ 更新，以及 (iii) 高系统负载下的相似度/负载门控旁路。在架构方面，TorR 实例化了一个具有存储体/精度门控的通道可扩展、位切片项目存储器，以及一个轻量级控制器，该控制器调度旁路/δ/完整路径，以在目标数量变化时满足 RT-30/RT-60 目标。在 TSMC 28 nm 工艺下综合并使用周期精确模拟器进行测试，TorR 以毫焦耳级的逐窗口能耗（60 FPS 时约为 50 mJ；30 FPS 时约为 113 mJ）和低延迟抖动维持了实时吞吐量，同时在五个任务提示中实现了具有竞争力的 AP@0.5（平均 44.27%），与强大的 VLM 基准相比差距在有限范围内，但能耗降低了几个数量级。该设计提供了部署时的可配置性（有效维度 D'、阈值、精度），以便在边缘预算内权衡准确度、延迟和能耗。

## Abstract
Task-oriented object detection (TOOD) atop CLIP offers open-vocabulary, prompt-driven semantics, yet dense per-window computation and heavy memory traffic hinder real-time, power-limited edge deployment. We present \emph{TorR}, a brain-inspired \textbf{algorithm--architecture co-design} that \textbf{replaces CLIP-style dense alignment with a hyperdimensional (HDC) associative reasoner} and turns temporal coherence into reuse. On the \emph{algorithm} side, TorR reformulates alignment as HDC similarity and graph composition, introducing \emph{partial-similarity reuse} via (i) query caching with per-class score accumulation, (ii) exact $δ$-updates when only a small set of hypervector bits change, and (iii) similarity/load-gated bypass under high system load. On the \emph{architecture} side, TorR instantiates a lane-scalable, bit-sliced item memory with bank/precision gating and a lightweight controller that schedules bypass/$δ$/full paths to meet RT-30/RT-60 targets as object counts vary. Synthesized in a TSMC 28\,nm process and exercised with a cycle-accurate simulator, TorR sustains real-time throughput with millijoule-scale energy per window ($\approx$50\,mJ at 60\,FPS; $\approx$113\,mJ at 30\,FPS) and low latency jitter, while delivering competitive AP@0.5 across five task prompts (mean 44.27\%) within a bounded margin to strong VLM baselines, but at orders-of-magnitude lower energy. The design exposes deployment-time configurability (effective dimension $D'$, thresholds, precision) to trade accuracy, latency, and energy for edge budgets.

---

## 论文详细总结（自动生成）

### TorR：通过面向缓存的算法-架构协同设计迈向类脑启发式任务导向推理

#### 1. 核心问题与研究动机
*   **核心问题**：如何在功耗极度受限的边缘设备（如家庭机器人、无人机、可穿戴设备）上实现实时的“任务导向目标检测”（TOOD）。
*   **研究动机**：
    *   **现有技术瓶颈**：目前的 TOOD 系统多基于 CLIP 或 Vision Transformers (ViTs)，这些模型计算量巨大且内存访问频繁，在边缘端难以达到实时性要求。
    *   **资源浪费**：传统视觉流水线逐帧处理图像，忽略了视频流中的时间相干性（即相邻帧之间变化很小），导致了大量重复计算。
    *   **类脑启发**：借鉴大脑“仅在环境变化时计算”和“通过关联记忆推理”的机制，提出一种基于超维计算（HDC）和事件驱动编码的高效方案。

#### 2. 方法论：TorR 协同设计
*   **核心思想**：将视觉对齐任务重构为 HDC 相似度计算，并通过查询缓存（Query Caching）将时间相干性转化为计算重用。
*   **关键技术细节**：
    *   **事件驱动编码器**：采用脉冲神经网络（SNN）处理 DVS 事件相机数据，仅在像素发生亮度变化时产生脉冲，天然支持“按需计算”。
    *   **HDC 关联推理器**：将对象和任务指令映射为高维双极超向量（Hypervectors, $\{-1, +1\}^D$）。利用简单的位运算（XNOR、Popcount）代替复杂的矩阵乘法。
    *   **部分相似性重用（$\delta$-updates）**：
        *   算法维护一个查询缓存，计算当前查询与缓存查询的相似度 $\rho$。
        *   若变化较小，仅针对翻转的位进行增量更新（$\delta$-update），计算复杂度从 $O(MD')$ 降至 $O(M|\Delta|)$。
    *   **负载门控旁路（Bypass）**：当系统负载过高且场景变化极小时，直接跳过推理，复用上一帧结果。
    *   **硬件架构**：设计了位切片（Bit-sliced）项存储器，支持存储体门控（Bank Gating）和精度调节，通过 FPS/QoS 控制器动态调整计算维度以确保满足实时目标（RT-30/60）。

#### 3. 实验设计
*   **实验场景**：评估了 5 个代表性日常任务：倒酒、体育运动、烹饪、吃早餐、休息。
*   **Benchmark（基准方法）**：
    *   **算法对比**：GGNN、TOIST、iTaskCLIP（基于 ViT-B/16 和 ViT-L/14）。
    *   **硬件对比**：在 NVIDIA RTX 4090 GPU 上运行的传统 VLM 流水线。
*   **评估指标**：平均精度（AP@0.5）、端到端延迟、吞吐量（FPS）、每帧能耗（mJ）、延迟抖动。

#### 4. 资源与算力
*   **硬件实现**：使用 Verilog HDL 编写，并在 **TSMC 28nm** 工艺下进行综合，主频为 **1GHz**。
*   **模拟工具**：使用周期精确（Cycle-accurate）模拟器，并结合综合后的开关活动数据进行功耗评估。
*   **对比算力**：文中对比了 RTX 4090 GPU 的能效，指出 GPU 在处理此类任务时功耗高达数百瓦，而 TorR 仅为瓦级。
*   **训练说明**：文中提到训练了 SNN 编码器以对齐 CLIP 空间，但未详细列出训练所用的具体 GPU 数量和时长。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种不同动态特性的任务场景，进行了从底层逻辑门面积到高层算法精度的全方位评估。
*   **消融与配置**：测试了 RT-30 和 RT-60 两种实时配置，并分析了不同任务下的延迟分布和能耗。
*   **充分性评价**：实验设计较为客观，通过与顶级 VLM 基准（如 iTaskCLIP）对比精度，同时与高端 GPU 对比能效，充分证明了该协同设计在边缘端的优势。

#### 6. 主要结论与发现
*   **极高能效**：在 60 FPS 下，每帧能耗仅约 **50 mJ**，平均功耗约 **3.1 W**，比 GPU 方案节能几个数量级。
*   **实时性保障**：通过动态维度门控，系统能将 95% 的帧延迟控制在预算内（RT-60 下 < 16.67ms），且延迟抖动极低（1.5-2.1ms）。
*   **精度平衡**：平均 AP 达到 **44.27%**，虽然略低于最强的 VLM 基准（约 53.95%），但在边缘端部署的实用性远超后者。
*   **瓶颈转移**：成功将计算瓶颈从密集的 ViT 骨干网络转移到了内存受限的关联搜索上，并通过缓存机制解决了内存瓶颈。

#### 7. 优点
*   **创新的重用机制**：提出的 $\delta$-update 机制巧妙地利用了 HDC 的位特性，将视觉冗余转化为硬件节能。
*   **高度可配置**：支持在部署时动态调整维度和精度，以适应不同的电池电量或实时性需求。
*   **端到端协同**：从事件传感器接口到硬件加速器逻辑进行了完整的闭环设计。

#### 8. 不足与局限
*   **精度差距**：在极高动态或极其复杂的语义推理任务中，HDC 的表达能力相比重型 Transformer 仍有损失。
*   **任务覆盖面**：实验仅限于 5 个特定任务，对于完全未见过的极端开放词汇场景，其泛化能力有待进一步验证。
*   **硬件依赖**：该方案依赖于事件相机（DVS）输入，对于传统的 RGB 帧序列需要额外的预处理或转换。

（完）
