---
title: Tiny Inference-Time Scaling with Latent Verifiers
title_zh: 利用潜空间验证器实现极小推理时间扩展
authors: "Davide Bucciarelli, Evelyn Turri, Lorenzo Baraldi, Marcella Cornia, Lorenzo Baraldi, Rita Cucchiara"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22492v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 生成模型的推理时缩放技术
tldr: 推理时缩放通过验证器筛选候选输出提升生成模型性能，但传统多模态大模型（MLLM）验证器需在像素空间操作，导致高昂的计算开销。本文提出 VHS（隐藏状态验证器），直接在扩散 Transformer（DiT）的中间隐藏表示上运行，无需解码至像素空间。该方法在极低推理预算下，显著降低了计算量、显存占用和耗时，同时在 GenEval 等基准测试上实现了性能提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22492v1/fig-001.webp\", \"caption\": \"Figure 1. (A) Comparison between standard inference-time scaling and VHS. VHS skips part of the generation pipeline and avoids the decoding and re-encoding steps. (B) VHS achieves a comparable quality score on GenEval [10] in just 57% of the compute time.\", \"page\": 1, \"index\": 1, \"width\": 485, \"height\": 439}, {\"url\": \"assets/figures/arxiv/2603.22492v1/fig-002.webp\", \"caption\": \"Table 2. Accuracy (%) on the GenEval benchmark [10] across computational budgets, generator backbones, and verifier configurations (on LLM Qwen2.5-0.5B). Results compare SANA-1.5 and SANA-Sprint [7] under matched wall-clock budgets (milliseconds), with each verifier operating under the same time constraint via adaptive Best-of-N .\", \"page\": 6, \"index\": 2, \"width\": 1026, \"height\": 397}, {\"url\": \"assets/figures/arxiv/2603.22492v1/fig-003.webp\", \"caption\": \"Figure 9. Visual comparison of the best pick images by different verifiers for images generated by SANA-Sprint [7] on GenEval [10] prompts. ✓ and × express the alignment with the GenEval verifier.\", \"page\": 15, \"index\": 3, \"width\": 1012, \"height\": 706}, {\"url\": \"assets/figures/arxiv/2603.22492v1/fig-004.webp\", \"caption\": \"Table 3. Accuracy (%) of SANA-Sprint [7] on the GenEval benchmark [10] across varying hidden layers, training losses, training data, and reference LLMs on a time budget of 1100 ms.\", \"page\": 7, \"index\": 4, \"width\": 1026, \"height\": 366}, {\"url\": \"assets/figures/arxiv/2603.22492v1/fig-005.webp\", \"caption\": \"Figure 7. Qualitative results of captioning produced by VHS and MLLM w/ CLIP after the alignment training phase.\", \"page\": 14, \"index\": 5, \"width\": 1014, \"height\": 358}, {\"url\": \"assets/figures/arxiv/2603.22492v1/fig-006.webp\", \"caption\": \"Table 7. Accuracy (%) of SANA-Sprint [7] on the GenEval benchmark [10], varying levels of LoRA Fine-Tuning on a time budget of 1100 ms for MLLM w/ CLIP and VHS.\", \"page\": 13, \"index\": 6, \"width\": 1026, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2603.22492v1/fig-007.webp\", \"caption\": \"Figure 5. Overall accuracy (%) of SANA-Sprint [7] on GenEval [10] across time (seconds) TFLOPs, and VRAM usage (GB).\", \"page\": 13, \"index\": 7, \"width\": 1020, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2603.22492v1/fig-008.webp\", \"caption\": \"Figure 3. Visual comparison of the best pick images by different verifiers for GenEval-generated images.\", \"page\": 8, \"index\": 8, \"width\": 972, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2603.22492v1/fig-009.webp\", \"caption\": \"Figure 2. Comparison between a standard generation-verification pipeline (top) and VHS (bottom). VHS consumes visual features directly from the hidden states of the generator, bypassing subsequent DiT layers, autoencoder (AE) decoding, and CLIP-based re-encoding, significantly reducing sampling and verification overhead.\", \"page\": 4, \"index\": 9, \"width\": 1036, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2603.22492v1/fig-010.webp\", \"caption\": \"Figure 4. Efficient Best-of-N pipeline with VHS.\", \"page\": 12, \"index\": 10, \"width\": 492, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.22492v1/fig-011.webp\", \"caption\": \"Table 5. Accuracy (%) of SANA-Sprint [7] on the GenEval benchmark [10]. Ablation on score methodology on a time budget of 1100 ms.\", \"page\": 12, \"index\": 11, \"width\": 1004, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2603.22492v1/fig-012.webp\", \"caption\": \"Table 6. GenEval benchmark [10] results with compute-equivalent baselines.\", \"page\": 12, \"index\": 12, \"width\": 1031, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2603.22492v1/fig-013.webp\", \"caption\": \"Figure 10. Prompts employed for dataset generation and image scoring.\", \"page\": 16, \"index\": 13, \"width\": 1036, \"height\": 1016}]"
motivation: 现有的 MLLM 验证器需要将候选图像解码回像素空间再重新编码，造成了巨大的计算冗余和推理延迟。
method: 提出 VHS 验证器，直接分析 DiT 生成器的中间隐藏层特征，跳过像素解码步骤进行高效评分。
result: "相比标准 MLLM 验证器，VHS 减少了 63.3% 的耗时和 51% 的计算量，并在 GenEval 上提升了 2.7% 的准确率。"
conclusion: 在潜空间直接进行验证是实现高效推理时缩放的有效途径，能在极低资源消耗下显著优化生成质量。
---

## 摘要
推理时间扩展（Inference-time scaling）已成为在测试阶段通过使用验证器对候选输出进行评分和筛选来改进生成模型的一种有效方法。一种常见的选择是采用多模态大语言模型（MLLMs）作为验证器，这虽然能提升性能，但会引入巨大的推理时间成本。事实上，扩散流水线在自编码器潜空间中运行以减少计算量，然而 MLLM 验证器仍需将候选样本解码到像素空间，并将其重新编码到视觉嵌入空间，从而导致了冗余且昂贵的操作。在这项工作中，我们提出了基于隐藏状态的验证器（Verifier on Hidden States, VHS），这是一种直接在扩散 Transformer（DiT）单步生成器的中间隐藏表示上运行的验证器。VHS 在不解码到像素空间的情况下分析生成器特征，从而降低了每个候选样本的验证成本，同时提高或达到了基于 MLLM 的竞争方案的性能。我们表明，在极小的推理预算下（每个提示词仅有少量候选样本），VHS 实现了更高效的推理时间扩展。与标准 MLLM 验证器相比，它将生成与验证的总时间减少了 63.3%，计算量（FLOPs）减少了 51%，显存（VRAM）占用减少了 14.5%，并在相同的推理时间预算下，在 GenEval 基准测试上实现了 +2.7% 的提升。

## Abstract
Inference-time scaling has emerged as an effective way to improve generative models at test time by using a verifier to score and select candidate outputs. A common choice is to employ Multimodal Large Language Models (MLLMs) as verifiers, which can improve performance but introduce substantial inference-time cost. Indeed, diffusion pipelines operate in an autoencoder latent space to reduce computation, yet MLLM verifiers still require decoding candidates to pixel space and re-encoding them into the visual embedding space, leading to redundant and costly operations. In this work, we propose Verifier on Hidden States (VHS), a verifier that operates directly on intermediate hidden representations of Diffusion Transformer (DiT) single-step generators. VHS analyzes generator features without decoding to pixel space, thereby reducing the per-candidate verification cost while improving or matching the performance of MLLM-based competitors. We show that, under tiny inference budgets with only a small number of candidates per prompt, VHS enables more efficient inference-time scaling reducing joint generation-and-verification time by 63.3%, compute FLOPs by 51% and VRAM usage by 14.5% with respect to a standard MLLM verifier, achieving a +2.7% improvement on GenEval at the same inference-time budget.

---

## 论文详细总结（自动生成）

以下是对论文《Tiny Inference-Time Scaling with Latent Verifiers》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：推理时缩放（Inference-time scaling）通过生成多个候选样本并由验证器（Verifier）筛选，能显著提升图像生成质量。然而，现有的验证器通常基于多模态大模型（MLLM），需要将生成器的潜变量（Latent）解码为像素图像，再由视觉编码器（如 CLIP）重新编码。
*   **核心问题**：这种“解码-再编码”的过程在计算上是冗余且昂贵的，尤其是在单步生成器（Single-step generators）场景下，验证器的开销甚至超过了生成过程本身，限制了其在低延迟、小算力预算场景下的应用。

### 2. 论文提出的方法论：VHS (Verifier on Hidden States)
*   **核心思想**：直接利用扩散 Transformer（DiT）生成器的**中间隐藏层表示**作为验证器的输入，完全跳过图像解码和外部视觉编码步骤。
*   **关键技术细节**：
    *   **特征提取**：从 DiT 的特定层（如第 7 层 $h_7$）提取隐藏状态，这些特征已包含丰富的语义信息且与提示词对齐。
    *   **轻量级连接器**：使用一个简单的两层 MLP 将 DiT 特征映射到大语言模型（LLM，如 Qwen2.5-0.5B）的嵌入空间。
    *   **两阶段训练**：
        1.  **对齐阶段**：使用图像-文本对训练连接器，使 LLM 理解 DiT 的隐藏特征。为解决语义漂移，使用 Gemma-3-4B 对生成的图像进行重新标注（Re-captioning）。
        2.  **微调阶段**：在带有二元标签（Yes/No）的数据集上微调 LLM 和连接器，使其具备评分能力。
    *   **加权损失函数**：针对正负样本不平衡问题，采用加权交叉熵（Weighted XE）或 Focal Loss 来提升模型对错误样本的识别能力。

### 3. 实验设计
*   **数据集与基准**：主要在 **GenEval** 基准测试上进行评估，涵盖单对象、双对象、计数、颜色、位置和属性绑定六大类别。
*   **生成器模型**：主要使用 **SANA-Sprint**（单步生成器），并在 **PixArt-α-DMD** 上验证了泛化性。
*   **对比方法**：
    *   **MLLM w/ CLIP**：标准的基于 CLIP 编码器的 MLLM 验证器。
    *   **MLLM w/ AE**：直接使用自编码器（AE）的潜变量 $z_0$ 作为输入。
    *   **多步生成基准**：对比了增加扩散步数（如 SANA-1.5）的性能。
*   **评估设置**：在相同的墙钟时间（Wall-clock time）预算下，对比不同验证器在 Best-of-N 策略下的表现。

### 4. 资源与算力
*   **硬件设备**：实验在 **NVIDIA A100 GPU** 上运行。
*   **训练开销**：
    *   **对齐阶段**：使用 16 张 A100，在 55.8 万个样本上训练 1 个 epoch。
    *   **微调阶段**：使用 8 张 A100，训练 10 个 epoch。
*   **推理效率**：VHS 相比标准验证器减少了 51% 的 TFLOPs，显存占用降低了 14.5%。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验，包括：
    *   不同 DiT 层的特征提取对比（$h_1$ 到 $h_{19}$）。
    *   不同损失函数（XE, Weighted XE, Focal Loss）的效果。
    *   不同 LLM 主干网络（Qwen2.5-0.5B vs Qwen2-1.5B）的性能权衡。
    *   不同生成器架构的迁移实验。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还深入分析了延迟、计算量和显存占用，符合其实际部署的初衷。通过在相同时间预算下对比 Best-of-N 的 N 值，体现了公平性。

### 6. 主要结论与发现
*   **高效性**：在相同的时间预算下，由于 VHS 极快，它可以筛选更多的候选样本（例如在 550ms 内，标准验证器只能做 Best-of-2，而 VHS 能做 Best-of-4）。
*   **性能提升**：在 GenEval 上，VHS 在相同时间预算下比 CLIP 基础的验证器提升了 **2.7%** 的总分。
*   **层级特性**：DiT 的中间层（如总层数的 1/3 处）比最深层或最浅层更适合做验证，因为深层特征更偏向像素重建而非语义理解。
*   **数据质量**：使用 LLM 重新标注合成图像对对齐阶段至关重要，能显著提高图像-文本的一致性。

### 7. 优点
*   **极高的推理效率**：通过消除冗余的编解码步骤，大幅降低了推理时缩放的门槛。
*   **端到端优化**：将验证过程与生成器的内部状态深度耦合，充分利用了生成器已有的计算结果。
*   **实用性强**：特别适合商业化图像生成服务中常见的“小样本、低延迟”需求。

### 8. 不足与局限
*   **模型耦合度高**：VHS 必须针对特定的生成器架构进行训练，缺乏通用性。如果更换生成器（如从 SANA 换到 Flux），验证器需要重新训练。
*   **对微小变动敏感**：虽然论文证明了对 LoRA 微调有一定的鲁棒性，但如果生成器权重发生剧烈变化，验证性能可能会下降。
*   **类别局限**：在某些特定类别（如颜色）上，基于 AE 潜变量的验证器有时表现更好，说明 VHS 在感知类特征的捕捉上仍有优化空间。

（完）
