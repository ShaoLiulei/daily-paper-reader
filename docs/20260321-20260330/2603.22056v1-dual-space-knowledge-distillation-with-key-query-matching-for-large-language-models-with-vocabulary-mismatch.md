---
title: Dual-Space Knowledge Distillation with Key-Query Matching for Large Language Models with Vocabulary Mismatch
title_zh: 针对词表不匹配大语言模型的键-查询匹配双空间知识蒸馏
authors: "Stella Eva Tsiapali, Cong-Thanh Do, Kate Knill"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22056v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 词表不匹配的大语言模型知识蒸馏
tldr: 针对大语言模型在不同分词器下知识蒸馏的挑战，本文分析了现有SOTA方法DSKD-CMA的局限性，发现其在处理异构模型的键值与查询分布时存在不匹配。为此，作者提出基于生成对抗学习的改进方法DSKD-CMA-GA，通过优化跨模型注意力匹配，在文本生成任务中取得了显著提升，特别是在分布外数据上表现优异，有效缩小了异构分词器间的蒸馏性能差距。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22056v1/fig-001.webp\", \"caption\": \"Fig. 5: Alignment weights assigned to pairs of tokens in the Student and Teacher sequences, using CMA and its chunk-based ablations: Chunk-Level Projection (CLP) and Chunk-Level Attention (CLA).\", \"page\": 4, \"index\": 1, \"width\": 310, \"height\": 790}, {\"url\": \"assets/figures/arxiv/2603.22056v1/fig-002.webp\", \"caption\": \"Table 1: Mean ROUGE-L scores from evaluation on five random seeds. Average scores across the out-of-distribution (OOD) and all (All) evaluation datasets are given. Results for the best f -divergence are reported for DSKD-CMA (SRKL), -GA (SKL) and -CT (KL).\", \"page\": 4, \"index\": 2, \"width\": 521, \"height\": 391}]"
motivation: 旨在解决现有跨分词器知识蒸馏方法中，由于不同模型产生的键（Key）与查询（Query）分布不匹配而导致的性能瓶颈。
method: 引入生成对抗学习（GA）机制来对齐不同模型间的键值与查询分布，从而优化跨模型注意力匹配过程。
result: 实验显示该方法在文本生成质量上获得持续提升，尤其在分布外数据上ROUGE-L平均提高0.37，缩小了与同分词器蒸馏的差距。
conclusion: 通过生成对抗学习优化跨空间注意力匹配，是提升异构分词器大语言模型知识蒸馏效果的有效途径。
---

## 摘要
大语言模型（LLMs）在各项语言任务中均取得了最先进（SOTA）的性能，但由于其庞大的规模和资源需求，部署成本高昂。知识蒸馏（KD）通过训练较小的学生模型来模仿较大的教师模型，从而在不显著损失性能的情况下提高效率。带有跨模型注意力机制的双空间知识蒸馏（DSKD-CMA）已成为具有不同分词器（tokenizer）的 LLM 之间进行知识蒸馏的 SOTA 方法，但其内部运作机制在很大程度上仍不透明。在这项工作中，我们通过手动标记对齐探测和热图可视化系统地分析了 DSKD-CMA 的注意力机制，揭示了其优势和局限性。在此基础上，我们提出了一种基于生成对抗（GA）学习的新方法 DSKD-CMA-GA，旨在解决由不同模型计算出的键（keys）和查询（queries）之间分布不匹配的问题。实验表明，该方法在文本生成质量上带来了虽小但持续的 ROUGE-L 提升，特别是在分布外数据上（平均提升 0.37），缩小了跨分词器与同分词器知识蒸馏之间的差距。

## Abstract
Large language models (LLMs) achieve state-of-the-art (SOTA) performance across language tasks, but are costly to deploy due to their size and resource demands. Knowledge Distillation (KD) addresses this by training smaller Student models to mimic larger Teacher models, improving efficiency without significant performance loss. Dual-Space Knowledge Distillation with Cross-Model Attention (DSKD-CMA) has emerged as a SOTA method for KD between LLMs with distinct tokenizers, yet its internal workings remain largely opaque. In this work, we systematically analyse the attention mechanism of DSKD-CMA through manual token alignment probing and heatmap visualisations, revealing both strengths and limitations. Building on this, we introduce a novel method, DSKD-CMA-GA, based on Generative Adversarial (GA) learning, to address the mismatched distributions between the keys and queries computed from distinct models. Experiments show modest but consistent ROUGE-L gains in text generation quality, particularly on out-of-distribution data (+0.37 on average), narrowing the gap between cross- and same-tokenizer KD.

---

## 论文详细总结（自动生成）

### 论文总结：针对词表不匹配大语言模型的键-查询匹配双空间知识蒸馏

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）规模的扩大，部署成本（计算、延迟、能耗）显著增加。**知识蒸馏（KD）**是解决这一问题的关键技术，但目前的 LLM 通常采用不同的分词器（Tokenizer）和词表（Vocabulary），导致教师模型和学生模型的输出序列长度及预测空间不一致。
*   **研究动机**：现有的 SOTA 方法 DSKD-CMA 虽然在跨分词器蒸馏上表现优异，但其内部的“跨模型注意力机制”运作尚不透明。此外，由于键（Key）和查询（Query）来自不同的模型空间，其分布不匹配会削弱注意力机制的有效性。
*   **核心目标**：分析 DSKD-CMA 的内部对齐行为，并提出改进方案以解决跨模型特征分布不匹配的问题。

#### 2. 论文提出的方法论
论文主要提出了分析框架和两种改进的蒸馏变体：
*   **分析框架（Chunk-based Probing）**：
    *   引入**手动块对齐（Manual Chunk Alignment）**：通过字符偏移量将不同分词器的序列划分为跨越相同文本的最小块（Chunks），作为可解释的对齐基准。
    *   提出两种消融变体：**CLP（块级投影）**和**CLA（块级注意力）**，用于验证 CMA 是否真正捕捉到了预期的块结构。
*   **核心改进方法（Key-Query Matching）**：
    *   **DSKD-CMA-GA（生成对抗对齐）**：引入判别器 $D$ 区分教师的 Key 和学生的 Query，学生的投影器 $P_Q$ 作为生成器尝试欺骗判别器，从而在对抗中强制对齐两者的分布。
    *   **DSKD-CMA-CT（条件传输对齐）**：基于最优传输（Optimal Transport）理论，通过可训练的评论器（Critic）鼓励 Key 和 Query 分布的双向匹配。
*   **最终目标函数**：将上述 Key-Query 匹配损失（$L_{KQ}$）与原始的 DSKD-CMA 损失相结合，共同优化。

#### 3. 实验设计
*   **数据集**：
    *   **训练/验证**：DataBricks Dolly 15K。
    *   **测试（分布内 ID）**：Dolly 测试集。
    *   **测试（分布外 OOD）**：Self-Instruct、Vicuna-Eval、Super-Natural Instructions (S-NI)、Unnatural Instructions (UnNI)，共计 26,387 个测试样本。
*   **模型设置**：
    *   **学生模型**：GPT-2 (124M)。
    *   **教师模型**：GPT-2 XL (1.5B，同分词器基准)；Qwen1.5 (1.8B，异分词器挑战)。
*   **对比方法（Baselines）**：
    *   监督微调（SFT）、MinED（最小编辑距离对齐）、ULD（通用 Logit 蒸馏）、原始 DSKD-CMA。
*   **评估指标**：ROUGE-L 分数。

#### 4. 资源与算力
*   **硬件设施**：使用了一个 Ampere 节点，配备 **4 台 NVIDIA A100 GPU (80GB)** 和 1TB RAM。
*   **训练时长**：每个变体的训练大约需要 **2-3 小时**。
*   **评估时长**：每个变体的评估大约需要 **1 小时**。

#### 5. 实验数量与充分性
*   **实验规模**：所有结果均基于 **5 个随机种子** 的平均值，保证了结果的统计显著性。
*   **消融与对比**：
    *   测试了 **6 种不同的 f-散度函数**（如 KL, RKL, SKL, JSD 等），以寻找每种方法的最优配置。
    *   对比了 CLP 和 CLA 两种消融模型，深入探讨了注意力机制的局部化影响。
*   **充分性评价**：实验覆盖了从 ID 到多种 OOD 场景，对比了多种主流跨分词器蒸馏算法，实验设计较为客观、公平且充分。

#### 6. 论文的主要结论与发现
*   **CMA 的运作机制**：通过热图可视化证实，DSKD-CMA 确实隐式学习到了块级对齐结构，但在处理重复 Token（如数字）时存在噪声。
*   **GA 改进的有效性**：DSKD-CMA-GA 在所有测试集上均取得了持续的提升，尤其在 OOD 数据上平均提升了 **0.37 ROUGE-L**。
*   **缩小差距**：改进后的方法显著缩小了“跨分词器蒸馏”与“同分词器蒸馏”之间的性能差距，在某些情况下甚至超越了同分词器蒸馏的效果。
*   **散度函数的影响**：发现不同的对齐机制需要配合特定的散度函数（如 GA 配合 SKL 效果最佳），强调了损失函数协同设计的重要性。

#### 7. 优点
*   **深度分析**：不仅提出了新方法，还通过手动对齐探测和可视化手段“打开了黑盒”，增强了对跨模型注意力机制的理解。
*   **通用性强**：提出的 Key-Query 匹配框架可以推广到其他需要对齐异构模型内部表示的场景。
*   **性能稳健**：在极具挑战性的词表差异（50K vs 150K）下，依然在 OOD 任务上表现出良好的泛化能力。

#### 8. 不足与局限
*   **提升幅度有限**：虽然 ROUGE-L 有所提升，但绝对分值的增长（约 0.4 点）相对较小。
*   **模型规模限制**：实验主要集中在 100M 到 2B 参数规模的模型，尚未在更大的模型（如 7B 或 70B）上验证其扩展性。
*   **任务类型**：主要评估了文本生成质量，未深入探讨逻辑推理、数学解题等更复杂的下游任务。
*   **计算开销**：虽然训练时间尚可，但引入对抗学习判别器会增加额外的训练复杂度和超参数调优成本。

（完）
