---
title: A transformer architecture alteration to incentivise externalised reasoning
title_zh: 一种旨在激励外部化推理的 Transformer 架构改进
authors: "Elizabeth Pavlova, Mariia Koroliuk, Karthik Viswanathan, Cameron Tice, Edward James Young, Puria Radmard"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21376v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于自适应计算的Transformer架构改进
tldr: 本研究提出一种改进Transformer架构的方法，旨在通过引入“早期退出”机制和强化学习训练，促使大语言模型将推理过程外部化。该方法允许模型在预测简单标记时提前结束计算，从而减少内部冗余计算。通过在小型推理模型上的初步实验，证明了该架构能自适应地分配计算资源，为未来在大规模模型上实现更透明、更高效的推理提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21376v2/fig-001.webp\", \"caption\": \"Figure 3: Distribution of coherence scores across models with different KL factors.3 As the KL factor increases, coherence distributions shift toward higher scores, approaching base model performance.\", \"page\": 10, \"index\": 1, \"width\": 664, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2603.21376v2/fig-002.webp\", \"caption\": \"Figure 2: Left: RL training dynamics on Qwen3-4B for the Theory of Mind (Sclar et al., 2024) task. Each panel shows individual runs (light lines), mean across runs (bold line with markers), and ±1 SE error bars. The dashed line indicates the pre-SFT base model performance. Step 0 represents the SFT-calibrated model before RL. Accuracy improves while average compute decreases, demonstrating that the model learns to exit earlier without sacrificing performance. Coherence remains stable throughout. Evaluated on 60 theory of mind prompts per step. Right: Token-level visualisation of early exit behaviour. Each token is coloured by the layer at which computation was terminated. The model adaptively varies computational depth, using fewer layers for predictable tokens and the full depth for more complex ones. We used λ = 1.5 and β = 0.25.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 519}]"
motivation: 旨在减少大语言模型内部隐藏的冗余计算，促使模型通过外部化的推理过程而非内部激活来进行复杂规划。
method: 在Transformer中间层增加早期退出机制，并结合校准阶段与强化学习，激励模型在保证性能的前提下尽可能在浅层完成计算。
result: 在小型推理模型上的初步实验表明，该方法能使模型根据标记的预测难度自适应地减少计算量。
conclusion: 该架构调整有望通过限制内部计算资源，强制模型将复杂推理转移到可观测的外部标记中，提高推理过程的透明度与效率。
---

## 摘要
我们提出了一种新的架构变更和训练后流水线，通过教导模型提前截断前向传播，使大语言模型（LLM）成为更冗长的推理者。我们在现有 Transformer 架构的中间层增加了早停（early-exit）机制，并训练模型在无需深度计算即可预测下一个 token 时，从较浅的层退出。在校准阶段之后，我们利用强化学习激励模型在保持任务性能的同时尽可能早地退出。我们为小型推理模型提供了初步实验结果，表明它们学会了在不同 token 之间自适应地减少计算量。我们预测，在合适的规模下应用该方法，可以最大限度地减少推理模型利用其内部激活进行非近视规划（non-myopic planning）时所能支配的冗余计算量，并将其仅保留给难以预测的 token。

## Abstract
We propose a new architectural change, and post-training pipeline, for making LLMs more verbose reasoners by teaching a model to truncate forward passes early. We augment an existing transformer architecture with an early-exit mechanism at intermediate layers and train the model to exit at shallower layers when the next token can be predicted without deep computation. After a calibration stage, we incentivise the model to exit as early as possible while maintaining task performance using reinforcement learning. We provide preliminary results to this effect for small reasoning models, showing that they learn to adaptively reduce computations across tokens. We predict that, applied at the right scale, our approach can minimise the amount of excess computation that reasoning models have at their disposal to perform non-myopic planning using their internal activations, reserving this only for difficult-to-predict tokens.

---

## 论文详细总结（自动生成）

这篇论文提出了一种改进 Transformer 架构的方法，旨在通过“早期退出”机制强制模型将推理过程从内部隐藏计算转移到外部的思维链（CoT）中。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的推理模型（如 OpenAI o1、DeepSeek-R1）虽然能生成思维链，但往往存在“不忠实”（CoT 不能反映真实认知）或“混淆”（在内部激活中进行隐藏规划）的问题。
*   **研究动机**：Transformer 是非近视的，即使在预测简单的 token 时，也会利用剩余的计算量为后续步骤做准备。为了提高模型的可监控性，研究者希望限制模型的内部计算资源，迫使模型必须通过外部生成的 token（CoT）来进行复杂推理。

### 2. 方法论
该方法包含架构调整和两个训练阶段：
*   **架构调整**：在 Transformer 的中间层（每 5 层）添加轻量级的“早期退出头”（Early-exit heads）和 LoRA 适配器。退出头输出一个标量，决定模型是否在该层停止计算并直接跳转到输出层。
*   **阶段 1：自校准（Self-distillation SFT）**：
    *   将原始全深度模型作为教师，改进模型作为学生。
    *   利用 **KL 散度** 衡量中间层预测与最终层预测的相似度。如果相似度高，则设定该层为目标退出点。
    *   通过交叉熵损失训练学生模型，使其在保证预测准确的同时，学习教师模型的退出分布。
*   **阶段 2：强化学习激励（RLOO）**：
    *   使用改进的 RLOO 算法，设计复合奖励函数：$R_{total} = R_{task} - \lambda \cdot (\text{平均退出层索引}) - \beta \cdot D_{KL}$。
    *   **$\lambda$ 参数**：作为一个“拨盘”，控制模型对早期退出的渴望程度。$\lambda$ 越高，模型越倾向于节省内部计算，从而被迫将推理过程转移到外部 token 中。

### 3. 实验设计
*   **模型对象**：DeepSeek-R1-Distill-Qwen-1.5B 和 Qwen3-4B（小型推理模型）。
*   **数据集**：
    *   **GSM8K**：数学文字题，用于测试逻辑推理。
    *   **Theory of Mind (ToM)**：心理理论数据集（ExploreToM），用于测试复杂的社会推理。
*   **对比基准（Benchmark）**：原始的、未修改权重的全深度模型（Base Model）。
*   **评估指标**：任务准确率（Accuracy）、回答连贯性（Coherence）、平均计算量（使用的层数比例）和总计算量（Token 数 × 平均层数）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及训练所需的总时长。考虑到实验对象是 1.5B 和 4B 规模的模型，且使用了 LoRA 适配器，预计算力需求相对较低。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了在两个不同任务（GSM8K 和 ToM）上的初步结果，并对 RL 训练动态进行了 5 次独立运行的平均分析。
*   **充分性评价**：目前属于**初步概念验证（Proof of Concept）**阶段。虽然展示了模型在降低计算量的同时保持甚至提升准确率的趋势，但实验主要集中在小模型上，且尚未对“推理忠实度”进行量化评估。

### 6. 主要结论与发现
*   **自适应计算**：模型学会了根据 token 的难度自适应分配深度。例如，在预测高度可预测的 token（如单词后缀）时，模型会显著提前退出。
*   **性能平衡**：通过 RL 训练，Qwen3-4B 在 ToM 任务上的准确率从 47% 提升至 55-60%，同时平均计算量从 98% 降至 95% 甚至更低。
*   **可行性**：证明了可以通过架构干预来调节模型的内部计算强度，而不破坏其生成连贯文本的能力。

### 7. 优点与亮点
*   **创新的安全视角**：不同于传统的 CoT 提示词工程，该方法从底层架构出发，通过物理限制内部“草稿纸”的大小来强制外部化推理。
*   **可调控性**：引入了 $\lambda$ 参数，允许开发者在“推理透明度（安全性）”与“计算效率/性能”之间进行权衡。
*   **随机性机制**：采用随机退出而非确定性退出，使得模型能够通过强化学习探索更优的计算分配策略。

### 8. 不足与局限
*   **规模限制**：实验仅在 1.5B 和 4B 模型上进行，尚未在大规模前沿模型（如 70B+）上验证其有效性。
*   **忠实度验证缺失**：虽然论文声称该方法能提高 CoT 的忠实度，但本次实验尚未提供直接证据证明 CoT 确实反映了模型的所有推理步骤。
*   **泛化性风险**：目前的实验主要针对特定任务，跨领域的泛化能力（Generalization）仍需进一步验证。
*   **质量权衡**：过度压制内部计算可能会导致模型在处理极度复杂的瞬时逻辑时出现性能崩塌。

（完）
