---
title: "Mixture of Chapters: Scaling Learnt Memory in Transformers"
title_zh: 章节混合：扩展 Transformer 中的学习记忆
authors: "Tasmay Pankaj Tibrewal, Pritish Saha, Ankit Meda, Kunal Singh, Pradeep Moturi"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21096v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 使用 Mixture of Chapters 架构扩展 Transformer 记忆
tldr: 针对Transformer缺乏显式知识存储机制的问题，本文提出“章节混合”（Mixture of Chapters）架构。通过引入可学习的稀疏记忆库并结合受MoE启发的路由机制，将记忆库划分为多个章节进行按需检索。该方法在保持计算效率的同时，将记忆容量扩展至262K个Token，在预训练和指令微调任务中均优于同等算力的基准模型，并显著提升了知识保留能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21096v1/fig-001.webp\", \"caption\": \"Figure 2: Loss curves. Left: Pretraining. Right: IFT (Freezing the bank yields identical curves).\", \"page\": 6, \"index\": 1, \"width\": 972, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-002.webp\", \"caption\": \"Figure 1: Single transformer block with chapter-based memory routing\", \"page\": 3, \"index\": 2, \"width\": 854, \"height\": 557}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-003.webp\", \"caption\": \"Table 6: Main training settings for pretraining and IFT.\", \"page\": 18, \"index\": 3, \"width\": 977, \"height\": 621}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-004.webp\", \"caption\": \"Table 3: Literature map (representative, not exhaustive).\", \"page\": 13, \"index\": 4, \"width\": 972, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-005.webp\", \"caption\": \"Table 5: Main MoC architecture used in our experiments.\", \"page\": 17, \"index\": 5, \"width\": 952, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-006.webp\", \"caption\": \"Table 2: Comparison of Vanilla, Mixture of Chapters (MoC) (∆ = IFT - pretrain; lower is better)\", \"page\": 5, \"index\": 6, \"width\": 786, \"height\": 188}]"
motivation: Transformer模型缺乏显式的架构机制来存储和组织训练过程中获取的知识。
method: 提出一种基于章节路由的可学习稀疏记忆库，通过交叉注意力机制和类似MoE的分区选择实现大规模知识检索。
result: 该模型在预训练和指令微调基准测试中超越了同等FLOPs的基准模型，并展现出更强的知识保留和抗遗忘能力。
conclusion: 显式关联记忆为模型参数提供了互补的容量，是Transformer模型在参数规模之外的一个重要扩展维度。
---

## 摘要
Transformer 缺乏一种显式的架构机制来存储和组织训练过程中获取的知识。我们引入了可学习的稀疏记忆库：一组随机初始化并端到端训练的潜标记（latent tokens），Transformer 层通过交叉注意力机制对其进行查询，以检索存储的知识。为了在不产生过高注意力计算成本的情况下扩展记忆容量，我们受专家混合（Mixture-of-Experts）架构启发，提出了基于章节的路由机制，将记忆库划分为多个章节，并训练一个路由器为每个输入选择相关的子集。这使得在保持计算可控的同时，能够将记忆扩展到 262K 个标记。我们在预训练和指令微调的相关基准测试中，将我们的方法与标准 Transformer（在等效 FLOPs 设置下）进行了评估。我们的模型超越了等效 FLOPs 的基准模型，这表明存在一个新的扩展维度，并证明了显式联想记忆提供了与模型参数中隐式捕获的知识互补的能力。此外，我们观察到在持续训练下知识保留能力有所提高，并且在不同训练阶段转换（例如从预训练到指令微调）时具有抗遗忘的鲁棒性。

## Abstract
Transformers lack an explicit architectural mechanism for storing and organizing knowledge acquired during training. We introduce learnable sparse memory banks: a set of latent tokens, randomly initialized and trained end-to-end, that transformer layers query via cross-attention to retrieve stored knowledge. To scale memory capacity without prohibitive attention costs, we propose chapter-based routing inspired by Mixture-of-Experts architectures, partitioning the memory bank into chapters and training a router to select relevant subsets per input. This enables scaling to 262K memory tokens while maintaining tractable computation. We evaluate our approach against standard transformers (in iso-FLOP settings) on pre-training and instruction fine-tuning across relevant benchmarks. Our models surpass iso-FLOP baselines suggesting scope for a new axis of scaling, demonstrating that explicit associative memory provides complementary capacity to what is captured implicitly in model parameters. Additionally, we observe improved knowledge retention under continued training, with robustness to forgetting when transitioning between training phases (e.g., pretraining to instruction fine-tuning).

---

## 论文详细总结（自动生成）

### 《Mixture of Chapters: Scaling Learnt Memory in Transformers》论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的 Transformer 架构缺乏显式的机制来存储和组织在训练过程中获取的事实性知识。知识通常被隐式地“压缩”在模型的稠密参数（如 MLP 层）中，这导致知识难以独立扩展、检查或编辑。虽然现有的检索增强生成（RAG）或长文本缓存（KV-Cache）技术可以利用外部信息，但它们并非模型内部学习到的持久化记忆。
**本文动机**：引入一种可学习的、显式的联想记忆库，并通过类似专家混合（MoE）的稀疏路由机制解决大规模记忆检索带来的计算开销问题，从而为 Transformer 提供一个新的扩展维度。

#### 2. 方法论：核心思想与技术细节
论文提出了 **Mixture of Chapters (MoC)** 架构，其核心组件包括：
*   **学习型记忆库（Learned Memory Bank）**：一组随机初始化并随模型端到端训练的潜标记（Latent Tokens）。这些标记不对应具体的文本，而是存储在潜空间中的知识表示。
*   **交叉注意力查询**：在 Transformer 块中插入记忆层。输入序列的隐藏状态作为查询（Query），记忆库提供键（Key）和值（Value），通过交叉注意力机制检索相关信息。
*   **章节化路由（Chapter-based Routing）**：
    *   **分区**：将庞大的记忆库划分为 $C$ 个“章节”（Chapters），每个章节包含 $T$ 个标记。
    *   **序列级路由**：对输入序列进行池化（如均值池化）得到序列表示，通过一个轻量级路由器计算每个章节的得分。
    *   **稀疏激活**：仅选择得分最高的 Top-$k$ 个章节参与交叉注意力计算。
*   **计算效率**：这种设计将注意力机制的复杂度从 $O(L \cdot N_m)$ 降低到 $O(L \cdot k \cdot T)$（其中 $L$ 是序列长度，$N_m$ 是总记忆大小），使得扩展到 262K 个记忆标记在计算上变得可行。

#### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：使用 FineWeb-Edu 数据集的子集（约 9.6B tokens）。
    *   **指令微调 (IFT)**：使用 UltraChat_200k 数据集（约 230M tokens），并将上下文长度从 1024 扩展到 2048。
*   **Benchmark**：评估了 MMLU（多任务理解）、ARC-Challenge（科学问答）、BoolQ（布尔判断）和 OpenBookQA。
*   **对比方法**：
    1.  **Vanilla (backbone-only)**：移除记忆模块的原始骨干模型。
    2.  **Vanilla (iso-FLOP)**：**关键基准**。通过增加层数（深度）来匹配 MoC 模型的训练算力，确保对比的公平性。
    3.  **Mixture of Chapters (MoC)**：本文提出的模型。

#### 4. 资源与算力
*   **算力规模**：预训练进行了 9,600 步（9.6B tokens），指令微调进行了 2 个 Epoch（3,200 步）。
*   **硬件设备**：预训练使用 **8 张 GPU** 进行分布式训练（DDP）；指令微调使用 **4 张 GPU**。
*   **明确说明**：论文**未明确提及具体的 GPU 型号**（如 A100 或 H100），但详细列出了 FLOPs 的解析计算过程，确保了在等效算力（iso-FLOP）前提下进行比较。

#### 5. 实验数量与充分性
*   **实验组数**：涵盖了预训练 Loss 曲线分析、下游任务 Benchmark 评测、指令微调后的知识保留实验，以及针对记忆库是否冻结的消融实验。
*   **充分性与公平性**：实验设计较为充分。特别是引入了 **iso-FLOP 基准**，避免了仅因参数量增加而带来的性能提升假象。此外，论文还探讨了在重度指令微调下模型的抗遗忘能力，这在当前大模型研究中非常具有现实意义。

#### 6. 主要结论与发现
*   **性能提升**：在相同算力预算下，MoC 模型的预训练验证 Loss 低于深层稠密模型，且在 MMLU 等多个 Benchmark 上表现更优。
*   **抗遗忘能力**：在经历重度指令微调后，标准 Transformer 在 ARC-C 和 BoolQ 等知识密集型任务上出现了显著的性能下降（灾难性遗忘），而 MoC 模型表现极其稳定，证明显式记忆库起到了“知识锚点”的作用。
*   **记忆库复用**：实验发现，在指令微调阶段**冻结记忆库**（不更新记忆参数）也能达到与更新参数相当的效果，这降低了微调时的显存开销。

#### 7. 优点
*   **扩展性**：通过章节路由机制，成功将可学习记忆的规模推向了 26 万标记以上，且计算成本可控。
*   **显式解耦**：将事实性知识存储与逻辑推理（骨干参数）部分解耦，为未来的知识编辑和模型解释提供了可能。
*   **鲁棒性**：在持续学习和阶段转换中展现了优异的知识保留能力。

#### 8. 不足与局限
*   **参数量增加**：虽然激活参数量受控，但总参数量（包含未激活的记忆章节）显著增加，增加了存储和传输的负担。
*   **路由粒度**：目前采用序列级路由，可能无法捕捉到序列内部细粒度的知识切换需求（如一个长文档包含多个主题）。
*   **实验规模限制**：实验主要在 10B token 级别的预训练规模上进行，尚未在万亿级（Trillion-scale）数据上验证其长期扩展规律。
*   **GPU 型号缺失**：未提供具体硬件型号和训练时长，难以精确评估实际的挂钟时间（Wall-clock time）效率。

（完）
