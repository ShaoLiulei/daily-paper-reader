---
title: Predictive Regularization Against Visual Representation Degradation in Multimodal Large Language Models
title_zh: 针对多模态大语言模型中视觉表示退化的预测正则化
authors: "Enguang Wang, Qiang Wang, Yuanchen Wu, Ke Yan, Xinbin Yuan, Shouhong Ding, Xialei Liu, Ming-Ming Cheng"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20808v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 多模态大模型中的视觉表示退化
tldr: 本研究揭示了多模态大语言模型（MLLM）在训练过程中存在的“视觉表示退化”现象，即为了优化文本生成目标，模型内部视觉特征的全局功能和局部结构会受损。为此，作者提出了预测正则化（PRe）方法，通过强制中间层特征预测初始视觉特征来保持视觉保真度。实验证明，缓解视觉退化能显著提升模型的跨模态理解能力，强调了维持内部视觉能力的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.20808v1/fig-001.webp\", \"caption\": \"Figure 8. The cosine similarity between the patch noted in green and all other patches in the middle layer. Images are from opensource COCO-stuff [8] dataset.\", \"page\": 6, \"index\": 1, \"width\": 471, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2603.20808v1/fig-002.webp\", \"caption\": \"Figure 13. Case studies comparing LLaVA-1.5-7B with and without our PRe method.\", \"page\": 15, \"index\": 2, \"width\": 1022, \"height\": 455}, {\"url\": \"assets/figures/arxiv/2603.20808v1/fig-003.webp\", \"caption\": \"Table 1. The effectiveness of the predictive regularization among various LLMs and visual encoders. * indicates the visual encoder is frozen during training, without * indicates the visual encoder is trainable.\", \"page\": 7, \"index\": 3, \"width\": 1036, \"height\": 451}, {\"url\": \"assets/figures/arxiv/2603.20808v1/fig-004.webp\", \"caption\": \"Table 2. Results of different regularized layers.\", \"page\": 7, \"index\": 4, \"width\": 496, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2603.20808v1/fig-005.webp\", \"caption\": \"Figure 9. Visual token decoding distributions across layers.\", \"page\": 7, \"index\": 5, \"width\": 487, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2603.20808v1/fig-006.webp\", \"caption\": \"Table 4. The effectiveness of the predictive regularization on a 3B-LLM. * indicates the visual encoder is frozen during training, without * indicates the visual encoder is trainable.\", \"page\": 14, \"index\": 6, \"width\": 1036, \"height\": 194}, {\"url\": \"assets/figures/arxiv/2603.20808v1/fig-007.webp\", \"caption\": \"Table 3. Results using different anchor features.\", \"page\": 8, \"index\": 7, \"width\": 496, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2603.20808v1/fig-008.webp\", \"caption\": \"Figure 4. The cosine similarity between the patch noted in green and all other patches. Compared with the initial representation, the middle layer blurs the visual semantic boundaries. (Images are from opensource COCO-stuff [8] dataset)\", \"page\": 4, \"index\": 8, \"width\": 488, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2603.20808v1/fig-009.webp\", \"caption\": \"Figure 2. Linear probe results on global visual features. Relative to the initial representation (Layer 0), a consistent performance drop is observed in the intermediate layers.\", \"page\": 4, \"index\": 9, \"width\": 496, \"height\": 366}]"
motivation: 旨在探究以文本生成为单一目标的训练是否会导致 MLLM 内部视觉基础能力的受损及其对性能的影响。
method: 提出预测正则化（PRe），通过约束 LLM 中间层的视觉特征去预测初始视觉特征，从而维持其固有的视觉属性。
result: 广泛的实验证实，该方法能有效缓解视觉退化并显著增强模型在各类视觉语言任务中的表现。
conclusion: 维持强大的内部视觉表示对于构建具备全面多模态理解能力的 MLLM 至关重要。
---

## 摘要
尽管多模态大语言模型（MLLMs）在视觉语言任务中表现出色，但其语言驱动的训练对内部视觉基础能力的影响尚不明确。在本文中，我们进行了详细的诊断分析，揭示了一个普遍存在的问题：MLLMs 中的视觉表示退化。具体而言，我们发现与初始视觉特征相比，LLM 中间层的视觉表示在全局功能和补丁（patch）结构上均表现出退化。我们将这一现象归因于单一文本生成目标驱动下的视觉牺牲，即模型为了优化答案生成而损害了其视觉保真度。我们认为，一个鲁棒的 MLLM 既需要强大的跨模态推理能力，也需要核心视觉能力，并提出了预测正则化（Predictive Regularization, PRe），通过强制退化的中间特征预测初始视觉特征，从而维持 MLLM 内部表示固有的视觉属性。大量实验证实，缓解这种视觉退化能有效提升视觉语言性能，强调了在 MLLMs 中培养鲁棒的内部视觉表示对于全面多模态理解的关键重要性。

## Abstract
While Multimodal Large Language Models (MLLMs) excel at vision-language tasks, the cost of their language-driven training on internal visual foundational competence remains unclear. In this paper, we conduct a detailed diagnostic analysis to unveil a pervasive issue: visual representation degradation in MLLMs. Specifically, we find that compared to the initial visual features, the visual representation in the middle layers of LLM exhibits both a degradation in global function and patch structure. We attribute this phenomenon to a visual sacrifice driven by the singular text-generation objective, where the model compromises its visual fidelity to optimize for answer generation. We argue that a robust MLLM requires both strong cross-modal reasoning and core visual competence, and propose Predictive Regularization (PRe) to force degraded intermediate features to predict initial visual features, thereby maintaining the inherent visual attributes of the MLLM's internal representations. Extensive experiments confirm that mitigating this visual degradation effectively boosts vision-language performance, underscoring the critical importance of fostering robust internal visual representations within MLLMs for comprehensive multimodal understanding.

---

## 论文详细总结（自动生成）

### 论文总结：针对多模态大语言模型中视觉表示退化的预测正则化

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）通常采用预训练视觉编码器（如 CLIP）配合大语言模型（LLM）的架构。目前的训练范式几乎完全由**语言建模目标**（即预测下一个文本 Token）驱动。
*   **研究动机**：作者发现这种“语言中心”的训练会导致 LLM 内部的视觉表示发生**退化（Degradation）**。
*   **核心发现**：通过线性探测（Linear Probing）和语义分析，作者证实了视觉特征在进入 LLM 中间层后，其全局分类功能下降，且局部补丁（patch）之间的语义边界变得模糊（即“语义融合”）。
*   **研究意义**：论文指出，这种退化是模型为了优化文本生成而做出的“视觉牺牲”。为了构建更稳健的 MLLM，必须在提升跨模态推理的同时，保持核心视觉能力的保真度。

#### 2. 方法论：预测正则化（PRe）
为了对抗视觉退化，作者提出了 **PRe (Predictive Regularization)**，其核心思想是借鉴“预测编码”理论，强制模型中间层的特征能够“记住”初始的视觉状态。
*   **核心思想**：在训练过程中，要求 LLM 中间层的视觉隐藏状态能够预测（重构）其进入 LLM 之前的初始视觉特征。
*   **关键技术细节**：
    *   **锚点特征（Anchor）**：将进入 LLM 之前的投影视觉特征作为目标，并施加 `stop-gradient`（停止梯度回传）操作。
    *   **预测头（Predictor）**：在 LLM 的中间层（如第 16 层）添加一个轻量级的 2 层 MLP 预测头。
    *   **损失函数**：计算预测输出与锚点特征之间的**负余弦相似度**。
    *   **总目标函数**：$L_{total} = L_{LLM} + \lambda L_{PRe}$，其中 $\lambda$ 是平衡系数（默认设为 0.5）。
*   **流程**：在执行标准的文本生成训练时，同步优化这个预测任务，从而在不改变推理架构的前提下，正则化中间层的特征演化路径。

#### 3. 实验设计
*   **数据集与场景**：
    *   **训练**：使用 LLaVA-558K 进行预训练，LLaVA-665K 进行指令微调。
    *   **评估（Benchmark）**：涵盖了 9 个主流评测集，包括 GQA（推理）、MMMU（学科知识）、TextVQA/OCRbench（文字识别）、RealWorldQA/MMVP（视觉感知）等，补充材料中又增加了 5 个 VQA 数据集。
*   **对比方法**：以 LLaVA-1.5 和 LLaVA-Next 为基础框架，对比了使用不同 LLM（Vicuna-7B, Qwen2.5-7B/3B）和不同视觉编码器（CLIP, SigLIP2）在加入 PRe 前后的表现。

#### 4. 资源与算力
*   **硬件环境**：文中提到使用了 **8 台设备**（未明确具体 GPU 型号，但根据 DeepSpeed-ZeRO-3 的配置推测为 A100 或 H100 级别）。
*   **训练参数**：
    *   Stage 1（投影层微调）：Batch size 256，学习率 1e-3。
    *   Stage 2（指令微调）：Batch size 128，学习率 2e-5（LLM）/ 2e-6（视觉编码器）。
*   **训练时长**：每个阶段仅训练 **1 个 Epoch**。补充材料显示 Qwen2.5-3B 的总训练时间约为 35,800 秒（约 10 小时）。

#### 5. 实验数量与充分性
*   **实验规模**：作者在 14 个不同的 Benchmark 上进行了测试，涵盖了从 3B 到 7B 不同规模的模型。
*   **消融实验**：非常充分。作者探讨了：
    1.  **正则化层的位置**：对比了中间层 vs. 最后一层（发现中间层效果最好，因为深层视觉特征会被模型主动“静默”以适配文本生成）。
    2.  **锚点来源**：对比了投影前特征、投影后特征以及外部模型（如 DINOv2）特征。
    3.  **正则化粒度**：对比了全局特征正则化 vs. 补丁级正则化（证实补丁级更有效）。
    4.  **权重敏感度**：测试了 $\lambda$ 在 0.2 到 2.0 之间的影响。
*   **客观性**：实验采用了多种主流开源模型作为 Baseline，且在冻结/非冻结视觉编码器的两种模式下均验证了有效性，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **视觉退化是普遍存在的**：所有主流 MLLM 在处理视觉信息时，中间层都会出现明显的视觉功能丧失。
*   **退化具有结构性**：这种退化源于局部补丁语义的过度融合，导致模型无法区分细微的视觉边界。
*   **PRe 的有效性**：通过简单的预测正则化，可以显著提升模型在视觉感知（如计数、颜色识别、OCR）方面的表现，且不会损害语言推理能力。
*   **层级差异**：LLM 的深层倾向于将视觉 Token 转化为语义模糊的占位符（如点号或换行符），因此正则化应作用于中浅层。

#### 7. 优点
*   **诊断深入**：首次系统性地量化并解释了 MLLM 内部视觉表示退化的现象，提供了多维度的诊断证据。
*   **即插即用**：PRe 方法极其轻量，仅在训练阶段增加微小的计算开销（约 0.85% 的时间增加），推理阶段完全无额外开销。
*   **启发性强**：将神经科学中的“预测编码”引入 MLLM 训练正则化，为解决多模态对齐中的“信息丢失”提供了新思路。

#### 8. 不足与局限
*   **偏差继承风险**：由于 PRe 强制中间层预测初始视觉特征，如果初始视觉编码器（如 CLIP）本身存在偏差或幻觉，该方法可能会强化这些缺陷。
*   **正则化层选择的启发式**：虽然实验证明中间层效果好，但对于不同深度、不同架构的 LLM，如何自动选择最优的正则化层尚缺乏理论指导。
*   **未探索更复杂的正则化**：目前仅使用了简单的余弦相似度预测，未探讨对比学习（Contrastive Learning）或掩码重构（Masked Reconstruction）在这一场景下的潜力。

（完）
