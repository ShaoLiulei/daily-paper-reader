---
title: "MINISA: Minimal Instruction Set Architecture for Next-gen Reconfigurable Inference Accelerator"
title_zh: MINISA：面向下一代可重构推理加速器的极简指令集架构
authors: "Jianming Tong, Devansh Jain, Yujie Li, Charith Mendis, Tushar Krishna"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20623v1"
tags: ["query:snn"]
score: 9.0
evidence: 用于可重构 AI 加速器的极简指令集架构
tldr: 针对现代可重构AI加速器在追求灵活性时面临的高昂控制开销问题，本文提出了MINISA指令集。它基于改进的FEATHER+架构，以虚拟神经元（VN）为粒度进行编程，通过仅有的三条布局指令和一条映射指令，在保留硬件灵活性的同时极大地减少了指令流量。实验表明，MINISA显著降低了离线指令传输量，消除了取指停顿，在多种工作负载下实现了高达31.6倍的端到端加速。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.20623v1/fig-001.webp\", \"caption\": \"Fig. 6: MINISA for layout Specification and SetWVNLayout illustration (K = 8, N = 8, AH = AW = 4 ⇒ KL0 = AH). Takeaway: MINISA organizes layout at VN granularity.\", \"page\": 6, \"index\": 1, \"width\": 502, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2603.20623v1/fig-002.webp\", \"caption\": \"Fig. 13: Latency breakdown and compute utilization of representative workloads. Takeaway: FEATHER+ (4×64, upper chart), (16×64, middle chart) and (16×256, lower chart) with MINISA could successfully utilize available PEs for all irregular workload shapes, such as K=10 and K = 2n, n ∈ Z+.\", \"page\": 11, \"index\": 2, \"width\": 517, \"height\": 873}, {\"url\": \"assets/figures/arxiv/2603.20623v1/fig-003.webp\", \"caption\": \"Fig. 1: Workload Illustration. Convolution is converted to MatMul via im2col. We use color coding: blue/green/purple/red for Input(I)/Weight(W )/Partial-sum(psum, P )/Outputs(O).\", \"page\": 2, \"index\": 3, \"width\": 520, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2603.20623v1/fig-004.webp\", \"caption\": \"Fig. 7: Mapping a matrix multiplication to FEATHER+ (AH×AW=4×4 NEST) as 2 compute tiles under MINISA.\", \"page\": 7, \"index\": 4, \"width\": 512, \"height\": 1250}, {\"url\": \"assets/figures/arxiv/2603.20623v1/fig-005.webp\", \"caption\": \"Fig. 12: Instruction-overhead reduction. At 16×256, MINISA lowers instruction bytes by a geometric mean of (2×105)× versus micro-instructions (bar chart). Black and red lines show instruction-to-data ratio for baseline and MINISA, respectively. Takeaway: MINISA shrinks instruction volume by at most 4.4×105×, removing instruction loading from the critical path.\", \"page\": 10, \"index\": 5, \"width\": 1063, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2603.20623v1/fig-006.webp\", \"caption\": \"Fig. 2: Programmer view of FEATHER+, the reconfigurable accelerator capable of co-switching both mapping and layout.\", \"page\": 3, \"index\": 6, \"width\": 507, \"height\": 809}, {\"url\": \"assets/figures/arxiv/2603.20623v1/fig-007.webp\", \"caption\": \"TABLE VII: FEATHER+ Mapper Search Knobs for a Symbolic GEMM OP=M,Q=N m,n = IM=M,J=K m,k ·WK=K,N=N k,n .\", \"page\": 13, \"index\": 7, \"width\": 1072, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2603.20623v1/fig-008.webp\", \"caption\": \"Fig. 9: Mapper illustration of “VN Group”, “Combination” and “Duplication” to lower ❷ in Fig. 7 to two compute tiles.\", \"page\": 8, \"index\": 8, \"width\": 519, \"height\": 269}]"
motivation: 细粒度微控制带来的高昂指令获取开销严重限制了可重构加速器的实际性能和利用率。
method: 提出以虚拟神经元为控制粒度的MINISA指令集，通过极简的布局与映射指令抽象控制逻辑，并优化了底层硬件架构。
result: 指令流量减少了35倍至40万倍，消除了绝大部分取指停顿，使16x256规模的加速器获得最高31.6倍的加速。
conclusion: MINISA证明了通过合理的控制粒度抽象，可以在不牺牲可重构硬件灵活性的前提下大幅提升系统能效。
---

## 摘要
现代可重构 AI 加速器依赖于丰富的映射和数据布局灵活性，以在矩阵乘法、卷积以及 AI 之外的新兴应用中保持高利用率。然而，通过细粒度的微控制来暴露这种灵活性会导致从片外存储器获取配置位的控制开销过高。本文提出了 MINISA，这是一种极简指令集，它以虚拟神经元（Virtual Neurons, VNs）为粒度对可重构加速器进行编程。VNs 是既能保留硬件灵活性，又能避免不必要控制成本的最粗控制粒度和最细粒度的平衡点。首先，我们介绍了 FEATHER+，它是对 FEATHER 的适度改进，消除了运行时数据流/布局协同切换所需的冗余片上复制，并支持输入和权重数据在执行前不可用于离线布局操作的动态情况。随后，MINISA 将 FEATHER+ 的控制抽象为针对输入、权重和输出 VN 的三条布局设置指令，以及一条用于设置数据流的映射指令。这在保留 FEATHER+ 支持的合法映射和布局空间的同时，减少了控制和指令占用空间。我们的结果显示，在涵盖 AI (GPT-oss)、全同态加密 (FHE) 和零知识证明 (ZKP) 的 50 个 GEMM 工作负载下，MINISA 在各种规模下将片外指令流量的几何平均值降低了 35 倍至 (4x10^5) 倍。这消除了消耗 96.9% 微指令周期的指令获取停顿，使 16x256 FEATHER+ 的端到端加速比达到 31.6 倍。我们的代码：https://github.com/maeri-project/FEATHER/tree/main/minisa。

## Abstract
Modern reconfigurable AI accelerators rely on rich mapping and data-layout flexibility to sustain high utilization across matrix multiplication, convolution, and emerging applications beyond AI. However, exposing this flexibility through fine-grained micro-control results in prohibitive control overhead of fetching configuration bits from off-chip memory. This paper presents MINISA, a minimal instruction set that programs a reconfigurable accelerator at the granularity of Virtual Neurons (VNs), the coarsest control granularity that retains flexibility of hardware and the finest granularity that avoids unnecessary control costs. First, we introduce FEATHER+, a modest refinement of FEATHER, that eliminates redundant on-chip replication needed for runtime dataflow/layout co-switching and supports dynamic cases where input and weight data are unavailable before execution for offline layout manipulation. MINISA then abstracts control of FEATHER+ into three layout-setting instructions for input, weight, and output VNs and a single mapping instruction for setting dataflow. This reduces the control and instruction footprint while preserving the legal mapping and layout space supported by the FEATHER+. Our results show that MINISA reduces geometric mean off-chip instruction traffic by factors ranging from 35x to (4x10^5)x under various sizes under 50 GEMM workloads spanning AI (GPT-oss), FHE, and ZKP. This eliminates instruction-fetch stalls that consume 96.9% of micro-instruction cycles, yielding up to 31.6x end-to-end speedup for 16x256 FEATHER+. Our code: https://github.com/maeri-project/FEATHER/tree/main/minisa.

---

## 论文详细总结（自动生成）

### MINISA：面向下一代可重构推理加速器的极简指令集架构总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代可重构AI加速器（如FEATHER）虽然能通过灵活的数据流和布局切换来适配多样化的算子（如LLM、全同态加密FHE、零知识证明ZKP），但其**控制开销（Control Overhead）**巨大。
*   **研究动机**：现有的可重构架构通常采用细粒度的微指令（Micro-coded control）来配置硬件开关和缓冲区地址。随着计算阵列规模扩大，从片外获取这些海量配置信息的带宽压力会导致严重的取指停顿（最高占总周期的96.9%），严重限制了硬件的实际吞吐量和算力密度。
*   **整体含义**：本文旨在寻找一个平衡点，既能保留硬件的重构灵活性，又能大幅压缩指令体积。

#### 2. 论文提出的方法论
*   **核心思想：虚拟神经元（Virtual Neuron, VN）抽象**。VN被定义为硬件执行点积运算的最小原子单位（即PE内部的乘累加规模）。MINISA通过在VN粒度而非单个开关粒度进行编程，实现了控制逻辑的高度压缩。
*   **关键技术细节**：
    *   **FEATHER+ 硬件改进**：在FEATHER基础上引入全对全（All-to-all）分布网络，消除了片上缓冲区的冗余数据复制，并支持动态权重（无需预先离线重排布局）。
    *   **MINISA 指令集**：仅包含8条指令。核心包括：
        *   **3条布局指令（Set*VNLayout）**：通过参数化嵌套循环顺序（如分区因子、维度排列顺序）来定义输入、权重和输出VN在缓冲区中的组织方式。
        *   **1条映射指令（ExecuteMapping）**：使用6个参数（起始索引、组大小、步长等）定义VN如何映射到PE阵列。
        *   **1条流指令（ExecuteStreaming）**：定义数据如何随时间流向阵列。
    *   **编译流程**：开发了FEATHER+ Mapper，采用“映射优先、布局随后”的搜索策略，自动将工作负载转换为MINISA指令序列。

#### 3. 实验设计
*   **数据集/场景**：涵盖了50个GEMM（通用矩阵乘法）工作负载，分为三大领域：
    1.  **AI推理**：GPT-oss (20B规模) 的注意力机制层。
    2.  **全同态加密 (FHE)**：Bootstrapping过程中的算子。
    3.  **零知识证明 (ZKP)**：数论变换 (NTT) 相关算子。
*   **Benchmark与对比方法**：
    *   **基准配置**：显式微指令控制（Micro-instruction）的FEATHER+。
    *   **行业对比**：NVIDIA RTX 5090 GPU 和 Google TPU v6e-8。
    *   **硬件规模**：从 4×4 到 16×256 的不同PE阵列规模。

#### 4. 资源与算力
*   **硬件实现**：使用 TSMC 28nm 工艺进行了逻辑综合与布局布线（PnR），评估了面积和功耗。
*   **算力对比基准**：在对比实验中，将64个FEATHER+实例（16×256规模）与8个TPU v6e核心及RTX 5090进行等功耗（约575W）对比。
*   **仿真环境**：使用了周期精确的RTL模型验证功能，并结合分析模型进行大规模性能评估。

#### 5. 实验数量与充分性
*   **实验规模**：测试了50种不同形状的矩阵乘法，覆盖了从极度不规则到高度规则的维度。
*   **消融实验**：对阵列宽度（$A_W$）和高度（$A_H$）进行了扩展性分析，验证了指令压缩率随规模增长的趋势。
*   **充分性评价**：实验设计较为充分，不仅对比了学术界的基准，还直接挑战了工业界最先进的GPU和TPU，并提供了详细的周期分解（Stall分析），客观地展示了性能提升的来源。

#### 6. 论文的主要结论与发现
*   **指令压缩**：MINISA将片外指令流量降低了35倍至40万倍（几何平均值）。
*   **性能加速**：在16×256规模下，通过消除取指停顿，实现了最高**31.6倍**的端到端加速。
*   **能效优势**：在处理不规则形状矩阵时，FEATHER+的利用率远高于传统脉动阵列（Systolic Array），相比RTX 5090和TPU v6e分别实现了23.7倍和7.8倍的几何平均加速。
*   **控制占比**：MINISA成功将指令周期占比降至0.1%以下。

#### 7. 优点
*   **抽象层级精准**：VN抽象精准捕捉了硬件灵活性与控制开销的折中点，具有很强的工程实用性。
*   **硬件开销极低**：FEATHER+相比原版FEATHER仅增加了约7%的面积，却换取了对动态工作负载的全面支持。
*   **跨领域适用性**：证明了AI加速器在经过合理重构设计后，能高效处理FHE和ZKP等非传统AI任务。

#### 8. 不足与局限
*   **算子覆盖面**：虽然GEMM是核心，但对于非线性算子或更复杂的非张量逻辑支持讨论较少。
*   **编译搜索空间**：尽管Mapper使用了启发式剪枝，但在面对极其复杂的全局布局优化时，搜索时间可能仍是一个挑战（文中提到16核并行需17分钟）。
*   **实际芯片验证**：目前结果主要基于RTL仿真和综合数据，缺乏实际流片后的实测功耗和热管理数据。

（完）
