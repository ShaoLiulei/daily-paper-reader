---
title: "Reasoner-Executor-Synthesizer: Scalable Agentic Architecture with Static O(1) Context Window"
title_zh: 推理器-执行器-合成器：具有静态 O(1) 上下文窗口的可扩展智能体架构
authors: Ivan Dobrovolskyi
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22367v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型的可扩展智能体架构
tldr: 针对大模型智能体在RAG模式下存在的幻觉风险和Token成本随数据量线性增长的问题，本文提出RES架构。该架构将任务分解为推理器、执行器和合成器三层，通过执行器进行确定性数据处理并仅向合成器传递固定大小的统计摘要。实验证明，该架构实现了O(1)的Token复杂度，在处理海量学术文献时能保持极低的固定成本，并从根本上消除了数据幻觉。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22367v1/fig-001.webp\", \"caption\": \"Figure 2. RES token cost remains constant as dataset size grows exponentially (O(1) proof).\", \"page\": 3, \"index\": 1, \"width\": 492, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2603.22367v1/fig-002.webp\", \"caption\": \"Figure 3. Log-log scaling comparison — RES O(1) vs. Naive O(n). At 16M articles, RES is 1.2 million times cheaper.\", \"page\": 3, \"index\": 2, \"width\": 492, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2603.22367v1/fig-003.webp\", \"caption\": \"Figure 4. Direct token cost comparison across all 20 query types. RES (red) vs. Naive with 50 articles (blue).\", \"page\": 4, \"index\": 3, \"width\": 492, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2603.22367v1/fig-004.webp\", \"caption\": \"Figure 5. Distribution of RES token costs across 95 runs. Tight clustering around μ=1,574 confirms predictable cost.\", \"page\": 4, \"index\": 4, \"width\": 492, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2603.22367v1/fig-005.webp\", \"caption\": \"Figure 1. Detailed RES architecture flow for a sample query. Token costs shown per layer.\", \"page\": 2, \"index\": 5, \"width\": 492, \"height\": 283}]"
motivation: 解决传统RAG架构中Token成本随数据集规模线性增长以及长上下文导致的模型幻觉问题。
method: 提出由推理器解析意图、执行器进行零Token确定性检索、合成器生成叙述的三层RES架构。
result: "在ScholarSearch测试中，无论数据集规模如何变化，平均Token消耗始终稳定在1,574个左右，实现了O(1)复杂度。"
conclusion: RES架构通过物理隔离原始数据与生成过程，在显著降低成本的同时从架构层面消除了数据幻觉。
---

## 摘要
部署为自主智能体的大语言模型（LLM）通常使用检索增强生成（RAG），将检索到的文档输入上下文窗口，这产生了两个问题：幻觉风险随上下文长度增加而增长，且 Token 成本随数据集规模线性扩展。我们提出了推理器-执行器-合成器（RES）架构，这是一种三层设计，严格分离了意图解析（推理器）、确定性数据检索与聚合（执行器）以及叙述生成（合成器）。执行器消耗零 LLM Token，且仅向合成器传递固定大小的统计摘要。我们从形式上证明了 RES 在数据集规模方面实现了 O(1) 的 Token 复杂度，并在基于 Crossref API（包含超过 1.3 亿篇文章）的学术研究助手 ScholarSearch 上验证了这一点。在 100 次基准测试运行中，无论数据集包含 4.2 万篇还是 1630 万篇文章，RES 的平均 Token 成本均为 1,574 个。该架构通过构造消除了数据幻觉：LLM 永远不会接触到原始记录。关键词：LLM 智能体；智能体架构；幻觉消除；Token 优化；上下文窗口；检索增强生成；确定性执行；学术元数据；Crossref API；O(1) 复杂度。

## Abstract
Large Language Models (LLMs) deployed as autonomous agents commonly use Retrieval-Augmented Generation (RAG), feeding retrieved documents into the context window, which creates two problems: the risk of hallucination grows with context length, and token cost scales linearly with dataset size. We propose the Reasoner-Executor-Synthesizer (RES) architecture, a three-layer design that strictly separates intent parsing (Reasoner), deterministic data retrieval and aggregation (Executor), and narrative generation (Synthesizer). The Executor uses zero LLM tokens and passes only fixed-size statistical summaries to the Synthesizer. We formally prove that RES achieves O(1) token complexity with respect to dataset size, and validate this on ScholarSearch, a scholarly research assistant backed by the Crossref API (130M+ articles). Across 100 benchmark runs, RES achieves a mean token cost of 1,574 tokens regardless of whether the dataset contains 42,000 or 16.3 million articles. The architecture eliminates data hallucination by construction: the LLM never sees raw records. KEYWORDS LLM agents; agentic architecture; hallucination elimination; token optimization; context window; retrieval-augmented generation; deterministic execution; scholarly metadata; Crossref API; O(1) complexity.

---

## 论文详细总结（自动生成）

以下是对论文《Reasoner-Executor-Synthesizer: Scalable Agentic Architecture with Static O(1) Context Window》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：当前大语言模型（LLM）智能体主要采用检索增强生成（RAG）模式。
*   **核心问题**：
    1.  **幻觉风险**：随着上下文长度增加，LLM 容易产生幻觉（如“迷失在中间”现象）。
    2.  **成本瓶颈**：Token 成本随检索到的数据集规模线性增长（$O(n)$），导致处理大规模数据（如数百万条记录）时经济成本极高。
*   **研究目标**：提出一种新型架构，使智能体在处理海量数据时，Token 消耗保持恒定，并从架构层面消除数据幻觉。

### 2. 方法论：RES 架构
RES 架构将任务严格划分为三个互不重叠的层级：
*   **推理器 (Reasoner - LLM层)**：
    *   **功能**：解析用户的自然语言查询，将其转化为结构化的查询计划（JSON 格式）。
    *   **特点**：不接触原始数据，仅负责意图识别（如趋势分析、对比、排名）。
*   **执行器 (Executor - 确定性代码层)**：
    *   **功能**：接收查询计划，通过确定性代码（如 API 调用、SQL、Python 聚合函数）在原始数据集上执行操作。
    *   **特点**：**消耗零 LLM Token**。它将海量数据处理为固定大小的统计摘要（如计数、平均值、分组统计）。
*   **合成器 (Synthesizer - LLM层)**：
    *   **功能**：接收执行器生成的固定大小统计摘要，生成人类可读的叙述性报告和可视化配置。
    *   **核心公式**：论文证明了总 Token 复杂度 $T_{RES} = O(|Q|) + 0 + O(1) = O(1)$。即无论数据集 $n$ 有多大，输入给 LLM 的 Token 数量始终是常数。

### 3. 实验设计
*   **实现系统**：**ScholarSearch**，一个基于 Python 的学术研究助手。
*   **数据集/场景**：接入 **Crossref API**，包含超过 1.3 亿篇学术文章元数据。
*   **对比基准 (Benchmark)**：
    *   **Naive RAG (朴素基准)**：将 50 条原始记录直接输入 LLM 的标准 RAG 模式。
*   **测试用例**：涵盖趋势分析、对比、排名、通用统计 4 大类，共 20 个不同查询。
*   **实验规模**：每个查询运行 5 次，共计 100 次基准测试运行。

### 4. 资源与算力
*   **模型使用**：使用了 Anthropic 的 **Claude 3.5 Sonnet** (claude-sonnet-4-20250514) 作为推理器和合成器。
*   **算力说明**：文中未明确提及具体的 GPU 硬件型号或训练时长，因为该研究侧重于**推理架构设计**而非模型训练。系统前端使用 Streamlit，数据处理依赖 Crossref 的 REST API。

### 5. 实验数量与充分性
*   **实验数量**：100 次运行，覆盖了从 4.2 万条到 1630 万条记录的不同数据规模。
*   **充分性**：实验通过对数坐标图清晰展示了 RES 架构在数据量指数级增长时 Token 消耗的平稳性（横线），与传统方法（斜线）形成鲜明对比。
*   **客观性**：实验包含了 API 超时导致的失败案例分析，并提供了标准差（$\sigma=259$），显示了结果的稳定性。

### 6. 主要结论与发现
*   **O(1) 复杂度验证**：无论处理 4.2 万还是 1630 万篇文章，平均 Token 成本稳定在 **1,574** 个。
*   **极端成本优势**：在处理 1600 万条记录时，RES 比传统 RAG 便宜约 **120 万倍**，Token 节省率达 **99.9996%**。
*   **消除幻觉**：由于 LLM 永远接触不到原始数据记录，只能基于执行器提供的确定性统计结果进行描述，从而在构造上消除了数据层面的幻觉。

### 7. 优点
*   **极高的可扩展性**：打破了 LLM 上下文窗口对大数据集分析的限制。
*   **确定性与可靠性**：将计算任务交给擅长计算的代码，将语言任务交给擅长语言的模型，分工明确。
*   **透明度**：架构允许 UI 实时显示每一层的中间输出，便于用户验证结果的真实性。

### 8. 不足与局限
*   **查询类型限制**：RES 擅长统计和分析类查询，但不适用于需要“深度阅读”单篇长文档细节的任务（如“总结某特定论文的实验方法”）。
*   **依赖底层能力**：执行器的效能取决于数据源是否支持聚合操作（如 API 的 facet 功能），否则需要在本地处理大量数据。
*   **指令遵循依赖**：推理器解析复杂查询的准确性仍受限于 LLM 本身的指令遵循能力。

（完）
