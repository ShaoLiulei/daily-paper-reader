---
title: "VolDiT: Controllable Volumetric Medical Image Synthesis with Diffusion Transformers"
title_zh: VolDiT：基于扩散 Transformer 的可控体素医学图像合成
authors: "Marvin Seyfarth, Salman Ul Hassan Dar, Yannik Frisch, Philipp Wild, Norbert Frey, Florian André, Sandy Engelhardt"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25181v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于体积医学图像合成的3D扩散Transformer
tldr: 本研究针对现有3D医疗图像合成中卷积U-Net架构存在的局部偏差和感受野受限问题，提出了VolDiT，这是首个纯基于Transformer的3D扩散模型。该模型通过体积补丁嵌入和全局自注意力机制直接处理3D数据，并引入时间步门控控制适配器，利用分割掩码实现精确的空间引导。实验证明，VolDiT在生成保真度、全局连贯性及可控性上均优于主流的U-Net架构模型，为医疗影像合成提供了更具扩展性的新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25181v1/fig-001.webp\", \"caption\": \"Table 1. Generative metrics as measured by FID, precision (P), recall (R), density (D), coverage (C), and MS-SSIM.\", \"page\": 6, \"index\": 1, \"width\": 662, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2603.25181v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the VolDiT framework. Input volumes are encoded into latents, patchified into tokens, and processed by the 3D Diffusion Transformer for denoising, with spatial conditioning injected through the timestep-gated control adapter. The transformer outputs are then unpatchified and decoded to reconstruct volumetric images.\", \"page\": 3, \"index\": 2, \"width\": 725, \"height\": 435}, {\"url\": \"assets/figures/arxiv/2603.25181v1/fig-003.webp\", \"caption\": \"Fig. 2. Synthetic examples on LUNA16 and TaviCT. HA-GAN produces anatomically implausible structures, while the U-Net LDM yields high-quality images but struggles with global anatomical consistency (red circles). VolDiT generates anatomically coherent volumes with sharper structural detail, reflecting the benefit of global self-attention.\", \"page\": 5, \"index\": 3, \"width\": 508, \"height\": 358}]"
motivation: 现有的3D医疗图像生成模型多依赖卷积U-Net架构，存在局部偏差和感受野受限的问题，难以捕捉全局上下文。
method: 提出VolDiT，首个纯Transformer架构的3D扩散模型，通过体积补丁嵌入和时间步门控控制适配器实现可控生成。
result: 在高分辨率3D医疗图像合成任务中，VolDiT在全局连贯性、生成保真度和可控性方面均优于现有的U-Net架构模型。
conclusion: 纯Transformer架构为体积医疗图像合成提供了更灵活且强大的基础，具有优越的扩展性和建模优势。
---

## 摘要
扩散模型已成为高保真医学图像合成的主流方法。然而，大多数现有的 3D 医学图像生成方法在潜扩散框架中依赖于卷积 U-Net 骨干网络。尽管这些架构有效，但它们引入了强烈的局部偏差和有限的感受野，这可能会限制可扩展性、全局上下文整合以及灵活的条件控制。在这项工作中，我们介绍了 VolDiT，这是首个纯基于 Transformer 的 3D 扩散 Transformer（Diffusion Transformer），用于体素医学图像合成。我们的方法通过体素补丁嵌入（volumetric patch embeddings）和直接在 3D token 上运行的全局自注意力机制，将扩散 Transformer 扩展到原生 3D 数据。为了实现结构化控制，我们提出了一种时间步门控控制适配器（timestep-gated control adapter），将分割掩码映射为可学习的控制 token，在去噪过程中调节 Transformer 层。这种 token 级别的条件机制在保留 Transformer 架构建模优势的同时，实现了精确的空间引导。我们在高分辨率 3D 医学图像合成任务上评估了我们的模型，并将其与基于 U-Net 的最先进 3D 潜扩散模型进行了比较。结果表明，该模型在全局一致性、生成保真度和可控性方面均有提升。我们的研究结果表明，全 Transformer 扩散模型为体素医学图像合成提供了一个灵活的基础。在公开数据上训练的代码和模型可在 https://github.com/Cardio-AI/voldit 获取。

## Abstract
Diffusion models have become a leading approach for high-fidelity medical image synthesis. However, most existing methods for 3D medical image generation rely on convolutional U-Net backbones within latent diffusion frameworks. While effective, these architectures impose strong locality biases and limited receptive fields, which may constrain scalability, global context integration, and flexible conditioning. In this work, we introduce VolDiT, the first purely transformer-based 3D Diffusion Transformer for volumetric medical image synthesis. Our approach extends diffusion transformers to native 3D data through volumetric patch embeddings and global self-attention operating directly over 3D tokens. To enable structured control, we propose a timestep-gated control adapter that maps segmentation masks into learnable control tokens that modulate transformer layers during denoising. This token-level conditioning mechanism allows precise spatial guidance while preserving the modeling advantages of transformer architectures. We evaluate our model on high-resolution 3D medical image synthesis tasks and compare it to state-of-the-art 3D latent diffusion models based on U-Nets. Results demonstrate improved global coherence, superior generative fidelity, and enhanced controllability. Our findings suggest that fully transformerbased diffusion models provide a flexible foundation for volumetric medical image synthesis. The code and models trained on public data are available at https://github.com/Cardio-AI/voldit.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **VolDiT**，这是首个专门为 3D 体积医学图像合成设计的纯 Transformer 扩散模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 医学图像生成模型（如潜扩散模型 LDM）大多依赖于 **卷积 U-Net** 架构。
*   **研究动机**：
    *   **局部偏差限制**：卷积操作具有强烈的局部偏差，难以捕捉 3D 医学影像中复杂的全局解剖结构和跨切片的一致性。
    *   **感受野受限**：U-Net 的感受野有限，且反复的下采样/上采样可能会丢失精细的解剖细节。
    *   **扩展性挑战**：在计算机视觉领域，基于 Transformer 的扩散模型（DiT）已显示出比 U-Net 更好的扩展性和生成质量，但在 3D 医疗领域尚属空白。

### 2. 方法论
*   **核心思想**：将 3D 潜空间体积数据视为一系列 Token，利用纯 Transformer 架构进行去噪，并引入时间步门控机制实现精确的条件控制。
*   **关键技术细节**：
    *   **潜体积表示**：首先使用 3D VQ-GAN 将原始医疗体积数据压缩到潜空间（压缩率为 8x8x8），所有扩散过程均在潜空间进行。
    *   **3D 补丁化（Patchification）**：将潜张量划分为 $p \times p \times p$ 的非重叠立方体补丁，通过 3D 卷积映射为 Token 序列，并加入 3D 正余弦位置编码。
    *   **3D Diffusion Transformer**：采用多层 Transformer 块处理 Token，利用全局自注意力机制建模长程空间依赖。
    *   **时间步门控控制适配器（TGCA）**：
        *   为了实现可控生成（如根据分割掩码生成图像），引入了一个轻量级适配器。
        *   **核心公式**：引入时间步相关的门控因子 $\gamma(t) = \sigma(MLP(t))$，对控制 Token 进行调制。
        *   这种设计允许模型在不同的去噪阶段动态调整控制强度，保证了解剖结构的精确对齐且不破坏预训练模型的生成能力。

### 3. 实验设计
*   **数据集**：
    *   **LUNA16**：公开的肺部 CT 数据集（888 个体积数据）。
    *   **TaviCT**：内部的心脏 CTA 扫描数据集（1002 个体积数据）。
*   **基准模型（Benchmarks）**：
    *   **U-Net LDM**：最先进的基于卷积 U-Net 的潜扩散模型。
    *   **HA-GAN**：一种高性能的非扩散生成对抗网络模型。
*   **评估指标**：
    *   **生成质量**：FID（2.5D 或 3D）、Precision（精确度）、Density（密度）。
    *   **多样性**：Recall（召回率）、Coverage（覆盖率）、MS-SSIM。
    *   **可控性**：Dice 系数、95% 豪斯多夫距离（HD95）。

### 4. 资源与算力
*   **论文说明**：文中**未明确指出**具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：实验涵盖了从 XS（1720 万参数）到 L（5.8 亿参数）的不同模型规模，并对比了补丁大小 $p=2$ 和 $p=4$ 的性能。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   **无条件合成实验**：在两个不同解剖部位（肺部、心脏）的数据集上验证了基础生成能力。
    *   **扩展性消融实验**：测试了模型深度、隐藏层维度及补丁大小对结果的影响。
    *   **条件合成实验**：在 TaviCT 数据集上验证了分割掩码引导下的生成效果。
*   **充分性评价**：实验设计较为全面，对比了 GAN 和扩散模型的主流架构。通过引入多种 3D 专用指标（如 MedicalNet 提取的特征），评估过程较为客观。但由于医疗数据集规模相对较小，模型规模的扩展性表现（Scaling Behavior）未呈现出如自然图像领域那样的严格单调提升。

### 6. 主要结论与发现
*   **性能优越**：VolDiT 在保真度（FID）和多样性指标上均优于基于 U-Net 的 LDM。
*   **全局一致性**：得益于全局自注意力机制，VolDiT 生成的解剖结构更加连贯，减少了 U-Net 常见的解剖畸变。
*   **控制精准**：提出的 TGCA 适配器在解剖对齐（Dice/HD95）方面显著优于固定强度的控制方法，且能保持更好的图像质量。
*   **架构潜力**：证明了纯 Transformer 架构可以自然地扩展到高维医疗数据，为未来的医疗大模型奠定了基础。

### 7. 优点
*   **架构创新**：首次将纯 Transformer 扩散模型引入 3D 医疗影像合成，打破了卷积架构的垄断。
*   **控制机制巧妙**：TGCA 门控机制解决了条件控制中“引导强度”与“生成质量”之间的平衡问题。
*   **开源贡献**：提供了代码和预训练模型，增强了研究的可复现性。

### 8. 不足与局限
*   **算力与数据限制**：由于医疗数据量较小，超大规模模型（L 级别）在实验中可能存在训练不足的情况，未能完全展现其潜力。
*   **评估局限**：主要集中在 CT 模态，尚未在 MRI 或超声等其他 3D 模态上进行广泛验证。
*   **计算开销**：虽然在潜空间运行，但全局自注意力在处理极高分辨率的 3D Token 序列时，计算复杂度仍是一个潜在瓶颈。

（完）
