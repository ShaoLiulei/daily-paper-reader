---
title: "Revisiting Tree Search for LLMs: Gumbel and Sequential Halving for Budget-Scalable Reasoning"
title_zh: 重新审视大语言模型的树搜索：用于预算可扩展推理的 Gumbel 与 Sequential Halving
authors: "Leonid Ugadiarov, Yuri Kuratov, Aleksandr Panov, Alexey Skrynnik"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21162v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于增强大模型推理能力的树搜索算法
tldr: 针对大语言模型在推理任务中使用AlphaZero式树搜索时出现的性能随预算增加而下降的“缩放失效”问题，本文提出了ReSCALE框架。该方法通过引入Gumbel采样和顺序减半（Sequential Halving）机制替代传统的Dirichlet噪声和PUCT选择，成功恢复了推理性能的单调增长。在GSM8K和Game24任务上，ReSCALE显著提升了高预算下的准确率，证明了其在预算可扩展推理中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21162v1/fig-001.webp\", \"caption\": \"Figure 1: Gumbel + Sequential Halving enables scaling of MCTS for reasoning. Comparison of tree-search methods for LLM reasoning on GSM8K across increasing token budgets. The standard AlphaZero-style approach plateaus and declines at higher budgets, while proposed ReSCALE decoding achieves sustained accuracy gains, demonstrating better scaling with additional compute.\", \"page\": 1, \"index\": 1, \"width\": 482, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2603.21162v1/fig-002.webp\", \"caption\": \"Figure 2: Left: A single simulation in tree search with sentence-level actions. Each simulation traverses from the root to a leaf, selecting actions via Sequential Halving with Gumbel noise at the root and an improved policy at non-root nodes. The selected leaf is expanded by sampling w actions from the LLM (πθ), each evaluated by the value network vϕ, and the resulting values are backpropagated to update ancestor statistics. Right: Sequential Halving at the root node. From M = 8 candidate actions, the budget of N = 24 simulations is split evenly across ⌈log2 M⌉ = 3 rounds. After each round, the bottom half of actions (by score gi + log p(ai) + σ(v(ai))) is eliminated, until a single winner remains.\", \"page\": 3, \"index\": 2, \"width\": 1044, \"height\": 446}, {\"url\": \"assets/figures/arxiv/2603.21162v1/fig-003.webp\", \"caption\": \"Table 1: Combined results on GSM8K and Game24. For each budget level, mean accuracy ± std is computed across hyperparameter configurations; for Best-of-N, across three random seeds. Best single-configuration accuracy (Max. Acc.) is also reported. On GSM8K, AlphaZero accuracy drops at Large budget (53.6%) while ReSCALE continues to improve (58.4%), both surpassing the Best-of-N baseline (53.4%). On Game24, AlphaZero similarly degrades at Large budget (82.9%), whereas ReSCALE continues to improve (85.3%), both outperforming the Best-of-N baseline (54.1%).\", \"page\": 4, \"index\": 3, \"width\": 1052, \"height\": 264}]"
motivation: 传统的AlphaZero式树搜索在LLM推理中存在预算增加反而导致性能下降的缩放失效问题。
method: 提出ReSCALE框架，利用Gumbel采样和顺序减半机制优化搜索过程，无需重新训练模型。
result: "在GSM8K和Game24基准测试中，ReSCALE在基准方法性能退化的预算水平下分别达到了58.4%和85.3%的准确率。"
conclusion: 顺序减半机制是恢复LLM树搜索预算可扩展性的关键，使推理性能随计算资源投入而稳定提升。
---

## 摘要
神经树搜索是一种强大的决策算法，广泛应用于博弈和基于模型的强化学习等复杂领域。近期的研究已应用 AlphaZero 式树搜索来增强大语言模型（LLMs）在推理过程中的推理能力，但我们发现这种方法存在扩展失效的问题：在 GSM8K 和 Game24 数据集上，准确率随着搜索预算的增加而下降。在本文中，我们提出了 ReSCALE，这是对 Gumbel AlphaZero MCTS 的一种改进，它将 Dirichlet 噪声和 PUCT 选择替换为 Gumbel 采样和 Sequential Halving，在不改变模型或其训练过程的情况下恢复了单调扩展性。在基准方法性能下降的预算水平下，ReSCALE 在 GSM8K 上达到了 58.4%，在 Game24 上达到了 85.3%。消融实验证实，Sequential Halving 是性能提升的主要驱动因素。

## Abstract
Neural tree search is a powerful decision-making algorithm widely used in complex domains such as game playing and model-based reinforcement learning. Recent work has applied AlphaZero-style tree search to enhance the reasoning capabilities of Large Language Models (LLMs) during inference, but we find that this approach suffers from a scaling failure: on GSM8K and Game24, accuracy drops as the search budget increases. In this paper, we present ReSCALE, an adaptation of Gumbel AlphaZero MCTS that replaces Dirichlet noise and PUCT selection with Gumbel sampling and Sequential Halving, restoring monotonic scaling without changes to the model or its training. ReSCALE reaches 58.4\% on GSM8K and 85.3\% on Game24 at budgets where the baseline degrades. Ablations confirm that Sequential Halving is the primary driver of the improvement.

---

## 论文详细总结（自动生成）

以下是对论文《Revisiting Tree Search for LLMs: Gumbel and Sequential Halving for Budget-Scalable Reasoning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 AlphaZero 式蒙特卡洛树搜索（MCTS）在应用于大语言模型（LLM）推理时，存在**“缩放失效”**（Scaling Failure）现象。即随着搜索预算（模拟次数或 Token 消耗）的增加，模型的推理准确率在达到中等水平后会停滞甚至下降，表现出“前瞻病理”（Lookahead Pathology）。
*   **研究背景**：虽然思维链（CoT）提升了推理能力，但其本质是单次尝试。树搜索被引入以实现多路径探索和回溯，但现有的基于 PUCT 算法的选择机制在高预算下无法有效转化计算资源为性能增益。

### 2. 方法论：ReSCALE 框架
ReSCALE（基于可扩展计算分配的 LLM 探索推理）核心思想是引入 **Gumbel AlphaZero** 的机制来替代传统的启发式搜索。
*   **根节点选择（Gumbel 采样 + Sequential Halving）**：
    *   **Gumbel 采样**：在根节点使用 Gumbel 噪声进行动作采样，提供更严谨的策略改进保证，替代了 AlphaZero 中常用的 Dirichlet 噪声。
    *   **顺序减半（Sequential Halving, SH）**：这是本文的关键。将总预算 $N$ 分配给 $M$ 个候选动作，通过多轮筛选，每轮淘汰表现较差的一半动作，将剩余预算集中在最有潜力的动作上。这解决了 AlphaZero 容易过度开发（Over-exploitation）局部错误路径的问题。
*   **非根节点选择**：使用改进的策略 $\pi'$，结合了先验概率和**混合价值近似（Mixed Value Approximation）**。该近似方法在节点访问次数较少时依赖价值网络预测，在访问次数增加时平滑过渡到实际观测到的平均价值。
*   **动作空间定义**：采用“句子级”动作（以换行符分隔），在保持搜索深度的同时降低了计算复杂度。

### 3. 实验设计
*   **数据集**：
    *   **GSM8K**：小学数学应用题，考察多步算术推理。
    *   **Game24**：数学规划任务，要求使用 4 个数字通过算术运算得到 24。
*   **基准模型（Backbone）**：Llama2-7B（经过 SFT 微调）。
*   **对比方法**：
    1.  **AlphaZero MCTS**：使用 PUCT 算法的传统树搜索。
    2.  **Best-of-N**：采样 N 个完整路径并使用价值模型进行重排序（Reranking）。
*   **预算分级**：将实验分为 Small、Medium、Large 三个等级（基于生成的 Token 数量）。

### 4. 资源与算力
*   **模型训练**：对 Llama2-7B 进行了 3 个 Epoch 的 SFT 训练，并训练了一个共享骨干网络的 MLP 价值头（Value Head）。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或具体的训练/推理总时长。但从实验描述看，涉及了大量的推理采样（如 Best-of-32，以及高达 18,000 Tokens 的搜索预算）。

### 5. 实验数量与充分性
*   **实验规模**：在两个代表性推理任务上进行了完整测试，每个任务涵盖了从低到高的多种预算配置。
*   **消融实验**：专门针对 Gumbel 噪声和 Sequential Halving 进行了消融，证明了 Sequential Halving 是恢复单调缩放性的核心驱动力（移除它会导致准确率下降 4.7%）。
*   **客观性**：实验采用了 3 个随机种子并报告了均值和标准差，对比了 Best-of-N 等强基准，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **恢复单调缩放**：ReSCALE 成功解决了性能退化问题。在 GSM8K 上，当 AlphaZero 性能下降至 53.6% 时，ReSCALE 持续提升至 58.4%。
*   **SH 的重要性**：Sequential Halving 通过在搜索初期保持探索多样性，有效防止了搜索陷入高访问量但错误的路径。
*   **价值近似的优化**：混合价值近似能有效纠正价值网络在未探索区域的预测偏差。

### 7. 优点
*   **即插即用**：该方法仅在推理侧（Inference-time）进行改进，无需改变原有的 LLM 架构或进行大规模重新训练。
*   **理论与实践结合**：将多臂老虎机（Multi-armed Bandit）理论中的最优策略（SH）引入 LLM 搜索，解决了实际应用中的缩放瓶颈。
*   **高效性**：在相同 Token 预算下，ReSCALE 的准确率显著优于 Best-of-N 和标准 MCTS。

### 8. 不足与局限
*   **模型规模局限**：实验仅基于 Llama2-7B，尚未在更大规模（如 70B+）或本身具备强推理能力的模型（如 o1-preview）上验证其增益。
*   **任务类型单一**：主要集中在数学和逻辑规划任务，对于创意写作、代码生成等非确定性奖励任务的适用性有待考察。
*   **推理延迟**：尽管预算可扩展，但树搜索本身带来的高 Token 消耗和多次调用 LLM 导致的推理延迟仍是实际部署的障碍。

（完）
