---
title: "Instruction-Tuned, but Not More Verifiable Instruction-Following: A Cross-Task Diagnosis for LoRA Adapters"
title_zh: 经过指令微调，但并非更具可验证性的指令遵循：LoRA 适配器的跨任务诊断
authors: Junyi Zou
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22379v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: LoRA适配器的指令遵循评估
tldr: 本研究探讨了LoRA适配器的名义标签（如“指令微调”）与其在不同任务中实际能力提升之间的不一致性。通过在IFEval等基准上测试，发现指令微调适配器往往无法提升可验证的指令遵循能力，反而可能在无关的数值任务上表现更好。这种现象被称为“能力漂移”，研究强调了在部署前进行跨任务评估的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22379v1/fig-001.webp\", \"caption\": \"Figure 2: Robustness quadrant plot (primary evidence). Per-run target gain on IFEval PLA (instr−base) versus off-target gain on numeric NM (instr−base) for the robustness evaluations underlying Table 2 and Appendix Table 6. Most points lie above y=x (positive drift), with occasional near-zero or negative cases.\", \"page\": 6, \"index\": 1, \"width\": 937, \"height\": 505}, {\"url\": \"assets/figures/arxiv/2603.22379v1/fig-002.webp\", \"caption\": \"Figure 4: Upper-layer geometry diagnostics. Top modules by geometric similarity between instructionand numeric-reasoning-tuned LoRA updates. This figure provides preliminary evidence but does not establish a localized causal mechanism.\", \"page\": 11, \"index\": 2, \"width\": 832, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2603.22379v1/fig-003.webp\", \"caption\": \"Figure 3: Selective shifts in verifiable instruction following (IFEval). Category-level and type-level shifts for the instruction adapter relative to base on IFEval. This breakdown is qualitative; low-support types should be interpreted cautiously.\", \"page\": 7, \"index\": 3, \"width\": 976, \"height\": 376}, {\"url\": \"assets/figures/arxiv/2603.22379v1/fig-004.webp\", \"caption\": \"Figure 5: Functional probing summary with null or small effects. Largest observed effect sizes from localized functional probing. We do not find strong localized causal evidence that explains the mismatch pattern in this setup.\", \"page\": 12, \"index\": 4, \"width\": 832, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2603.22379v1/fig-005.webp\", \"caption\": \"Table 6: Robustness: drift score summary (instr→numeric reasoning). Drift score mean and standard deviation across seeds (when n > 1); values are rounded to three decimals.\", \"page\": 12, \"index\": 5, \"width\": 996, \"height\": 528}, {\"url\": \"assets/figures/arxiv/2603.22379v1/fig-006.webp\", \"caption\": \"Figure 1: Drift score summary. Heatmap of DriftScore(a → b) computed from cross-task evaluation metrics. Compact overview of score magnitudes from the same evaluations; see §5 for context.\", \"page\": 5, \"index\": 6, \"width\": 832, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2603.22379v1/fig-007.webp\", \"caption\": \"Table 2: Robustness summary of drift score (instr→numeric reasoning). Drift score remains positive across five seeds (Qwen3-8B) and across multiple base models and LoRA settings, with one model/setting showing near-zero or slightly negative drift (configuration sensitivity).\", \"page\": 5, \"index\": 7, \"width\": 742, \"height\": 197}]"
motivation: 旨在验证LoRA适配器的名义训练目标（如指令微调）是否能可靠地反映其在实际应用中跨任务的能力增益。
method: 在多种基础模型和LoRA配置下，通过对比适配器在可验证指令遵循任务（IFEval）与数值基准任务上的表现来进行跨任务诊断。
result: 发现指令微调适配器常出现“能力漂移”，即在未针对的数值任务上表现大幅提升，但在名义上的指令遵循任务上却无明显改善甚至退化。
conclusion: 名义标签并非可靠的能力代理指标，建议在部署适配器前进行常规的跨任务评估以避免能力误判。
---

## 摘要
适配器通常根据名义标签（例如“指令微调”）进行选择和部署，这些标签隐含地暗示了适配后哪些能力会得到提升。我们通过在不同任务中评估同一个 LoRA 适配器，测试名义训练目标是否与实现的跨任务能力增益可靠地保持一致。我们最强有力的证据与 IFEval 衡量的严格、可自动验证的指令遵循能力相关：在多个随机种子、基座模型和 LoRA 设置下，名义标签反复（但并非普遍）无法预测这一可验证目标上的改进，且表现出明显的配置敏感性，包括接近零或负增长的情况。作为一个受控的“指令对数值”设置中的典型最强案例，一个经过指令微调的适配器显著提高了非目标 NM 数值基准测试的性能（从 0.133 提升至 0.632），但并未提高 IFEval 上的可验证指令遵循能力（ILA：0.313 降至 0.271；PLA：0.250 降至 0.143；数值保留三位小数）。我们将这种名义与实际表现不匹配的模式描述为“能力漂移”（capability drift）。这种不匹配在原始跨任务性能矩阵中清晰可见；我们使用漂移分数仅作为与基础指标单位相同的简洁总结，而非作为一种新的正式指标贡献。来自更广泛指令遵循基准测试的证据取决于具体基准且结果不一，反映了指令遵循在操作化定义上的异质性；因此，我们不将跨基准的一致性作为前提。总之，实际的启示是在部署前进行常规的跨任务评估，并避免将名义标签视为可靠的能力代理。

## Abstract
Adapters are often selected and deployed based on nominal labels (e.g., instruction-tuned), which implicitly suggest what capability improves after adaptation. We test whether nominal training objectives reliably align with realized cross-task capability gains by evaluating the same LoRA adapter across tasks. Our strongest evidence is tied to strict, automatically verifiable instruction following as measured by IFEval: across multiple seeds, base models, and LoRA settings, nominal labels recurrently but not universally fail to predict improvements on this verifiable target, with clear configuration sensitivity including a near-zero or negative case. As an illustrative strongest-case example in a controlled instruction-versus-numeric setting, an instruction-tuned adapter substantially improves off-target NM-based numeric benchmark performance from 0.133 to 0.632 while not improving verifiable instruction following on IFEval (ILA: 0.313 to 0.271; PLA: 0.250 to 0.143; values rounded to three decimals). We refer to this nominal-versus-realized mismatch pattern as capability drift as a descriptive label. The mismatch is visible in the raw cross-task performance matrix; we use a drift score only as a compact summary in the same units as the underlying metrics, not as a new formal metric contribution. Evidence from broader instruction-following benchmarks is benchmark-dependent and mixed, reflecting heterogeneity in how instruction following is operationalized; we therefore do not treat cross-benchmark agreement as a premise. Overall, the practical takeaway is to perform routine cross-task evaluation before deployment and to avoid treating nominal labels as reliable capability proxies.

---

## 论文详细总结（自动生成）

这是一份关于论文《Instruction-Tuned, but Not More Verifiable Instruction-Following: A Cross-Task Diagnosis for LoRA Adapters》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了 LoRA 适配器在部署中的一个关键假设：**名义标签（如“指令微调”）是否真实反映了适配后的能力增益？**
研究者发现，开发者通常根据训练目标（如 Instruction-tuned）来预期模型能力的提升，但在实际应用中，这种名义上的改进往往与实际表现不符。论文特别关注了“严格、可自动验证的指令遵循能力”（以 IFEval 为代表），旨在诊断适配器是否存在“能力漂移”（Capability Drift）现象，即适配器在非目标任务上的增益反而超过了目标任务。

### 2. 论文提出的方法论
论文提出了一种**跨任务诊断（Cross-Task Diagnosis）**框架，核心思想是通过对比适配器在目标任务和非目标任务上的表现来量化名义与实际的偏差。
*   **核心指标：漂移分数（Drift Score）**
    其计算逻辑如下：
    1.  **目标增益（TargetGain）**：适配器在名义目标任务（如指令遵循）上的性能提升。
    2.  **非目标增益（OffTargetGain）**：适配器在非关联任务（如数值推理）上的性能提升。
    3.  **漂移分数 = 非目标增益 - 目标增益**。
*   **诊断流程**：使用相同的 LoRA 适配器在多个任务族（数值推理、IFEval、领域 QA）上进行评估，观察性能矩阵的原始变化。

### 3. 实验设计
*   **基座模型**：包括 Qwen3-8B/14B、Llama-3.1-8B-Instruct、Qwen2.5-7B-Instruct 以及 DeepSeek-R1-Distill-Qwen-7B。
*   **适配器设置**：对比了“指令微调适配器”（instr）和“数值推理适配器”（reason）。
*   **评估基准（Benchmarks）**：
    *   **IFEval**：衡量严格的可验证指令遵循（指标：ILA 指令级准确率，PLA 提示词级准确率）。
    *   **数值推理基准**：衡量数值匹配（NM）。
    *   **补充基准**：FollowBench（规则评分）和 IFBench（严格 PLA）。
*   **对比方法**：将适配后的模型与 Base 模型进行横向对比，并跨不同随机种子、LoRA Rank（8, 16, 32）和作用模块（Attention, MLP）进行鲁棒性测试。

### 4. 资源与算力
论文**未明确说明**具体的算力细节（如 GPU 型号、数量或训练总时长）。作者提到“所有分析均重用现有的训练和评估产物”，暗示其研究重点在于对已训练适配器的评估与诊断，而非从头训练大规模模型。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 5 个随机种子（针对 Qwen3-8B）、多种基座模型架构、以及多种 LoRA 配置（Rank 和模块组合）。
*   **充分性**：实验设计较为充分且客观。作者不仅报告了平均值，还提供了标准差和鲁棒性象限图（Figure 2），展示了现象的普遍性。
*   **客观性**：作者诚实地指出了“配置敏感性”，即在某些特定模型/设置下，漂移分数可能接近零或为负，并未夸大结论的普适性。

### 6. 主要结论与发现
*   **名义标签不可靠**：指令微调适配器往往无法提升 IFEval 上的严格指令遵循能力，但在数值推理任务上却表现出显著的“意外”提升（例如 NM 从 0.133 升至 0.632，而 IFEval 性能反而下降）。
*   **能力漂移普遍存在**：在多个模型和种子下，漂移分数持续为正，表明这种名义与实际的错位是一个反复出现的现象。
*   **基准异质性**：指令遵循的提升取决于如何定义。在 FollowBench 上可能有提升，但在更严格的 IFEval/IFBench 上则没有，说明“指令遵循”并非单一能力。
*   **局部解释失效**：通过几何探测和功能干预发现，这种漂移无法简单地归因于某个特定模块或层的变化，表现出分布式特征。

### 7. 优点
*   **视角务实**：直接针对模型部署中的常见误区（盲信标签）提出警告，具有很强的工程实践指导意义。
*   **诊断严谨**：引入了受控的“指令 vs 数值”对比实验，通过漂移分数直观地揭示了模型能力的非预期转移。
*   **多维度验证**：不仅看综合分，还深入分析了 IFEval 的具体错误类型（如语言约束、格式约束等），提供了细粒度的洞察。

### 8. 不足与局限
*   **机制解释不足**：虽然识别了现象，但未能通过探测实验（Probing）给出确切的因果机制解释（即为什么指令微调会提升数值能力）。
*   **任务覆盖有限**：主要集中在指令遵循与数值推理的交叉上，对于其他能力对（如创意写作 vs 逻辑推理）的漂移情况探讨较少。
*   **指标依赖性**：漂移分数的高度依赖于所选的 Target Metric（如 PLA vs ILA），不同的聚合方式会改变分数的绝对值。

（完）
