---
title: A transformer architecture alteration to incentivise externalised reasoning
title_zh: 一种旨在激励外部化推理的 Transformer 架构改进
authors: "Elizabeth Pavlova, Mariia Koroliuk, Karthik Viswanathan, Cameron Tice, Edward James Young, Puria Radmard"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21376v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于推理的Transformer架构修改
tldr: 本研究提出了一种改进Transformer架构的方法，旨在通过引入“早期退出”机制和强化学习训练流程，促使大语言模型将推理过程外部化。通过在中间层设置退出点，模型被训练在预测简单标记时减少计算深度，而在复杂标记上保留深度计算。这种方法能有效减少模型利用内部激活进行隐式规划的冗余计算，从而鼓励模型通过显式的推理步骤来解决问题，提高推理的透明度与计算效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21376v1/fig-001.webp\", \"caption\": \"Figure 3: Distribution of coherence scores across models with different KL factors.3 As the KL factor increases, coherence distributions shift toward higher scores, approaching base model performance.\", \"page\": 10, \"index\": 1, \"width\": 664, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2603.21376v1/fig-002.webp\", \"caption\": \"Figure 2: Left: RL training dynamics on Qwen3-4B for the Theory of Mind (Sclar et al., 2024) task. Each panel shows individual runs (light lines), mean across runs (bold line with markers), and ±1 SE error bars. The dashed line indicates the pre-SFT base model performance. Step 0 represents the SFT-calibrated model before RL. Accuracy improves while average compute decreases, demonstrating that the model learns to exit earlier without sacrificing performance. Coherence remains stable throughout. Evaluated on 60 theory of mind prompts per step. Right: Token-level visualisation of early exit behaviour. Each token is coloured by the layer at which computation was terminated. The model adaptively varies computational depth, using fewer layers for predictable tokens and the full depth for more complex ones. We used λ = 1.5 and β = 0.25.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 519}]"
motivation: 旨在限制大语言模型利用内部隐藏状态进行隐式推理的能力，从而迫使其通过外部化的标记进行显式推理。
method: 在Transformer中引入中间层早期退出机制，并利用强化学习激励模型在不损失性能的前提下尽可能在浅层完成计算。
result: 初步实验证明小型推理模型能够学会根据标记难度自适应地减少计算深度，实现了计算资源的按需分配。
conclusion: 该方法通过优化计算分配，有望在保持性能的同时，将复杂的内部规划转化为更易于观察和解释的外部推理过程。
---

## 摘要
我们提出了一种新的架构变更和训练后流水线，通过教导模型提前截断前向传播，使大语言模型（LLM）成为更冗长的推理者。我们在现有 Transformer 架构的中间层增加了早停（early-exit）机制，并训练模型在无需深度计算即可预测下一个 token 时，从较浅的层退出。在校准阶段之后，我们利用强化学习激励模型在保持任务性能的同时尽可能早地退出。我们为小型推理模型提供了初步实验结果，表明它们学会了在不同 token 之间自适应地减少计算量。我们预测，在合适的规模下应用该方法，可以最大限度地减少推理模型利用其内部激活进行非近视规划（non-myopic planning）时所能支配的冗余计算量，并将其仅保留给难以预测的 token。

## Abstract
We propose a new architectural change, and post-training pipeline, for making LLMs more verbose reasoners by teaching a model to truncate forward passes early. We augment an existing transformer architecture with an early-exit mechanism at intermediate layers and train the model to exit at shallower layers when the next token can be predicted without deep computation. After a calibration stage, we incentivise the model to exit as early as possible while maintaining task performance using reinforcement learning. We provide preliminary results to this effect for small reasoning models, showing that they learn to adaptively reduce computations across tokens. We predict that, applied at the right scale, our approach can minimise the amount of excess computation that reasoning models have at their disposal to perform non-myopic planning using their internal activations, reserving this only for difficult-to-predict tokens.

---

## 论文详细总结（自动生成）

这篇论文提出了一种创新的 Transformer 架构改进方案，旨在通过技术手段强制大语言模型（LLM）将推理过程“外部化”，从而提高模型的可解释性和安全性。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的推理模型（如 OpenAI o1, DeepSeek-R1）虽然能生成思维链（CoT），但存在“不忠实”或“隐匿推理”（Obfuscation）的风险。模型可能利用其深层网络的内部激活进行复杂的隐式规划，而输出的 CoT 只是表象，这使得安全监控变得困难。
*   **研究动机**：为了确保模型的推理过程对人类透明，研究者希望限制模型在单个 token 生成时的内部计算量，迫使模型在遇到复杂问题时必须通过增加外部 token（即 CoT）来完成推理，而不是在“黑盒”内部消化。

### 2. 方法论
该方法包含架构修改和两个训练阶段：
*   **架构修改**：
    *   在 Transformer 的中间层（每 5 层）添加轻量级的**早期退出头（Early-exit heads）**。
    *   使用 **LoRA 适配器**微调现有权重。
    *   **随机退出机制**：模型在每一层会预测一个退出概率，并据此决定是继续计算还是直接跳过剩余层进入输出层（Unembedding）。
*   **关键技术流程**：
    1.  **阶段一：自蒸馏校准（Self-distillation SFT）**：
        *   将原始全深度模型作为教师，带退出头的模型作为学生。
        *   利用中间层预测与最终层预测之间的 **KL 散度**作为启发式信号，训练退出头识别哪些 token 在浅层即可准确预测。
    2.  **阶段二：强化学习激励（RLOO）**：
        *   使用改进的 RLOO 算法，奖励函数定义为：$R_{total} = R_{task} - \lambda \cdot (\text{平均退出层索引}) - \beta \cdot D_{KL}$。
        *   通过参数 $\lambda$ 调节模型对“早退”的渴望程度，强制模型在保证任务准确率的前提下，尽可能减少每 token 的计算深度。

### 3. 实验设计
*   **实验对象**：DeepSeek-R1-Distill-Qwen-1.5B 和 Qwen3-4B（小型推理模型）。
*   **数据集/场景**：
    *   **GSM8K**：数学文字题推理。
    *   **Theory of Mind (ToM)**：心理理论推理数据集（使用 ExploreToM 生成）。
*   **Benchmark 与对比**：主要对比 SFT 前后的基座模型性能，以及不同 $\lambda$（退出压力）和 KL 因子下的模型表现。
*   **评估指标**：任务准确率（Accuracy）、回答连贯性（Coherence）、平均计算量（ALS，节省的层数比例）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **实现细节**：提到了使用了 LoRA 进行高效微调，这暗示了该方法在算力受限的情况下具有一定的可行性。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同架构的模型（DeepSeek 和 Qwen）上验证了方法。
*   **充分性评价**：
    *   作为**概念验证（Proof of Concept）**，实验展示了 RL 训练动态、token 级别的退出行为可视化以及准确率与计算量的权衡曲线。
    *   **局限性**：实验主要集中在小型模型（1.5B-4B），且仅覆盖了两个特定任务领域。对于大规模模型（如 70B+）和更广泛任务的泛化性尚未验证。

### 6. 主要结论与发现
*   **自适应计算**：模型学会了根据 token 难度分配计算资源。例如，预测简单的词汇（如“walkie-talkie”中的“-talk”）时会极早退出，而复杂推理步骤则保留深层计算。
*   **性能提升**：在 Qwen3-4B 上的实验显示，经过 RL 训练，模型在减少平均计算深度的同时，任务准确率反而从 47% 提升到了 55-60%，超过了基座模型。
*   **可调控性**：通过调节 $\lambda$ 参数，可以像拨盘一样控制模型推理的“外部化”程度。

### 7. 优点
*   **创新的安全视角**：不同于传统的 CoT 提示词工程，该方法从底层架构和计算资源分配的角度强制实现推理透明化。
*   **计算效率**：在实现安全目标的同时，客观上减少了推理时的计算开销（FLOPs）。
*   **端到端训练**：通过自蒸馏和 RL 的结合，使模型能够自主学习最优的退出策略，而非人工硬编码规则。

### 8. 不足与局限
*   **忠实度验证缺失**：论文虽然实现了“早退”，但尚未通过实验严格证明这种做法是否真的提高了 CoT 的忠实度（Faithfulness）。
*   **规模效应未知**：在超大规模模型上，这种架构改动是否会破坏模型涌现出的复杂能力尚存疑。
*   **质量权衡**：过于激进的早期退出（低 KL 因子）会导致模型生成的连贯性大幅下降。
*   **跨领域泛化**：目前仅在数学和心理推理任务上测试，缺乏对通用对话或代码生成等领域的验证。

（完）
