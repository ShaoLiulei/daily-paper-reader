---
title: "KARMA: Knowledge-Action Regularized Multimodal Alignment for Personalized Search at Taobao"
title_zh: KARMA：面向淘宝个性化搜索的知识-动作正则化多模态对齐
authors: "Zhi Sun, Wenming Zhang, Yi Wei, Liren Yu, Zhixuan Zhang, Dan Ou, Haihong Tang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22779v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 针对个性化任务的大语言模型微调与语义对齐
tldr: 针对淘宝个性化搜索中大语言模型（LLM）微调导致的“知识-行为鸿沟”和语义坍缩问题，本文提出KARMA框架。该框架通过引入语义重构作为训练正则项，在优化检索兴趣嵌入的同时，利用历史条件生成和嵌入条件重构来保持LLM的语义解码能力。实验证明，KARMA有效缓解了语义坍缩，显著提升了淘宝搜索的点击率和召回率，并已成功上线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22779v1/fig-001.webp\", \"caption\": \"Figure 1: KARMA architecture: A train-only regularizer that bridges the Knowledge–Action Gap.\", \"page\": 2, \"index\": 1, \"width\": 500, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2603.22779v1/fig-002.webp\", \"caption\": \"Table 1: Impact of decodability regularization (Δ over actiononly Pairwise-CE).\", \"page\": 4, \"index\": 2, \"width\": 506, \"height\": 279}]"
motivation: 直接在个性化任务上微调LLM会导致预训练语义知识与特定行为目标冲突，引发语义坍缩并限制泛化能力。
method: 提出KARMA框架，将语义重构作为正则化项，通过历史条件生成和嵌入条件重构确保兴趣嵌入具备语义可恢复性。
result: "在淘宝搜索系统中，KARMA显著提升了点击率（+0.25% AUC）和召回率（+2.51% HR），并带来0.5%的在线点击增长。"
conclusion: 通过在训练中平衡语义保持与行为对齐，KARMA成功解决了LLM在工业级个性化搜索中的应用瓶颈。
---

## 摘要
大语言模型（LLMs）具备深厚的语义知识，使其成为为个性化搜索系统注入语义泛化能力的自然选择。然而，在实践中我们发现，直接在工业级个性化任务（如下一商品预测）上微调 LLM 往往效果不佳。我们将这一瓶颈归因于关键的“知识-动作鸿沟”（Knowledge-Action Gap）：即保留预训练语义知识与通过判别式目标对齐特定个性化动作之间的内在冲突。经验表明，仅针对动作的训练目标会导致“语义崩溃”（Semantic Collapse），例如出现“注意力汇聚”（attention sinks）现象。这种退化严重削弱了 LLM 的泛化能力，无法为个性化搜索系统带来提升。我们提出了 KARMA（知识-动作正则化多模态对齐），这是一个将语义重构视为仅训练期正则化项的统一框架。KARMA 在优化用于检索的下一兴趣嵌入（动作）的同时，通过两个互补目标强制执行语义可解码性（知识）：(i) 基于历史的语义生成，将优化锚定在 LLM 原生的下一标记（next-token）分布上；(ii) 基于嵌入的语义重构，约束兴趣嵌入保持语义可恢复性。在淘宝搜索系统中，KARMA 缓解了语义崩溃（通过注意力汇聚分析验证），并提升了动作指标和语义保真度。在消融实验中，语义可解码性带来了高达 +22.5 的 HR@200 提升。通过 KARMA，我们在精排阶段实现了 +0.25 的 CTR AUC 提升，在粗排阶段实现了 +1.86 的 HR 提升，在召回阶段实现了 +2.51 的 HR 提升。该模型已在线部署，精排阶段推理开销较低，带动商品点击率（Item Click）增长了 +0.5%。

## Abstract
Large Language Models (LLMs) are equipped with profound semantic knowledge, making them a natural choice for injecting semantic generalization into personalized search systems. However, in practice we find that directly fine-tuning LLMs on industrial personalized tasks (e.g. next item prediction) often yields suboptimal results. We attribute this bottleneck to a critical Knowledge--Action Gap: the inherent conflict between preserving pre-trained semantic knowledge and aligning with specific personalized actions by discriminative objectives. Empirically, action-only training objectives induce Semantic Collapse, such as attention ``sinks''. This degradation severely cripples the LLM's generalization, failing to bring improvements to personalized search systems.   We propose KARMA (Knowledge--Action Regularized Multimodal Alignment), a unified framework that treats semantic reconstruction as a train-only regularizer. KARMA optimizes a next-interest embedding for retrieval (Action) while enforcing semantic decodability (Knowledge) through two complementary objectives: (i) history-conditioned semantic generation, which anchors optimization to the LLM's native next-token distribution, and (ii) embedding-conditioned semantic reconstruction, which constrains the interest embedding to remain semantically recoverable.   On Taobao search system, KARMA mitigates semantic collapse (attention-sink analysis) and improves both action metrics and semantic fidelity. In ablations, semantic decodability yields up to +22.5 HR@200. With KARMA, we achieve +0.25 CTR AUC in ranking, +1.86 HR in pre-ranking and +2.51 HR in recalling. Deployed online with low inference overhead at ranking stage, KARMA drives +0.5% increase in Item Click.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **KARMA** 的框架，旨在解决大语言模型（LLM）在工业级个性化搜索应用中的“语义坍缩”问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在个性化搜索中，直接微调 LLM 进行“下一商品预测”往往效果不佳。作者发现这是由于**“知识-动作鸿沟”（Knowledge-Action Gap）**导致的：即 LLM 预训练的语义知识与基于用户行为反馈的判别式目标（Action）之间存在冲突。
*   **语义坍缩（Semantic Collapse）**：仅使用行为数据训练会导致模型走捷径（Shortcut Learning），注意力机制会退化为“条形码状”的注意力汇聚（Attention Sinks），使得模型丧失语义泛化能力，在长尾查询和冷启动场景下表现糟糕。

### 2. 方法论：KARMA 框架
KARMA 的核心思想是将**语义重构**作为“仅训练期有效”的正则化项，确保生成的兴趣嵌入（Embedding）既能对齐用户行为，又具备语义可解释性。

*   **连续标记接口（Continuous-Token Interface）**：为了工业效率，将历史商品压缩为嵌入向量输入 LLM 编码器。
*   **动作对齐目标（Action Alignment）**：使用成对交叉熵损失（Pairwise CE）优化点击行为预测。
*   **知识正则化（双路径解码）**：
    *   **任务 1：历史条件语义生成**。根据用户历史直接预测目标商品的文本 Token，将优化锚定在 LLM 原生的 Token 分布上。
    *   **任务 2：嵌入条件语义重构**。强制要求模型生成的“下一兴趣嵌入” $h_t$ 能够重新解码出目标商品的文本。这为嵌入向量设置了语义瓶颈，防止其变成无意义的 ID 类标识符。
*   **多模态扩展**：引入扩散模型（Diffusion/Flow-matching）头，利用兴趣嵌入重构商品的视觉语义特征。
*   **推理优化**：解码头仅在训练时存在，推理时直接使用嵌入向量进行向量检索，实现**零额外推理开销**。

### 3. 实验设计
*   **数据集与场景**：在**淘宝搜索**的大规模匿名日志上进行实验，涵盖召回（Recalling）、粗排（Pre-ranking）和精排（Ranking）三个阶段。
*   **基准测试（Benchmark）**：以仅使用行为监督（Action-only）的 Pairwise-CE 训练方案为 Baseline。
*   **对比指标**：
    *   **动作指标**：gAUC（排序能力）、HR@K（召回率）。
    *   **语义指标**：JS@50（Jaccard 相似度），用于探测检索结果与真实目标的语义重合度。
*   **消融实验**：分别验证了任务 1、任务 2 以及多模态重构对性能的贡献。

### 4. 资源与算力
*   **模型规格**：使用了 Qwen3 系列模型，包括 0.6B（用户解码器）、1.7B（文本编码器）和 2B（多模态编码器）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。但考虑到其工业级背景和模型参数量，通常涉及大规模 GPU 集群训练。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了离线消融实验、注意力图可视化分析、不同生成目标（MSE vs Diffusion）的对比实验，以及为期 14 天的在线 A/B 测试。
*   **充分性评价**：实验设计较为充分。通过 JS@50 指标和注意力图可视化，客观地证明了“语义坍缩”的存在及 KARMA 的缓解作用。在线 A/B 测试验证了该方法在真实高并发工业场景下的有效性。

### 6. 主要结论与发现
*   **嵌入重构是关键**：任务 2（从嵌入重构语义）对缓解坍缩、提升召回率的作用远大于任务 1。
*   **注意力修复**：KARMA 成功消除了“注意力汇聚”现象，使模型注意力分布更均匀、更聚焦于语义信息。
*   **扩散模型的角色**：扩散模型适合作为语义正则化项，但在生成检索嵌入时不如简单的 AR+MSE 目标（因为检索需要“均值寻求”而非“众数寻求”）。
*   **业务提升**：在淘宝上线后，精排 CTR AUC 提升 0.25，召回 HR 提升 2.51，在线商品点击量增长 0.5%。

### 7. 优点
*   **解决痛点**：精准识别并解决了 LLM 在推荐/搜索领域微调时的语义退化问题。
*   **部署友好**：通过“训练期正则、推理期移除”的设计，实现了模型能力的增强而无需增加任何在线推理延迟。
*   **多模态融合**：巧妙地将视觉特征重构整合进正则化框架中。

### 8. 不足与局限
*   **算力细节缺失**：缺乏具体的训练资源消耗数据，外部研究者难以评估复现成本。
*   **场景局限**：实验主要集中在淘宝电商搜索，对于非电商类（如新闻、短视频）个性化任务的泛化性有待进一步验证。
*   **超参数敏感性**：正则化项的权重 $\lambda$ 对平衡“知识”与“动作”至关重要，论文未详细讨论其在不同规模数据下的调优难度。

（完）
