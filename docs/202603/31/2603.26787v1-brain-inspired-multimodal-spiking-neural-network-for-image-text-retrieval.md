---
title: Brain-Inspired Multimodal Spiking Neural Network for Image-Text Retrieval
title_zh: 用于图文检索的类脑多模态脉冲神经网络
authors: "Xintao Zong, Xian Zhong, Wenxuan Liu, Jianhao Ding, Zhaofei Yu, Tiejun Huang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.26787v1"
tags: ["query:snn"]
score: 10.0
evidence: 用于图文检索的多模态脉冲神经网络
tldr: 针对现有图文检索方法在跨模态交互、检索延迟和能效方面的不足，本文提出了一种类脑跨模态脉冲融合网络（CMSF）。这是首个直接训练用于图文检索的多模态脉冲神经网络（SNN）。CMSF通过脉冲级融合机制生成增强的多模态表示，并将其作为软监督信号优化单模态嵌入，在极低能耗和高检索速度下实现了超越主流人工神经网络的性能，为多模态SNN研究提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26787v1/fig-001.webp\", \"caption\": \"Table 2. Results on MSCOCO 5K Test Set. Best results are in bold, and the second-best scores are underlined.\", \"page\": 7, \"index\": 1, \"width\": 492, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2603.26787v1/fig-002.webp\", \"caption\": \"Figure 9. Visualization of retrieval results. Top: image-to-text retrieval examples. Bottom: text-to-image retrieval examples by CMSF on FLICKR30K.\", \"page\": 14, \"index\": 2, \"width\": 929, \"height\": 624}, {\"url\": \"assets/figures/arxiv/2603.26787v1/fig-003.webp\", \"caption\": \"Figure 2. Brain-Inspired Multimodal Spiking Neural Network for Image-Text Retrieval. (a) Overview of CMSF. (b) Working principle of the Spike Generator. (c,d) Details of Intra-modal Attention blocks. (e) One implementation of the Spike Fusion module. Pre-extracted region and word features are converted into spikes for unimodal semantic modeling within a sparse embedding space. The Spike Fusion module performs spike-level cross-modal interaction, generating enhanced embeddings as soft labels to guide unimodal encoders. This stage is excluded during inference. Bidirectional Hard-Alignment computes the fine-grained similarity matrix, while Early-Alignment and Soft-Label Alignment optimize unimodal encoders from both input and output perspectives. Spike Comb Cross Attention (SCCA) is detailed in §3.5, with additional fusion variants in the Supplementary Material A.\", \"page\": 3, \"index\": 3, \"width\": 1036, \"height\": 508}, {\"url\": \"assets/figures/arxiv/2603.26787v1/fig-004.webp\", \"caption\": \"Figure 3. Illustration of Our Bidirectional Hard Alignment. It identifies fine-grained region-word hard matches and integrates them to compute the overall image-text similarity.\", \"page\": 4, \"index\": 4, \"width\": 496, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2603.26787v1/fig-005.webp\", \"caption\": \"Figure 4. Details of Our Spike Comb Cross Attention Structure. With the mask operation, it achieves lower time complexity and reduced energy consumption.\", \"page\": 5, \"index\": 5, \"width\": 496, \"height\": 272}, {\"url\": \"assets/figures/arxiv/2603.26787v1/fig-006.webp\", \"caption\": \"Figure 5. Illustration of Spike Fusion Soft-Label Alignment Strategy. The left part represents the similarity computation involved in both training and inference; the right part is training-only.\", \"page\": 5, \"index\": 6, \"width\": 496, \"height\": 268}]"
motivation: 现有基于人工神经网络的图文检索方法往往追求复杂的单模态语义，却忽视了跨模态交互效率、检索延迟以及能效问题。
method: 提出CMSF网络，通过脉冲级融合机制整合特征，并利用生成的多模态表示作为软监督信号来精炼单模态脉冲嵌入。
result: CMSF仅需两个时间步即可达到顶尖检索精度，在显著降低能耗和提升速度的同时，性能超越了现有的主流人工神经网络模型。
conclusion: 该工作成功将直接训练的SNN应用于图文检索领域，实现了时间动力学与跨模态对齐的统一，为低功耗多模态研究开辟了新路径。
---

## 摘要
脉冲神经网络 (SNNs) 近期在单模态视觉和文本任务中展现出强大潜力，但为图文检索 (ITR) 等多模态应用构建直接训练、低能耗且高性能的 SNN 仍极具挑战。现有的基于人工神经网络 (ANN) 的方法通常利用更深、更复杂的架构来追求更丰富的单模态语义，却忽视了跨模态交互、检索延迟和能效。为解决这些局限性，我们提出了一种类脑跨模态脉冲融合网络 (CMSF)，并首次将其应用于图文检索。所提出的脉冲融合机制在脉冲层面整合单模态特征，生成增强的多模态表示，并将其作为软监督信号来优化单模态脉冲嵌入，从而有效缓解了 CMSF 内部的语义损失。尽管仅需两个时间步，CMSF 仍实现了顶级的检索精度，在保持极低能耗和高检索速度的同时，超越了最先进的 ANN 对手。这项工作标志着向多模态 SNN 迈出了重要一步，提供了一个将时间动力学与跨模态对齐相统一的类脑框架，并为未来基于脉冲的多模态研究提供了新见解。代码可在 https://github.com/zxt6174/CMSF 获取。

## Abstract
Spiking neural networks (SNNs) have recently shown strong potential in unimodal visual and textual tasks, yet building a directly trained, low-energy, and high-performance SNN for multimodal applications such as image-text retrieval (ITR) remains highly challenging. Existing artificial neural network (ANN)-based methods often pursue richer unimodal semantics using deeper and more complex architectures, while overlooking cross-modal interaction, retrieval latency, and energy efficiency. To address these limitations, we present a brain-inspired Cross-Modal Spike Fusion network (CMSF) and apply it to ITR for the first time. The proposed spike fusion mechanism integrates unimodal features at the spike level, generating enhanced multimodal representations that act as soft supervisory signals to refine unimodal spike embeddings, effectively mitigating semantic loss within CMSF. Despite requiring only two time steps, CMSF achieves top-tier retrieval accuracy, surpassing state-of-the-art ANN counterparts while maintaining exceptionally low energy consumption and high retrieval speed. This work marks a significant step toward multimodal SNNs, offering a brain-inspired framework that unifies temporal dynamics with cross-modal alignment and provides new insights for future spiking-based multimodal research. The code is available at https://github.com/zxt6174/CMSF.

---

## 论文详细总结（自动生成）

### 论文总结：用于图文检索的类脑多模态脉冲神经网络 (CMSF)

#### 1. 核心问题与研究动机
图文检索（ITR）旨在实现图像与文本之间的跨模态匹配。目前主流方法依赖深度人工神经网络（ANN），虽然精度高，但存在以下问题：
*   **高能耗与高延迟**：复杂的注意力机制和深层架构导致计算开销巨大。
*   **忽视生物启发性**：人类大脑在处理多模态信息时具有极高的能效和时间动力学特性，而传统 ANN 难以模拟这种脉冲驱动的稀疏处理模式。
*   **SNN 多模态挑战**：脉冲神经网络（SNN）虽节能，但在多模态任务中面临表示能力受限（稀疏性导致信息丢失）和缺乏高效跨模态融合机制的难题。

#### 2. 方法论：CMSF 架构与关键技术
论文提出了 **CMSF (Cross-Modal Spike Fusion)** 框架，其核心思想是模拟大脑皮层从单模态编码到高阶语义整合的分层处理机制。

*   **单模态脉冲嵌入 (Unimodal Spike Embedding)**：
    *   使用**脉冲生成器 (Spike Generator)** 将浮点特征转换为时空脉冲序列。
    *   采用**脉冲自注意力 (SSA)** 和**脉冲门控 MLP (SG-MLP)** 建模模态内语义。
*   **跨模态脉冲融合 (Cross-Modal Spike Fusion)**：
    *   提出 **SCCA (Spike Comb Cross Attention)**：受生物启发，通过“梳状”掩码操作在脉冲层面进行跨模态交互。
    *   **软标签对齐策略**：融合模块仅在训练阶段使用，生成增强的多模态表示作为“软标签”，引导单模态编码器学习，缓解 SNN 的信息损失。
*   **双向硬对齐 (Bidirectional Hard Alignment)**：
    *   在推理阶段，通过计算区域-单词间的双向最大相似度外积，强化细粒度匹配，并使用 LogSumExp 池化得到全局相似度。
*   **早期对齐 (Early Alignment)**：在输入端对浮点特征进行初步对齐，防止脉冲传播过程中的差异放大。

#### 3. 实验设计
*   **数据集**：使用了图文检索领域的两大标准基准数据集：**MSCOCO** (1K 和 5K 测试集) 和 **Flickr30K** (1K 测试集)。
*   **对比方法**：对比了多种基于 Faster R-CNN + BiGRU/BERT 的经典及前沿 ANN 模型，如 SCAN, VSE++, VSE∞, NAAF, CHAN, HREM, USER, MaxMatch 等。
*   **评价指标**：Recall@K (K=1, 5, 10) 以及 R@Sum（六项召回率之和）。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 GPU (24GB)**。
*   **软件框架**：基于 **SpikingJelly** 脉冲神经网络框架实现。
*   **训练细节**：训练 35 个 epochs，初始学习率 $5 \times 10^{-4}$。
*   **时间步**：极短的时间步设置（$T=2$），确保了低延迟。

#### 5. 实验数量与充分性
论文进行了详尽的实验验证：
*   **性能对比**：在两个数据集的多个子集上进行了全面测试，结果显示 CMSF 在 R@Sum 指标上多次达到最优。
*   **消融实验**：针对双向硬对齐、早期对齐、脉冲融合机制、时间步长（T=1~4）、脉冲生成器设计、梳状头数（h）等关键组件均做了独立验证。
*   **能效分析**：对比了理论能耗（mJ）和推理速度（ms）。
*   **充分性评价**：实验设计客观、公平（在相同硬件和特征提取器下对比），消融实验覆盖了模型的所有核心创新点，论证充分。

#### 6. 主要结论与发现
*   **高性能与低能耗并存**：CMSF 在精度上超越了许多复杂的 ANN 模型，同时理论能耗比同类注意力模型（如 HREM）降低了约 **78%**。
*   **脉冲融合的有效性**：通过跨模态脉冲融合产生的软标签能显著补偿 SNN 因稀疏性带来的语义损失。
*   **极速推理**：仅需 2 个时间步即可实现稳定收敛和高效检索，推理速度优于现有的高性能 ANN 模型。

#### 7. 优点与亮点
*   **首创性**：这是首个直接训练用于图文检索任务的多模态 SNN 框架。
*   **生物启发性强**：SCCA 机制和分层对齐策略很好地模拟了大脑处理多模态信息的逻辑。
*   **训练/推理解耦**：跨模态融合模块仅在训练时存在，不增加推理负担，保证了检索的轻量化。
*   **双向硬对齐**：有效解决了图文数据中区域与单词匹配不一致的问题。

#### 8. 不足与局限
*   **特征提取依赖**：模型仍依赖预训练的 Faster R-CNN 和 BERT 提取初始浮点特征，并非全流程的端到端脉冲处理（虽然这是目前 ITR 领域的通用做法）。
*   **硬件限制**：目前的能效优势主要基于理论计算（45nm 芯片参数），在通用 GPU 上由于缺乏对位运算的底层优化，实际运行速度提升受限，需在神经形态芯片上才能发挥最大潜力。
*   **超参数敏感性**：如时间步 $T$ 和梳状头数 $h$ 对性能有一定影响，需针对不同数据集进行微调。

（完）
