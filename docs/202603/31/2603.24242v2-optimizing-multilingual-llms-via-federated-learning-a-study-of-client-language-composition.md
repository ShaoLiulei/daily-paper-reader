---
title: "Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition"
title_zh: 通过联邦学习优化多语言大语言模型：客户端语言组成的研究
authors: "Aleix Sant, Jordi Luque, Carlos Escolano"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24242v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型的多语言指令微调与联邦学习
tldr: 本研究探讨了多语言环境下联邦学习（FL）优化大语言模型（LLM）的挑战，重点分析了客户端语言组成对模型性能的影响。研究扩展了FederatedScope-LLM框架，并提出局部动态早停机制（LDES-FL）以提升训练效率。实验表明，增加客户端内的语言多样性可显著提升全局模型的性能与公平性，尤其利好低资源语言，尽管这会增加优化成本。该研究为多语言联邦学习的设计提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24242v2/fig-001.webp\", \"caption\": \"Table 4: Training cost normalized by the higheststep configuration, Local FT (multilingual), which requires 1.83× 105 total optimization steps. Each value is reported as a fraction of this reference.\", \"page\": 7, \"index\": 1, \"width\": 415, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2603.24242v2/fig-002.webp\", \"caption\": \"Table 3: Results for the multilingual test set of the server. R and Fb stand for ROUGE-L (Lin, 2004) and Fbert (Zhang et al., 2020) respectively. The language in columns corresponds to the monolingual part of the test set. Mean reports the average score across all languages, whereas σ denotes the corresponding standard deviation, used here as an indicator of multilingual fairness. A bootstrap test with 100 sets (p-value < 0.05) confirms that all federated models achieve statistically significant improvements over the Base Model on all evaluated metrics and languages, as indicated by an asterisk (*).\", \"page\": 7, \"index\": 2, \"width\": 943, \"height\": 250}]"
motivation: 针对多语言联邦学习中客户端语言分布不均和资源差异导致的模型性能不稳定及训练效率低下等问题。
method: 扩展了FederatedScope-LLM框架，并引入一种根据客户端验证表现自动暂停或恢复训练的局部动态早停机制（LDES-FL）。
result: 增加客户端内部的语言多样性能显著提升全局模型的质量和公平性，并有效缩小与中心化多语言微调之间的差距。
conclusion: 客户端语言组成是多语言联邦学习的关键设计变量，直接决定了模型的最终性能、公平性以及训练成本。
---

## 摘要
在多语言环境中，大语言模型（LLMs）的联邦学习（FL）面临着重大挑战，这些挑战源于客户端之间异构的语言分布以及语言资源可用性的差异。为了应对这些挑战，我们扩展了 FederatedScope-LLM 框架，以支持 LLMs 的多语言指令微调实验。我们还引入了一种新型的客户端特定早停机制——局部动态早停（LDES-FL），该机制允许客户端根据客户端侧的验证性能暂停和恢复本地训练，从而提高了训练效率和可持续性。通过一系列实验，我们研究了客户端语言组成（从完全单语言到日益增加的多语言客户端）如何影响多语言质量、公平性和训练成本。研究发现，单语言本地微调对于单语言专业化仍然最为有效，而联邦训练则更适合学习单个平衡的多语言模型。在联邦学习中，增加客户端内的多语言性可以产生更强大且更公平的全局模型，缩小与集中式多语言微调的差距，并为低资源语言带来最大的收益，尽管这是以增加优化步骤为代价的。总之，我们的研究结果表明，客户端语言组成是多语言联邦学习中的一个关键设计变量，它决定了模型的性能、公平性和效率。

## Abstract
Federated Learning (FL) of Large Language Models (LLMs) in multilingual environments presents significant challenges stemming from heterogeneous language distributions across clients and disparities in language resource availability. To address these challenges, we extended the FederatedScope-LLM framework to support multilingual instruction-tuning experiments with LLMs. We also introduced a novel client-specific early stopping mechanism, Local Dynamic Early Stopping (LDES-FL), which allows clients to pause and resume local training based on client-side validation performance, enhancing training efficiency and sustainability. Through a series of experiments, we studied how client language composition - from fully monolingual to increasingly multilingual clients - affects multilingual quality, fairness and training cost. Monolingual local fine-tuning remains the most effective for single-language specialization, whereas federated training is better suited to learning a single balanced multilingual model. In FL, increasing within-client multilinguality leads to stronger and fairer global models, narrows the gap to centralized multilingual fine-tuning, and yields the largest gains for lower-resource languages, albeit at the cost of more optimization steps. Overall, our results identify client language composition as a key design variable in multilingual FL, shaping performance, fairness and efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**多语言环境下大语言模型（LLM）的联邦学习（FL）优化**。
*   **核心问题**：在联邦学习中，客户端的数据通常是高度异构的（Non-IID），尤其是在多语言场景下，每个客户端可能仅持有特定语言的数据。这种语言分布的极度倾斜会导致“客户端漂移（Client Drift）”，使全局模型难以收敛或在不同语言间表现不均。
*   **研究动机**：探讨“客户端内部的语言组成”（即客户端是纯单语言还是多种语言混合）如何影响多语言 LLM 的微调性能、语言间的公平性以及训练的计算成本。

### 2. 方法论：核心思想与关键技术
论文主要提出了两个技术贡献：
*   **LDES-FL（局部动态早停机制）**：
    *   **核心思想**：允许客户端根据其私有验证集的表现自主决定暂停或恢复训练。
    *   **流程**：客户端监控本地验证损失。若连续 $P_{max}$ 轮无提升则停止更新并向服务器发送其历史最佳模型；若后续下载的全局模型在本地验证集上表现有所改善，客户端会动态“复活”并重新参与训练。
    *   **意义**：相比全局统一早停，LDES-FL 实现了个性化收敛，减少了无效计算。
*   **多语言联邦微调框架**：
    *   基于 `FederatedScope-LLM` 扩展，支持多语言指令微调。
    *   采用 **LoRA（低秩自适应）** 进行参数高效微调（PEFT），仅更新约 1%-2% 的参数，降低通信开销。
    *   使用 **FedAvg** 算法进行权重聚合。

### 3. 实验设计
*   **数据集**：使用 Alpaca Cleaned 的多语言版本，涵盖 8 种语言（英语、西班牙语、德语、加泰罗尼亚语、丹麦语、塞尔维亚语、克罗地亚语、巴斯克语）。
*   **模型基础（Backbone）**：`salamandra-2b-instruct`（一个 20 亿参数的多语言指令微调模型）。
*   **实验场景（Benchmark）**：
    *   **6 种联邦配置**：从 100% mono（全单语言客户端）到 15% mono（高度混合的多语言客户端），系统性改变异构程度。
    *   **对比基准**：
        1.  **Base Model**：未微调的原始模型。
        2.  **Local FT (Monolingual)**：各语言独立进行本地微调（代表单语言专业化上限）。
        3.  **Local FT (Multilingual)**：中心化多语言微调（代表隐私不敏感场景下的性能上限）。
*   **评估指标**：ROUGE-L 和 BERTScore ($F_{bert}$)，并以标准差 ($\sigma$) 衡量多语言公平性。

### 4. 资源与算力
*   **模型参数**：2B 参数量，LoRA 配置为 $r=16, \alpha=32$。
*   **训练细节**：每个客户端每轮执行 160 个 mini-batch 步骤，微批次大小为 2，梯度累积 16 步（有效 Batch Size 为 32）。
*   **算力说明**：论文**未明确提及具体的 GPU 型号和数量**。但提供了优化步骤的统计：中心化多语言微调约需 $1.83 \times 10^5$ 步，而最便宜的联邦配置（100% mono）仅需 $1.12 \times 10^4$ 步。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 种语言的独立实验、6 种不同异构程度的联邦实验以及中心化对比实验，总计超过 15 组大型微调实验。
*   **充分性与客观性**：
    *   使用了 **Bootstrap Test**（100 组样本，p-value < 0.05）来验证改进的统计显著性。
    *   控制变量严格：所有联邦设置中每个客户端的训练样本总数保持一致（48,960 条），排除了数据量差异的干扰。
    *   **公平性考虑**：通过标准差分析不同资源等级语言（高、中、低资源）的获益情况，实验设计较为全面。

### 6. 主要结论与发现
*   **语言组成的影响**：增加客户端内部的语言多样性（使数据更趋向 IID）能显著提升全局模型的平均质量和公平性，缩小与中心化训练的差距。
*   **低资源语言获益最大**：随着客户端多语言化程度提高，低资源语言（如巴斯克语、塞尔维亚语）的性能提升幅度远高于高资源语言。
*   **性能与成本的权衡**：高度多语言化的客户端虽然效果更好，但由于减少了过早陷入局部最优的风险，其收敛所需的优化步骤也更多（15% mono 的成本是 100% mono 的 6.8 倍）。
*   **LDES-FL 的有效性**：该机制在不损失性能的前提下，相比标准早停法减少了约 22%-32% 的计算量。

### 7. 优点（亮点）
*   **机制创新**：LDES-FL 解决了联邦学习中客户端因数据差异导致的收敛步调不一问题，具有很强的实用性。
*   **系统性研究**：首次深入探讨了“客户端语言组成”这一特定变量对多语言 LLM 联邦学习的影响，填补了该领域的空白。
*   **公平性视角**：不仅关注平均分，还深入分析了跨语言的性能偏差，对构建普惠 AI 具有参考价值。

### 8. 不足与局限
*   **数据重叠风险**：实验使用的多语言 Alpaca 数据集是通过翻译生成的，虽然不是严格的平行语料，但可能存在任务模式的重叠，导致跨语言迁移效果被高估。
*   **模型规模限制**：实验仅在 2B 规模的模型上进行，尚不确定相关结论是否能完美迁移到 70B 或更大规模的模型。
*   **理想化假设**：实验假设每个客户端的数据量相等，但在现实世界中，不同语言的客户端往往存在严重的数据量不平衡（Quantity Skew），论文未对此进行建模。
*   **提示词偏差**：虽然翻译了提示词模板，但不同语言对提示词的敏感度差异可能影响最终评分。

（完）
