---
title: Corruption-robust Offline Multi-agent Reinforcement Learning From Human Feedback
title_zh: 抗污染的离线多智能体人类反馈强化学习
authors: "Andi Nika, Debmalya Mandal, Parameswaran Kamalaruban, Adish Singla, Goran Radanović"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28281v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 基于人类反馈的多智能体强化学习 (MARLHF)
tldr: 本研究探讨了离线多智能体人类反馈强化学习（MARLHF）在数据受损情况下的鲁棒性。针对强污染模型，论文在多智能体线性马尔可夫博弈框架下，分别在均匀覆盖和单边覆盖假设下提出了鲁棒估计器，以应对对抗性数据污染。研究不仅给出了纳什均衡差距的理论界限，还针对计算效率问题提出了准多项式时间的粗相关均衡（CCE）算法，填补了该领域在对抗性鲁棒性方面的研究空白。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28281v1/fig-001.webp\", \"caption\": \"Table 1: Summary of our bounds for: (i) NE gap minimization under uniform coverage, (ii) NE gap minimization under unilateral coverage, and (iii) CCE gap minimization under unilateral coverage. Here, Õ hides any poly-logarithmic factors, n denotes the number of agents, H denotes the horizon and d denotes the dimension. Moreover, ξR and ξP denote the uniform coverage coefficients, while CR and CP denote the unilateral coverage coefficients; ϵ denotes the corruption parameter, m is the data size, T1 is the number of gradient steps while T2 is the number of steps for which OptimisticHedge is run. It is worth mentioning that our bounds have optimal dependence on ϵ in the uniform coverage setting, while maintaining the same dependence as the single-agent (and two-player zero-sum Markov game) settings under non-uniform coverage. Moreover, note that the algorithm for CCE approximation is also computationally efficient.\", \"page\": 3, \"index\": 1, \"width\": 810, \"height\": 211}]"
motivation: 旨在解决离线多智能体人类反馈强化学习中，部分偏好数据可能被恶意篡改而导致学习策略失效的问题。
method: 基于线性马尔可夫博弈框架，针对不同数据覆盖假设设计了鲁棒估计器，并引入准多项式时间算法来优化计算效率。
result: "在均匀覆盖下实现了 O(ε^{1-o(1)}) 的纳什均衡差距，在单边覆盖下实现了 O(√ε) 的差距。"
conclusion: 本文首次系统性地处理了离线 MARLHF 中的对抗性数据污染问题，并提供了理论保证与高效算法。
---

## 摘要
我们研究了在强污染模型下，离线多智能体人类反馈强化学习（MARLHF）对数据污染的鲁棒性。给定一个由轨迹-偏好元组组成的数据集 $D$（每个偏好是一个 $n$ 维二值标签向量，代表 $n$ 个智能体中每一个的偏好），其中 $ε$ 比例的样本可能被任意污染。我们使用线性马尔可夫博弈框架对该问题进行建模。首先，在均匀覆盖假设下（即所有感兴趣的策略在干净数据（污染前）中都有充分的体现），我们引入了一种鲁棒估计器，保证纳什均衡间隙（Nash equilibrium gap）具有 $O(ε^{1 - o(1)})$ 的界。接下来，我们转向更具挑战性的单边覆盖（unilateral coverage）设定，即仅覆盖纳什均衡及其单玩家偏离。在这种情况下，我们提出的算法实现了 $O(\sqrtε)$ 的纳什间隙界。然而，这两项程序都面临计算不可行的问题。为了解决这个问题，我们将解概念放宽为粗相关均衡（CCE）。在相同的单边覆盖机制下，我们推导出一个拟多项式时间算法，其 CCE 间隙规模为 $O(\sqrtε)$。据我们所知，这是对离线 MARLHF 中对抗性数据污染的首次系统性研究。

## Abstract
We consider robustness against data corruption in offline multi-agent reinforcement learning from human feedback (MARLHF) under a strong-contamination model: given a dataset $D$ of trajectory-preference tuples (each preference being an $n$-dimensional binary label vector representing each of the $n$ agents' preferences), an $ε$-fraction of the samples may be arbitrarily corrupted. We model the problem using the framework of linear Markov games. First, under a uniform coverage assumption - where every policy of interest is sufficiently represented in the clean (prior to corruption) data - we introduce a robust estimator that guarantees an $O(ε^{1 - o(1)})$ bound on the Nash equilibrium gap. Next, we move to the more challenging unilateral coverage setting, in which only a Nash equilibrium and its single-player deviations are covered. In this case, our proposed algorithm achieves an $O(\sqrtε)$ bound on the Nash gap. Both of these procedures, however, suffer from intractable computation. To address this, we relax our solution concept to coarse correlated equilibria (CCE). Under the same unilateral coverage regime, we derive a quasi-polynomial-time algorithm whose CCE gap scales as $O(\sqrtε)$. To the best of our knowledge, this is the first systematic treatment of adversarial data corruption in offline MARLHF.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：抗污染的离线多智能体人类反馈强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
本文聚焦于**离线多智能体人类反馈强化学习（MARLHF）**在面临**对抗性数据污染**时的鲁棒性问题。
*   **背景**：RLHF 已成为微调大语言模型（LLM）的主流方法，但在自动驾驶、分布式系统和战略市场等现实场景中，往往涉及多个决策者的交互（即多智能体环境）。
*   **动机**：离线数据集极易受到“数据投毒”攻击（如恶意篡改偏好标签），攻击者只需污染一小部分（$\epsilon$ 比例）数据，就可能诱导模型产生偏见或有害输出。
*   **核心挑战**：多智能体环境具有战略复杂性，智能体之间的策略相互耦合，导致单智能体鲁棒方法难以直接迁移。此外，如何在仅有部分数据覆盖（非均匀覆盖）的情况下保证学习到的纳什均衡（NE）质量，是一个重大的理论难题。

#### 2. 论文提出的方法论
论文在**线性马尔可夫博弈（Linear Markov Games）**框架下，针对不同数据覆盖强度提出了三种主要的鲁棒算法流程：

*   **核心思想**：遵循“鲁棒奖励估计 $\Rightarrow$ 鲁棒 Q 函数估计 $\Rightarrow$ 估计间隙最小化”的流水线。
*   **关键技术细节**：
    *   **偏好模型**：采用 Bradley-Terry (BT) 模型对智能体偏好进行建模。
    *   **均匀覆盖下的 NE 学习**：使用 `TrimmedMLE`（修剪极大似然估计）鲁棒地恢复奖励参数，结合 `RobEst`（鲁棒均值估计器）进行后向归纳，证明其纳什间隙界限为 $O(\epsilon^{1-o(1)})$，达到了近乎线性的最优速率。
    *   **单边覆盖下的 NE 学习**：在仅覆盖纳什策略及其单方偏离的更苛刻条件下，利用投影梯度上升（PGA）优化奖励参数。由于无法直接获取真实奖励梯度，作者引入了一种基于行为策略特征差异的**偏置梯度替代方案**。其纳什间隙界限为 $O(\sqrt{\epsilon})$。
    *   **计算高效的 CCE 学习**：针对 NE 计算不可行（NP-hard）的问题，将解概念放宽为**粗相关均衡（CCE）**。在每个阶段博弈中使用 `OptimisticHedge`（乐观对冲算法）进行无悔学习，从而在拟多项式时间内获得 $O(\sqrt{\epsilon})$ 的 CCE 间隙保证。

#### 3. 实验设计
**特别说明**：本论文是一篇**纯理论研究论文（Theoretical Paper）**。
*   **数据集/场景**：文中没有使用真实的物理数据集或模拟环境进行实证实验。
*   **Benchmark 与对比**：其“实验”体现为严密的数学证明。对比对象是现有的单智能体鲁棒 RLHF 理论界限（如 Mandal et al. 2025）以及非鲁棒的多智能体离线学习界限。
*   **验证方式**：通过 33 页的数学推导和引理证明，验证了在 $\epsilon$-污染模型下算法的收敛速率和误差界限。

#### 4. 资源与算力
*   由于本文属于理论推导，**文中未提及任何关于 GPU 型号、数量、训练时长或具体算力消耗的信息**。其算法复杂度分析主要集中在样本复杂度和计算复杂度的理论量级（如拟多项式时间）。

#### 5. 实验数量与充分性
*   **不适用**：由于缺乏实证实验，无法评估实验组数或消融实验的充分性。
*   **理论充分性**：从理论角度看，论文涵盖了从“均匀覆盖”到“单边覆盖”、从“纳什均衡”到“粗相关均衡”的多个维度，论证过程严谨，涵盖了多智能体学习的核心挑战，具有很强的理论完备性。

#### 6. 论文的主要结论与发现
*   **首次系统性处理**：这是首个针对离线 MARLHF 对抗性数据污染问题的系统性理论研究。
*   **误差界限确立**：证明了在均匀覆盖下可以实现近乎线性的鲁棒性（$O(\epsilon)$ 级别），而在更现实的单边覆盖下可以实现 $O(\sqrt{\epsilon})$ 的鲁棒性。
*   **计算可行性**：通过引入 CCE 概念和 `OptimisticHedge` 算法，解决了多智能体博弈中纳什均衡计算复杂度过高的问题，提供了拟多项式时间的解决方案。
*   **梯度替代的有效性**：发现即使在奖励函数未知且数据受损的情况下，利用特征期望的偏置估计仍能有效引导策略优化。

#### 7. 优点（亮点）
*   **理论前沿性**：填补了 MARLHF 在安全性和鲁棒性研究领域的空白。
*   **创新的梯度处理**：提出了一种在无需真实奖励函数梯度的情况下，利用平衡结构构造可用梯度替代物的方法，这对离线博弈学习具有启发意义。
*   **严谨的界限分析**：对不同覆盖条件下的误差界限给出了精确的量级描述，与单智能体最优界限保持了一致性。

#### 8. 不足与局限
*   **缺乏实证验证**：完全依赖理论证明，没有在实际的多智能体环境（如星际争霸、自动驾驶模拟器）中验证算法的实际表现。
*   **线性假设限制**：研究基于线性马尔可夫博弈，对于具有高度非线性特征的复杂环境（如深度强化学习直接处理原始像素），该理论的适用性尚待研究。
*   **计算开销**：虽然 CCE 算法是拟多项式时间的，但在智能体数量或动作空间巨大时，计算开销依然可能非常显著。
*   **覆盖假设依赖**：算法性能高度依赖于数据覆盖假设（如单边覆盖），如果离线数据质量极差（完全未覆盖最优路径），算法将失效。

（完）
