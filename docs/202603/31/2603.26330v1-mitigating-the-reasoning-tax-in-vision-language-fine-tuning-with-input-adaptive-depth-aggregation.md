---
title: Mitigating the Reasoning Tax in Vision-Language Fine-Tuning with Input-Adaptive Depth Aggregation
title_zh: 通过输入自适应深度聚合缓解视觉-语言微调中的推理税
authors: "Yiming Ren, Yujiu Yang, Junjie Wang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26330v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 视觉指令数据的监督微调
tldr: 视觉语言模型（VLM）在指令微调过程中常面临感知能力提升但推理能力下降的“推理税”问题。本文研究发现，推理能力的退化与深度表征访问受阻有关。为此，作者提出了输入自适应深度聚合（IADA），这是一种轻量级的跨深度检索机制，具有输入自适应、模态感知和低秩参数化的特点。在Qwen3-VL-2B上的实验表明，IADA仅需极少额外参数即可显著提升推理和感知得分，有效缓解了微调带来的性能权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26330v1/fig-001.webp\", \"caption\": \"Table 4: Ablation on query design and modality conditioning. Group I compares fixed and adaptive queries under different conditioning strategies. Group II isolates modality-specific contributions. Input adaptivity provides the dominant gain, while modality-specific conditioning offers additional but smaller improvements. Full benchmark results are reported in Appendix.\", \"page\": 7, \"index\": 1, \"width\": 830, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2603.26330v1/fig-002.webp\", \"caption\": \"Table 9: Complete benchmark results for all fully evaluated configurations on Qwen3-VL-2B. Scores are reported in percentage points; MME is normalized to the 0–100 range. Rows are sorted by overall average (All-Avg) in descending order. Best results in each column are shown in bold; ties are bolded.\", \"page\": 14, \"index\": 2, \"width\": 826, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2603.26330v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of IADA. Compared with LoRA fine-tuning in (a), IADA in (b) inserts an IADA block at block boundaries to enable explicit cross-depth routing. IADA generates modality-specific queries from the current hidden states, retrieves information from cached states at previous nd block boundaries, and injects the resulting aggregated residuals back through gated residual addition.\", \"page\": 3, \"index\": 3, \"width\": 826, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2603.26330v1/fig-004.webp\", \"caption\": \"Table 8: Supplementary results for additional architectural variants discussed in Section 4.3.5. All configurations use LoRA rank 16 as the backbone unless otherwise noted. We report only variants with complete evaluation results.\", \"page\": 13, \"index\": 4, \"width\": 829, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2603.26330v1/fig-005.webp\", \"caption\": \"Table 6: Extended design exploration beyond the default IADA configuration. Sequence-level routing, a linear bottleneck, and a globally learned gate remain preferable in our setting. Even under extreme compression (r = 4), IADA still improves over LoRA-only.\", \"page\": 8, \"index\": 5, \"width\": 718, \"height\": 183}]"
motivation: 探究视觉指令微调导致推理能力下降是否源于模型对跨深度表征访问的破坏。
method: 提出一种名为IADA的轻量级机制，通过低秩瓶颈实现输入自适应且模态感知的跨深度信息聚合。
result: 在Qwen3-VL-2B模型上，IADA以仅0.14M的参数增量使推理平均分提升9.5分，感知平均分提升3.3分。
conclusion: 保持对跨深度表征的访问是缓解VLM微调中推理性能损失的关键因素。
---

## 摘要
在视觉指令数据上进行有监督微调（SFT）通常会提升视觉-语言模型（VLM）的感知能力，但会降低推理性能，从而在训练后阶段产生持续的推理税。我们研究了这种退化是否与深度表征访问受阻有关，并发现即使是固定的跨深度聚合也能显著恢复推理能力，这表明保留跨深度访问是 VLM 微调中一个重要的缺失因素。基于这一观察，我们提出了输入自适应深度聚合（IADA），这是一种轻量级机制，通过低秩瓶颈使跨深度检索具备输入自适应性、模态感知性，并实现高效的参数化。在 Qwen3-VL-2B 上，相比于仅使用 LoRA 的微调，IADA 仅增加了 0.14M 额外参数，就将平均推理分数提高了 9.5 分，平均感知分数提高了 3.3 分，且在参数高效的低秩设置下提升最为显著。

## Abstract
Supervised fine-tuning (SFT) on visual instruction data often improves perceptual capabilities in vision-language models (VLMs) while degrading reasoning performance, creating a persistent reasoning tax during post-training. We investigate whether this degradation is related to disrupted access to depth-wise representations, and find that even fixed cross-depth aggregation substantially restores reasoning, suggesting that preserved cross-depth access is an important missing factor in VLM fine-tuning. Building on this observation, we propose Input-Adaptive Depth Aggregation (IADA), a lightweight mechanism that makes cross-depth retrieval input-adaptive, modality-aware, and efficiently parameterized through a low-rank bottleneck. On Qwen3-VL-2B, IADA improves the average reasoning score by 9.5 points and the average perception score by $3.3$ points over LoRA-only fine-tuning with only 0.14M additional parameters, with the strongest gains appearing in parameter-efficient low-rank settings.

---

## 论文详细总结（自动生成）

以下是对论文《Mitigating the Reasoning Tax in Vision-Language Fine-Tuning with Input-Adaptive Depth Aggregation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉语言模型（VLM）在进行有监督微调（SFT）时，普遍存在“推理税”（Reasoning Tax）现象——即模型在提升感知能力（如OCR、图像识别）的同时，其推理能力（如数学、科学推理）往往会显著下降。
*   **研究动机**：作者假设这种退化是因为微调破坏了模型对预训练阶段形成的“深度特征层级”的访问。通常，浅层编码感知模式，深层编码抽象推理。统一的微调可能导致模型无法有效检索到深层推理相关的表征。
*   **研究意义**：通过恢复跨深度的信息流，旨在不牺牲感知增益的前提下，挽回甚至提升模型的推理性能。

### 2. 论文提出的方法论：IADA
**核心思想**：在 Transformer 块边界插入轻量级的“输入自适应深度聚合”（IADA）模块，显式地建模跨层信息路由。

**关键技术细节与流程**：
1.  **输入自适应查询生成**：对当前隐藏状态进行平均池化，通过一个低秩瓶颈（Low-rank Bottleneck，由 $W_{down}$ 和 $W_{up}$ 组成）生成查询向量 $q$。这使得路由决策取决于具体的输入内容。
2.  **模态感知路由**：利用模态掩码分离视觉和文本标记，分别为两种模态维护独立的路由流。这承认了视觉和文本在深度处理模式上的差异。
3.  **跨深度检索**：使用交叉注意力机制（Cross-Attention），以生成的查询 $q$ 从之前所有块边界缓存的隐藏状态中检索信息。
4.  **低秩门控注入**：将检索到的聚合残差通过一个可学习的门控（初始化为接近0）注入回当前隐藏状态。
5.  **参数高效**：整个模块极其轻量，仅需约 0.14M 额外参数。

### 3. 实验设计
*   **基座模型**：Qwen3-VL-2B-Instruct。
*   **数据集**：在 LLaVA-Instruct-Mix（259K 样本）上进行微调。
*   **Benchmark**：
    *   **推理类（5个）**：MMMU, MathVista, ScienceQA, MMStar, AI2D。
    *   **感知类（6个）**：MME, POPE, RealWorldQA, ChartQA, TextVQA, OCRBench。
*   **对比方法**：
    *   Pre-trained（未微调基准）。
    *   LoRA-only（标准微调）。
    *   AttnRes + LoRA（固定查询的深度聚合）。
    *   IADA + LoRA（本文方法）。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA H200 GPU**。
*   **训练细节**：训练 1 个 epoch，共 16,198 个优化步数，有效 Batch Size 为 16。
*   **精度与优化**：采用 bf16 混合精度训练，开启了梯度检查点（Gradient Checkpointing）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 11 个主流多模态评测基准，实验数据较为全面。
*   **消融实验**：作者进行了多维度的消融研究，包括：
    *   固定查询 vs. 自适应查询。
    *   模态共享 vs. 模态分离路由。
    *   LoRA 秩（Rank）对 IADA 效果的影响。
    *   瓶颈秩（Bottleneck Rank）的大小选择。
    *   序列级路由 vs. 标记级路由。
*   **客观性**：通过对比“固定聚合”这一诊断性基准，有力证明了“跨深度访问”本身的重要性，而非仅仅是增加了参数量。实验设计逻辑严密，对比公平。

### 6. 论文的主要结论与发现
*   **推理税的确认**：标准 LoRA 微调使 Qwen3-VL-2B 的平均推理得分下降了 5.1 分。
*   **跨深度访问的关键性**：即使是简单的固定深度聚合也能恢复大部分推理损失，证明了“访问受阻”是推理税的核心诱因。
*   **IADA 的优越性**：IADA 在推理平均分上比 LoRA-only 提升了 **9.5 分**，感知分提升了 **3.3 分**。
*   **低秩的高效性**：仅需 Rank-16 的极小瓶颈即可捕捉绝大部分有用的路由行为，增加秩并不一定带来持续提升。
*   **与 PEFT 的互补性**：IADA 在低秩 LoRA 设置下增益最明显，说明它是对现有参数高效微调方法的结构性补充。

### 7. 优点（亮点）
*   **视角独特**：从结构化的“跨层信息流”角度而非单纯的“训练稳定性”角度解释推理退化。
*   **极其轻量**：0.14M 的参数增量几乎可以忽略不计，但带来了显著的性能飞跃。
*   **即插即用**：作为一种结构补充，可以轻松集成到现有的 Transformer 架构和 PEFT 流程中。
*   **模态感知设计**：针对 VLM 特性设计的模态分离路由，比通用的深度聚合更符合多模态处理逻辑。

### 8. 不足与局限
*   **模型覆盖面**：实验仅在 Qwen3-VL-2B 这一种架构上验证，尚未在更大规模（如 7B, 72B）或其他架构（如 InternVL）上广泛测试。
*   **模态分离的局限**：依赖于固定的模态掩码，在模型深层（视觉和文本表征高度融合后）这种分离可能不再是最优的。
*   **评估维度**：主要集中在 Benchmark 分数，缺乏对模型生成质量、长文本对话或自由形式交互的定性分析。
*   **数据依赖**：仅使用了一个指令微调数据集，其在不同分布数据下的泛化能力有待观察。

（完）
