---
title: Efficient Encrypted Computation in Convolutional Spiking Neural Networks with TFHE
title_zh: 基于 TFHE 的卷积脉冲神经网络高效加密计算
authors: "Longfei Guo, Pengbo Li, Ting Gao, Yonghai Zhong, Haojie Fan, Jinqiao Duan"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.26781v1"
tags: ["query:snn"]
score: 10.0
evidence: 具有加密计算能力的卷积脉冲神经网络
tldr: 针对AI数据隐私问题，本文提出FHE-DiCSNN框架，利用TFHE全同态加密方案实现卷积脉冲神经网络（SNN）的加密计算。该框架利用SNN的离散特性，通过自举技术在密文上实现了LIF神经元模型，支持任意深度的网络。结合卷积方法和并行计算，该方案在保持高精度的同时显著提升了计算效率，为隐私保护下的深度学习提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26781v1/fig-001.webp\", \"caption\": \"Table 1. Different θ and τ parameters on the MNIST dataset (%)\", \"page\": 12, \"index\": 1, \"width\": 670, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2603.26781v1/fig-002.webp\", \"caption\": \"Table 2. Different θ and τ parameters on the FashionMNIST dataset (%)\", \"page\": 12, \"index\": 2, \"width\": 670, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2603.26781v1/fig-003.webp\", \"caption\": \"Fig. 1. Schematic visualisation of the CSNN network. For the network, we use a convolutional layer with kernal_size = 3, stridge = 1,padding=1, and an average pooling layer with kernal_size = 2, stridge = 2, padding=0.\", \"page\": 6, \"index\": 3, \"width\": 725, \"height\": 211}]"
motivation: 传统全同态加密难以处理连续非多项式函数，而脉冲神经网络的离散特性与加密计算天然契合。
method: 基于TFHE方案构建FHE-DiCSNN框架，利用自举技术实现密文LIF神经元，并结合卷积与并行计算技术进行加速。
result: "在MNIST和FashionMNIST上精度损失低于3%，单次预测时间少于1秒，并成功应用于医学图像分类。"
conclusion: 该研究证明了在全同态加密环境下高效运行深度卷积脉冲神经网络的可行性，平衡了数据隐私与计算性能。
---

## 摘要
随着人工智能技术的飞速发展，数据隐私问题日益受到关注，这促使了关于加密计算机器学习的前沿研究。全同态加密（FHE）是隐私保护计算的一项关键技术，但由于其在离散整数上运行且仅支持加法和乘法，在处理连续非多项式函数时面临挑战。脉冲神经网络（SNNs）使用离散脉冲信号，自然地补充了 FHE 的特性。在本文中，我们介绍了 FHE-DiCSNN，这是一个基于 TFHE 方案构建的框架，利用 SNN 的离散特性进行安全高效的计算。通过利用自举（bootstrapping）技术，我们成功地在密文上实现了漏电积分发放（LIF）神经元模型，从而允许构建任意深度的 SNN。我们的框架可适配其他脉冲神经元模型，为 SNN 的同态评估提供了一种新颖的方法。此外，我们集成了受 CNN 启发的卷积方法，以提高准确性并减少与随机编码相关的模拟时间。并行计算技术进一步加速了自举操作。在 MNIST 和 FashionMNIST 数据集上的实验结果验证了 FHE-DiCSNN 的有效性，与明文相比，准确率损失分别小于 3%，且每次预测的计算时间不到 1 秒。我们还将该模型应用于实际的医学图像分类问题，并分析了参数优化与选择。

## Abstract
With the rapid advancement of AI technology, we have seen more and more concerns on data privacy, leading to some cutting-edge research on machine learning with encrypted computation. Fully Homomorphic Encryption (FHE) is a crucial technology for privacy-preserving computation, while it struggles with continuous non-polynomial functions, as it operates on discrete integers and supports only addition and multiplication. Spiking Neural Networks (SNNs), which use discrete spike signals, naturally complement FHE's characteristics. In this paper, we introduce FHE-DiCSNN, a framework built on the TFHE scheme, utilizing the discrete nature of SNNs for secure and efficient computations. By leveraging bootstrapping techniques, we successfully implement Leaky Integrate-and-Fire (LIF) neuron models on ciphertexts, allowing SNNs of arbitrary depth. Our framework is adaptable to other spiking neuron models, offering a novel approach to homomorphic evaluation of SNNs. Additionally, we integrate convolutional methods inspired by CNNs to enhance accuracy and reduce the simulation time associated with random encoding. Parallel computation techniques further accelerate bootstrapping operations. Experimental results on the MNIST and FashionMNIST datasets validate the effectiveness of FHE-DiCSNN, with a loss of less than 3\% compared to plaintext, respectively, and computation times of under 1 second per prediction. We also apply the model into real medical image classification problems and analyze the parameter optimization and selection.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **FHE-DiCSNN** 的框架，旨在通过全同态加密（FHE）技术实现卷积脉冲神经网络（CSNN）的高效、隐私保护计算。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着AI隐私保护需求增加，全同态加密（FHE）成为保护数据隐私的关键工具。然而，传统的全同态加密方案（如CKKS、TFHE）在处理人工神经网络（ANN）中的非线性激活函数（如ReLU、Sigmoid）时效率极低，因为FHE原生仅支持整数的加法和乘法。
*   **核心动机**：脉冲神经网络（SNN）作为第三代神经网络，其核心组件——漏电积分发放（LIF）神经元模型本质上是基于离散脉冲信号的。这种离散特性与FHE的离散运算逻辑天然契合。
*   **研究目的**：利用TFHE方案的自举（Bootstrapping）特性，在密文域内高效实现SNN的非线性操作，解决隐私保护深度学习中的计算效率与精度平衡问题。

### 2. 方法论
*   **核心思想**：将卷积神经网络（CNN）的特征提取能力与SNN的离散脉冲处理能力结合，构建离散化卷积脉冲神经网络（DiCSNN），并利用TFHE的“可编程自举”技术实现密文神经元动力学。
*   **关键技术细节**：
    *   **离散化处理（DiCSNN）**：通过缩放因子 $\theta$ 将网络权重转换为整数，以适配FHE的整数环运算。
    *   **密文LIF神经元实现**：利用TFHE的自举过程（原本用于消除噪声）来同时计算非线性函数。论文提出了 `FHE-Fire`（判断是否发放脉冲）和 `FHE-Reset`（重置膜电位）两个密文函数。
    *   **多级缩放方法**：解决了平均池化层中的除法运算在FHE中难以实现的问题，通过调整后续层的缩放因子来抵消除法影响。
    *   **卷积编码优化**：放弃了高延迟的随机泊松编码，改用卷积层提取特征并生成脉冲，将模拟时间步（Time-steps）从几十个缩减至 2-4 个，大幅提升速度。
*   **算法流程**：输入图像加密为LWE密文 $\rightarrow$ 密文权重求和（WeightSum） $\rightarrow$ 密文LIF神经元处理（自举计算Fire/Reset） $\rightarrow$ 迭代 $T$ 个时间步 $\rightarrow$ 解密输出分类结果。

### 3. 实验设计
*   **数据集**：
    1.  **MNIST**：标准手写数字识别。
    2.  **FashionMNIST**：复杂的服装分类任务。
    3.  **Brain Tumor Dataset**：真实的医学脑肿瘤图像分类（4类），用于验证实际应用价值。
*   **Benchmark（基准）**：对应的明文CSNN模型。
*   **对比实验**：
    *   对比了不同衰减参数 $\tau$ 和缩放因子 $\theta$ 对精度的影响。
    *   对比了基于泊松编码的SNN与基于卷积编码的CSNN在计算开销上的差异。

### 4. 资源与算力
*   **软件实现**：使用 C++ 编写，基于 **TFHE 开源库**。
*   **加速技术**：采用了 FFT（快速傅里叶变换）加速多项式乘法，并利用多线程并行技术加速自举操作。
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 CPU/GPU 的具体型号及核心数）以及模型训练的总时长。但在推理阶段，给出了单次预测时间小于 1 秒的性能数据。

### 5. 实验数量与充分性
*   **实验规模**：针对三个数据集进行了详尽的参数网格搜索（$\theta$ 取值 20-100，$\tau$ 取值 2.0-∞）。
*   **充分性评价**：实验设计较为充分。不仅在标准学术数据集上验证了性能，还引入了医疗影像这一对隐私极度敏感的实际场景。论文还通过理论证明（Theorem 2）给出了噪声增长和信息空间边界的分析，增强了实验结果的说服力。

### 6. 主要结论与发现
*   **精度表现**：FHE-DiCSNN 在 MNIST 上达到 98.00% 准确率（损失 < 1%），在 FashionMNIST 上达到 87.29%（损失 < 3%）。
*   **效率提升**：相比于传统的泊松编码加密SNN，该框架通过减少模拟时间步，使计算效率提升了约 **10 倍**。
*   **深度支持**：由于自举技术在每一层都会刷新噪声，该框架理论上支持构建**任意深度**的加密神经网络，而不会导致噪声爆炸。

### 7. 优点
*   **架构契合度高**：巧妙地利用了 SNN 的离散脉冲特性来适配 FHE 的计算约束，避开了 ANN 在加密域处理非线性激活函数的难题。
*   **推理速度快**：单次密文预测时间控制在 1 秒以内，在同类全同态加密推理研究中属于领先水平。
*   **实用性强**：在医疗数据集上的成功应用证明了其在隐私计算领域的落地潜力。

### 8. 不足与局限
*   **精度仍有差距**：尽管损失较小，但在复杂数据集（如 FashionMNIST）上相比明文仍有约 3% 的差距，在医疗数据上差距扩大到 5.4%。
*   **模型复杂度受限**：实验中使用的网络结构相对简单（较浅的卷积层），尚未在大规模深度网络（如 ResNet 等）或更复杂的数据集（如 CIFAR-10/ImageNet）上进行验证。
*   **参数敏感性**：缩放因子 $\theta$ 的选择对结果影响较大，若选择不当易导致数据溢出或精度大幅下降，缺乏自动化的参数选择机制。

（完）
