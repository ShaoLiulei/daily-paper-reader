---
title: Stabilizing Iterative Self-Training with Verified Reasoning via Symbolic Recursive Self-Alignment
title_zh: 通过符号递归自对齐利用验证推理稳定迭代自训练
authors: Xinyu Zhang
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21558v1"
tags: ["query:ai-llm"]
score: 8.0
evidence: 通过符号验证稳定迭代自训练
tldr: 针对递归自改进中推理错误累积导致的性能退化问题，本文提出神经符号递归自对齐（NSRSA）框架。该方法通过嵌入符号验证子系统，在推理步骤层面过滤训练数据，利用sympy验证算术运算并检查逻辑一致性。实验表明，NSRSA能有效剔除“侥幸猜对”的错误推理，结合DPO进一步提升了模型区分正确推理的能力，为实现稳定可靠的迭代自训练提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21558v1/fig-001.webp\", \"caption\": \"Table 1: GSM8K test accuracy (%) across self-training iterations. NSRSA maintains improvement over 5 iterations while no-verification collapses and outcome-only plateaus. Recursive depth counts iterations before accuracy drops below baseline −1%.\", \"page\": 6, \"index\": 1, \"width\": 774, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2603.21558v1/fig-002.webp\", \"caption\": \"Figure 2: GSM8K accuracy across 5 self-training iterations. NSRSA (green) enables stable recursive improvement. Outcome verification (orange) plateaus after iteration 2. No verification (red) collapses by iteration 3.\", \"page\": 7, \"index\": 2, \"width\": 560, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2603.21558v1/fig-003.webp\", \"caption\": \"Figure 3: NSRSA verification rate across iterations. The fraction of correct-answer solutions that also pass full symbolic verification increases over iterations for the NSRSA condition, indicating that the model learns to produce more symbolically sound reasoning.\", \"page\": 7, \"index\": 3, \"width\": 560, \"height\": 362}]"
motivation: 递归自改进过程中，模型在自身生成的数据上迭代训练会导致推理错误累积和性能退化（递归漂移）。
method: 提出NSRSA框架，利用符号验证系统在推理步骤级别对数据质量进行把关，验证算术运算、逻辑流和领域约束。
result: "NSRSA在GSM8K任务中剔除了约34%虽答案正确但推理错误的样本，并通过DPO将奖励准确率从46%提升至63%。"
conclusion: 引入外部符号验证能有效稳定迭代自训练过程，使递归自改进在可自动化验证的领域中变得可衡量且可靠。
---

## 摘要
递归自我提升——即模型在其自身输出上进行迭代训练——有望实现能力的持续增长，但面临着一个根本性障碍：递归漂移。随着模型在多个迭代中基于自生成数据进行训练，中间推理过程中的错误会不断累积，导致模式崩溃和性能下降。我们提出了神经符号递归自对齐（NSRSA），通过嵌入一个符号验证子系统，在推理步骤层面把控训练数据质量，从而稳定迭代自训练。与仅针对结果的过滤（这种方式会接纳推理有误但结果正确的“侥幸猜测”）不同，NSRSA 通过 sympy 验证每个算术运算，检查推理步骤间的逻辑流一致性，并强制执行领域约束。我们在 GSM8K 数据集上使用 Qwen3-4B-Thinking 模型，在五种条件下进行了 5 次自训练迭代评估：无验证、结果验证、多数投票、完整的 NSRSA 符号验证以及结合 DPO 的 NSRSA。我们的过滤分析显示，NSRSA 拒绝了约 34% 通过结果验证但答案正确的解法，从而从训练集中消除了推理有误的“侥幸猜测”。我们进一步证明，利用 NSRSA 验证构建 DPO 偏好对可以教会模型区分合理推理与错误推理（奖励准确率从 46% 提升至 63%）。NSRSA 提供了一个可扩展的框架，展示了在可进行自动验证的领域中，外部符号验证如何使递归自我提升变得可衡量且可靠。

## Abstract
Recursive self-improvement--where a model iteratively trains on its own outputs--promises sustained capability growth but faces a fundamental obstacle: recursive drift. As models train on self-generated data across multiple iterations, errors in intermediate reasoning compound, leading to mode collapse and performance degradation. We propose Neuro-Symbolic Recursive Self-Alignment (NSRSA), which stabilizes iterative self-training by embedding a symbolic verification subsystem that gates training data quality at the reasoning step level. Unlike outcome-only filtering (which admits "lucky guesses" with flawed reasoning), NSRSA verifies each arithmetic operation via sympy, checks logical flow consistency across reasoning steps, and enforces domain constraints. We evaluate NSRSA on GSM8K using Qwen3-4B-Thinking across 5 self-training iterations under five conditions: no verification, outcome verification, majority voting, full NSRSA symbolic verification, and NSRSA with DPO. Our filtering analysis shows that NSRSA rejects approximately 34% of correct-answer solutions that pass outcome verification, eliminating "lucky guesses" with flawed reasoning from the training set. We further demonstrate that constructing DPO preference pairs from NSRSA verification teaches the model to distinguish sound from flawed reasoning (reward accuracy 46% to 63%). NSRSA provides an extensible framework that demonstrates how external symbolic verification can make recursive self-improvement measurable and reliable within domains where automated verification is available.