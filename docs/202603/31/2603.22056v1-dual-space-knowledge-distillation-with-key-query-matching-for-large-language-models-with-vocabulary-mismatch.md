---
title: Dual-Space Knowledge Distillation with Key-Query Matching for Large Language Models with Vocabulary Mismatch
title_zh: 针对词表不匹配大语言模型的基于键-查询匹配的双空间知识蒸馏
authors: "Stella Eva Tsiapali, Cong-Thanh Do, Kate Knill"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22056v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 针对词表不匹配的大语言模型知识蒸馏及注意力机制分析
tldr: 针对大语言模型在词表不匹配情况下的知识蒸馏难题，本文在深入分析现有DSKD-CMA方法局限性的基础上，提出了一种基于生成对抗学习的新方法DSKD-CMA-GA。该方法通过对齐不同模型生成的键（Key）和查询（Query）分布，有效解决了跨分词器蒸馏中的分布失配问题，在多项文本生成任务中取得了性能提升，显著缩小了跨词表与同词表蒸馏之间的性能差距。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22056v1/fig-001.webp\", \"caption\": \"Fig. 5: Alignment weights assigned to pairs of tokens in the Student and Teacher sequences, using CMA and its chunk-based ablations: Chunk-Level Projection (CLP) and Chunk-Level Attention (CLA).\", \"page\": 4, \"index\": 1, \"width\": 310, \"height\": 790}, {\"url\": \"assets/figures/arxiv/2603.22056v1/fig-002.webp\", \"caption\": \"Table 1: Mean ROUGE-L scores from evaluation on five random seeds. Average scores across the out-of-distribution (OOD) and all (All) evaluation datasets are given. Results for the best f -divergence are reported for DSKD-CMA (SRKL), -GA (SKL) and -CT (KL).\", \"page\": 4, \"index\": 2, \"width\": 521, \"height\": 391}]"
motivation: 旨在解决大语言模型在跨词表知识蒸馏过程中，由于分词器不同导致的键值对分布不匹配及现有方法机制不透明的问题。
method: 引入生成对抗学习（GA）机制来对齐不同模型计算出的键和查询分布，优化跨模型注意力机制。
result: 实验表明该方法在文本生成质量上获得了一致的提升，尤其在分布外数据上ROUGE-L平均提高了0.37。
conclusion: 通过对抗学习匹配跨空间特征分布，是提升异构词表模型间知识蒸馏效率的有效途径。
---

## 摘要
大语言模型（LLMs）在各项语言任务中均取得了最先进（SOTA）的性能，但由于其庞大的规模和资源需求，部署成本高昂。知识蒸馏（KD）通过训练较小的学生模型来模仿较大的教师模型，从而在不显著损失性能的情况下提高效率。具有跨模型注意力机制的双空间知识蒸馏（DSKD-CMA）已成为具有不同分词器的大语言模型之间进行知识蒸馏的一种最先进方法，但其内部运作机制在很大程度上仍不透明。在这项工作中，我们通过手动标记对齐探测和热图可视化，系统地分析了 DSKD-CMA 的注意力机制，揭示了其优势和局限性。在此基础上，我们提出了一种基于生成对抗（GA）学习的新方法 DSKD-CMA-GA，旨在解决由不同模型计算出的键（keys）和查询（queries）之间的分布不匹配问题。实验表明，该方法在文本生成质量上带来了适度但持续的 ROUGE-L 提升，特别是在分布外数据上（平均提升 0.37），缩小了跨分词器与同分词器知识蒸馏之间的差距。

## Abstract
Large language models (LLMs) achieve state-of-the-art (SOTA) performance across language tasks, but are costly to deploy due to their size and resource demands. Knowledge Distillation (KD) addresses this by training smaller Student models to mimic larger Teacher models, improving efficiency without significant performance loss. Dual-Space Knowledge Distillation with Cross-Model Attention (DSKD-CMA) has emerged as a SOTA method for KD between LLMs with distinct tokenizers, yet its internal workings remain largely opaque. In this work, we systematically analyse the attention mechanism of DSKD-CMA through manual token alignment probing and heatmap visualisations, revealing both strengths and limitations. Building on this, we introduce a novel method, DSKD-CMA-GA, based on Generative Adversarial (GA) learning, to address the mismatched distributions between the keys and queries computed from distinct models. Experiments show modest but consistent ROUGE-L gains in text generation quality, particularly on out-of-distribution data (+0.37 on average), narrowing the gap between cross- and same-tokenizer KD.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dual-Space Knowledge Distillation with Key-Query Matching for Large Language Models with Vocabulary Mismatch》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：大语言模型（LLMs）虽然性能卓越，但部署成本极高。知识蒸馏（KD）是压缩模型的有效手段，但现代 LLM 往往使用不同的分词器（Tokenizer）和词表（Vocabulary），导致教师和学生模型的序列长度及输出空间不一致，难以直接对齐。
*   **核心问题**：现有的 SOTA 方法 DSKD-CMA（双空间跨模型注意力蒸馏）虽然在处理词表不匹配时表现优异，但其内部机制不透明（黑盒），且由于教师和学生模型特征空间不同，计算注意力时的“键（Key）”与“查询（Query）”存在分布失配问题，限制了蒸馏效果。

### 2. 方法论：核心思想与技术细节
*   **分析框架（探测与可视化）**：
    *   提出**手动块对齐（Manual Chunk Alignment）**：通过字符偏移量将不同分词器的 Token 序列划分为语义等价的“块（Chunks）”，作为解释跨模型注意力（CMA）的基准。
    *   设计了两种消融变体：**CLP（块级投影）**和**CLA（块级注意力）**，用于验证 CMA 是否真正捕捉到了局部的 Token 对应关系。
*   **核心改进方法（DSKD-CMA-GA）**：
    *   **生成对抗对齐（GA）**：引入一个判别器 $D$，学习区分教师模型的 Key 分布和学生模型的 Query 分布。学生模型的投影器作为生成器，目标是生成让判别器无法区分的特征。
    *   **条件传输对齐（CT）**：作为另一种变体，利用约束版的最优传输（Optimal Transport）理论，通过训练一个评论器（Critic）来最小化 Key 和 Query 分布之间的传输成本。
    *   **最终目标函数**：将上述对抗损失（$L_{KQ}$）与原始的 DSKD-CMA 损失（包含双空间下的 KL 散度等）相结合，共同优化。

### 3. 实验设计
*   **数据集**：
    *   **训练/分布内（ID）测试**：DataBricks Dolly 15K。
    *   **分布外（OOD）测试**：Self-Instruct、Vicuna-Eval、Super-Natural Instructions (S-NI)、Unnatural Instructions (UnNI)。
*   **模型配置**：
    *   **学生模型**：GPT-2 (124M, 50K 词表)。
    *   **教师模型**：Qwen1.5 (1.8B, 150K 词表，用于跨词表实验)；GPT-2 XL (1.5B, 用于同词表基准对比)。
*   **对比方法（Benchmark）**：
    *   监督微调（SFT）、最小编辑距离对齐（MinED）、通用 Logit 蒸馏（ULD）、原始 DSKD-CMA。
*   **评估指标**：ROUGE-L（衡量文本生成的重合度）。

### 4. 资源与算力
*   **硬件环境**：使用了 Ampere 节点，配备 **4 张 NVIDIA A100 GPU (80GB)** 和 1TB RAM。
*   **训练时长**：每个训练任务耗时约 **2-3 小时**，评估过程约耗时 1 小时。

### 5. 实验数量与充分性
*   **实验规模**：所有结果均基于 **5 个随机种子** 的平均值，确保了结果的统计显著性。
*   **消融与对比**：
    *   测试了 **6 种不同的 f-散度函数**（如 KL, RKL, JSD 等），以寻找每种方法的最优配置。
    *   对比了同词表与跨词表蒸馏的性能差距。
    *   通过热图可视化直观展示了注意力机制的改进。
*   **评价**：实验设计较为充分，涵盖了从 ID 到 OOD 的多种场景，对比基准选择合理，实验过程客观公平。

### 6. 主要结论与发现
*   **机制理解**：分析发现 CMA 确实能隐式捕捉到 Token 块结构，但在处理重复 Token（如数字）时存在噪声。
*   **性能提升**：提出的 **DSKD-CMA-GA** 在所有数据集上均优于原始 DSKD-CMA，尤其在 OOD 数据上平均提升了 **0.37 ROUGE-L**。
*   **缩小差距**：GA 变体显著缩小了跨词表蒸馏与同词表蒸馏之间的性能鸿沟，在某些任务上甚至超越了同词表蒸馏的效果。
*   **散度选择**：发现散度函数的选择与对齐机制有强耦合关系，例如 GA 变体在配合 Skewed KL 散度时效果最佳。

### 7. 优点
*   **可解释性增强**：通过手动块对齐探测，为原本“黑盒”的跨模型注意力机制提供了直观的分析手段。
*   **理论与实践结合**：引入对抗学习来解决特征分布失配问题，具有较强的理论支撑且易于在现有框架中实现。
*   **泛化性强**：在多个分布外数据集上验证了其稳健的性能提升。

### 8. 不足与局限
*   **提升幅度有限**：虽然结果一致向好，但 ROUGE-L 的绝对分值提升并不算巨大（约 0.4 左右）。
*   **任务覆盖面**：实验主要集中在通用的指令遵循和文本生成任务上，尚未在更复杂的逻辑推理（如 GSM8K）或长文本任务中进行验证。
*   **计算开销**：虽然文中提到训练时间不长，但引入判别器和对抗训练过程会增加额外的超参数调优负担。

（完）
