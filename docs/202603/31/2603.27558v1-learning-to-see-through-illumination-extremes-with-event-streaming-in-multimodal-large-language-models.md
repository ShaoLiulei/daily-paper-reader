---
title: Learning to See through Illumination Extremes with Event Streaming in Multimodal Large Language Models
title_zh: 在多模态大语言模型中利用事件流学习在极端光照下进行视觉感知
authors: "Baoheng Zhang, Jiahui Liu, Gui Zhao, Weizhou Zhang, Yixuan Ma, Jun Jiang, Yingxian Chen, Wilton W. T. Fok, Xiaojuan Qi, Hayden Kwok-Hay So"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27558v1"
tags: ["query:snn"]
score: 9.0
evidence: 多模态大语言模型中的事件流视觉推理
tldr: 针对多模态大模型在极端光照条件下视觉推理失效的问题，本文提出了Event-MLLM。该模型通过动态融合事件流与RGB图像，利用事件相机的高动态范围特性弥补视觉信息缺失。核心贡献包括光照指示器、光照校正损失函数以及首个多光照事件指令微调数据集，显著提升了模型在极端环境下的感知与推理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27558v1/fig-001.webp\", \"caption\": \"Figure 1. Extreme lighting scenarios are demonstrated. When the normal (a) RGB frame can not be obtained, only (c) overexposed or (d) underexposed frame can be used as input. Existing baselines often produce illusions due to information degradation, while our method cleverly incorporates information from the (b) event frame, enabling the model to provide more accurate responses.\", \"page\": 1, \"index\": 1, \"width\": 492, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2603.27558v1/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison under various illumination levels for the multi-choice task (top) and the object-counting task (bottom). Under severe underexposure and overexposure, the baseline model frequently produces incorrect or hallucinated predictions, whereas our illumination-guided event-enhanced model remains stable and accurate across all brightness ratios. These examples illustrate the robustness of our method in extreme-light conditions.\", \"page\": 6, \"index\": 2, \"width\": 1020, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2603.27558v1/fig-003.webp\", \"caption\": \"Figure 4. Visualization results of features before and after fusion with t-SNE. (a) Before fusion: event features and RGB features with different brightness ratios. (b) After fusion, fusion features under different brightness ratios cluster around the normal-light feature.\", \"page\": 7, \"index\": 3, \"width\": 491, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2603.27558v1/fig-004.webp\", \"caption\": \"Figure 2. An overview of our proposed model is illustrated: (a) Features are extracted from extreme-light RGB frames by the original vision encoder and the DINOv2 vision encoder, respectively. These features are then fused with the features extracted from the event frame using our designed MLPs with our proposed Illumination Indicator (Fillu) to obtain fused features Ffusion (Section 3.2). (b) Features are extracted from normal-light RGB frames and used for representation learning with the fused features using our proposed Illumination Correction Loss LIC (Section 3.3). (c) During training, the fused features participate in the fine-tuning of the entire MLLM, while (a) and (c) participate in inference (Section 3.4).\", \"page\": 3, \"index\": 4, \"width\": 1032, \"height\": 372}, {\"url\": \"assets/figures/arxiv/2603.27558v1/fig-005.webp\", \"caption\": \"Table 4. Cosine similarity comparison of vision encoder features between original and varying brightness ratios with/without illumination correction (Section 3.2) across Qwen-3B/7B backbones.\", \"page\": 8, \"index\": 5, \"width\": 1031, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2603.27558v1/fig-006.webp\", \"caption\": \"Table 1. Compared to existing MLLMs of different types, our method significantly outperforms all other methods to achieve state-of-the-art performance on both multi-choice and object-counting tasks on all metrics. Furthermore, we take experiments with different sizes of Qwen [51] as baselines and apply our design, where consistently achieves performance improvements (Best results are shown in bold).\", \"page\": 5, \"index\": 6, \"width\": 1031, \"height\": 323}]"
motivation: 传统多模态大模型依赖的RGB图像在极端光照下会丢失关键语义信息，导致视觉推理任务失败。
method: 提出Event-MLLM，通过光照指示器自适应融合事件流与RGB特征，并利用光照校正损失在潜空间对齐正常光照语义。
result: 在涵盖17种亮度水平的新建基准测试中，Event-MLLM在推理、计数和细粒度识别任务上均显著优于现有通用及专用模型。
conclusion: 结合事件流的异步感知能力是提升多模态大模型在全天候、极端光照环境下鲁棒性的有效途径。
---

## 摘要
多模态大语言模型（MLLMs）在标准条件下表现出强大的视觉语言推理能力，但在极端光照下会失效，因为此时 RGB 输入会丢失不可逆的结构和语义信息。我们提出了 Event-MLLM，这是一种事件增强模型，通过动态融合事件流与 RGB 帧来实现全光照视觉推理。驱动我们方法的两个关键组件是：光照指示器（Illumination Indicator）——一个源自 DINOv2 分支的可学习信号，用于表征曝光退化并自适应地调节事件-RGB 融合；以及光照校正损失（Illumination Correction Loss），用于在潜空间中将融合特征与未退化（正常光照）的语义对齐，从而补偿极端光照下丢失的信息。我们为 MLLMs 构建了首个多光照事件指令语料库，包含跨越不同场景和 17 种亮度倍率（0.05x - 20x）的 2,241 个事件-RGB 样本（每个样本约含 6 个问答对），以及一个用于极端光照下推理、计数和细粒度识别的指令遵循基准。实验表明，Event-MLLM 显著优于通用型、光照自适应型和仅事件型的基线模型，在挑战性光照条件下的鲁棒多模态感知与推理方面达到了当前最先进的水平。

## Abstract
Multimodal Large Language Models (MLLMs) perform strong vision-language reasoning under standard conditions but fail in extreme illumination, where RGB inputs lose irrevocable structure and semantics. We propose Event-MLLM, an event-enhanced model that performs all-light visual reasoning by dynamically fusing event streams with RGB frames. Two key components drive our approach: an Illumination Indicator - a learnable signal derived from a DINOv2 branch that represents exposure degradation and adaptively modulates event-RGB fusion - and an Illumination Correction Loss that aligns fused features with non-degraded (normal-light) semantics in the latent space, compensating for information lost in extreme lighting. We curate the first multi-illumination event-instruction corpus for MLLMs, with 2,241 event-RGB samples (around 6 QA pairs each) across diverse scenes and 17 brightness rates (0.05x - 20x), plus an instruct-following benchmark for reasoning, counting, and fine-grained recognition under extreme lighting. Experiments show that Event-MLLM markedly outperforms general-purpose, illumination-adaptive, and event-only baselines, setting a new state of the art in robust multimodal perception and reasoning under challenging illumination.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to See through Illumination Extremes with Event Streaming in Multimodal Large Language Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态大语言模型（MLLM）在标准光照下表现优异，但在**极端光照（如极暗或过曝）**环境下，RGB图像会发生不可逆的结构和语义信息丢失，导致模型产生幻觉或推理失败。
*   **研究背景**：传统的图像增强或外部工具调用方法属于“被动响应”，无法从根本上增强模型自身的鲁棒性。而**事件相机（Event Camera）**具有高动态范围（>120 dB）和微秒级响应，能捕捉亮度变化而非绝对强度，在极端光照下仍能保留丰富的结构线索。

### 2. 方法论：Event-MLLM
该模型通过动态融合事件流与RGB帧，实现全光照视觉推理。核心架构包含以下关键技术：
*   **光照指示器（Illumination Indicator）**：利用一个冻结的 DINOv2 编码器提取特征，通过轻量级 MLP 生成可学习的信号 $F_{illu}$。该信号用于量化曝光退化程度，并作为“权重指南”自适应地调节事件特征与 RGB 特征的融合比例。
*   **自适应特征融合**：将 RGB 特征、事件特征与光照指示器特征进行拼接，通过融合 MLP 生成统一的视觉表示 $F_{fusion}$。
*   **光照校正损失（Illumination Correction Loss, $L_{IC}$）**：在训练阶段，利用正常光照下的 RGB 特征作为监督信号，通过均方误差（MSE）强制融合特征在潜空间中对齐正常光照的语义。这使得模型在推理时即使只有退化的 RGB 输入，也能通过事件流“补全”丢失的信息。
*   **两阶段训练策略**：
    1.  **特征融合预训练**：训练融合模块实现语义对齐。
    2.  **指令微调**：使用 LoRA 技术对整个 MLLM 进行端到端微调，使其适应视觉-语言对齐任务。

### 3. 实验设计与基准
*   **数据集**：构建了首个**多光照事件指令数据集**。包含 2,241 个样本，涵盖 17 种亮度倍率（0.05x 至 20x），总计 10,129 个问答对。数据源自 DSEC、NER-Net+ 等四个已有的 RGB-Event 匹配数据集，并利用 GPT-4o 生成细粒度描述。
*   **Benchmark 任务**：
    1.  **多选题（Multi-Choice）**：评估复杂的场景理解。
    2.  **物体计数（Object-Counting）**：评估精确的枚举能力。
*   **对比方法**：
    *   **通用模型**：LLaVA-7B, DeepSeek-VL, InternVL。
    *   **事件专用模型**：EventGPT。
    *   **光照感知模型**：Q-Instruct。
    *   **消融基线**：基于 Qwen-3B/7B 的不同融合策略（Pre-fusion, Post-fusion）。

### 4. 资源与算力
*   **硬件设备**：使用了 **NVIDIA RTX 5090D** 和 **NVIDIA A800** GPU。
*   **训练细节**：
    *   光照校正阶段：Batch size 为 4，学习率 0.001，训练 30 个 epoch。
    *   LoRA 微调阶段：训练 1 个 epoch。
*   **模型规模**：实验涵盖了 3B 和 7B 两种参数规模的 Qwen 后端。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的定量对比（跨 17 种亮度水平）、定性可视化（t-SNE 特征聚类分析、案例对比）以及消融实验。
*   **充分性与公平性**：实验覆盖了从极暗到极亮的完整光谱，对比了当前主流的各类 MLLM 变体。通过余弦相似度分析证明了特征对齐的有效性，实验设计逻辑严密，对比基准设置合理，具有较高的客观性。

### 6. 主要结论与发现
*   **性能领先**：Event-MLLM 在所有指标上均显著优于现有模型。在 7B 规模下，多选题准确率比基线提升了 8.42%。
*   **特征稳定性**：引入光照校正后，极端光照下的特征与正常光照特征的余弦相似度从 ~0.65 提升至 ~0.92。
*   **模型规模效应**：更强大的语言模型（如 7B vs 3B）对残余特征波动的容忍度更高，能更可靠地整合多模态线索。

### 7. 优点与亮点
*   **创新性**：首次将事件相机数据引入 MLLM 领域，解决了极端环境下的视觉鲁棒性难题。
*   **设计巧妙**：光照指示器避免了手动设置融合权重的繁琐，$L_{IC}$ 损失函数实现了无需像素级修复的语义级补偿。
*   **资源贡献**：填补了多光照事件-语言指令数据集的空白，为后续研究提供了基准。

### 8. 不足与局限
*   **硬件依赖**：模型高度依赖事件相机硬件，在缺乏事件信号或信号稀疏的常规场景下，其优势无法体现。
*   **场景覆盖**：虽然涵盖了室内外场景，但对于更复杂的动态模糊、极端天气（如浓雾）等其他视觉退化情况的泛化能力尚待验证。
*   **实时性**：论文未详细讨论双分支视觉编码器及大模型推理在移动端或嵌入式设备上的实时性表现。

（完）
