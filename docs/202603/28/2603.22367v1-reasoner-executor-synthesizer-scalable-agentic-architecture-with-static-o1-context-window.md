---
title: "Reasoner-Executor-Synthesizer: Scalable Agentic Architecture with Static O(1) Context Window"
title_zh: 推理器-执行器-合成器：具有静态 O(1) 上下文窗口的可扩展智能体架构
authors: Ivan Dobrovolskyi
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22367v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 可扩展的LLM智能体架构与上下文窗口优化
tldr: 针对大模型智能体在RAG模式下存在的幻觉风险及Token成本随数据量线性增长的问题，本文提出RES架构。该架构将任务分解为推理器、执行器和合成器三层，通过执行器进行确定性数据处理并仅向合成器传递固定大小的统计摘要。实验证明，该架构实现了O(1)的Token复杂度，在处理海量学术数据时能显著降低成本并从根本上消除数据幻觉。
selection_source: fresh_fetch
motivation: 解决传统RAG架构中Token成本随数据集规模线性增长以及长上下文导致的模型幻觉问题。
method: 提出由推理器解析意图、执行器进行零Token确定性检索、合成器生成叙述的三层RES架构。
result: "在ScholarSearch测试中，无论数据集规模如何变化，平均Token消耗始终稳定在1,574个左右。"
conclusion: RES架构通过将数据处理与生成分离，实现了极高的扩展性并从结构上消除了数据幻觉。
---

## 摘要
作为自主智能体部署的大语言模型（LLMs）通常使用检索增强生成（RAG），将检索到的文档输入上下文窗口，这引发了两个问题：幻觉风险随上下文长度增加而增长，且 Token 成本随数据集规模线性扩展。我们提出了推理器-执行器-合成器（RES）架构，这是一种三层设计，严格分离了意图解析（推理器）、确定性数据检索与聚合（执行器）以及叙述生成（合成器）。执行器不消耗 LLM Token，仅向合成器传递固定大小的统计摘要。我们正式证明了 RES 在数据集规模上实现了 O(1) 的 Token 复杂度，并在基于 Crossref API（包含超过 1.3 亿篇文章）的学术研究助手 ScholarSearch 上验证了这一点。在 100 次基准测试运行中，无论数据集包含 42,000 篇还是 1,630 万篇文章，RES 的平均 Token 成本均为 1,574 个。该架构通过构造消除了数据幻觉：LLM 永远不会接触原始记录。关键词：LLM 智能体；智能体架构；幻觉消除；Token 优化；上下文窗口；检索增强生成；确定性执行；学术元数据；Crossref API；O(1) 复杂度。

## Abstract
Large Language Models (LLMs) deployed as autonomous agents commonly use Retrieval-Augmented Generation (RAG), feeding retrieved documents into the context window, which creates two problems: the risk of hallucination grows with context length, and token cost scales linearly with dataset size. We propose the Reasoner-Executor-Synthesizer (RES) architecture, a three-layer design that strictly separates intent parsing (Reasoner), deterministic data retrieval and aggregation (Executor), and narrative generation (Synthesizer). The Executor uses zero LLM tokens and passes only fixed-size statistical summaries to the Synthesizer. We formally prove that RES achieves O(1) token complexity with respect to dataset size, and validate this on ScholarSearch, a scholarly research assistant backed by the Crossref API (130M+ articles). Across 100 benchmark runs, RES achieves a mean token cost of 1,574 tokens regardless of whether the dataset contains 42,000 or 16.3 million articles. The architecture eliminates data hallucination by construction: the LLM never sees raw records. KEYWORDS LLM agents; agentic architecture; hallucination elimination; token optimization; context window; retrieval-augmented generation; deterministic execution; scholarly metadata; Crossref API; O(1) complexity.

---

## 论文详细总结（自动生成）

以下是对论文《Reasoner-Executor-Synthesizer: Scalable Agentic Architecture with Static O(1) Context Window》的结构化深入总结：

### 1. 核心问题与研究动机
*   **背景**：当前大语言模型（LLM）智能体主要采用检索增强生成（RAG）模式，将检索到的原始文档注入上下文窗口。
*   **核心痛点**：
    1.  **幻觉风险**：随着上下文长度增加，模型更容易产生幻觉，且存在“迷失在中间（Lost in the Middle）”的问题。
    2.  **成本线性增长**：Token 消耗随检索到的文档数量线性扩展（$O(n)$）。在处理海量数据集（如数百万条记录）时，经济成本和上下文窗口限制使其变得不可行。
*   **目标**：设计一种架构，使 Token 成本不随数据集规模增长，并从结构上消除数据幻觉。

### 2. 方法论：RES 架构
RES 架构将任务严格划分为三个互不重叠的层：
*   **Reasoner（推理器 - LLM 驱动）**：
    *   **功能**：解析用户自然语言意图，将其转化为结构化的查询计划（JSON 格式）。
    *   **约束**：不接触原始数据，仅负责逻辑拆解。Token 消耗仅取决于查询复杂度，与数据集大小无关。
*   **Executor（执行器 - 确定性代码驱动）**：
    *   **功能**：运行传统的 Python 代码或 API 调用，执行过滤、聚合、计数、排序等确定性操作。
    *   **关键点**：**消耗 0 个 LLM Token**。它处理海量数据并仅输出一个固定大小的统计摘要（如 JSON 格式的统计表）。
*   **Synthesizer（合成器 - LLM 驱动）**：
    *   **功能**：接收执行器生成的固定大小摘要，将其转化为人类可读的叙述性文字和可视化图表配置。
    *   **约束**：由于输入摘要的大小是恒定的，其 Token 消耗也是恒定的（$O(1)$）。

### 3. 实验设计
*   **应用场景**：构建了名为 **ScholarSearch** 的学术研究助手。
*   **数据集**：接入 Crossref API，涵盖超过 1.3 亿篇学术文章元数据。
*   **Benchmark 与对比方法**：
    *   **对比对象**：朴素 RAG 基准（Naive Baseline），即直接将 50 条原始记录喂给 LLM。
    *   **测试用例**：20 个多样化查询，涵盖趋势分析、对比、排名和通用统计四类意图。
    *   **规模跨度**：测试了从 4.2 万条到 1630 万条记录的不同数据集规模。

### 4. 资源与算力
*   **模型使用**：采用了 Anthropic 的 **Claude 3.5 Sonnet** (claude-sonnet-4-20250514) 作为推理器和合成器。
*   **算力说明**：文中未提及具体的 GPU 训练算力，因为该研究侧重于**推理架构设计**而非模型训练。实验主要涉及 API 调用和基于 Python 的确定性计算。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 100 次基准测试运行（20 个查询 × 5 次重复）。
*   **充分性评价**：
    *   实验涵盖了从万级到千万级的数据跨度，通过 log-log 缩放图清晰地证明了 $O(1)$ 复杂度的有效性。
    *   数据点分布紧密（标准差仅 259 tokens），证明了成本的可预测性。
    *   实验设计较为客观，但对比的朴素 RAG 基准由于上下文限制无法处理千万级数据，因此在超大规模下的对比主要基于理论推导。

### 6. 主要结论与发现
*   **Token 复杂度为 $O(1)$**：无论处理 4.2 万还是 1630 万条记录，平均 Token 消耗稳定在 **1,574** 个左右。
*   **成本优势巨大**：在 1600 万条数据的规模下，RES 架构比传统 RAG 方案节省了 **99.9996%** 的 Token 成本（便宜约 120 万倍）。
*   **零数据幻觉**：由于 LLM 永远接触不到原始数据记录，只能基于执行器提供的确定性统计结果进行叙述，从而在结构上消除了事实性幻觉。

### 7. 优点
*   **极高的扩展性**：打破了上下文窗口对大数据分析的限制。
*   **确定性与可靠性**：将计算任务交给代码而非概率性的 LLM，确保了统计结果（如计数、求和）的 100% 准确。
*   **透明度**：三层架构的中间产物（查询计划、统计摘要）均可被人类审计。

### 8. 不足与局限
*   **任务局限性**：RES 擅长分析型查询（统计、趋势），但不适用于需要“深度阅读”或“全文总结”单篇长文档的任务。
*   **依赖底层能力**：执行器的效率高度依赖于数据源（如数据库或 API）是否支持高效的聚合查询。
*   **指令遵循风险**：如果推理器（Reasoner）未能正确解析复杂的嵌套查询意图，整个流水线仍会失效。

（完）
