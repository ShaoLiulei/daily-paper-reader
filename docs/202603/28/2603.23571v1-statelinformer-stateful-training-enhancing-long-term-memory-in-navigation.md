---
title: "StateLinFormer: Stateful Training Enhancing Long-term Memory in Navigation"
title_zh: StateLinFormer：通过有状态训练增强导航中的长期记忆
authors: "Zhiyuan Chen, Yuxuan Zhong, Fan Wang, Bo Yu, Pengtao Shao, Shaoshan Liu, Ning Ding"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23571v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 具有状态记忆的线性注意力导航模型
tldr: StateLinFormer 是一种基于线性注意力的导航模型，旨在解决传统 Transformer 在长程记忆方面的局限。该模型引入了有状态训练机制，通过在连续训练段之间保留递归记忆状态而非重新初始化，模拟了在无限长序列上的学习。在 MAZE 和 ProcTHOR 环境中的实验表明，该方法显著提升了长时记忆保持能力和上下文学习（ICL）性能，使其在复杂导航任务中表现优异。
selection_source: fresh_fetch
motivation: 现有的导航系统在灵活性与长程记忆之间存在权衡，尤其是 Transformer 模型受限于固定上下文窗口，难以在长时间交互中保持持久记忆。
method: 提出 StateLinFormer 模型，采用线性注意力机制并结合有状态训练范式，在训练过程中跨批次边界保留并传递记忆状态。
result: 在 MAZE 和 ProcTHOR 任务中，StateLinFormer 的表现显著优于无状态线性注意力模型和具有固定窗口的标准 Transformer。
conclusion: 有状态训练能有效增强模型的长程记忆保持能力，并显著提升导航任务中的上下文学习与自适应能力。
---

## 摘要
有效的导航智能依赖于长期记忆，以支持即时泛化和持续适应。然而，现有方法面临困境：模块化系统依赖显式建图但缺乏灵活性，而基于 Transformer 的端到端模型受限于固定上下文窗口，限制了在长时间交互中的持久记忆。我们提出了 StateLinFormer，这是一种采用有状态记忆机制训练的线性注意力导航模型，该机制在连续的训练片段中保留循环记忆状态，而不是在每个批次边界重新初始化。这种训练范式有效地近似了在无限长序列上的学习，使模型能够实现长时程记忆保留。在 MAZE 和 ProcTHOR 环境中的实验表明，StateLinFormer 的性能显著优于其无状态线性注意力对应模型以及具有固定上下文窗口的标准 Transformer 基准模型。值得注意的是，随着交互长度的增加，持久的有状态训练显著提高了上下文相关的适应能力，这表明模型在导航任务中的上下文学习（ICL）能力得到了增强。

## Abstract
Effective navigation intelligence relies on long-term memory to support both immediate generalization and sustained adaptation. However, existing approaches face a dilemma: modular systems rely on explicit mapping but lack flexibility, while Transformer-based end-to-end models are constrained by fixed context windows, limiting persistent memory across extended interactions. We introduce StateLinFormer, a linear-attention navigation model trained with a stateful memory mechanism that preserves recurrent memory states across consecutive training segments instead of reinitializing them at each batch boundary. This training paradigm effectively approximates learning on infinitely long sequences, enabling the model to achieve long-horizon memory retention. Experiments across both MAZE and ProcTHOR environments demonstrate that StateLinFormer significantly outperforms its stateless linear-attention counterpart and standard Transformer baselines with fixed context windows. Notably, as interaction length increases, persistent stateful training substantially improves context-dependent adaptation, suggesting an enhancement in the model's In-Context Learning (ICL) capabilities for navigation tasks.

---

## 论文详细总结（自动生成）

### StateLinFormer：通过有状态训练增强导航中的长期记忆

#### 1. 核心问题与整体含义（研究动机和背景）
具身智能（Embodied AI）导航的核心挑战在于如何有效地整合长时程的观察信息，以构建连贯的空间表示并做出一致的决策。
*   **现有技术的局限性**：传统的 SLAM 系统虽然精确但缺乏语义灵活性；基于 Transformer 的端到端模型虽然泛化能力强，但受限于**固定上下文窗口**，导致在长距离交互中无法保持持久记忆，经常出现重复探索。
*   **训练与推理的不匹配**：即使架构本身支持长序列（如线性注意力），现有的训练协议通常在每个批次（Batch）开始时重置隐藏状态（Stateless Training），这限制了模型学习利用持续演进的记忆状态。
*   **研究目标**：提出一种能够模拟“无限长序列”学习的训练范式，使模型在不增加计算负担的前提下，获得卓越的长期记忆和在线自适应能力。

#### 2. 方法论：核心思想与关键技术
StateLinFormer 的核心在于将**线性注意力架构**与**有状态训练（Stateful Training）**相结合。
*   **核心思想**：在训练过程中，不再于批次边界重置记忆状态，而是将前一个批次的最终状态 $M_T^{(b)}$ 传递给下一个批次的初始状态 $M_0^{(b+1)}$。
*   **关键技术细节**：
    *   **线性注意力架构**：采用 RWKV-7 架构，其记忆更新公式为 $M_t = M_{t-1} + \phi(k_t)v_t^\top$，输出为 $h_t = \phi(q_t)^\top M_t$。这种设计保证了每一步的计算复杂度为常数，支持处理理论上无限长的交互流。
    *   **有状态优化协议**：通过跨批次保留状态，模型参数是在由自身循环动力学诱导的“记忆状态分布”下进行优化的，而非集中在零初始化状态附近。
    *   **模块化编码器-解码器**：使用预训练的 SigLIP 提取视觉特征，通过目标编码器处理文本指令，最后由线性注意力解码器预测动作。

#### 3. 实验设计
论文引入了**持续对象导航（Continual Object Navigation, CON）**基准，模拟家庭机器人在陌生环境中通过持续交互逐渐熟悉的场景。
*   **实验场景/数据集**：
    1.  **MAZE**：15×15 的随机网格世界，用于测试基础的空间推理。
    2.  **ProcTHOR**：基于 AI2-THOR 的逼真 3D 室内环境，包含复杂的语义对象和视觉细节。
*   **对比方法（Baselines）**：
    *   **Stateless Linear Attention**：相同架构但采用传统重置状态的训练方式（消融实验）。
    *   **SPOC-10M**：相同数据量训练的 Transformer 变体。
    *   **SPOC-Pretrained**：在 40M 帧大规模数据上预训练的原始 Transformer 模型。

#### 4. 资源与算力
*   **硬件设备**：使用了 **8 张 NVIDIA A800 GPU**。
*   **训练配置**：
    *   Batch size 为 4。
    *   训练时长为 10 个 Epoch。
    *   初始学习率 1e-4，采用线性衰减调度。
    *   参数量约为 **2 亿（0.2B）**。

#### 5. 实验数量与充分性
*   **实验规模**：在 MAZE（1亿步数据）和 ProcTHOR（1000万步数据）两个维度迥异的数据集上进行了验证。
*   **充分性分析**：
    *   实验涵盖了成功率（Success Rate）和到达目标步数（Steps to Goal）等核心指标。
    *   进行了**上下文长度分析实验**，观察模型性能随交互步数增加的变化趋势。
    *   对比了同等参数规模下的不同架构（Transformer vs Linear Attention）和不同训练协议（Stateful vs Stateless），实验设计较为客观、公平，能够有力支撑其核心论点。

#### 6. 主要结论与发现
*   **有状态训练的优越性**：在相同架构下，Stateful 训练在 ProcTHOR 环境中的成功率（0.580）远高于 Stateless 训练（0.420）。
*   **超越大规模预训练**：仅用 10M 帧数据训练的 StateLinFormer 性能超过了在 40M 帧数据上预训练的 SPOC 模型。
*   **涌现的上下文学习（ICL）**：随着交互步数的增加，StateLinFormer 的成功率稳步上升，而无状态模型性能停滞甚至下降。这表明模型学会了利用历史记忆来优化当前决策，实现了无需参数更新的在线自适应。
*   **记忆稳定性**：通过分析记忆模值的相对标准差（RSD），证明有状态训练能诱导出更稳定的记忆分布。

#### 7. 优点
*   **训练协议创新**：通过简单的状态传递解决了长程记忆训练难的问题，无需复杂的梯度回传（BPTT）即可模拟长序列学习。
*   **计算效率高**：线性注意力机制解决了 Transformer 随序列增长计算量呈平方级增加的痛点。
*   **任务设定贴近现实**：提出的 CON 基准比传统的单次导航任务更符合机器人实际部署的连续工作模式。

#### 8. 不足与局限
*   **理论证明缺失**：论文主要基于经验观察，缺乏关于循环动力学收敛性和状态分布稳定性的严格数学证明。
*   **任务覆盖面有限**：目前仅在导航任务上验证，尚未在视觉语言操纵（VLM）或纯语言建模等更广泛的序列建模领域进行测试。
*   **长程衰减风险**：虽然支持长序列，但未深入探讨在极长交互（如数万步以上）中是否存在记忆漂移或状态饱和的问题。

（完）
