---
title: "Adaptive Layerwise Perturbation: Unifying Off-Policy Corrections for LLM RL"
title_zh: 自适应逐层扰动：统一大语言模型强化学习中的离策修正
authors: "Chenlu Ye, Xuanchang Zhang, Yifan Hao, Zhou Yu, Ziji Zhang, Abhinav Gullapalli, Hao Chen, Jing Huang, Tong Zhang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19470v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型强化学习中的离策略修正
tldr: 针对大语言模型强化学习（LLM RL）中因策略陈旧和训练推理不匹配导致的训练不稳定问题，本文提出了一种自适应逐层扰动（ALP）方法。该方法通过在更新期间向各层隐藏状态注入可学习的微小扰动，有效平滑了策略分布，缓解了重要性采样比率的重尾现象。实验证明，ALP在数学和工具集成推理任务中显著提升了性能，增强了训练稳定性与探索能力。
selection_source: fresh_fetch
motivation: 解决LLM强化学习中由于推理与更新策略分布差距过大导致的重要性采样比率重尾及训练不稳定性问题。
method: 在模型更新过程中，向每一层输入的隐藏状态注入自适应的可学习扰动，以平滑策略分布并缩小策略间隙。
result: 在单轮数学和多轮工具推理任务中，ALP不仅提升了最终性能，还显著抑制了重要性比率爆炸和KL散度激增。
conclusion: 逐层表示级的扰动是缓解离策（off-policy）偏差、提升LLM强化学习稳定性和探索效率的有效手段。
---

## 摘要
离策（Off-policy）问题，如策略陈旧和训练-推理不匹配，已成为大语言模型强化学习（LLM RL）训练稳定性和进一步探索的主要瓶颈。为了提高推理效率，推理策略与更新策略之间的分布差距不断扩大，导致重要性采样比率呈现重尾分布。当策略在局部过于尖锐时，会出现重尾比率，这会进一步放大剧烈梯度，并可能使更新超出置信区域。为了解决这一问题，我们提出了自适应逐层扰动（ALP），通过在更新过程中向每一层的输入隐藏状态注入微小的可学习扰动，并将其作为目标函数中相对于未改变推理策略的重要性比率的分子。直观地说，通过向中间表示添加受控噪声，ALP 防止了更新后的策略与推理策略发生过于剧烈的偏离，并扩大了策略族，使其能够覆盖带有不匹配噪声的推理策略族。因此，平滑后的分布可以自然地缩小更新策略与推理策略之间的差距，并减少重要性比率的尾部，从而维持训练稳定性。这一点已在实证中得到进一步验证。在单轮数学和多轮工具集成推理任务上的实验表明，ALP 不仅提高了最终性能，还避免了迭代训练过程中重要性比率尾部的爆炸和 KL 散度激增，同时增强了探索能力。消融实验表明，跨所有层的表示级扰动最为有效，显著优于部分层和仅 Logits 的变体。

## Abstract
Off-policy problems such as policy staleness and training-inference mismatch, has become a major bottleneck for training stability and further exploration for LLM RL. To enhance inference efficiency, the distribution gap between the inference and updated policy grows, leading to heavy-tailed importance ratios. Heavy-tailed ratios arise when the policy is locally sharp, which further inflates sharp gradients and can push updates outside the trust region. To address this, we propose Adaptive Layerwise Perturbation(ALP) by injecting small learnable perturbations into input hidden states of each layer during updates, which is used as the numerator of the importance ratio against the unchanged inference policy in the objective. Intuitively, by adding controlled noise to intermediate representations, ALP prevents the updated policy from deviating too sharply from the inference policy, and enlarges the policy family to cover the inference policy family with mismatch noises. Hence, the flattened distribution can naturally tighten the updated and inference policy gap and reduce the tail of importance ratios, thus maintaining training stability. This is further validated empirically. Experiments on single-turn math and multi-turn tool-integrated reasoning tasks show that ALP not only improves final performance, but also avoid blow up of importance ratio tail and KL spikes during iterative training, along with boosted exploration. Ablations show that representation-level perturbations across all layers are most effective, substantially outperforming partial-layer and logits-only variants.

---

## 论文详细总结（自动生成）

### 论文总结：Adaptive Layerwise Perturbation (ALP)

#### 1. 核心问题与整体含义（研究动机和背景）
在大语言模型（LLM）的强化学习（RL）中，**离策（Off-policy）不稳定性**是一个重大瓶颈。这种不稳定性主要源于两个方面：
*   **策略陈旧（Policy Staleness）：** 为了提高效率，同一批次轨迹通常用于多次策略更新。
*   **训练-推理不匹配（Training-Inference Mismatch）：** 推理引擎（如 vLLM）与训练框架在量化、批处理和内核实现上的差异，导致即使权重相同，输出分布也存在偏差。
这些因素会导致**重要性采样比率（Importance Ratio）出现重尾分布**，引发梯度爆炸、KL 散度激增，最终导致训练崩溃。论文旨在通过一种统一的算法手段，在不增加系统复杂性的前提下，平滑策略分布并增强训练稳定性。

#### 2. 方法论：核心思想与关键技术
论文提出了 **自适应逐层扰动（Adaptive Layerwise Perturbation, ALP）**：
*   **核心思想：** 在策略更新期间，向模型每一层的输入隐藏状态（Hidden States）注入微小的、可学习的随机扰动（通常为高斯噪声 $\delta \sim N(0, \sigma^2 I)$）。
*   **统一比率公式：** 在计算重要性比率 $\rho$ 时，分子使用注入扰动后的训练策略 $\pi_{\theta, \sigma}$，分母使用未扰动的推理策略 $\pi_{infer}$。
*   **平滑机制：** 这种做法在几何上平滑了优化景观（Landscape），防止策略变得过于“尖锐”或“脆弱”。通过扩大训练策略族的覆盖范围，使其能够包容推理系统带来的噪声。
*   **理论支撑：** 论文证明了当扰动标准差 $\sigma$ 匹配或超过推理偏差时，训练与推理分布之间的 KL 散度是有界的，从而保证更新维持在置信区域（Trust Region）内。

#### 3. 实验设计
*   **场景与数据集：**
    *   **单轮数学推理：** 使用 Guru RL-92k 和 OpenR1 子集进行训练。
    *   **多轮工具集成推理（TIR）：** 使用 Python 解释器解决复杂数学问题，数据集整合了 SimpleRL、Deepscaler 和 rStar2-Agent。
*   **Benchmark：** Math500、Minerva Math、Olympiad Bench、AIME2024、AIME2025。
*   **对比方法：** 
    *   **GRPO：** 标准的组相对策略优化。
    *   **Bypass：** 直接计算更新策略与推理策略比率的基准。
    *   **MIS（Masked Importance Sampling）：** 采用双重比率修正并进行截断/掩码的现有主流方法。

#### 4. 资源与算力
*   **硬件设备：** 实验使用了 **8 台 H100 GPU 和 4 台 A100 GPU**。
*   **模型规模：** 单轮任务基于 Qwen2.5-Math-1.5B-base；多轮任务基于 Qwen2.5-7B-base。
*   **训练细节：** 学习率为 $1 \times 10^{-6}$，KL 系数为 0.001，扰动初始标准差设为 $1 \times 10^{-4}$。

#### 5. 实验数量与充分性
*   **实验规模：** 论文涵盖了单轮和多轮两种核心推理范式，并在 5 个主流数学评测集上进行了验证。
*   **消融实验：** 进行了详尽的消融研究，包括扰动位置（全层 vs. 部分层 vs. 仅 Logits）、扰动规模敏感性、以及 Token 级与序列级比率的对比。
*   **充分性评价：** 实验设计较为全面，不仅关注最终准确率，还深入分析了训练过程中的梯度范数、熵、KL 散度等动态指标，客观地展示了 ALP 在抑制“训练崩溃”方面的优势。

#### 6. 主要结论与发现
*   **性能提升：** ALP 在所有基准测试中均优于 MIS 和 Bypass，尤其在 AIME 等高难度任务中表现突出。
*   **稳定性增强：** ALP 有效抑制了重要性比率的重尾现象，避免了训练后期的 KL 激增和奖励骤降。
*   **促进探索：** 通过 Pass@k 分析发现，ALP 增加了生成结果的多样性，显著提升了模型的探索效率。
*   **层级效应：** 扰动**所有层**的效果最好；在局部扰动中，底层（靠近输入）的效果通常优于顶层。

#### 7. 优点
*   **简洁高效：** 相比于需要精细调节多个截断阈值的 MIS 方法，ALP 提供了一个统一且易于实现的框架。
*   **理论与实证结合：** 论文不仅有直观的几何解释，还提供了严格的数学证明来解释为何扰动能控制分布间隙。
*   **通用性强：** 能够同时解决策略陈旧和系统级不匹配问题，适用于多种推理引擎和训练设置。

#### 8. 不足与局限
*   **扰动分布单一：** 目前主要探索了高斯噪声，虽然理论上支持任何零均值分布，但未对其他噪声类型（如拉普拉斯噪声）进行深入对比。
*   **异步系统验证不足：** 论文提到 ALP 在完全异步 RL 系统中具有潜力，但实验部分尚未覆盖这种极端复杂的离策场景。
*   **MoE 模型适配：** 尚未在混合专家模型（MoE）上进行测试，MoE 的路由机制可能会对扰动的分布产生更复杂的影响。

（完）
