---
title: Off-Policy Value-Based Reinforcement Learning for Large Language Models
title_zh: 大语言模型的离策基于价值的强化学习
authors: "Peng-Yuan Wang, Ziniu Li, Tian Xu, Bohan Yang, Tian-Shuo Liu, ChenYang Wang, Xiong-Hui Chen, Yi-Chen Li, Tianyun Yang, Congliang Chen, Yang Yu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23355v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型的离策强化学习
tldr: 针对大语言模型强化学习中在线策略方法采样效率低的问题，本文提出了一种基于价值的离线策略强化学习框架 ReVal。该方法通过贝尔曼更新结合逐步一致性信号与轨迹级结果验证，并引入经验回放机制以实现高效的数据复用。实验证明，ReVal 在数学推理任务上的收敛速度和最终性能均优于 GRPO，在 AIME24 和 GPQA 基准上取得了显著提升，为 LLM 训练提供了高效的新路径。
selection_source: fresh_fetch
motivation: 当前 LLM 强化学习主流方法多为在线策略，导致昂贵的采样数据被一次性丢弃，采样效率极低。
method: 提出 ReVal 框架，通过贝尔曼更新整合逐步一致性与结果验证信号，并利用经验回放池实现离线策略学习。
result: "ReVal 在 DeepSeek-R1-Distill-1.5B 模型上相比 GRPO 在 AIME24 提升了 2.7%，在 GPQA 提升了 4.5%，且收敛更快。"
conclusion: 基于价值的离线策略强化学习是提升大语言模型训练效率和推理能力的有效替代方案。
---

## 摘要
提高数据利用效率对于在生成轨迹成本高昂的长程任务中扩展强化学习（RL）至关重要。然而，目前主流的大语言模型（LLM）强化学习方法大多是同策（on-policy）的：它们对每批数据仅更新一次后便将其丢弃，随后收集新样本，导致样本效率低下。在这项工作中，我们探索了一种替代的基于价值的 LLM 强化学习框架，该框架天然支持离策（off-policy）学习。我们提出了 ReVal，这是一种基于贝尔曼更新（Bellman-update）的方法，它结合了捕捉内部一致性的步进式信号和源自结果验证的轨迹级信号。ReVal 天然支持基于经验回放池（replay-buffer）的训练，从而实现了对过去轨迹的高效重用。在标准数学推理基准测试上的实验表明，ReVal 不仅收敛更快，而且在最终性能上也优于 GRPO。在 DeepSeek-R1-Distill-1.5B 上，ReVal 提高了训练效率，并在 AIME24 上比 GRPO 提升了 2.7%，在域外基准测试 GPQA 上提升了 4.5%。这些结果表明，基于价值的强化学习是 LLM 训练中基于策略方法的实用替代方案。

## Abstract
Improving data utilization efficiency is critical for scaling reinforcement learning (RL) for long-horizon tasks where generating trajectories is expensive. However, the dominant RL methods for LLMs are largely on-policy: they update each batch of data only once, discard it, and then collect fresh samples, resulting in poor sample efficiency. In this work, we explore an alternative value-based RL framework for LLMs that naturally enables off-policy learning. We propose ReVal, a Bellman-update-based method that combines stepwise signals capturing internal consistency with trajectory-level signals derived from outcome verification. ReVal naturally supports replay-buffer-based training, allowing efficient reuse of past trajectories. Experiments on standard mathematical reasoning benchmarks show that ReVal not only converges faster but also outperforms GRPO in final performance. On DeepSeek-R1-Distill-1.5B, ReVal improves training efficiency and achieves improvement of 2.7% in AIME24 and 4.5% in out-of-domain benchmark GPQA over GRPO. These results suggest that value-based RL is a practical alternative to policy-based methods for LLM training.

---

## 论文详细总结（自动生成）

### 大语言模型的离策基于价值的强化学习 (ReVal) 论文总结

#### 1. 核心问题与整体含义
*   **研究动机**：当前大语言模型（LLM）的主流强化学习方法（如 PPO、GRPO、ReMax）大多是**同策（On-policy）**的。这意味着模型每生成一批数据，在进行一次或极少数次参数更新后就必须将其丢弃，然后重新采样。
*   **核心矛盾**：在长程推理任务中，自回归生成轨迹的计算成本极高。同策方法导致昂贵的采样数据利用率极低，成为扩展 LLM 推理能力的瓶颈。
*   **研究目标**：探索一种**离策（Off-policy）**的基于价值的强化学习框架，通过复用历史经验（经验回放）来提高采样效率和训练速度。

#### 2. 方法论：ReVal 框架
*   **核心思想**：将 LLM 的 Logits 直接解释为 Q 值（Logits-as-Q），从而在不增加额外价值模型（Critic）的情况下，实现基于贝尔曼更新（Bellman Update）的价值学习。
*   **关键技术细节**：
    *   **Logits 参数化 Q 函数**：利用预训练模型已有的 Logits 编码价值信息的特性，避免了从零学习 Q 函数的困难。
    *   **校准初始化（Calibrated Initialization）**：论文指出前人工作（如 TBRM）在奖励为零时会导致策略漂移。ReVal 通过引入**奖励塑造（Reward Shaping）**重新定义贝尔曼目标，确保在无奖励信号时模型保持与参考模型一致。
    *   **轨迹级贝尔曼残差损失**：设计了一个结合了逐步一致性信号和轨迹级结果验证信号的损失函数，通过最小化预测 Q 值与目标 Q 值之间的差异来优化模型。
    *   **经验回放池（Replay Buffer）**：引入 FIFO（先进先出）缓冲区存储历史轨迹。在每次迭代中，模型不仅从当前策略采样，还从缓冲区抽取旧数据进行多次更新（Off-policy Update）。

#### 3. 实验设计
*   **模型与场景**：
    *   **模型**：DeepSeek-R1-Distill-1.5B 和 Qwen2.5-Math-7B。
    *   **场景**：复杂的数学推理任务。
*   **基准测试（Benchmarks）**：包括 AIME24、AIME25、AMC、MATH、MINERVA、Olympiad Bench 和 GPQA（域外泛化测试）。
*   **对比方法**：
    *   **GRPO**：目前主流的 Actor-only 同策策略梯度方法。
    *   **TBRM**：一种基于价值但通常以同策方式运行的基准方法。

#### 4. 资源与算力
*   **算力说明**：论文提到了使用大规模 RL 训练框架 **Verl** 进行实现。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号（如 H100 或 A100）及具体数量。但提到 1.5B 模型训练使用了 8K 的最大序列长度，7B 模型也使用了 8K 限制，训练迭代次数为 650 次。在 1.5B 模型上，单次更新耗时约 2.8s，单次轨迹生成耗时约 36.8s。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同架构、不同规模的开源模型上进行了验证。
    *   覆盖了 7 个主流数学与科学推理基准。
    *   进行了**消融实验**，探讨了 KL 正则化、超参数 $\beta$、参考策略更新频率以及负样本利用对性能的影响。
*   **充分性评价**：实验设计较为充分，特别是针对“有限采样（N=1）”场景的测试，有力证明了离策方法在资源受限下的优势。对比实验涵盖了当前最先进的基准（GRPO），结果具有说服力。

#### 6 主要结论与发现
*   **效率提升**：ReVal 通过数据复用，在达到相同性能水平时，比 GRPO 实现了平均 **4.3 倍的收敛加速**。
*   **性能超越**：在 DeepSeek-R1-Distill-1.5B 上，ReVal 在 AIME24 上提升了 2.7%，在 GPQA 上提升了 4.5%。
*   **低采样场景优势**：在每轮仅采样 1 条轨迹（N=1）的极端情况下，ReVal 的领先优势更加明显，证明了其卓越的样本利用率。
*   **训练成本降低**：由于减少了生成轮数，尽管增加了更新次数，总训练时长仍缩短了约 18%。

#### 7. 优点
*   **单模型架构**：无需额外的 Critic 网络，节省了显存和计算开销，适合大规模模型。
*   **理论严谨性**：解决了价值学习中的初始化校准问题，保证了训练的稳定性。
*   **离策能力**：成功将传统 RL 中的经验回放机制引入 LLM 训练，打破了同策方法的效率瓶颈。

#### 8. 不足与局限
*   **超参数敏感性**：超参数 $\beta$ 与响应长度高度相关，不同模型和任务可能需要精细调优。
*   **回放策略简单**：目前仅使用了简单的 FIFO 缓冲区，尚未探索更高级的“优先经验回放（Prioritized Experience Replay）”。
*   **任务局限**：实验主要集中在有明确验证信号（Verifiable Rewards）的数学推理任务上，在主观性较强的通用对话任务中的表现尚待验证。

（完）
