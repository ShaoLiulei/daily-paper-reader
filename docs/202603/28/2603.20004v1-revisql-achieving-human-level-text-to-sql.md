---
title: "ReViSQL: Achieving Human-Level Text-to-SQL"
title_zh: ReViSQL：实现人类水平的 Text-to-SQL
authors: "Yuxuan Zhu, Tengjun Jin, Yoojin Choi, Daniel Kang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20004v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于Text-to-SQL推理的大语言模型
tldr: "ReViSQL是一个旨在实现人类水平Text-to-SQL性能的框架。针对现有模型在BIRD基准测试中存在的准确率瓶颈，该研究通过专家纠错构建了高质量数据集BIRD-Verified，并结合可验证奖励的强化学习（RLVR）和推理时缩放技术。实验证明，ReViSQL在BIRD Mini-Dev上达到了93.2%的执行准确率，首次超越人类水平，且在成本和效率上具有显著优势。"
selection_source: fresh_fetch
motivation: 现有的Text-to-SQL模型即便采用复杂的流水线设计，仍因训练数据质量问题无法在BIRD基准上达到人类水平的准确率。
method: 提出ReViSQL框架，通过专家验证纠正BIRD训练集错误，并利用可验证奖励的强化学习（RLVR）及推理时执行对齐和多数投票进行优化。
result: "ReViSQL-235B模型在BIRD Mini-Dev上取得93.2%的准确率，超越了92.96%的人类基准，且轻量级版本以更低成本达到了SOTA水平。"
conclusion: 提升数据质量而非增加架构复杂度是实现人类水平Text-to-SQL的关键，ReViSQL为该领域提供了高效且高性能的范式。
---

## 摘要
将自然语言转换为 SQL（Text-to-SQL）是数据库研究和数据分析应用中的一项关键挑战。最近的研究工作集中在通过开发大语言模型和 AI 智能体来增强 SQL 推理能力，这些智能体将 Text-to-SQL 任务分解为手动设计的逐步流水线。然而，尽管进行了这些广泛的架构工程努力，仍然存在显著差距：即使是最先进的（SOTA）AI 智能体在 BIRD 基准测试上尚未达到人类水平的准确率。在本文中，我们证明缩小这一差距并不需要进一步增加架构复杂性，而是需要干净的训练数据来提高底层模型的 SQL 推理能力。我们介绍了 ReViSQL，这是一个精简的框架，首次在 BIRD 上实现了人类水平的准确率。ReViSQL 并非采用复杂的 AI 智能体，而是在 BIRD-Verified 上利用带有可验证奖励的强化学习（RLVR）。BIRD-Verified 是我们基于 BIRD 训练集构建的一个包含 2500 个经过验证的 Text-to-SQL 实例的数据集。为了构建 BIRD-Verified，我们设计了一个涉及 SQL 专家的数据纠错和验证工作流。我们在 BIRD 训练集的一个子集中识别并纠正了 61.1% 的数据错误。通过在 BIRD-Verified 上进行训练，我们表明在相同的 RLVR 算法下，仅通过提高数据质量就能将单次生成准确率提升 8.2-13.9%。为了进一步增强性能，ReViSQL 通过基于执行的一致性协调和多数投票进行推理时扩展。在实验中，我们通过两种模型规模证明了我们框架的优越性：ReViSQL-235B-A22B 和 ReViSQL-30B-A3B。在专家验证的 BIRD Mini-Dev 集上，ReViSQL-235B-A22B 实现了 93.2% 的执行准确率，超过了代理人类水平准确率（92.96%），并比之前的开源 SOTA 方法高出 9.8%。我们的轻量级 ReViSQL-30B-A3B 以低 7.5 倍的单次查询成本达到了之前的 SOTA 水平。

## Abstract
Translating natural language to SQL (Text-to-SQL) is a critical challenge in both database research and data analytics applications. Recent efforts have focused on enhancing SQL reasoning by developing large language models and AI agents that decompose Text-to-SQL tasks into manually designed, step-by-step pipelines. However, despite these extensive architectural engineering efforts, a significant gap remains: even state-of-the-art (SOTA) AI agents have not yet achieved the human-level accuracy on the BIRD benchmark. In this paper, we show that closing this gap does not require further architectural complexity, but rather clean training data to improve SQL reasoning of the underlying models.   We introduce ReViSQL, a streamlined framework that achieves human-level accuracy on BIRD for the first time. Instead of complex AI agents, ReViSQL leverages reinforcement learning with verifiable rewards (RLVR) on BIRD-Verified, a dataset we curated comprising 2.5k verified Text-to-SQL instances based on the BIRD Train set. To construct BIRD-Verified, we design a data correction and verification workflow involving SQL experts. We identified and corrected data errors in 61.1% of a subset of BIRD Train. By training on BIRD-Verified, we show that improving data quality alone boosts the single-generation accuracy by 8.2-13.9% under the same RLVR algorithm. To further enhance performance, ReViSQL performs inference-time scaling via execution-based reconciliation and majority voting. Empirically, we demonstrate the superiority of our framework with two model scales: ReViSQL-235B-A22B and ReViSQL-30B-A3B. On an expert-verified BIRD Mini-Dev set, ReViSQL-235B-A22B achieves 93.2% execution accuracy, exceeding the proxy human-level accuracy (92.96%) and outperforming the prior open-source SOTA method by 9.8%. Our lightweight ReViSQL-30B-A3B matches the prior SOTA at a 7.5$\times$ lower per-query cost.

---

## 论文详细总结（自动生成）

以下是对论文《ReViSQL: Achieving Human-Level Text-to-SQL》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）和复杂的 AI Agent 流水线在 Text-to-SQL 任务上取得了进展，但在权威基准测试 BIRD 上，AI 的表现始终无法触及人类专家水平（约 93% 的准确率）。
*   **核心瓶颈**：作者指出，当前的瓶颈不在于推理架构的复杂度，而在于**训练数据的严重污染**。现有主流数据集（如 BIRD 训练集）存在超过 50% 的标注错误，这导致强化学习（RL）过程中产生虚假奖励信号，阻碍了模型内化真正的 SQL 推理能力。
*   **研究目标**：通过提升数据质量和引入推理时缩放（Inference-time Scaling），证明精简的框架也能实现人类水平的 Text-to-SQL 性能。

### 2. 方法论
ReViSQL 框架由三个核心支柱组成：
*   **BIRD-Verified 数据集构建**：
    *   **专家纠错流水线**：针对 BIRD 训练集中的 2500 个实例，通过“AI 提示 + 专家多轮人工修正 + 独立验证 + 冲突解决”的严苛流程，纠正了 52.1% 的 SQL 错误、26.2% 的问题错误和 18.2% 的外部知识错误。
    *   **精细化评分**：引入了基于子集（Subset-based）和基于列表（List-based）的评分方法，以应对 Top-k 或排序等复杂查询。
*   **基于可验证奖励的强化学习（RLVR）**：
    *   使用 **CISPO 算法**（一种改进的 GRPO 算法）在 BIRD-Verified 上进行训练。
    *   模型在训练中被允许进行多轮交互（Rollout），通过执行结果的正确性获得奖励，从而将复杂的推理路径（如 Schema 探索、逻辑自纠）内化到模型参数中。
*   **推理时缩放与生成协调（Reconciliation）**：
    *   **多候选生成**：在推理阶段并行生成多个 SQL 候选。
    *   **协调过滤**：利用未经过 RL 训练的原始基座模型（保留了更广泛的语言理解能力）作为过滤器，检查候选 SQL 是否满足问题中的显式约束。
    *   **多数投票**：在过滤后的候选池中通过执行结果的一致性进行多数投票。

### 3. 实验设计
*   **数据集**：
    *   **BIRD 相关**：Arcwise-Plat-Full（全纠错版）和 Arcwise-Plat-SQL（仅纠错 SQL 版）。
    *   **Spider 2 相关**：Spider 2-SQLite 和 Spider 2-Snow（测试跨方言和复杂长上下文的泛化性）。
*   **对比方法**：
    *   **SOTA Agent**：OpenSearch-SQL, GenaSQL, SHARE, Contextual-SQL 等。
    *   **微调模型**：OmniSQL-32B, XiYanSQL-32B, Arctic-R1-7B 等。
    *   **前沿模型**：GPT-5.2, Kimi-K2 等。
*   **评估指标**：执行准确率（Execution Accuracy）和推理成本（USD/Query）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Intel 8568Y+ CPU**、**512 GB 内存**和 **NVIDIA H100 GPU** 的机器上完成。
*   **软件框架**：使用 Tinker 作为训练基础设施。
*   **训练细节**：LoRA 秩为 32，Batch Size 为 64，学习率为 5e-5。训练时长未明确给出具体小时数，但提到根据验证集准确率收敛情况决定。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 3B（活跃参数）到 235B（总参数）两种规模的模型。
    *   在 4 个主流基准测试上进行了全面评估。
    *   **消融实验**：详细分析了训练数据质量、验证集质量、采样候选数量（从 1 到 129 个）、训练数据规模对最终性能的影响。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了准确率，还对比了推理成本，并展示了模型在不同预算下的 Pareto 前沿。通过跨数据集（Spider 2）的测试，验证了方法的泛化性而非仅仅是过拟合 BIRD。

### 6. 主要结论与发现
*   **人类水平达成**：ReViSQL-235B 在 BIRD 验证集上达到了 **93.2%** 的准确率，首次超过了 92.96% 的人类基准。
*   **数据质量胜过架构**：在相同算法下，使用纠错后的数据训练，准确率比原始数据提升了 **8.2%–13.9%**。
*   **成本优势**：轻量级的 ReViSQL-30B 模型以 **7.5 倍更低的成本**达到了之前最强开源 Agent 的性能。
*   **推理缩放有效性**：推理时增加候选数量能显著提升性能，但前提是底层模型具备足够的 SQL 推理基础。

### 7. 优点
*   **大道至简**：打破了“必须使用复杂多阶段 Agent 才能做好 Text-to-SQL”的固有思维，回归到数据质量和强化学习本质。
*   **高质量资源贡献**：开源了 BIRD-Verified 数据集，为社区提供了极具价值的干净训练资源。
*   **帕累托最优**：在准确率和推理成本之间建立了新的最优边界，具有极强的工业应用潜力。

### 8. 不足与局限
*   **专家标注依赖**：构建 BIRD-Verified 需要极高的人工成本和 SQL 专家参与，难以快速扩展到所有垂直领域。
*   **长上下文挑战**：在处理 Spider 2-Snow 等超长 Schema 场景时，仍需依赖外部模型（如 Kimi-K2）进行预过滤，尚未实现完全的端到端原生处理。
*   **方言覆盖**：虽然在 Snowflake 上表现良好，但对于更多样化的数据库方言（如 BigQuery, Oracle）的覆盖仍有待进一步验证。

（完）
