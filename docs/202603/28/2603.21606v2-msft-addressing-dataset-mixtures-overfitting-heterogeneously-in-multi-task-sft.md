---
title: "mSFT: Addressing Dataset Mixtures Overfitting Heterogeneously in Multi-task SFT"
title_zh: mSFT：解决多任务 SFT 中混合数据集的异质性过拟合问题
authors: "Woosung Koh, Jeyoung Jeon, Youngjin Song, Yujin Cheon, Soowon Oh, Jaehyeong Choi, Se-Young Yun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21606v2"
tags: ["query:ai-llm"]
score: 10.0
evidence: 语言模型的多任务有监督微调算法
tldr: 针对多任务监督微调（SFT）中不同数据集学习动态不一致导致的过拟合与欠拟合并存问题，本文提出mSFT算法。该算法通过迭代识别并剔除过拟合子任务，并回退至对应最优检查点继续训练，从而优化数据混合策略。实验证明mSFT在多个基准和模型上均优于基准方法，且能有效降低计算开销并提升模型整体性能。
selection_source: fresh_fetch
motivation: 传统多任务SFT采用统一计算预算，导致学习较快的任务过早过拟合，而学习较慢的任务仍处于欠拟合状态。
method: 提出一种迭代式的过拟合感知搜索算法，通过动态识别并排除已过拟合的子数据集，并回退到该任务的最优状态继续训练。
result: 在10个基准测试和6个基础模型上的实验显示，mSFT一致优于4种基准方法，且在低计算预算下能提升性能并减少训练开销。
conclusion: mSFT为多任务SFT提供了一种实用的过拟合感知优化方案，能够最大化模型在多样化数据混合中的潜力。
---

## 摘要
目前的语言模型训练通常在所有子数据集上使用统一的计算预算进行多任务有监督微调（SFT）。这种方法从根本上说是次优的：异质的学习动态导致学习较快的任务过早过拟合，而学习较慢的任务仍处于欠拟合状态。为了解决这一问题，我们引入了 mSFT，这是一种针对多任务数据混合的迭代式、过拟合感知的搜索算法。mSFT 在活跃的混合数据集上训练模型，识别并排除最早过拟合的子数据集，并在继续训练前恢复到该特定的最优检查点。广泛的评估表明，mSFT 在 10 个基准测试和 6 个基础模型上始终优于 4 个基准方法。进一步的分析证实，mSFT 在不同的数据集规模、任务粒度下均能保持稳健的增益，且对其唯一的新超参数（计算预算）不敏感。值得注意的是，在低计算预算下，mSFT 可以在降低训练 FLOPs 的同时提高性能。最终，mSFT 为多任务 SFT 建立了一种实用的过拟合感知算法，能够最大限度地发挥模型在各种数据混合中的潜力。

## Abstract
Current language model training commonly applies multi-task Supervised Fine-Tuning (SFT) using a homogeneous compute budget across all sub-datasets. This approach is fundamentally sub-optimal: heterogeneous learning dynamics cause faster-learning tasks to overfit early while slower ones remain under-fitted. To address this, we introduce mSFT, an iterative, overfitting-aware search algorithm for multi-task data mixtures. mSFT trains the model on an active mixture, identifies and excludes the earliest overfitting sub-dataset, and reverts to that specific optimal checkpoint before continuing. Extensive evaluations demonstrate that mSFT consistently outperforms 4 baselines across 10 benchmarks and 6 base models. Further analysis confirms mSFT maintains robust gains across diverse dataset sizes, task granularities, and is insensitive to its single new hyperparameter (compute budget). Notably, at low compute budget, mSFT can improve performance while lowering training FLOPs. Ultimately, mSFT establishes a practical overfitting-aware algorithm for multi-task SFT that maximizes the potential of models across diverse data mixtures.

---

## 论文详细总结（自动生成）

以下是对论文《mSFT: Addressing Dataset Mixtures Overfitting Heterogeneously in Multi-task SFT》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在大型语言模型（LLM）的多任务有监督微调（SFT）阶段，现有的主流做法是对所有子数据集应用统一的计算预算（如相同的 Epoch 数）。
*   **研究动机**：作者发现不同任务的学习动态具有**异质性（Heterogeneous）**。如果采用统一的计算量，学习较快的任务会过早发生过拟合，而学习较慢的任务则可能处于欠拟合状态。
*   **整体含义**：论文旨在打破“同质化训练”的现状，提出一种能够感知各任务过拟合状态并动态调整训练路径的算法，以最大化多任务混合训练的泛化性能。

### 2. 论文提出的方法论：mSFT
mSFT（multi-task mixture SFT）是一种迭代式的、过拟合感知的搜索与训练算法。其核心流程如下：
*   **核心思想**：通过“试错”与“回退”机制，确保每个子任务都在其泛化性能最强点停止贡献梯度。
*   **算法流程**：
    1.  **Roll-out（展开）**：在当前活跃的子数据集混合体上训练模型，设定一个较小的计算预算 $C$（如 1/4 个 Epoch），并记录每个子任务在验证集上的准确率。
    2.  **识别过拟合**：在训练过程中，识别出最早达到性能峰值并开始下降（即过拟合）的子数据集 $D_{exclude}$。
    3.  **Roll-back（回退）**：将模型参数回退到该特定子数据集达到最优性能时的检查点（Checkpoint）。
    4.  **排除与迭代**：将已过拟合的 $D_{exclude}$ 从活跃数据集中剔除，然后从回退后的检查点开始，对剩余任务重复上述过程，直到所有任务都被排除或达到总预算。
*   **关键技术细节**：为了解决排除数据后优化轨迹偏移的问题，mSFT 采用迭代搜索而非单次搜索，确保搜索阶段与实际训练阶段的梯度环境一致。

### 3. 实验设计
*   **基础模型**：涵盖了不同架构和规模的 6 个模型，包括 OLMo 2 (1B)、Qwen2.5 (0.5B, 1.5B, 3B, 7B) 和 Qwen3 (8B)。
*   **数据集与基准**：使用了 10 个主流基准测试，涵盖科学知识（SciQ, ARC-Easy等）、常识语言（HellaSwag, BoolQ等）和数学推理（GSM8K, AQUA-RAT等）。
*   **对比方法（Baselines）**：
    1.  **Standard SFT**：标准的同质化微调。
    2.  **Continual SFT**：顺序训练各任务。
    3.  **DynamixSFT**：基于多臂老虎机的动态混合比例优化。
    4.  **IES (Instance-dependent Early Stopping)**：基于样本级二阶导数的早停方法。
    5.  **消融基准**：SRO SFT（单次搜索后剔除）和 Soft SRO SFT（软性比例调整）。

### 4. 资源与算力
*   **硬件设备**：论文提到了使用 **NVIDIA B200, H200, RTX A5000 和 RTX 3090** 进行实验。
*   **算力开销分析**：虽然 mSFT 涉及多次回退和重复训练，但作者指出 SFT 阶段的计算量仅占模型全生命周期（预训练+后训练）总算力的约 **0.01%**。
*   **训练时长**：未明确给出总小时数，但通过 FLOPs 分析显示，在低计算预算 $C=1$ 时，mSFT 甚至能比标准 SFT 节省约 120.3 PFLOPs 的算力，因为它能及时剔除冗余数据。

### 5. 实验数量与充分性
*   **实验规模**：
    *   跨越了从 0.5B 到 8B 的多个模型参数量。
    *   进行了不同数据集规模（9K, 18K, 27K 样本）和任务数量（5, 10, 15 个任务）的鲁棒性测试。
    *   在 MedMCQA 的 21 个细分医疗类别上验证了极细粒度下的有效性。
*   **充分性与公平性**：实验设计较为全面，包含了消融实验来验证“迭代回退”的必要性。所有对比方法均在相同的超参数搜索空间下进行了优化，确保了比较的客观性。

### 6. 论文的主要结论与发现
*   **性能提升**：mSFT 在所有 6 个模型和 10 个基准上均取得了最佳的平均准确率，尤其在数学和逻辑推理任务上提升显著（最高达 +3.0%）。
*   **稳定性**：mSFT 显著降低了不同任务间性能表现的标准差，说明它提升了模型的整体均衡性，而非仅靠个别任务拉高平均分。
*   **算力效率**：在低计算预算下，mSFT 实现了“性能提升”与“算力节省”的双赢。
*   **训练动态**：通过剔除过拟合任务，mSFT 往往能获得比标准 SFT 更低的训练损失（Train Loss），这暗示了减少梯度冲突（Gradient Conflict）的潜在益处。

### 7. 优点
*   **解决核心痛点**：直接针对多任务 SFT 中普遍存在的异质过拟合问题，理论动机清晰。
*   **算法简洁实用**：不需要复杂的梯度操作或二阶导数计算，仅在数据调度层面进行干预，易于集成到现有框架。
*   **鲁棒性强**：对新增的超参数（计算预算 $C$）不敏感，且在不同任务粒度下均有效。
*   **算力友好**：证明了在后训练阶段增加少量搜索开销是极其划算的。

### 8. 不足与局限
*   **磁盘空间开销**：由于需要频繁保存和回退检查点，mSFT 对磁盘存储的要求高于标准 SFT（实验显示平均约为标准 SFT 的 4.44 倍）。虽然论文提出了检查点剪枝算法，但仍存在一定负担。
*   **验证集依赖**：算法高度依赖验证集的质量和代表性。如果验证集与测试集分布不一致，早停机制可能会失效。
*   **硬剔除风险**：采用“硬剔除（Hard Exclusion）”可能导致轻微的灾难性遗忘，虽然实验证明过拟合收益大于遗忘损失，但在极长期训练中的表现仍需观察。

（完）
