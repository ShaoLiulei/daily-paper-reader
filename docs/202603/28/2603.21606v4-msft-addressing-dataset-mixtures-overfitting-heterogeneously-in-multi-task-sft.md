---
title: "mSFT: Addressing Dataset Mixtures Overfitting Heterogeneously in Multi-task SFT"
title_zh: mSFT：解决多任务 SFT 中混合数据集的异构过拟合问题
authors: "Woosung Koh, Jeyoung Jeon, Youngjin Song, Yujin Cheon, Soowon Oh, Jaehyeong Choi, Se-Young Yun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21606v4"
tags: ["query:ai-llm"]
score: 10.0
evidence: 多任务SFT的过拟合感知搜索算法
tldr: 针对多任务监督微调（SFT）中不同数据集学习动态不一致导致的过拟合与欠拟合并存问题，本文提出mSFT算法。该算法通过迭代识别并剔除过拟合子任务，并回退至对应最优检查点继续训练，从而优化数据混合策略。实验证明mSFT在多个基准和模型上均优于基准方法，且能有效降低计算开销并提升模型整体性能。
selection_source: fresh_fetch
motivation: 传统多任务SFT采用统一计算预算，导致学习较快的任务过早过拟合，而学习较慢的任务仍处于欠拟合状态。
method: 提出一种迭代式的过拟合感知搜索算法，通过动态识别并排除已过拟合的子数据集，并回退到该任务的最优状态继续训练。
result: 在10个基准测试和6个基础模型上的实验显示，mSFT一致优于4种基准方法，且在低计算预算下能提升性能并减少训练开销。
conclusion: mSFT为多任务SFT提供了一种实用的过拟合感知优化方案，能够最大化模型在多样化数据混合中的潜力。
---

## 摘要
当前的语言模型训练通常在所有子数据集上使用同质的计算预算进行多任务有监督微调（SFT）。这种方法从根本上说是次优的：异构的学习动态导致学习较快的任务过早过拟合，而学习较慢的任务仍处于欠拟合状态。为解决这一问题，我们引入了 mSFT，这是一种针对多任务数据混合的迭代式、过拟合感知的搜索算法。mSFT 在活跃的混合数据集上训练模型，识别并排除最早过拟合的子数据集，并在继续训练前回退到该特定的最优检查点。广泛的评估表明，mSFT 在 10 个基准测试和 6 个基础模型上始终优于 4 个基准方法。进一步的分析证实，mSFT 在不同的数据集规模、任务粒度下均能保持稳健的增益，且对其唯一的新超参数（计算预算）不敏感。值得注意的是，在低计算预算下，mSFT 可以在降低训练 FLOPs 的同时提高性能。最终，mSFT 为多任务 SFT 建立了一种实用的过拟合感知算法，能够最大限度地发挥模型在多样化数据混合中的潜力。

## Abstract
Current language model training commonly applies multi-task Supervised Fine-Tuning (SFT) using a homogeneous compute budget across all sub-datasets. This approach is fundamentally sub-optimal: heterogeneous learning dynamics cause faster-learning tasks to overfit early while slower ones remain under-fitted. To address this, we introduce mSFT, an iterative, overfitting-aware search algorithm for multi-task data mixtures. mSFT trains the model on an active mixture, identifies and excludes the earliest overfitting sub-dataset, and reverts to that specific optimal checkpoint before continuing. Extensive evaluations demonstrate that mSFT consistently outperforms 4 baselines across 10 benchmarks and 6 base models. Further analysis confirms mSFT maintains robust gains across diverse dataset sizes, task granularities, and is insensitive to its single new hyperparameter (compute budget). Notably, at low compute budget, mSFT can improve performance while lowering training FLOPs. Ultimately, mSFT establishes a practical overfitting-aware algorithm for multi-task SFT that maximizes the potential of models across diverse data mixtures.

---

## 论文详细总结（自动生成）

这是一份关于论文《mSFT: Addressing Dataset Mixtures Overfitting Heterogeneously in Multi-task SFT》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在大型语言模型（LLM）的后训练阶段，多任务监督微调（SFT）通常将多个子数据集混合在一起，并对所有数据应用统一的计算预算（如相同的训练轮数 Epoch）。
*   **核心矛盾**：论文指出这种“同质化”训练是次优的。由于不同任务的数据分布和复杂度不同，它们表现出**异构的学习动态**。学习较快的任务会过早进入过拟合状态，而学习较慢的任务此时仍处于欠拟合状态。
*   **研究意义**：论文旨在打破这种僵化的训练范式，提出一种能够感知各任务过拟合状态并动态调整训练进程的算法，以最大化模型在复杂混合数据上的泛化能力。

### 2. 论文提出的方法论：mSFT
mSFT（multi-task SFT）的核心思想是**“迭代式搜索与回退”**。
*   **核心流程**：
    1.  **初始化**：将所有子数据集放入活跃集合。
    2.  **Roll-out（试运行）**：在当前活跃的数据混合体上训练模型一个预设的计算预算 $C$（如 1/4 个 Epoch），并记录每个子任务在验证集上的性能曲线。
    3.  **识别过拟合**：确定哪个子数据集最早达到了性能峰值（即开始过拟合的点 $c_{min}$）。
    4.  **Roll-back（回退）**：将模型参数回退到该最早过拟合任务的最优检查点状态。
    5.  **排除与迭代**：将该已达最优的任务从活跃集合中剔除，剩余任务继续从回退点开始下一轮训练，直到所有任务都被排除。
*   **关键技术细节**：论文通过实验证明，简单的“一次性搜索”是无效的，因为剔除一个任务会改变剩余任务的梯度方向和优化轨迹。因此，mSFT 采用迭代方式，确保搜索过程与实际训练轨迹对齐。

### 3. 实验设计
*   **基础模型**：涵盖了不同架构和规模的 6 个模型：OLMo 2 (1B)、Qwen2.5 (0.5B, 1.5B, 3B, 7B) 以及 Qwen3 (8B)。
*   **数据集与基准**：使用了 10 个主流基准测试，涵盖科学知识（ARC-Easy, SciQ）、常识推理（HellaSwag, Winogrande, BoolQ）和数学逻辑（GSM8K, AQUA-RAT）等领域。
*   **对比方法（Baselines）**：
    1.  **Standard SFT**：标准的同质化微调。
    2.  **Continual SFT**：顺序训练各任务。
    3.  **DynamixSFT**：基于多臂老虎机的动态比例优化。
    4.  **IES (Instance-dependent Early Stopping)**：基于实例级别的二阶导数早停法。
    5.  **消融变体**：SRO SFT（单次搜索）和 Soft SRO SFT（软性比例调整）。

### 4. 资源与算力
*   **硬件设备**：实验使用了 NVIDIA **B200、H200、RTX A5000 和 RTX 3090** GPU。
*   **算力开销**：论文强调 SFT 阶段的算力仅占模型全生命周期（预训练+微调）的约 **0.01%**。
*   **效率发现**：在低计算预算（$C=1$）下，mSFT 不仅性能更好，且由于动态剔除了已完成的任务，其实际训练 FLOPs 比标准 SFT 更低（节省了约 120.3 PFLOPs）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 6 个模型在 10 个基准上的交叉验证。
*   **鲁棒性测试**：针对不同数据规模（9K 到 27K 样本）、不同任务数量（5 到 15 个任务）以及不同任务粒度（将 MedMCQA 拆分为 21 个子类）进行了测试。
*   **充分性评价**：实验设计非常充分且客观。作者不仅关注平均分，还分析了标准差（证明增益的稳定性）和 1st Place 占据次数。消融实验清晰地展示了迭代机制的必要性。

### 6. 论文的主要结论与发现
*   **性能领先**：mSFT 在所有 6 个模型上均取得了最高平均精度，尤其在数学和逻辑推理任务上提升显著（最高达 +3.0%）。
*   **解决梯度冲突**：通过剔除过拟合任务，mSFT 能够降低训练损失（Train Loss），这表明该方法缓解了不同任务间的梯度干扰。
*   **计算效率**：mSFT 证明了“多花一点搜索算力”可以换取“更早排除冗余任务”，在特定配置下实现了性能与算力的双赢。

### 7. 优点：亮点与创新
*   **机制创新**：首次系统性地提出并解决了多任务 SFT 中的异构过拟合问题，将“早停”概念从全局细化到了子任务级别。
*   **实用性强**：算法逻辑清晰，仅引入了一个不敏感的超参数 $C$，易于在工业界大规模训练流水线中集成。
*   **存储优化**：针对回退机制带来的磁盘压力，提出了动态检查点剪枝算法，将存储开销控制在合理范围内（平均约为模型大小的 4.44 倍）。

### 8. 不足与局限
*   **存储开销**：尽管有剪枝算法，但相比标准 SFT 只存一个检查点，mSFT 仍需占用数倍的磁盘空间。
*   **验证集依赖**：该方法高度依赖于高质量的验证集来准确捕捉过拟合点。如果验证集与测试集存在分布偏移，搜索到的最优值可能失效。
*   **任务定义**：算法的效果受限于“任务”的划分粒度。如果一个大型数据集中包含多个隐含的异构子任务但未被显式拆分，mSFT 的优势将无法完全发挥。

（完）
