---
title: "Schema on the Inside: A Two-Phase Fine-Tuning Method for High-Efficiency Text-to-SQL at Scale"
title_zh: 模式内置：一种用于大规模高效 Text-to-SQL 的两阶段微调方法
authors: "Chinmay Soni, Shivam Chourasia, Gaurav Kumar, Hitesh Kapoor"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24023v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于Text-to-SQL的两阶段监督微调
tldr: "针对大规模Text-to-SQL应用中API调用成本高、延迟大的挑战，本文提出一种两阶段监督微调方法，使8B参数的本地模型能够内化完整的数据库模式。该方法在拥有2.5亿用户的板球统计平台CriQ中应用，将输入Token减少了99%以上，且在执行成功率和语义准确率上均优于Gemini Flash 2.0，为工业级Text-to-SQL提供了高效、低成本的解决方案。"
selection_source: fresh_fetch
motivation: 旨在解决依赖大型API模型处理复杂Schema时带来的高昂Token成本和显著推理延迟问题。
method: 采用两阶段监督微调策略，使模型在训练阶段内化数据库模式，从而在推理时无需在Prompt中包含冗长的Schema信息。
result: "输入Token从1.7万降至不足100个，执行成功率达98.4%，语义准确率达92.5%，性能全面超越了Gemini Flash 2.0基准。"
conclusion: 研究证明通过领域专用的自托管小模型内化Schema，是实现高精度、低延迟且极具成本效益的大规模Text-to-SQL生产部署的可行路径。
---

## 摘要
将大型、专有的基于 API 的语言模型应用于 Text-to-SQL 任务面临着重大的行业挑战：依赖包含大量模式（schema）的冗长提示词会导致极高的单 token API 成本和高延迟，阻碍了大规模生产环境的部署。我们展示了一个专门的、自托管的 8B 参数模型，该模型专为 CriQ 中的对话机器人设计。CriQ 是印度最大的幻想体育平台 Dream11（拥有超过 2.5 亿用户）的姐妹应用，用于回答用户关于板球统计数据的查询。我们新颖的两阶段有监督微调方法使模型能够内化整个数据库模式，从而消除了对长上下文提示词的需求。这将输入 token 减少了 99% 以上，从 17k token 的基准减少到不足 100 个，并用高效的本地推理取代了昂贵的外部 API 调用。由此产生的系统实现了 98.4% 的执行成功率和 92.5% 的语义准确率，显著优于使用 Google Gemini Flash 2.0 的提示工程基准（95.6% 的执行成功率，89.4% 的语义准确率）。这些结果展示了在大规模生产环境中使用领域专门化、自托管的语言模型实现高精度、低延迟 Text-to-SQL 应用的切实可行路径。

## Abstract
Applying large, proprietary API-based language models to text-to-SQL tasks poses a significant industry challenge: reliance on massive, schema-heavy prompts results in prohibitive per-token API costs and high latency, hindering scalable production deployment. We present a specialized, self-hosted 8B-parameter model designed for a conversational bot in CriQ, a sister app to Dream11, India's largest fantasy sports platform with over 250 million users, that answers user queries about cricket statistics. Our novel two-phase supervised fine-tuning approach enables the model to internalize the entire database schema, eliminating the need for long-context prompts. This reduces input tokens by over 99%, from a 17k-token baseline to fewer than 100, and replaces costly external API calls with efficient local inference. The resulting system achieves 98.4% execution success and 92.5% semantic accuracy, substantially outperforming a prompt-engineered baseline using Google's Gemini Flash 2.0 (95.6% execution, 89.4% semantic accuracy). These results demonstrate a practical path toward high-precision, low-latency text-to-SQL applications using domain-specialized, self-hosted language models in large-scale production environments.

---

## 论文详细总结（自动生成）

以下是对论文《Schema on the Inside: A Two-Phase Fine-Tuning Method for High-Efficiency Text-to-SQL at Scale》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在工业级 Text-to-SQL 应用中，主流做法是调用大型 API 模型（如 Gemini、GPT-4）。为了让模型理解数据库，必须在 Prompt 中加入完整的数据库模式（Schema），这导致 Prompt 极其冗长（本案中达 1.7 万 Token）。
*   **核心问题**：冗长的 Prompt 带来了**高昂的 API 成本**和**显著的推理延迟**，严重阻碍了在大规模生产环境（如拥有 2.5 亿用户的 Dream11 平台）中的部署。
*   **整体含义**：本文旨在通过微调使模型“内化”Schema 知识，从而在推理时无需输入 Schema，实现高精度、低延迟且低成本的本地化部署。

### 2. 方法论
核心思想是**模式内化（Schema Internalization）**，通过两阶段监督微调（SFT）将数据库结构编码进模型参数中。
*   **数据准备**：利用高性能推理模型（Qwen/QwQ 32B）通过“自我纠错循环”生成高质量的“问题-SQL”对，构建训练集。
*   **第一阶段：模式学习与关联（Schema Learning）**
    *   **混合任务训练**：模型同时学习“全上下文 SQL 生成”（带 Schema 的 Prompt）和“显式模式记忆”（给定数据库 ID，输出完整 Schema 结构）。
    *   **目的**：让模型在参数中建立数据库 ID 与具体表结构、关联关系的强映射。
*   **第二阶段：模式召回与应用（Schema Recall）**
    *   **去“拐杖”训练**：移除 Prompt 中的 Schema 信息，仅保留用户问题和数据库 ID 触发器。
    *   **目的**：强制模型依赖第一阶段内化的参数知识来生成 SQL。
*   **关键技术**：使用 LoRA（Rank=64）进行参数高效微调，并采用 vLLM 框架进行高性能推理。

### 3. 实验设计
*   **应用场景**：板球统计数据查询（CriQ 应用），涉及复杂的表连接、聚合和领域特定逻辑。
*   **数据集**：
    *   训练集：包含 8 万个全上下文示例、1.5 万个模式记忆示例及 15 万个微型 Prompt 示例。
    *   测试集：3 万条真实的、未见过的用户查询。
*   **Benchmark（基准）**：
    1.  **初始原型**：Gemini Flash（10k Token Prompt）。
    2.  **提示工程基准**：经过深度优化的 Gemini 2.0 Flash（17k Token Prompt）。
*   **对比指标**：执行成功率（Execution Success）和语义准确率（Semantic Accuracy）。

### 4. 资源与算力
*   **硬件环境**：使用了由 **8 台 NVIDIA H100 80GB GPU** 组成的集群。
*   **训练细节**：采用分布式数据并行（DDP）加速，使用 AdamW 优化器，学习率为 2e-5。
*   **时长说明**：文中未明确给出具体的训练总时长（小时/天），但提到通过 LoRA 显著加快了迭代速度。

### 5. 实验数量与充分性
*   **实验规模**：测试集规模达 3 万条，远超许多学术论文的规模，具有极强的工业代表性。
*   **消融实验**：
    1.  **两阶段必要性**：对比了直接进行单阶段微调的效果（准确率从 98.4% 跌至 74.96%）。
    2.  **显式记忆任务的影响**：证明了加入“模式记忆任务”能显著提升召回鲁棒性。
    3.  **模型规模对比**：测试了 Qwen 3 系列从 0.5B 到 8B 的表现，验证了 8B 是精度与成本的最优平衡点。
*   **充分性评价**：实验设计非常充分且具有客观性，通过消融实验清晰地证明了每一个技术决策的合理性。

### 6. 主要结论与发现
*   **效率飞跃**：输入 Token 从 1.7 万个减少到不足 100 个，**缩减幅度超过 99%**。
*   **性能超越**：8B 本地模型在执行成功率（98.4%）和语义准确率（92.5%）上均**全面超越**了强大的 Gemini 2.0 Flash 基准。
*   **成本转型**：将高昂且波动的 API 流量计费转变为固定且可控的本地基础设施成本。

### 7. 优点
*   **极具实用性**：直接解决了工业界 Text-to-SQL 落地最头疼的“Token 膨胀”问题。
*   **两阶段配方清晰**：提出的“先学后考”微调策略具有很强的可操作性和可重复性。
*   **高性能推理**：结合 vLLM 和 Paged-Attention，确保了高并发下的低延迟响应。

### 8. 不足与局限
*   **模式静态性限制**：该方法假设数据库 Schema 是相对静态的。如果数据库结构发生重大变化，模型需要重新进行微调循环，缺乏动态适应性。
*   **领域局限**：实验主要集中在板球统计领域，虽然方法论通用，但在跨领域（Cross-domain）的泛化能力未在文中通过 Spider 等通用榜单进一步验证。
*   **长尾错误**：模型在处理极复杂的临时推理或歧义别名时仍存在少量失败案例。

（完）
