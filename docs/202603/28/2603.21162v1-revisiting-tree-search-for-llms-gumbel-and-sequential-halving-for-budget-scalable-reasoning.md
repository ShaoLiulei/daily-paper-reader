---
title: "Revisiting Tree Search for LLMs: Gumbel and Sequential Halving for Budget-Scalable Reasoning"
title_zh: 重新审视大语言模型的树搜索：用于预算可扩展推理的 Gumbel 与 Sequential Halving
authors: "Leonid Ugadiarov, Yuri Kuratov, Aleksandr Panov, Alexey Skrynnik"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21162v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 通过可扩展预算的树搜索增强大语言模型的推理能力
tldr: 针对大语言模型在推理任务中使用AlphaZero式树搜索时出现的性能随预算增加而下降的“缩放失效”问题，本文提出了ReSCALE框架。该方法通过引入Gumbel采样和顺序减半（Sequential Halving）机制替代传统的Dirichlet噪声和PUCT选择，成功恢复了推理性能的单调增长。在GSM8K和Game24任务上，ReSCALE显著提升了高预算下的准确率，证明了其在预算可扩展推理中的有效性。
selection_source: fresh_fetch
motivation: 传统的AlphaZero式树搜索在LLM推理中存在预算增加反而导致性能下降的缩放失效问题。
method: 提出ReSCALE框架，利用Gumbel采样和顺序减半机制优化搜索过程，无需重新训练模型即可实现性能提升。
result: "在GSM8K和Game24基准测试中，ReSCALE在基准方法性能退化的预算水平下分别达到了58.4%和85.3%的准确率。"
conclusion: 顺序减半机制是恢复LLM树搜索预算可扩展性的关键，使推理性能随计算资源投入而稳定提升。
---

## 摘要
神经树搜索是一种强大的决策算法，广泛应用于博弈和基于模型的强化学习等复杂领域。近期的研究已应用 AlphaZero 式树搜索来增强大语言模型（LLMs）在推理过程中的推理能力，但我们发现这种方法存在扩展失效的问题：在 GSM8K 和 Game24 数据集上，准确率随着搜索预算的增加而下降。在本文中，我们提出了 ReSCALE，这是对 Gumbel AlphaZero MCTS 的一种改进，它将 Dirichlet 噪声和 PUCT 选择替换为 Gumbel 采样和 Sequential Halving，在不改变模型或其训练过程的情况下恢复了单调扩展性。在基准方法性能下降的预算水平下，ReSCALE 在 GSM8K 上达到了 58.4%，在 Game24 上达到了 85.3%。消融实验证实，Sequential Halving 是性能提升的主要驱动因素。

## Abstract
Neural tree search is a powerful decision-making algorithm widely used in complex domains such as game playing and model-based reinforcement learning. Recent work has applied AlphaZero-style tree search to enhance the reasoning capabilities of Large Language Models (LLMs) during inference, but we find that this approach suffers from a scaling failure: on GSM8K and Game24, accuracy drops as the search budget increases. In this paper, we present ReSCALE, an adaptation of Gumbel AlphaZero MCTS that replaces Dirichlet noise and PUCT selection with Gumbel sampling and Sequential Halving, restoring monotonic scaling without changes to the model or its training. ReSCALE reaches 58.4\% on GSM8K and 85.3\% on Game24 at budgets where the baseline degrades. Ablations confirm that Sequential Halving is the primary driver of the improvement.

---

## 论文详细总结（自动生成）

这是一份关于论文《Revisiting Tree Search for LLMs: Gumbel and Sequential Halving for Budget-Scalable Reasoning》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLMs）在复杂多步推理任务中表现不佳。为了提升性能，研究者引入了蒙特卡洛树搜索（MCTS，如 AlphaZero 风格），通过在推理时增加计算预算（Test-time compute）来探索不同的推理路径。
*   **核心问题**：论文发现传统的 AlphaZero 式树搜索在 LLM 推理中存在“缩放失效”（Scaling Failure）现象。即当搜索预算（模拟次数或生成的 Token 数）增加到一定程度后，模型的准确率反而会停滞甚至下降（类似于搜索病态现象）。
*   **研究目的**：提出一种能够随计算预算增加而稳定提升性能（单调缩放）的树搜索算法。

### 2. 方法论：ReSCALE 框架
论文提出了 **ReSCALE**（Reasoning via Scalable Compute Allocation for LLM Exploration），其核心思想是将 Gumbel AlphaZero 的机制引入 LLM 解码过程。
*   **核心思想**：用更具理论保障的动作选择机制替代传统的启发式方法，以解决过度开发（Over-exploitation）问题。
*   **关键技术细节**：
    *   **根节点选择（Sequential Halving + Gumbel）**：不再使用 PUCT 算法和 Dirichlet 噪声，而是采用 Gumbel 采样初始化分数，并使用“顺序减半”（Sequential Halving）算法分配预算。该算法在多轮中逐步淘汰表现较差的动作，将更多预算集中在最有潜力的候选动作上。
    *   **非根节点选择**：使用改进的策略 $\pi'$，结合了先验概率和“完成价值”（Completed Value）。
    *   **混合价值近似（Mixed Value Approximation）**：对于未访问过的节点，算法结合了价值网络的预测值和已访问兄弟节点的平均值，防止因价值网络预测不准而导致的错误决策。
    *   **动作空间定义**：以“句子”为单位（由换行符分隔）作为搜索树的动作，而非单个 Token，以平衡搜索深度和分支因子。

### 3. 实验设计
*   **数据集/场景**：
    *   **GSM8K**：数学应用题推理。
    *   **Game24**：数学规划任务（给定4个数字，通过加减乘除得到24）。
*   **基准模型（Base Model）**：Llama2-7B（经过 SFT 微调）。
*   **对比方法（Benchmarks）**：
    *   **AlphaZero-style MCTS**：之前的 SOTA 树搜索解码方法。
    *   **Best-of-N**：采样 N 条路径并根据价值模型选择最优路径。
*   **实验设置**：将预算分为 Small、Medium、Large 三个等级，观察准确率随 Token 消耗量的变化曲线。

### 4. 资源与算力
*   **模型规模**：使用了 Llama2-7B。
*   **训练细节**：对 Llama2-7B 进行了 3 个 Epoch 的全参数微调（SFT）；价值网络（Value Network）基于相同的架构，添加了一个 MLP 头部，并在生成的 51 个样本/提示词的数据集上训练了 3 个 Epoch。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长，但提供了 GitHub 代码库以供复现。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流推理基准上进行了测试，每个实验点通常基于 3 个随机种子取平均值。
*   **消融实验**：专门针对 Gumbel 噪声和 Sequential Halving 进行了消融，证明了 Sequential Halving 是恢复单调缩放特性的最关键因素（移除它会导致准确率下降约 4.7%）。
*   **客观性**：通过对比不同预算下的性能曲线，直观展示了基准方法的退化和新方法的提升，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **恢复了单调缩放**：ReSCALE 成功解决了 AlphaZero 在高预算下的性能退化问题。在 GSM8K 上达到 58.4%，在 Game24 上达到 85.3%，显著优于基准方法。
*   **病态原因分析**：传统 AlphaZero 容易过度陷入某些节点，导致访问计数极度不平衡。ReSCALE 通过顺序减半机制保持了探索的多样性。
*   **计算效率**：在相同的 Token 预算下，ReSCALE 能够比 Best-of-N 和标准 MCTS 更有效地转化计算资源为准确率。

### 7. 优点
*   **即插即用**：无需改变基础 LLM 的权重或训练方式，仅通过改进推理端的搜索策略即可实现性能提升。
*   **理论支撑**：引入了多臂老虎机（Multi-armed Bandit）领域成熟的 Sequential Halving 理论，解决了 LLM 搜索中的启发式偏差。
*   **鲁棒性**：在不同难度和类型的任务（数学推理 vs 逻辑规划）上均表现出一致的改进。

### 8. 不足与局限
*   **模型参数量限制**：实验仅在 7B 模型上进行，尚未验证该方法在 70B 或更大规模模型（如 Llama-3-70B）上的表现。
*   **任务覆盖面**：主要集中在数学和逻辑任务，对于创意写作、代码生成等其他领域的适用性有待验证。
*   **推理延迟**：虽然预算可扩展，但树搜索本身比标准解码慢得多，实际部署时仍面临高延迟挑战。
*   **价值模型依赖**：搜索质量高度依赖于价值训练数据的质量，如果价值模型本身存在系统性偏差，搜索可能仍会失效。

（完）
