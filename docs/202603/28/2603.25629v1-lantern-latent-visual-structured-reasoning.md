---
title: "LanteRn: Latent Visual Structured Reasoning"
title_zh: LanteRn：潜空间视觉结构化推理
authors: "André G. Viveiros, Nuno Gonçalves, Matthias Lindemann, André Martins"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25629v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于视觉推理的大型多模态模型架构
tldr: "针对当前大语言模型在视觉推理中过度依赖文本描述而导致细粒度空间理解不足的问题，本文提出 LanteRn 框架。该框架允许模型在推理过程中交替使用语言和紧凑的潜空间视觉表示，从而直接在潜空间进行视觉推理。通过监督微调和强化学习两阶段训练，LanteRn 在 VisCoT、V* 和 Blink 等基准测试中显著提升了视觉定位和推理能力，为高效多模态推理提供了新方向。"
selection_source: fresh_fetch
motivation: 现有模型将视觉内容转化为文本或直接在像素空间推理，限制了细粒度空间理解且计算开销巨大。
method: 提出 LanteRn 框架，通过在视觉语言 Transformer 中引入连续的视觉思维嵌入，并结合监督微调与强化学习进行训练。
result: "在 VisCoT、V* 和 Blink 等多个以感知为核心的基准测试中，该方法在视觉定位和细粒度推理方面均表现出持续的改进。"
conclusion: 内部潜空间表示是实现更高效、更精准的多模态推理的一种极具前景的方向。
---

## 摘要
尽管语言推理模型在许多任务中表现出色，但视觉推理对于当前的大型多模态模型（LMMs）而言仍具挑战性。因此，大多数 LMM 默认将感知内容转化为文本，这对于需要细粒度空间和视觉理解的任务来说是一个严重的局限。虽然最近的方法通过调用工具或生成中间图像在“以图思考”方面取得了进展，但它们要么依赖外部模块，要么因直接在像素空间进行推理而产生不必要的计算开销。在本文中，我们介绍了 LanteRn，这是一个使 LMM 能够将语言与紧凑的潜视觉表示交织在一起的框架，从而允许视觉推理直接在潜空间中进行。LanteRn 增强了视觉-语言 Transformer 的能力，使其在推理过程中能够生成并关注连续的视觉思维嵌入。我们分两个阶段训练该模型：首先通过监督微调将视觉特征锚定在潜状态中，随后通过强化学习使潜推理与任务级效用对齐。我们在三个以感知为中心的基准测试（VisCoT、V* 和 Blink）上评估了 LanteRn，观察到在视觉定位和细粒度推理方面有一致的提升。这些结果表明，内部潜表示为更高效的多模态推理提供了一个充满前景的方向。

## Abstract
While language reasoning models excel in many tasks, visual reasoning remains challenging for current large multimodal models (LMMs). As a result, most LMMs default to verbalizing perceptual content into text, a strong limitation for tasks requiring fine-grained spatial and visual understanding. While recent approaches take steps toward thinking with images by invoking tools or generating intermediate images, they either rely on external modules, or incur unnecessary computation by reasoning directly in pixel space. In this paper, we introduce LanteRn, a framework that enables LMMs to interleave language with compact latent visual representations, allowing visual reasoning to occur directly in latent space. LanteRn augments a vision-language transformer with the ability to generate and attend to continuous visual thought embeddings during inference. We train the model in two stages: supervised fine-tuning to ground visual features in latent states, followed by reinforcement learning to align latent reasoning with task-level utility. We evaluate LanteRn on three perception-centric benchmarks (VisCoT, V*, and Blink), observing consistent improvements in visual grounding and fine-grained reasoning. These results suggest that internal latent representations provide a promising direction for more efficient multimodal reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《LanteRn: Latent Visual Structured Reasoning》的结构化深度总结：

### 1. 核心问题与研究背景
*   **研究动机**：当前的大型多模态模型（LMMs）在处理视觉任务时，大多采用“关于图像的思考”（Thinking about images），即先将视觉内容转化为文本描述，再进行逻辑推理。这种方式存在“低带宽”限制，难以捕捉细粒度的空间和视觉结构。
*   **现有方案局限**：
    *   **工具化方法**：依赖外部插件（如检测器、裁剪工具），计算开销大且非端到端。
    *   **像素空间推理**：通过生成中间图像进行推理，但在像素层面生成细节非常耗时且包含大量与任务无关的冗余信息。
*   **核心目标**：提出 **LanteRn** 框架，使模型能够直接在**潜空间（Latent Space）**中生成并处理连续的视觉“思维”向量，实现语言与视觉特征的深度交织推理。

### 2. 方法论
LanteRn 将推理建模为文本标记（Discrete Tokens）与潜空间向量（Continuous Vectors）交替出现的混合轨迹。
*   **核心架构**：基于 Qwen2.5-VL-3B。引入了三个特殊控制标记：`<|lvr_start|>`、`<|lvr_sep|>` 和 `<|lvr_end|>`。
    *   **文本模式**：标准自回归生成。
    *   **潜视觉模式**：当生成开始标记后，模型在接下来的 $K$ 个步长中跳过语言头，直接输出 Transformer 最后一层的隐藏状态（$z_1, ..., z_K$），作为内部视觉思维。
*   **两阶段训练流程**：
    1.  **监督微调（SFT）——视觉锚定**：
        *   **教师信号**：利用预训练视觉编码器的特征作为 Ground Truth。
        *   **损失函数**：结合交叉熵损失（文本）和均方误差（MSE）损失（潜状态）。通过最小化模型生成的潜向量与视觉编码器提取的区域特征之间的差异，使潜状态具备视觉表征能力。
    2.  **强化学习（RL）——任务对齐**：
        *   **算法**：采用 **GRPO**（Group Relative Policy Optimization）。
        *   **关键技术**：**潜状态回放（Latent State Replay）**。在策略更新时固定采样时的潜向量，确保概率比率稳定，同时允许梯度回传至产生潜状态的参数。
        *   **奖励设计**：准确率奖励（结果导向）+ 格式奖励（强制模型使用潜推理块）。

### 3. 实验设计
*   **数据集**：
    *   **SFT 阶段**：基于 Visual-CoT 构建的合成数据集，利用 Qwen3-VL-235B 生成带有视觉定位框的推理轨迹。
    *   **RL 阶段**：使用 VIRL-39k 数据集，包含多样的视觉推理问题。
*   **评估基准（Benchmarks）**：
    *   **VisCoT**：评估多步视觉推理。
    *   **V\***：评估视觉搜索能力（包括直接归因 DA 和相对位置 RP）。
    *   **Blink**：评估核心视觉感知（如物体定位 OL 和相对位置 RP）。
*   **对比方法**：
    *   **Qwen2.5-VL-3B**：原始基座模型。
    *   **LantErn-NTP**：消融基准，使用相同的特殊标记和计算量，但中间推理过程全部使用标准文本标记（Next Token Prediction）。

### 4. 资源与算力
*   **模型规模**：主要在 3B 参数规模的模型上进行实验。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到使用了 TRL 库进行强化学习训练，并冻结了视觉编码器以提高训练稳定性。

### 5. 实验数量与充分性
*   **实验设置**：
    *   针对潜向量大小 $K$ 进行了消融实验（$K \in \{4, 8, 16, 32\}$）。
    *   对比了 SFT 阶段和 RL 阶段后的性能变化。
    *   在三个主流视觉感知/推理基准上进行了多维度测试。
*   **充分性评价**：实验设计较为合理，通过引入 NTP 基准证明了性能提升源于“潜空间表示”而非单纯的“推理步数增加”。但在模型规模扩展性（如 7B 或更大模型）上的验证尚不充分。

### 6. 主要结论与发现
*   **潜空间推理的有效性**：LanteRn 在感知密集型任务上显著优于纯文本推理基准。
*   **RL 的关键作用**：SFT 阶段主要让潜状态具备感知保真度，而 RL 阶段则让潜状态从“视觉重建”转向“任务驱动”，显著提升了逻辑推理和空间关系理解能力（如 Blink RP 性能从 0.68 提升至 0.81）。
*   **效率优势**：3B 规模的 LanteRn 在多个指标上达到了 7B 规模模型的水平，证明了潜空间推理是比单纯增加参数量更高效的路径。

### 7. 优点
*   **架构优雅**：无需外部工具或昂贵的像素生成，直接在 Transformer 内部实现多模态交织推理。
*   **训练策略科学**：通过 SFT 解决“冷启动”锚定问题，通过 RL 解决“任务效用”对齐问题。
*   **计算高效**：相比生成图像，生成紧凑的潜向量极大地节省了计算资源。

### 8. 不足与局限
*   **固定容量限制**：目前使用固定的潜向量长度 $K$，无法根据任务复杂度动态调整推理深度。
*   **数据领域局限**：训练数据主要集中在特定的视觉推理领域，泛化到更广泛场景的能力有待验证。
*   **可解释性挑战**：潜向量对人类而言是不可读的，目前缺乏有效的可视化手段来直观展示模型在潜空间中到底“想”了什么。
*   **算力细节缺失**：缺乏具体的训练资源消耗报告，不利于复现成本评估。

（完）
