---
title: An Empirical Study of SFT-DPO Interaction and Parameterization in Small Language Models
title_zh: 小语言模型中 SFT-DPO 交互与参数化的实证研究
authors: "Yuming Feng, Christy Yang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20100v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 小模型中SFT与DPO相互作用的实证研究
tldr: 本研究针对小规模语言模型（GPT-2级别），系统探讨了监督微调（SFT）与直接偏好优化（DPO）的交互作用，并对比了全参数微调（FFT）与LoRA的表现。研究发现，在小模型和有限数据下，全参数适配对性能的影响远大于偏好优化，DPO仅在特定条件下提供微小增益，而LoRA在性能和训练效率上均不如FFT，强调了全参数适配在小模型中的核心地位。
selection_source: fresh_fetch
motivation: 旨在探究在小规模模型和有限数据场景下，SFT与DPO的结合方式以及参数化方案对模型性能的具体影响。
method: 在GPT-2规模模型上，通过释义检测和诗歌续写任务，对比了SFT、DPO的不同训练阶段以及FFT与LoRA的性能差异。
result: 实验显示DPO相比强SFT提升有限，且全参数微调在性能上始终优于LoRA，且LoRA在实验硬件上并未缩短训练时间。
conclusion: 在小规模模型领域，全参数监督适配是提升性能的主要杠杆，而偏好优化和低秩适配提供的边际收益较小。
---

## 摘要
直接偏好优化（DPO）在有监督微调（SFT）之后被广泛用于对齐语言模型，但在小规模骨干网络和适量数据下的实证表现尚不明确。我们在 GPT-2 规模的解码器上，系统地比较了仅 SFT、仅 DPO 以及分阶段 SFT 到 DPO 的训练方式，并对比了全参数微调（FFT）与 LoRA 的效果，评估任务包括释义检测和莎士比亚十四行诗续写。结果显示，相比于强 SFT，DPO 带来的提升较小且取决于具体任务；当偏好构建与有监督目标紧密平行时，DPO 无需预热启动即可达到具有竞争力的 SFT 准确率。相比之下，参数化方式起主导作用：在相同的训练深度下，FFT 的表现始终优于 LoRA，且 LoRA 在我们的硬件上并未缩短实际运行时间。这些发现表明，在这种小规模范式下，有监督全参数适配仍然是提升性能的主要手段，而偏好优化和低秩适配带来的边际收益有限。

## Abstract
Direct Preference Optimization (DPO) is widely used after supervised fine-tuning (SFT) to align language models, yet empirical behavior under small backbones and modest data is under-specified. We systematically compare SFT-only, DPO-only, and staged SFT-to-DPO training alongside full fine-tuning (FFT) versus LoRA on a GPT-2-scale decoder, evaluating paraphrase detection and Shakespearean sonnet continuation. DPO yields small, task-dependent gains over strong SFT and can match competitive SFT accuracy without a warm start when the preference construction closely parallels the supervised objective. In contrast, parameterization dominates: FFT consistently outperforms LoRA at matched training depth, and LoRA does not reduce wall-clock time on our hardware. These findings indicate that, in this small-scale regime, supervised full-parameter adaptation remains the primary performance lever, while preference optimization and low-rank adaptation provide limited marginal returns.

---

## 论文详细总结（自动生成）

这篇论文对小语言模型（Small Language Models, SLMs）在微调过程中的参数化策略和训练目标进行了深入的实证研究。以下是详细的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在模型规模较小（如 GPT-2 124M）且数据量有限的情况下，监督微调（SFT）与直接偏好优化（DPO）如何相互作用？全参数微调（FFT）与低秩适配（LoRA）相比孰优孰劣？
*   **研究动机**：虽然 SFT、DPO 和 LoRA 在大语言模型（LLM）中已成为标准流程，但在小模型领域的表现尚未得到充分界定。开发者往往假设大模型的经验可以直接迁移，但小模型在计算预算和参数容量上存在本质差异，需要专门的实证指导。

### 2. 方法论
*   **核心思想**：通过控制变量法，在分类（释义检测）和生成（诗歌续写）两种任务上，系统对比不同的训练阶段组合和参数更新策略。
*   **关键技术细节**：
    *   **训练目标**：对比了“仅 SFT”、“仅 DPO”以及“SFT $\rightarrow$ DPO”分阶段训练。
    *   **DPO 机制**：利用 DPO 损失函数直接优化偏好对（Chosen vs. Rejected），在分类任务中将正确标签设为 Chosen，错误标签设为 Rejected；在生成任务中将原著设为 Chosen，模型生成内容设为 Rejected。
    *   **参数化策略**：对比了更新所有参数的 **FFT** 和通过低秩矩阵 $W = W_0 + \frac{\alpha}{r} BA$ 更新部分参数的 **LoRA**。
    *   **分阶段切换**：研究了在 SFT 训练的不同阶段（早期 vs. 接近收敛）引入 DPO 的效果。

### 3. 实验设计
*   **数据集/场景**：
    *   **释义检测**：使用 Quora Question Pairs (QQP) 数据集，包含约 28.3 万条数据。
    *   **诗歌续写**：使用莎士比亚的 155 首十四行诗，进行风格化的文本生成。
*   **Benchmark 与对比方法**：
    *   基座模型：GPT-2 (124M)。
    *   对比维度：数据规模（2.83k 到 283k）、LoRA 秩（r=4, 8, 16）、DPO 超参数（学习率、$\beta$ 值）、训练策略（SFT-only, DPO-only, Staged）。
*   **评估指标**：分类任务使用 Accuracy 和 F1；生成任务使用 chrF（字符级 n-gram 分数）及采样稳定性评估。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA H100 GPU（80GB VRAM），部署在 Modal 云平台。
*   **训练时长**：
    *   释义检测任务：每轮（epoch）约 7 分钟。
    *   诗歌续写任务：规模较小，每轮约 0.7 秒。
*   **效率发现**：研究特别指出，在 H100 这种算力受限而非显存受限的硬件上，LoRA 相比 FFT 并没有缩短实际训练时间（Wall-clock time）。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度的消融实验，包括数据量级对比、LoRA 秩的参数搜索、DPO 超参数扫频、以及针对生成任务的三种不同偏好对构建方案（V1/V2/V3）。
*   **充分性评价**：实验设计较为客观、公平。作者不仅关注最终指标，还记录了训练曲线和采样随机性（20 个不同随机种子），确保了结论的可靠性。

### 6. 主要结论与发现
*   **参数化策略主导性能**：在小模型上，**FFT 始终优于 LoRA**。LoRA 虽然参数更少，但在相同训练深度下无法达到 FFT 的准确度。
*   **DPO 收益边际化**：相比于经过良好调优的强 SFT 基准，DPO 带来的提升非常微小（通常小于 1%）。
*   **DPO 无需预热的条件**：如果偏好对的构建逻辑与监督任务高度一致（如分类标签），DPO 可以在没有 SFT 预热的情况下直接训练并达到不错的效果。
*   **数据多样性胜过重复训练**：在相同计算时间内，在大数据集上训练较少轮次的效果优于在小数据集上多次迭代。

### 7. 优点
*   **实证严谨**：挑战了“LoRA 必然更快”和“DPO 必然大幅提升性能”的普遍认知，提供了针对小模型的实用建议。
*   **硬件视角**：不仅关注算法指标，还关注了实际运行时间（Wall-clock time），这对工业界部署具有参考价值。
*   **任务覆盖**：同时涵盖了判别式（分类）和生成式任务，增强了结论的普适性。

### 8. 不足与局限
*   **模型单一**：研究仅基于 GPT-2 (124M)，未在其他架构（如 Llama-tiny 或 BERT 类模型）上验证。
*   **任务复杂度有限**：所选任务（释义检测、诗歌续写）相对简单，可能无法完全体现 DPO 在复杂逻辑推理或安全性对齐上的潜力。
*   **硬件偏差**：关于 LoRA 训练时间的结论高度依赖于 H100 这种高性能硬件，在显存极度受限的旧型号 GPU 上，LoRA 的优势可能会更明显。

（完）
