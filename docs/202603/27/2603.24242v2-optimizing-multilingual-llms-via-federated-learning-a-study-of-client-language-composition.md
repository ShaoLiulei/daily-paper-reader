---
title: "Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition"
title_zh: 通过联邦学习优化多语言大语言模型：客户端语言构成的研究
authors: "Aleix Sant, Jordi Luque, Carlos Escolano"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24242v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 多语言指令微调与联邦学习
tldr: 本研究探讨了多语言环境下大语言模型的联邦学习优化，重点分析了客户端语言组成对模型性能的影响。研究扩展了FederatedScope-LLM框架，并提出局部动态早停机制（LDES-FL）以提升训练效率。实验表明，增加客户端内的语言多样性可显著提升全局模型的性能与公平性，尤其对低资源语言有益，尽管这会增加一定的训练成本。
selection_source: fresh_fetch
motivation: 针对多语言联邦学习中客户端语言分布不均和资源差异带来的挑战，探索如何优化多语言大模型的训练。
method: 扩展了FederatedScope-LLM框架，并引入一种根据客户端验证表现自动暂停或恢复训练的局部动态早停机制（LDES-FL）。
result: 增加客户端内的语言多样性能提升全局模型的强度和公平性，缩小与中心化训练的差距，并使低资源语言获益最大。
conclusion: 客户端语言组成是多语言联邦学习的关键设计变量，直接影响模型的性能、公平性及训练效率。
---

## 摘要
多语言环境下大语言模型（LLMs）的联邦学习（FL）面临着重大挑战，这些挑战源于客户端之间异构的语言分布以及语言资源可用性的差异。为了应对这些挑战，我们扩展了 FederatedScope-LLM 框架，以支持 LLM 的多语言指令微调实验。我们还引入了一种新型的客户端特定早停机制——局部动态早停（LDES-FL），该机制允许客户端根据客户端验证性能暂停和恢复本地训练，从而提高训练效率和可持续性。通过一系列实验，我们研究了客户端语言构成（从完全单语言到日益多语言的客户端）如何影响多语言质量、公平性和训练成本。单语言本地微调对于单语言专业化仍然最有效，而联邦训练则更适合学习单个平衡的多语言模型。在联邦学习中，增加客户端内部的多语言性可以产生更强大、更公平的全局模型，缩小与集中式多语言微调的差距，并为低资源语言带来最大的收益，尽管代价是需要更多的优化步骤。总之，我们的研究结果表明，客户端语言构成是多语言联邦学习中的一个关键设计变量，它塑造了性能、公平性和效率。

## Abstract
Federated Learning (FL) of Large Language Models (LLMs) in multilingual environments presents significant challenges stemming from heterogeneous language distributions across clients and disparities in language resource availability. To address these challenges, we extended the FederatedScope-LLM framework to support multilingual instruction-tuning experiments with LLMs. We also introduced a novel client-specific early stopping mechanism, Local Dynamic Early Stopping (LDES-FL), which allows clients to pause and resume local training based on client-side validation performance, enhancing training efficiency and sustainability. Through a series of experiments, we studied how client language composition - from fully monolingual to increasingly multilingual clients - affects multilingual quality, fairness and training cost. Monolingual local fine-tuning remains the most effective for single-language specialization, whereas federated training is better suited to learning a single balanced multilingual model. In FL, increasing within-client multilinguality leads to stronger and fairer global models, narrows the gap to centralized multilingual fine-tuning, and yields the largest gains for lower-resource languages, albeit at the cost of more optimization steps. Overall, our results identify client language composition as a key design variable in multilingual FL, shaping performance, fairness and efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在多语言环境下，联邦学习（FL）面临客户端数据高度异构（Non-IID）的挑战，即不同客户端拥有的语言种类和资源量极不均衡。这会导致“客户端漂移”现象，影响模型收敛和泛化。
*   **核心问题**：论文重点探讨了**客户端内部的语言构成**（从纯单语言到混合多语言）如何影响多语言大语言模型（LLM）在联邦微调过程中的性能、公平性以及训练成本。

### 2. 方法论
*   **核心思想**：通过系统性地改变客户端本地数据集的语言多样性，观察其对全局模型的影响，并引入一种更灵活的早停机制来优化分布式训练。
*   **关键技术细节**：
    *   **框架扩展**：基于 `FederatedScope-LLM` 进行了多语言适配，支持语言感知的样本处理和灵活的 Prompt 集成。
    *   **LDES-FL（局部动态早停）**：这是一种新型机制。客户端根据本地验证集的损失函数自主决定暂停或恢复训练。如果全局模型在某轮更新后使该客户端的本地验证表现提升，已停止的客户端可以“复活”重新参与训练。
    *   **微调技术**：采用 LoRA（低秩自适应）进行参数高效微调（PEFT），仅更新约 1%-2% 的参数，以降低通信和计算开销。
    *   **聚合算法**：使用标准的 `FedAvg` 算法进行模型参数聚合。

### 3. 实验设计
*   **数据集**：使用多语言版本的 `Alpaca Cleaned` 数据集，涵盖 8 种语言：英语 (en)、西班牙语 (es)、德语 (de)、加泰罗尼亚语 (ca)、丹麦语 (da)、塞尔维亚语 (sr)、克罗地亚语 (hr) 和巴斯克语 (eu)。
*   **实验场景**：设计了 6 种不同的客户端语言构成比例，从“100% 单语言（每个客户端仅一种语言）”到“15% 单语言（高度混合多语言）”。
*   **Benchmark 与对比方法**：
    *   **Base Model**：原始预训练指令模型（salamandra-2b-instruct）。
    *   **Local FT (Monolingual)**：在单语言数据上进行本地微调（孤岛模式）。
    *   **Local FT (Multilingual)**：中心化多语言微调（作为性能上限）。
    *   **FedAvg (不同比例)**：不同语言构成下的联邦学习表现。
*   **评估指标**：使用 ROUGE-L 和 BERTScore (Fbert)，并以标准差 ($\sigma$) 作为多语言公平性的指标。

### 4. 资源与算力
*   **模型规模**：使用 `salamandra-2b-instruct` (2B 参数)。
*   **训练配置**：本地微调步数为每轮 160 步，微批次大小 (micro-batch) 为 2，梯度累积 16 步，有效批次大小为 32。使用 `OneBitAdam` 优化器。
*   **算力说明**：论文**未明确列出具体的 GPU 型号和数量**，但详细记录了优化步骤（Optimization Steps）。例如，中心化训练需要约 $1.83 \times 10^5$ 步，而最简单的联邦设置仅需其 6.1%。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 种语言的单语言微调、1 组中心化多语言微调以及 6 组不同异构程度的联邦学习实验。
*   **充分性评价**：实验设计较为系统，通过控制变量法（保持每个客户端总样本量不变，仅改变语言比例）排除了样本量干扰。引入了 Bootstrap 显著性检验（p-value < 0.05），确保结论的客观性。

### 6 主要结论与发现
*   **语言构成的平衡**：增加客户端内部的语言多样性（即使总数据量不变）能显著减少客户端漂移，提升全局模型的平均性能，并缩小不同语言间的表现差距（提升公平性）。
*   **低资源语言获益最大**：客户端多语言化对塞尔维亚语、巴斯克语等低资源语言的提升最为明显（最高达 16.11%）。
*   **性能与成本权衡**：虽然多语言客户端能带来更好的模型，但会导致收敛更慢，需要更多的通信轮数和优化步骤。
*   **LDES-FL 的有效性**：相比传统全局早停，LDES-FL 在保持性能的同时，能减少约 22%-32% 的不必要计算。

### 7. 优点
*   **机制创新**：LDES-FL 解决了联邦学习中客户端参与度的动态调整问题，提高了训练的可持续性。
*   **实战导向**：针对多语言 LLM 这一前沿领域，提供了关于数据分布如何影响联邦训练的量化指导。
*   **公平性关注**：不仅关注平均分，还深入分析了资源多寡对语言表现的影响，具有社会意义。

### 8. 不足与局限
*   **数据对齐风险**：实验使用的多语言 Alpaca 是通过翻译获得的，不同语言的样本可能存在内容重叠，这可能导致对“跨语言迁移能力”的评估存在偏差。
*   **模型单一性**：仅在 2B 规模的单个模型上进行了验证，尚未探讨在更大规模（如 7B 或 70B）模型上的扩展性。
*   **现实复杂性简化**：实验中每个客户端的数据量被设为相等，而现实中不同语言的客户端数据量往往存在巨大差异。

（完）
