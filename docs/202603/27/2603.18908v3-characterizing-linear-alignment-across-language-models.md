---
title: Characterizing Linear Alignment Across Language Models
title_zh: 刻画跨语言模型的线性对齐
authors: "Matt Gorbett, Suman Jana"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18908v3"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型之间的表示收敛与线性对齐
tldr: 本研究探讨了不同语言模型在表示空间上的收敛性，即尽管训练目标和架构不同，模型往往学习到相似的表示。作者通过学习独立模型最终隐藏状态之间的仿射变换，评估了线性对齐在文本生成、嵌入分类和分布外检测中的效果。研究首次证明线性对齐可实现跨模型的文本生成，并提出了一种基于同态加密的隐私保护跨机构推理框架，实现了亚秒级的推理延迟。
selection_source: fresh_fetch
motivation: 旨在研究独立训练的大语言模型之间是否存在表示收敛，以及能否通过简单的线性变换实现跨模型的对齐与协作。
method: 在不同模型的最终隐藏层表示之间学习仿射变换，并将其应用于文本生成、分类及隐私保护推理等任务。
result: 实验表明线性对齐能有效保留模型性能，支持跨模型文本生成，并在加密推理场景下达到亚秒级延迟。
conclusion: 线性对齐证实了模型间表示的兼容性，为无需共享原始数据或模型的跨模型协作和隐私保护应用开辟了新途径。
---

## 摘要
尽管在训练目标、架构和数据模态上存在差异，语言模型似乎越来越多地学习到相似的表示。独立训练的模型之间这种新兴的兼容性，为跨模型对齐下游目标提供了新的机遇。此外，这种能力开启了新的潜在应用领域，例如在安全、隐私或竞争约束禁止直接共享数据或模型的场景中。在这项工作中，我们研究了表示收敛在多大程度上实现了大语言模型之间实际的线性对齐。具体而言，我们学习了独立模型最终隐藏状态之间的仿射变换，并在文本生成、嵌入分类和分布外检测任务中对这些映射进行了实证评估。我们发现，在不同模型对之间，性能在很大程度上得到了保留，并首次展示了线性对齐有时能够实现跨独立训练模型的文本生成。我们进一步强调了线性对齐在隐私保护跨孤岛推理中的潜在应用。该框架在共享的公共数据集上学习仿射变换，并使用同态加密来保护客户端查询。通过仅对线性分类操作进行加密，该方法实现了亚秒级的推理延迟。

## Abstract
Language models increasingly appear to learn similar representations, despite differences in training objectives, architectures, and data modalities. This emerging compatibility between independently trained models introduces new opportunities for cross-model alignment to downstream objectives. Moreover, this capability unlocks new potential application domains, such as settings where security, privacy, or competitive constraints prohibit direct data or model sharing. In this work, we investigate the extent to which representational convergence enables practical linear alignment between large language models. Specifically, we learn affine transformations between the final hidden states of independent models and empirically evaluate these mappings across text generation, embedding classification, and out-of-distribution detection. We find that performance is largely preserved across model pairs, and show for the first time that linear alignment sometimes enables text generation across independently trained models. We further highlight a potential application of linear alignment for privacy-preserving cross-silo inference. The framework learns an affine transformation over a shared public dataset and uses homomorphic encryption to protect client queries. By encrypting only the linear classification operation, the method achieves sub-second inference latency.

---

## 论文详细总结（自动生成）

以下是对论文《Characterizing Linear Alignment Across Language Models》的深度结构化总结：

### 1. 研究动机与背景
*   **核心问题**：尽管大语言模型（LLM）在架构、训练数据和目标函数上存在差异，但它们是否学习到了相似的内部表示？如果这种“表示收敛”确实存在，能否仅通过简单的线性变换（仿射变换）实现不同模型间的互操作？
*   **研究背景**：随着模型规模的扩大，研究者提出了“柏拉图表征假设”，即模型正趋向于对现实世界的一种共享统计理解。这种兼容性为跨模型协作、隐私保护推理以及在不共享原始数据/模型权重的情况下进行知识转移提供了可能。

### 2. 方法论
*   **核心思想**：在源模型（Source）的最终隐藏层表示与目标模型（Target）的特征空间之间学习一个仿射变换（$z_A = z_B W + b$），从而将源模型的输出“缝合”到目标模型的预测头（Classification Head 或 LM Head）上。
*   **关键技术细节**：
    *   **线性对齐**：使用带有岭回归（Ridge Regularization）的普通最小二乘法（OLS）来学习映射矩阵 $W$ 和偏置 $b$。
    *   **文本生成对齐**：在自回归生成中，提取源模型的倒数第二层隐藏状态，通过线性映射后输入到目标模型冻结的输出层（LM Head）进行下一词预测。
    *   **HELIX 框架**：提出“同态加密线性推理（HELIX）”协议。利用 CKKS 同态加密方案，客户端在本地提取特征并进行线性映射，服务端仅在加密状态下执行线性分类操作。
*   **算法流程**：
    1.  在共享的公共数据集上提取两组模型的嵌入向量。
    2.  计算协方差统计量并求解线性映射方程。
    3.  推理时，源模型生成的表示经映射后，直接由目标模型的预测头处理。

### 3. 实验设计
*   **数据集与场景**：
    *   **分类与 OOD 检测**：SST-2, AG News, TREC, MNLI, DBpedia 等。
    *   **文本生成**：MMLU（结构化输出）、Alpaca（指令遵循）。
*   **模型选择**：涵盖了主流开源模型（Llama 3/3.2, Qwen 2.5/3, Mistral, Olmo, Gemma）以及闭源 API（OpenAI text-embedding-3, Gemini embedding-001, Cohere）。
*   **Benchmark 与对比**：
    *   **基准（Baseline）**：目标模型在原生表示下的性能。
    *   **对比指标**：线性对齐后的准确率、OOD 检测的 AUROC、生成文本的 LLM-as-a-Judge 评分、嵌入相似度及人工评估。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **计算开销备注**：作者强调线性映射的训练非常高效（通常仅需 4,000 个样本），且 HELIX 框架在推理时达到了**亚秒级（sub-second）**延迟，远快于需要加密整个 Transformer 架构的现有方案。

### 5. 实验数量与充分性
*   **实验规模**：
    *   评估了 **34 组**不同的模型对进行文本生成。
    *   在 **6 个以上**的分类数据集上验证了表示兼容性。
    *   进行了消融实验，分析了分词器（Tokenizer）兼容性和模型规模对对齐效果的影响。
*   **充分性评价**：实验设计较为全面，不仅涵盖了判别式任务，还深入探讨了生成式任务。引入了 GPT-4o/Claude 3.5 作为自动评测员，并辅以人工盲测，增强了结果的客观性和说服力。

### 6. 主要结论与发现
*   **线性对齐的有效性**：简单的线性映射足以在不同模型间转移分类和 OOD 检测能力，性能损失极小。
*   **跨模型文本生成**：首次证明线性对齐可以支持跨独立训练模型的连贯文本生成。
*   **成功的关键因素**：
    1.  **分词器兼容性**：这是预测生成质量的最强指标（相关性 $r=0.898$）。
    2.  **模型规模**：源模型需达到一定规模（通常 $>4B$）才能提供足够的语义结构进行有效映射。
*   **不对称性**：从强模型映射到弱模型的效果通常优于反向映射。

### 7. 优点与亮点
*   **简洁高效**：证明了无需复杂的非线性适配器，仅靠线性层即可实现模型“缝合”。
*   **隐私保护创新**：HELIX 框架通过只加密线性层而非整个模型，极大地降低了同态加密推理的计算负担，具有很强的实用价值。
*   **理论支撑**：通过 CKA 和 SVCCA 等指标量化了模型间的表示收敛，为实验结果提供了几何解释。

### 8. 不足与局限
*   **分词器依赖**：对于分词器差异巨大的模型（如 Gemma 与其他模型），线性对齐效果显著下降，限制了跨家族模型的通用性。
*   **规模门槛**：小参数模型（$<1B$）在生成任务中表现不佳，说明表示收敛在小模型上尚不充分。
*   **隐私泄露风险**：虽然保护了查询数据，但线性映射矩阵 $W$ 本身可能泄露目标模型表示空间的部分结构信息。
*   **深度限制**：研究主要集中在最终隐藏层，未探讨模型中间层的线性对齐潜力。

（完）
