---
title: "DAK-UCB: Diversity-Aware Prompt Routing for LLMs and Generative Models"
title_zh: DAK-UCB：面向大语言模型与生成式模型的多样性感知提示词路由
authors: "Donya Jafari, Farzan Farnia"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23140v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 生成式人工智能和大型语言模型服务的自适应模型选择
tldr: 随着生成式AI服务的扩展，为用户提示词选择合适模型的需求日益增长。现有方法多侧重于保真度（如CLIP-Score），忽略了生成结果的多样性。本文提出DAK-UCB算法，这是一种基于上下文老虎机的在线模型选择框架，通过结合保真度和基于核函数的提示词感知多样性指标，实现了在保持高质量生成的同时显著提升结果多样性。
selection_source: fresh_fetch
motivation: 现有的生成模型选择方法仅关注保真度评分，导致生成的响应缺乏多样性，无法满足复杂的用户需求。
method: 提出DAK-UCB算法，利用上下文老虎机框架并结合联合核距离与核熵指标，在在线选择过程中平衡保真度与多样性。
result: 实验证明，DAK-UCB在处理一系列提示词时，能够有效提升模型选择的多样性，同时不损失生成结果的保真度。
conclusion: 该研究为生成式AI的模型路由提供了一种兼顾质量与多样性的新方案，具有较强的实用价值。
---

## 摘要
生成式人工智能和大语言模型（LLM）服务的扩张，凸显了对自适应机制日益增长的需求，以便选择合适的可用模型来响应用户的提示词。最近的研究提出了离线和在线学习方案，仅基于最大化基于提示词的保真度评估分数（例如文本生成图像中的 CLIP-Score）来为输入提示词确定最佳生成式 AI 模型。然而，这类基于保真度的选择方法忽略了生成输出的多样性，因此可能无法解决生成响应中潜在的多样性缺陷。在本文中，我们引入了多样性感知核化置信上限（DAK-UCB）方法，作为一种考虑多样性的生成模型在线选择上下文老虎机算法。所提出的 DAK-UCB 方法将保真度和多样性相关的指标都纳入了选择过程。我们基于提示词感知的多样性评分函数设计了该框架，该函数可分解为前几轮生成中提示词-输出对的基于两样本的期望。具体而言，我们利用联合核距离和核熵度量展示了该框架的应用。我们的实验结果证明了 DAK-UCB 在促进多样性感知模型选择方面的有效性，同时在一系列提示词的生成中保持了保真度。代码可在 https://github.com/Donya-Jafari/DAK-UCB 获取。

## Abstract
The expansion of generative AI and LLM services underscores the growing need for adaptive mechanisms to select an appropriate available model to respond to a user's prompts. Recent works have proposed offline and online learning formulations to identify the optimal generative AI model for an input prompt, based solely on maximizing prompt-based fidelity evaluation scores, e.g., CLIP-Score in text-to-image generation. However, such fidelity-based selection methods overlook the diversity of generated outputs, and hence, they can fail to address potential diversity shortcomings in the generated responses. In this paper, we introduce the Diversity-Aware Kernelized Upper Confidence Bound (DAK-UCB) method as a contextual bandit algorithm for the online selection of generative models with diversity considerations. The proposed DAK-UCB method incorporates both fidelity and diversity-related metrics into the selection process. We design this framework based on prompt-aware diversity score functions that decompose to a two-sample-based expectation over prompt-output pairs in the previous generation rounds. Specifically, we illustrate the application of our framework using joint kernel distance and kernel entropy measures. Our experimental results demonstrate the effectiveness of DAK-UCB in promoting diversity-aware model selection while maintaining fidelity in the generations for a sequence of prompts. The code is available at https://github.com/Donya-Jafari/DAK-UCB.

---

## 论文详细总结（自动生成）

以下是对论文《DAK-UCB: Diversity-Aware Prompt Routing for LLMs and Generative Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在面对海量的生成式 AI 模型（如 LLM、文生图模型）时，如何根据用户的输入提示词（Prompt）在线选择最合适的模型？
*   **研究动机**：
    *   **现有局限**：目前的模型路由（Routing）方法（如 PAK-UCB）主要关注**保真度（Fidelity）**（如 CLIP-Score），即输出是否符合提示词。
    *   **多样性缺失**：仅关注保真度会导致模型选择趋向于产生“安全”但单调的响应，甚至出现严重的偏见（如在生成人物时总是倾向于某一特定性别或族裔）。
    *   **技术挑战**：多样性是一个“群体属性”（Group-level property），而传统的上下文老虎机（Contextual Bandit）算法通常处理的是“样本级奖励”（Sample-level reward），难以直接优化多样性。

### 2. 方法论：DAK-UCB 框架
*   **核心思想**：将模型选择建模为上下文老虎机问题，并在奖励函数中显式引入**多样性感知项**。通过核函数技术，将复杂的群体多样性指标转化为可在线估计的数学形式。
*   **关键技术细节**：
    *   **联合核度量（Joint Kernel Metrics）**：提出了 **Joint-RKE (JRKE)** 和 **Joint-KD (JKD)**。这些指标不仅考虑输出数据的分布，还考虑了输出与输入提示词之间的联合分布。
    *   **两样本分解（Two-sample Decomposition）**：这是本文的数学突破。作者证明了 JRKE 和 JKD 可以分解为提示词-输出对的期望形式。这意味着算法可以利用历史生成的单个样本，通过**核脊回归（Kernel Ridge Regression, KRR）**来实时估计当前模型的多样性贡献。
    *   **置信上限（UCB）机制**：
        *   **DAK-UCB**：计算每个模型的保真度估计值和多样性估计值，并加上各自的置信区间（Confidence Bound），选择综合得分最高的模型。
        *   **Mixture-DAK-UCB**：考虑到最优解可能不是单一模型而是模型组合，该变体通过二次规划（Quadratic Optimization）计算提示词相关的模型混合概率。
*   **算法流程**：观察提示词 $\rightarrow$ 预测各模型的保真度与多样性 $\rightarrow$ 计算 UCB 分数 $\rightarrow$ 选择模型并生成结果 $\rightarrow$ 根据结果更新 KRR 模型。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **文生图（Text-to-Image）**：使用 MS-COCO 数据集提示词，候选模型包括 Kandinsky、SDXL 和 GigaGAN。
    *   **动物生成模拟**：测试模型在面对具有偏见的候选模型（如只生成猫或狗的模型）时的选择策略。
    *   **大语言模型（LLM）**：使用 Llama 3.2、Qwen 2 和 Gemma 3，测试其在地理描述等任务中的多样性。
    *   **图像描述（Image Captioning）**：使用 LLaVA、InstructBLIP 和 BLIP-2。
*   **基准方法（Benchmarks）**：
    *   **One-Arm Oracle**：始终选择表现最好的单一模型。
    *   **Random Selection**：随机选择模型。
    *   **PAK-UCB**：仅关注保真度的 SOTA 在线学习方法。
*   **评估指标**：CLIP-Score（保真度）、Joint-RKE（多样性）、JKD（分布匹配度）、Vendi Score。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **计算特性**：由于该方法属于在线学习（Online Learning），其核心计算开销在于 KRR 的更新。作者在消融实验中提到了使用 **随机傅里叶特征（Random Fourier Features, RFF）** 来近似核计算，以将计算复杂度从随时间平方增长降低到线性增长，从而提升实时性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了文生图、LLM 文本生成、图像描述三大主流任务。
    *   进行了多组消融实验，包括：噪声鲁棒性测试、核函数选择敏感性、超参数 $\lambda$（多样性权重）的影响、以及新模型加入时的自适应能力。
*   **充分性评价**：实验设计较为全面，不仅在标准数据集上验证了性能，还通过模拟实验直观展示了算法如何纠正模型偏见。对比实验涵盖了从随机到 SOTA 的多种基准，结果具有较强的说服力。

### 6. 主要结论与发现
*   **平衡性**：DAK-UCB 能够在不显著损失保真度的情况下，大幅提升生成结果的多样性。
*   **混合策略的优势**：Mixture-DAK-UCB 在多样性指标上通常优于确定性的 DAK-UCB，证明了模型融合在解决多样性坍缩问题上的必要性。
*   **偏见纠正**：实验显示 DAK-UCB 能识别并避开那些输出高度重复或具有特定偏见（如性别偏见）的模型。
*   **理论保证**：论文为 Sup-DAK-UCB（算法的阶段性版本）提供了理论上的遗憾边界（Regret Bound）证明。

### 7. 优点与亮点
*   **理论创新**：成功将群体属性（多样性）整合进样本级的在线决策框架中，解决了传统老虎机算法的痛点。
*   **通用性强**：算法不依赖于特定模态，可广泛应用于文本、图像、视频等多种生成任务。
*   **数学优雅**：利用核函数的两样本分解特性，使得在线估计多样性在计算上变得可行。

### 8. 不足与局限
*   **计算开销**：尽管有 RFF 近似，但在处理极大规模候选模型库或极长序列时，核矩阵的维护仍有计算压力。
*   **超参数依赖**：多样性权重 $\lambda$ 需要根据具体应用场景进行调节，缺乏自动化的参数寻优机制。
*   **嵌入模型依赖**：算法的效果高度依赖于所使用的嵌入模型（如 CLIP 或 DINOv2）的质量，如果嵌入空间本身存在偏见，算法可能无法完全消除这些偏见。

（完）
