---
title: "Surrogates, Spikes, and Sparsity: Performance Analysis and Characterization of SNN Hyperparameters on Hardware"
title_zh: 代理、脉冲与稀疏性：SNN 超参数在硬件上的性能分析与特征化
authors: "Ilkin Aliyev, Jesus Lopez, Tosiron Adegbija"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24891v1"
tags: ["query:snn"]
score: 10.0
evidence: SNN超参数与硬件级激活稀疏性
tldr: 脉冲神经网络（SNN）因其稀疏性在低功耗推理方面具有潜力，但训练超参数对硬件性能的影响尚不明确。本文通过在FPGA平台上对多种事件驱动数据集进行测试，量化分析了代理梯度函数和神经元模型对推理延迟和准确率的影响。研究发现，准确率并非硬件效率的可靠指标，通过感知稀疏性的超参数选择，可在保持高准确率的同时显著降低推理延迟，为SNN硬件优化提供了新方法。
selection_source: fresh_fetch
motivation: 旨在解决SNN训练超参数与硬件推理效率之间关系不透明的问题，探索如何通过超参数优化提升硬件性能。
method: 在FPGA硬件平台上，对比分析了不同代理梯度函数和神经元模型在多个事件视觉数据集上的表现。
result: "实验表明，特定超参数选择可使推理延迟降低高达28%，且感知稀疏性的配置能将延迟优化2倍以上。"
conclusion: 证明了神经元模型和代理梯度的选择对硬件效率至关重要，建立了一套从训练参数预测硬件行为的有效方法。
---

## 摘要
脉冲神经网络 (SNN) 通过稀疏、事件驱动的计算，在低功耗推理方面具有固有优势。然而，由于训练时的选择与推理时的稀疏性之间存在不透明的关系，SNN 的理论能量优势往往与实际硬件性能脱节。虽然先前的工作主要集中在权重剪枝和压缩上，但训练超参数（特别是代理梯度函数和神经元模型配置）在塑造硬件级激活稀疏性方面的作用仍未得到充分探索。本文提出了一项工作负载特征化研究，量化了硬件延迟对 SNN 超参数的敏感性。我们在三个基于事件的视觉数据集（DVS128-Gesture、N-MNIST 和 DVS-CIFAR10）上，解耦了代理梯度函数（如 Fast Sigmoid、Spike Rate Escape）和神经元模型（LIF、Lapicque）对分类准确率和推理效率的影响。我们的分析表明，标准的准确率指标难以很好地预测硬件效率。虽然 Fast Sigmoid 在 DVS-CIFAR10 上达到了最高准确率，但 Spike Rate Escape 在 DVS128-Gesture 上将推理延迟降低了高达 12.2%，且准确率损失极小。我们还证明了神经元模型的选择与参数调优同样重要；从 LIF 神经元转变为 Lapicque 神经元可使延迟降低高达 28%。我们在一个定制的、基于 FPGA 的周期精确 SNN 测量平台上进行了验证，结果表明，与基准方案相比，感知稀疏性的超参数选择可以将准确率提高 9.1%，并将延迟降低 2 倍以上。这些发现建立了一种根据训练参数预测硬件行为的方法。RTL 和可重复性工件可在 https://zenodo.org/records/18893738 获取。

## Abstract
Spiking Neural Networks (SNNs) offer inherent advantages for low-power inference through sparse, event-driven computation. However, the theoretical energy benefits of SNNs are often decoupled from real hardware performance due to the opaque relationship between training-time choices and inference-time sparsity. While prior work has focused on weight pruning and compression, the role of training hyperparameters -- specifically surrogate gradient functions and neuron model configurations -- in shaping hardware-level activation sparsity remains underexplored.   This paper presents a workload characterization study quantifying the sensitivity of hardware latency to SNN hyperparameters. We decouple the impact of surrogate gradient functions (e.g., Fast Sigmoid, Spike Rate Escape) and neuron models (LIF, Lapicque) on classification accuracy and inference efficiency across three event-based vision datasets: DVS128-Gesture, N-MNIST, and DVS-CIFAR10. Our analysis reveals that standard accuracy metrics are poor predictors of hardware efficiency. While Fast Sigmoid achieves the highest accuracy on DVS-CIFAR10, Spike Rate Escape reduces inference latency by up to 12.2% on DVS128-Gesture with minimal accuracy trade-offs. We also demonstrate that neuron model selection is as critical as parameter tuning; transitioning from LIF to Lapicque neurons yields up to 28% latency reduction. We validate on a custom cycle-accurate FPGA-based SNN instrumentation platform, showing that sparsity-aware hyperparameter selection can improve accuracy by 9.1% and latency by over 2x compared to baselines. These findings establish a methodology for predicting hardware behavior from training parameters. The RTL and reproducibility artifacts are at https://zenodo.org/records/18893738.

---

## 论文详细总结（自动生成）

以下是对论文《Surrogates, Spikes, and Sparsity: Performance Analysis and Characterization of SNN Hyperparameters on Hardware》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
脉冲神经网络（SNN）理论上通过事件驱动的稀疏计算具备极高能效，但**训练时的算法选择（如超参数、神经元模型）如何影响硬件推理时的实际性能（延迟、功耗）**一直是一个“黑盒”。
*   **研究动机**：现有的 SNN 优化多集中在权重剪枝，而忽略了训练时的**代理梯度函数（Surrogate Gradient）**和**神经元模型配置**对激活稀疏性的决定性作用。
*   **核心问题**：旨在消除算法准确率与硬件效率之间的脱节，探索如何通过感知稀疏性的超参数选择来优化硬件表现。

### 2. 论文提出的方法论
论文采用了一种**硬件在环（Hardware-in-the-loop）**的特征化方法，主要包含以下核心思想：
*   **两阶段设计空间探索（DSE）**：
    1.  **代理梯度敏感性分析**：评估 Fast Sigmoid (FS)、Arctangent (ATAN)、Spike Rate Escape (SRE) 和 Stochastic Spike Operator (SSO) 四种函数在不同斜率（$\alpha$）下对准确率和硬件稀疏性的影响。
    2.  **神经元模型探索**：对比标准的 **LIF（Leaky Integrate-and-Fire）** 模型与生物学保真度更高的 **Lapicque (LAP)** 模型。
*   **硬件测量平台**：开发了一个定制的**参数化稀疏感知卷积引擎**（基于 FPGA），具有周期精确性。其核心是**事件控制单元（ECU）**，通过优先级编码器（PENC）跳过零值激活，使硬件延迟直接与脉冲稀疏度挂钩。
*   **数学模型**：通过公式定义了层延迟与活跃神经元数量（$N_{active}$）的线性关系，并量化了 Lapicque 模型中 RC 时间常数对脉冲抑制的作用。

### 3. 实验设计
*   **数据集**：选取了三个典型的基于事件的视觉数据集：**DVS128-Gesture**（手势识别）、**N-MNIST**（脉冲版手写数字）和 **DVS-CIFAR10**（复杂物体识别）。
*   **网络架构**：基于 **VGG9** 的卷积 SNN，权重经过 4-bit 量化以适应边缘硬件。
*   **Benchmark 与对比方法**：
    *   **算法对比**：对比了四种代理梯度函数在不同超参数下的表现。
    *   **硬件对比**：将优化后的 VGG9 模型与现有的 SNN ASIC 加速器（如 SNE [16]）和 FPGA 实现 [25] 进行对比。

### 4. 资源与算力
*   **训练算力**：使用 **NVIDIA RTX 4090 GPU** 进行训练。
*   **硬件验证**：使用 **Xilinx Kintex UltraScale+ FPGA** (xcvu9p) 进行周期精确的硬件仿真和性能测量。
*   **训练时长**：全量设计空间探索（DSE）涉及每个代理梯度函数约 8,000 个 epoch 的训练量（40 次独立试验 $\times$ 200 epoch）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 4 种代理梯度函数进行了广泛的斜率扫描（$\alpha \in [1, 48]$）。
    *   对神经元参数（衰减因子 $\beta$、阈值 $\theta$）进行了网格搜索。
    *   每个配置在硬件平台上进行了独立的延迟和脉冲计数测量。
*   **充分性与客观性**：实验涵盖了从简单到复杂的多个数据集，并使用了自动化超参数优化框架 **Optuna**，确保了搜索过程的客观性。通过 Pareto 前沿分析（准确率 vs. 延迟）展示了多维度的权衡，实验设计较为充分。

### 6. 论文的主要结论与发现
*   **准确率并非效率的可靠指标**：在 DVS-CIFAR10 上准确率最高的配置，在硬件延迟上未必最优。
*   **代理梯度的决定性作用**：**SRE（Spike Rate Escape）** 函数虽然在训练稳定性上较弱，但在 DVS128-Gesture 任务中能将推理延迟降低 **12.2%**，因为它能更有效地抑制冗余脉冲。
*   **神经元模型的反直觉结论**：**Lapicque 模型**虽然单次更新的算术复杂度更高（需要乘法器），但由于其更优的脉冲抑制能力，总硬件延迟比简单的 LIF 模型降低了高达 **28%**。
*   **性能提升**：通过感知稀疏性的调优，VGG9 在 FPGA 上的延迟可与定制 ASIC 媲美，且准确率提升了 9.1%。

### 7. 优点
*   **软硬件解耦分析**：深入揭示了训练算法（代理梯度）如何通过改变权重分布来间接控制硬件底层的开关活动。
*   **生物学模型的硬件价值**：证明了增加神经元模型的生物学复杂性（如 Lapicque）在硬件系统层面反而可能带来更高的能效。
*   **开源贡献**：提供了完整的 RTL 代码和实验工件，具有极高的可重复性。

### 8. 不足与局限
*   **硬件平台局限性**：主要基于 FPGA 进行验证，虽然具有代表性，但在 ASIC 上的功耗表现（mW 级别）仅通过间接对比得出，缺乏直接的硅片测试数据。
*   **网络规模**：虽然使用了 VGG9，但对于更深层的网络（如 ResNet 或 Transformer 结构的 SNN）在超大规模数据集上的泛化能力尚待验证。
*   **训练开销**：为了寻找最优的稀疏性配置，需要进行大规模的 DSE 搜索，这在实际工业应用中可能会带来较高的前期训练成本。

（完）
