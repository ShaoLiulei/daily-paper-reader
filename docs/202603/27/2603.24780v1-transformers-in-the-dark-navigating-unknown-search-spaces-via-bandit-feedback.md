---
title: "Transformers in the Dark: Navigating Unknown Search Spaces via Bandit Feedback"
title_zh: 黑暗中的 Transformer：通过 Bandit 反馈在未知搜索空间中导航
authors: "Jungtaek Kim, Thomas Zeng, Ziqian Lin, Minjae Lee, Chungpa Lee, Jy-yong Sohn, Hyung Il Koo, Kangwook Lee"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24780v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: Transformer架构模拟搜索算法
tldr: 本研究探讨了 Transformer 架构是否能自主模拟外部搜索算法。研究者提出了“带老虎机反馈的未知树搜索”框架，通过在搜索轨迹上进行训练，证明了 Transformer 在理论上足以实现多种搜索策略。实验显示，模型不仅能从零开始学习这些策略，还能泛化至更深、更长的搜索空间。此外，通过微调预训练 LLM，可以解锁其在复杂搜索任务中的潜力，减少对外部搜索组件的依赖。
selection_source: fresh_fetch
motivation: 探索 Transformer 架构是否能够不依赖外部组件，自主学习并模拟复杂的树搜索算法以优化问题解决过程。
method: 引入了“带老虎机反馈的未知树搜索”框架，通过在搜索轨迹数据上训练 Transformer 来模拟不同的搜索策略。
result: Transformer 模型成功近似了多种搜索策略，并展现出在更深树结构和更长搜索步长上的泛化能力。
conclusion: Transformer 具备实现复杂搜索算法的理论表达能力，且通过特定任务的微调可以显著提升预训练 LLM 的自主搜索性能。
---

## 摘要
当大语言模型（LLM）与外部搜索算法结合时，其解决问题的效率可以得到提升。通过将多样化想法及其后续可能性的空间视为树状结构，搜索算法可以导航该搜索空间，并更有效地引导 LLM 找到更好的解决方案。虽然搜索算法能够在树状结构的利用（exploitation）与探索（exploration）之间实现有效平衡，但对外部组件的需求可能会使整体问题解决过程变得复杂。因此，我们提出了以下问题：LLM 或其底层的 Transformer 架构能否模拟搜索算法？为了回答这个问题，我们首先引入了一个简化框架，其中树的扩展和反馈信号由外部指定，从而允许对搜索能力进行受控评估。我们将这种设定称为带有 Bandit 反馈的未知树搜索。在此设定下，我们证明了 Transformer 在理论上具有足够的表达能力来实现不同的搜索策略，并且可以从头开始训练以模拟这些策略。我们的 Transformer 模型展现出了泛化到未见条件（如更长的步长或更深的树）的可能性。此外，我们证明了通过在搜索轨迹上对预训练 LLM 进行微调，持续的任务导向训练可以解锁其完整能力。

## Abstract
Effective problem solving with Large Language Models (LLMs) can be enhanced when they are paired with external search algorithms. By viewing the space of diverse ideas and their follow-up possibilities as a tree structure, the search algorithm can navigate such a search space and guide the LLM toward better solutions more efficiently. While the search algorithm enables an effective balance between exploitation and exploration of a tree-structured space, the need for an external component can complicate the overall problem-solving process. We therefore pose the following question: Can LLMs or their underlying Transformer architectures approximate a search algorithm? To answer this question, we first introduce a simplified framework in which tree extensions and feedback signals are externally specified, allowing for controlled evaluation of search capabilities. We call this setting unknown tree search with bandit feedback. Within this setting, we show that Transformers are theoretically expressive enough to implement distinct search strategies and can be trained from scratch to approximate those strategies. Our Transformer models exhibit the possibility of generalizing to unseen conditions such as longer horizons or deeper trees. Furthermore, we demonstrate that continued task-focused training unlocks the complete capabilities of a pretrained LLM, by fine-tuning the LLM on search trajectories.

---

## 论文详细总结（自动生成）

### 论文总结：黑暗中的 Transformer：通过 Bandit 反馈在未知搜索空间中导航

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）在解决复杂问题时，通常需要结合外部搜索算法（如 MCTS、A*）来导航可能的想法空间。虽然这种组合很强大，但依赖外部组件增加了系统的复杂性。本论文的核心问题是：**Transformer 架构本身是否具备实现或近似搜索算法的内在能力？** 
研究者提出了“带老虎机（Bandit）反馈的未知树搜索”框架，旨在隔离模型在不确定性下平衡“利用”与“探索”的能力，探讨 Transformer 是否能内化搜索逻辑，从而减少对外部算法的依赖。

#### 2. 方法论
*   **核心框架：** 引入“未知树搜索”设定。在该设定中，树的扩展（子节点）和奖励信号（Bandit 反馈）由外部环境提供，模型仅负责根据历史轨迹选择下一个要访问的节点。
*   **理论证明：** 论文通过构造性证明，展示了具有恒定深度和特定嵌入维度的 Transformer 能够精确实现多种搜索策略（如均匀采样、贪婪采样、UCT 算法等）。
*   **行为克隆（Behavior Cloning）：** 使用 Llama 架构从零训练 Transformer，使其模仿已知搜索算法的轨迹。
*   **关键技术细节：**
    *   **Tokenization 方案：** 设计了“基于叶节点的标记化”和“基于路径的标记化”来编码搜索历史。
    *   **模型接口：** 将搜索过程建模为多轮对话，模型预测下一个要访问的节点 ID。
    *   **微调策略：** 对预训练 LLM（如 Qwen3-8B）在特定搜索任务（学术论文搜索）的轨迹上进行 LoRA 微调。

#### 3. 实验设计
*   **场景与数据集：**
    1.  **多奖励树搜索（Multi-Reward Tree Search）：** 合成的二叉/四叉树，目标节点稀疏且带有不同奖励。
    2.  **多奖励导航（Multi-Reward Navigation）：** 在带有障碍物的网格（迷宫）中寻找通往高奖励目标的路径。
    3.  **学术论文搜索（Academic Paper Search）：** 基于 OpenAlex 数据的真实场景，从起点论文导航至目标论文。
*   **Benchmark 与对比方法：**
    *   **基准算法：** 均匀叶采样、贪婪叶采样、均匀路径采样、基于策略的路径采样（纯探索、贪婪、UCT）。
    *   **对比模型：** GPT-4.1、Gemini 2.5-Flash、Qwen3-8B（包括其“思维/Thinking”版本）。

#### 4. 资源与算力
*   **硬件设备：** 使用了 NVIDIA L40S GPU，以及 Intel Xeon Platinum 8352Y 和 AMD EPYC 9374F CPU。
*   **训练细节：** 
    *   从零训练的模型包含 8 个 Block，隐藏维度 512。
    *   离线训练集包含 20,000 条轨迹，验证集占 30%。
    *   微调 Qwen3-8B 时使用了 LoRA 技术，最大序列长度为 10,240，训练 1 个 Epoch。

#### 5. 实验数量与充分性
*   **实验规模：** 针对每种设置生成了 200 个不同的问题实例，测试阶段使用 1,000 条独立轨迹计算均值和标准差。
*   **维度覆盖：** 进行了行为克隆一致性实验、KL 散度分析，以及针对测试步长、墙壁密度、目标数量、树深度的**泛化性实验**。
*   **评价：** 实验设计较为充分，涵盖了从合成数据到真实数据的跨度，并对比了当前最先进的商用模型，实验过程客观、公平。

#### 6. 主要结论与发现
*   **架构潜力：** Transformer 在理论上足以表达复杂的搜索策略，且在实践中能通过行为克隆成功近似这些策略。
*   **泛化能力：** 从零训练的模型展现出了一定的泛化能力，能够处理比训练时更深或搜索步长更长的任务。
*   **LLM 现状：** 现有的预训练 LLM（如 Qwen3、Gemini）在原生状态下的搜索能力有限，甚至不如简单的均匀采样。
*   **微调增益：** 针对性的搜索轨迹微调能显著提升 LLM 的搜索效率，超越“LLM+外部算法”的简单组合，证明了 LLM 内部搜索能力的“可解锁性”。

#### 7. 优点
*   **理论与实践结合：** 不仅提供了 Transformer 实现搜索算法的理论保证，还通过大量实验验证了其可学习性。
*   **创新的框架：** 提出的“未知树搜索”框架为评估 LLM 的决策逻辑提供了一个受控且纯粹的环境。
*   **泛化性探索：** 深入探讨了模型在未见过的搜索深度和复杂度下的表现，具有前瞻性。

#### 8. 不足与局限
*   **效率问题：** 目前依赖多轮交互（Multi-turn）来实现搜索，过程相对缓慢且 Token 消耗大，尚未完全实现搜索逻辑的“内化”（即在单次推理中完成复杂搜索）。
*   **规模限制：** 实验主要集中在相对较小的模型和中等规模的搜索空间，在超大规模问题上的表现尚待验证。
*   **偏差风险：** 微调过程中使用的训练轨迹（由特定 LLM 生成）可能引入未知的模型偏差。

（完）
