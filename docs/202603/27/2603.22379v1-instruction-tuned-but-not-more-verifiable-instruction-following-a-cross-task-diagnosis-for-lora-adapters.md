---
title: "Instruction-Tuned, but Not More Verifiable Instruction-Following: A Cross-Task Diagnosis for LoRA Adapters"
title_zh: 经过指令微调，但可验证的指令遵循能力并未提升：LoRA 适配器的跨任务诊断
authors: Junyi Zou
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22379v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 评估用于指令遵循任务的 LoRA 适配器
tldr: 本研究探讨了LoRA适配器的名义标签（如“指令微调”）与其在不同任务中实际能力提升之间的不一致性。通过在IFEval等基准上测试，发现指令微调适配器往往无法提升可验证的指令遵循能力，反而可能在无关的数值任务上表现更好。这种现象被称为“能力漂移”，研究强调了在部署前进行全面跨任务评估的必要性，以避免名义标签误导实际应用。
selection_source: fresh_fetch
motivation: 探究LoRA适配器的名义训练目标（如指令微调）是否能真实且可靠地反映其在不同任务中的实际能力增益。
method: 在多种基础模型和LoRA配置下，通过跨任务评估矩阵对比适配器在名义目标任务与非目标任务（如数值基准）上的表现。
result: 指令微调适配器在可验证指令遵循任务上常表现出改进失败甚至负增长，却在无关的数值任务上显著提升，呈现出明显的“能力漂移”。
conclusion: 名义标签并非可靠的能力代理，建议在部署适配器前进行常规的跨任务评估，以确保其实际表现符合预期。
---

## 摘要
适配器通常根据名义标签（例如“指令微调”）进行选择和部署，这些标签隐含地暗示了适配后哪些能力会得到提升。我们通过在不同任务中评估同一个 LoRA 适配器，测试名义训练目标是否与实际实现的跨任务能力增益可靠地保持一致。我们最强有力的证据与 IFEval 衡量的严格、可自动验证的指令遵循能力相关：在多个随机种子、基座模型和 LoRA 设置下，名义标签反复（但非普遍）无法预测该可验证目标上的改进，且表现出明显的配置敏感性，包括接近零或负增长的情况。作为一个受控的“指令对数值”设置中的典型最强案例，一个经过指令微调的适配器显著提高了非目标 NM 数值基准的性能（从 0.133 提升至 0.632），但并未提高 IFEval 上的可验证指令遵循能力（ILA：0.313 降至 0.271；PLA：0.250 降至 0.143；数值保留三位小数）。我们将这种名义与实际表现不匹配的模式描述为“能力漂移”（capability drift）。这种不匹配在原始跨任务性能矩阵中清晰可见；我们使用漂移分数仅作为与基础指标单位相同的简洁总结，而非作为一种新的正式指标贡献。来自更广泛指令遵循基准的证据取决于具体基准且结果不一，反映了指令遵循在操作化定义上的异质性；因此，我们不将跨基准的一致性作为前提。总之，实际的启示是在部署前进行常规的跨任务评估，并避免将名义标签视为可靠的能力代理。

## Abstract
Adapters are often selected and deployed based on nominal labels (e.g., instruction-tuned), which implicitly suggest what capability improves after adaptation. We test whether nominal training objectives reliably align with realized cross-task capability gains by evaluating the same LoRA adapter across tasks. Our strongest evidence is tied to strict, automatically verifiable instruction following as measured by IFEval: across multiple seeds, base models, and LoRA settings, nominal labels recurrently but not universally fail to predict improvements on this verifiable target, with clear configuration sensitivity including a near-zero or negative case. As an illustrative strongest-case example in a controlled instruction-versus-numeric setting, an instruction-tuned adapter substantially improves off-target NM-based numeric benchmark performance from 0.133 to 0.632 while not improving verifiable instruction following on IFEval (ILA: 0.313 to 0.271; PLA: 0.250 to 0.143; values rounded to three decimals). We refer to this nominal-versus-realized mismatch pattern as capability drift as a descriptive label. The mismatch is visible in the raw cross-task performance matrix; we use a drift score only as a compact summary in the same units as the underlying metrics, not as a new formal metric contribution. Evidence from broader instruction-following benchmarks is benchmark-dependent and mixed, reflecting heterogeneity in how instruction following is operationalized; we therefore do not treat cross-benchmark agreement as a premise. Overall, the practical takeaway is to perform routine cross-task evaluation before deployment and to avoid treating nominal labels as reliable capability proxies.

---

## 论文详细总结（自动生成）

以下是对论文《Instruction-Tuned, but Not More Verifiable Instruction-Following: A Cross-Task Diagnosis for LoRA Adapters》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在大型语言模型（LLM）的微调中，LoRA 适配器通常被冠以“指令微调”、“数学微调”等名义标签。开发者往往默认这些标签准确描述了适配器所提升的能力。
*   **核心问题**：适配器的名义训练目标（Nominal Labels）是否真的能可靠地预测其在部署时的实际能力增益？
*   **整体含义**：论文通过实验揭示了一种“能力漂移”（Capability Drift）现象，即适配器在目标任务上可能提升微小甚至退化，却在非目标任务上表现出显著的非预期增长。这警示开发者不能盲信适配器的标签，必须进行跨任务评估。

### 2. 论文提出的方法论
*   **核心思想**：提出“跨任务诊断”（Cross-Task Diagnosis）框架，通过在多个不同性质的任务上评估同一个适配器，量化名义目标与实际表现之间的偏差。
*   **关键指标：漂移分数 (Drift Score)**：
    *   **目标增益 (TargetGain)**：适配器在名义目标任务上的得分提升。
    *   **非目标增益 (OffTargetGain)**：适配器在非目标任务上的得分提升。
    *   **漂移分数公式**：$DriftScore = OffTargetGain - TargetGain$。
    *   **逻辑**：若分数为正且数值较大，说明适配器在非目标领域的表现远超其名义目标领域。
*   **分析流程**：对比基座模型与适配器在 IFEval（指令遵循）和数值推理（NM）等任务上的原始性能矩阵，计算漂移分数，并结合细分类型分析（Breakdown Analysis）和几何探测（Probing）来探究变化。

### 3. 实验设计
*   **基座模型**：包括 Qwen3 (8B/14B)、Llama-3.1-8B-Instruct、Qwen2.5-7B-Instruct、DeepSeek-R1-Distill-Qwen-7B。
*   **适配器设置**：
    *   `instr`：指令微调适配器。
    *   `reason`：数值推理微调适配器。
    *   `domain`：特定领域微调适配器（作为次要参考）。
*   **Benchmark（基准测试）**：
    *   **IFEval**：核心基准，用于评估严格、可自动验证的指令遵循能力（指标：PLA 提示级准确率、ILA 指令级准确率）。
    *   **数值推理**：使用 NM (Numeric Match) 指标衡量数值匹配能力。
    *   **补充基准**：FollowBench（规则评分）、IFBench（严格 PLA）。
*   **对比方法**：将适配器在名义目标任务上的表现与其在跨任务（Off-target）场景下的表现进行横向对比。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实验性质**：作者指出“所有分析均重用现有的训练和评估产物”，这意味着研究重点在于对已训练好的适配器进行大规模的推理评估和诊断分析，而非从头训练模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 Qwen3-8B 进行了 5 个随机种子的重复实验以排除偶然性。
    *   覆盖了多种模型家族（Llama, Qwen, DeepSeek）和不同参数规模（7B, 8B, 14B）。
    *   测试了不同的 LoRA 配置（Rank 8/16/32，以及仅 Attention 或 Attention+MLP 模块）。
*   **充分性与客观性**：实验设计较为充分，通过多模型、多种子、多配置的鲁棒性测试证明了现象的普遍性。作者客观地指出该现象虽“反复出现”但“并非普遍存在”，并承认了不同基准测试之间存在异质性。

### 6. 论文的主要结论与发现
*   **名义标签失效**：指令微调适配器在 IFEval 这一严格的可验证指令遵循任务上经常无法提供增益，甚至导致性能下降。
*   **显著的能力漂移**：一个典型的“最强案例”显示，指令微调适配器将数值推理性能从 0.133 提升至 0.632，但其在 IFEval 上的 PLA 却从 0.250 降至 0.143。
*   **配置敏感性**：漂移的程度受基座模型和 LoRA 设置的影响，存在接近零或轻微负漂移的特例，说明该现象具有配置敏感性。
*   **指令类型差异**：细分分析显示，指令微调后，语言约束和特定格式约束（如关键字存在）往往退化最严重。

### 7. 优点
*   **诊断视角新颖**：不同于以往关注适配器如何“提升”性能，本文关注标签与实际能力的“错位”，具有很强的工程实践指导意义。
*   **评估严谨**：坚持使用“可自动验证”的硬性指标（IFEval），避免了基于 LLM 打分的主观性和不确定性。
*   **鲁棒性强**：通过多维度（种子、模型、参数）的扫参，证明了结论的可靠性。

### 8. 不足与局限
*   **机制解释尚浅**：虽然进行了几何探测和功能探测，但未能给出导致“能力漂移”的底层因果机制或物理性解释。
*   **任务覆盖面**：主要集中在指令遵循与数值推理的冲突，尚未探讨如代码生成、多语言能力等其他任务组合中的漂移情况。
*   **基准依赖性**：结论在很大程度上依赖于 IFEval 的严格定义，在更宽松或基于人类偏好的评估标准下，结论可能存在差异。

（完）
