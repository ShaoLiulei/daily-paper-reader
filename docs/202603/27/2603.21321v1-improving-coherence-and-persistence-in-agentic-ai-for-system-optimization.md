---
title: Improving Coherence and Persistence in Agentic AI for System Optimization
title_zh: 提升用于系统优化的代理式 AI 的连贯性与持久性
authors: "Pantea Karimi, Kimia Noorbakhsh, Mohammad Alizadeh, Hari Balakrishnan"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21321v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 利用大语言模型进行系统优化的智能体架构
tldr: 本研究针对LLM在系统优化中面临的局部最优陷阱和长上下文连贯性瓶颈，提出了Engram智能体架构。Engram通过将长期探索过程与单一上下文窗口解耦，利用持久化的Archive存储代码日志，并使用Research Digest提炼高层见解，使后续智能体能基于先前发现持续迭代。该方法有效解决了知识积累失效问题，在多云组播和缓存优化等任务中显著提升了性能。
selection_source: fresh_fetch
motivation: 传统LLM在复杂系统优化中容易陷入局部最优，且难以在长周期任务中保持上下文连贯性或积累跨运行的知识。
method: 提出Engram架构，通过持久化存储代码快照和提炼研究摘要，实现跨智能体运行的知识传承与解耦探索。
result: 在多云组播、LLM请求路由及数据库KV缓存重用等多样化领域中，Engram均展现出超越现有方法的性能。
conclusion: 引入持久化记忆和解耦探索机制是提升AI智能体处理复杂、多步骤系统优化问题的关键。
---

## 摘要
设计高性能系统启发式算法是一个创造性的迭代过程，需要专家形成假设并执行多步概念转变。虽然大语言模型（LLMs）在自动化这一循环方面展现出潜力，但由于两种关键的失败模式，它们在处理复杂系统问题时仍面临困难：进化邻域偏差（evolutionary neighborhood bias）和连贯性上限（coherence ceiling）。进化方法通常依赖标量基准分数，从而陷入局部最优，在需要协调的多步变更时往往会失败。相反，现有的代理（agentic）框架在长周期内会遭受上下文退化，或者无法在独立的运行之间积累知识。我们提出了 Engram，这是一种代理研究员架构，通过将长周期探索与单一上下文窗口的限制解耦来解决这些局限性。Engram 将探索组织为一系列代理，这些代理迭代地设计、测试和分析机制。在每次运行结束时，代理会将代码快照、日志和结果存储在持久化存档（Archive）中，并将高层建模见解提炼为简洁、持久的研究摘要（Research Digest）。随后的代理将从全新的上下文窗口开始，通过阅读研究摘要来基于先前的发现进行构建。我们发现 Engram 在多个领域表现出优异的性能，包括多云组播、LLM 推理请求路由，以及自然语言查询数据库中的 KV 缓存重用优化。

## Abstract
Designing high-performance system heuristics is a creative, iterative process requiring experts to form hypotheses and execute multi-step conceptual shifts. While Large Language Models (LLMs) show promise in automating this loop, they struggle with complex system problems due to two critical failure modes: evolutionary neighborhood bias and the coherence ceiling. Evolutionary methods often remain trapped in local optima by relying on scalar benchmark scores, failing when coordinated multi-step changes are required. Conversely, existing agentic frameworks suffer from context degradation over long horizons or fail to accumulate knowledge across independent runs.   We present Engram, an agentic researcher architecture that addresses these limitations by decoupling long-horizon exploration from the constraints of a single context window. Engram organizes exploration into a sequence of agents that iteratively design, test, and analyze mechanisms. At the conclusion of each run, an agent stores code snapshots, logs, and results in a persistent Archive and distills high-level modeling insights into a compact, persistent Research Digest. Subsequent agents then begin with a fresh context window, reading the Research Digest to build on prior discoveries.   We find that Engram exhibits superior performance across diverse domains including multi-cloud multicast, LLM inference request routing, and optimizing KV cache reuse in databases with natural language queries.

---

## 论文详细总结（自动生成）

### 论文总结：提升用于系统优化的代理式 AI 的连贯性与持久性

#### 1. 核心问题与研究动机
论文指出，利用大语言模型（LLM）自动化设计高性能系统启发式算法（如路由协议、调度策略）面临两大瓶颈：
*   **进化邻域偏差（Evolutionary Neighborhood Bias）：** 传统的代码进化系统（如 OpenEvolve）依赖标量分数进行筛选，容易陷入局部最优。当改进需要重大的概念转变（如从贪心算法转向数学规划）而非简单的代码微调时，这类方法往往失效。
*   **连贯性上限（Coherence Ceiling）：** 现有的代理框架在长周期任务中，要么受限于单一上下文窗口的“上下文腐烂”（注意力不集中），要么在多次独立运行间无法积累知识，导致重复发现已有的错误。

#### 2. 方法论：Engram 架构
Engram 的核心思想是将**长周期探索**与**单一上下文窗口的限制**解耦，通过结构化的知识传递实现“持久性”和“连贯性”。
*   **序列化代理探索：** 将研究过程分解为一系列连续的智能体。每个智能体执行“假设 $\rightarrow$ 实现 $\rightarrow$ 实验 $\rightarrow$ 分析”的科学循环。
*   **持久化存档（Archive）：** 存储所有历史代码快照、执行日志和实验结果，供后续智能体按需检索。
*   **研究摘要（Research Digest）：** 这是 Engram 的关键创新。每个智能体在结束任务前，会将高层建模见解、失败诊断和下一步建议提炼成简洁的摘要。
*   **结构化交接（Handoff）：** 下一个智能体启动时拥有全新的上下文窗口，通过阅读《研究摘要》快速继承前人的知识，从而避免上下文负担，同时保持长期的研究逻辑连贯。

#### 3. 实验设计
研究在三个具有代表性的复杂系统问题上进行了评估：
*   **多云组播（Multi-Cloud Multicast）：** 在 71 个节点的全球拓扑中优化数据传输成本和时间。
*   **LLM 推理请求路由：** 在分布式推理系统中调度请求，以降低平均响应时间（RT）。
*   **数据库 KV 缓存重用：** 优化自然语言查询中的行/列排序，以最大化前缀缓存命中率。
*   **对比方法：** 包括进化算法（EoH, FunSearch, OpenEvolve）、代理框架（Glia）以及人类专家设计的尖端算法（Human SOTA）。
*   **基准测试：** 使用了 ADRS 基准测试集中的 8 个任务及 Vidur 模拟器。

#### 4. 资源与算力
*   **模型使用：** 实验主要使用了 OpenAI 的 **o3** 和 **GPT-5.2**（注：论文日期标注为 2026 年，提及了当时的最先进模型）。
*   **算力环境：** 模拟实验（如 Vidur）在 4 张 NVIDIA A10 GPU 上运行。
*   **运行规模：** 每种方法运行 10 次独立实验，每次实验包含 100 次模拟评估预算。
*   **说明：** 论文未详细列出 API 调用的总 Token 数或具体训练时长，重点在于推理和探索过程。

#### 5. 实验数量与充分性
*   **实验规模：** 论文涵盖了 9 个不同的系统优化任务，每个任务均进行了 10 次重复实验并提供了 90% 的置信区间。
*   **消融实验：** 针对“单一智能体”、“仅代码传递”、“无摘要”、“无存档”等变体进行了详细消融，验证了各组件的必要性。
*   **评价：** 实验设计非常充分，不仅对比了多种 SOTA AI 方法，还直接挑战了人类专家的最优解，具有很强的说服力和客观性。

#### 6. 主要结论与发现
*   **超越人类专家：** Engram 在 9 个任务中的 8 个任务上超过了人类专家的 SOTA 表现。例如在多云组播中，Engram 发现了基于混合整数线性规划（MILP）的新启发式算法，成本低于人类设计的 Cloudcast。
*   **突破局部最优：** 相比进化算法，Engram 能够忍受暂时的性能下降（回归），通过分析失败原因最终实现算法家族的跨越（如从 Steiner 树转向动态规划）。
*   **高效性：** 相比 Glia 等框架，Engram 在相同的模拟预算下收敛速度更快，且不会因上下文增长而导致性能退化。

#### 7. 优点
*   **知识传承机制：** 通过《研究摘要》解决了 LLM 在长程任务中的遗忘问题，模拟了人类科学研究的交接过程。
*   **非单调改进：** 能够处理“先变差再变好”的复杂优化路径，这是传统进化算法难以企及的。
*   **通用性强：** 在网络、数据库、AI 推理等多个异构领域均表现出极强的适应性。

#### 8. 不足与局限
*   **模型依赖性：** Engram 的性能高度依赖于底层 LLM 的推理能力（如 o3 或 GPT-5.2），在较弱的模型上可能无法有效提炼摘要。
*   **成本风险：** 序列化的智能体运行和多次模拟评估可能产生较高的 API 调用成本。
*   **应用限制：** 目前主要在模拟器环境下验证，在真实生产系统中的实时性、安全性及在线调整能力尚待进一步研究。

（完）
