---
title: "Visual Attention Drifts,but Anchors Hold:Mitigating Hallucination in Multimodal Large Language Models via Cross-Layer Visual Anchors"
title_zh: 视觉注意力会漂移，但锚点依然稳固：通过跨层视觉锚点缓解多模态大语言模型中的幻觉
authors: "Chengxu Yang, Jingling Yuan, Chuang Hu, Jiawei Jiang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25088v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 减轻多模态大语言模型中的幻觉
tldr: 针对多模态大语言模型中的物体幻觉问题，本文研究发现幻觉源于深层注意力向早期视觉噪声的回归。为此，作者提出了CLVA（跨层视觉锚点），这是一种无需训练的方法，通过强化中间层的关键视觉锚点并抑制深层回归噪声，有效纠正注意力偏移。实验证明，该方法在多种架构和基准测试中均能显著减少幻觉，且不增加计算负担。
selection_source: fresh_fetch
motivation: 研究发现多模态模型在深层会出现注意力偏移，回归到早期的视觉噪声，从而导致物体幻觉。
method: 提出一种名为CLVA的无需训练的方法，通过利用中间层的视觉锚点来强化关键特征并抑制深层的回归噪声。
result: 在多种模型架构和基准测试中均表现出色，显著降低了幻觉率，且未明显增加计算开销。
conclusion: 通过跨层视觉锚点引导深层注意力，是解决多模态大模型幻觉问题的一种高效且具有解释性的方案。
---

## 摘要
多模态大语言模型（MLLMs）经常受到物体幻觉的困扰。虽然现有研究利用注意力增强和视觉回溯，但我们发现这些工作在模型最终阶段的注意力漂移方面缺乏足够的解释性。在本文中，我们研究了视觉特征的逐层演变，并发现幻觉源于深层注意力向早期层的初始视觉噪声回归。我们观察到，输出的可靠性取决于在中间层而非最终层获取视觉锚点。基于这些见解，我们提出了 CLVA（Cross-Layer Visual Anchors，跨层视觉锚点），这是一种无需训练的方法，它在抑制回归噪声的同时增强了关键的中间层特征。该方法通过利用从注意力动态中捕获的关键锚点，有效地将深层注意力拉回到正确的视觉区域。我们在多种架构和基准测试上评估了我们的方法，证明了其在不显著增加计算时间和 GPU 显存的情况下具有出色的性能。

## Abstract
Multimodal Large Language Models often suffer from object hallucination. While existing research utilizes attention enhancement and visual retracing, we find these works lack sufficient interpretability regarding attention drift in final model stages. In this paper, we investigate the layer wise evolution of visual features and discover that hallucination stems from deep layer attention regressing toward initial visual noise from early layers. We observe that output reliability depends on acquiring visual anchors at intermediate layers rather than final layers. Based on these insights, we propose CLVA, which stands for Cross-Layer Visual Anchors, a training free method that reinforces critical mid layer features while suppressing regressive noise. This approach effectively pulls deep layer attention back to correct visual regions by utilizing essential anchors captured from attention dynamics. We evaluate our method across diverse architectures and benchmarks, demonstrating outstanding performance without significant increase in computational time and GPU memory.

---

## 论文详细总结（自动生成）

以下是对论文《Visual Attention Drifts, but Anchors Hold: Mitigating Hallucination in Multimodal Large Language Models via Cross-Layer Visual Anchors》的结构化总结：

### 1. 核心问题与整体含义
论文针对多模态大语言模型（MLLM）中的**物体幻觉（Object Hallucination）**问题展开研究。作者发现，模型在生成文本时，深层（Later Layers）的注意力机制会发生“漂移”：原本在中间层能够准确锁定图像中相关区域的注意力，在深层会退化并回归到早期层（Early Layers）的初始视觉噪声分布。这种现象导致模型在输出阶段过度依赖语言先验而非视觉证据，从而产生幻觉。

### 2. 方法论：CLVA（跨层视觉锚点）
CLVA 是一种**无需训练（Training-free）**、即插即用的干预方法，核心思想是利用中间层的“正向引导”和早期层的“负向噪声”来修正深层的注意力偏移。
*   **识别功能头**：通过计算注意力头对视觉 Token 的关注强度，将注意力头分为“视觉敏感头”和“视觉不敏感头”。
*   **提取双锚点**：
    *   **正锚点（Positive Anchor）**：从中间层的敏感头中提取，代表模型已经识别出的任务相关语义区域。
    *   **负锚点（Negative Anchor）**：从初始层的不敏感头中提取，代表无意义的空间偏见或背景噪声。
*   **注意力重锚定**：在深层的推理过程中，根据双锚点生成的空间掩码调整注意力矩阵。公式上通过增强正锚点区域权重、抑制负锚点区域权重，强制模型将注意力“拉回”到正确的视觉区域，并重新归一化以维持概率分布。

### 3. 实验设计
*   **模型对象**：选取了三种具有代表性的架构：LLaVA-1.5-7B（线性投影）、Qwen-VL-7B（视觉抽象器）和 InstructBLIP-7B（Q-Former 架构）。
*   **基准测试（Benchmarks）**：
    *   **POPE**：评估物体存在性的判别能力（包含随机、热门、对抗三种采样）。
    *   **CHAIR**：评估图像描述任务中的物体幻觉率（CHAIR_i 和 CHAIR_s）。
    *   **MME**：综合评估模型的感知（存在、计数、位置、颜色）和认知能力。
*   **对比方法**：对比了 Greedy Search、Beam Search 以及主流的幻觉缓解方法如 IMCCD、MemVR 和 ClearSight。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA H20 GPU** 上完成。
*   **算力开销**：由于是无需训练的方法，CLVA 不涉及训练时长。在推理阶段，CLVA 表现出极高的效率。例如在 LLaVA-1.5 上，总推理时间仅比原始 Greedy Search 增加约 1.2%（从 7分48秒 增至 7分54秒），显存占用仅增加 0.1G。相比之下，对比解码方法（如 IMCCD）的时间开销和显存占用几乎是 CLVA 的两倍。

### 5. 实验数量与充分性
作者进行了多维度的实验：
*   **跨模型验证**：在三种不同架构的模型上均取得了提升，证明了通用性。
*   **多任务评估**：涵盖了判别式（POPE）和生成式（CHAIR）任务。
*   **消融实验**：分别验证了正锚点和负锚点的必要性，并测试了超参数（如缩放因子 $\alpha$、惩罚因子 $\beta$）的敏感度。
*   **可视化分析**：通过热力图直观展示了干预前后注意力的变化。
实验设计客观、公平，对比基准涵盖了当前最先进的同类方法，样本量（如 POPE 的 3000 个问题，CHAIR 的 500 张图像）足以支撑结论。

### 6. 主要结论与发现
*   **注意力回归规律**：幻觉并非随机产生，而是深层注意力向早期层噪声模式的系统性回归。
*   **中间层的重要性**：模型在中间层具备最强的视觉定位能力，这些“锚点”在后续层中被语言偏见覆盖。
*   **CLVA 的有效性**：通过简单的跨层注意力修正，可以显著降低幻觉率（如在 CHAIR 任务中显著降低了幻觉指标），同时提升模型在 MME 等综合基准上的得分。

### 7. 优点
*   **高解释性**：基于对模型内部层级演化的深入观察，而非黑盒式的经验调整。
*   **极低开销**：无需重新训练或微调，且推理延迟和显存增加几乎可以忽略不计，非常适合端侧部署。
*   **架构无关性**：对线性投影和 Q-Former 等不同模态对齐机制均有效。

### 8. 不足与局限
*   **超参数敏感性**：虽然论文给出了推荐值，但不同模型可能需要微调正负锚点的权重参数以达到最优效果。
*   **语言任务权衡**：在 MME 的某些纯语言任务（如数值计算、翻译）中，由于强制模型关注视觉，可能会导致轻微的性能下降。
*   **锚点层选择**：目前正锚点层固定在中间层（如 L/2-2），对于层数极深或极浅的新型架构，可能需要重新寻找最佳锚点层。

（完）
