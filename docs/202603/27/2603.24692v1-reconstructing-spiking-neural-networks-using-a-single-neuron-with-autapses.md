---
title: Reconstructing Spiking Neural Networks Using a Single Neuron with Autapses
title_zh: 利用具有自突触的单个神经元重构脉冲神经网络
authors: "Wuque Cai, Hongze Sun, Quan Tang, Shifeng Mao, Zhenxing Wang, Jiayi He, Duo Chen, Dezhong Yao, Daqing Guo"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24692v1"
tags: ["query:snn"]
score: 10.0
evidence: 使用单个LIF神经元重构脉冲神经网络
tldr: 本研究针对传统脉冲神经网络（SNN）多层架构导致的高通信和存储成本，提出了一种基于自突触启发的时间延迟自突触脉冲神经网络（TDA-SNN）。该框架仅使用单个LIF神经元，通过原型学习策略和内部时间状态重组，实现了储层、多层感知机及类卷积架构。实验证明，TDA-SNN在显著降低神经元数量和内存开销的同时，在多项基准测试中保持了竞争性能，揭示了时间复用单神经元模型在紧凑型类脑计算中的巨大潜力。
selection_source: fresh_fetch
motivation: 旨在解决传统多层脉冲神经网络在神经形态计算中面临的高通信成本和状态存储开销问题。
method: 提出TDA-SNN框架，利用单个带有时间延迟自突触的LIF神经元，通过重组时间状态和原型学习来重构多种网络架构。
result: 在多个基准测试中，该模型以极低的神经元数量和内存占用实现了与传统SNN相当的性能，但在极端设置下存在时间延迟增加的权衡。
conclusion: 验证了时间复用单神经元模型作为高效、紧凑计算单元的可行性，为类脑计算提供了新的设计思路。
---

## 摘要
脉冲神经网络（SNN）在类脑计算领域具有广阔前景，但高性能模型仍依赖于密集的多层架构，伴随着巨大的通信和状态存储开销。受自突触启发，我们提出了时延自突触 SNN（TDA-SNN），这是一个利用单个泄漏整合发放（LIF）神经元和基于原型学习的训练策略来重构 SNN 的框架。通过重组内部时间状态，TDA-SNN 可以在统一框架内实现储备池、多层感知机和类卷积的脉冲架构。在序列、基于事件和图像基准测试上的实验表明，该模型在储备池和 MLP 设置下具有竞争力的性能，而卷积结果则揭示了明显的时空权衡。与标准 SNN 相比，TDA-SNN 在极大减少神经元数量和状态内存的同时，提高了单个神经元的信息容量，代价是在极端的单神经元设置下增加了额外的时间延迟。这些发现突显了时间复用单神经元模型作为类脑计算紧凑型计算单元的潜力。

## Abstract
Spiking neural networks (SNNs) are promising for neuromorphic computing, but high-performing models still rely on dense multilayer architectures with substantial communication and state-storage costs. Inspired by autapses, we propose time-delayed autapse SNN (TDA-SNN), a framework that reconstructs SNNs with a single leaky integrate-and-fire neuron and a prototype-learning-based training strategy. By reorganizing internal temporal states, TDA-SNN can realize reservoir, multilayer perceptron, and convolution-like spiking architectures within a unified framework. Experiments on sequential, event-based, and image benchmarks show competitive performance in reservoir and MLP settings, while convolutional results reveal a clear space--time trade-off. Compared with standard SNNs, TDA-SNN greatly reduces neuron count and state memory while increasing per-neuron information capacity, at the cost of additional temporal latency in extreme single-neuron settings. These findings highlight the potential of temporally multiplexed single-neuron models as compact computational units for brain-inspired computing.

---

## 论文详细总结（自动生成）

以下是对论文《Reconstructing Spiking Neural Networks Using a Single Neuron with Autapses》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统脉冲神经网络（SNN）虽然具有高能效潜力，但高性能模型通常依赖于密集的多层架构，这导致了巨大的神经元通信开销和状态存储需求，限制了其在资源极度受限的边缘设备上的部署。
*   **研究动机**：受生物学中自突触（Autapse，神经元与自身的突触连接）和浦肯野细胞复杂动力学的启发，探索是否能利用**单个神经元**通过时间复用（Temporal Multiplexing）来重构复杂的网络架构，从而实现极高的空间压缩比。

### 2. 方法论
*   **核心思想**：提出**TDA-LIF模型**，在泄漏整合发放（LIF）神经元中引入带延迟的自突触反馈，将神经元在不同时间步的内部状态视为“虚拟节点”，通过自突触连接这些节点来模拟神经元间的空间连接。
*   **关键技术细节**：
    *   **TDA-LIF 公式**：在膜电位更新方程中增加自突触电流项 $I_{autapse} = \sum w_{da} s_{t-d}$，其中 $d$ 为延迟步数。
    *   **架构重构映射**：
        *   **储备池（RC）**：通过全连接或随机延迟的自突触，使单个神经元在时间轴上展开为具有复杂递归特性的储备池。
        *   **多层感知机（MLP）**：通过“自突触剪枝”，将时间轴划分为多个段，段与段之间的延迟连接模拟层间前向传播。
        *   **类卷积（Conv）**：利用权重共享机制和特定的延迟模式，在时间维度上模拟卷积核的空间聚合过程。
    *   **原型学习（Prototype Learning）**：设计了一种时空原型匹配解码器，将网络输出的脉冲序列与可学习的类别原型进行相似度对比，以捕获动态发放特征。
    *   **训练算法**：采用时空反向传播（STBP）和代理梯度（Surrogate Gradient）解决脉冲不可微问题。

### 3. 实验设计
*   **数据集**：
    *   **序列/音频**：DEAP（脑电情感分析）、SHD（语音识别）。
    *   **静态图像**：MNIST、Fashion-MNIST、CIFAR-10、CIFAR-100。
    *   **神经形态/事件流**：DVS Gesture（手势识别）。
*   **Benchmark**：标准多神经元脉冲神经网络（STD-SNN）。
*   **对比方法**：在相同架构规模（节点数一致）下，对比 TDA-SNN 与 STD-SNN 的准确率、参数量、神经元数及内存占用。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA A100 GPU** 进行训练和推理。
*   **软件框架**：基于 PyTorch 实现。
*   **训练细节**：通常训练 100 个 epoch，使用 Adam 优化器和余弦退火学习率调度。文中未明确提及具体的 GPU 数量，但实验规模暗示单卡即可完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单图像到复杂事件流、从音频到脑电信号的 7 个主流基准数据集，实验覆盖面广。
*   **消融实验**：详细探讨了自突触选择策略（随机延迟 RD、最大连接 MC、时间不变 T-Inv）、网络深度（层数）、节点数量以及学习率敏感性。
*   **客观性**：RC 和 MLP 实验均重复 10 次取平均值和标准差，卷积实验重复 5 次。通过与神经元数量对等的标准 SNN 进行公平对比，客观展示了性能与效率的权衡。

### 6. 主要结论与发现
*   **功能等效性**：证明了单个 TDA-LIF 神经元在理论和实践上均能构造出 RC、MLP 和类卷积结构。
*   **性能表现**：在 RC 和 MLP 设置下，TDA-SNN 表现出与标准 SNN 相当甚至更优的竞争力（如在 DEAP 和 SHD 上）。
*   **空间效率**：神经元数量从 $N$ 减少到 1，状态内存大幅下降（例如 CIFAR-10 任务中内存从 8KB 降至 4B）。
*   **时空权衡**：虽然空间极度压缩，但带来了显著的时间延迟。在单神经元卷积设置下，训练速度比标准 SNN 慢约 178 倍，且准确率存在一定差距。

### 7. 优点
*   **极致压缩**：将空间复杂度转化为时间复杂度，为极小规模硬件实现深度学习提供了可能。
*   **信息容量高**：单神经元的信息承载量（bits per neuron）远高于传统模型。
*   **灵活性**：统一框架可适配多种网络拓扑，且支持通过增加并行神经元来灵活调节“空间-时间”的平衡点。

### 8. 不足与局限
*   **卷积性能瓶颈**：在处理复杂视觉特征（如 CIFAR-10）时，单神经元模型的准确率和收敛速度仍逊于标准多层卷积网络。
*   **时间开销巨大**：串行的时间演化导致推理延迟高，不适合对实时性要求极高的场景。
*   **优化挑战**：延迟反馈引入了复杂的梯度依赖，在深层或大尺度节点设置下，训练稳定性及梯度消失/爆炸风险增加。

（完）
