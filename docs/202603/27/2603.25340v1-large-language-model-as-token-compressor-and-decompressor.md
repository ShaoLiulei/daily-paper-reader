---
title: Large Language Model as Token Compressor and Decompressor
title_zh: 大语言模型作为 Token 压缩器与解压器
authors: "Wenbing Li, Zikai Song, Jielei Zhang, Tianhao Zhao, Junkai Lin, Yiran Wang, Wei Yang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25340v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 微调预训练大模型进行标记压缩与重构
tldr: 本研究发现预训练大语言模型（LLM）可作为高效的 Token 压缩器与解压器。通过自表达自编码框架和 LoRA 微调，将长文本转化为紧凑的离散变长潜码（Z-tokens）。该方法具有内容自适应性，能针对语义密度动态分配压缩率。实验显示，在 Wikipedia 等数据集上可实现 18 倍压缩，且能完美重构并支持 Z 空间推理，为长上下文处理提供了高效方案。
selection_source: fresh_fetch
motivation: 旨在解决长文本处理中 Token 冗余导致的计算资源浪费，提升长上下文推理的效率。
method: 采用基于 LoRA 的自编码框架，将 LLM 微调为能将文本映射至变长潜码空间（Z-tokens）的压缩工具。
result: 在多个主流数据集上实现高达 18 倍的 Token 缩减，且重构精度和下游任务表现优异。
conclusion: 证明了 LLM 具备强大的自压缩能力，为实现 Token 高效的长文本生成和推理开辟了新方向。
---

## 摘要
在本文中，我们提出了一个新颖的见解，即现成的大语言模型（LLM）可以作为出色的 Token 压缩器和解压器。为了证明这一点，我们设计了一个自表达自编码学习框架，该框架微调预训练的 LLM，将长文本翻译成一种由离散、变长潜码组成的紧凑内部语言（称为 Z-tokens），并从中精确地重建原始文本。由此产生的表示是内容自适应的：通过轻量级的基于 LoRA 的适配器头，语义密集的片段会获得更多的 Z-tokens，而冗余或可预测的区域则会被大幅压缩。实验表明，我们的方法在 Wikipedia、CNN/DailyMail、HotpotQA 和 Qulac 风格的长查询数据集上实现了高达 18 倍的 Token 缩减，同时保持了重建保真度和下游性能。这种简单而有效的设计支持包括提示词压缩和直接在 Z-token 空间中进行自回归生成在内的应用，为实现 Token 高效的长上下文推理提供了一条潜在路径。

## Abstract
In this paper, we establish the novel insight that an off-the-shelf LLM can function as an excellent token compressor and decompressor. To demonstrate, we design a self-expressive autoencoding learning framework fine-tunes a pretrained LLM to translate long texts into a compact internal language of discrete, variable-length latent codes, termed Z-tokens, and to reconstruct the original text exactly from them. The resulting representation is content-adaptive: semantically dense segments receive more Z-tokens, while redundant or predictable regions are aggressively compressed, via lightweight LoRA-based adapter heads. Empirically, our method achieves up to 18 times token reduction on Wikipedia, CNN/DailyMail, HotpotQA, and Qulac-style long-query datasets, while preserving reconstruction fidelity and downstream performance. This simple yet effective design supports applications including prompt compression and autoregressive generation directly in the Z-token space, offering a potential pathway toward token-efficient long-context reasoning.

---

## 论文详细总结（自动生成）

这篇论文提出了一种将大语言模型（LLM）同时作为 Token 压缩器和解压器的创新框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：LLM 在处理长上下文时面临注意力机制平方级复杂度的挑战，导致计算和内存开销巨大。
*   **核心问题**：现有的 Token 压缩方法（如启发式裁剪或固定长度的嵌入压缩）要么损失语义细节，要么缺乏灵活性和可解释性。
*   **核心见解**：LLM 本身具备强大的语义抽象能力（如理解成语、隐喻）。论文旨在利用这种能力，让 LLM 学习一种内部的、离散的、变长的“语义语言”（Z-tokens），实现高效的文本压缩与精确重构。

### 2. 方法论
*   **核心思想**：构建一个自表达的自编码框架，通过微调将输入文本 $X$ 映射为紧凑的离散 Token 序列 $Z$（Z-tokens），再由解压器还原或进行下游推理。
*   **关键技术细节**：
    *   **变长压缩**：不同于以往固定长度的压缩，该方法采用自回归方式生成 Z-tokens，语义复杂的片段分配更多 Token，简单的则更少。
    *   **离散化处理**：使用 **Gumbel-Softmax** 采样和 **直通估计器（STE）**，在保持离散性的同时允许梯度反向传播。
    *   **双路径设计**：
        1.  **推理路径**：直接从 Z-tokens 生成下游任务答案（如 QA）。
        2.  **重构路径**：先从 Z-tokens 还原原始文本，或在 Z-token 空间内进行纯符号推理。
    *   **滑动窗口压缩**：针对超长文本，采用带重叠的滑动窗口，并通过一致性约束（Cosine Similarity）确保语义连贯。
    *   **训练策略**：使用 **LoRA** 进行轻量化微调，并引入 **计划采样（Scheduled Sampling）** 来缓解自回归生成中的曝光偏差。

### 3. 实验设计
*   **数据集**：
    *   **重构与续写**：Wikipedia。
    *   **摘要生成**：CNN/DailyMail。
    *   **长文本问答（QA）**：HotpotQA、QuALITY、NarrativeQA、QASPER。
*   **基准模型（Backbone）**：Qwen3-0.4B、1.7B、4B。
*   **对比方法**：ICAE、Gist-Token、AutoCompressor、LLOCO 等主流压缩模型。
*   **评价指标**：BLEU-4（重构）、PPL（困惑度）、ROUGE（摘要）、F1/EM（问答）。

### 4. 资源与算力
*   **硬件环境**：使用了 6 台 NVIDIA GPU，具体包括 **2x A800-80G** 和 **4x V100-32G**。
*   **计算配置**：配备 Intel Xeon Gold 6348 CPU。
*   **软件环境**：Ubuntu 22.04, PyTorch 2.5.1, CUDA 12.4。
*   **训练时长**：文中未明确给出总训练小时数，但提到推理速度提升了 4.6 倍。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个大型数据集，跨越了重构、续写、摘要、多跳推理问答等多种任务。
*   **消融实验**：针对 Z-token 数量的影响、计划采样的有效性、滑动窗口的表现以及模型规模的扩展性进行了详细测试。
*   **客观性**：通过多次独立重复实验取平均值，并与多种 SOTA 方法在相同压缩预算下对比，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **高效压缩**：在保持高性能的同时，实现了高达 **18 倍** 的 Token 缩减。
*   **性能优异**：在重构任务中 BLEU-4 接近完美（>99），在下游 QA 任务中超越了现有的固定长度压缩方法。
*   **Z-token 的多义性**：发现 Z-tokens 并非简单的 1:1 映射，而是具有上下文相关性的抽象语义单元，表现出类似自然语言的“多义性”且受控。
*   **推理加速**：显著降低了内存占用，推理速度提升高达 2.1x 至 4.6x。

### 7. 优点与亮点
*   **内容自适应性**：变长压缩机制比固定比例压缩更符合语言逻辑。
*   **端到端集成**：将压缩任务内化为 LLM 的一种“翻译”行为，无需复杂的外部模块。
*   **可解释性探索**：对 Z-token 的语义一致性进行了定量分析，证明了其作为内部语言的合理性。
*   **支持 Z 空间推理**：模型可以直接在压缩后的符号空间“思考”，极大地扩展了长文本处理的潜力。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 4B 以下的中小规模模型，尚未验证在超大规模模型（如 70B+）上的表现。
*   **训练成本**：虽然使用了 LoRA，但仍需针对特定任务或通用重构进行微调，无法完全“开箱即用”。
*   **长程依赖挑战**：虽然有滑动窗口，但在处理极长文档时，如何实现跨窗口的全局 Z-token 协调仍有待优化。
*   **任务感知压缩**：目前的压缩相对通用，未来可能需要根据特定任务（如只关注事实或只关注逻辑）进行定制化压缩。

（完）
