---
title: "How Pruning Reshapes Features: Sparse Autoencoder Analysis of Weight-Pruned Language Models"
title_zh: 剪枝如何重塑特征：权重剪枝语言模型的稀疏自编码器分析
authors: "Hector Borobia, Elies Seguí-Mas, Guillermina Tormo-Carbó"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25325v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 权重剪枝对大语言模型内部表示影响的研究
tldr: 本研究利用稀疏自编码器（SAEs）系统分析了非结构化权重剪枝对大语言模型内部特征表示的影响。通过对Gemma和Llama系列模型在不同剪枝方法（量级剪枝和Wanda）及稀疏度下的实验，揭示了特征存活的规律。研究发现剪枝具有隐式的特征选择作用，且Wanda剪枝在保留特征结构上表现更优，为压缩模型的解释性提供了新见解。
selection_source: fresh_fetch
motivation: 尽管权重剪枝被广泛用于模型压缩，但其如何重塑模型内部学习到的特征几何结构仍缺乏深入理解。
method: 采用SAEs作为探测工具，在多种模型和稀疏度下对比研究了量级剪枝与Wanda剪枝对特征稳定性、存活率及因果相关性的影响。
result: 实验表明低频稀有特征比高频通用特征更易在剪枝中存活，且Wanda剪枝对特征结构的保护能力最高可达量级剪枝的3.7倍。
conclusion: 剪枝过程倾向于保留专业化特征而破坏通用特征，且特征的几何存活并不等同于其对模型输出的因果重要性。
---

## 摘要
权重剪枝是压缩大型语言模型的标准技术，但其对学习到的内部表示的影响仍不为人所知。我们利用稀疏自编码器（SAEs）作为可解释性探针，首次系统地研究了非结构化剪枝如何重塑语言模型的特征几何。在三个模型系列（Gemma 3 1B、Gemma 2 2B、Llama 3.2 1B）、两种剪枝方法（量级剪枝和 Wanda）以及六个稀疏度水平（0-60%）中，我们调查了涵盖种子稳定性、特征存活、SAE 可迁移性、特征脆弱性和因果相关性的五个研究问题。我们最引人注目的发现是，稀有的 SAE 特征（即激活率较低的特征）在剪枝后的存活情况远好于频繁特征，在 17 个实验条件中的 11 个中，条件内 Spearman 相关系数 rho = -1.0。这一违反直觉的结果表明，剪枝起到了隐式特征选择的作用，优先破坏高频通用特征，同时保留专门的稀有特征。我们进一步表明，Wanda 剪枝在保留特征结构方面比量级剪枝好 3.7 倍，预训练的 SAE 在稀疏度高达 50% 的 Wanda 剪枝模型上仍然有效，且几何特征存活并不能预测因果重要性——这种解耦对压缩下的可解释性具有重要意义。

## Abstract
Weight pruning is a standard technique for compressing large language models, yet its effect on learned internal representations remains poorly understood. We present the first systematic study of how unstructured pruning reshapes the feature geometry of language models, using Sparse Autoencoders (SAEs) as interpretability probes. Across three model families (Gemma 3 1B, Gemma 2 2B, Llama 3.2 1B), two pruning methods (magnitude and Wanda), and six sparsity levels (0--60%), we investigate five research questions spanning seed stability, feature survival, SAE transferability, feature fragility, and causal relevance. Our most striking finding is that rare SAE features--those with low firing rates--survive pruning far better than frequent ones, with within-condition Spearman correlations of rho = -1.0 in 11 of 17 experimental conditions. This counter-intuitive result suggests that pruning acts as implicit feature selection, preferentially destroying high-frequency generic features while preserving specialized rare ones. We further show that Wanda pruning preserves feature structure up to 3.7x better than magnitude pruning, that pre-trained SAEs remain viable on Wanda-pruned models up to 50% sparsity, and that geometric feature survival does not predict causal importance--a dissociation with implications for interpretability under compression.

---

## 论文详细总结（自动生成）

这篇论文《剪枝如何重塑特征：权重剪枝语言模型的稀疏自编码器分析》对大语言模型（LLM）在压缩过程中的内部机制提供了深入见解。以下是详细的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：权重剪枝（Weight Pruning）是压缩 LLM 的标准手段，但其对模型内部学习到的“特征几何结构”（Feature Geometry）有何影响？
*   **研究动机**：以往研究多关注剪枝对任务性能（如困惑度、准确率）的影响，而忽略了内部表示的变化。随着机械可解释性（Mechanistic Interpretability）的发展，利用稀疏自编码器（SAE）可以像显微镜一样观察剪枝如何破坏、保留或重塑模型特征。

### 2. 方法论
*   **核心思想**：通过在原始（稠密）模型和剪枝后的模型上分别训练 SAE，对比两者的特征字典，从而量化特征的演变。
*   **关键技术细节**：
    *   **SAE 架构**：采用 **TopK SAE**，将残差流激活向量分解为稀疏特征。
    *   **特征匹配指标**：使用**互近邻（MNN）率**、单向匹配率和贪婪 1-to-1 匹配，通过余弦相似度阈值（$\tau=0.7$）判断特征是否在剪枝后“存活”。
    *   **剪枝方法**：对比了**全局量级剪枝（Magnitude Pruning）**和**Wanda 剪枝**（考虑权重大小与激活范数的乘积）。
    *   **因果消融协议**：通过归零特定特征并测量输出分布的 KL 散度，评估特征的因果重要性。

### 3. 实验设计
*   **模型与数据集**：
    *   **模型**：Gemma 3 1B（主实验）、Gemma 2 2B、Llama 3.2 1B。
    *   **数据集**：使用 FineWeb-Edu（1.5M token）训练 SAE，WikiText-2 评估模型性能。
*   **实验场景**：设置了 0% 到 60% 的六个稀疏度梯度。
*   **对比基准**：以未剪枝的稠密模型作为 Baseline，对比不同剪枝算法在相同稀疏度下的特征保留能力。

### 4. 资源与算力
*   **算力说明**：文中**未明确指出**具体的 GPU 型号和数量。
*   **训练规模**：每个 SAE 训练 20,000 步，Batch Size 为 2,048–4,096。总共训练了约 80 个 SAE 实例（涵盖不同种子和实验条件）。
*   **开源贡献**：作者提供了 GitHub 仓库，包含可复现的 Colab 笔记本。

### 5. 实验数量与充分性
*   **实验规模**：共完成了 22 组成功的实验运行，每组包含 3-5 个随机种子以确保统计稳定性。
*   **充分性评估**：实验覆盖了三个不同的模型家族、两种主流剪枝方法以及广泛的稀疏度区间，并进行了跨模型的验证。
*   **客观性**：通过 MNN 等保守指标和因果消融实验，从几何和功能两个维度评估特征，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **稀有特征更易存活（核心发现）**：激活频率较低的“稀有特征”在剪枝后的存活率远高于高频特征。在多数条件下，激活率与存活率呈完美的负相关（$\rho = -1.0$）。
*   **Wanda 优于量级剪枝**：在相同稀疏度下，Wanda 保留特征结构的能力最高可达量级剪枝的 3.7 倍。
*   **SAE 的可迁移性**：在 Wanda 剪枝模型上，即使稀疏度达 50%，直接使用为稠密模型预训练的 SAE 仍能保持极高的重构质量（FVU 极低）。
*   **几何与因果的解耦**：几何上“存活”的特征并不比“消失”的特征具有更高的因果重要性，两者对模型输出的影响几乎一致。
*   **预测模型**：仅凭特征的激活率和模型稀疏度，即可通过逻辑回归有效预测特征的存活概率（AUC > 0.79）。

### 7. 优点
*   **视角独特**：首次系统性地将 SAE 应用于剪枝模型分析，填补了压缩模型可解释性的空白。
*   **发现深刻**：揭示了剪枝作为“隐式特征选择器”的本质——优先剔除通用高频特征，保留专门化稀有特征。
*   **实用性强**：为从业者提供了建议（如优先使用 Wanda、关注高频特征的流失），并证明了现有 SAE 工具在压缩模型上的持续有效性。

### 8. 不足与局限
*   **模型规模限制**：实验仅限于 1B-2B 参数的小型模型，在大规模模型（如 70B+）上的泛化性有待验证。
*   **上下文长度**：实验使用的上下文长度较短（256 token），可能无法捕捉长程依赖特征。
*   **剪枝方式单一**：仅研究了一次性（One-shot）剪枝，未涉及剪枝后的微调（Fine-tuning）过程，而后者在实际应用中非常普遍。
*   **数值稳定性**：在深层网络（如 Gemma 3 Layer 18）中出现了半精度浮点数溢出导致实验失败的情况。

（完）
