---
title: "Towards Video Anomaly Detection from Event Streams: A Baseline and Benchmark Datasets"
title_zh: 面向事件流视频异常检测：基准模型与基准数据集
authors: "Peng Wu, Yuting Yan, Guansong Pang, Yujia Sun, Qingsen Yan, Peng Wang, Yanning Zhang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24991v1"
tags: ["query:snn"]
score: 10.0
evidence: 基于事件的视觉与异常检测
tldr: 本研究旨在填补基于事件流的视频异常检测（VAD）领域的空白。针对缺乏专用数据集和有效建模策略的问题，作者构建了包含同步事件和RGB记录的基准数据集，并提出了EWAD框架。该框架通过事件密度感知动态采样、密度调制时间建模以及RGB到事件的知识蒸馏，充分利用事件相机的低冗余和动态捕捉特性。实验证明，EWAD在多个基准上显著优于现有方法，展示了事件驱动建模在异常检测中的巨大潜力。
selection_source: fresh_fetch
motivation: 尽管事件相机具有低冗余和隐私保护等优势，但由于缺乏专用数据集和建模策略，基于事件流的视频异常检测研究进展缓慢。
method: 提出了EWAD框架，包含动态采样策略、密度调制时间建模以及增强弱监督表征的RGB到事件知识蒸馏机制。
result: 在三个新构建的基准数据集上的实验表明，EWAD框架相比现有方法取得了显著的性能提升。
conclusion: 本研究成功建立了基于事件的视频异常检测研究方向，并证明了事件驱动建模在处理动态异常任务中的有效性。
---

## 摘要
基于事件的视觉具有低冗余、关注动态运动以及固有的隐私保护特性，自然地契合了视频异常检测（VAD）的需求。然而，缺乏专门的事件流异常检测数据集和有效的建模策略，显著阻碍了该领域的发展。在这项工作中，我们迈出了将基于事件的 VAD 确立为统一研究方向的关键一步。我们首先构建了多个基于事件流的视频异常检测基准，其特点是包含同步的事件和 RGB 录像。利用事件的独特属性，我们提出了一种以事件为中心的时空视频异常检测框架，即 EWAD，该框架具有三项关键创新：一种感知事件密度的动态采样策略，用于选择具有时间信息量的片段；一种密度调制的时序建模方法，用于从稀疏事件流中捕获上下文关系；以及一种 RGB 到事件的知识蒸馏机制，用于在弱监督下增强基于事件的表示。在三个基准上的广泛实验表明，我们的 EWAD 相比现有方法取得了显著改进，突显了事件驱动建模在视频异常检测中的潜力和有效性。基准数据集将公开发布。

## Abstract
Event-based vision, characterized by low redundancy, focus on dynamic motion, and inherent privacy-preserving properties, naturally fits the demands of video anomaly detection (VAD). However, the absence of dedicated event-stream anomaly detection datasets and effective modeling strategies has significantly hindered progress in this field. In this work, we take the first major step toward establishing event-based VAD as a unified research direction. We first construct multiple event-stream based benchmarks for video anomaly detection, featuring synchronized event and RGB recordings. Leveraging the unique properties of events, we then propose an EVent-centric spatiotemporal Video Anomaly Detection framework, namely EWAD, with three key innovations: an event density aware dynamic sampling strategy to select temporally informative segments; a density-modulated temporal modeling approach that captures contextual relations from sparse event streams; and an RGB-to-event knowledge distillation mechanism to enhance event-based representations under weak supervision. Extensive experiments on three benchmarks demonstrate that our EWAD achieves significant improvements over existing approaches, highlighting the potential and effectiveness of event-driven modeling for video anomaly detection. The benchmark datasets will be made publicly available.

---

## 论文详细总结（自动生成）

以下是对论文《Towards Video Anomaly Detection from Event Streams: A Baseline and Benchmark Datasets》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的基于 RGB 视频的异常检测（VAD）存在数据冗余高、延迟大、隐私保护弱等问题。而事件相机（Event Camera）具有高时间分辨率、低延迟、仅响应动态变化的特性，非常契合异常检测的需求。然而，该领域目前面临两个主要障碍：**缺乏大规模专用事件流 VAD 数据集**，以及**缺乏能有效处理异步、稀疏事件数据的建模策略**。
*   **整体含义**：本文旨在将基于事件的 VAD 确立为一个统一的研究方向。通过构建基准数据集并提出 EWAD 框架，填补了从 RGB 视觉向事件视觉迁移在异常检测任务上的空白。

### 2. 论文提出的方法论：EWAD 框架
EWAD（EVent-centric Video Anomaly Detection）是一个以事件为中心的时空异常检测框架，核心包含以下技术：
*   **自适应事件帧生成**：利用 `v2e` 模拟器将 RGB 视频转换为事件流，并提出一种基于中值事件计数的自适应策略来生成事件帧，平衡稀疏性与结构完整性，减少“重影”或模糊。
*   **事件密度感知动态采样（EDS）**：考虑到异常通常伴随剧烈的亮度变化（即高事件密度），该策略利用双区间核采样（Dual-interval Nucleus Sampling）优先选择高密度片段，同时保留部分低密度背景，提高模型对异常信号的敏感度。
*   **事件调制距离衰减注意力（EDA）**：在时间建模中引入事件密度和时间间隔。公式上，通过事件密度动态调整注意力权重，使模型能根据运动强度动态调整“时间感知”，增强长程依赖建模。
*   **RGB 到事件的知识蒸馏（KD）**：在训练阶段，利用预训练的 RGB 模型作为教师，通过二元分类逻辑（Binary Logit）和多类别逻辑（Multi-class Logit）蒸馏，将 RGB 领域的语义先验迁移给事件模型（学生），弥补事件数据语义信息不足的问题。
*   **空间异常定位**：利用事件激活的空间分布，通过简单的阈值和形态学操作实现无需额外训练的空间异常区域定位。

### 3. 实验设计
*   **数据集与场景**：
    *   通过模拟生成了三个大规模基准：**UCF-Crime**（城市街道、室内监控）、**CCTV-Fights**（真实打斗场景）和 **UBnormal**（合成场景，包含多种异常类型）。
    *   涵盖了射击、跌倒、打斗、交通事故等多种异常。
*   **对比方法**：
    *   **基于 SNN 的方法**：如 MSF、SFormer、ResSNN 等。
    *   **适配后的 RGB 方法**：将 DeepMIL、DMU、VadCLIP 等经典方法适配到事件帧特征上进行公平对比。
*   **评价指标**：使用帧级 AUC（曲线下面积）衡量检测性能，使用 TIoU（时间交并比）衡量空间定位精度。

### 4. 资源与算力
*   **训练参数**：使用 Adam 优化器，学习率为 $2 \times 10^{-5}$，Batch Size 为 128，训练 10 个 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，也未提及具体的训练总时长。但从其使用的 ViT 骨干网络和 Batch Size 来看，通常需要消费级或企业级 GPU（如 RTX 3090 或 A100）支持。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流数据集上进行了测试，涵盖了从 SNN 到 ViT 的多种架构对比。
*   **消融实验**：针对 EDS、EDA、B-KD、M-KD 四个核心组件分别进行了组合测试，验证了各模块的增量贡献。
*   **真实性验证**：为了弥补模拟数据的局限，作者在真实的 **EventVOT** 数据集上进行了验证，证明模型学习的是运动语义而非模拟伪影。
*   **客观性**：实验对比了多种不同架构的方法，且在相同特征（ViT）下与现有最强方法对比，结果具有较强的说服力和客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：EWAD 在所有基准上均达到 SOTA。在 UCF-Crime 上，AUC 达到 76.55%，比之前的事件方法 MSF 提升了 11.5%。
*   **事件数据的潜力**：实验证明，即使不依赖 RGB 输入（仅在训练时蒸馏），纯事件模型也能在异常检测中表现出色，且在空间定位上具有天然优势。
*   **知识蒸馏的必要性**：跨模态蒸馏显著提升了事件模型的判别能力，证明了 RGB 语义先验对稀疏事件流的补充作用。

### 7. 优点（亮点）
*   **首创性**：建立了第一个大规模、同步的 RGB-事件 VAD 基准，为后续研究提供了基础设施。
*   **针对性设计**：EDS 和 EDA 充分利用了事件相机的物理特性（密度与运动相关性），而非简单套用 RGB 模型。
*   **训练与推理分离**：推理时仅需事件流，保证了低功耗、低延迟和隐私保护的实际应用价值。

### 8. 不足与局限
*   **数据偏差风险**：主要基准是通过 `v2e` 模拟生成的，虽然经过了真实数据验证，但模拟数据与真实事件相机采集的噪声分布、动态范围仍可能存在差异。
*   **时间连续性**：可视化结果显示，检测到的异常片段在时间上不如 RGB 方法连续，存在波动。
*   **空间定位精度**：虽然实现了训练自由的空间定位，但 TIoU 指标仍低于最先进的 RGB 方法，说明纯事件流在精细语义理解上仍有提升空间。

（完）
