---
title: On Neural Scaling Laws for Weather Emulation through Continual Training
title_zh: 关于通过持续训练实现天气模拟的神经标度律研究
authors: "Shashank Subramanian, Alexander Kiefer, Arnur Nigmetov, Amir Gholami, Dmitriy Morozov, Michael W. Mahoney"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25687v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: Transformer架构的神经缩放定律
tldr: 本研究探讨了气象预报模型中的神经缩放法则。研究者采用通用的Swin Transformer架构，并结合恒定学习率与周期性冷却的持续训练策略，系统分析了模型、数据和计算规模对性能的影响。结果表明，该方法不仅遵循可预测的缩放趋势，且在多步预测和预测清晰度上优于传统方案。通过构建IsoFLOP曲线，研究确定了计算最优的训练方案，为气象大模型的资源分配提供了重要参考。
selection_source: fresh_fetch
motivation: 旨在探索科学机器学习（尤其是气象预测）中神经缩放法则的适用性，以优化大模型的资源分配和性能预测。
method: 采用可扩展的Swin Transformer架构，并结合恒定学习率与周期性冷却阶段的持续训练策略进行实验。
result: 模型展现出可预测的缩放趋势，且在多步预测任务中表现优异，同时通过IsoFLOP曲线确定了计算最优的训练制度。
conclusion: 神经缩放法则在气象模拟领域具有显著的诊断价值，能够有效指导高效的资源分配并揭示潜在的性能极限。
---

## 摘要
神经标度律在某些领域可以将大型神经网络的性能预测为模型、数据和计算规模的函数，是自然语言处理和计算机视觉领域构建基础模型的基石。我们研究了科学机器学习中的神经标度，重点关注天气预报模型。为了在尽可能简单的设置下分析标度行为，我们采用了一种极简、可扩展且通用的 Swin Transformer 架构，并使用带有恒定学习率和周期性冷却（cooldowns）的持续训练作为一种高效的训练策略。我们表明，以这种极简方式训练的模型遵循可预测的标度趋势，甚至优于标准的余弦学习率调度。冷却阶段可以被重新利用以提高下游性能，例如，通过频谱损失调整实现更长预测时效下的准确多步外推（rollouts）以及更清晰的预测。我们还在各种计算预算下系统地探索了广泛的模型和数据集规模，以构建 IsoFLOP 曲线，并确定了计算最优的训练方案。将这些趋势外推到更大规模揭示了潜在的性能极限，证明了神经标度可以作为高效资源分配的重要诊断工具。我们开源了代码以确保可复现性。

## Abstract
Neural scaling laws, which in some domains can predict the performance of large neural networks as a function of model, data, and compute scale, are the cornerstone of building foundation models in Natural Language Processing and Computer Vision. We study neural scaling in Scientific Machine Learning, focusing on models for weather forecasting. To analyze scaling behavior in as simple a setting as possible, we adopt a minimal, scalable, general-purpose Swin Transformer architecture, and we use continual training with constant learning rates and periodic cooldowns as an efficient training strategy. We show that models trained in this minimalist way follow predictable scaling trends and even outperform standard cosine learning rate schedules. Cooldown phases can be re-purposed to improve downstream performance, e.g., enabling accurate multi-step rollouts over longer forecast horizons as well as sharper predictions through spectral loss adjustments. We also systematically explore a wide range of model and dataset sizes under various compute budgets to construct IsoFLOP curves, and we identify compute-optimal training regimes. Extrapolating these trends to larger scales highlights potential performance limits, demonstrating that neural scaling can serve as an important diagnostic for efficient resource allocation. We open-source our code for reproducibility.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了气象预报领域中科学机器学习（SciML）模型的神经标度律（Neural Scaling Laws）。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在 NLP 和 CV 领域，神经标度律已成为构建大模型的基石，但在 SciML（尤其是天气模拟）领域，研究尚不充分。
*   **核心问题**：气象预报性能是否遵循可预测的标度趋势？在不依赖复杂领域特定架构的情况下，仅通过增加模型规模、数据量和计算量（即“苦涩的教训”）能达到什么效果？
*   **动机**：旨在通过极简架构和高效训练策略，系统化地识别计算最优（Compute-optimal）的训练方案，为气象大模型的资源分配提供理论指导。

### 2. 方法论
*   **极简架构**：采用标准的 **Swin Transformer** 作为骨干网络，去除了领域特定的修改（如分层结构、相对位置偏差等），仅保留窗口自注意力（W-MHSA）和 MLP，以减少架构干扰。
*   **持续训练策略**：
    *   **恒定学习率 + 周期性冷却（Cooldown）**：不同于传统的余弦退火（需从头训练），该方法在恒定 LR 下训练，仅在最后 5% 的迭代中快速降至零。这允许从同一检查点继续训练以探索更高的计算预算，极大地提高了实验效率。
*   **下游任务对齐**：
    *   利用冷却阶段重新引入 **多步自回归（AR）损失** 或 **频谱调整损失（AMSE）**，使预训练模型在不重新训练的情况下，获得更长的预测时效或更清晰的频谱特征。
*   **分布式并行**：实现了 **2D 空间并行（Spatial Parallelism）** 结合数据并行，解决了高分辨率气象数据带来的显存压力。

### 3. 实验设计
*   **数据集**：使用 **ERA5** 再分析数据集（0.25° 分辨率，约 30 万个样本），包含 71 个大气变量。
*   **实验设置**：
    *   训练年份：1979-2016；验证：2017；测试：2020。
    *   模型规模：从 3M 到 456M 参数，外推实验达到 1.3B。
    *   计算预算：跨越两个数量级（6E17 至 6E19 FLOPs）。
*   **Benchmark**：对比了金标准数值天气预报系统 **HRES** 和最先进的深度学习模型 **GraphCast**。
*   **评估指标**：面积加权均方根误差（RMSE）和功率谱密度（PSD）。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA **A100 GPU**（40GB 和 80GB 版本）。
*   **算力分配**：
    *   小模型使用 16 张 GPU 进行数据并行。
    *   大模型（如 456M）使用 64 张 GPU，结合 2x2 空间并行。
    *   进行 4 步 AR 冷却训练时，最多使用了 **256 张 GPU**。
*   **训练时长**：1.3B 参数模型在 64 张 GPU 上训练了约 27.2 万次迭代（约 13 个 Epoch）。

### 5. 实验数量与充分性
*   **实验规模**：构建了多条 **IsoFLOP 曲线**（等计算量曲线），每条曲线包含多个不同参数量和训练时长的组合。
*   **充分性**：实验覆盖了从百万级到十亿级的参数跨度，并进行了外推验证。通过拟合抛物线确定了每个预算下的最优模型大小，实验设计严谨且遵循了“Chinchilla”标度分析的范式，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **标度律存在**：气象模拟性能遵循幂律缩放，验证损失随计算量增加而稳定下降。
*   **冷却策略优越**：恒定 LR + 冷却的效果优于标准的余弦调度，且冷却阶段能有效对齐下游任务。
*   **计算最优关系**：得出最优样本数 $S^* \propto C^{0.41}$ 和最优模型大小 $N^* \propto C^{0.59}$。
*   **性能饱和**：外推至 1.3B 参数（2.25E21 FLOPs）时，模型出现过拟合和性能饱和。这表明在当前分辨率和数据量下，单纯增加模型规模可能面临边际效应递减，强调了扩展数据规模的重要性。

### 7. 优点
*   **方法简洁高效**：通过冷却阶段重新利用模型，避免了重复训练的巨大开销。
*   **解耦缩放与对齐**：证明了可以在简单 MSE 损失下研究缩放，而在冷却阶段解决复杂的科学任务对齐问题。
*   **工程实现强**：开源了支持空间并行的代码，解决了高分辨率 SciML 模型训练的瓶颈。

### 8. 不足与局限
*   **确定性限制**：目前仅关注确定性预测，未探讨气象中至关重要的概率预测（如扩散模型或集成预报）。
*   **架构单一**：主要基于 Swin Transformer，未对比神经算子（Neural Operators）或图神经网络（GNN）的标度行为。
*   **数据瓶颈**：实验显示在十亿参数规模下受到 ERA5 数据量和分辨率的限制，暗示未来需要更多样化的地球科学数据。

（完）
