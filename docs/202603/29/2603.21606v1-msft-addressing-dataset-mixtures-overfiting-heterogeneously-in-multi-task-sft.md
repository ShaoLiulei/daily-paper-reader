---
title: "mSFT: Addressing Dataset Mixtures Overfiting Heterogeneously in Multi-task SFT"
title_zh: mSFT：解决多任务 SFT 中混合数据集的异质性过拟合问题
authors: "Woosung Koh, Jeyoung Jeon, Youngjin Song, Yujin Cheon, Soowon Oh, Jaehyeong Choi, Se-Young Yun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21606v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 语言模型的多任务监督微调算法
tldr: 针对多任务监督微调（SFT）中不同子数据集学习动态不一致导致的过拟合问题，本文提出 mSFT 算法。该方法通过迭代识别并剔除过拟合任务，并回退至最优检查点继续训练，有效解决了传统统一计算预算下的性能瓶颈。实验证明 mSFT 在多个基准测试和模型上均优于基准方法，且能在提升性能的同时降低计算开销。
selection_source: fresh_fetch
motivation: 传统多任务 SFT 采用统一计算预算，导致学习较快的任务过早过拟合，而学习较慢的任务仍欠拟合。
method: 提出一种迭代式过拟合感知搜索算法，通过动态识别并排除已过拟合的子数据集，并回退到该任务的最优检查点继续训练。
result: 在 10 个基准测试和 6 个基础模型上的评估显示，mSFT 性能一致优于 4 种基准方法，且对超参数不敏感。
conclusion: mSFT 为多任务 SFT 提供了一种实用的过拟合感知方案，能最大化模型在多样化数据混合中的潜力并优化计算效率。
---

## 摘要
目前的语言模型训练通常在所有子数据集上使用统一的计算预算进行多任务有监督微调（SFT）。这种方法从根本上说是次优的：异质的学习动态导致学习较快的任务过早过拟合，而学习较慢的任务仍处于欠拟合状态。为了解决这一问题，我们引入了 mSFT，这是一种针对多任务混合数据的迭代式、过拟合感知的搜索算法。mSFT 在活跃的混合数据上训练模型，识别并排除最早过拟合的子数据集，并在继续训练前回退到该特定的最优检查点。广泛的评估表明，mSFT 在 10 个基准测试和 6 个基础模型上始终优于 4 个基准方法。进一步的分析证实，mSFT 在不同的数据集规模、任务粒度下均能保持稳健的增益，且对其唯一的新超参数（计算预算）不敏感。值得注意的是，在低计算预算下，mSFT 可以在降低训练 FLOPs 的同时提高性能。最终，mSFT 为多任务 SFT 建立了一种实用的过拟合感知算法，能够最大限度地发挥模型在各种混合数据上的潜力。

## Abstract
Current language model training commonly applies multi-task Supervised Fine-Tuning (SFT) using a homogeneous compute budget across all sub-datasets. This approach is fundamentally sub-optimal: heterogeneous learning dynamics cause faster-learning tasks to overfit early while slower ones remain under-fitted. To address this, we introduce mSFT, an iterative, overfitting-aware search algorithm for multi-task data mixtures. mSFT trains the model on an active mixture, identifies and excludes the earliest overfitting sub-dataset, and reverts to that specific optimal checkpoint before continuing. Extensive evaluations demonstrate that mSFT consistently outperforms 4 baselines across 10 benchmarks and 6 base models. Further analysis confirms mSFT maintains robust gains across diverse dataset sizes, task granularities, and is insensitive to its single new hyperparameter (compute budget). Notably, at low compute budget, mSFT can improve performance while lowering training FLOPs. Ultimately, mSFT establishes a practical overfitting-aware algorithm for multi-task SFT that maximizes the potential of models across diverse data mixtures.

---

## 论文详细总结（自动生成）

### 论文总结：mSFT —— 解决多任务 SFT 中的异质性过拟合问题

#### 1. 核心问题与整体含义（研究动机和背景）
在大型语言模型（LLM）的训练中，多任务监督微调（SFT）通常对所有子数据集采用**统一的计算预算**（如相同的 Epoch 数）。然而，论文指出这种做法是次优的，因为不同的任务具有**异质的学习动态**：
*   **学习较快**的任务在统一训练结束前就已发生过拟合。
*   **学习较慢**的任务在此时仍处于欠拟合状态。
论文旨在打破这种“一刀切”的训练范式，通过动态调整每个任务的训练量，最大化模型在多任务混合数据中的泛化能力。

#### 2. 方法论：核心思想与算法流程
论文提出了 **mSFT**（multi-task mixture SFT），这是一种迭代式的、过拟合感知的搜索与训练算法。其核心流程如下：
*   **初始化**：设定活跃数据集混合体、基础模型及计算预算步长 $C$（如 1/4 个 Epoch）。
*   **迭代试运行（Roll-out）**：在当前的活跃数据集混合体上训练模型，并记录每个子任务在验证集上的性能曲线。
*   **识别与回退（Roll-back）**：
    1.  识别出在当前阶段**最早达到性能峰值（即开始过拟合）**的子任务 $D_{exclude}$。
    2.  将模型状态**回退**到该任务达到最优性能时的特定检查点（Checkpoint）。
    3.  将该任务从活跃混合体中剔除，加入排除集 $E$。
*   **循环执行**：使用回退后的模型和缩减后的数据集混合体重复上述过程，直到所有任务都被处理或排除。
*   **关键细节**：该方法避免了简单“一次性搜索”的误差，因为排除一个任务会改变剩余任务的梯度方向和优化轨迹。

#### 3. 实验设计
*   **基础模型**：涵盖了不同规模和家族的 6 个模型，包括 OLMo 2 (1B)、Qwen2.5 (0.5B, 1.5B, 3B, 7B) 和 Qwen3 (8B)。
*   **数据集与基准测试**：使用了 10 个主流基准测试，涵盖科学知识（ARC-Easy, SciQ, MedMCQA）、常识与语言（BoolQ, HellaSwag, Winogrande, OpenBookQA, CommonsenseQA）以及数学推理（GSM8K, AQUA-RAT）。
*   **对比方法（Baselines）**：
    1.  **Standard SFT**：标准的统一训练。
    2.  **Continual SFT**：顺序微调每个任务。
    3.  **DynamixSFT**：基于多臂老虎机的动态混合比例优化。
    4.  **IES (Instance-dependent Early Stopping)**：基于实例级二阶导数的早停法。
    5.  **消融实验**：SRO SFT（单次搜索）和 Soft SRO SFT（软性比例调整）。

#### 4. 资源与算力
*   **硬件设备**：实验使用了 NVIDIA **B200、H200、RTX A5000 和 RTX 3090** GPU。
*   **算力分析**：论文特别指出，SFT 阶段的计算量仅占模型全生命周期（预训练+后训练）总 FLOPs 的约 **0.01%**。
*   **训练时长**：未明确给出总小时数，但通过 FLOPs 分析证明，在低计算预算 $C=1$ 时，mSFT 甚至能比标准 SFT 节省约 120.3 PFLOPs 的计算开销。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个模型和 10 个基准测试上进行了详尽测试，总计获得了数十组核心对比数据。
*   **充分性**：
    *   进行了**鲁棒性分析**：验证了在不同数据集规模（9K 到 27K）和任务数量（5 到 15 个）下的稳定性。
    *   进行了**粒度分析**：在 MedMCQA 的 21 个细分子类别上验证了算法在极细粒度任务下的有效性。
    *   进行了**消融实验**：对比了单次搜索与迭代搜索的差异。
*   **客观性**：通过标准差分析证明了性能提升并非来自个别任务的突增，而是整体性能的稳健提升。

#### 6. 主要结论与发现
*   **性能领先**：mSFT 在所有 6 个模型上的平均表现均优于所有基准方法，尤其在数学推理（+3.0%）和常识语言（+2.4%）领域提升显著。
*   **计算效率**：mSFT 证明了在 SFT 阶段增加少量的搜索开销是值得的，且在特定配置下能实现“性能提升+算力节省”的双赢。
*   **缓解梯度冲突**：通过训练损失曲线发现，剔除已过拟合的任务能显著降低剩余任务的训练损失，缓解了多任务学习中的梯度冲突。
*   **超参数不敏感**：算法对唯一的超参数 $C$（计算预算步长）不敏感，具有很强的实用性。

#### 7. 优点
*   **切中痛点**：首次系统性地揭示并解决了多任务 SFT 中“异质过拟合”这一被长期忽视的问题。
*   **算法简洁**：迭代回退逻辑清晰，易于在现有的训练框架中实现。
*   **普适性强**：实验跨越了从 0.5B 到 8B 的多种模型架构，证明了方法的通用性。
*   **效率优化**：提出了配套的检查点剪枝算法（Checkpoint Pruning），有效控制了存储开销。

#### 8. 不足与局限
*   **存储开销**：尽管有剪枝算法，但在搜索过程中仍需保存多个中间检查点，平均磁盘占用约为模型大小的 4.44 倍，对存储空间有一定要求。
*   **评估依赖**：算法高度依赖于验证集的质量和评估频率。如果验证集无法准确反映泛化能力，回退点可能失效。
*   **顺序性限制**：迭代搜索过程具有顺序性，虽然 SFT 计算量小，但在超大规模数据集上，多次回退可能会增加整体训练的墙钟时间（Wall-clock time）。

（完）
