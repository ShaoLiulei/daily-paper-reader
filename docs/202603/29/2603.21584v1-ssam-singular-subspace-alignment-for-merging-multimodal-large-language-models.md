---
title: "SSAM: Singular Subspace Alignment for Merging Multimodal Large Language Models"
title_zh: SSAM：用于合并多模态大语言模型的奇异子空间对齐
authors: "Md Kaykobad Reza, Ameya Patil, Edward Ayrapetian, M. Salman Asif"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21584v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 多模态大语言模型合并
tldr: 本研究针对多模态大模型（MLLM）训练成本高昂的问题，提出了一种名为 SSAM 的无训练模型合并框架。该方法旨在将多个独立训练的专家级 MLLM（如视觉-语言和音频-语言模型）统一为一个多模态模型。SSAM 通过识别语言相关参数更新的共享低秩子空间，并在该空间内进行对齐与合并，有效解决了参数干扰问题。实验表明，SSAM 在无需任何多模态训练数据的情况下，在多个基准测试中达到了 SOTA 性能，证明了参数空间对齐在模型扩展中的高效性。
selection_source: fresh_fetch
motivation: 针对多模态大模型训练资源消耗大以及不同模态专家模型合并时存在的参数干扰和表示差异挑战。
method: 提出 SSAM 框架，通过在共享低秩子空间中对齐并合并语言相关的参数更新，实现无训练的多模态模型集成。
result: 在四个数据集上均取得 SOTA 表现，优于现有的无训练合并方法及部分联合训练的多模态模型。
conclusion: 证明了在参数空间进行模型对齐是构建多模态模型的一种资源高效且极具扩展性的新途径。
---

## 摘要
多模态大语言模型（MLLMs）通过联合处理来自视觉、音频和语言等多种模态的输入，实现了强大的性能。然而，构建此类模型或将其扩展到新模态通常需要大规模的配对数据集和大量的计算资源。鉴于许多预训练的 MLLMs（例如视觉-语言或音频-语言模型）已经公开可用，我们探究是否可以将它们合并为一个能够处理多种模态的单一 MLLM？合并具有不同输入模态的 MLLMs 仍然具有挑战性，部分原因是学习到的表示存在差异以及它们参数空间之间的干扰。为了应对这些挑战，我们提出了奇异子空间对齐与合并（SSAM），这是一种无需训练的模型合并框架，它将独立训练的专家级 MLLMs 统一为一个能够处理任何输入模态组合的单一模型。SSAM 分别保留特定模态的参数更新，并为语言相关的参数更新识别出一个共享的低秩子空间，在该子空间内对齐它们并进行合并，从而在最小化参数干扰的同时保留互补知识。在不使用任何多模态训练数据的情况下，SSAM 在四个数据集上均达到了最先进的性能，超越了先前的无需训练的合并方法，甚至优于联合训练的多模态模型。这些结果表明，在参数空间中对齐模型为传统的联合多模态训练提供了一种可扩展且资源高效的替代方案。

## Abstract
Multimodal large language models (MLLMs) achieve strong performance by jointly processing inputs from multiple modalities, such as vision, audio, and language. However, building such models or extending them to new modalities often requires large paired datasets and substantial computational resources. Since many pretrained MLLMs (e.g., vision-language or audio-language) are publicly available, we ask whether we can merge them into a single MLLM that can handle multiple modalities? Merging MLLMs with different input modalities remains challenging, partly because of differences in the learned representations and interference between their parameter spaces. To address these challenges, we propose Singular Subspace Alignment and Merging (SSAM), a training-free model merging framework that unifies independently trained specialist MLLMs into a single model capable of handling any combination of input modalities. SSAM maintains modality-specific parameter updates separately and identifies a shared low-rank subspace for language-related parameter updates, aligns them within this subspace, and merges them to preserve complementary knowledge while minimizing parameter interference. Without using any multimodal training data, SSAM achieves state-of-the-art performance across four datasets, surpassing prior training-free merging methods and even jointly trained multimodal models. These results demonstrate that aligning models in parameter space provides a scalable and resource-efficient alternative to conventional joint multimodal training.

---

## 论文详细总结（自动生成）

以下是对论文《SSAM: Singular Subspace Alignment for Merging Multimodal Large Language Models》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是如何**高效且低成本地构建能够处理多种模态（如视觉、音频、点云、语言）的统一大模型**。
*   **研究动机**：现有的多模态大语言模型（MLLM）通常针对特定模态对（如视觉-语言）进行训练。若要构建全模态模型，传统的“联合训练”方法需要海量的多模态配对数据和巨大的计算资源。
*   **核心挑战**：直接合并多个预训练的专家模型（如合并一个视觉专家和一个音频专家）会遇到**参数干扰（Parameter Interference）**和**表示空间不一致**的问题，导致合并后的模型性能大幅下降。

### 2. 方法论：SSAM 框架
SSAM（Singular Subspace Alignment and Merging，奇异子空间对齐与合并）是一种**无需训练（Training-free）**的合并方案。
*   **核心思想**：利用专家模型参数更新的低秩特性，识别出一个共享的“共识子空间”，在该空间内对齐不同模型的语言参数更新，从而过滤冲突并保留互补知识。
*   **关键技术步骤**：
    1.  **参数解耦**：将模型分为模态特定参数（编码器/投影器）和语言特定参数（解码器）。合并时保留各自的模态参数，仅处理语言解码器参数。
    2.  **计算语言向量（Language Vectors）**：定义 $\Delta_i = W_i - W_0$，即专家模型相对于原始预训练权重的参数偏移。
    3.  **构建共享共识子空间**：通过对所有专家模型的语言向量进行协方差分析，利用**奇异值分解（SVD）**提取前 $k$ 个主成分方向，形成共享的左、右基底（$U_c$ 和 $V_c$）。
    4.  **子空间投影与合并**：将每个专家的语言向量投影到这个共享子空间中进行对齐，消除噪声和不一致的更新方向，最后进行加权合并。
    5.  **低秩重构**：对于使用 LoRA 训练的模型，SSAM 可以直接在低秩矩阵形式下操作，保持参数效率。

### 3. 实验设计
*   **数据集与场景**：
    *   **AVQA（音视频问答）**：使用 MUSIC-AVQA 和 AVQA 数据集，评估模型在图像、音频、视频组合下的推理能力。
    *   **MCUB（多模态共性理解）**：使用 MCUB-3 和 MCUB-4，测试模型在未见过的模态组合（如图像+音频+点云）下识别共同语义的能力。
    *   **领域特定基准**：MMLU（语言理解）、OCR-Bench（视觉文本）、MMAU（音频理解），用于验证合并是否损害了原有的专家能力。
*   **对比方法（Baselines）**：
    *   **联合训练模型**：如 ImageBind-LLM、OneLLM、X-InstructBLIP 等。
    *   **无训练合并方法**：Task Arithmetic、TIES-Merging、WUDI、OptMerge、DAMC 等。

### 4. 资源与算力
*   **算力说明**：论文强调该方法是 **Training-free（无需训练）** 的。这意味着它不需要昂贵的 GPU 集群进行梯度下降优化。
*   **模型基础**：实验基于 Vicuna-7B-v1.5 语言解码器，配合 CLIP（视觉）、BEATs（音频）、LanguageBind（视频）和 PointLLM（点云）编码器。
*   **具体消耗**：文中未详细列出合并过程的具体耗时，但由于仅涉及 SVD 分解和矩阵乘法，其计算开销远小于任何形式的微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主要多模态数据集和 3 个领域基准，进行了大量的定量对比。
*   **消融实验**：详细探讨了子空间秩 $k$ 的选择（测试了 64 到 512 的范围）以及不同合并策略的对比。
*   **客观性与公平性**：对比了当前最先进的合并算法，并使用了相同的专家模型权重作为基础，确保了对比的公平性。实验不仅关注多模态性能，还检查了语言能力的保留情况，设计较为全面。

### 6. 主要结论与发现
*   **性能领先**：SSAM 在所有测试的多模态数据集上均达到了 SOTA 性能，显著优于现有的合并方法。
*   **超越联合训练**：在无需任何配对数据的情况下，SSAM 的表现甚至超过了一些在海量数据上联合训练的模型（如在 AVQA 上比 Proj-Only 高出约 14%）。
*   **跨模态泛化**：合并后的模型展现出了“涌现”能力，能够处理专家模型在训练时从未见过的模态组合（如音频+点云）。
*   **能力保留**：在 MMLU 等单模态测试中，SSAM 几乎没有性能退化，甚至略有提升。

### 7. 优点
*   **极高效率**：无需训练数据，无需反向传播，极大地降低了多模态模型的构建门槛。
*   **数学解释性强**：通过 SVD 和子空间投影解决参数干扰问题，比简单的权重平均更具理论支撑。
*   **扩展性好**：可以轻松地将新模态专家集成到现有模型中，而无需重新训练整个系统。

### 8. 不足与局限
*   **架构一致性要求**：要求所有待合并的专家模型必须共享相同的语言解码器架构（如都是 Vicuna-7B）。
*   **超参数依赖**：子空间秩 $k$ 的选择对性能有一定影响，虽然实验给出了推荐值（$k=128$），但在不同规模的模型上可能需要重新调整。
*   **安全性与对齐风险**：论文未深入探讨合并过程是否会引入新的偏见或安全风险，这在模型合并领域是一个潜在的隐患。

（完）
