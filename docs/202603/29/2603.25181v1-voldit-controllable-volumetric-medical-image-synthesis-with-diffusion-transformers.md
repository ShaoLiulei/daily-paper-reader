---
title: "VolDiT: Controllable Volumetric Medical Image Synthesis with Diffusion Transformers"
title_zh: VolDiT：基于扩散 Transformer 的可控体素医学图像合成
authors: "Marvin Seyfarth, Salman Ul Hassan Dar, Yannik Frisch, Philipp Wild, Norbert Frey, Florian André, Sandy Engelhardt"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25181v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于体积合成的扩散Transformer
tldr: 本研究针对现有3D医学图像合成中U-Net架构存在的局部偏差和感受野受限问题，提出了VolDiT，这是首个纯基于Transformer的3D扩散模型。该模型通过体积补丁嵌入和全局自注意力机制直接处理3D数据，并引入时间步门控控制适配器，利用分割掩码实现精确的空间引导。实验证明，VolDiT在生成图像的全局一致性、保真度及可控性上均优于传统的U-Net架构，为体积医学图像合成提供了更灵活的基础。
selection_source: fresh_fetch
motivation: 传统的基于U-Net的3D扩散模型受限于局部偏差和有限的感受野，难以捕捉全局上下文并实现灵活的可控生成。
method: 提出VolDiT，首个纯Transformer架构的3D扩散模型，通过体积补丁嵌入和时间步门控控制适配器实现高保真、可控的医学图像合成。
result: 在高分辨率3D医学图像合成任务中，VolDiT在全局一致性、生成保真度和可控性方面均优于现有的基于U-Net的潜扩散模型。
conclusion: 纯Transformer架构为体积医学图像合成提供了更具灵活性和扩展性的基础，是该领域的一个重要进展。
---

## 摘要
扩散模型已成为高保真医学图像合成的主流方法。然而，大多数现有的 3D 医学图像生成方法在潜扩散框架中依赖于卷积 U-Net 骨干网络。尽管这些架构有效，但它们引入了强烈的局部偏差和有限的感受野，这可能会限制可扩展性、全局上下文整合以及灵活的条件控制。在这项工作中，我们介绍了 VolDiT，这是首个纯基于 Transformer 的 3D 扩散 Transformer（Diffusion Transformer），用于体素医学图像合成。我们的方法通过体素补丁嵌入和直接在 3D token 上运行的全局自注意力机制，将扩散 Transformer 扩展到原生 3D 数据。为了实现结构化控制，我们提出了一种时间步门控控制适配器，将分割掩码映射为可学习的控制 token，在去噪过程中调节 Transformer 层。这种 token 级别的条件机制在保留 Transformer 架构建模优势的同时，实现了精确的空间引导。我们在高分辨率 3D 医学图像合成任务上评估了我们的模型，并将其与基于 U-Net 的最先进 3D 潜扩散模型进行了比较。结果表明，该模型在全局一致性、生成保真度和可控性方面均有提升。我们的研究结果表明，全 Transformer 扩散模型为体素医学图像合成提供了一个灵活的基础。在公开数据上训练的代码和模型可在 https://github.com/Cardio-AI/voldit 获取。

## Abstract
Diffusion models have become a leading approach for high-fidelity medical image synthesis. However, most existing methods for 3D medical image generation rely on convolutional U-Net backbones within latent diffusion frameworks. While effective, these architectures impose strong locality biases and limited receptive fields, which may constrain scalability, global context integration, and flexible conditioning. In this work, we introduce VolDiT, the first purely transformer-based 3D Diffusion Transformer for volumetric medical image synthesis. Our approach extends diffusion transformers to native 3D data through volumetric patch embeddings and global self-attention operating directly over 3D tokens. To enable structured control, we propose a timestep-gated control adapter that maps segmentation masks into learnable control tokens that modulate transformer layers during denoising. This token-level conditioning mechanism allows precise spatial guidance while preserving the modeling advantages of transformer architectures. We evaluate our model on high-resolution 3D medical image synthesis tasks and compare it to state-of-the-art 3D latent diffusion models based on U-Nets. Results demonstrate improved global coherence, superior generative fidelity, and enhanced controllability. Our findings suggest that fully transformerbased diffusion models provide a flexible foundation for volumetric medical image synthesis. The code and models trained on public data are available at https://github.com/Cardio-AI/voldit.

---

## 论文详细总结（自动生成）

### VolDiT：基于扩散 Transformer 的可控体素医学图像合成论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 医学图像合成方法大多依赖于卷积 U-Net 架构。尽管有效，但卷积操作存在强烈的**局部偏差（Locality Bias）**和**有限的感受野**，难以捕捉复杂的全局解剖结构，且在处理需要跨切片高度一致性的体积数据时，扩展性受到限制。
*   **研究背景**：在 2D 计算机视觉领域，扩散 Transformer（DiT）已展现出优于 U-Net 的扩展性和生成质量。本研究旨在探索纯 Transformer 架构是否能取代 3D U-Net，成为体积医学图像合成的新基石，并实现精确的空间可控生成。

#### 2. 方法论
*   **核心思想**：提出 **VolDiT**，这是首个完全基于 Transformer 的 3D 潜扩散模型（Latent Diffusion Model），直接在 3D 潜空间 token 上进行全局自注意力建模。
*   **关键技术细节**：
    *   **潜体积表示**：使用 3D VQ-GAN 将原始医学体积压缩为紧凑的潜张量（压缩率为 8x8x8），所有扩散过程均在潜空间进行。
    *   **3D 补丁化（Patchification）**：将 3D 潜张量划分为非重叠的立方补丁（如 $p \times p \times p$），通过 3D 卷积将其映射为 token 序列，并加入 3D 正余弦位置编码。
    *   **3D DiT 骨干网络**：采用纯 Transformer 块序列，利用全局自注意力处理所有空间位置的 token。
    *   **时间步门控控制适配器（TGCA）**：为了引入空间条件（如分割掩码），设计了一个轻量级适配器。它通过 3D 卷积提取特征并映射为控制 token。关键创新在于引入了**时间步相关的门控机制** $\gamma(t) = \sigma(MLP(t))$，动态调节每个扩散步骤中控制信号的强度，从而在不破坏预训练权重的前提下实现稳定、精确的解剖引导。

#### 3. 实验设计
*   **数据集**：
    *   **LUNA16**：公共肺部 CT 数据集（888 个体积），分辨率处理为 $512 \times 512 \times 256$。
    *   **TaviCT**：内部心脏 CTA 数据集（1002 个体积），分辨率处理为 $192 \times 192 \times 192$。
*   **基准模型（Benchmarks）**：
    *   **U-Net LDM**：最先进的基于 U-Net 的潜扩散模型。
    *   **HA-GAN**：一种非扩散的层次化对抗生成网络，作为 GAN 类方法的对比。
*   **评估指标**：FID（生成保真度）、Precision/Recall（精确度/召回率）、Density/Coverage（密度/覆盖度）、MS-SSIM（多样性）、Dice 系数和 95% 豪斯多夫距离（HD95，用于评估条件生成的解剖对齐度）。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **开源情况**：作者提供了 GitHub 代码库（https://github.com/Cardio-AI/voldit），通常此类 3D 任务需要高性能显存（如 A100 或 H100）来处理体积数据。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了**无条件生成**对比实验（VolDiT vs. U-Net LDM vs. HA-GAN）。
    *   进行了**模型缩放（Scaling）**实验，涵盖了从 XS（17.2M 参数）到 L（580M 参数）的不同模型规模。
    *   进行了**补丁大小（Patch Size）**的消融研究（$p=2$ vs. $p=4$）。
    *   进行了**有条件生成**实验，对比了不同控制强度（固定强度 vs. 学习门控）。
*   **充分性评价**：实验设计较为全面，涵盖了质量、多样性、可控性和扩展性等多个维度。使用了公共和内部两个数据集，增强了结果的可信度。

#### 6. 主要结论与发现
*   **性能优越性**：VolDiT 在 FID 和多样性指标上均优于基于 U-Net 的 LDM，尤其在心脏 TaviCT 数据集上，FID 从 36.8 显著降至 21.4。
*   **全局一致性**：得益于全局自注意力，VolDiT 生成的解剖结构比 U-Net 更具连贯性，减少了局部伪影。
*   **可控性增强**：提出的 TGCA 门控适配器在保持生成保真度的同时，实现了极高的解剖对齐精度（Dice 分数显著提升），证明了在 token 空间进行自适应调节的有效性。
*   **缩放特性**：在医学小数据集上，模型并非简单地“越大越好”，较小的 DiT 模型（如 XS）已能表现出极强的竞争力。

#### 7. 优点
*   **架构创新**：首次将纯 Transformer 扩散模型应用于原生 3D 医学体积合成，打破了 U-Net 的局限。
*   **控制机制灵活**：TGCA 适配器设计巧妙，通过时间步门控解决了条件引导与生成先验之间的平衡问题。
*   **解剖保真度高**：实验证明该模型在处理复杂解剖结构（如心脏、主动脉）时具有更好的全局逻辑。

#### 8. 不足与局限
*   **数据集规模限制**：由于医学数据集相对较小，论文未观察到像 2D 自然图像领域那样严格的单调缩放定律（即模型越大效果越好）。
*   **模态覆盖**：主要集中在 CT 图像，尚未在 MRI 或超声等其他 3D 模态上进行广泛验证。
*   **计算开销**：虽然 Transformer 具有扩展性，但 3D 全局自注意力的计算复杂度较高，对显存和算力的要求可能限制其在超高分辨率数据上的直接应用。

（完）
