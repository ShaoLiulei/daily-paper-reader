---
title: "PERMA: Benchmarking Personalized Memory Agents via Event-Driven Preference and Realistic Task Environments"
title_zh: PERMA：通过事件驱动偏好和真实任务环境评估个性化记忆智能体
authors: "Shuochen Liu, Junyi Zhu, Long Shu, Junda Lin, Yuhao Chen, Haotian Zhang, Chao Zhang, Derong Xu, Jia Li, Bo Tang, Zhiyu Li, Feiyu Xiong, Enhong Chen, Tong Xu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23231v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型长期记忆与个性化的基准测试
tldr: 针对大语言模型在长期记忆和个性化适配方面的不足，本文提出了PERMA基准。不同于以往仅关注简单信息检索的评估，PERMA强调用户偏好在真实噪声环境下的动态演变。该基准通过模拟跨领域、跨会话的交互事件，并引入文本变异性和语言风格对齐，全面考察智能体在时间维度上的角色一致性。实验表明，尽管现有记忆系统有所进步，但在应对长时跨度和跨领域干扰时仍显乏力。
selection_source: fresh_fetch
motivation: 现有评估方法将个性化简化为简单的信息检索，忽视了用户偏好随时间演变及在噪声环境下的动态积累过程。
method: 提出PERMA基准，通过模拟具有文本变异性和语言对齐特征的跨领域、跨会话交互事件，评估模型在动态时间线上的角色一致性。
result: 实验显示先进记忆系统能更精准提取偏好并降低成本，但在处理长跨度时间深度和跨领域干扰时仍面临巨大挑战。
conclusion: PERMA揭示了当前智能体在维护长期连贯人格方面的局限性，强调了开发更鲁棒的个性化记忆管理系统的必要性。
---

## 摘要
为大语言模型赋予长期记忆能力，对于构建能够适应用户不断变化需求的智能体至关重要。然而，先前的评估通常将偏好相关的对话与无关对话交织在一起，将任务简化为“大海捞针”式的检索，而忽略了驱动用户偏好演变的事件之间的关系。这种设置忽略了现实世界个性化的一个基本特征：偏好是在噪声背景下的多次交互中逐渐显现并累积的。为了弥补这一差距，我们推出了 PERMA，这是一个旨在评估随时间推移的人格一致性（而非仅仅是静态偏好召回）的基准测试。此外，我们结合了（1）文本变异性和（2）语言对齐，以模拟现实数据中不稳定的用户输入和个人习惯用语。PERMA 由跨越多个会话和领域的按时间排序的交互事件组成，并随时间插入偏好相关的查询。我们设计了多项选择和交互式任务，以探测模型在交互时间线上对人格的理解。实验表明，通过链接相关的交互，先进的记忆系统可以提取更精确的偏好并减少 Token 消耗，优于传统的原始对话语义检索。尽管如此，它们在跨越时间深度和跨领域干扰时仍难以维持连贯的人格，这凸显了智能体中需要更稳健的个性化记忆管理。我们的代码和数据已在 https://github.com/PolarisLiu1/PERMA 开源。

## Abstract
Empowering large language models with long-term memory is crucial for building agents that adapt to users' evolving needs. However, prior evaluations typically interleave preference-related dialogues with irrelevant conversations, reducing the task to needle-in-a-haystack retrieval while ignoring relationships between events that drive the evolution of user preferences. Such settings overlook a fundamental characteristic of real-world personalization: preferences emerge gradually and accumulate across interactions within noisy contexts. To bridge this gap, we introduce PERMA, a benchmark designed to evaluate persona consistency over time beyond static preference recall. Additionally, we incorporate (1) text variability and (2) linguistic alignment to simulate erratic user inputs and individual idiolects in real-world data. PERMA consists of temporally ordered interaction events spanning multiple sessions and domains, with preference-related queries inserted over time. We design both multiple-choice and interactive tasks to probe the model's understanding of persona along the interaction timeline. Experiments demonstrate that by linking related interactions, advanced memory systems can extract more precise preferences and reduce token consumption, outperforming traditional semantic retrieval of raw dialogues. Nevertheless, they still struggle to maintain a coherent persona across temporal depth and cross-domain interference, highlighting the need for more robust personalized memory management in agents. Our code and data are open-sourced at https://github.com/PolarisLiu1/PERMA.

---

## 论文详细总结（自动生成）

### 论文总结：PERMA - 评估个性化记忆智能体的事件驱动基准测试

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）在构建个性化智能体时，长期记忆是维持人格（Persona）连贯性的关键。然而，现有的评估框架（如“大海捞针”测试）存在三大局限：
*   **偏好静态化**：将用户偏好视为预定义的静态陈述，忽略了偏好在交互中逐渐显现和演变的动态过程。
*   **缺乏现实噪声**：忽略了现实中用户输入的模糊性、意图跳转及个人语言风格。
*   **评估维度单一**：通常只关注最终输出的准确性，未能将记忆检索质量与生成推理能力解耦。
**PERMA** 旨在通过模拟真实的、事件驱动的交互环境，评估智能体在长时跨度下维护人格一致性的能力。

#### 2. 方法论：核心思想与关键技术
PERMA 提出了一种**动态人格状态（Persona State）**建模方法，核心流程如下：
*   **事件驱动的对话重构**：将交互分为三种类型：
    *   *Emergence（出现）*：用户首次接触某领域并建立初始偏好。
    *   *Supplement（补充）*：后续交互中对偏好的细化、修改或深化。
    *   *Task（任务）*：探测模型整合历史偏好解决复杂问题的能力。
*   **两阶段生成管线**：
    1.  **时间线规划**：由 LLM 规划跨越 1-6 个月的交互事件，确保逻辑连贯。
    2.  **对话生成与验证**：根据规划生成多轮对话，并通过 LLM 验证和人工校验确保偏好细节被隐式或显式包含。
*   **现实性增强技术**：
    *   **文本变异性注入**：引入 5 种人类提示偏差（信息缺失、上下文切换、偏好矛盾、多语言、口语化）。
    *   **语言风格对齐**：利用 WildChat 真实数据对齐合成对话的语言风格，并加入无关对话作为干扰项。
*   **数学定义**：定义人格状态 $S_u^t = f(\{C_1, \dots, C_t\})$，其中 $f$ 是记忆系统的聚合算子，目标是最大化响应 $a$ 与演化人格状态的一致性。

#### 3. 实验设计
*   **数据集规模**：涵盖 10 个代表性用户画像、20 个领域（如旅游、金融、电影等）、808 个事件、3.5k+ 对话，总计约 1.8M tokens。
*   **评估场景**：
    *   **Clean**：标准交互。
    *   **Noise**：注入文本变异性。
    *   **Style-aligned Long-context**：长达 128k tokens 的风格对齐背景。
*   **对比方法**：
    *   **独立 LLM**：包括推理模型（MiniMax-M2.5, GLM-5, Kimi-K2.5）和聊天模型（Qwen, Llama3.3, Gemini, GPT-4o-mini）。
    *   **记忆系统/RAG**：RAG (BGE-M3), MemOS, Mem0, LightMem, Memobase, EverMemOS, Supermemory。
*   **Benchmark 任务**：
    *   **MCQ（多选题）**：从任务完成度、偏好一致性、信息置信度三个维度设计选项。
    *   **交互式评估**：使用 LLM 模拟器进行多轮反馈，直到任务达成或达到轮次上限。

#### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 型号和训练时长。
*   **实现细节**：实验主要依赖于商业模型 API（如 GPT-4o, Gemini 等）。本地部署部分使用了 **MongoDB** 存储信息，**Milvus** 作为向量数据库。

#### 5. 实验数量与充分性
*   **实验规模**：进行了单领域、多领域、噪声干扰、长文本（128k）等多种配置下的对比。
*   **维度覆盖**：通过时间点探测（Type 1/2/3 检查点）和位置探测（10%-100% 深度）分析了记忆的衰减与干扰。
*   **客观性**：引入了人工校验（97.75% 的 MCQ 准确率验证）和“LLM-as-a-Judge”评分，实验设计较为全面、客观，充分考虑了跨领域干扰和时间深度。

#### 6. 主要结论与发现
*   **记忆系统的优越性**：先进记忆系统（如 MemOS）通过结构化人格状态，能以极低的 Token 消耗（减少 99% 以上）达到甚至超过全量上下文 LLM 的效果。
*   **推理模型更强**：推理模型（如 Kimi-K2.5, Qwen3）在合成跨领域偏好方面显著优于普通聊天模型。
*   **噪声的“催化”作用**：有趣的是，适度的噪声有时能促使记忆系统更关注偏好细节，从而在某些指标上表现更好。
*   **长时挑战**：随着交互深度增加（Type 3 检查点），所有系统均表现出性能下降，说明跨领域干扰和时间漂移仍是个性化记忆的瓶颈。

#### 7. 优点
*   **范式转移**：从静态的“事实检索”转向动态的“人格维护”，更符合真实应用场景。
*   **高度仿真**：通过注入 5 种噪声和真实语言风格，极大地提升了基准测试的难度和可信度。
*   **解耦评估**：通过 MCQ 和交互式模拟，能够区分模型是“记不住”还是“推理不出”。

#### 8. 不足与局限
*   **合成数据偏差**：尽管经过人工校验，但核心数据仍由 LLM 生成，可能存在模型特有的逻辑偏见。
*   **评估模型限制**：实验中记忆系统的后端生成器多采用 GPT-4o-mini，其本身的推理上限可能限制了复杂记忆系统的发挥。
*   **实时性挑战**：部分先进记忆系统（如 EverMemOS）检索延迟高达 16 秒，难以直接应用于实时交互。

（完）
