---
title: Compute Allocation for Reasoning-Intensive Retrieval Agents
title_zh: 推理密集型检索智能体的计算分配
authors: "Sreeja Apparaju, Nilesh Gupta"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14635v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: LLM流水线中的计算分配与推理时思考
tldr: 随着智能体运行时间增长，记忆库不断扩大，推理密集型检索变得至关重要。本文研究了在查询扩展和重排序阶段如何分配计算资源。通过在BRIGHT基准上测试Gemini系列模型，发现重排序阶段对模型能力和候选深度更敏感，而查询扩展收益有限。研究建议应将计算资源集中在重排序而非均匀分布，以实现更高效的推理性能。
selection_source: fresh_fetch
motivation: 针对长程智能体中推理密集型检索的高昂推理成本，探索如何优化计算资源在查询扩展与重排序之间的分配。
method: 使用Gemini 2.5模型家族，在BRIGHT基准上通过改变模型规模、推理思考时间和重排序深度，对比不同阶段的性能表现。
result: 重排序阶段显著受益于更强的模型和更深的候选池，而查询扩展在轻量级模型之外收益递减，且推理思考对两阶段提升均较小。
conclusion: 检索流水线的计算资源应优先分配给重排序阶段，而非在各阶段间平均分配。
---

## 摘要
随着智能体在长周期内运行，其内存存储持续增长，使得检索对于获取相关信息变得至关重要。许多智能体查询需要推理密集型检索，其中查询与相关文档之间的联系是隐式的，需要通过推理来衔接。大语言模型（LLM）增强的流水线通过查询扩展和候选重排序来解决这一问题，但引入了显著的推理成本。我们利用 BRIGHT 基准测试和 Gemini 2.5 模型系列，研究了推理密集型检索流水线中的计算分配。我们在查询扩展和重排序阶段改变了模型容量、推理时思考以及重排序深度。我们发现，重排序从更强大的模型（NDCG@10 提升 7.5）和更深的候选池（k 从 10 增加到 100 时提升 21%）中获益显著，而查询扩展在超过轻量级模型后表现出收益递减（从弱模型到强模型 NDCG@10 仅提升 1.1）。推理时思考在任何阶段提供的改进都微乎其微。这些结果表明，计算资源应集中在重排序上，而不是在流水线各阶段之间均匀分布。

## Abstract
As agents operate over long horizons, their memory stores grow continuously, making retrieval critical to accessing relevant information. Many agent queries require reasoning-intensive retrieval, where the connection between query and relevant documents is implicit and requires inference to bridge. LLM-augmented pipelines address this through query expansion and candidate re-ranking, but introduce significant inference costs. We study computation allocation in reasoning-intensive retrieval pipelines using the BRIGHT benchmark and Gemini 2.5 model family. We vary model capacity, inference-time thinking, and re-ranking depth across query expansion and re-ranking stages. We find that re-ranking benefits substantially from stronger models (+7.5 NDCG@10) and deeper candidate pools (+21% from $k$=10 to 100), while query expansion shows diminishing returns beyond lightweight models (+1.1 NDCG@10 from weak to strong). Inference-time thinking provides minimal improvement at either stage. These results suggest that compute should be concentrated on re-ranking rather than distributed uniformly across pipeline stages.

---

## 论文详细总结（自动生成）

这篇论文针对长程智能体（Long-horizon Agents）在处理海量记忆时面临的“推理密集型检索”难题，系统研究了如何在不同检索阶段优化计算资源分配。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在有限的计算预算下，如何在大语言模型（LLM）增强的检索流水线（查询扩展 + 初始检索 + 重排序）中分配算力，以实现最优的检索性能？
*   **研究背景**：智能体在长期运行中会积累大量记忆。传统的基于嵌入（Embedding）的检索依赖表面语义相似性，难以处理“推理密集型”查询（即查询与文档之间无直接词汇重叠，需逻辑推理才能建立联系，如：根据“实习生”身份推理出其缺乏“安全权限”）。
*   **研究意义**：LLM 虽然能提升检索质量，但推理成本高昂。找到算力分配的最优解对构建高效、低成本的智能体记忆系统至关重要。

### 2. 方法论
论文将检索过程建模为一个三阶段流水线，并对各阶段的变量进行控制实验：
*   **查询扩展 (QE)**：使用 LLM 将原始查询 $q$ 扩展为包含隐式约束和领域术语的 $q_{exp}$，旨在提高召回率。
*   **初始检索**：采用 **BM25** 算法从大规模语料库中检索出候选列表。选择 BM25 是因为它能更好地处理 LLM 生成的分布外（OOD）查询。
*   **列表重排序 (RR)**：使用 LLM 对初始检索到的前 $k$ 个候选文档进行深度推理，评估其相关性并重新排序。
*   **关键变量控制**：
    *   **模型能力**：使用 Gemini 2.5 家族（Flash-Lite, Flash, Pro）。
    *   **推理深度**：对比标准模式与“思考（Thinking）”模式（即思维链推理）。
    *   **重排序深度**：改变候选池大小 $k \in \{10, 20, 50, 100\}$。

### 3. 实验设计
*   **数据集/基准测试**：使用 **BRIGHT** 基准测试，该测试涵盖 12 个领域，专门设计用于评估需要多步推理和隐式约束满足的检索任务。
*   **评估指标**：NDCG@10（排序质量）、Recall@100（召回上限）、单次查询成本（美元）和延迟（秒）。
*   **对比方法**：对比了不同模型组合（如 Flash-Lite 做 QE + Pro 做 RR）以及不同 $k$ 值下的性能表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **成本量化**：由于使用的是 Gemini API，作者通过“每查询美元成本”和“每查询延迟秒数”来间接衡量计算资源消耗。例如，Pro 模型的成本约为 Flash-Lite 的 27 倍。

### 5. 实验数量与充分性
*   **实验规模**：作者针对 QE 模型变体、RR 模型变体以及 RR 深度进行了多组消融实验。
*   **充分性评价**：实验设计较为系统，涵盖了从轻量级到旗舰级的模型梯度，并深入探讨了“推理时间思考”这一新兴特性的影响。虽然仅基于 Gemini 系列，但在推理密集型这一特定场景下的结论具有较强的说服力。

### 6. 主要结论与发现
*   **查询扩展收益递减**：从无增强提升到轻量级模型（Flash-Lite）时增益巨大（NDCG@10 +14.35），但进一步升级到 Pro 模型收益极微（仅 +1.1），且“思考”模式在 QE 阶段甚至可能因过度思考导致性能下降。
*   **重排序是性能核心**：重排序阶段对模型能力高度敏感。升级重排序模型可带来显著提升（NDCG@10 +7.5）。
*   **增加重排序深度效果显著**：将候选池 $k$ 从 10 增加到 100，NDCG@10 持续提升约 20%。
*   **“思考”模式性价比低**：在 QE 和 RR 阶段，开启模型的“思考”模式（Thinking mode）带来的性能提升非常有限，但会显著增加成本和延迟。

### 7. 优点
*   **实战导向**：直接回答了开发者在构建 RAG 或智能体系统时最关心的“钱该花在哪”的问题。
*   **发现直觉背后的规律**：明确了 QE 本质上是“词汇覆盖”任务（适合小模型），而 RR 才是真正的“推理”任务（适合大模型）。
*   **深度消融**：通过对 $k$ 值的细致观察，证明了增加候选深度是比升级 QE 模型更有效的算力分配方式。

### 8. 不足与局限
*   **模型单一性**：实验完全基于 Gemini 2.5 系列，结论是否适用于 GPT-4o 或 Claude 3.5 等其他模型家族尚待验证。
*   **检索算法局限**：初始检索仅使用了 BM25，未探讨向量检索（Dense Retrieval）或混合检索在这一框架下的表现。
*   **基准测试覆盖面**：虽然 BRIGHT 很专业，但单一基准测试可能无法完全代表所有智能体应用场景（如长文本对话记忆）。

（完）
