---
title: "Thinking Deeper, Not Longer: Depth-Recurrent Transformers for Compositional Generalization"
title_zh: 思考更深，而非更长：用于组合泛化的深度循环 Transformer
authors: Hung-Hsuan Chen
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21676v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 用于推理的深度循环Transformer架构
tldr: 针对标准Transformer固定计算深度限制复杂推理的问题，本文提出一种深度递归Transformer架构。该模型通过在潜空间迭代应用共享权重的Transformer块，实现了计算深度与参数量的解耦，允许在推理时通过增加迭代步数来处理更复杂的任务。研究引入了静默思考目标、LayerScale初始化和恒等偏置递归等机制以确保深层递归的稳定性。实验证明，该模型在图遍历、布尔逻辑和关系文本等任务中展现出显著的组合泛化能力，揭示了计算步数与任务复杂度之间的“计算前沿”关系。
selection_source: fresh_fetch
motivation: 标准Transformer的固定深度限制了其处理需要可变深度推理（如多跳图遍历）的复杂任务的能力。
method: 提出一种深度递归架构，通过共享权重的迭代应用、静默思考目标及LayerScale等机制，使模型能在推理时动态扩展计算深度。
result: 在图路径、逻辑运算和关系文本任务中，模型性能随推理步数增加呈现出从随机到近乎完美的“计算前沿”跃迁。
conclusion: 深度递归提供了一种垂直链式思考机制，通过解耦深度与参数量，有效提升了模型在分布外复杂推理任务中的泛化表现。
---

## 摘要
标准 Transformer 具有固定的计算深度，这从根本上限制了它们在需要变长深度推理任务（如多跳图遍历或嵌套逻辑）上的泛化能力。我们提出了一种深度循环 Transformer，通过在潜空间中迭代应用共享权重的 Transformer 块，将计算深度与参数量解耦——使模型能够在推理时通过增加循环步数来换取更深层次的推理。我们的架构包含三种机制以稳定深度循环（20步以上）：(1) 静默思考目标，仅监督最终输出，从而强制进行真正的多步推理，而非中间启发式捷径；(2) LayerScale 初始化，保护脆弱的推理状态免受未训练层的噪声干扰；(3) 恒等偏差循环，在多步之间创建梯度高速公路。我们在三个归纳偏置递减的组合推理领域进行了评估：图可达性（严格的邻接掩码）、嵌套布尔逻辑（相对定位）和非结构化关系文本（序列位置不提供结构提示）。在所有任务中，我们观察到一个清晰的“计算前沿”——即随着思考步数随任务复杂度扩展，性能从随机猜测转变为近乎完美的边界。此外，这些任务揭示了性质迥异的泛化行为：精确但脆弱（图）、近似但鲁棒（逻辑）以及无结构提示下的自主潜空间路由（文本）。这一进展阐明了任务无关的循环推理核心与任务特定的感知接口之间的相互作用如何塑造分布外（OOD）泛化，为垂直思维链提供了一个机械论视角，补充了当前主流的水平 Token 生成范式。

## Abstract
Standard Transformers have a fixed computational depth, fundamentally limiting their ability to generalize to tasks requiring variable-depth reasoning, such as multi-hop graph traversal or nested logic. We propose a depth-recurrent Transformer that decouples computational depth from parameter count by iteratively applying a shared-weight Transformer block in latent space -- enabling the model to trade recurrence steps for deeper reasoning at inference time. Our architecture incorporates three mechanisms to make deep recurrence (20+ steps) stable: (1) a silent thinking objective that supervises only the final output, forcing genuine multi-step reasoning rather than intermediate heuristic shortcuts; (2) LayerScale initialization to protect fragile reasoning states from untrained layer noise; and (3) an identity-biased recurrence that creates a gradient highway across many steps. We evaluate on three compositional reasoning domains with decreasing inductive biases: graph reachability (strict adjacency masking), nested boolean logic (relative positioning), and unstructured relational text (where sequence position provides no structural hints). Across all tasks, we observe a clear \emph{computational frontier} -- a boundary where performance transitions from chance to near-perfect as thinking steps scale with task complexity. Moreover, these tasks reveal qualitatively different generalization behaviors: precise but brittle (graph), approximate but robust (logic), and autonomous latent routing without structural hints (text). This progression illuminates how the interplay between a task-invariant recurrent reasoning core and task-specific perceptual interfaces shapes out-of-distribution (OOD) generalization, offering a mechanistic perspective on vertical chain-of-thought that complements the prevailing horizontal token-generation paradigm.

---

## 论文详细总结（自动生成）

### 论文总结：Thinking Deeper, Not Longer: Depth-Recurrent Transformers for Compositional Generalization

#### 1. 核心问题与整体含义
该论文针对标准 Transformer 架构的根本限制提出了挑战：**固定计算深度**。在处理需要变长推理深度的任务（如多跳图遍历、嵌套逻辑）时，标准模型往往力不从心。目前 LLM 主要依赖“水平思维链”（Horizontal CoT），即通过生成更多 Token 来增加计算量，但这会消耗上下文窗口并导致错误累积。
论文提出了一种**“垂直思维链”（Vertical CoT）**范式，通过在潜空间（Latent Space）中循环应用共享权重的 Transformer 块，实现了计算深度与参数量、上下文长度的解耦。其核心含义是：模型可以通过“想得更深”（增加迭代步数）而非“写得更长”来解决复杂问题。

#### 2. 方法论
论文提出了一种**深度循环 Transformer（Depth-Recurrent Transformer）**架构，核心组件包括：
*   **不变推理核心（Invariant Reasoning Core）：** 一个共享权重的 Transformer 块，在潜空间对隐藏状态 $H$ 进行 $T$ 次迭代。
*   **关键技术细节：**
    1.  **静默思考目标（Silent Thinking）：** 仅在最后一次循环步计算损失函数（Cross-Entropy），不进行中间监督。这迫使模型学习真正的多步算法，而非寻找中间步骤的启发式捷径。
    2.  **LayerScale 初始化：** 在子层后引入学习缩放向量（初始值为 $10^{-4}$），使模型在训练初期接近恒等映射，保护脆弱的逻辑表示免受随机噪声干扰。
    3.  **恒等偏置循环（Identity-Biased Recurrence）：** 采用类似 GRU 的门控机制结合残差，并将门控偏置初始化为 -2.0。这确保了初始状态能保留约 88% 的信息，为深层递归（20步以上）创建了梯度高速公路。
    4.  **深度嵌入（Depth Embeddings）：** 在每一步注入学习到的步数编码，帮助模型识别当前的推理进度。
*   **感知接口：** 针对不同任务设计了拓扑掩码（图）、相对位置编码（逻辑）和标准序列注意力（文本）三种接口。

#### 3. 实验设计
研究在三个复杂度递增、归纳偏置递减的领域进行了评估：
*   **图可达性（Graph Reachability）：** 验证严格的拓扑约束。训练 1-5 跳路径，测试扩展至 12 跳。
*   **嵌套布尔逻辑（Nested Boolean Logic）：** 验证层次化状态维护。训练深度 1-8，测试扩展至 14。
*   **非结构化关系文本（Relational Text）：** 类似 CLUTRR 的亲属关系推理。训练深度 2-5，测试扩展至 9。
*   **对比方法：** 主要对比了“中间监督（Per-step Loss）”与“静默思考”的效果，并分析了不同推理步数（Thinking Steps）对性能的影响。

#### 4. 资源与算力
*   **模型规模：** 论文明确指出使用了相对较小的模型，参数量**小于 100 万（<1M parameters）**。
*   **算力细节：** 文中**未明确说明**具体的 GPU 型号、数量或总训练时长。考虑到参数量级较小，该实验可能在单张消费级 GPU 上即可完成。

#### 5. 实验数量与充分性
*   **实验组数：** 涵盖了三个大类任务，每个任务都包含了大量的分布内（ID）和分布外（OOD）测试。
*   **消融实验：** 重点消融了监督策略（中间监督 vs 静默思考），证明了中间监督会导致模型学习统计捷径而非逻辑算法。
*   **充分性：** 实验设计较为系统，通过热力图清晰地展示了“计算前沿”现象。虽然模型规模较小，但对于验证“深度递归”这一机械论假设而言，实验是充分且客观的。

#### 6. 主要结论与发现
*   **计算前沿（Computational Frontier）：** 准确率随推理步数增加呈现出“阶跃式”提升。当思考步数匹配任务复杂度时，性能会从随机猜测迅速跃升至近乎完美。
*   **OOD 泛化能力：** 模型展现了强大的外推能力，能够通过在推理时增加步数，解决比训练集更深、更复杂的逻辑问题。
*   **中间监督的危害：** 强制中间步骤输出会诱导模型学习“捷径”（如通过图密度预测可达性），从而破坏真正的算法发现能力。
*   **稳定性：** 证明了通过 LayerScale 和门控偏置，Transformer 可以在潜空间稳定循环 20 步以上而不崩溃。

#### 7. 优点
*   **架构创新：** 成功解耦了计算深度与序列长度，为解决 LLM 上下文窗口限制提供了新思路。
*   **机械论视角：** 深入探讨了感知接口与推理核心之间的相互作用，揭示了模型如何自主发现潜空间路由。
*   **训练策略：** 提出的“静默思考”和“恒等偏置”有效解决了深度递归模型难以训练的顽疾。

#### 8. 不足与局限
*   **模型规模受限：** 实验仅限于百万参数级的小模型，尚未在大规模预训练模型（如 Llama 等）上验证其有效性。
*   **人工干预：** 感知接口（如拓扑掩码）仍需根据任务手动设计，缺乏完全端到端的自动化构建。
*   **理论证明缺失：** 结论主要基于经验性观察，缺乏关于泛化边界的严谨数学证明。
*   **应用场景：** 目前仅在符号推理和结构化任务上表现优异，在通用自然语言生成任务上的表现尚待观察。

（完）
