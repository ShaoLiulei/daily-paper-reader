---
title: Dual-Space Knowledge Distillation with Key-Query Matching for Large Language Models with Vocabulary Mismatch
title_zh: 针对词表不匹配大语言模型的键-查询匹配双空间知识蒸馏
authors: "Stella Eva Tsiapali, Cong-Thanh Do, Kate Knill"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22056v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 针对词表不匹配的大语言模型知识蒸馏
tldr: 针对大语言模型在词表不匹配情况下的知识蒸馏问题，本文深入分析了现有SOTA方法DSKD-CMA的注意力机制，揭示了其在处理不同模型生成的键（Key）和查询（Query）分布不匹配时的局限性。为此，作者提出了一种基于生成对抗学习的改进方法DSKD-CMA-GA，通过优化键值匹配提升了跨词表蒸馏的文本生成质量，尤其在分布外数据上表现优异，有效缩小了跨词表与同词表蒸馏的性能差距。
selection_source: fresh_fetch
motivation: 现有的跨词表知识蒸馏方法在处理来自不同模型的键值对分布时存在不匹配问题，且其内部运作机制缺乏透明度。
method: 提出了一种基于生成对抗学习的改进方法DSKD-CMA-GA，旨在解决不同模型间键和查询分布不一致的问题。
result: 实验显示该方法在文本生成质量上获得了稳步提升，特别是在分布外数据上平均提高了0.37个ROUGE-L分数。
conclusion: 通过对抗学习优化键值匹配，可以有效提升跨词表知识蒸馏的效率并缩小其与同词表蒸馏的性能鸿沟。
---

## 摘要
大语言模型（LLMs）在各项语言任务中均取得了最先进（SOTA）的性能，但由于其庞大的规模和资源需求，部署成本高昂。知识蒸馏（KD）通过训练较小的学生模型来模仿较大的教师模型，从而在不显著损失性能的情况下提高效率。带有跨模型注意力机制的双空间知识蒸馏（DSKD-CMA）已成为具有不同分词器（tokenizer）的 LLM 之间进行知识蒸馏的 SOTA 方法，但其内部运作机制在很大程度上仍不透明。在这项工作中，我们通过手动标记对齐探测和热图可视化系统地分析了 DSKD-CMA 的注意力机制，揭示了其优势和局限性。在此基础上，我们提出了一种基于生成对抗（GA）学习的新方法 DSKD-CMA-GA，旨在解决由不同模型计算出的键（keys）和查询（queries）之间分布不匹配的问题。实验表明，该方法在文本生成质量上带来了虽小但持续的 ROUGE-L 提升，特别是在分布外数据上（平均提升 0.37），缩小了跨分词器与同分词器知识蒸馏之间的差距。

## Abstract
Large language models (LLMs) achieve state-of-the-art (SOTA) performance across language tasks, but are costly to deploy due to their size and resource demands. Knowledge Distillation (KD) addresses this by training smaller Student models to mimic larger Teacher models, improving efficiency without significant performance loss. Dual-Space Knowledge Distillation with Cross-Model Attention (DSKD-CMA) has emerged as a SOTA method for KD between LLMs with distinct tokenizers, yet its internal workings remain largely opaque. In this work, we systematically analyse the attention mechanism of DSKD-CMA through manual token alignment probing and heatmap visualisations, revealing both strengths and limitations. Building on this, we introduce a novel method, DSKD-CMA-GA, based on Generative Adversarial (GA) learning, to address the mismatched distributions between the keys and queries computed from distinct models. Experiments show modest but consistent ROUGE-L gains in text generation quality, particularly on out-of-distribution data (+0.37 on average), narrowing the gap between cross- and same-tokenizer KD.

---

## 论文详细总结（自动生成）

以下是对论文《DUAL-SPACE KNOWLEDGE DISTILLATION WITH KEY-QUERY MATCHING FOR LARGE LANGUAGE MODELS WITH VOCABULARY MISMATCH》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：大语言模型（LLM）部署成本高，知识蒸馏（KD）是主流压缩方案。然而，现代 LLM 往往使用不同的分词器（Tokenizer）和词表（Vocabulary），导致教师和学生模型的序列长度、隐藏维度及预测空间不匹配，难以直接对齐。
*   **研究动机**：现有的 SOTA 方法 DSKD-CMA（双空间跨模型注意力蒸馏）虽然效果好，但其内部对齐机制像“黑盒”一样不透明。此外，由于 Key（来自教师）和 Query（来自学生）源自不同模型，它们的分布存在天然错位，限制了注意力机制的对齐效率。
*   **研究目标**：分析 DSKD-CMA 的内部运作逻辑，并提出改进方案以解决 Key-Query 分布不匹配问题。

### 2. 核心方法论
论文提出了分析框架和改进算法：
*   **分析框架（Chunk Alignment）**：
    *   提出一种**手动块对齐探测（Manual Chunk Alignment Probing）**。通过字符偏移量将教师和学生的不同分词序列切分为对应相同文本的“最小块（Chunks）”。
    *   基于此设计了两种消融变体：**CLP（块级投影）**强制进行均匀块对齐；**CLA（块级注意力）**仅在块内允许注意力权重分配。
*   **改进算法：DSKD-CMA-GA（生成对抗对齐）**：
    *   **核心思想**：引入对抗学习，使学生模型的 Query 投影器（Generator）生成的分布尽可能接近教师模型的 Key 分布。
    *   **技术细节**：增加一个判别器 $D$，其目标是区分教师的 Key 和学生的 Query。通过最小化对抗损失 $L_{KQ}$，强制两个模型的中间表示在注意力空间中对齐。
*   **备选算法：DSKD-CMA-CT（条件传输对齐）**：
    *   利用受限的最优传输（Optimal Transport）理论，通过训练一个 Critic 函数来鼓励 Key 和 Query 分布的双向匹配。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：DataBricks Dolly 15K。
    *   **测试集**：包含分布内（ID）的 Dolly 采样，以及 4 个分布外（OOD）数据集：Self-Instruct、Vicuna-Eval、Super-Natural Instructions (S-NI) 和 Unnatural Instructions (UnNI)。
*   **模型配置**：
    *   **学生模型**：GPT-2 (124M, 50K 词表)。
    *   **教师模型**：GPT-2 XL (1.5B, 同词表对比) 和 Qwen1.5 (1.8B, 150K 词表，跨词表核心实验)。
*   **Benchmark 与对比方法**：
    *   对比了 SFT（监督微调）、MinED（最小编辑距离对齐）、ULD（通用 Logit 蒸馏）以及原始的 DSKD-CMA。
    *   评估指标采用 **ROUGE-L**。

### 4. 资源与算力
*   **硬件环境**：Ampere 节点，配备 **4 张 NVIDIA A100 GPU (80GB)** 和 1TB RAM。
*   **训练时长**：每个变体的训练大约需要 **2-3 小时**。
*   **评估时长**：每个变体的评估大约需要 **1 小时**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   所有结果均基于 **5 个随机种子**的平均值，保证了结果的统计显著性。
    *   测试了 **6 种不同的 f-散度函数**（KL, RKL, SKL, SRKL, AKL, JSD），以寻找每种方法的最优配置。
    *   进行了详细的消融实验（CLP, CLA）和可视化分析（注意力热图）。
*   **充分性评价**：实验设计较为充分，涵盖了从同词表到跨词表的多种场景，且对比了多种 SOTA 基准，实验过程客观公平。

### 6. 主要结论与发现
*   **机制验证**：通过热图分析证实，DSKD-CMA 确实能够隐式学习到“块状”对齐结构，但在处理重复标记（如数字）时存在定位模糊。
*   **性能提升**：提出的 **DSKD-CMA-GA** 在所有数据集上均优于原始 DSKD-CMA，尤其在 OOD 数据上平均提升了 **0.37** 个 ROUGE-L 点。
*   **缩小差距**：GA 改进版显著缩小了跨词表蒸馏与同词表蒸馏之间的性能鸿沟，在某些任务上甚至实现了超越。
*   **散度函数敏感性**：发现不同的对齐方法对散度函数有特定偏好（如 GA 配合 Skewed KL 效果最佳），强调了损失函数与对齐机制协同设计的重要性。

### 7. 优点与亮点
*   **可解释性强**：通过手动块对齐探测，为原本黑盒的跨模型注意力机制提供了直观的解释和验证手段。
*   **方法论创新**：将对抗学习引入 Key-Query 匹配，从分布对齐的角度解决了跨模型特征空间不一致的根本问题。
*   **泛化性好**：在多个 OOD 数据集上表现稳健，证明了该方法在真实场景下的实用价值。

### 8. 不足与局限
*   **提升幅度有限**：虽然 ROUGE-L 有所提升，但绝对分值的增长（约 0.4-1.0）相对较小，对于极具挑战性的任务可能感知不明显。
*   **任务覆盖面**：实验主要集中在文本生成质量上，尚未在更复杂的逻辑推理（Reasoning）或长文本摘要任务上进行深入验证。
*   **计算开销**：虽然训练时间可控，但引入判别器和对抗训练增加了模型训练的复杂度和超参数调优的难度。

（完）
