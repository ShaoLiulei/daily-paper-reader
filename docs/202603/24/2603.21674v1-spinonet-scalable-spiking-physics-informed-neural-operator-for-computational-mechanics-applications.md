---
title: "SPINONet: Scalable Spiking Physics-informed Neural Operator for Computational Mechanics Applications"
title_zh: SPINONet：用于计算力学应用的可扩展脉冲物理信息神经算子
authors: "Shailesh Garg, Luis Mandl, Somdatta Goswami, Souvik Chakraborty"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21674v1"
tags: ["query:snn"]
score: 10.0
evidence: 可扩展的脉冲物理信息神经算子
tldr: 针对计算力学中物理信息算子学习模型在边缘设备上的高能耗问题，本文提出了SPINONet。该框架结合了受神经科学启发的脉冲神经元，通过架构感知设计实现了稀疏、事件驱动的计算，同时保留了计算时空导数所需的连续坐标微分路径。在多种偏微分方程上的实验表明，SPINONet在保持预测精度的同时显著提高了能效，并能通过混合监督学习解决纯物理驱动训练中的伪解问题。
selection_source: fresh_fetch
motivation: 旨在解决物理信息算子学习模型在资源受限设备上进行重复评估时面临的高计算成本和能耗挑战。
method: 提出了一种结合脉冲神经元与可分离架构的算子网络，利用稀疏事件驱动计算并支持物理信息驱动的自动微分。
result: 在多种计算力学偏微分方程任务中，该模型达到了与传统物理信息算子相当的预测精度，并展现出更优的能效。
conclusion: SPINONet证明了脉冲神经网络在科学计算中的潜力，为实现高效、可扩展的物理信息机器学习提供了新途径。
---

## 摘要
能效仍然是在计算力学和科学计算中部署物理信息算子学习模型的一个关键挑战，特别是在边缘和嵌入式设备等功耗受限的环境中，密集网络中重复的算子评估会产生巨大的计算和能量成本。为了应对这一挑战，我们引入了可分离物理信息类脑算子网络（SPINONet），这是一个受神经科学启发的框架，在保持与物理信息训练兼容的同时，减少了重复评估中的冗余计算。SPINONet 通过架构感知设计结合了回归友好的类脑脉冲神经元，实现了稀疏、事件驱动的计算，在提高能效的同时，保留了计算时空导数所需的连续、坐标可微路径。我们在代表计算力学问题的一系列偏微分方程上评估了 SPINONet，这些方程在时变和稳态设置中具有空间、时间和参数依赖性，并证明了尽管引入了稀疏通信，其预测性能仍可与传统的物理信息算子学习方法相媲美。此外，研究表明，在混合设置中，有限的数据监督可以提高在纯物理信息训练可能收敛到伪解的挑战性情况下的性能。最后，我们提供了一个分析性讨论，将 SPINONet 的架构组件和设计选择与计算负载和能耗的降低联系起来。

## Abstract
Energy efficiency remains a critical challenge in deploying physics-informed operator learning models for computational mechanics and scientific computing, particularly in power-constrained settings such as edge and embedded devices, where repeated operator evaluations in dense networks incur substantial computational and energy costs. To address this challenge, we introduce the Separable Physics-informed Neuroscience-inspired Operator Network (SPINONet), a neuroscience-inspired framework that reduces redundant computation across repeated evaluations while remaining compatible with physics-informed training. SPINONet incorporates regression-friendly neuroscience-inspired spiking neurons through an architecture-aware design that enables sparse, event-driven computation, improving energy efficiency while preserving the continuous, coordinate-differentiable pathways required for computing spatio-temporal derivatives. We evaluate SPINONet on a range of partial differential equations representative of computational mechanics problems, with spatial, temporal, and parametric dependencies in both time-dependent and steady-state settings, and demonstrate predictive performance comparable to conventional physics-informed operator learning approaches despite the induced sparse communication. In addition, limited data supervision in a hybrid setup is shown to improve performance in challenging regimes where purely physics-informed training may converge to spurious solutions. Finally, we provide an analytical discussion linking architectural components and design choices of SPINONet to reductions in computational load and energy consumption.

---

## 论文详细总结（自动生成）

这是一份关于论文《SPINONet: Scalable Spiking Physics-informed Neural Operator for Computational Mechanics Applications》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：物理信息算子学习（如 PI-DeepONet）在计算力学中表现优异，但在边缘计算或嵌入式设备等资源受限环境下，其高昂的计算成本和能耗成为瓶颈。传统的深度学习模型依赖连续激活的神经元，无论输入复杂度如何，都会执行全量的乘加运算（MAC）。
*   **核心问题**：如何在保持物理信息驱动（Physics-informed）训练所需的可微性的同时，引入受神经科学启发的脉冲神经网络（SNN）来实现稀疏、高效的事件驱动计算？
*   **整体含义**：论文提出了 **SPINONet**，通过将“可分离架构”与“脉冲神经元”结合，解决了脉冲动力学的不连续性与物理残差求导之间的矛盾，为高维偏微分方程（PDE）的高效求解提供了新方案。

### 2. 方法论
*   **核心思想**：**架构解耦**。将算子网络分为分支（Branch）和主干（Trunk）两部分。
    *   **脉冲分支网络**：在处理输入函数的分支网络中引入**可变脉冲神经元（VSN）**。VSN 支持连续值的脉冲输出，适合回归任务。由于分支网络仅参与代数运算，不涉及对时空坐标的求导，因此可以使用非连续的脉冲动力学。
    *   **连续主干网络**：主干网络保持传统的连续激活函数，用于编码时空坐标。这确保了通过自动微分计算 PDE 残差所需的时空导数路径是连续且可微的。
*   **关键技术细节**：
    *   **可分离架构**：采用可分离 DeepONet 结构，将 $d$ 维坐标分解为 $d$ 个一维主干网络，通过外积组合。这避免了全网格评估，将计算复杂度从指数级降低为线性级。
    *   **前向模式自动微分（FAD）**：利用 FAD 处理小输入维度、大输出维度的特性，显著提升残差计算效率。
    *   **代理梯度训练**：针对脉冲神经元的不可微阈值函数，在反向传播中使用平滑的代理梯度进行参数更新。
    *   **混合监督**：在纯物理驱动易收敛至伪解的情况下（如 Eikonal 方程），引入少量标注数据进行辅助训练。

### 3. 实验设计
*   **实验场景**：
    1.  **粘性 Burgers 方程**：1D 空间 + 1D 时间，测试非线性对流扩散问题的处理能力。
    2.  **带参数扩散的显热方程**：2D 空间 + 1D 时间 + 1D 参数（扩散系数），测试高维参数化 PDE 的扩展性。
    3.  **带参数边界的 Eikonal 方程**：2D 空间，测试几何输入表示和符号距离场的恢复。
*   **Benchmark 与对比方法**：
    *   **PI-DeepONet**：标准的物理信息深度算子网络。
    *   **Vanilla Baseline**：不含脉冲神经元的可分离物理信息算子网络。
    *   **SPINONet**：本文提出的脉冲版本。

### 4. 资源与算力
*   **算力说明**：文中展示了 GPU 显存消耗和每轮（Epoch）训练时间的对比图表。
*   **具体细节**：
    *   在显存方面，SPINONet 在大网格下（如 2000x2000）仅需约 10GB 显存，而 PI-DeepONet 迅速飙升至 40GB 以上。
    *   训练时长方面，SPINONet 的单轮耗时远低于非可分离架构。
*   **缺失信息**：论文未明确指出具体的 GPU 型号（如 A100 或 V100）以及总的训练时长（仅给出了单轮耗时和总 Epoch 数，如 40k-100k 次迭代）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2 维到 4 维（时空+参数）的多种 PDE 类型。
*   **充分性**：
    *   **消融与对比**：对比了不同网格分辨率下的内存和时间开销，证明了可扩展性。
    *   **稀疏性分析**：统计了不同实验中 VSN 层的平均脉冲活动率（约 28% - 65%），客观展示了能效潜力。
    *   **稳定性测试**：针对 Eikonal 方程展示了“伪解”模式，并证明了混合监督的必要性。
*   **评价**：实验设计较为全面，涵盖了精度、效率、内存和收敛稳定性等多个维度，对比实验公平（在相同架构基准下对比脉冲与非脉冲版本）。

### 6. 主要结论与发现
*   **精度保持**：SPINONet 在引入稀疏脉冲通信后，预测精度（相对 L2 误差）与连续基准模型相当（误差差异通常在 1% 以内）。
*   **能效显著**：分支网络的神经元活动率大幅降低，结合可分离架构，显著减少了 MAC 运算和内存访问。
*   **扩展性极佳**：得益于可分离主干和 FAD，SPINONet 能够处理 PI-DeepONet 因内存溢出而无法处理的高维大网格问题。
*   **物理一致性**：证明了在分支网络中使用脉冲神经元不会干扰主干网络的物理残差求导。

### 7. 优点
*   **架构创新**：巧妙地利用 DeepONet 的分支/主干分离特性，规避了 SNN 在物理信息学习中求导难的核心痛点。
*   **回归友好**：选用了 VSN 而非传统的 LIF 神经元，解决了 SNN 在高精度科学计算中难以收敛的问题。
*   **理论支撑**：提供了硬件无关的能耗分析模型，从算术运算和内存访问两个维度论证了能效。

### 8. 不足与局限
*   **硬件依赖**：虽然理论分析证明了能效，但 SNN 的优势在当前通用 GPU 上难以完全发挥，需在类脑芯片（如 Loihi）或专用 FPGA 上才能实现真正的功耗飞跃。
*   **超参数敏感**：脉冲神经元的阈值、泄漏率以及代理梯度的斜率等超参数增加了调参难度。
*   **实验覆盖**：尚未在极度复杂的湍流或多物理场耦合问题上进行验证，目前的 PDE 仍属于相对标准的基准问题。

（完）
