---
title: Advancing Multimodal Agent Reasoning with Long-Term Neuro-Symbolic Memory
title_zh: 利用长期神经符号记忆提升多模态智能体推理能力
authors: "Rongjie Jiang, Jianwei Wang, Gengda Zhao, Chengyang Luo, Kai Wang, Wenjie Zhang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15280v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 多模态大模型智能体的神经符号记忆
tldr: 针对多模态智能体在长期推理中缺乏演绎推理能力的问题，本文提出NS-Mem框架。该框架结合了神经表示与显式符号结构，通过三层存储架构（情节、语义、逻辑规则）、SK-Gen知识整合机制以及混合检索方式，显著提升了智能体在复杂决策中的分析推理能力，在多项基准测试中表现优于传统纯神经存储系统。
selection_source: fresh_fetch
motivation: 现有的多模态智能体存储系统多依赖神经向量检索，难以支持现实决策中关键的分析性演绎推理。
method: 提出NS-Mem框架，通过三层存储架构、SK-Gen自动知识整合机制以及结合相似度与符号查询的混合检索实现神经符号协同。
result: "在真实多模态推理基准测试中，NS-Mem比纯神经存储系统平均准确率提升4.35%，在受限推理任务中提升高达12.5%。"
conclusion: NS-Mem通过整合神经与符号存储，有效增强了多模态智能体的长期推理和结构化决策能力。
---

## 摘要
大语言模型的最新进展推动了在开放世界、多模态环境中运行的智能体的出现。为了支持长期推理，这类智能体通常配备有外部记忆系统。然而，大多数现有的多模态智能体记忆主要依赖于神经表示和基于向量的检索，这虽然适用于归纳式、直觉式推理，但在支持对现实世界决策至关重要的分析式、演绎式推理方面存在根本局限。为了解决这一局限性，我们提出了 NS-Mem，这是一个长期神经符号记忆框架，旨在通过将神经记忆与显式符号结构和规则相结合，提升多模态智能体的推理能力。具体而言，NS-Mem 围绕记忆系统的三个核心组件运行：(1) 由情节层、语义层和逻辑规则层组成的三层记忆架构；(2) 由 SK-Gen 实现的记忆构建与维护机制，该机制能从积累的多模态经验中自动整合结构化知识，并增量更新神经表示和符号规则；(3) 一种混合记忆检索机制，结合了基于相似性的搜索与确定性符号查询函数，以支持结构化推理。在现实世界多模态推理基准测试上的实验表明，神经符号记忆相比纯神经记忆系统在整体推理准确率上平均提升了 4.35%，在受限推理查询中提升高达 12.5%，验证了 NS-Mem 的有效性。

## Abstract
Recent advances in large language models have driven the emergence of intelligent agents operating in open-world, multimodal environments. To support long-term reasoning, such agents are typically equipped with external memory systems. However, most existing multimodal agent memories rely primarily on neural representations and vector-based retrieval, which are well-suited for inductive, intuitive reasoning but fundamentally limited in supporting analytical, deductive reasoning critical for real-world decision making. To address this limitation, we propose NS-Mem, a long-term neuro-symbolic memory framework designed to advance multimodal agent reasoning by integrating neural memory with explicit symbolic structures and rules. Specifically, NS-Mem is operated around three core components of a memory system: (1) a three-layer memory architecture that consists episodic layer, semantic layer and logic rule layer, (2) a memory construction and maintenance mechanism implemented by SK-Gen that automatically consolidates structured knowledge from accumulated multimodal experiences and incrementally updates both neural representations and symbolic rules, and (3) a hybrid memory retrieval mechanism that combines similarity-based search with deterministic symbolic query functions to support structured reasoning. Experiments on real-world multimodal reasoning benchmarks demonstrate that Neural-Symbolic Memory achieves an average 4.35% improvement in overall reasoning accuracy over pure neural memory systems, with gains of up to 12.5% on constrained reasoning queries, validating the effectiveness of NS-Mem.

---

## 论文详细总结（自动生成）

### 论文总结：利用长期神经符号记忆提升多模态智能体推理能力

#### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态智能体记忆系统主要依赖**神经表示（向量嵌入）**和**相似度检索**。这种方式类似于人类思维的“系统 1”（直觉、感性），擅长语义匹配，但在处理需要“系统 2”（逻辑、理性）的**分析性演绎推理**（如处理前提条件、顺序依赖、约束满足）时存在根本局限。
*   **研究动机**：在现实决策中，智能体不仅需要记住“发生了什么”，还需要理解“如何做”以及“在特定约束下该怎么做”。例如，当智能体知道“碗碎了”这一约束时，纯向量检索可能仍会建议“搅拌水果”，而忽略了“先买碗”的逻辑前提。

#### 2. 方法论：NS-Mem 框架
NS-Mem 结合了神经感知的高效性和符号逻辑的确定性，核心包含三个部分：
*   **三层记忆架构**：
    1.  **情节层 (Episodic Layer)**：存储带时间戳的细粒度多模态观察（事件描述、视觉/音频特征）。
    2.  **语义层 (Semantic Layer)**：抽象实体属性和关系，通过强化/削弱机制维护实体知识的一致性。
    3.  **逻辑层 (Logic Layer)**：核心创新点。每个逻辑节点包含**神经索引**（用于检索发现）和**程序有向无环图 (Procedural DAG)**（用于符号查询）。DAG 编码了步骤依赖、可选路径和执行概率。
*   **SK-Gen 知识整合机制**：
    *   **构建**：通过动作序列提取、**PrefixSpan 序列模式挖掘**、LLM 验证及 DAG 构造，从原始观察中蒸馏出结构化知识。
    *   **维护**：采用**指数移动平均 (EMA)** 增量更新神经索引，并根据新观察动态调整 DAG 的转移概率和结构，避免全量重构。
*   **混合检索与推理**：
    *   **查询分类**：将查询分为事实型、约束型和角色型。
    *   **双层检索**：结合目标级（Goal-level）和步骤级（Step-level）匹配。
    *   **符号增强**：在检索到的 DAG 上直接执行确定性函数（如路径枚举、约束过滤），确保推理结果的精确性和可重复性。

#### 3. 实验设计
*   **数据集**：使用 **M3-Bench** 基准测试，包含两个子集：
    *   **M3-Bench-robot**：100 个机器人视角拍摄的真实视频。
    *   **M3-Bench-web**：920 个网络来源的视频。
*   **评估维度**：涵盖多细节 (MD)、多跳 (MH)、跨模态 (CM)、人类理解 (HU) 和通用知识 (GK) 五类推理任务。
*   **对比方法**：
    *   **苏格拉底模型**：GPT-4o, Gemini-1.5-Pro, Qwen2.5-VL 等。
    *   **在线视频理解模型**：MovieChat, MA-LMM, Flash-VStream。
    *   **智能体方法**：**M3-Agent**（目前最先进的纯向量存储智能体系统）。

#### 4. 资源与算力
*   **硬件环境**：服务器配备 Intel(R) Xeon(R) Silver 4314 CPU，512GB 内存。
*   **GPU**：使用 **NVIDIA RTX A5000** GPU。
*   **训练/运行细节**：文中未明确给出具体的训练时长，但强调了其增量维护机制（SK-Gen）在处理新观察时的计算复杂度较低（$O(d \log |N|)$），适合在线运行。

#### 5. 实验数量与充分性
*   **实验规模**：在 Robot 子集（703 个问题）和 Web 子集（2,066 个问题）上进行了大规模测试。
*   **实验类型**：
    *   **总体性能对比**：验证 NS-Mem 在所有指标上的领先地位。
    *   **推理类型分析**：展示在多跳和通用知识任务上的显著提升。
    *   **消融实验**：分别验证了符号组件、神经组件及 DAG 结构的贡献。
    *   **超参数分析**：探讨了验证阈值 $\tau$、门控阈值 $\delta$ 和检索权重 $\alpha$ 的影响。
    *   **效率评估**：对比了对话轮数和响应时间。
*   **评价**：实验设计全面，涵盖了从准确率到效率、从静态到动态维护的多个维度，对比基准选择合理，结果具有说服力。

#### 6. 主要结论与发现
*   **准确率提升**：NS-Mem 相比纯神经记忆系统（M3-Agent）平均提升了 **4.35%** 的准确率。
*   **约束推理优势**：在**受限推理（Constrained Reasoning）**任务中，准确率提升高达 **12.5%**，证明了符号结构在处理逻辑约束方面的优越性。
*   **效率优化**：由于符号函数可以一次性返回完整的步骤序列，NS-Mem 减少了约 **10%-15%** 的对话轮数，显著降低了推理延迟。
*   **增量更新有效性**：动态更新的逻辑节点比静态节点在准确率和效率上均有提升，证明了 EMA 和概率转移更新的必要性。

#### 7. 优点
*   **架构创新**：成功将神经检索的“模糊匹配”与符号逻辑的“确定性推理”结合，解决了长程推理中的逻辑断层问题。
*   **理论支撑**：为增量维护的后验一致性、融合一致性和符号函数的确定性提供了数学证明。
*   **实用性强**：SK-Gen 机制支持在线增量更新，无需重新训练模型即可吸收新知识。

#### 8. 不足与局限
*   **LLM 依赖性**：在知识验证（SK-Gen Step 3）和查询分类中仍依赖 LLM，这可能引入额外的推理开销和潜在的偏见。
*   **冷启动问题**：逻辑层的构建依赖于 PrefixSpan 挖掘重复模式，对于仅出现一次的孤立复杂事件，可能难以形成有效的逻辑节点。
*   **多模态融合深度**：虽然支持多模态输入，但符号层主要处理的是从多模态中提取出的文本化逻辑，视觉特征在符号推理过程中的直接参与度有限。

（完）
