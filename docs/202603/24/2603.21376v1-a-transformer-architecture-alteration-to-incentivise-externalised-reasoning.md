---
title: A transformer architecture alteration to incentivise externalised reasoning
title_zh: 一种旨在激励外部化推理的 Transformer 架构改进
authors: "Elizabeth Pavlova, Mariia Koroliuk, Karthik Viswanathan, Cameron Tice, Edward James Young, Puria Radmard"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21376v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于推理的 Transformer 架构改进
tldr: 本研究提出了一种改进Transformer架构及其后训练流程的新方法，旨在通过引入早期退出机制促使大语言模型进行外部化推理。该方法在中间层增加退出路径，并结合强化学习激励模型在预测简单标记时尽早退出，从而减少内部冗余计算。初步实验表明，该架构能有效实现自适应计算分配，将复杂的内部规划转化为可观察的外部推理步骤，为提高模型透明度和推理效率提供了新思路。
selection_source: fresh_fetch
motivation: 旨在减少大语言模型在内部激活中进行非近视规划的冗余计算，促使其通过外部化推理来解决复杂问题。
method: 在Transformer架构中引入中间层早期退出机制，并利用强化学习激励模型在保证性能的前提下尽可能提前退出。
result: 在小型推理模型上的初步实验证明，该方法能够使模型根据标记预测的难易程度自适应地减少计算量。
conclusion: 该架构调整能有效优化计算资源分配，通过限制内部计算深度来引导模型更多地依赖外部推理过程。
---

## 摘要
我们提出了一种新的架构变更和训练后流水线，通过教导模型提前截断前向传播，使大语言模型（LLM）成为更冗长的推理者。我们在现有的 Transformer 架构中增加了中间层的早期退出机制，并训练模型在无需深度计算即可预测下一个 token 时，从较浅的层退出。在校准阶段之后，我们利用强化学习激励模型在保持任务性能的同时尽可能早地退出。我们为小型推理模型提供了初步结果，表明它们学会了在不同 token 之间自适应地减少计算量。我们预测，在合适的规模下应用该方法，可以最大限度地减少推理模型利用其内部激活进行非近视规划（non-myopic planning）时的冗余计算量，并将其仅保留给难以预测的 token。

## Abstract
We propose a new architectural change, and post-training pipeline, for making LLMs more verbose reasoners by teaching a model to truncate forward passes early. We augment an existing transformer architecture with an early-exit mechanism at intermediate layers and train the model to exit at shallower layers when the next token can be predicted without deep computation. After a calibration stage, we incentivise the model to exit as early as possible while maintaining task performance using reinforcement learning. We provide preliminary results to this effect for small reasoning models, showing that they learn to adaptively reduce computations across tokens. We predict that, applied at the right scale, our approach can minimise the amount of excess computation that reasoning models have at their disposal to perform non-myopic planning using their internal activations, reserving this only for difficult-to-predict tokens.

---

## 论文详细总结（自动生成）

这篇论文提出了一种通过修改 Transformer 架构和训练流程来强制模型进行“外部化推理”的方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）在生成思维链（CoT）时，往往会在内部隐藏层中进行“非近视规划”（non-myopic planning），即在预测当前 token 时利用剩余算力为后续步骤做准备。这种“内部计算”导致 CoT 可能与模型真实的推理过程不一致（不忠实）或被刻意掩盖（模糊化），使得安全监控变得困难。
*   **研究动机**：通过架构调整，限制模型在单个 token 上的内部算力，迫使模型将复杂的推理过程转移到可观察的外部 token（CoT）中，从而提高模型的可监测性和安全性。

### 2. 方法论：核心思想与关键技术
该方法包含架构修改和两个训练阶段：
*   **架构修改**：
    *   在 Transformer 的中间层（每 5 层）添加**早期退出头（Early-exit heads）**。
    *   使用 **LoRA 适配器**调整现有权重。
    *   **随机退出机制**：模型在每一层根据退出头输出的概率随机决定是否停止计算。若退出，残差流直接跳过后续层进入解嵌入层（Unembedding layer）。
*   **阶段 1：自校准（Self-distillation SFT）**：
    *   以全深度模型为教师，计算中间层预测与最终层预测之间的 **KL 散度**。
    *   若 KL 散度低，说明该 token 可以在浅层预测。通过启发式函数将 KL 散度转化为目标退出概率，训练学生模型模仿这种退出行为。
*   **阶段 2：强化学习激励（RLOO）**：
    *   使用改进的 RLOO 算法，在奖励函数中加入**算力惩罚项**。
    *   **公式**：$R_{total} = R_{task} - \lambda \cdot (\text{平均退出层索引}) - \beta \cdot D_{KL}(\text{策略} \parallel \text{基座})$。
    *   通过调节 $\lambda$，可以控制模型在“任务准确性”与“减少内部计算（即强制外部化）”之间的平衡。

### 3. 实验设计
*   **数据集/场景**：
    *   **GSM8K**：数学文字题推理。
    *   **Theory of Mind (ToM)**：心理理论推理数据集（使用 ExploreToM 生成）。
*   **模型（Benchmark）**：
    *   DeepSeek-R1-Distill-Qwen-1.5B
    *   Qwen3-4B（注：文中提及 Qwen3-4B，可能为特定版本或笔误，通常指 Qwen 系列）。
*   **对比指标**：任务准确率（Accuracy）、回答连贯性（Coherence，由 GPT-5 评分）、平均层算力节省（ALS）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。考虑到模型规模为 1.5B 和 4B，且使用了 LoRA 训练，预计算力需求在中等规模。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在阶段 1 中，针对 5 种不同的 KL 因子进行了消融实验。
    *   在阶段 2 中，展示了 5 组 RL 训练运行的动态曲线。
    *   测试集规模较小（每次评估约 50-60 个 prompt）。
*   **充分性评价**：作为**初步研究（Preliminary results）**和**概念验证（Proof of Concept）**，实验展示了方法的可行性。但由于数据集种类较少、模型规模较小且缺乏跨领域验证，实验的充分性尚不足以支撑大规模工业应用，仍需进一步验证。

### 6. 主要结论与发现
*   **自适应计算**：模型成功学会了根据 token 难度分配算力。简单的 token（如单词后缀）在浅层退出，复杂的推理 token 使用全深度。
*   **性能平衡**：通过 RL 训练，模型在减少平均计算深度（从 98% 降至 90-95%）的同时，任务准确率反而有所提升（从 47% 提升至 55-60%）。
*   **可调控性**：通过超参数 $\lambda$ 和推理时的退出阈值，可以像拨盘一样调节模型推理的“外部化”程度。

### 7. 优点
*   **创新的安全视角**：不同于传统的 CoT 提示词工程，该方法从底层架构出发，通过“挤压”内部算力来强制模型诚实。
*   **训练效率**：结合了自监督蒸馏和 RL，使模型能够快速学习复杂的退出策略。
*   **推理灵活性**：提供了推理时干预的可能性，允许在安全敏感场景下强制增加 CoT 冗长程度。

### 8. 不足与局限
*   **忠实性验证缺失**：论文虽然证明了模型可以提前退出，但尚未验证这种做法是否真的提高了 CoT 的忠实度（即 CoT 是否真的反映了模型的推理逻辑）。
*   **规模限制**：实验仅在 1.5B 和 4B 模型上进行，在大规模模型（如 70B+）上的表现未知。
*   **质量权衡风险**：过度追求早期退出可能会导致模型在处理极高难度逻辑时性能崩溃。
*   **评估依赖**：部分定性评估依赖于 GPT-5，可能存在模型偏见。

（完）
