---
title: "CoVFT: Context-aware Visual Fine-tuning for Multimodal Large Language Models"
title_zh: CoVFT：面向多模态大语言模型的上下文感知视觉微调
authors: "Nan Zhou, Huiqun Wang, Yaoyan Zheng, Di Huang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21077v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 多模态大语言模型的视觉微调
tldr: 针对多模态大模型中视觉编码器是否应微调的争议，本文指出传统微调方法因忽略上下文而导致视觉偏好冲突，难以稳定提升性能。为此，提出了CoVFT框架，利用上下文向量提取和上下文混合专家模块，将多模态信息显式融入视觉适配过程。该方法在12个基准测试上表现优异且稳定性极高，甚至使7B模型性能超越了13B模型，揭示了视觉编码器优化的巨大潜力。
selection_source: fresh_fetch
motivation: 现有视觉微调方法因忽略多模态上下文导致的视觉偏好冲突，难以在各类任务中稳定超越冻结视觉编码器的基准。
method: 提出CoVFT框架，通过上下文向量提取（CVE）和上下文混合专家（CoMoE）模块，将多模态上下文显式引入视觉适配过程以分解冲突信号。
result: 在12个多模态基准测试中取得SOTA性能，且微调后的7B模型平均表现超过了其13B版本的对应模型。
conclusion: 通过引入上下文感知的微调机制，可以有效解决视觉编码器优化中的冲突问题，显著提升多模态大模型的性能上限。
---

## 摘要
多模态大语言模型（MLLMs）在跨模态感知和推理方面取得了显著进展，但一个基本问题仍未解决：视觉编码器应该被微调还是冻结？尽管 LLaVA 和 Qwen-VL 等模型取得了成功，但设计选择的不一致和训练设置的异构性阻碍了对 MLLM 中视觉微调（VFT）的统一理解。通过配置对齐的基准测试，我们发现现有的 VFT 方法在各种多模态任务中无法始终优于冻结基线。我们的分析表明，这种不稳定性源于视觉偏好冲突，即视觉编码器的上下文无关特性在多样化的多模态上下文下引发了发散的参数更新。为了解决这个问题，我们提出了上下文感知视觉微调（CoVFT）框架，该框架将多模态上下文显式地融入视觉适配中。通过集成上下文向量提取（CVE）和上下文混合专家（CoMoE）模块，CoVFT 分解了冲突的优化信号，并实现了稳定且上下文敏感的视觉更新。在 12 个多模态基准测试上的广泛实验表明，CoVFT 以卓越的稳定性实现了最先进的性能。值得注意的是，使用 CoVFT 微调的 7B MLLM 超过了其 13B 对应模型的平均性能，揭示了 MLLM 中视觉编码器优化方面巨大的未开发潜力。

## Abstract
Multimodal large language models (MLLMs) achieve remarkable progress in cross-modal perception and reasoning, yet a fundamental question remains unresolved: should the vision encoder be fine-tuned or frozen? Despite the success of models such as LLaVA and Qwen-VL, inconsistent design choices and heterogeneous training setups hinder a unified understanding of visual fine-tuning (VFT) in MLLMs. Through a configuration-aligned benchmark, we find that existing VFT methods fail to consistently outperform the frozen baseline across multimodal tasks. Our analysis suggests that this instability arises from visual preference conflicts, where the context-agnostic nature of vision encoders induces divergent parameter updates under diverse multimodal context. To address this issue, we propose the Context-aware Visual Fine-tuning (CoVFT) framework, which explicitly incorporates multimodal context into visual adaptation. By integrating a Context Vector Extraction (CVE) and a Contextual Mixture-of-Experts (CoMoE) module, CoVFT decomposes conflicting optimization signals and enables stable, context-sensitive visual updates. Extensive experiments on 12 multimodal benchmarks demonstrate that CoVFT achieves state-of-the-art performance with superior stability. Notably, fine-tuning a 7B MLLM with CoVFT surpasses the average performance of its 13B counterpart, revealing substantial untapped potential in visual encoder optimization within MLLMs.

---

## 论文详细总结（自动生成）

### CoVFT: 面向多模态大语言模型的上下文感知视觉微调论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
论文探讨了多模态大语言模型（MLLM）领域一个长期存在的争议：**在指令微调阶段，视觉编码器（Vision Encoder）究竟应该被冻结还是微调？**
*   **研究动机**：虽然 LLaVA（冻结）和 Qwen-VL（微调）等模型各行其道，但缺乏统一结论。作者通过实验发现，现有的视觉微调（VFT）方法（如全量微调、LoRA等）在不同任务间表现极不稳定，往往无法持续优于“冻结”基线。
*   **核心发现**：这种不稳定性源于**“视觉偏好冲突（Visual Preference Conflicts）”**。由于视觉编码器是上下文无关的，当面对定位（关注局部）和描述（关注全局）等截然不同的指令时，优化信号会产生冲突，导致参数更新方向发散，损害泛化性。

#### 2. 论文提出的方法论：CoVFT 框架
为了解决上述冲突，作者提出了 **CoVFT（Context-aware Visual Fine-tuning）**，核心思想是将多模态上下文显式引入视觉适配过程，将单一的后验概率 $p(z|I)$ 建模为上下文感知的 $p(z|I, c)$。
*   **上下文向量提取（CVE）**：
    *   利用冻结的文本编码器（如 BERT）提取指令特征。
    *   在视觉编码器内部通过轻量级残差块和交叉注意力机制，将文本特征与视觉特征聚合，生成一个紧凑的上下文向量 $c$。
*   **上下文混合专家（CoMoE）**：
    *   将视觉编码器后半部分的 FFN 层替换为多个并行专家（Experts）。
    *   **路由机制**：利用上下文向量 $c$ 动态计算路由权重，决定视觉 Token 如何在专家空间中分布。
    *   **梯度调制**：通过上下文引导的路由，使具有相似语义的任务共享专家，而冲突的任务则在不同的专家子空间中更新，从而分解冲突信号。

#### 3. 实验设计
*   **模型架构**：采用 LLaVA-1.5 范式，视觉编码器为 CLIP-ViT-L/14，语言模型为 Vicuna-1.5（7B 和 13B）。
*   **数据集**：
    *   **预训练**：558K 图像-文本对。
    *   **指令微调**：665K 多模态指令数据。
*   **Benchmark（12个基准测试）**：
    *   **通用 VQA**：MME、MMBench、GQA。
    *   **知识与 OCR**：ScienceQA、AI2D、TextVQA。
    *   **视觉中心任务**：MMVP、RealWorldQA、CV-Bench（包含 COCO、ADE、Omni3D）。
*   **对比方法**：Freeze（冻结）、Full Fine-tuning（全量微调）、BitFit、VPT、SVPT、LoRA。

#### 4. 资源与算力
*   **硬件设备**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练时长**：
    *   预训练阶段约 2 小时 38 分钟。
    *   指令微调阶段（以 CoVFT 为例）约 5 小时 32 分钟。
    *   总训练时间约 **8 小时 10 分钟**。
*   **开销分析**：相比全量微调，CoVFT 增加了约 4% 的训练时间开销和约 13.5% 的显存占用。

#### 5. 实验数量与充分性
*   **实验规模**：在 12 个主流基准上进行了全面测试，涵盖了感知、推理、OCR 和视觉中心化等多个维度。
*   **消融实验**：
    *   测试了不同的上下文向量设计（仅图像、仅文本、拼接、CVE）。
    *   测试了不同的路由策略（随机、均匀、稀疏 Top-k、稠密路由）。
    *   测试了 CoMoE 层的放置位置和专家数量。
*   **泛化性验证**：将 CoVFT 应用于不同的视觉编码器（SigLIP, DINOv3）和不同的 MLLM 架构（InternVL 2.0），均取得提升。
*   **数据扩展性**：测试了从 12.5% 到 100% 不同数据量下的表现。
*   **评价**：实验设计严谨，通过配置对齐（Configuration-aligned）确保了对比的客观性与公平性。

#### 6. 论文的主要结论与发现
*   **VFT 的潜力**：视觉编码器虽然仅占总参数量的不到 5%，但其优化对模型性能至关重要。
*   **CoVFT 的优越性**：在所有 12 个基准上均优于冻结基线，平均性能提升 2.15%。
*   **以小博大**：经过 CoVFT 微调的 **7B 模型平均性能超过了冻结视觉编码器的 13B 模型**。
*   **冲突缓解**：可视化分析证明，上下文向量能有效区分任务类型（如关系推理 vs. 区域定位），从而引导专家进行差异化更新。

#### 7. 优点
*   **理论深刻**：敏锐捕捉到 MLLM 中视觉微调不稳定的根本原因——视觉偏好冲突。
*   **设计精巧**：通过 MoE 结构巧妙地将“上下文感知”转化为“参数空间的分离优化”，解决了梯度冲突问题。
*   **稳定性强**：在不同任务、不同数据规模和不同模型架构下均表现出极高的鲁棒性。

#### 8. 不足与局限
*   **计算开销**：虽然作者认为开销可控，但 MoE 结构和额外的文本编码器推理确实增加了训练和推理的延迟（推理延迟增加约 10ms）。
*   **模型规模限制**：实验主要集中在 7B 和 13B 规模，尚未在超大规模（如 70B+）模型上验证其上限。
*   **专家负载平衡**：论文中提到稠密路由效果优于稀疏路由，但在更大规模数据下，如何平衡专家负载并保持推理效率仍是挑战。

（完）
