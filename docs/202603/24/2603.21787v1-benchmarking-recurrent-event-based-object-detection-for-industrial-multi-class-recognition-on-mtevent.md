---
title: Benchmarking Recurrent Event-Based Object Detection for Industrial Multi-Class Recognition on MTEvent
title_zh: 在 MTEvent 数据集上针对工业多类别识别的循环基于事件目标检测基准测试
authors: "Lokeshwaran Manohar, Moritz Roidl"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21787v1"
tags: ["query:snn"]
score: 10.0
evidence: 基于事件的物体检测与时间记忆
tldr: "本研究针对工业机器人场景，利用MTEvent数据集对循环事件目标检测模型ReYOLOv8s进行了基准测试。通过对比非循环基准模型，验证了时间记忆在多类别识别中的重要性。实验表明，从零训练的循环模型比基准提升了9.6%，而使用GEN1数据集预训练的模型表现最佳，达到0.329 mAP50。研究还分析了预训练领域匹配度及类别不平衡对性能的影响，为工业环境下的事件相机应用提供了重要参考。"
selection_source: fresh_fetch
motivation: 现有的事件相机目标检测研究多集中于户外驾驶，缺乏针对工业多类别识别场景的深入基准测试。
method: 在MTEvent数据集上对比了循环模型ReYOLOv8s与非循环YOLOv8s基准，并探讨了不同预训练策略和序列长度的影响。
result: 循环模型在从零训练时优于基准，且GEN1预训练模型在长序列下达到了0.329 mAP50的最优性能。
conclusion: 时间记忆和领域匹配的预训练对提升工业场景事件检测性能至关重要，但仍需解决类别不平衡等挑战。
---

## 摘要
事件相机因其高时间分辨率、高动态范围和减少的运动模糊而对工业机器人极具吸引力。然而，大多数基于事件的目标检测研究都集中在户外驾驶场景或有限的类别设置上。在这项工作中，我们在 MTEvent 数据集上对循环 ReYOLOv8s 进行基准测试，用于工业多类别识别，并使用非循环 YOLOv8s 变体作为基准，以分析时间记忆的影响。在 MTEvent 验证集上，表现最好的从零训练（scratch）循环模型（C21）达到了 0.285 mAP50，相比非循环 YOLOv8s 基准（0.260）实现了 9.6% 的相对提升。事件域预训练具有更显著的效果：基于 GEN1 初始化的微调在剪辑长度为 21 时获得了 0.329 mAP50 的最佳总体结果，且与从零训练不同，GEN1 预训练模型随着剪辑长度的增加而持续改进。PEDRo 初始化下降到 0.251，表明不匹配的源域预训练可能比从零训练效果更差。持续存在的失败模式主要受类别不平衡和人机交互的影响。总之，我们将这项工作定位为对工业环境中循环基于事件检测的专项基准测试和分析研究。

## Abstract
Event cameras are attractive for industrial robotics because they provide high temporal resolution, high dynamic range, and reduced motion blur. However, most event-based object detection studies focus on outdoor driving scenarios or limited class settings. In this work, we benchmark recurrent ReYOLOv8s on MTEvent for industrial multi-class recognition and use a non-recurrent YOLOv8s variant as a baseline to analyze the effect of temporal memory. On the MTEvent validation split, the best scratch recurrent model (C21) reaches 0.285 mAP50, corresponding to a 9.6% relative improvement over the nonrecurrent YOLOv8s baseline (0.260). Event-domain pretraining has a stronger effect: GEN1-initialized fine-tuning yields the best overall result of 0.329 mAP50 at clip length 21, and unlike scratch training, GEN1-pretrained models improve consistently with clip length. PEDRo initialization drops to 0.251, indicating that mismatched source-domain pretraining can be less effective than training from scratch. Persistent failure modes are dominated by class imbalance and human-object interaction. Overall, we position this work as a focused benchmarking and analysis study of recurrent event-based detection in industrial environments.

---

## 论文详细总结（自动生成）

以下是对论文《Benchmarking Recurrent Event-Based Object Detection for Industrial Multi-Class Recognition on MTEvent》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：探讨循环神经网络（Recurrent Neural Networks）在工业多类别识别场景下，对基于事件（Event-based）的目标检测性能的提升作用，并分析预训练策略和时间上下文长度的影响。
*   **研究动机**：
    *   事件相机具有高动态范围和低运动模糊，适合工业机器人，但现有研究多集中于自动驾驶（如 GEN1 数据集）或单一类别（如人）。
    *   工业环境具有类别多、类别极度不平衡、环境杂乱、人机交互频繁等特点，对检测器提出了更高挑战。
    *   事件数据具有稀疏性和时间分布性，单帧信息不足，理论上循环模型能通过累积历史信息提升鲁棒性，但其在工业场景下的实际收益尚不明确。

### 2. 方法论
*   **核心思想**：对比非循环基准模型与加入循环模块的变体，在不同时间剪辑长度（Clip Length）和预训练权重下进行性能评估。
*   **关键技术细节**：
    *   **输入表示**：将异步事件流转换为 5 通道的时间分箱事件张量（Event-tensor），每个窗口 50ms，分辨率 256×320。
    *   **模型架构**：
        *   **基准模型 (YOLOv8s)**：非循环检测器，仅处理单个时间窗口。
        *   **循环模型 (ReYOLOv8s)**：在 YOLOv8s 主干网络的中间特征阶段嵌入 **ConvLSTM** 模块，用于跨时间步聚合特征。
    *   **时间采样策略**：采用基于步长的采样（Stride-based sampling），在固定间隔的序列中传播隐藏状态，并在序列结束时重置。
    *   **迁移学习**：对比了从零训练（Scratch）、GEN1 预训练（驾驶域）和 PEDRo 预训练（机器人人像域）三种初始化方式。

### 3. 实验设计
*   **数据集与场景**：使用 **MTEvent** 数据集，包含仓库和工厂模拟场景。
*   **类别设置**：共 17 个类别（16 种工业刚性物体 + 人类）。
*   **Benchmark 设置**：
    *   将数据集划分为训练/验证/测试集（60/13/2 个场景）。
    *   主要评估指标为 mAP50 和 mAP50-95。
*   **对比实验**：
    *   非循环 YOLOv8s vs. 不同剪辑长度（3, 7, 11, 21）的 ReYOLOv8s。
    *   不同预训练源（GEN1, PEDRo）对微调效果的影响。
    *   针对“人类”类别的零样本迁移（Zero-shot transfer）分析。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练参数**：提到了使用 AdamW 优化器，初始学习率 0.00181，Batch Size 为 16，图像尺寸 320，早停策略（Patience=50）。

### 5. 实验数量与充分性
*   **实验规模**：进行了约 10 组核心消融实验（涵盖不同剪辑长度和预训练组合），并包含定性的零样本迁移分析和失败案例分析。
*   **充分性评价**：
    *   **优点**：实验设计逻辑严密，涵盖了时间维度（剪辑长度）和知识维度（预训练）的关键变量。
    *   **局限**：所有结果均基于**单次训练运行**，未提供多次实验的均值和标准差，可能存在训练随机性带来的偏差。此外，测试集仅包含 2 个场景，规模较小。

### 6. 主要结论与发现
*   **循环机制的有效性**：从零训练时，循环模型（C21）比非循环基准提升了 **9.6%** 的相对性能（mAP50 从 0.260 升至 0.285）。
*   **预训练的决定性作用**：GEN1 预训练效果最显著，最高达到 **0.329 mAP50**。且预训练模型能更好地利用长序列信息，性能随剪辑长度增加而稳定提升。
*   **领域匹配至关重要**：PEDRo 预训练效果（0.251）甚至低于从零训练，说明源域与目标域的场景结构、物体尺度匹配比单纯的模态一致性更重要。
*   **失败模式**：主要的性能瓶颈在于**长尾类别（样本稀缺）**以及**人机交互（如人搬运物体时导致的事件信号重叠/模糊）**。

### 7. 优点
*   **填补空白**：针对工业多类别这一特定细分领域提供了急需的基准数据。
*   **深入分析**：不仅给出了分数，还深入探讨了时间记忆（循环）与预训练之间的相互作用。
*   **实用性强**：识别出了工业部署中的实际挑战（如人机交互导致的信号融合），对后续算法改进有指导意义。

### 8. 不足与局限
*   **数据局限性**：MTEvent 数据集存在严重的类别不平衡；测试集场景过少，可能导致评估结果的泛化性参考价值受限。
*   **架构单一**：仅评估了 YOLOv8 系列，未对比基于 Transformer（如 RVT）或其他更先进的事件处理架构。
*   **实验稳健性**：缺乏多次重复实验的统计学支持。
*   **传感器利用**：仅使用了左侧事件流，未利用 MTEvent 提供的双目或 RGB 融合信息。

（完）
