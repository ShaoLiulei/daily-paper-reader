---
title: "ConsRoute:Consistency-Aware Adaptive Query Routing for Cloud-Edge-Device Large Language Models"
title_zh: ConsRoute：面向云-边-端大语言模型的一致性感知自适应查询路由
authors: "Haoyu Qiao, Hao Zhang, Shanwen Mao, Siyao Cheng, Jie Liu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21237v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 云-边-端大语言模型的自适应查询路由
tldr: "针对云-边-端协作的大语言模型推理中延迟和成本高的问题，本文提出ConsRoute框架。该框架通过重排序器评估不同层级模型响应的语义一致性，提供细粒度路由信号。它复用LLM预填充阶段的隐藏状态作为查询表示以降低开销，并利用聚类和贝叶斯优化动态调整路由阈值。实验表明，ConsRoute在保持95%以上云端性能的同时，降低了约40%的延迟和成本。"
selection_source: fresh_fetch
motivation: 现有的云-边-端协作推理路由方法依赖粗粒度的质量预测，且在资源受限的设备端存在较高的计算开销。
method: 提出一种基于语义一致性评估的轻量级路由框架，通过复用LLM隐藏状态并结合聚类与贝叶斯优化实现自适应路由。
result: "在保持接近云端模型性能（≥95%）的前提下，将端到端延迟和推理成本降低了近40%，优于现有基准。"
conclusion: ConsRoute通过细粒度的语义一致性感知和高效的特征复用，实现了云-边-端协同推理中质量与效率的最优平衡。
---

## 摘要
大语言模型（LLMs）展现了令人印象深刻的能力，但其推理延迟和成本也十分巨大，这阻碍了它们在延迟敏感和资源受限场景下的部署。云-边-端协同推理通过在不同层级间动态地将查询路由到不同能力的模型，已成为一种极具前景的范式。在本文中，我们提出了 ConsRoute，这是一个轻量级、语义感知且自适应的路由框架，它在显著提高推理效率的同时，最大限度地减少了对响应质量的影响。与以往依赖预测粗粒度输出质量差异的路由方法不同，ConsRoute 利用重排序器（reranker）直接评估不同层级模型生成的响应之间的语义一致性，从而为路由提供细粒度的软监督信号。为了最小化设备端开销，ConsRoute 复用了 LLM 预填充（prefilling）阶段的隐藏状态作为紧凑的查询表示，避免了额外的编码器或推理过程。此外，这些表示被进行聚类，并采用贝叶斯优化来学习特定于簇的路由阈值，从而在异构查询分布下动态平衡质量、延迟和成本。广泛的实验表明，ConsRoute 在实现接近云端性能（>=95%）的同时，将端到端延迟和推理成本降低了近 40%，在响应质量和系统效率方面均持续优于现有的路由基准方法。

## Abstract
Large language models (LLMs) deliver impressive capabilities but incur substantial inference latency and cost, which hinders their deployment in latency-sensitive and resource-constrained scenarios. Cloud-edge-device collaborative inference has emerged as a promising paradigm by dynamically routing queries to models of different capacities across tiers. In this paper, we propose ConsRoute, a lightweight, semantic-aware, and adaptive routing framework that significantly improves inference efficiency while minimizing impact on response quality. Unlike prior routing methods that rely on predicting coarse-grained output quality gaps, ConsRoute leverages a reranker to directly assess the semantic consistency between responses generated by models at different tiers, yielding fine-grained soft supervision signals for routing. To minimize device-side overhead, ConsRoute reuses hidden states from the LLM prefilling stage as compact query representations, avoiding additional encoders or inference passes. Furthermore, these representations are clustered, and Bayesian optimization is employed to learn cluster-specific routing thresholds that dynamically balance quality, latency, and cost under heterogeneous query distributions. Extensive experiments demonstrate that ConsRoute achieves near-cloud performance (>=95%) while reducing end-to-end latency and inference cost by nearly 40%, consistently outperforming existing routing baselines in both response quality and system efficiency.

---

## 论文详细总结（自动生成）

### ConsRoute：面向云-边-端大语言模型的一致性感知自适应查询路由

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）规模的增加，推理延迟和计算成本成为其在资源受限设备（如手机、IoT设备）上部署的主要障碍。**云-边-端协同推理**通过将简单查询留在本地（端）、中等查询发往边缘（边）、复杂查询路由至云端（云）来平衡性能与成本。
然而，现有的路由方法存在三个核心缺陷：
*   **监督信号粗糙**：通常依赖标量质量分数（如BartScore）的差异，忽略了响应之间细粒度的语义一致性。
*   **端侧开销大**：往往需要额外的编码器（如BERT）或外部API来提取查询特征，增加了推理延迟。
*   **阈值静态**：使用全局统一的路由阈值，无法适应异构的查询分布和动态变化的网络环境。

#### 2. 方法论：核心思想与关键技术
ConsRoute 提出了一个轻量级、语义感知且自适应的路由框架，其核心组件包括：
*   **语义一致性监督（Semantic Consistency）**：在离线阶段，利用预训练的重排序器（Reranker）直接测量端侧模型与更强模型输出之间的语义相似度，并结合正确性规则和高级LLM（如DeepSeek V3）的判断生成“软标签”，作为路由器的监督信号。
*   **提示词引导的特征复用（Prompt-Guided Representation）**：为了消除额外编码器的开销，ConsRoute 复用了端侧模型（DLM）预填充阶段的隐藏状态。通过在查询后附加特定提示词（如“上述查询的一致性是：”），提取最后一个Token的隐藏状态作为查询的语义表示。
*   **基于聚类的自适应阈值（Cluster-Based Adaptive Thresholding）**：
    *   将查询在表示空间进行聚类（K-means）。
    *   利用**贝叶斯优化（BO）**为每个簇学习最优的路由阈值（$\tau_1, \tau_2$），以最大化包含准确性、延迟和成本的效用函数。
*   **在线自适应机制**：在部署阶段，根据实时反馈（如用户反馈或自动验证器）增量更新贝叶斯优化模型，使路由策略能随网络状况和工作负载漂移而动态调整。

#### 3. 实验设计
*   **数据集**：使用了 **RouterBench** 基准测试，涵盖 MMLU（通用知识）、GSM8K（数学推理）、HumanEval（代码生成）和 MT-Bench（对话能力）。
*   **模型配置**：
    *   **端侧 (DLM)**：Qwen3-1.7B。
    *   **边缘 (ELM)**：Qwen3-14B。
    *   **云端 (CLM)**：Qwen3-32B。
    *   **异构测试**：额外测试了 Llama-3.2-3B (端) + Qwen3-14B (边) + DeepSeek-V3 (云) 的组合。
*   **对比方法**：包括 Device-only、Edge-only、Cloud-only，以及主流路由算法 RouteLLM (BERT/SW ranking) 和 MixLLM。
*   **场景模拟**：模拟了“良好”和“恶劣”两种网络带宽与延迟环境。

#### 4. 资源与算力
*   **硬件设备**：
    *   **端侧**：Intel i5-12500H CPU + NVIDIA RTX 3050 GPU。
    *   **边缘/云端**：NVIDIA RTX A6000 GPU（云端使用2块）。
*   **训练开销**：路由器的核心是一个极轻量级的 MLP（仅 5M 参数），训练开销极低。
*   **推理开销**：由于复用了 DLM 的预填充状态，路由决策的额外延迟仅为 15-20ms 左右，远低于使用 BERT 编码器的方案。

#### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **性能对比**：在四个主流基准数据集上验证了准确性、延迟和成本的权衡。
*   **消融实验**：验证了标签增强（Augmentation）和自适应阈值（Adaptive Threshold）的有效性。
*   **敏感性分析**：探讨了不同提示词策略、监督信号来源（Reranker vs Reward Model）、效用函数权重以及网络条件变化对结果的影响。
*   **跨模型族测试**：验证了框架在不同架构模型组合下的泛化能力。
实验设计客观、公平，通过归一化指标清晰地展示了 ConsRoute 在帕累托前沿（Pareto frontier）上的优势。

#### 6. 主要结论与发现
*   **高效性**：ConsRoute 在保持云端模型 **95% 以上性能**的同时，将端到端延迟和推理成本降低了约 **40%**。
*   **语义感知的优越性**：使用 Reranker 提供的语义一致性信号比传统的质量分数差异（Quality Gap）更能准确指导路由。
*   **低开销路由**：复用端侧 LLM 内部状态不仅节省了内存和计算资源，且在路由准确率上优于专门的轻量级编码器（如 BERT-base）。
*   **动态适应性**：在线贝叶斯优化能有效应对网络波动，在网络变差时自动增加端侧处理比例以维持响应速度。

#### 7. 优点
*   **架构优雅**：巧妙地将路由特征提取与 LLM 自身的预填充过程结合，实现了“零额外编码开销”。
*   **细粒度控制**：通过聚类和特定簇的阈值优化，解决了不同类型任务对模型能力需求差异大的问题。
*   **实用性强**：考虑了现实中的网络波动和异构模型部署，具有很高的工程参考价值。

#### 8. 不足与局限
*   **离线标签依赖**：训练阶段仍需要高性能重排序器和高级 LLM 生成标签，这增加了数据准备阶段的成本。
*   **反馈延迟**：在线自适应机制依赖于对响应质量的反馈（如用户点击或自动评测），在某些缺乏即时反馈的闭环场景中，阈值更新可能会滞后。
*   **聚类假设**：路由效果部分取决于初始聚类的质量，若推理时的查询分布与训练时的聚类中心偏差过大，可能影响初期表现。

（完）
