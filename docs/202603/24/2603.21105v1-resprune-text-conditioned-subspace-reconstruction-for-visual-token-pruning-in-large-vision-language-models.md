---
title: "ResPrune: Text-Conditioned Subspace Reconstruction for Visual Token Pruning in Large Vision-Language Models"
title_zh: ResPrune：大视觉语言模型中用于视觉 Token 剪枝的文本条件子空间重构
authors: "Xu Li, Yi Zheng, Yuxuan Liang, Zhe Liu, Xiaolei Chen, Haotian Chen, Rui Zhu, Xiangyang Xue"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21105v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大型视觉语言模型的视觉标记剪枝
tldr: ResPrune是一个针对大语言视觉模型（LVLM）的无须训练的视觉Token剪枝框架。它将剪枝建模为子空间重构问题，通过残差能量引导的贪婪扩展策略，并结合文本相关性，筛选出紧凑且信息丰富的Token子集。该方法具有模型无关性，能显著降低推理延迟和显存占用，同时在多个主流模型上保持了优异的性能。
selection_source: fresh_fetch
motivation: 针对大语言视觉模型中密集视觉Token导致的巨大计算和内存开销问题，寻求一种高效且无需重训的剪枝方案。
method: 将视觉Token剪枝视为子空间重构问题，利用残差能量引导的贪婪策略并结合文本指令相关性来选择关键Token。
result: 在LLaVA和Qwen等多个模型上的实验表明，ResPrune在显著减少计算量和延迟的同时，性能优于现有的剪枝方法。
conclusion: ResPrune提供了一种轻量级、通用的视觉Token压缩方案，在不改变模型架构的情况下实现了高效的LVLM推理。
---

## 摘要
大视觉语言模型（LVLMs）依赖密集的视觉 Token 来捕捉细粒度的视觉信息，但在推理过程中处理所有这些 Token 会产生巨大的计算和内存开销。为了解决这一问题，我们提出了 ResPrune，这是一个无需训练的视觉 Token 剪枝框架，通过选择一个紧凑且信息丰富的视觉 Token 子集来实现高效的 LVLM 推理。ResPrune 将视觉 Token 剪枝建模为子空间重构问题，并采用一种由残差能量引导的贪婪子空间扩展策略，从而能够保留原始视觉 Token 空间的几何结构。为了进一步结合跨模态对齐，选择过程以文本相关性为条件，鼓励保留既具有信息量又与指令相关的 Token。该方法轻量且与模型无关，可以无缝集成到现有的 LVLM 流水线中，无需重新训练或修改架构。在包括 LLaVA-1.5、LLaVA-NeXT 和 Qwen2.5-VL 在内的多个 LVLM 骨干网络上的广泛实验表明，ResPrune 在各种基准测试中始终优于现有的剪枝方法，同时有效降低了计算量、内存消耗和推理延迟。

## Abstract
Large Vision-Language Models (LVLMs) rely on dense visual tokens to capture fine-grained visual information, but processing all these tokens incurs substantial computational and memory overhead during inference. To address this issue, we propose ResPrune, a training-free visual token pruning framework that enables efficient LVLM inference by selecting a compact yet informative subset of visual tokens. ResPrune formulates visual token pruning as a subspace reconstruction problem and employs a greedy subspace expansion strategy guided by residual energy, allowing it to preserve the geometric structure of the original visual token space. To further incorporate cross modal alignment, the selection process is conditioned on textual relevance, encouraging the retention of tokens that are both informative and instruction-relevant. The proposed method is lightweight and model-agnostic, and can be seamlessly integrated into existing LVLM pipelines without retraining or architectural modifications. Extensive experiments on multiple LVLM backbones, including LLaVA-1.5, LLaVA-NeXT, and Qwen2.5-VL, demonstrate that ResPrune consistently outperforms existing pruning approaches across a wide range of benchmarks, while achieving effective reductions in computation, memory consumption, and inference latency.

---

## 论文详细总结（自动生成）

### 论文总结：ResPrune —— 大视觉语言模型中用于视觉 Token 剪枝的文本条件子空间重构

#### 1. 核心问题与整体含义（研究动机和背景）
现代大视觉语言模型（LVLMs）为了捕捉细粒度信息，通常将图像编码为数百甚至上万个视觉 Token。由于 Transformer 的自注意力机制具有平方级计算复杂度，密集的 Token 导致了巨大的计算开销和内存占用，限制了其在低延迟或资源受限场景下的应用。
现有的剪枝方法存在局限性：
*   **基于视觉注意力：** 容易导致语义冗余，且缺乏指令引导。
*   **基于跨模态注意力：** 存在位置偏差，且提取注意力权重会破坏 FlashAttention 等优化内核的效率。
*   **基于多样性：** 往往与任务无关，无法针对特定查询保留关键信息。
**ResPrune 的动机**在于提出一种既能保证视觉覆盖范围、又能响应文本指令、且无需重新训练的通用剪枝框架。

#### 2. 方法论
ResPrune 将视觉 Token 剪枝重新表述为一个**子空间重构（Subspace Reconstruction）**问题。
*   **核心思想：** 寻找一个视觉 Token 子集，使其张成的子空间能够最大限度地重构原始全量 Token 空间。
*   **关键技术细节：**
    1.  **残差能量引导的贪婪扩展：** 算法迭代式地选择 Token。在每一步中，计算候选 Token 相对于当前已选子空间的“残差能量”（即无法被当前子空间解释的新信息量），优先选择残差能量最高的 Token。
    2.  **文本相关性调节：** 为了使剪枝具有指令感知能力，利用视觉和文本 Token 在 LLM 输入层处于共享嵌入空间的特性，计算视觉 Token 与指令中名词的余弦相似度。
    3.  **文本引导门控：** 使用文本相关性得分对残差能量进行加权调制，从而优先保留既能提供新视觉信息又与当前问题相关的 Token。
    4.  **高效实现：** 通过施密特正交化（Gram-Schmidt）维护正交基，实现残差能量的快速增量更新，无需复杂的矩阵求逆。

#### 3. 实验设计
*   **骨干模型：** LLaVA-1.5-7B（固定分辨率）、LLaVA-NeXT-7B（切片策略）、Qwen2.5-VL-7B（动态分辨率）。
*   **数据集/基准测试：** 涵盖 9 个主流基准，包括 VQAv2、VizWiz、GQA（通用问答）、SQA-I（科学推理）、TextVQA（场景文本）、POPE（物体幻觉评估）、MME、MMB-en、MM-Vet（综合能力评估）。
*   **对比方法：** 对比了包括 ToMe、FastV、PruMerge、SparseVLM、DART、VisionZIP、DivPrune、SCOPE 等在内的十余种近年发表于顶会（CVPR, ICCV, NeurIPS 等）的先进剪枝方法。

#### 4. 资源与算力
*   **训练需求：** 该方法为 **Training-free（无需训练）**，不需要任何微调或架构修改。
*   **推理测试算力：** 效率分析实验在 **4 台 NVIDIA L20 GPU** 上进行。
*   **效率提升：** 在 LLaVA-NeXT-7B 上，剪枝 88.9% 的 Token 可使预填充阶段的 FLOPs 降低约 90%，KV 缓存减少 88.9%，吞吐量提升至 2.28 倍。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 3 个不同架构的模型上进行了测试，涵盖了从 66.7% 到 88.9% 的多种剪枝比例。
*   **消融实验：** 进行了详尽的消融研究，验证了子空间重构、文本引导、文本预处理、引导强度参数 $\alpha$、种子 Token 选择策略以及文本相关性计算公式的有效性。
*   **客观性：** 实验覆盖了多种任务类型（从基础感知到高级推理），并使用了标准化的评估指标，对比基线涵盖了当前该领域几乎所有主流技术路线，实验设计严谨且公平。

#### 6. 主要结论与发现
*   **性能保持：** ResPrune 在大幅剪枝的情况下表现出极强的鲁棒性。例如在 LLaVA-1.5 上剪掉 77.8% 的 Token 后，仍能保留 99.3% 的原始性能。
*   **超越基准：** 在某些任务（如 POPE 物体幻觉测试）中，剪枝后的模型性能甚至超过了全量 Token 模型，这表明 ResPrune 有效过滤了可能导致幻觉的视觉噪声。
*   **通用性：** 无论模型是否具有 CLS Token，或者是否采用动态分辨率，ResPrune 均能稳定提升推理效率并保持高精度。

#### 7. 优点
*   **理论严谨：** 相比于启发式的注意力得分，基于子空间重构的公式化方法更具数学解释性。
*   **指令感知：** 无需进入 LLM 内部提取权重即可实现文本引导，避免了额外的推理延迟。
*   **兼容性强：** 与 FlashAttention 等硬件加速技术完全兼容，且适用于各种 LVLM 架构。
*   **轻量高效：** 剪枝过程本身的计算开销远小于其节省的 LLM 推理开销。

#### 8. 不足与局限
*   **超参数敏感性：** 文本引导强度 $\alpha$ 在不同模型家族间需要手动微调（如 LLaVA 设为 0.75，Qwen 设为 0.3）。
*   **文本处理依赖：** 依赖于轻量级 NLP 工具提取名词，如果指令极其复杂或缺乏明确名词，引导效果可能受限。
*   **静态权重：** 目前 $\alpha$ 是全局固定的，未能根据不同图像的复杂程度或指令的特异性实现动态自适应调节。

（完）
