---
title: "DRTriton: Large-Scale Synthetic Data Reinforcement Learning for Triton Kernel Generation"
title_zh: DRTriton：用于 Triton Kernel 生成的大规模合成数据强化学习
authors: "Siqi Guo, Ming Lin, Tianbao Yang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21465v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于大模型代码生成的强化学习
tldr: 针对高效CUDA算子开发难题，本文提出DRTriton框架，利用大规模合成数据和强化学习将PyTorch代码自动转换为高性能Triton算子。该框架包含CSP-DAG合成算法、解耦奖励的课程强化学习以及推理时搜索算法。实验表明，仅在合成数据上训练的DRTriton-7B在KernelBench Level 2任务中表现远超GPT-5.2和Claude-Sonnet-4.5，展现了极强的泛化能力和优化性能。
selection_source: fresh_fetch
motivation: 现有大语言模型在将PyTorch代码转换为高效CUDA/Triton算子方面仍面临巨大挑战，难以满足生成式AI工业界对高性能算子的需求。
method: 提出包含CSP-DAG合成算法、解耦奖励的课程强化学习以及推理时搜索算法的DRTriton框架，实现从合成数据生成到算子优化的全流程。
result: "DRTriton-7B在KernelBench Level 2中实现了92%的加速成功率，显著优于GPT-5.2（23%）和Claude-Sonnet-4.5（19%）。"
conclusion: 通过大规模合成数据与强化学习相结合，可以有效训练出在算子生成任务上超越顶尖通用大模型的专用模型，并具备优秀的现实场景泛化能力。
---

## 摘要
开发高效的 CUDA kernel 是生成式人工智能行业中一项基础且具有挑战性的任务。最近的研究利用大语言模型 (LLMs) 将 PyTorch 参考实现自动转换为 CUDA kernel，从而显著减少了工程工作量。然而，即使是目前最先进的 LLM（如 GPT-5.2 和 Claude-Sonnet-4.5）在这一特定任务上仍然面临困难。为了应对这一挑战，我们提出了 DRTriton，这是一个可扩展的学习框架，用于训练 LLM 将 PyTorch 代码转换为高度优化的 Triton kernel，这些 kernel 随后在运行时被编译为 CUDA kernel。DRTriton 由三个关键组件组成：(i) 一种名为 CSP-DAG 的数据合成算法，它保证了在难度可控的情况下，对算子空间进行全覆盖和无偏均匀采样；(ii) 一种具有解耦奖励的课程强化学习，可同时高效地优化转换成功率和推理速度；(iii) 一种测试时搜索算法，进一步提高了生成的 Triton kernel 的推理速度。值得注意的是，尽管完全在合成数据上进行训练，DRTriton 仍能有效地泛化到现实世界中即使对人类专家也具有挑战性的 CUDA kernel。实验结果表明，DRTriton-7B 在 KernelBench Level 2 的 92% 任务上实现了加速，相比之下，GPT-5.2 的加速比例为 23%，Claude-Sonnet-4.5 为 19%。

## Abstract
Developing efficient CUDA kernels is a fundamental yet challenging task in the generative AI industry. Recent researches leverage Large Language Models (LLMs) to automatically convert PyTorch reference implementations to CUDA kernels, significantly reducing the engineering efforts. State-of-the-art LLMs, such as GPT-5.2 and Claude-Sonnet-4.5, still struggle in this specific task. To address this challenge, we propose DRTriton, a scalable learning framework for training LLMs to convert PyTorch codes into highly optimized Triton kernels, which are then compiled to CUDA kernels at runtime. DRTriton consists of three key components: (i) a data synthetic algorithm CSP-DAG that guarantees full coverage and unbiased uniform sampling over the operator space with controlled difficulty; (ii) a curriculum reinforcement learning with decoupled reward efficiently optimizes conversion success rate and inference speed simultaneously; and (iii) a test-time search algorithm that further improves the inference speed of the generated Triton kernels. Notably, despite being trained exclusively on synthetic data, DRTriton generalizes effectively to real-world CUDA kernels that are challenging even for human experts. Experimental results show that DRTriton-7B achieves speedup on 92% of the KernelBench Level 2, compared to 23% for GPT-5.2 and 19% for Claude-Sonnet-4.5.

---

## 论文详细总结（自动生成）

### DRTriton：用于 Triton Kernel 生成的大规模合成数据强化学习

#### 1. 论文的核心问题与整体含义（研究动机和背景）
在生成式 AI 行业中，开发高效的 CUDA 算子（Kernel）是提升推理效率的关键，但其门槛极高，即使是专家也需耗费大量时间。虽然 Triton 语言降低了开发难度，但编写高性能 Triton 代码仍需深厚的 GPU 编程知识。
目前利用大语言模型（LLM）自动将 PyTorch 转换为 Triton 的研究面临三大瓶颈：
*   **训练数据规模有限**：现有数据集通常仅包含万级样本，不足以让模型掌握复杂的算子逻辑。
*   **质量与难度不可控**：从公开代码库收集的数据往往难度分布不均，导致强化学习（RL）初期因奖励稀疏而难以收敛。
*   **通用模型性能不足**：即使是 GPT-5.2 或 Claude-Sonnet-4.5，在处理复杂算子融合和性能优化时仍表现欠佳。

#### 2. 论文提出的方法论
DRTriton 框架通过“合成数据+课程强化学习”的思路，实现了从简单算子到复杂融合算子的自动化生成。
*   **CSP-DAG 合成算法**：
    *   **核心思想**：将 PyTorch 程序生成建模为有向无环图（DAG）上的约束满足问题（CSP）。
    *   **流程**：首先随机生成算子节点并连边，然后利用 Google 的 CP-SAT 求解器，在满足张量维度（Broadcasting 规则）、算子特定约束（如卷积步长、填充）以及显存/计算量限制的前提下，自动确定所有张量的具体 Shape。
    *   **意义**：保证了合成程序的语法正确性和张量形状兼容性，实现了对算子空间的无偏均匀采样。
*   **解耦奖励的课程强化学习（DRPO）**：
    *   **解耦奖励**：将奖励分为“正确性”和“速度”两部分。在 DRPO 框架下，模型首先被要求生成正确的代码，随后通过速度诱导的权重（Speed-induced weight）来优化推理耗时。
    *   **课程学习**：分为三个阶段进行 RL 训练：Level 1（单算子，2万样本）、Level 2（双算子序列，6万样本）、Level 5（五算子序列，2万样本）。这种由易到难的策略解决了 RL 初期的奖励稀疏问题。
*   **推理时搜索（Test-time Search）**：
    *   针对超长程序（如 20 个算子），模型难以一次性生成单个高效 Kernel。该算法将长程序分解为多个片段（Fragment），为每个片段生成 Kernel 并验证，最后搜索出最优的 Kernel 组合方案。

#### 3. 实验设计
*   **数据集/场景**：
    *   **合成基准（Synthetic Benchmark）**：包含 406 个未见过的 PyTorch 程序，涵盖 Level 1、2、5、20 四个难度等级。
    *   **现实基准（KernelBench）**：包含 250 个真实神经网路操作，分为单算子（L1）、融合模式（L2）和完整架构（L3，如 MobileNet、VGG）。
*   **对比方法**：
    *   **商业模型**：GPT-5.2、Claude-Sonnet-4.5。
    *   **开源模型**：DeepSeek-R1、Qwen-3-Coder-480B。
    *   **专用模型**：AutoTriton。
*   **评估指标**：准确率（Acc）、加速成功率（Faster1，即正确且比 PyTorch 原生实现快）、几何平均加速比。

#### 4. 资源与算力
*   **基础模型**：Qwen-2.5-Coder-7B-Instruct。
*   **训练细节**：
    *   **SFT 阶段**：使用 2,026 个单算子对训练 10 个 Epoch。
    *   **RL 阶段**：每个课程阶段训练 1 个 Epoch，学习率为 $1 \times 10^{-6}$，每个 Prompt 生成 8 个 Rollout。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但提到 CSP-DAG 算法在单核 CPU 上仅需 1-2 秒即可生成一个样本。

#### 5. 实验数量与充分性
*   **实验规模**：RL 阶段使用了总计 10 万个合成样本，测试集涵盖了从简单到极复杂的多种场景。
*   **充分性与客观性**：
    *   进行了 **RL 算法消融**（DRPO vs GRPO），证明了解耦奖励在代码生成任务中的优越性。
    *   进行了 **奖励函数消融**（对数函数 vs 幂函数），确定了最优的速度评估方式。
    *   使用了 **KernelBench** 这一第三方真实基准，证明了模型虽然只在合成数据上训练，但具备极强的泛化能力。
    *   实验设计考虑了“Monkey-patch”测试，防止模型通过直接调用 PyTorch 算子来“作弊”。

#### 6. 论文的主要结论与发现
*   **合成数据胜过真实数据**：仅靠 CSP-DAG 生成的合成数据进行 RL，DRTriton-7B 的表现就超过了在真实代码库上训练的模型。
*   **性能大幅领先**：在 KernelBench Level 2 中，DRTriton 实现了 92% 的加速成功率，而 GPT-5.2 仅为 23%。
*   **泛化能力强**：模型能够处理复杂的现实架构（如 Level 3 的完整模型），在 34% 的任务中甚至超过了 `torch.compile` 的优化效果。
*   **搜索算法的必要性**：推理时搜索显著提升了长程序的处理能力，使 Level 20 的准确率从 0 提升至 99%。

#### 7. 优点
*   **数据生成创新**：CSP-DAG 解决了代码合成中“合法性”与“多样性”的矛盾，为垂直领域代码模型训练提供了新范式。
*   **训练策略高效**：通过课程学习和解耦奖励，成功在 7B 小模型上诱导出了超越闭源大模型的底层优化能力。
*   **实用性强**：提供了自动重写工具，能将现实中的面向对象 PyTorch 代码转换为模型可处理的函数式格式。

#### 8. 不足与局限
*   **算子覆盖范围**：虽然涵盖了 53 种常用算子，但对于一些极其冷门或自定义的复杂算子，合成算法可能尚未完全覆盖。
*   **硬件依赖**：目前的优化目标主要针对特定 GPU 环境，生成的 Triton 代码在不同架构硬件上的可移植性和性能一致性有待进一步验证。
*   **搜索开销**：推理时搜索虽然效果好，但会增加推理阶段的计算成本和时间延迟。

（完）
