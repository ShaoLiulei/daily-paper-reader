---
title: Stabilizing Iterative Self-Training with Verified Reasoning via Symbolic Recursive Self-Alignment
title_zh: 通过符号递归自对齐，利用经过验证的推理稳定迭代自训练
authors: Xinyu Zhang
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21558v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 稳定模型的迭代自训练
tldr: 针对迭代自训练中模型因错误累积导致性能退化的“递归漂移”问题，本文提出神经符号递归自对齐（NSRSA）框架。该方法通过嵌入符号验证子系统，在推理步骤层面过滤训练数据，利用sympy验证算术运算并检查逻辑一致性。实验表明，NSRSA能有效剔除“侥幸猜对”的错误推理，并通过DPO显著提升模型区分正确推理的能力，为实现可靠的递归自我提升提供了可扩展框架。
selection_source: fresh_fetch
motivation: 解决模型在迭代自训练过程中因推理错误累积而导致的递归漂移和性能退化问题。
method: 提出NSRSA框架，利用符号验证子系统在推理步骤级别对自生成数据进行质量把关和逻辑校验。
result: "NSRSA成功剔除了34%虽答案正确但推理错误的样本，并使DPO偏好对齐的奖励准确率从46%提升至63%。"
conclusion: 外部符号验证能有效稳定迭代自训练过程，使特定领域内的模型递归自我提升变得可衡量且更可靠。
---

## 摘要
递归自我提升——即模型在其自身输出上进行迭代训练——有望实现能力的持续增长，但面临着一个根本性障碍：递归漂移。随着模型在多次迭代中利用自生成数据进行训练，中间推理过程中的错误会不断累积，导致模式崩溃和性能下降。我们提出了神经符号递归自对齐（NSRSA），通过嵌入一个符号验证子系统，在推理步骤层面把控训练数据质量，从而稳定迭代自训练。与仅针对结果的过滤（这种方式会接纳带有错误推理的“侥幸猜中”）不同，NSRSA 通过 sympy 验证每个算术运算，检查推理步骤间的逻辑流一致性，并强制执行领域约束。我们在 GSM8K 数据集上使用 Qwen3-4B-Thinking 模型，在五种条件下进行了 5 次自训练迭代评估：无验证、结果验证、多数投票、完整的 NSRSA 符号验证以及结合 DPO 的 NSRSA。我们的过滤分析表明，NSRSA 拒绝了约 34% 通过结果验证但答案正确的解法，从而从训练集中消除了带有错误推理的“侥幸猜中”。我们进一步证明，利用 NSRSA 验证构建 DPO 偏好对，可以教会模型区分正确与错误的推理（奖励准确率从 46% 提升至 63%）。NSRSA 提供了一个可扩展的框架，展示了在可进行自动验证的领域中，外部符号验证如何使递归自我提升变得可衡量且可靠。

## Abstract
Recursive self-improvement--where a model iteratively trains on its own outputs--promises sustained capability growth but faces a fundamental obstacle: recursive drift. As models train on self-generated data across multiple iterations, errors in intermediate reasoning compound, leading to mode collapse and performance degradation. We propose Neuro-Symbolic Recursive Self-Alignment (NSRSA), which stabilizes iterative self-training by embedding a symbolic verification subsystem that gates training data quality at the reasoning step level. Unlike outcome-only filtering (which admits "lucky guesses" with flawed reasoning), NSRSA verifies each arithmetic operation via sympy, checks logical flow consistency across reasoning steps, and enforces domain constraints. We evaluate NSRSA on GSM8K using Qwen3-4B-Thinking across 5 self-training iterations under five conditions: no verification, outcome verification, majority voting, full NSRSA symbolic verification, and NSRSA with DPO. Our filtering analysis shows that NSRSA rejects approximately 34% of correct-answer solutions that pass outcome verification, eliminating "lucky guesses" with flawed reasoning from the training set. We further demonstrate that constructing DPO preference pairs from NSRSA verification teaches the model to distinguish sound from flawed reasoning (reward accuracy 46% to 63%). NSRSA provides an extensible framework that demonstrates how external symbolic verification can make recursive self-improvement measurable and reliable within domains where automated verification is available.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stabilizing Iterative Self-Training with Verified Reasoning via Symbolic Recursive Self-Alignment》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：递归自我提升（Recursive Self-Improvement, RSI）允许模型在自身生成的数据上迭代训练以增强能力。然而，该过程面临**“递归漂移”（Recursive Drift）**的根本挑战：模型可能通过错误的推理步骤得到正确的最终答案（即“侥幸猜对”）。如果将这些包含错误逻辑的样本加入训练集，错误会随迭代累积，导致模型性能退化或模式崩溃。
*   **核心问题**：如何在不需要大规模人类标注的情况下，在迭代自训练过程中识别并过滤掉推理逻辑错误的样本，从而实现稳定的能力增长？

### 2. 论文提出的方法论：NSRSA
*   **核心思想**：提出**神经符号递归自对齐（NSRSA）**框架，通过在自训练循环中嵌入一个**符号验证子系统**，在“推理步骤”级别（而非仅结果级别）对数据进行质量把关。
*   **关键技术细节（四个验证维度）**：
    1.  **答案正确性**：提取最终答案并与 Ground Truth 对比。
    2.  **算术验证**：利用 `sympy` 库解析思维链（CoT）中的算术表达式（如 $A \odot B = C$），验证计算是否准确。要求正确率 $\ge 80\%$。
    3.  **逻辑流验证**：跟踪推理步骤中的变量赋值。如果同一变量在非相邻步骤中出现大幅度数值跳变（$>50\%$ 相对变化），则判定为逻辑幻觉。
    4.  **约束满足**：强制执行领域约束，如计数变量必须为非负数、离散物体必须为整数等。
*   **DPO 变体**：利用验证结果构建偏好对——“通过全部验证的解”作为正例，“答案正确但验证失败的解”作为负例，通过直接偏好优化（DPO）教会模型区分稳健推理与侥幸猜中。

### 3. 实验设计
*   **数据集**：
    *   **GSM8K**：小学数学应用题，作为主要的训练和评估基准。
    *   **MATH-500**：MATH 数据集的子集，用于测试跨任务的泛化迁移能力。
*   **对比方法（Baseline）**：
    1.  **无验证（No Verification）**：随机采样自生成解，不进行过滤。
    2.  **结果验证（Outcome Verification）**：仅根据最终答案是否正确进行过滤（类似 STaR 方法）。
    3.  **多数投票（Majority Voting）**：基于自一致性原则，选择模型输出中最频繁的答案。
    4.  **NSRSA (Symbolic)**：本文提出的全量符号验证 SFT。
    5.  **NSRSA + DPO**：在 NSRSA 基础上增加偏好对齐训练。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 块 NVIDIA A10G GPU** (每块 24GB VRAM)。
*   **模型规模**：基础模型为 **Qwen3-4B-Thinking**。
*   **训练细节**：采用 LoRA 微调（r=16, alpha=32），每轮迭代训练 1 个 epoch。
*   **时长与开销**：DPO 训练约耗时 2.25 小时；符号验证过程每轮增加约 10 分钟 CPU 时间，相对于生成和训练时间几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：进行了 **5 次完整的自训练迭代**，每轮迭代生成 8 个候选解（共约 6 万个样本）。
*   **实验维度**：涵盖了准确率曲线、验证通过率分析、跨任务迁移实验、解析器覆盖率消融实验以及多样性（Self-BLEU）分析。
*   **充分性评价**：实验设计较为全面，通过对比多种主流自训练策略，客观地展示了符号验证在抑制模型崩溃方面的作用。特别是对“侥幸猜对”样本的量化分析（剔除 34% 样本）有力支撑了其核心论点。

### 6. 论文的主要结论与发现
*   **性能提升**：NSRSA 使 Qwen3-4B 在 GSM8K 上的准确率从 80.5% 提升至 **91.0%**，而无验证策略在第 3 轮迭代即发生崩溃（降至 73.2%）。
*   **稳定性**：结果验证（Outcome-only）在 2 轮后进入平台期（约 86%），而 NSRSA 展现了更持久的递归提升能力。
*   **推理质量**：NSRSA 显著提高了模型生成“可验证推理”的比例，且在 MATH-500 上实现了 +5.7% 的正向迁移。
*   **多样性保持**：NSRSA 的 Self-BLEU 分数保持在较低水平（0.35），有效防止了模型陷入单一、脆弱的推理模式（模式崩溃）。

### 7. 优点
*   **粒度精细**：将验证从“结果”深入到“步骤”，解决了自训练中长期存在的“侥幸猜对”噪声问题。
*   **神经符号结合**：巧妙利用成熟的符号计算工具（sympy）作为外部 Oracle，为神经网络提供硬约束反馈。
*   **递归稳定性**：证明了外部验证是实现长期递归自我提升（RSI）的关键。
*   **可解释性**：通过四个维度的检查，使得模型改进的过程变得可衡量、可解释。

### 8. 不足与局限
*   **解析器局限性**：目前的符号解析主要依赖模式匹配，无法处理自然语言描述的复杂算式（如“五乘以三”），导致部分正确推理被“平庸通过”或漏检。
*   **领域局限性**：该方法目前高度依赖数学领域的符号特性，扩展到法律、医疗或通用逻辑领域需要重新设计复杂的验证器。
*   **初始模型依赖**：如果基础模型太弱，无法生成任何通过验证的样本，该方法可能会因过度过滤导致训练数据不足（文中提到了降级处理机制）。

（完）
