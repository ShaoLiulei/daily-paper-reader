---
title: "Sparse but not Simpler: A Multi-Level Interpretability Analysis of Vision Transformers"
title_zh: 稀疏但不简单：视觉 Transformer 的多层级可解释性分析
authors: Siyu Zhang
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15919v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 视觉 Transformer 的可解释性分析
tldr: 本研究探讨了视觉 Transformer (ViT) 的权重稀疏性与可解释性之间的关系。研究者利用 Wanda 剪枝的 DeiT-III 模型，引入了名为 IMPACT 的多层级评估框架，从神经元、层表示、任务电路和模型归因四个维度进行系统分析。结果表明，尽管稀疏模型生成的任务电路边数减少了约 2.5 倍，但在神经元选择性、SAE 特征可解释性及归因忠实度上并无显著改善。这证明了结构稀疏性并不等同于语义可解释性的提升，强调了多维度评估的重要性。
selection_source: fresh_fetch
motivation: 探究视觉 Transformer 中的结构稀疏性是否能如预期般提升模型的语义可解释性。
method: 提出 IMPACT 多层级框架，通过神经元分析、稀疏自编码器 (SAE)、电路提取和归因评估对剪枝模型进行全面测试。
result: 稀疏模型虽然显著减少了电路边数，但在神经元选择性、特征可解释性和归因忠实度方面并未表现出系统性优势。
conclusion: 结构稀疏性本身不足以保证模型更易于理解，未来的可解释性研究需超越单纯的电路紧凑度指标。
---

## 摘要
稀疏神经网络通常被假设比稠密模型更具可解释性，其动机源于在语言模型中发现权重稀疏性可以产生紧凑的电路。然而，结构稀疏性本身是否能提高语义可解释性仍不清楚。在这项工作中，我们使用经 Wanda 剪枝的 DeiT-III B/16 模型，系统地评估了视觉 Transformer 中权重稀疏性与可解释性之间的关系。为了全面评估可解释性，我们引入了 IMPACT，这是一个多层级框架，从四个互补的层面评估可解释性：神经元、层级表示、任务电路和模型级归因。层级表示使用 BatchTopK 稀疏自编码器进行分析，电路通过可学习的节点掩码提取，而解释则使用 Transformer 归因并通过插入和删除指标进行评估。我们的结果揭示了明显的结构效应，但可解释性的提升有限。稀疏模型产生的电路边数比稠密模型少约 2.5 倍，但活跃节点的比例保持相似或更高，这表明剪枝是重新分配了计算，而不是隔离出更简单的功能模块。与这一观察结果一致，稀疏模型在神经元级选择性、SAE 特征可解释性或归因忠实度方面没有表现出系统性的改进。这些发现表明，仅靠结构稀疏性并不能可靠地产生更具可解释性的视觉模型，强调了开发超越电路紧凑性来评估可解释性的框架的重要性。

## Abstract
Sparse neural networks are often hypothesized to be more interpretable than dense models, motivated by findings that weight sparsity can produce compact circuits in language models. However, it remains unclear whether structural sparsity itself leads to improved semantic interpretability. In this work, we systematically evaluate the relationship between weight sparsity and interpretability in Vision Transformers using DeiT-III B/16 models pruned with Wanda. To assess interpretability comprehensively, we introduce \textbf{IMPACT}, a multi-level framework that evaluates interpretability across four complementary levels: neurons, layer representations, task circuits, and model-level attribution. Layer representations are analyzed using BatchTopK sparse autoencoders, circuits are extracted via learnable node masking, and explanations are evaluated with transformer attribution using insertion and deletion metrics. Our results reveal a clear structural effect but limited interpretability gains. Sparse models produce circuits with approximately $2.5\times$ fewer edges than dense models, yet the fraction of active nodes remains similar or higher, indicating that pruning redistributes computation rather than isolating simpler functional modules. Consistent with this observation, sparse models show no systematic improvements in neuron-level selectivity, SAE feature interpretability, or attribution faithfulness. These findings suggest that structural sparsity alone does not reliably yield more interpretable vision models, highlighting the importance of evaluation frameworks that assess interpretability beyond circuit compactness.

---

## 论文详细总结（自动生成）

以下是对论文《Sparse but not Simpler: A Multi-Level Interpretability Analysis of Vision Transformers》的结构化深度总结：

### 1. 核心问题与整体含义
该论文探讨了**权重稀疏性（Weight Sparsity）与视觉 Transformer（ViT）可解释性之间的关系**。
*   **研究动机**：在语言模型中，稀疏性被认为能产生更紧凑、易于分析的电路。然而，在处理连续且高度相关的图像数据的 ViT 中，结构上的稀疏（参数减少）是否真的能带来语义上的简单（特征更清晰、逻辑更易懂）尚无定论。
*   **核心假设挑战**：论文挑战了“更小的电路即更具可解释性”的直觉，试图厘清结构紧凑性与语义透明度之间的脱节。

### 2. 方法论
论文提出了 **IMPACT**（可解释性多层级评估框架），从四个维度进行量化分析：
*   **神经元级（Neuron Level）**：使用选择性（AUROC）、消融影响、类方差和标签熵四个指标，衡量单个神经元是否趋向于单语义化。
*   **层级表示（Layer Level）**：利用 **BatchTopK 稀疏自编码器（SAE）** 将稠密激活分解为稀疏特征，评估特征的重构质量和可解释性。
*   **电路级（Circuit Level）**：通过**可学习的节点掩码（Node Masking）**提取特定类别的最小功能子图。引入了**归一化电路质量（NCQ）**指标，综合衡量电路的忠实度（Fidelity）与压缩率。
*   **模型级（Model Level）**：采用 Transformer 归因方法（Chefer et al.），并通过**插入（Insertion）和删除（Deletion）**曲线下面积（AUC）来评估输入归因图的忠实度。
*   **关键技术**：使用 **Wanda 剪枝**（结合权重大小与激活范数）对 DeiT-III 模型进行处理，并进行微调以恢复性能。

### 3. 实验设计
*   **模型与数据集**：以 **DeiT-III B/16** 为基准模型，在 **ImageNet-1K** 数据集上进行实验。
*   **对比基准**：对比了稠密模型与不同稀疏度（10% 到 90%）的稀疏模型。
*   **剪枝方法对比**：对比了量级剪枝（Magnitude Pruning）与 Wanda 剪枝。
*   **实验范围**：神经元、层级和模型级分析覆盖了 ImageNet 的全部 1000 个类别；电路提取由于计算量巨大，针对涵盖 13 个语义组的 100 个代表性类别进行。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   文中提到电路提取过程“计算成本高昂”（computationally expensive），并使用了贝叶斯优化（CARBS）来搜索超参数，暗示了实验具有相当大的计算规模。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 0.1 到 0.9 的多个稀疏梯度，对 SAE 的超参数（扩展因子、k 值）进行了详尽的消融实验。
*   **充分性与客观性**：实验设计非常全面，从微观（神经元）到宏观（模型输出）构建了完整的证据链。通过引入 NCQ 等新指标，避免了单一指标（如电路大小）带来的偏见。实验结果显示了跨类别的一致性，具有较强的说服力。

### 6 主要结论与发现
*   **结构稀疏 $\neq$ 语义简单**：虽然稀疏模型的电路边数减少了约 2.5 倍，但其使用的**节点比例反而更高**。这被称为“表示扩散”（Representation Spreading），即剪枝迫使模型将计算分布到更多剩余组件上，而非简化逻辑。
*   **特征纠缠持续存在**：在神经元和 SAE 特征层面，稀疏模型并未表现出更高的选择性或更低的标签熵，说明稀疏化未能有效解决“叠加态”（Superposition）问题。
*   **归因质量无显著提升**：稀疏模型生成的解释图在空间定位和语义接地方面与稠密模型相比没有系统性改善。
*   **结论总结**：结构上的稀疏化仅仅是重新分配了计算，并没有从根本上使视觉模型变得更易于人类理解。

### 7. 优点
*   **多维度视角**：IMPACT 框架提供了一个系统化的工具包，超越了以往仅关注电路大小的局限。
*   **严谨的量化分析**：引入 NCQ 指标，科学地平衡了电路的性能与紧凑性。
*   **直击痛点**：揭示了模型压缩（剪枝）与机械可解释性之间的复杂关系，纠正了领域内的普遍误区。

### 8. 不足与局限
*   **剪枝方式限制**：研究主要基于事后剪枝（Post-hoc Pruning），而非从头开始训练的稀疏网络（Sparse Training），后者的表示动力学可能有所不同。
*   **代理指标风险**：评估高度依赖于选择性、熵等代理指标，而非人类感知的真实概念标注。
*   **模型局限**：主要集中在有监督学习的 ViT，对于自监督（如 DINO）或多模态模型（如 CLIP）的可解释性表现尚待验证。

（完）
