---
title: "Mixture of Chapters: Scaling Learnt Memory in Transformers"
title_zh: 章节混合：扩展 Transformer 中的学习记忆
authors: "Tasmay Pankaj Tibrewal, Pritish Saha, Ankit Meda, Kunal Singh, Pradeep Moturi"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21096v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 通过交叉注意力和路由扩展 Transformer 记忆
tldr: 本研究针对 Transformer 缺乏显式知识存储机制的问题，提出了“章节混合”（Mixture of Chapters）架构。该方法引入了可学习的稀疏记忆库，通过类似混合专家（MoE）的路由机制将记忆划分为多个“章节”，使模型能以较低计算成本扩展至 262K 个记忆令牌。实验表明，该模型在预训练和指令微调任务中均优于同等计算量的基准模型，并展现出更强的知识保留能力和抗遗忘特性，为模型扩展提供了新维度。
selection_source: fresh_fetch
motivation: Transformer 架构缺乏显式的机制来存储和组织在训练过程中获取的知识。
method: 引入可学习的稀疏记忆库，并结合受 MoE 启发的章节路由机制，通过交叉注意力实现大规模记忆的高效检索。
result: 该模型在预训练和指令微调中均超越了同等计算量的基准模型，并成功将记忆容量扩展至 262K 个令牌。
conclusion: 显式关联记忆能与模型参数互补，有效提升知识保留能力并减少在不同训练阶段转换时的灾难性遗忘。
---

## 摘要
Transformer 缺乏一种显式的架构机制来存储和组织训练过程中获取的知识。我们引入了可学习的稀疏记忆库：一组随机初始化并端到端训练的潜标记（latent tokens），Transformer 层通过交叉注意力机制对其进行查询，以检索存储的知识。为了在不产生过高注意力计算成本的情况下扩展记忆容量，我们受专家混合（Mixture-of-Experts）架构启发，提出了基于章节的路由机制，将记忆库划分为多个章节，并训练一个路由器为每个输入选择相关的子集。这使得在保持计算可控的同时，能够将记忆扩展到 262K 个标记。我们在预训练和指令微调的相关基准测试中，将我们的方法与标准 Transformer（在等效 FLOPs 设置下）进行了评估。我们的模型超越了等效 FLOPs 的基准模型，这表明存在一个新的扩展维度，并证明了显式联想记忆提供了与模型参数中隐式捕获的知识互补的能力。此外，我们观察到在持续训练下知识保留能力有所提高，并且在不同训练阶段转换（例如从预训练到指令微调）时具有抗遗忘的鲁棒性。

## Abstract
Transformers lack an explicit architectural mechanism for storing and organizing knowledge acquired during training. We introduce learnable sparse memory banks: a set of latent tokens, randomly initialized and trained end-to-end, that transformer layers query via cross-attention to retrieve stored knowledge. To scale memory capacity without prohibitive attention costs, we propose chapter-based routing inspired by Mixture-of-Experts architectures, partitioning the memory bank into chapters and training a router to select relevant subsets per input. This enables scaling to 262K memory tokens while maintaining tractable computation. We evaluate our approach against standard transformers (in iso-FLOP settings) on pre-training and instruction fine-tuning across relevant benchmarks. Our models surpass iso-FLOP baselines suggesting scope for a new axis of scaling, demonstrating that explicit associative memory provides complementary capacity to what is captured implicitly in model parameters. Additionally, we observe improved knowledge retention under continued training, with robustness to forgetting when transitioning between training phases (e.g., pretraining to instruction fine-tuning).

---

## 论文详细总结（自动生成）

以下是对论文《Mixture of Chapters: Scaling Learnt Memory in Transformers》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的 Transformer 架构将知识隐式地存储在稠密参数中，这导致知识难以独立扩展、难以检查或编辑。此外，在持续训练（如从预训练转向指令微调）时，模型容易出现“灾难性遗忘”，丢失已习得的事实性知识。
*   **核心问题**：如何在不显著增加计算成本的前提下，为 Transformer 引入一个显式的、可大规模扩展的、且能有效保留知识的内部记忆机制？
*   **整体含义**：论文提出了一种名为“章节混合”（Mixture of Chapters, MoC）的架构，通过将记忆库划分为多个可路由的“章节”，实现了记忆容量的稀疏扩展，为模型提供了一个与参数互补的显式联想记忆层。

### 2. 方法论
*   **核心思想**：在 Transformer 块中嵌入一个由可学习潜标记（Latent Tokens）组成的记忆库，利用交叉注意力机制进行查询，并借鉴混合专家（MoE）的思路进行稀疏激活。
*   **关键技术细节**：
    *   **记忆层设计**：在标准 Transformer 层中插入交叉注意力模块。输入序列作为查询（Query），记忆库中的潜标记作为键（Key）和值（Value）。
    *   **章节路由（MoC）**：为了扩展记忆容量（如 262K 个标记），将记忆库 $M$ 划分为 $C$ 个“章节”。
    *   **路由流程**：
        1.  对输入序列的隐藏状态进行池化（如均值池化），得到序列表示 $r$。
        2.  通过一个轻量级路由器计算各章节的得分。
        3.  选择得分最高的 Top-k 个章节（例如从 4097 个章节中选 64 个）。
        4.  仅对选中的章节进行交叉注意力计算，将计算复杂度从 $O(LN_m)$ 降低到 $O(LkT)$。
    *   **残差集成**：读取的记忆信息通过残差连接合并回主标记流。

### 3. 实验设计
*   **数据集/场景**：
    *   **预训练**：使用 FineWeb-Edu 10B 子集（训练 9.6B tokens）。
    *   **指令微调（IFT）**：使用 UltraChat 200k 数据集（230M tokens），并将上下文长度从 1024 扩展到 2048。
*   **Benchmark**：MMLU、ARC-Challenge (ARC-C)、BoolQ、OpenBookQA (OBQA)。
*   **对比方法**：
    1.  **Vanilla (backbone-only)**：16 层基础模型。
    2.  **Vanilla (iso-FLOP)**：24 层模型，通过增加深度来对齐 MoC 模型的预训练总计算量（FLOPs）。
    3.  **Mixture of Chapters (MoC)**：本文提出的模型（12 层标准层 + 4 层记忆层）。

### 4. 资源与算力
*   **硬件使用**：预训练阶段使用 8 张 GPU 进行分布式数据并行（DDP）训练；指令微调阶段使用 4 张 GPU。
*   **具体型号**：文中未明确标注 GPU 具体型号（如 A100 或 H100），但提供了详细的解析 FLOPs 计算过程。
*   **训练规模**：预训练 9600 步，微调 2 个 epoch（3200 步）。MoC 模型单次前向传播约消耗 0.459T FLOPs。

### 5. 实验数量与充分性
*   **实验规模**：进行了预训练损失对比、多维度下游任务评测、以及针对指令微调阶段的消融实验。
*   **充分性与公平性**：
    *   **公平性**：采用了严格的 **iso-FLOP（等效算力）** 基准，确保性能提升并非单纯来自计算量的增加。
    *   **消融实验**：针对记忆库的更新策略（冻结 vs. 不同学习率更新）做了对比，证明了预训练记忆库的稳健性。
    *   **客观性**：实验覆盖了知识密集型任务，并观察到了模型在不同阶段转换时的遗忘曲线，结论较为客观。

### 6. 主要结论与发现
*   **性能超越**：在相同算力预算下，MoC 模型的预训练验证损失低于标准 Transformer。
*   **知识锚定效应**：在重度指令微调后，标准模型在 ARC-C 和 BoolQ 上的准确率大幅下降（接近随机猜测），而 MoC 模型表现极其稳定，证明显式记忆库能有效“锚定”事实知识。
*   **记忆可重用性**：在微调阶段冻结记忆库，其效果与持续更新记忆库相当，这暗示记忆库可以作为一种模块化的知识插件。

### 7. 优点
*   **高效扩展性**：通过章节路由机制，成功将记忆容量扩展至 262K tokens，且计算成本保持在可控范围内。
*   **缓解遗忘**：为解决大模型在微调过程中的知识流失提供了一种有效的架构方案。
*   **模块化潜力**：显式记忆库的设计使得未来可能实现针对特定领域知识的“章节”插入或编辑。

### 8. 不足与局限
*   **参数量冗余**：虽然计算量（FLOPs）对齐了，但 MoC 模型的总参数量（含静态记忆库）远高于基准模型，这会增加显存占用和存储成本。
*   **路由粒度**：目前采用的是序列级路由（Sequence-level），对于包含多种主题的长序列，可能不如 Token 级路由精细。
*   **任务局限**：实验主要集中在选择题形式的知识评测，对于长文本生成质量、逻辑推理等生成式任务的探讨相对较少。

（完）
