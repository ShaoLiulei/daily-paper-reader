---
title: Generative Artificial Intelligence Assisted Multi-modal Semantic Extraction for NOMA-based Image Transmissions
title_zh: 生成式人工智能辅助的基于非正交多址接入（NOMA）图像传输的多模态语义提取
authors: "Songhan Zhao, Shimin Gong, Bo Gu, Hongyang Du, Xidong Mu, Zehui Xiong, Yuming Fang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21092v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 生成式人工智能用于语义通信
tldr: 本文研究了生成式人工智能（GAI）辅助的非正交多址接入（NOMA）图像语义通信框架。通过跨模态语义特征提取与GAI图像恢复，结合NOMA的高效传输能力，提出了一种重要性感知且模型驱动的近端策略优化（IM-PPO）框架。该框架联合优化语义特征选择、接收波束成形及NOMA解码顺序，旨在平衡传输延迟与恢复精度，显著提升了通信效率与图像重建质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21092v1/fig-001.webp\", \"caption\": \"\", \"page\": 9, \"index\": 1, \"width\": 459, \"height\": 536}]"
motivation: 旨在解决NOMA图像传输中语义提取与传输控制协同优化的问题，以平衡传输延迟与语义恢复精度。
method: 提出了一种重要性感知且模型驱动的近端策略优化（IM-PPO）框架，通过量化特征重要性并结合模型驱动方法优化传输变量。
result: 实验结果表明，联合调整语义特征选择与传输控制能显著提高图像恢复准确性并降低传输延迟。
conclusion: 该研究证明了GAI与NOMA结合在语义通信中的潜力，且IM-PPO框架在学习效率和性能上优于基准方法。
---

## 摘要
本文研究了一种用于基于非正交多址接入（NOMA）图像传输的生成式人工智能（GAI）辅助语义通信框架。语义用户（SU）从原始图像中提取跨模态语义特征，随后利用 GAI 模型进行图像恢复。GAI 增强了语义图像传输的泛化能力和恢复效果，而 NOMA 则根据流量需求为语义用户高效分配传输容量。因此，语义提取和传输控制共同影响语义恢复性能和传输开销。我们通过联合优化语义层面的语义特征选择，以及传输层面的接收波束赋形和 NOMA 解码顺序，最大化传输延迟和语义恢复准确性的加权性能。为了减少语义特征中潜在的冗余并提高优化效率，我们开发了一种重要性感知且模型驱动的近端策略优化（IM-PPO）框架。具体而言，我们量化并保留高重要性的语义特征以增强 PPO 的学习效率，同时使用基于模型的优化方法来调整传输控制变量。数值结果验证了语义特征选择和传输控制的联合调整显著提高了语义恢复准确性和传输延迟性能。此外，与基准方法相比，IM-PPO 框架有效地利用模型信息提高了学习效率。

## Abstract
In this paper, we investigate a generative artificial intelligence (GAI)-assisted semantic communication framework for non-orthogonal multiple access (NOMA)-based image transmissions. Semantic users (SUs) extract cross-modal semantic features from the raw images, which are then used for image recovery by leveraging a GAI model. The GAI enhances the generalization and recovery of semantic image transmissions, while NOMA efficiently allocates transmission capacities to SUs based on their traffic demands. Thus, the semantic extraction and transmission control jointly affect both semantic recovery performance and transmission overhead. We maximize a weighted performance of transmission latency and semantic recovery accuracy by jointly optimizing the semantic feature selection at the semantic level, as well as the receive beamforming and NOMA decoding order at the transmission level. To reduce potential redundancy in semantic features and improve optimization efficiency, we develop an importance-aware and model-driven proximal policy optimization (IM-PPO) framework. Specifically, we quantify and retain high-importance semantic features to enhance the learning efficiency of PPO, while model-based optimization methods are used to adapt the transmission control variables. Numerical results validate that the joint adjustment of the semantic feature selection and the transmission control significantly improves the semantic recovery accuracy and the transmission latency performance. Moreover, the IM-PPO framework effectively leverages the model information to improve the learning efficiency compared to benchmark methods.

---

## 论文详细总结（自动生成）

### 论文总结：生成式人工智能辅助的 NOMA 图像语义传输

#### 1. 核心问题与研究动机
随着物联网（IoT）规模的扩大，图像传输带来的高数据流量对频谱资源和传输延迟提出了严峻挑战。传统的比特级传输难以应对动态网络环境。
*   **核心问题**：如何在基于非正交多址接入（NOMA）的无线网络中，通过生成式人工智能（GAI）实现高效的图像语义传输。
*   **研究动机**：语义通信通过提取关键特征而非原始像素来降低流量，但语义特征的选择（语义层）与传输资源分配（物理层）高度耦合。现有的深度强化学习（DRL）方法在处理高维控制变量时效率低下，且往往忽略了已知的物理通信模型信息。

#### 2. 方法论
论文提出了一种**联合传输与语义控制（JTSC）方案**，并开发了**重要性感知且模型驱动的近端策略优化（IM-PPO）框架**。
*   **核心思想**：将控制变量解耦。利用已知模型信息优化传输变量（波束成形、解码顺序），利用模型无关的 PPO 优化黑盒化的语义特征选择，并通过跨模态匹配剪枝搜索空间。
*   **关键技术细节**：
    *   **跨模态特征提取**：利用 BLIP 提取文本特征（高层意图），利用 UperNet 提取视觉分割图（底层结构）。
    *   **重要性感知剪枝**：使用 Grad-CAM 技术计算文本特征对图像区域的贡献度，结合“依赖性”和“贡献度”矩阵量化特征重要性，剔除冗余特征。
    *   **模型驱动优化**：
        *   **接收波束成形**：通过解决广义特征值问题获得最优向量。
        *   **NOMA 解码顺序**：利用二次变换（Quadratic Transform）和一阶泰勒展开将非凸的 SINR 约束转化为凸约束进行迭代求解。
    *   **GAI 恢复**：接收端采用预训练的稳定扩散模型（Stable Diffusion Model, SDM），通过迭代去噪过程重建图像。

#### 3. 实验设计
*   **数据集**：使用计算机视觉领域标准的 **COCO-2017** 数据集。
*   **场景设置**：考虑多个语义用户（SU）向接入点（AP）传输图像，AP 配备多天线并运行 SDM 模型。
*   **基准方法（Benchmarks）**：
    *   **算法对比**：M-PPO（无剪枝）、Plain-PPO（全模型无关 DRL）。
    *   **策略对比**：ALL-Selection（传输所有特征）、Random-Selection（随机选择）、Location-Decoding（基于距离的 NOMA 解码）。
*   **评价指标**：LPIPS（感知相似度，衡量恢复精度）和传输延迟（Latency）。

#### 4. 资源与算力
*   **模型细节**：使用了 Stable Diffusion v1-5 作为 GAI 后端。
*   **超参数**：学习率设为 $1 \times 10^{-4}$，去噪步数 $N=10$。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：论文展示了收敛性曲线、延迟与 LPIPS 的权衡分析、特征选择比例分析，以及针对带宽、用户数量、发射功率等变量的灵敏度测试。
*   **充分性**：实验涵盖了从算法收敛性到系统性能增益的多个维度，并提供了定性的视觉恢复效果对比。通过消融实验验证了“模型驱动”和“重要性剪枝”各自的贡献，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **联合优化必要性**：联合调整语义特征和传输控制比单一层面的优化能显著提升加权性能。
*   **IM-PPO 效率高**：相比于纯 DRL 方法，IM-PPO 收敛速度提升了约 2-3 倍，且能有效避免陷入局部最优。
*   **冗余抑制**：通过跨模态匹配剪枝掉的低重要性特征对恢复精度影响极小，但能大幅降低传输延迟。
*   **GAI 鲁棒性**：在去噪步数达到一定阈值后，精简后的语义特征甚至能比全量特征产生更稳定的恢复效果（减少了冲突信息的干扰）。

#### 7. 优点
*   **混合驱动架构**：巧妙地结合了数学优化（模型驱动）和强化学习（数据驱动），解决了 SemCom 中黑盒模型难以分析的问题。
*   **跨模态重要性量化**：提出了一种新颖的基于 Grad-CAM 的文本-视觉特征匹配方法，为语义重要性评估提供了可解释的依据。
*   **系统性强**：完整覆盖了从特征提取、无线传输到生成式重建的全流程。

#### 8. 不足与局限
*   **计算开销**：虽然传输延迟降低了，但接收端运行 Stable Diffusion 模型的计算延迟和功耗较高，论文未对此进行深入探讨。
*   **模型依赖**：方案高度依赖预训练的 BLIP、UperNet 和 SDM，若任务场景发生剧烈变化（如从自然图像转为医学影像），可能需要重新微调模型。
*   **实时性挑战**：GAI 的迭代去噪过程（即使只有 10 步）在实时通信系统中仍可能成为瓶颈。

（完）
