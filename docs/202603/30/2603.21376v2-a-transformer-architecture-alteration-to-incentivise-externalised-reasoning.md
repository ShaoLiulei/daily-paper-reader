---
title: A transformer architecture alteration to incentivise externalised reasoning
title_zh: 一种旨在激励外部化推理的 Transformer 架构改进
authors: "Elizabeth Pavlova, Mariia Koroliuk, Karthik Viswanathan, Cameron Tice, Edward James Young, Puria Radmard"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21376v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: Transformer架构改进与强化学习推理
tldr: 本研究提出一种改进Transformer架构的方法，旨在通过提前退出机制促使大语言模型将推理过程外部化。研究团队在中间层加入早期退出分支，并结合强化学习微调，激励模型在预测简单词元时减少计算深度。这种方法能有效减少模型利用内部激活进行非近视规划的冗余计算，从而将计算资源集中于关键推理步骤，提升推理透明度与效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21376v2/fig-001.webp\", \"caption\": \"Figure 3: Distribution of coherence scores across models with different KL factors.3 As the KL factor increases, coherence distributions shift toward higher scores, approaching base model performance.\", \"page\": 10, \"index\": 1, \"width\": 664, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2603.21376v2/fig-002.webp\", \"caption\": \"Figure 2: Left: RL training dynamics on Qwen3-4B for the Theory of Mind (Sclar et al., 2024) task. Each panel shows individual runs (light lines), mean across runs (bold line with markers), and ±1 SE error bars. The dashed line indicates the pre-SFT base model performance. Step 0 represents the SFT-calibrated model before RL. Accuracy improves while average compute decreases, demonstrating that the model learns to exit earlier without sacrificing performance. Coherence remains stable throughout. Evaluated on 60 theory of mind prompts per step. Right: Token-level visualisation of early exit behaviour. Each token is coloured by the layer at which computation was terminated. The model adaptively varies computational depth, using fewer layers for predictable tokens and the full depth for more complex ones. We used λ = 1.5 and β = 0.25.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 519}]"
motivation: 旨在减少大语言模型内部冗余计算，通过强制模型在简单任务上提前退出来激励其进行更显式的外部化推理。
method: 在Transformer架构中引入中间层早期退出机制，并利用强化学习在保持性能的前提下优化模型的计算深度。
result: 在小型推理模型上的初步实验表明，该方法能使模型根据词元难度自适应地减少计算量。
conclusion: 该架构调整有望通过限制内部隐式计算，迫使模型将复杂逻辑转移到可观测的外部推理步骤中。
---

## 摘要
我们提出了一种新的架构变更和训练后流水线，通过教导模型提前截断前向传播，使大语言模型（LLMs）成为更详尽的推理者。我们在现有的 Transformer 架构中增加了中间层的早退（early-exit）机制，并训练模型在无需深度计算即可预测下一个 token 时，从较浅的层退出。在校准阶段之后，我们利用强化学习激励模型在保持任务性能的同时尽可能早地退出。我们为小型推理模型提供了初步结果，表明它们学会了在不同 token 之间自适应地减少计算量。我们预测，在合适的规模下应用该方法，可以最大限度地减少推理模型利用其内部激活进行非近视规划（non-myopic planning）时所能支配的冗余计算量，并将其仅保留给难以预测的 token。

## Abstract
We propose a new architectural change, and post-training pipeline, for making LLMs more verbose reasoners by teaching a model to truncate forward passes early. We augment an existing transformer architecture with an early-exit mechanism at intermediate layers and train the model to exit at shallower layers when the next token can be predicted without deep computation. After a calibration stage, we incentivise the model to exit as early as possible while maintaining task performance using reinforcement learning. We provide preliminary results to this effect for small reasoning models, showing that they learn to adaptively reduce computations across tokens. We predict that, applied at the right scale, our approach can minimise the amount of excess computation that reasoning models have at their disposal to perform non-myopic planning using their internal activations, reserving this only for difficult-to-predict tokens.

---

## 论文详细总结（自动生成）

这篇论文提出了一种创新的 Transformer 架构修改方案，旨在通过“强制早退”机制解决大语言模型（LLM）推理过程中的隐式计算和不透明问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：当前的思维链（CoT）推理存在**不忠实性（Unfaithfulness）**和**混淆性（Obfuscation）**。模型可能在内部激活中进行复杂的非近视规划（提前计算后续 token 的逻辑），而输出的 CoT 文字并不能真实反映其内部思维过程。
*   **研究动机**：如果模型在预测简单的 token 时仍使用全部网络深度，它就有冗余的计算能力来隐藏其真实的推理意图。作者希望通过架构调整，迫使模型在简单任务上“早退”，从而将复杂的推理过程逼入可被监控的外部 CoT 文本中。

### 2. 方法论
*   **核心思想**：在 Transformer 的中间层引入“早退头”（Early-exit heads），允许模型在处理简单 token 时提前结束前向传播。
*   **关键技术细节**：
    *   **架构修改**：每 5 层添加一个早退头，将嵌入向量投影为标量 logit，代表退出概率。使用 LoRA 适配器调整现有权重。
    *   **两阶段训练流水线**：
        1.  **阶段 1：自蒸馏校准（SFT）**。将原始全深度模型作为老师，计算中间层与最终层输出分布的 KL 散度。若 KL 散度低，则设定较高的目标退出概率。
        2.  **阶段 2：强化学习激励（RL）**。使用改进的 RLOO 算法，其奖励函数公式为：
            $$R_{total} = R_{task} - \lambda \cdot (\text{average exit layer index}) - \beta \cdot D_{KL}$$
            其中 $\lambda$ 是惩罚项系数，用于激励模型在保证任务准确率（$R_{task}$）的前提下，尽量降低平均退出层数。
*   **算法流程**：模型在每一层随机采样是否退出。若退出，则残差流直接跳过后续层进入输出层，后续层的 KV 缓存由冻结的残差流表示。

### 3. 实验设计
*   **数据集/场景**：
    *   **GSM8K**：数学推理任务。
    *   **ExploreToM**：心理理论（Theory of Mind）推理任务。
*   **Benchmark 与对比对象**：
    *   对比基准为未经修改的原始模型（Base Model）。
    *   使用了 **DeepSeek-R1-Distill-Qwen-1.5B** 和 **Qwen3-4B** 两个小型推理模型作为实验对象。
*   **评估指标**：任务准确率（Accuracy）、答案相干性（Coherence，由 GPT-5 评分）、平均计算量（ALS，节省的层数比例）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练总时长。
*   **实现细节**：提到了在 RL 阶段使用了 5 次运行（runs）来观察训练动态，并在每个检查点评估 60 个 prompt。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 1.5B 模型进行了 5 种不同 KL 因子（0.25 到 4.0）的消融实验，验证了早退激进程度与性能的权衡。
    *   针对 4B 模型进行了 RL 训练动态分析。
*   **充分性评价**：实验属于**初步概念验证（Proof of Concept）**。虽然展示了 RL 能够降低计算深度且不损失准确率，但样本量较小（如 RL 评估仅 60 个 prompt，SFT 验证仅 50 个），且缺乏在大规模模型（如 70B+）上的验证。

### 6 主要结论与发现
*   **自适应计算**：模型成功学会了根据 token 难度调整计算深度。例如，在预测高度可预测的 token（如词组中的固定搭配）时，模型会显著提前退出。
*   **性能保持**：通过 RL 训练，模型在减少约 5%-10% 的平均层数计算量时，准确率反而有所提升（从 47% 提升至 55-60%），说明早退机制与推理性能可以兼容。
*   **可调控性**：通过调整 $\lambda$ 参数，可以在“推理外部化压力”与“生成质量”之间进行权衡。

### 7. 优点
*   **安全视角独特**：不同于传统的加速推理研究，本文从 AI 安全和可监控性出发，将早退作为一种强制模型“诚实”的手段。
*   **训练策略巧妙**：结合了自蒸馏校准和强化学习，解决了早退机制在训练初期不稳定、难以收敛的问题。
*   **推理效率提升**：虽然初衷是安全，但客观上也提供了一种降低推理延迟的方案。

### 8. 不足与局限
*   **诚实性未验证**：论文虽然声称能激励外部化推理，但**并未实际评估** CoT 的忠实度（Faithfulness）是否真的得到了提升。
*   **实验覆盖有限**：仅在两个特定任务（数学和心理理论）上进行了测试，缺乏跨领域通用性的验证。
*   **质量权衡风险**：过于激进的早退（如 KL 因子过低）会导致模型输出相干性大幅下降，在复杂长文本生成中的稳定性存疑。
*   **模型规模限制**：实验仅限于 4B 以下的小模型，在大模型上是否存在同样的“冗余计算”规律尚待观察。

（完）
