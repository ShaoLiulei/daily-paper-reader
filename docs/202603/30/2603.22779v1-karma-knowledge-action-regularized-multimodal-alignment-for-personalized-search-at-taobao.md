---
title: "KARMA: Knowledge-Action Regularized Multimodal Alignment for Personalized Search at Taobao"
title_zh: KARMA：淘宝个性化搜索中知识-行为正则化的多模态对齐
authors: "Zhi Sun, Wenming Zhang, Yi Wei, Liren Yu, Zhixuan Zhang, Dan Ou, Haihong Tang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22779v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 针对个性化搜索对大语言模型进行微调
tldr: 针对淘宝个性化搜索中大语言模型（LLM）直接微调导致的“知识-行动鸿沟”和语义坍缩问题，本文提出 KARMA 框架。该框架将语义重构作为训练时的正则化约束，通过历史条件语义生成和嵌入条件语义重构，在优化搜索行动目标的同时保持 LLM 的原生语义泛化能力。实验证明 KARMA 在淘宝搜索的召回、粗排和精排阶段均取得显著提升，并已成功上线运行。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22779v1/fig-001.webp\", \"caption\": \"Figure 1: KARMA architecture: A train-only regularizer that bridges the Knowledge–Action Gap.\", \"page\": 2, \"index\": 1, \"width\": 500, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2603.22779v1/fig-002.webp\", \"caption\": \"Table 1: Impact of decodability regularization (Δ over actiononly Pairwise-CE).\", \"page\": 4, \"index\": 2, \"width\": 506, \"height\": 279}]"
motivation: 解决 LLM 在个性化搜索任务中因过度拟合判别式目标而导致的语义坍缩及预训练知识与特定行动之间的冲突。
method: 提出 KARMA 框架，将语义重构作为训练正则项，通过历史条件生成和嵌入条件重构来确保兴趣嵌入的语义可恢复性。
result: "在淘宝搜索系统中，KARMA 显著提升了召回、粗排和精排的各项指标，并带来了 0.5% 的线上点击量增长。"
conclusion: 通过引入语义可解码性约束，可以有效缓解 LLM 在工业级个性化任务中的语义退化，实现更好的泛化与对齐。
---

## 摘要
大语言模型（LLMs）具备深厚的语义知识，使其成为为个性化搜索系统注入语义泛化能力的自然选择。然而，在实践中我们发现，在工业级个性化任务（如预测下一商品）上直接微调 LLM 往往效果不佳。我们将这一瓶颈归因于关键的“知识-行为鸿沟”（Knowledge-Action Gap）：即保留预训练语义知识与通过判别式目标对齐特定个性化行为之间的内在冲突。经验表明，仅针对行为的训练目标会导致“语义坍缩”（Semantic Collapse），例如出现“注意力汇聚”（attention sinks）现象。这种退化严重削弱了 LLM 的泛化能力，导致其无法为个性化搜索系统带来提升。我们提出了 KARMA（知识-行为正则化多模态对齐），这是一个将语义重构视为仅训练阶段正则化项的统一框架。KARMA 在优化用于检索的下一兴趣嵌入（行为）的同时，通过两个互补目标强制实现语义可解码性（知识）：(i) 基于历史条件的语义生成，将优化锚定在 LLM 原生的下一词元（next-token）分布上；(ii) 基于嵌入条件的语义重构，约束兴趣嵌入保持语义可恢复性。在淘宝搜索系统中，KARMA 缓解了语义坍缩（注意力汇聚分析），并提升了行为指标和语义保真度。在消融实验中，语义可解码性带来了高达 +22.5 的 HR@200 提升。借助 KARMA，我们在精排阶段实现了 +0.25 的 CTR AUC 提升，在粗排阶段实现了 +1.86 的 HR 提升，在召回阶段实现了 +2.51 的 HR 提升。KARMA 已在线部署，在排序阶段推理开销较低，带动了商品点击量增长 +0.5%。

## Abstract
Large Language Models (LLMs) are equipped with profound semantic knowledge, making them a natural choice for injecting semantic generalization into personalized search systems. However, in practice we find that directly fine-tuning LLMs on industrial personalized tasks (e.g. next item prediction) often yields suboptimal results. We attribute this bottleneck to a critical Knowledge--Action Gap: the inherent conflict between preserving pre-trained semantic knowledge and aligning with specific personalized actions by discriminative objectives. Empirically, action-only training objectives induce Semantic Collapse, such as attention ``sinks''. This degradation severely cripples the LLM's generalization, failing to bring improvements to personalized search systems.   We propose KARMA (Knowledge--Action Regularized Multimodal Alignment), a unified framework that treats semantic reconstruction as a train-only regularizer. KARMA optimizes a next-interest embedding for retrieval (Action) while enforcing semantic decodability (Knowledge) through two complementary objectives: (i) history-conditioned semantic generation, which anchors optimization to the LLM's native next-token distribution, and (ii) embedding-conditioned semantic reconstruction, which constrains the interest embedding to remain semantically recoverable.   On Taobao search system, KARMA mitigates semantic collapse (attention-sink analysis) and improves both action metrics and semantic fidelity. In ablations, semantic decodability yields up to +22.5 HR@200. With KARMA, we achieve +0.25 CTR AUC in ranking, +1.86 HR in pre-ranking and +2.51 HR in recalling. Deployed online with low inference overhead at ranking stage, KARMA drives +0.5% increase in Item Click.

---

## 论文详细总结（自动生成）

这是一份关于论文《KARMA: Knowledge-Action Regularized Multimodal Alignment for Personalized Search at Taobao》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在工业级个性化搜索中，直接微调大语言模型（LLM）往往效果不佳。
*   **研究背景**：传统的搜索排序模型依赖 ID 类特征（如商品 ID、共现统计），在长尾查询或冷启动场景下泛化能力差。虽然 LLM 拥有丰富的语义知识，但在适配个性化任务（如预测下一商品）时，存在**“知识-行为鸿沟”（Knowledge-Action Gap）**。
*   **关键发现**：如果仅针对用户行为（Action）进行判别式训练，LLM 会出现**语义坍缩（Semantic Collapse）**。表现为注意力机制退化为“条形码状”的注意力汇聚（Attention Sinks），模型倾向于寻找捷径（Shortcut）而非理解语义，导致泛化失败。

### 2. 方法论：KARMA 框架
KARMA 的核心思想是将**语义重构**作为一种“仅训练阶段有效”的正则化约束，确保模型在学习用户行为对齐的同时，保持语义的可解码性。

*   **连续词元接口（Continuous-Token Interface）**：为了满足工业级延迟要求，不直接输入原始文本，而是通过编码器将历史商品压缩为单个嵌入向量（Embedding Token），由 Decoder 建模历史序列。
*   **双重解码任务（Knowledge Regularization）**：
    1.  **历史条件语义生成（Task 1）**：根据用户历史行为序列，直接生成目标商品的文本描述。这利用了 LLM 原生的 Next-token 预测能力，防止模型忘记预训练知识。
    2.  **嵌入条件语义重构（Task 2）**：强制要求模型生成的“下一兴趣嵌入（$h_t$）”能够重新解码回目标商品的文本。这为 $h_t$ 设立了语义瓶颈，防止其退化为无意义的 ID 标识符。
*   **多模态对齐**：引入扩散模型（Diffusion/Flow-matching）头，利用 $h_t$ 重构商品的视觉语义特征。
*   **训练策略**：采用两阶段对齐。第一阶段进行语义预热（从单个商品嵌入解码语义）；第二阶段进行联合训练（行为对齐 + 语义正则）。
*   **推理优化**：所有解码头仅在训练时存在，推理时直接使用 $h_t$ 进行向量检索，**零额外推理开销**。

### 3. 实验设计
*   **数据集与场景**：使用淘宝搜索的真实匿名日志，包含用户历史交互、点击商品及曝光未点击的负样本。
*   **评估指标**：
    *   **行为指标**：gAUC（排序判别力）、HR@K（召回/粗排命中率）。
    *   **语义指标**：JS@50（Jaccard 相似度），用于检测检索结果与真实目标的语义重合度。
*   **对比基准（Benchmark）**：以仅使用行为目标（Pairwise-CE）训练的 LLM 为基线，对比了不同正则化组合（Task 1、Task 2、多模态扩展）的效果。

### 4. 资源与算力
*   **模型规模**：用户解码器使用 Qwen3-0.6B，商品编码器使用 Qwen3-1.7B（文本）和 Qwen3-VL-2B（多模态）。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量及训练时长。但考虑到其模型参数量（约 2B-4B 级别）和淘宝的业务规模，通常需要大规模 GPU 集群（如 A100/H800）进行分布式训练。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了消融实验（验证 Task 1/2 的有效性）、注意力图可视化分析（定性分析语义坍缩）、不同生成器对比（验证 Diffusion 在检索任务中的表现）以及全链路（召回、粗排、精排）的离线评估。
*   **线上验证**：进行了为期 14 天的线上 A/B 测试。
*   **充分性评价**：实验设计较为全面，不仅有宏观指标提升，还深入到了模型内部注意力机制的微观分析，客观地证明了 KARMA 缓解语义坍缩的能力。

### 6. 主要结论与发现
*   **语义可解码性是关键**：Task 2（嵌入重构）对提升检索效果的贡献远大于 Task 1，证明了约束“兴趣向量”语义完整性的重要性。
*   **缓解注意力汇聚**：可视化证明 KARMA 能让注意力分布更均匀，避免模型陷入局部特征捷径。
*   **生成 vs 检索的差异**：发现扩散模型（Diffusion）适合作为语义正则项（Mode-seeking），但不适合直接生成检索嵌入（Mean-seeking），因为检索需要的是聚合多种潜在意图的“均值”表示。
*   **业务价值**：在淘宝线上实现了 +0.5% 的点击量增长，且不增加推理延迟。

### 7. 优点
*   **理论与实践结合**：敏锐捕捉到了工业界微调 LLM 时的“语义坍缩”痛点，并给出了直观的注意力图解释。
*   **架构优雅**：通过“训练时正则、推理时移除”的设计，完美解决了 LLM 性能与工业级实时性要求的矛盾。
*   **多模态融合**：将视觉特征重构纳入统一框架，增强了表征的鲁棒性。

### 8. 不足与局限
*   **算力开销**：虽然推理无开销，但训练阶段需要维护多个解码头和复杂的扩散模型，训练成本显著高于传统模型。
*   **模型依赖**：效果高度依赖于预训练 LLM 的质量，对于极度垂直或缺乏文本描述的领域，语义重构的效果可能受限。
*   **细节缺失**：未披露具体的超参数设置（如 $\lambda$ 权重）和具体的硬件资源消耗，不便于外部完全复现。

（完）
