---
title: A transformer architecture alteration to incentivise externalised reasoning
title_zh: 一种旨在激励外部化推理的 Transformer 架构改进
authors: "Elizabeth Pavlova, Mariia Koroliuk, Karthik Viswanathan, Cameron Tice, Edward James Young, Puria Radmard"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21376v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 旨在激励外部化推理的Transformer架构修改
tldr: 本研究提出一种改进Transformer架构的方法，旨在通过提前退出机制促使大语言模型将推理过程外部化。研究团队在中间层加入早期退出分支，并结合强化学习微调，激励模型在预测简单词元时减少计算深度。这种方法能有效减少模型利用内部激活进行非近视规划的冗余计算，从而将计算资源集中于关键推理步骤，提升推理透明度与效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21376v1/fig-001.webp\", \"caption\": \"Figure 3: Distribution of coherence scores across models with different KL factors.3 As the KL factor increases, coherence distributions shift toward higher scores, approaching base model performance.\", \"page\": 10, \"index\": 1, \"width\": 664, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2603.21376v1/fig-002.webp\", \"caption\": \"Figure 2: Left: RL training dynamics on Qwen3-4B for the Theory of Mind (Sclar et al., 2024) task. Each panel shows individual runs (light lines), mean across runs (bold line with markers), and ±1 SE error bars. The dashed line indicates the pre-SFT base model performance. Step 0 represents the SFT-calibrated model before RL. Accuracy improves while average compute decreases, demonstrating that the model learns to exit earlier without sacrificing performance. Coherence remains stable throughout. Evaluated on 60 theory of mind prompts per step. Right: Token-level visualisation of early exit behaviour. Each token is coloured by the layer at which computation was terminated. The model adaptively varies computational depth, using fewer layers for predictable tokens and the full depth for more complex ones. We used λ = 1.5 and β = 0.25.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 519}]"
motivation: 旨在减少大语言模型内部冗余计算，通过强制模型在简单任务上提前退出来激励其进行更显式的外部化推理。
method: 在Transformer架构中引入中间层早期退出机制，并利用强化学习在保持性能的前提下优化模型的计算深度。
result: 在小型推理模型上的初步实验表明，该方法能使模型根据词元难度自适应地减少计算量。
conclusion: 该架构调整有望通过限制内部隐式计算，迫使模型将复杂逻辑转移到可观测的外部推理步骤中。
---

## 摘要
我们提出了一种新的架构变更和训练后流水线，通过教导模型提前截断前向传播，使大语言模型（LLM）成为更详尽的推理者。我们在现有的 Transformer 架构中增加了中间层的早退（early-exit）机制，并训练模型在无需深度计算即可预测下一个 token 时，从较浅的层退出。在校准阶段之后，我们利用强化学习激励模型在保持任务性能的同时尽可能早地退出。我们为小型推理模型提供了初步结果，表明它们学会了在不同 token 之间自适应地减少计算量。我们预测，在合适的规模下应用该方法，可以最大限度地减少推理模型利用其内部激活进行非近视规划时所能支配的冗余计算量，并将其仅保留给难以预测的 token。

## Abstract
We propose a new architectural change, and post-training pipeline, for making LLMs more verbose reasoners by teaching a model to truncate forward passes early. We augment an existing transformer architecture with an early-exit mechanism at intermediate layers and train the model to exit at shallower layers when the next token can be predicted without deep computation. After a calibration stage, we incentivise the model to exit as early as possible while maintaining task performance using reinforcement learning. We provide preliminary results to this effect for small reasoning models, showing that they learn to adaptively reduce computations across tokens. We predict that, applied at the right scale, our approach can minimise the amount of excess computation that reasoning models have at their disposal to perform non-myopic planning using their internal activations, reserving this only for difficult-to-predict tokens.

---

## 论文详细总结（自动生成）

这是一份关于论文《A transformer architecture alteration to incentivise externalised reasoning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨的核心问题是**大语言模型（LLM）推理的透明度与忠实性**。
*   **背景**：虽然思维链（CoT）已成为提升模型推理能力的标准，但存在“不忠实”（CoT 不能反映真实认知）和“混淆”（模型在内部隐藏推理过程）的风险。
*   **动机**：Transformer 架构具有“非近视性”，即模型会在处理当前简单词元（token）时，利用剩余的计算能力为未来的词元预先缓存信息。这种内部隐式计算使得监管者难以通过 CoT 监控模型的真实意图。
*   **目标**：通过架构修改，迫使模型在预测简单词元时尽早“退出”计算，从而限制其内部隐式计算的空间，激励其将复杂的逻辑推理转移到外部可见的 CoT 步骤中。

### 2. 方法论：核心思想与关键技术
作者提出了一种包含架构修改和两个训练阶段的流水线：

*   **架构修改**：
    *   在 Transformer 的中间层（每 5 层）添加**早退头（Early-exit heads）**。
    *   使用 **LoRA 适配器**微调现有权重。
    *   **随机退出机制**：模型在每个早退层输出一个标量 logit，代表退出的概率。如果选择退出，残差流将直接跳过后续层进入输出层。
*   **阶段 1：自蒸馏校准（SFT）**：
    *   以原始全深度模型为“教师”，计算中间层预测与最终层预测之间的 **KL 散度**。
    *   如果 KL 散度小，说明该层已具备预测能力，目标退出概率设为高。
    *   训练学生模型匹配这种退出分布，确保早退不会导致输出乱码。
*   **阶段 2：强化学习激励（RL）**：
    *   使用 **RLOO 算法**进行微调。
    *   **核心奖励公式**：$R_{total} = R_{task} - \lambda \cdot (\text{平均退出层索引}) - \beta \cdot D_{KL}$。
    *   通过超参数 $\lambda$ 调节模型对“节省计算”的渴望程度。$\lambda$ 越高，模型越倾向于更早退出。

### 3. 实验设计
*   **模型对象**：DeepSeek-R1-Distill-Qwen-1.5B 和 Qwen3-4B（小型推理模型）。
*   **数据集**：
    *   **GSM8K**：小学数学应用题，用于测试逻辑推理。
    *   **Theory of Mind (ToM)**：心理理论数据集，用于测试模型理解他人状态的能力。
*   **Benchmark 与对比**：
    *   基准线（Baseline）是未经修改的原始模型。
    *   对比了不同 **KL 因子**（控制校准阶段的早退激进程度）对模型连贯性的影响。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实验规模**：考虑到使用的是 1.5B 和 4B 规模的模型，且实验属于初步验证（Proof of Concept），预计算力消耗在学术实验室可接受范围内。

### 5. 实验数量与充分性
*   **实验组数**：
    *   针对 1.5B 模型进行了 5 种不同 KL 因子的消融实验。
    *   针对 4B 模型进行了 5 次独立的 RL 训练运行以观察稳定性。
    *   进行了 Token 级别的可视化分析。
*   **充分性评价**：作为**初步研究（Preliminary results）**，实验涵盖了校准验证、性能权衡和训练动态。但由于模型规模较小（最高 4B），且未在超大规模模型或更多样化的任务上验证，其结论的普适性仍需进一步观察。实验设计相对客观，通过均值和标准差展示了 RL 训练的波动。

### 6 主要结论与发现
*   **自适应计算**：模型成功学会了根据词元难度调整计算深度。对于可预测的词元（如固定搭配中的后半部分），模型会显著提前退出。
*   **性能保持**：在 RL 阶段，模型在减少平均计算深度的同时（从 98% 降至 90%-95%），任务准确率反而有所提升（从 47% 升至 55%-60%）。
*   **可调控性**：通过调节 $\lambda$ 或推理时的 logit 偏移，可以灵活平衡“推理外部化程度”与“生成质量”。

### 7. 优点
*   **创新性**：不同于以往为了推理加速的早退技术，本文首次将其作为一种**AI 安全工具**，用于强制推理外部化。
*   **随机性引入**：将早退决策建模为随机过程并纳入 RL 框架，使得模型能够通过试错学习最优的退出策略。
*   **实用性**：提供了“调节旋钮”，允许安全研究员根据需求强制模型增加 CoT 的详尽程度。

### 8. 不足与局限
*   **忠实性未验证**：虽然模型减少了内部计算，但论文尚未证明这是否真的提高了 CoT 的忠实性（即 CoT 是否真的反映了模型的所有思考）。
*   **规模限制**：实验仅限于 4B 以下的模型，在大规模模型（如 70B+）上是否存在“性能崩塌”尚不可知。
*   **任务覆盖面窄**：主要集中在数学和心理理论任务，缺乏对编程、创意写作等其他复杂任务的验证。
*   **潜在风险**：过度压制内部计算可能会限制模型的某些高级涌现能力，导致其在极复杂问题上表现下降。

（完）
