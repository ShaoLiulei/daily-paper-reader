---
title: "Thinking Deeper, Not Longer: Depth-Recurrent Transformers for Compositional Generalization"
title_zh: 思考更深，而非更久：用于组合泛化的深度循环 Transformer
authors: Hung-Hsuan Chen
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21676v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于推理的深度递归Transformer
tldr: 本文针对标准Transformer固定计算深度难以应对复杂推理的问题，提出一种深度递归Transformer。该架构通过在潜空间迭代应用共享权重的Transformer块，实现了计算深度与参数量的解耦。研究引入了静默思考目标、LayerScale初始化和恒等偏置递归以确保深层迭代的稳定性。实验证明，该模型在图遍历、逻辑运算等任务中展现出强大的组合泛化能力，能通过增加推理步数解决更高难度的任务。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21676v1/fig-001.webp\", \"caption\": \"Figure 3: Accuracy heatmap for the relational composition task over unstructured text. Without task-aligned structural inductive biases (no adjacency masking; while RoPE is retained for local word order, the fully shuffled input facts ensure that 1D relative positions carry no meaningful structural signal), the task exhibits a strictly monotonic increase in difficulty. The invariant reasoning core discovers latent pointer-chasing routes, achieving solid OOD generalization at depths 6 and 7 when additional thinking steps are provided. Dashed lines mark the training boundaries in both the depth and step dimensions.\", \"page\": 8, \"index\": 1, \"width\": 795, \"height\": 586}, {\"url\": \"assets/figures/arxiv/2603.21676v1/fig-002.webp\", \"caption\": \"Figure 2: Accuracy heatmap for the nested boolean expression evaluation task. Compared to the graph task, the computational frontier here is more gradual. The model exhibits robust out-of-distribution generalization with graceful degradation, maintaining >90% accuracy up to depth 14. Notice that the performance remains highly stable even when the model is unrolled for 24 steps (well beyond the training range of 4–16 steps), which validates the effectiveness of our design.\", \"page\": 7, \"index\": 2, \"width\": 790, \"height\": 524}, {\"url\": \"assets/figures/arxiv/2603.21676v1/fig-003.webp\", \"caption\": \"Figure 1: Accuracy heatmap for the graph reachability task. A sharp computational frontier exists along the diagonal, confirming the strict 1-hop = 1-step physical constraint enforced by adjacency masking. The model generalizes perfectly to 8 hops (OOD) with sufficient steps, but collapses abruptly to chance (∼50%) at 10 hops. Dashed vertical lines mark the step training boundary (5–8 thinking steps); columns outside this range are also OOD, yet accuracy is preserved.\", \"page\": 6, \"index\": 3, \"width\": 796, \"height\": 586}]"
motivation: 传统Transformer的固定深度限制了其在处理需要可变深度推理（如多跳逻辑或图遍历）任务时的泛化能力。
method: 提出一种深度递归架构，利用共享权重的Transformer块进行多次迭代，并结合静默思考目标和恒等偏置递归来稳定深层推理。
result: 在图路径、布尔逻辑和关系文本任务中，模型表现出明显的“计算边界”，即性能随推理步数的增加而从随机转为近乎完美。
conclusion: 该研究揭示了垂直递归推理在组合泛化中的潜力，为超越水平Token生成的深度推理提供了新的机制视角。
---

## 摘要
标准 Transformer 具有固定的计算深度，这从根本上限制了它们泛化到需要变长深度推理任务的能力，例如多跳图遍历或嵌套逻辑。我们提出了一种深度循环 Transformer，通过在潜空间中迭代应用共享权重的 Transformer 块，将计算深度与参数量解耦——使模型能够在推理时通过增加循环步数来换取更深层的推理。我们的架构包含三种机制以稳定深度循环（20步以上）：(1) 静默思考目标，仅监督最终输出，强制进行真正的多步推理而非中间启发式捷径；(2) LayerScale 初始化，保护脆弱的推理状态免受未训练层的噪声干扰；(3) 恒等偏差循环，在多步之间创建梯度高速公路。我们在三个归纳偏置递减的组合推理领域进行了评估：图可达性（严格的邻接掩码）、嵌套布尔逻辑（相对定位）以及非结构化关系文本（序列位置不提供结构提示）。在所有任务中，我们观察到一个清晰的“计算前沿”——即随着思考步数随任务复杂度增加，性能从随机猜测转变为近乎完美的边界。此外，这些任务揭示了性质迥异的泛化行为：精确但脆弱（图）、近似但鲁棒（逻辑）以及无结构提示下的自主潜空间路由（文本）。这一进展阐明了任务无关的循环推理核心与任务特定的感知接口之间的相互作用如何塑造分布外（OOD）泛化，为垂直思维链提供了一个机械论视角，补充了当前主流的水平标记生成范式。

## Abstract
Standard Transformers have a fixed computational depth, fundamentally limiting their ability to generalize to tasks requiring variable-depth reasoning, such as multi-hop graph traversal or nested logic. We propose a depth-recurrent Transformer that decouples computational depth from parameter count by iteratively applying a shared-weight Transformer block in latent space -- enabling the model to trade recurrence steps for deeper reasoning at inference time. Our architecture incorporates three mechanisms to make deep recurrence (20+ steps) stable: (1) a silent thinking objective that supervises only the final output, forcing genuine multi-step reasoning rather than intermediate heuristic shortcuts; (2) LayerScale initialization to protect fragile reasoning states from untrained layer noise; and (3) an identity-biased recurrence that creates a gradient highway across many steps. We evaluate on three compositional reasoning domains with decreasing inductive biases: graph reachability (strict adjacency masking), nested boolean logic (relative positioning), and unstructured relational text (where sequence position provides no structural hints). Across all tasks, we observe a clear \emph{computational frontier} -- a boundary where performance transitions from chance to near-perfect as thinking steps scale with task complexity. Moreover, these tasks reveal qualitatively different generalization behaviors: precise but brittle (graph), approximate but robust (logic), and autonomous latent routing without structural hints (text). This progression illuminates how the interplay between a task-invariant recurrent reasoning core and task-specific perceptual interfaces shapes out-of-distribution (OOD) generalization, offering a mechanistic perspective on vertical chain-of-thought that complements the prevailing horizontal token-generation paradigm.

---

## 论文详细总结（自动生成）

这是一份关于论文《Thinking Deeper, Not Longer: Depth-Recurrent Transformers for Compositional Generalization》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：标准 Transformer 架构具有固定的计算深度（层数固定），这限制了它们处理需要“变长深度推理”任务的能力（如多跳逻辑、复杂算法）。
*   **研究背景**：目前的 LLM 主要依赖“水平思维链”（Horizontal CoT），即通过生成更多 Token 来增加计算量。但这会消耗上下文窗口，且每一步生成都存在误差累积风险。
*   **研究意义**：本文提出“垂直思维链”（Vertical CoT），通过在潜空间（Latent Space）进行深度循环迭代，实现计算深度与参数量、Token 长度的解耦，使模型能在不增加 Token 的情况下通过“想得更深”来解决更难的问题。

### 2. 方法论：深度循环 Transformer
核心思想是使用一个**共享权重的 Transformer 块**作为推理核心，对输入表示进行 $T$ 次迭代更新。
*   **静默思考目标 (Silent Thinking)**：仅在最后一次循环步 $T$ 施加交叉熵损失监督。这种“末端监督”强制模型学习真正的多步算法，而非依赖中间步骤的启发式捷径。
*   **LayerScale 初始化**：在每个子层后引入可学习的通道缩放向量 $\Gamma$（初始化为 $10^{-4}$）。这在训练初期保护了脆弱的逻辑状态，使其接近恒等映射，避免随机噪声破坏表示。
*   **恒等偏置循环 (Identity-Biased Recurrence)**：采用类似 GRU 的门控机制结合新旧状态，并将门控偏置初始化为 $-2.0$（使初始保留率约 88%）。这为深层迭代（20步以上）创建了梯度高速公路，解决了梯度消失/爆炸问题。
*   **任务特定感知接口**：设计了三种接口以注入不同强度的归纳偏置：
    *   **拓扑掩码**：通过邻接矩阵限制注意力，模拟图神经网络。
    *   **相对位置编码 (RoPE)**：用于处理嵌套逻辑的层次结构。
    *   **标准序列注意力**：用于处理无结构的随机打乱文本。

### 3. 实验设计与 Benchmark
研究者设计了三个复杂度递增、归纳偏置递减的组合推理任务：
*   **任务 I：图可达性 (Graph Reachability)**：判断图中两点是否连通。训练 1-5 跳路径，测试 OOD（分布外）至 12 跳。
*   **任务 II：嵌套布尔逻辑 (Nested Boolean Logic)**：评估复杂的嵌套表达式（如 `!((T&F)|(!(T|F)))`）。训练深度 1-8，测试 OOD 至 14 层。
*   **任务 III：非结构化关系文本 (Relational Composition)**：基于 CLUTRR 数据集，处理随机打乱的家庭关系推理。训练路径长度 2-5，测试 OOD 至 9 层。
*   **对比与消融**：主要对比了**中间监督 (Per-step Loss)** 与**静默思考**的效果，并分析了不同循环步数对准确率的影响。

### 4. 资源与算力
*   **模型规模**：论文明确提到使用了极小的模型，参数量**小于 1M**（约 100 万参数）。
*   **硬件说明**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **实现工具**：作者披露使用了 Gemini、Claude 辅助编写代码和润色语言。

### 5. 实验数量与充分性
*   **实验规模**：针对三个不同性质的任务分别绘制了详细的准确率热图（思考步数 × 任务复杂度），展示了清晰的“计算前沿”。
*   **消融实验**：专门针对监督策略（中间 vs 末端）做了对比实验，揭示了中间监督会导致模型学习“统计捷径”而非“逻辑算法”。
*   **充分性评价**：实验设计较为科学，通过控制变量展示了模型在步数和难度两个维度的 OOD 泛化能力。虽然模型规模较小，但对于验证“机制性”假设（Mechanistic perspective）而言是充分且客观的。

### 6. 主要结论与发现
*   **计算前沿 (Computational Frontier)**：存在一个明显的对角线边界，当思考步数随任务难度同步增加时，性能会从随机猜测突变为近乎完美。
*   **OOD 泛化**：模型展现了强大的外推能力，能够解决比训练时更深、更复杂的逻辑链条。
*   **中间监督的危害**：强制模型在每一步输出结果会诱导其学习浅层的统计特征（如根据图密度猜连通性），从而破坏其学习深层通用算法的能力。
*   **稳定性**：通过 LayerScale 和恒等偏置，模型在循环 20-28 步时依然保持极高的数值稳定性。

### 7. 优点与亮点
*   **架构创新**：成功将循环神经网络（RNN）的迭代思想与 Transformer 的空间建模能力结合，实现了真正的“垂直扩展”。
*   **机制洞察**：深刻讨论了“静默思考”对算法学习的重要性，为理解模型如何学习逻辑提供了新视角。
*   **高效性**：用极小的参数量（<1M）实现了复杂的组合泛化，证明了推理能力不一定依赖于参数堆砌。

### 8. 不足与局限
*   **模型规模限制**：实验仅限于百万级参数的小模型，尚未在大规模预训练模型（如百亿级 LLM）上验证其有效性。
*   **感知接口依赖**：部分任务（如图任务）仍依赖于人工设计的感知接口（如邻接掩码），自动化程度有待提高。
*   **理论证明缺失**：主要基于实证观察，缺乏关于泛化边界的严格数学理论证明。
*   **应用场景**：目前仅在合成的符号推理任务上表现优异，在处理自然语言的模糊性和多样性方面仍需进一步验证。

（完）
