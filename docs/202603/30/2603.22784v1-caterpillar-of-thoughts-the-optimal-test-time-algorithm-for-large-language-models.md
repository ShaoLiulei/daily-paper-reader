---
title: "Caterpillar of Thoughts: The Optimal Test-Time Algorithm for Large Language Models"
title_zh: 思维毛虫：大语言模型的最优测试时算法
authors: "Amir Azarmehr, Soheil Behnezhad, Alma Ghafari"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22784v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型的最优推理时计算算法
tldr: 本研究探讨了大语言模型在固定计算预算下如何实现最优推理。作者将测试时计算建模为可回溯的马尔可夫链，并从理论上证明了最优算法生成的搜索空间呈现“毛毛虫树”结构（即去除叶子后为一条路径）。基于此发现，作者提出了 Caterpillar of Thoughts (CaT) 算法。该算法通过优化回溯策略，在减少 Token 生成量的同时，实现了比 Tree-of-Thoughts (ToT) 更高的任务成功率，为高效推理提供了理论支撑和实践方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22784v1/fig-001.webp\", \"caption\": \"\", \"page\": 7, \"index\": 1, \"width\": 1764, \"height\": 1764}]"
motivation: 尽管增加测试时计算能提升模型性能，但目前缺乏关于如何最优分配计算预算的理论指导。
method: 将推理过程建模为可回溯的马尔可夫链，证明最优算法生成的结构是毛毛虫树，并据此设计了 CaT 算法。
result: 实验结果显示，CaT 算法在成功率上优于 Tree-of-Thoughts，同时显著降低了 Token 的生成成本。
conclusion: 研究证明了受限的回溯形式足以达到最优推理效果，为大模型测试时计算提供了更高效的算法框架。
---

## 摘要
当允许使用额外的测试时计算（如采样、思维链、回溯或修改部分解）时，大语言模型（LLMs）通常能产生显著更好的输出。尽管此类技术在实证上取得了日益增长的成功，但对于推理时间计算应如何结构化，或者在固定计算预算下什么构成了最优使用，目前的理论理解还很有限。我们将测试时计算建模为一种与马尔可夫链交互的算法：在任何时间点，该算法都可以从之前观察到的任何状态恢复生成。也就是说，与状态被动抽取的标准马尔可夫链不同，我们允许算法随时回溯到马尔可夫链中之前观察到的任何状态。许多现有的测试时算法，如思维链（CoT）、思维树（ToT）或 Best-of-$k$，都可以被视为该模型中的特定算法。我们证明了虽然回溯可以指数级地减少生成次数，但一种非常有限的回溯形式在理论上就足够了。具体而言，我们证明了最优算法总是生成一棵毛虫树（Caterpillar Tree）。也就是说，如果我们移除最优算法生成的状态树的叶节点，就会得到一条路径。受我们对最优算法特征描述的启发，我们提出了思维毛虫（Caterpillar of Thoughts, CaT），这是一种新的测试时计算算法，能够减少 Token 或状态的生成数量。我们的实证评估表明，与 ToT 相比，CaT 在提高成功率的同时，还减少了 Token 的生成数量。

## Abstract
Large language models (LLMs) can often produce substantially better outputs when allowed to use additional test-time computation, such as sampling, chain of thought, backtracking, or revising partial solutions. Despite the growing empirical success of such techniques, there is limited theoretical understanding of how inference time computation should be structured, or what constitutes an optimal use of a fixed computation budget.   We model test-time computation as an algorithm interacting with a Markov chain: at any point, the algorithm may resume generation from any previously observed state. That is, unlike standard Markov chains where the states are drawn passively, we allow the algorithm to backtrack to any previously observed state of the Markov chain at any time. Many of the existing test-time algorithms, such as Chain-of-Thought (CoT) (Wei et al., 2023), Tree-of-Thoughts (ToT) (Yao et al., 2023), or Best-of-$k$ (Brown et al., 2024) could be seen as specific algorithms in this model.   We prove that while backtracking can reduce the number of generations exponentially, a very limited form of backtracking is theoretically sufficient. Namely, we show that the optimal algorithm always generates a caterpillar tree. That is, if we remove the leaves of the state tree generated by the optimal algorithm, we obtain a path. Motivated by our characterization of the optimal algorithm, we present Caterpillar of Thoughts (CaT), a new test-time computation algorithm, reducing the number of token/state generations. Our empirical evaluation shows that CaT, compared to ToT, achieves a better success rate while also reducing the number of token generations.

---

## 论文详细总结（自动生成）

### 论文总结：《Caterpillar of Thoughts: The Optimal Test-Time Algorithm for Large Language Models》

#### 1. 论文的核心问题与整体含义
*   **研究背景**：大语言模型（LLM）在推理时（Test-time）通过增加计算量（如思维链 CoT、思维树 ToT、多次采样等）可以显著提升性能。
*   **核心问题**：在给定的固定计算预算下，如何结构化地分配推理时间计算以达到最优效果？目前大多数方法（如 ToT、MCTS）多基于启发式设计，缺乏理论上的最优性证明。
*   **研究动机**：建立一个理论框架来描述测试时计算，并寻找在期望步数最少的情况下达到目标状态的最优算法。

#### 2. 论文提出的方法论
*   **核心思想**：将 LLM 的推理过程建模为在**可回溯马尔可夫链（Markov Chain with Rewinding）**上的搜索。算法可以随时“回溯”到之前观察到的任何状态重新开始生成。
*   **理论发现**：论文证明了无论马尔可夫链结构如何，最优算法生成的搜索树始终是一棵**“毛虫树”（Caterpillar Tree）**。即：如果去掉叶子节点，剩下的路径是一条直线。这意味着最优策略不需要复杂的广度或深度分支，而是在当前最优路径上不断尝试，失败则回溯到该路径的最后一个有效节点。
*   **算法流程（CaT）**：
    1.  **初始化**：设定起始状态 $x^*$。
    2.  **采样**：从当前状态 $x^*$ 生成下一个状态 $g$。
    3.  **评估与更新**：计算状态的价值函数 $OPT(x)$（即从该状态到达目标的期望步数）。
    4.  **决策**：如果新状态 $g$ 的价值优于当前状态（$OPT(g) < OPT(x^*)$），则将 $x^*$ 更新为 $g$（前进）；否则，保留 $x^*$（回溯，重新从 $x^*$ 采样）。
*   **实践改进**：针对现实中无法精确获知 $OPT$ 值的情况，提出了基于 **Softmax 分布**采样父节点的变体，以增强对评估噪声的鲁棒性。

#### 3. 实验设计
*   **数据集/场景**：
    *   **Game of 24（24点游戏）**：给定4个数字，通过算术运算得到24。测试集采用难度较高的 901-1000 索引题目。
    *   **5×5 Crosswords（填字游戏）**：在 5×5 网格中根据线索填入单词。
*   **Benchmark（基准）**：主要对比 **Tree-of-Thoughts (ToT)**。
*   **对比方法**：ToT（基于 BFS 的搜索）、CaT（本文方法，设置不同的迭代步数预算，如10步和15步）。

#### 4. 资源与算力
*   **模型使用**：实验统一使用 **GPT-4** 作为底层语言模型和状态评估器。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究关注的是推理算法而非模型训练，其成本主要体现为 API 调用产生的 Token 数量。

#### 5. 实验数量与充分性
*   **实验规模**：在 24点游戏上测试了 100 个难题实例；在填字游戏上测试了 20 个具有代表性的用例（遵循 ToT 的测试标准）。
*   **充分性评价**：实验设计较为客观，通过固定相同的底层模型和 Prompt 策略，直接对比了成功率与 Token 消耗。虽然任务种类（2个）不算极多，但涵盖了搜索空间较大的典型推理任务，且提供了不同计算预算下的性能曲线，论证较为充分。

#### 6. 论文的主要结论与发现
*   **最优性证明**：证明了受限的回溯形式（毛虫结构）在理论上足以实现最优推理效率，复杂的树状分支在期望步数上并不占优。
*   **性能提升**：在 24点游戏中，CaT 以更少的 Token 消耗（减少约 20%）实现了更高的成功率（81% vs ToT 的 74%）。
*   **效率优势**：CaT 能够更有效地识别并放弃无效路径，将计算资源集中在最有希望的状态上。

#### 7. 优点
*   **理论贡献突出**：为 LLM 测试时计算提供了坚实的数学基础，揭示了搜索树的最优形态。
*   **高性价比**：相比 ToT 等方法，CaT 在提升任务表现的同时显著降低了推理成本（Token 数）。
*   **算法简洁**：CaT 算法逻辑简单，易于在现有的推理框架中实现，且对评估噪声有较好的容忍度。

#### 8. 不足与局限
*   **评估器依赖**：算法高度依赖于对状态价值（$OPT$）的估计。如果模型对当前步骤的评分不准，算法可能会陷入局部最优或频繁回溯。
*   **任务局限性**：实验主要集中在有明确解空间的逻辑/数学任务上，在开放式创作或长篇对话等缺乏明确“目标状态”的任务中，该算法的应用效果尚待验证。
*   **噪声模型假设**：理论分析中对噪声的假设（如拉普拉斯噪声）可能与实际 LLM 评分的偏差分布存在差异。

（完）
