---
title: Dual-Space Knowledge Distillation with Key-Query Matching for Large Language Models with Vocabulary Mismatch
title_zh: 针对词表不匹配大语言模型的键-查询匹配双空间知识蒸馏
authors: "Stella Eva Tsiapali, Cong-Thanh Do, Kate Knill"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22056v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型的知识蒸馏
tldr: 针对大语言模型在知识蒸馏中存在的词表不匹配问题，本文深入分析了现有SOTA方法DSKD-CMA的注意力机制，揭示了其在键值对分布匹配上的局限性。为此，作者提出了一种基于生成对抗学习的新方法DSKD-CMA-GA，通过对抗训练优化不同模型间的键值匹配。实验证明，该方法在文本生成质量上取得了稳步提升，特别是在分布外数据上表现优异，有效缩小了跨词表蒸馏与同词表蒸馏之间的性能差距。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22056v1/fig-001.webp\", \"caption\": \"Fig. 5: Alignment weights assigned to pairs of tokens in the Student and Teacher sequences, using CMA and its chunk-based ablations: Chunk-Level Projection (CLP) and Chunk-Level Attention (CLA).\", \"page\": 4, \"index\": 1, \"width\": 310, \"height\": 790}, {\"url\": \"assets/figures/arxiv/2603.22056v1/fig-002.webp\", \"caption\": \"Table 1: Mean ROUGE-L scores from evaluation on five random seeds. Average scores across the out-of-distribution (OOD) and all (All) evaluation datasets are given. Results for the best f -divergence are reported for DSKD-CMA (SRKL), -GA (SKL) and -CT (KL).\", \"page\": 4, \"index\": 2, \"width\": 521, \"height\": 391}]"
motivation: 旨在解决大语言模型知识蒸馏中，由于教师和学生模型词表不一致导致的跨空间特征对齐难题。
method: 提出DSKD-CMA-GA方法，利用生成对抗学习来校准和匹配来自不同模型的键（Key）与查询（Query）向量分布。
result: 在文本生成任务中实现了ROUGE-L指标的持续增长，尤其在分布外数据集上平均提升了0.37。
conclusion: 通过对抗学习优化双空间注意力机制，能有效提升跨词表知识蒸馏的效率和泛化能力。
---

## 摘要
大语言模型 (LLMs) 在各项语言任务中均取得了最先进 (SOTA) 的性能，但由于其庞大的规模和资源需求，部署成本高昂。知识蒸馏 (KD) 通过训练较小的学生模型来模仿较大的教师模型，从而在不显著损失性能的情况下提高效率。带有跨模型注意力机制的双空间知识蒸馏 (DSKD-CMA) 已成为具有不同分词器 (tokenizer) 的 LLM 之间进行知识蒸馏的 SOTA 方法，但其内部运作机制在很大程度上仍不透明。在本研究中，我们通过手动词元对齐探测和热力图可视化，系统地分析了 DSKD-CMA 的注意力机制，揭示了其优势和局限性。在此基础上，我们提出了一种基于生成对抗 (GA) 学习的新方法 DSKD-CMA-GA，旨在解决不同模型计算出的键 (keys) 和查询 (queries) 之间的分布不匹配问题。实验表明，该方法在文本生成质量上带来了适度但持续的 ROUGE-L 提升，特别是在分布外数据上（平均提升 0.37），缩小了跨分词器和同分词器知识蒸馏之间的差距。

## Abstract
Large language models (LLMs) achieve state-of-the-art (SOTA) performance across language tasks, but are costly to deploy due to their size and resource demands. Knowledge Distillation (KD) addresses this by training smaller Student models to mimic larger Teacher models, improving efficiency without significant performance loss. Dual-Space Knowledge Distillation with Cross-Model Attention (DSKD-CMA) has emerged as a SOTA method for KD between LLMs with distinct tokenizers, yet its internal workings remain largely opaque. In this work, we systematically analyse the attention mechanism of DSKD-CMA through manual token alignment probing and heatmap visualisations, revealing both strengths and limitations. Building on this, we introduce a novel method, DSKD-CMA-GA, based on Generative Adversarial (GA) learning, to address the mismatched distributions between the keys and queries computed from distinct models. Experiments show modest but consistent ROUGE-L gains in text generation quality, particularly on out-of-distribution data (+0.37 on average), narrowing the gap between cross- and same-tokenizer KD.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dual-Space Knowledge Distillation with Key-Query Matching for Large Language Models with Vocabulary Mismatch》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）的知识蒸馏（KD）中，教师模型和学生模型通常使用不同的分词器（Tokenizer）和词表（Vocabulary）。这导致两者生成的序列长度不一致、预测空间不匹配，使得传统的逻辑值（Logits）对齐方法难以直接应用。
*   **研究动机**：虽然现有的 SOTA 方法 DSKD-CMA（双空间跨模型注意力蒸馏）通过注意力机制隐式解决了对齐问题，但其内部运作机制缺乏透明度。此外，由于键（Key）和查询（Query）来自不同的模型空间，其分布不匹配会削弱注意力机制的有效性。

### 2. 方法论
*   **核心思想**：通过分析 DSKD-CMA 的注意力对齐行为，引入“键-查询匹配”（Key-Query Matching）机制，利用对抗学习或最优传输理论来校准不同模型间的特征分布。
*   **关键技术细节**：
    *   **分析框架**：提出了**手动块对齐（Manual Chunk Alignment）**。通过比较字符偏移量，将不同分词器的词元序列划分为覆盖相同文本的最小块（Chunks），以此作为基准来探测注意力机制的对齐质量。
    *   **DSKD-CMA-GA（生成对抗对齐）**：引入一个判别器 $D$，学习区分教师模型的 Key 和学生模型的 Query。学生模型的投影器作为生成器，目标是生成能“欺骗”判别器的 Query，从而使两者的分布在特征空间内达成一致。
    *   **DSKD-CMA-CT（条件传输对齐）**：基于约束版本的最佳传输（Optimal Transport），通过可训练的评论器（Critic）鼓励 Key 和 Query 分布的双向匹配。
    *   **损失函数**：在原始 DSKD-CMA 的双空间蒸馏损失基础上，增加了键-查询匹配损失 $L_{KQ}$，并结合了多种 $f$-散度（如 SKL, JSD 等）进行优化。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：DataBricks Dolly 15K（指令微调数据）。
    *   **测试集**：包含分布内（ID）数据 Dolly (500样本) 和分布外（OOD）数据 Self-Instruct、Vicuna-Eval、Super-Natural Instructions (S-NI)、Unnatural Instructions (UnNI)，总计超过 2.6 万个测试样本。
*   **Benchmark 与对比方法**：
    *   **基准模型**：学生模型 GPT-2 (124M)；教师模型 Qwen1.5 (1.8B，跨词表) 和 GPT-2 XL (1.5B，同词表)。
    *   **对比方法**：监督微调（SFT）、最小编辑距离对齐（MinED）、通用逻辑值蒸馏（ULD）以及原始的 DSKD-CMA。
*   **评估指标**：ROUGE-L（衡量文本生成质量）。

### 4. 资源与算力
*   **硬件环境**：使用了 Ampere 节点，配备 **4 张 NVIDIA A100 GPU (80GB)** 和 1TB RAM。
*   **训练时长**：每个变体的训练大约需要 **2-3 小时**，评估过程约需 1 小时。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 5 个不同的数据集进行了测试。
    *   所有结果均基于 **5 个随机种子**的平均值，保证了结果的统计显著性。
    *   **消融实验**：对比了块级投影（CLP）和块级注意力（CLA），验证了注意力机制在处理重复词元时的鲁棒性。
    *   **散度测试**：对 6 种不同的 $f$-散度进行了详尽对比，探讨了对齐机制与散度选择之间的相互作用。
*   **客观性**：实验设计涵盖了从同词表到跨词表的多种场景，对比了多种 SOTA 基线，实验过程较为客观、公平。

### 6. 主要结论与发现
*   **分布匹配的重要性**：DSKD-CMA-GA（对抗学习版）表现最优，在 OOD 数据上平均提升了 **0.37 ROUGE-L**，证明了校准 Key-Query 分布能显著增强跨词表蒸馏的效果。
*   **注意力机制的本质**：通过可视化发现，原始 CMA 能够隐式学习到文本块结构，但在处理数字等重复词元时存在噪声；而强制的局部块对齐（CLA）反而会因过度敏感而降低性能。
*   **缩小差距**：改进后的方法有效缩小了跨词表蒸馏与同词表蒸馏之间的性能差距，在某些指标上甚至超越了同词表蒸馏。

### 7. 优点
*   **可解释性提升**：通过手动块对齐探测，为原本“黑盒”的跨模型注意力机制提供了直观的分析手段。
*   **理论与实践结合**：将对抗学习引入注意力对齐，解决了跨模型特征空间不一致的本质问题。
*   **泛化能力强**：在多个 OOD 数据集上表现稳健，证明了该方法在处理分布偏移时的优越性。

### 8. 不足与局限
*   **提升幅度有限**：虽然 ROUGE-L 有所提升，但绝对分值的增长（约 0.3-1.0 点）相对较小。
*   **任务覆盖面**：实验主要集中在通用的指令遵循和文本生成任务上，尚未在更复杂的逻辑推理（如 GSM8K）或长文本生成任务中进行验证。
*   **计算开销**：虽然比某些基于最优传输的方法快，但引入对抗训练和判别器仍会增加额外的训练复杂度和超参数调优难度。

（完）
