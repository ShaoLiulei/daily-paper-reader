---
title: "Mixture of Chapters: Scaling Learnt Memory in Transformers"
title_zh: 章节混合：扩展 Transformer 中的学习记忆
authors: "Tasmay Pankaj Tibrewal, Pritish Saha, Ankit Meda, Kunal Singh, Pradeep Moturi"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21096v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 使用交叉注意力和受MoE启发的路由扩展Transformer内存
tldr: 针对Transformer缺乏显式知识存储机制的问题，本文提出“章节混合”（Mixture of Chapters）架构。该方法引入了可学习的稀疏内存库，通过交叉注意力机制检索知识，并借鉴混合专家（MoE）思想将内存划分为多个“章节”进行路由，从而在不增加计算负担的情况下将内存扩展至262K个token。实验证明该方法在预训练和微调中均优于同等算力的基准模型，并能有效缓解灾难性遗忘，提升知识保留能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21096v1/fig-001.webp\", \"caption\": \"Figure 2: Loss curves. Left: Pretraining. Right: IFT (Freezing the bank yields identical curves).\", \"page\": 6, \"index\": 1, \"width\": 972, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-002.webp\", \"caption\": \"Figure 1: Single transformer block with chapter-based memory routing\", \"page\": 3, \"index\": 2, \"width\": 854, \"height\": 557}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-003.webp\", \"caption\": \"Table 6: Main training settings for pretraining and IFT.\", \"page\": 18, \"index\": 3, \"width\": 977, \"height\": 621}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-004.webp\", \"caption\": \"Table 3: Literature map (representative, not exhaustive).\", \"page\": 13, \"index\": 4, \"width\": 972, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-005.webp\", \"caption\": \"Table 5: Main MoC architecture used in our experiments.\", \"page\": 17, \"index\": 5, \"width\": 952, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-006.webp\", \"caption\": \"Table 2: Comparison of Vanilla, Mixture of Chapters (MoC) (∆ = IFT - pretrain; lower is better)\", \"page\": 5, \"index\": 6, \"width\": 786, \"height\": 188}]"
motivation: 传统的Transformer架构缺乏显式的机制来存储和组织训练过程中获取的知识，导致知识获取效率受限。
method: 提出一种基于章节路由的可学习稀疏内存库，通过交叉注意力检索知识，并利用类似MoE的路由机制实现大规模内存扩展。
result: 在同等FLOPs设置下，该模型在预训练和指令微调基准测试中均超越了标准Transformer，并展现出更强的知识保留能力和抗遗忘性。
conclusion: 显式关联内存为模型提供了与隐式参数互补的容量，是扩展大语言模型能力的一个重要且有效的新维度。
---

## 摘要
Transformer 缺乏一种显式的架构机制来存储和组织训练过程中获取的知识。我们引入了可学习的稀疏记忆库：一组随机初始化并端到端训练的潜代币（latent tokens），Transformer 层通过交叉注意力机制对其进行查询，以检索存储的知识。为了在不产生过高注意力成本的情况下扩展记忆容量，我们受混合专家（Mixture-of-Experts）架构启发，提出了基于章节的路由机制，将记忆库划分为若干章节，并训练一个路由器为每个输入选择相关的子集。这使得在保持计算可控的同时，能够将记忆扩展到 262K 个代币。我们在相关基准测试的预训练和指令微调任务中，将我们的方法与标准 Transformer（在等效 FLOPs 设置下）进行了对比评估。我们的模型超越了等效 FLOPs 基线，这表明存在一个新的扩展维度，并证明了显式关联记忆提供了与模型参数中隐式捕获的知识相补充的能力。此外，我们观察到在持续训练下知识保留能力有所提高，并且在不同训练阶段（例如从预训练到指令微调）转换时具有抗遗忘的鲁棒性。

## Abstract
Transformers lack an explicit architectural mechanism for storing and organizing knowledge acquired during training. We introduce learnable sparse memory banks: a set of latent tokens, randomly initialized and trained end-to-end, that transformer layers query via cross-attention to retrieve stored knowledge. To scale memory capacity without prohibitive attention costs, we propose chapter-based routing inspired by Mixture-of-Experts architectures, partitioning the memory bank into chapters and training a router to select relevant subsets per input. This enables scaling to 262K memory tokens while maintaining tractable computation. We evaluate our approach against standard transformers (in iso-FLOP settings) on pre-training and instruction fine-tuning across relevant benchmarks. Our models surpass iso-FLOP baselines suggesting scope for a new axis of scaling, demonstrating that explicit associative memory provides complementary capacity to what is captured implicitly in model parameters. Additionally, we observe improved knowledge retention under continued training, with robustness to forgetting when transitioning between training phases (e.g., pretraining to instruction fine-tuning).

---

## 论文详细总结（自动生成）

这是一份关于论文《Mixture of Chapters: Scaling Learnt Memory in Transformers》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 Transformer 架构主要通过其稠密参数（Weights）隐式地存储知识，缺乏一种显式的、可独立扩展的、且具备地址索引能力的内存机制。
*   **研究背景**：虽然 RAG（检索增强生成）或长文本 KV 缓存能处理外部或上下文信息，但模型内部缺乏一种在训练过程中端到端学习到的、存储事实性知识的“潜空间仓库”。
*   **整体含义**：本文旨在探索一种新的模型扩展（Scaling）维度——即通过增加显式的“学习记忆库”而非仅仅增加模型深度或宽度，来提升模型容量和知识保留能力。

### 2. 方法论：Mixture of Chapters (MoC)
*   **核心思想**：在 Transformer 块中嵌入一个可学习的潜代币（Latent Tokens）库。模型通过交叉注意力（Cross-Attention）查询该库，类似于从书本中检索知识。
*   **关键技术细节**：
    *   **内存库（Memory Bank）**：由 $N_m$ 个随机初始化、随模型一同训练的向量组成。
    *   **章节划分（Chapters）**：为了避免对海量内存代币进行全量交叉注意力计算（计算复杂度高），将内存库划分为 $C$ 个“章节”，每个章节包含 $T$ 个代币。
    *   **序列级路由（Sequence-level Routing）**：借鉴 MoE（混合专家）思想，引入轻量级路由器。路由器对输入序列进行池化后，为该序列选择最相关的 $k$ 个章节（Top-k）。
    *   **交叉注意力检索**：查询（Query）来自输入 Token 流，键（Key）和值（Value）来自被选中的内存章节。
*   **算法流程**：输入序列 $\rightarrow$ 路由器选择章节 $\rightarrow$ 提取选中章节的 K/V $\rightarrow$ 交叉注意力计算 $\rightarrow$ 残差连接回到主干网络。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 FineWeb-Edu 的 10B Token 子集。
    *   **指令微调 (IFT)**：使用 UltraChat_200k 数据集（约 230M Token）。
*   **Benchmark**：评估了 MMLU（综合知识）、ARC-Challenge（科学推理）、BoolQ（布尔问答）和 OpenBookQA。
*   **对比方法**：
    1.  **Vanilla (backbone-only)**：与 MoC 使用相同层数的基准模型。
    2.  **Vanilla (iso-FLOP)**：通过增加层数（从 16 层增加到 24 层）来匹配 MoC 计算量的等效算力基准模型。
    3.  **Mixture of Chapters (MoC)**：本文提出的内存增强模型。

### 4. 资源与算力
*   **硬件环境**：预训练使用 **8 张 GPU** 进行 DDP 训练；指令微调使用 **4 张 GPU**。
*   **训练时长/规模**：预训练进行了 9,600 步（约 9.6B Token）；指令微调进行了 2 个 Epoch（3,200 步）。
*   **具体型号**：文中未明确提及具体的 GPU 型号（如 A100 或 H100），但提供了详细的 FLOPs 分析，证明 MoC 在推理/训练时的计算开销与 24 层稠密模型相当。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从头预训练、指令微调、以及针对内存库更新策略（冻结 vs 学习率调整）的消融实验。
*   **充分性与公平性**：
    *   **公平性**：采用了严格的 **iso-FLOP（等效算力）** 对比，确保 MoC 的性能提升不是单纯因为堆砌计算量。
    *   **客观性**：观察到了模型在 IFT 阶段的遗忘现象，并对比了 MoC 在缓解遗忘方面的表现。
    *   **局限性**：实验主要集中在 10B Token 级别的中等规模训练，尚未在千亿级参数或万亿级 Token 规模上验证。

### 6. 主要结论与发现
*   **性能超越**：在相同算力预算下，MoC 的验证集 Loss 低于增加深度的标准 Transformer，且在下游知识任务上表现更好。
*   **缓解灾难性遗忘**：在重度指令微调（IFT）后，标准模型在 ARC-C 和 BoolQ 上准确率大幅下降（接近随机猜测），而 MoC 表现极其稳定，证明显式内存能“锚定”事实知识。
*   **内存可复用性**：在微调阶段，即使**冻结内存库**不更新，模型性能依然稳健，说明预训练学到的知识可以被有效锁定。

### 7. 优点与亮点
*   **扩展新维度**：提出了一种除了“深度、宽度、数据量”之外的新 Scaling 路径——内存容量。
*   **稀疏性优势**：通过章节路由，实现了 262K 巨大内存容量的同时，保持了计算开销的线性增长（而非二次方）。
*   **知识解耦**：将事实性知识存储在显式内存中，使模型主干能更专注于逻辑推理和指令遵循。

### 8. 不足与局限
*   **参数量增加**：虽然计算量（FLOPs）受控，但由于存在巨大的内存库，模型的**参数总量**和显存占用显著增加。
*   **路由粒度**：目前采用序列级路由，可能无法处理单个长序列中包含多个跨领域知识点的情况（未来可能需要 Token 级路由）。
*   **评估范围**：尚未在长文本生成、代码编写等更广泛的领域进行压力测试。
*   **黑盒性质**：虽然内存是显式的，但潜代币（Latent Tokens）本身仍难以直接被人眼解读，其可解释性有待进一步研究。

（完）
