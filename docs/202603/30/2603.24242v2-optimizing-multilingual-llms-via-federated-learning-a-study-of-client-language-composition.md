---
title: "Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition"
title_zh: 通过联邦学习优化多语言大语言模型：客户端语言组成的研究
authors: "Aleix Sant, Jordi Luque, Carlos Escolano"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24242v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 联邦学习环境下大语言模型的多语言指令微调实验
tldr: 本研究探讨了多语言环境下大语言模型的联邦学习优化问题，重点分析了客户端语言组成对模型性能的影响。研究扩展了FederatedScope-LLM框架，并提出了一种名为LDES-FL的局部动态早停机制以提升训练效率。实验表明，增加客户端内部的语言多样性不仅能提升全局模型的性能和公平性，还能显著改善低资源语言的表现，为多语言联邦学习的设计提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24242v2/fig-001.webp\", \"caption\": \"Table 4: Training cost normalized by the higheststep configuration, Local FT (multilingual), which requires 1.83× 105 total optimization steps. Each value is reported as a fraction of this reference.\", \"page\": 7, \"index\": 1, \"width\": 415, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2603.24242v2/fig-002.webp\", \"caption\": \"Table 3: Results for the multilingual test set of the server. R and Fb stand for ROUGE-L (Lin, 2004) and Fbert (Zhang et al., 2020) respectively. The language in columns corresponds to the monolingual part of the test set. Mean reports the average score across all languages, whereas σ denotes the corresponding standard deviation, used here as an indicator of multilingual fairness. A bootstrap test with 100 sets (p-value < 0.05) confirms that all federated models achieve statistically significant improvements over the Base Model on all evaluated metrics and languages, as indicated by an asterisk (*).\", \"page\": 7, \"index\": 2, \"width\": 943, \"height\": 250}]"
motivation: 针对多语言联邦学习中客户端语言分布不均和资源差异带来的挑战，探索如何优化多语言大模型的训练。
method: 扩展了FederatedScope-LLM框架，并引入了局部动态早停机制（LDES-FL）来根据验证集表现自主调节训练。
result: 增加客户端内的语言多样性可显著提升全局模型的公平性和低资源语言性能，但会增加优化步骤。
conclusion: 客户端语言组成是多语言联邦学习的关键设计变量，直接影响模型的性能、公平性与训练效率。
---

## 摘要
在多语言环境中，大语言模型（LLMs）的联邦学习（FL）面临着重大挑战，这些挑战源于客户端之间异构的语言分布以及语言资源可用性的差异。为了应对这些挑战，我们扩展了 FederatedScope-LLM 框架，以支持 LLMs 的多语言指令微调实验。我们还引入了一种新型的客户端特定早停机制——局部动态早停（LDES-FL），该机制允许客户端根据客户端侧的验证性能暂停和恢复本地训练，从而提高了训练效率和可持续性。通过一系列实验，我们研究了客户端语言组成（从完全单语言到日益增加的多语言客户端）如何影响多语言质量、公平性和训练成本。研究发现，单语言本地微调对于单语言专业化仍然最为有效，而联邦训练则更适合学习单个平衡的多语言模型。在联邦学习中，增加客户端内的多语言性可以产生更强大且更公平的全局模型，缩小与集中式多语言微调的差距，并为低资源语言带来最大的收益，尽管这是以增加优化步骤为代价的。总之，我们的结果表明，客户端语言组成是多语言联邦学习中的一个关键设计变量，它决定了性能、公平性和效率。

## Abstract
Federated Learning (FL) of Large Language Models (LLMs) in multilingual environments presents significant challenges stemming from heterogeneous language distributions across clients and disparities in language resource availability. To address these challenges, we extended the FederatedScope-LLM framework to support multilingual instruction-tuning experiments with LLMs. We also introduced a novel client-specific early stopping mechanism, Local Dynamic Early Stopping (LDES-FL), which allows clients to pause and resume local training based on client-side validation performance, enhancing training efficiency and sustainability. Through a series of experiments, we studied how client language composition - from fully monolingual to increasingly multilingual clients - affects multilingual quality, fairness and training cost. Monolingual local fine-tuning remains the most effective for single-language specialization, whereas federated training is better suited to learning a single balanced multilingual model. In FL, increasing within-client multilinguality leads to stronger and fairer global models, narrows the gap to centralized multilingual fine-tuning, and yields the largest gains for lower-resource languages, albeit at the cost of more optimization steps. Overall, our results identify client language composition as a key design variable in multilingual FL, shaping performance, fairness and efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了在**联邦学习（FL）**框架下，如何优化**多语言大语言模型（LLM）**的指令微调。
*   **研究动机**：在现实世界的联邦学习中，客户端数据通常具有高度的异构性（Non-IID），特别是语言分布不均。这种语言上的差异会导致“客户端漂移（Client Drift）”，使全局模型难以收敛，并导致不同语言间性能不均（不公平）。
*   **核心问题**：客户端内部的语言组成（即一个客户端是只含有一种语言还是含有多种语言）如何影响多语言模型的最终质量、公平性以及训练成本？

### 2. 方法论：核心思想与关键技术
论文主要提出了两个方面的技术贡献：
*   **扩展 FederatedScope-LLM 框架**：增加了对多语言指令微调的支持，包括灵活的 Prompt 集成、语言感知的样本处理以及多语言 FL 数据流水线。
*   **局部动态早停机制（LDES-FL）**：
    *   **核心思想**：允许每个客户端根据其私有验证集的损失值自主决定暂停或恢复训练。
    *   **流程**：客户端监控本地验证损失。如果连续 $P_{max}$ 轮没有提升，则停止训练并向服务器发送其历史最佳模型；如果后续下载的全局模型在本地验证集上表现有所提升，客户端会重新加入训练。
    *   **意义**：解决了标准 FL 中所有客户端必须同步停止的低效问题，减少了不必要的计算开销。
*   **参数高效微调（PEFT）**：采用 **LoRA** 算法（秩 $r=16$），仅优化约 1%-2% 的参数，以降低通信和计算负担。

### 3. 实验设计
*   **数据集**：使用 **Alpaca Cleaned** 的多语言版本，涵盖 8 种语言：英语（en）、西班牙语（es）、德语（de）、加泰罗尼亚语（ca）、丹麦语（da）、塞尔维亚语（sr）、克罗地亚语（hr）和巴斯克语（eu）。
*   **实验场景（Benchmark）**：
    1.  **100% Mono**：每个客户端仅含一种语言（最高异构性）。
    2.  **混合比例（85% 到 15% Mono）**：逐渐增加客户端内的语言多样性，模拟趋向 IID 的分布。
    3.  **对比基准**：
        *   **Base Model**：原始预训练模型。
        *   **Local FT (Monolingual)**：在单语言数据上进行本地集中式微调。
        *   **Local FT (Multilingual)**：在所有语言汇总的数据上进行集中式微调（性能上限）。
*   **评估指标**：ROUGE-L 和 BERTScore ($F_{bert}$)，并使用标准差 ($\sigma$) 衡量多语言公平性。

### 4. 资源与算力
*   **模型规模**：使用 **salamandra-2b-instruct**（20亿参数的多语言 LLM）。
*   **训练细节**：使用 OneBitAdam 优化器，学习率 0.001。微批次（Micro-batch）大小为 2，梯度累积 16 步，每轮进行 10 次优化更新。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及具体的 GPU 数量。但文中通过“归一化优化步骤”详细对比了不同设置下的计算成本，指出联邦学习的总步数远低于训练 8 个独立单语言模型的总和。

### 5. 实验数量与充分性
*   **实验规模**：论文针对 6 种不同的客户端语言组成比例进行了完整的联邦学习实验，并与 8 个单语言本地微调实验及 1 个集中式多语言实验进行了对比。
*   **充分性与客观性**：
    *   实验涵盖了高资源、中资源和低资源语言组，具有代表性。
    *   引入了 **Bootstrap 显著性检验**（100 个样本集，p-value < 0.05），确保结果不是偶然产生的。
    *   通过控制变量法（保持每个客户端总样本数一致），排除了数据量对实验结果的干扰，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **语言组成的影响**：随着客户端内部语言多样性的增加（即从单语言向多语言混合转变），全局模型的平均性能提升，且语言间的性能差异（标准差）缩小，即**公平性提高**。
*   **低资源语言获益最大**：增加客户端的多语言性对低资源语言（如巴斯克语、塞尔维亚语）的提升最为显著（ROUGE-L 提升高达 16.11%）。
*   **性能与成本的权衡**：高度混合的多语言客户端虽然能产生更好的模型，但需要更多的优化步骤（训练时间更长）才能收敛。
*   **LDES-FL 的有效性**：相比标准早停，LDES-FL 在保持性能的同时，减少了约 22%-32% 的不必要计算步骤。
*   **联邦 vs 本地**：单语言本地微调在特定语言上表现最强，但联邦学习在构建单一、平衡的多语言模型方面具有显著优势，且总计算成本更低。

### 7. 优点与亮点
*   **机制创新**：提出的 **LDES-FL** 机制非常契合联邦学习的分布式特性，支持客户端动态“离线”和“上线”，具有很强的实用价值。
*   **系统性研究**：首次深入探讨了“客户端语言组成”这一特定变量对多语言 LLM 联邦学习的影响，填补了该领域的空白。
*   **关注公平性**：不仅关注平均分，还通过标准差深入分析了多语言环境下的算法公平性问题。

### 8. 不足与局限
*   **数据偏差风险**：实验使用的多语言 Alpaca 数据集是通过机器翻译生成的，且不同语言的样本并非严格平行，这可能导致评估跨语言迁移能力时存在偏差。
*   **算法覆盖有限**：实验主要基于 FedAvg 算法，未对比 FedProx、SCAFFOLD 等专门针对 Non-IID 优化的进阶联邦算法。
*   **硬件描述缺失**：缺乏具体的硬件环境和实际训练时长（小时/天）的描述，不利于其他研究者评估复现成本。
*   **模型规模限制**：实验仅在 2B 规模的模型上进行，尚不清楚相关结论在更大规模（如 70B+）模型上是否依然成立。

（完）
