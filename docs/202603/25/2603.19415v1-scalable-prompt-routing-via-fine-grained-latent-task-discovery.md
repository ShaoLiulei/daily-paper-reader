---
title: Scalable Prompt Routing via Fine-Grained Latent Task Discovery
title_zh: 通过细粒度潜在任务发现实现的可扩展提示词路由
authors: "Yunyi Zhang, Soji Adeshina, Patrick Guan, Ashwin Ganesh, Zhen Han, Vassilis N. Ioannidis, Huzefa Rangwala, George Karypis"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19415v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型池的提示词路由
tldr: 本研究针对大规模语言模型路由中的细粒度任务区分难题，提出了一种两阶段路由架构。该架构首先通过基于图的聚类自动发现潜在任务类型并进行分类，随后利用混合专家架构（MoE）进行任务感知的质量评估。实验表明，该方法在10个基准测试中均优于现有基准，在成本降低一半以上的同时，性能超越了最强的单一模型，为高效且可扩展的模型路由提供了新方案。
selection_source: fresh_fetch
motivation: 现有的模型路由方法依赖手动定义的任务分类，难以捕捉细粒度的能力差异，且单一路由模型难以应对多样化任务。
method: 提出一种两阶段架构，通过图聚类自动发现潜在任务，并结合具有任务特定预测头的混合专家架构进行质量评估。
result: "在10个基准测试和11个前沿模型上的评估显示，该方法在成本降低超过50%的情况下，性能优于最强的单一模型。"
conclusion: 通过自动化细粒度任务发现与任务感知评估的结合，可以显著提升大规模语言模型路由的效率与准确性。
---

## 摘要
提示词路由为每个查询从候选池中动态选择最合适的大语言模型，在管理成本的同时优化性能。随着模型池扩展到包含数十个性能差距较小的尖端模型，现有方法面临重大挑战：手动定义的任务分类法无法捕捉细粒度的能力差异，而单一路由模型难以区分不同任务间的细微差别。我们提出了一种两阶段路由架构，通过自动化的细粒度任务发现和任务感知质量评估来解决这些局限性。第一阶段采用基于图的聚类来发现潜在任务类型，并训练一个分类器将提示词分配给发现的任务。第二阶段使用带有任务特定预测头的混合专家架构，进行专门的质量评估。在推理时，我们聚合两个阶段的预测结果，以平衡任务层面的稳定性和提示词层面的适应性。在包含 11 个尖端模型的 10 个基准测试上的评估结果显示，我们的方法始终优于现有基准，并在成本不到一半的情况下超越了最强的单个模型。

## Abstract
Prompt routing dynamically selects the most appropriate large language model from a pool of candidates for each query, optimizing performance while managing costs. As model pools scale to include dozens of frontier models with narrow performance gaps, existing approaches face significant challenges: manually defined task taxonomies cannot capture fine-grained capability distinctions, while monolithic routers struggle to differentiate subtle differences across diverse tasks. We propose a two-stage routing architecture that addresses these limitations through automated fine-grained task discovery and task-aware quality estimation. Our first stage employs graph-based clustering to discover latent task types and trains a classifier to assign prompts to discovered tasks. The second stage uses a mixture-of-experts architecture with task-specific prediction heads for specialized quality estimates. At inference, we aggregate predictions from both stages to balance task-level stability with prompt-specific adaptability. Evaluated on 10 benchmarks with 11 frontier models, our method consistently outperforms existing baselines and surpasses the strongest individual model while incurring less than half its cost.

---

## 论文详细总结（自动生成）

以下是对论文《Scalable Prompt Routing via Fine-Grained Latent Task Discovery》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）的爆发式增长，不同模型在不同任务上表现各异。**提示词路由（Prompt Routing）**旨在为每个查询动态选择最合适的模型，以平衡性能与成本。
*   **核心挑战**：现有的路由方法要么依赖**手动定义的粗粒度任务分类**（无法捕捉模型间的细微差异），要么使用**单一路由模型**（难以在海量异构任务中进行精确区分）。
*   **研究背景**：当候选模型池包含数十个性能接近的尖端模型（如 Claude 4.5, DeepSeek-R1 等）时，如何实现自动化、细粒度且可扩展的路由成为了亟待解决的问题。

### 2. 方法论：FineRouter 架构
论文提出了一种名为 **FineRouter** 的两阶段路由架构：

*   **第一阶段：潜在任务发现与匹配（Stage 1）**
    *   **任务发现**：利用 LLM 为训练数据生成简短的任务描述。
    *   **图聚类**：构建提示词图，节点为提示词，边权重结合了**语义相似度**和**模型偏好相似度**（使用 RBO 秩相关度量）。通过 Leiden 社区检测算法自动发现数百个细粒度任务簇。
    *   **候选缩减**：为每个任务簇自适应选择表现最好的模型子集。
    *   **分类器训练**：训练一个多标签文本分类器，将新提示词映射到这些发现的任务类型上。
*   **第二阶段：任务感知动态路由（Stage 2）**
    *   **MoE 架构**：采用混合专家（Mixture-of-Experts）质量评估架构。
    *   **专用预测头**：包含“通用适配器”（预测所有模型的基准质量）和“任务特定适配器”（仅针对该任务下的候选模型进行精细预测）。
*   **推理聚合**：最终路由分数由第一阶段的任务层级先验得分和第二阶段的提示词层级预测得分加权聚合而成。

### 3. 实验设计
*   **数据集/场景**：涵盖 10 个主流基准测试，包括问答（NQ, TriviaQA）、多项选择（MMLU, ARC）、数学推理（GSM8K, MATH）和代码生成（HumanEval, MBPP）。
*   **候选模型池**：包含 11 个前沿模型，如 Claude-Sonnet-4.5、DeepSeek-R1、Llama-4-Maverick、Qwen3-235B 等。
*   **对比方法（Benchmark）**：
    *   **传统方法**：kNN, MLP。
    *   **先进路由算法**：RouteLLM, RouterDC, GraphRouter, IPR。
*   **评估指标**：任务特定指标（如准确率、Pass@1）的宏平均值，以及基于 Skywork-Reward-V2 的质量得分。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练时长**：整个流水线训练约需 **17 小时**（总计约 135 GPU 小时）。
    *   基础模型训练：6 小时。
    *   任务特定适配器微调：11 小时。
*   **模型规模**：提示词编码器采用 STELLA (400M) 和 Qwen3-Embedding (0.6B)。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同领域的基准测试上进行了广泛测试，训练样本量约为 27.9 万，测试样本约 3.5 万。
*   **消融实验**：对比了“仅阶段 1”、“仅阶段 2”、“粗粒度分类 vs. 细粒度发现”等变体，验证了各组件的有效性。
*   **客观性**：使用了 11 个真实的前沿模型而非小型开源模型，更贴近工业界实际部署场景；采用统一的奖励模型作为基准，避免了分类路由中常见的偏见问题。实验设计详尽且具有说服力。

### 6. 主要结论与发现
*   **性能超越单体最强**：FineRouter 的综合表现优于任何单个候选模型（包括最强的 Claude-Sonnet-4.5）。
*   **成本优势显著**：在达到甚至超过最强模型性能的同时，其推理成本**不到最强模型的一半**。
*   **细粒度发现的价值**：自动发现的 332 个细粒度任务比手动定义的 10 类粗粒度分类提供了更有效的路由信号。
*   **路由分布均衡**：系统并未盲目选择单一模型，而是根据任务特性在 Claude、DeepSeek、Llama 和 Qwen 之间进行了合理的负载分配。

### 7. 优点
*   **自动化与可扩展性**：无需人工干预即可从数据中提取任务结构，能够轻松扩展到包含数百个模型的模型池。
*   **任务感知专家化**：通过 MoE 架构让预测头专注于特定任务，解决了单一路由模型在处理复杂多样任务时的“能力平庸”问题。
*   **冷启动与鲁棒性**：结合了任务层级的稳定性和提示词层级的灵活性，对未见过的提示词具有更好的泛化能力。

### 8. 不足与局限
*   **依赖奖励模型**：路由器的训练质量高度依赖于所使用的奖励模型（如 Skywork-Reward-V2），若奖励模型在某些领域（如创意写作）存在偏见，路由效果会受限。
*   **训练成本**：训练阶段需要获取所有候选模型对训练数据的响应结果，这在模型池极大时会产生较高的前期数据采集成本。
*   **超参数敏感性**：图聚类中的 RBO 阈值、覆盖率阈值等超参数需要针对不同数据集进行微调。
*   **模态限制**：目前仅支持文本提示词，尚未扩展到多模态（图像、音频）路由场景。

（完）
