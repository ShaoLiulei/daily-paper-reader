---
title: Training-Free Sparse Attention for Fast Video Generation via Offline Layer-Wise Sparsity Profiling and Online Bidirectional Co-Clustering
title_zh: 通过离线逐层稀疏度分析与在线双向协同聚类实现快速视频生成的无需训练稀疏注意力
authors: "Jiayi Luo, Jiayu Chen, Jiankun Wang, Cong Wang, Hanxin Zhu, Qingyun Sun, Chen Gao, Zhibo Chen, Jianxin Li"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18636v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 扩散Transformer的稀疏注意力机制
tldr: 针对视频生成模型（DiT）推理成本高的问题，本文提出了一种无需训练的稀疏注意力框架SVOO。该方法通过离线分析各层的固有稀疏性敏感度，并结合在线双向协同聚类算法进行块级稀疏化，解决了现有方法忽视层间异质性和查询-键耦合的问题。实验表明，SVOO在保持高质量生成效果的同时，实现了高达1.93倍的加速，显著优于现有技术。
selection_source: fresh_fetch
motivation: 现有视频生成稀疏注意力方法忽视了层间异质性和查询-键耦合，导致生成质量与推理速度难以兼顾。
method: 提出SVOO框架，通过离线层级敏感度分析确定各层剪枝比例，并利用在线双向协同聚类算法实现高效的块级稀疏注意力。
result: 在七个主流视频生成模型上验证，SVOO实现了最高1.93倍的加速，并在Wan2.1模型上保持了29 dB的高PSNR。
conclusion: SVOO通过结合离线层级特性与在线动态聚类，为高效、高质量的视频生成提供了一种有效的免训练稀疏化方案。
---

## 摘要
扩散 Transformer (DiTs) 实现了强大的视频生成质量，但由于密集的 3D 注意力而面临高昂的推理成本，这促使了稀疏注意力技术的发展以提高效率。然而，现有的视频生成中无需训练的稀疏注意力方法仍面临两个尚未解决的局限性：在注意力剪枝中忽略了层间异构性，以及在分块划分中忽略了查询-键（query-key）耦合，这阻碍了更好的质量-加速权衡。在这项工作中，我们发现了一个关键见解：每一层的注意力稀疏度是其固有属性，受不同输入的影响较小。受此启发，我们提出了 SVOO，这是一个通过离线逐层稀疏度分析和在线双向协同聚类实现快速视频生成的无需训练稀疏注意力框架。具体而言，SVOO 采用了一个两阶段范式：(i) 离线逐层敏感度分析以推导固有的每层剪枝水平，以及 (ii) 通过一种新颖的双向协同聚类算法实现在线分块稀疏注意力。在七个广泛使用的视频生成模型上进行的广泛实验表明，SVOO 相比现有最先进方法实现了更优的质量-加速权衡，在 Wan2.1 上实现了高达 1.93 倍的加速，同时保持了高达 29 dB 的 PSNR。

## Abstract
Diffusion Transformers (DiTs) achieve strong video generation quality but suffer from high inference cost due to dense 3D attention, leading to the development of sparse attention technologies to improve efficiency. However, existing training-free sparse attention methods in video generation still face two unresolved limitations: ignoring layer heterogeneity in attention pruning and ignoring query-key coupling in block partitioning, which hinder a better quality-speedup trade-off. In this work, we uncover a critical insight that the attention sparsity of each layer is its intrinsic property, with minor effects across different inputs. Motivated by this, we propose SVOO, a training-free Sparse attention framework for fast Video generation via Offline layer-wise sparsity profiling and Online bidirectional co-clustering. Specifically, SVOO adopts a two-stage paradigm: (i) offline layer-wise sensitivity profiling to derive intrinsic per-layer pruning levels, and (ii) online block-wise sparse attention via a novel bidirectional co-clustering algorithm. Extensive experiments on seven widely used video generation models demonstrate that SVOO achieves a superior quality-speedup trade-off over state-of-the-art methods, delivering up to $1.93\times$ speedup while maintaining a PSNR of up to 29 dB on Wan2.1.

---

## 论文详细总结（自动生成）

### 论文总结：SVOO —— 基于离线层级分析与在线双向聚类的免训练视频生成加速框架

#### 1. 核心问题与研究动机
*   **核心问题**：基于扩散 Transformer (DiT) 的视频生成模型虽然质量卓越，但其 3D 自注意力机制具有随 Token 数量平方增长的计算复杂度，导致推理成本极高。
*   **研究动机**：现有的“免训练稀疏注意力”方法存在两个关键缺陷：
    1.  **忽视层间异质性 (Layer Heterogeneity)**：现有方法通常对所有 Transformer 层应用统一的稀疏比例，忽略了不同层对剪枝的敏感度差异。
    2.  **忽视 Q-K 耦合 (Q-K Coupling)**：在进行分块（Block partitioning）时，现有方法独立地对查询 (Query) 和键 (Key) 进行分组，导致 Q-K 之间的语义对应关系错位，降低了稀疏模式的准确性。

#### 2. 方法论
论文提出了 **SVOO** 框架，包含两个核心阶段：
*   **离线阶段：逐层稀疏度分析 (Offline Layer-Wise Sparsity Profiling)**
    *   **核心发现**：通过理论证明（定理 4.2）和实证分析发现，每一层的注意力稀疏度是其**固有属性**，在不同输入下表现出极高的稳定性（Layer-wise Stability），但在不同层之间差异巨大（Layer-wise Heterogeneity）。
    *   **技术细节**：使用少量随机输入作为校准集，计算每层每个头的注意力密度，并利用高斯分布拟合得到保守的剪枝阈值，从而为每一层定制专属的稀疏计划。
*   **在线阶段：双向协同聚类 (Online Bidirectional Co-Clustering)**
    *   **核心思想**：为了解决 Q-K 耦合问题，提出了一种交替迭代的聚类算法。
    *   **流程**：首先根据当前的查询中心对键进行聚类（查询感知的键划分），然后根据更新后的键中心对查询进行聚类（键感知的查询划分）。
    *   **优化**：为了降低聚类开销，SVOO 引入了**聚类重用机制**（每隔 $N$ 个扩散步重新计算一次）和基于 **Triton** 的自定义内核实现。

#### 3. 实验设计
*   **数据集与场景**：
    *   **文本生成视频 (T2V)**：使用 VBench 评测集及其提示词优化版本。
    *   **图像生成视频 (I2V)**：使用 VBench++ 提示词-图像对。
*   **模型 (Benchmark)**：涵盖了 7 个主流模型，包括 Wan2.1 (1.3B/14B)、Wan2.2 (A14B)、HunyuanVideo 等。
*   **对比方法**：SpargeAttention、SVG、SVG2、Radial 等当前最先进的免训练加速方法。
*   **评测指标**：PSNR、SSIM、LPIPS（衡量与原始模型的一致性）；VBench 综合得分（图像质量、美学、一致性等）；推理延迟与加速比。

#### 4. 资源与算力
*   **硬件平台**：所有实验均在单张 **NVIDIA H200 GPU** 上完成。
*   **生成配置**：分辨率为 720p (720×1280)。Wan 系列生成 81 帧，HunyuanVideo 系列生成 129 帧。
*   **训练时长**：由于是免训练方法，不涉及模型训练。离线分析阶段仅需极短时间（使用 10 个校准输入即可）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个不同的模型架构上进行了全面测试，涵盖了从 1.3B 到 14B 以及 MoE 架构（Wan2.2）。
*   **消融实验**：针对离线分析模块、在线双向聚类模块分别进行了消融，验证了各自对效率和质量的贡献。
*   **参数敏感性分析**：详细探讨了聚类重用间隔、块数量（#Q/#K Blocks）、预热步数（Warm-up）对结果的影响。
*   **评价**：实验设计非常充分且客观。通过跨模型、跨任务（T2V/I2V）的对比，证明了该方法的通用性和鲁棒性。

#### 6. 主要结论与发现
*   **性能领先**：SVOO 在所有测试模型上均实现了最佳的“质量-速度”权衡。例如在 Wan2.1-1.3B 上实现了 **1.93×** 的加速，同时保持了约 30 dB 的 PSNR。
*   **层级特性**：验证了 DiT 模型中层级稀疏度的稳定性，证明了“离线分析+在线应用”策略的有效性。
*   **耦合重要性**：双向协同聚类相比于 SVG2 使用的独立 K-means 聚类，能显著提高注意力召回率（Attention Recall），从而在更高加速比下维持生成质量。

#### 7. 优点
*   **免训练且通用**：无需昂贵的重训练过程，可直接应用于现有的各种 DiT 架构。
*   **理论与实证结合**：不仅有直观的实验观察，还提供了关于层级稀疏度稳定性的数学证明。
*   **高效实现**：通过聚类重用和高性能算子（FlashInfer/Triton）确保了理论上的节省能转化为实际的推理加速。

#### 8. 不足与局限
*   **硬件依赖**：虽然方法通用，但实际加速效果高度依赖于底层算子（如 FlashInfer）对特定硬件（NVIDIA GPU）的优化。
*   **超参数敏感性**：虽然提供了默认配置，但针对不同模型可能仍需微调预热步数和重用间隔以达到极致性能。
*   **极端场景**：对于极长视频或极高动态变化的视频，聚类重用机制可能会带来轻微的质量波动（尽管文中显示影响较小）。

（完）
