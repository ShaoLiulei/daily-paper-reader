Title: Trained Persistent Memory for Frozen Decoder-Only LLMs

URL Source: https://arxiv.org/pdf/2603.22329v1

Published Time: Wed, 25 Mar 2026 00:03:42 GMT

Number of Pages: 15

Markdown Content:
# Trained Persistent Memory for Frozen Decoder-Only LLMs 

Hong Jeong 

Inha University in Tashkent, Uzbekistan 

hjeong@postech.ac.kr 

## Abstract 

Decoder-only language models are stateless: hidden representations are discarded after every forward pass and nothing persists across sessions. Jeong (2026a) showed that trained memory adapters give a frozen encoder–decoder backbone persistent latent-space memory, building on the lateral-memory framework of Jeong (2026b, c). Here we ask whether the same principle transfers to the decoder-only setting, where no cross-attention pathway exists and memory must enter through self-attention alone. We adapt six methods—prefix, parallel cross-attention, KV extension, Hebbian memory, context-gated branch, and slot-based sparse write—to a frozen GPT-2, training only a small adapter θMem . The write rule is shared; only the read injection changes from decoder cross-attention to self-attention KV prefix or parallel branch. On LoCoMo we find a striking inductive-bias dichotomy : at 1× capacity, three methods with strong architectural priors—cross-attention (M.2), Hebbian (M.4), and slot write (M.6)—achieve retained-memory scores of 7–18% and knowledge gains ∆K of 7–10, while the other three fail (<0.4%). At 10 × capacity all six converge, showing the gap is architectural, not fundamental. Together with the encoder–decoder results of Jeong (2026a) and the brain-inspired modules of Jeong (2026b, c), these findings establish persistent latent-space memory as a general paradigm spanning major transformer families. 

## 1 Introduction 

Modern decoder-only language models—GPT-2 [ 1], LLaMA [ 2], Phi-2 [ 3], Mistral [ 4]—process input autoregressively through a stack of causal self-attention layers. The forward pass is: 

Ht = Dfrozen (xt), ˆyt = head( Ht), (1) where Dfrozen is the frozen transformer body, Ht ∈ Rn×d is the final hidden state, and ˆyt is the output distribution. After generation, Ht is discarded. There is no mechanism for information to survive from one session to the next. Jeong [5] introduced persistent latent-space memory for frozen encoder–decoder models, showing that a small trained adapter θMem can maintain a memory bank Pt ∈ RnP ×d that accumulates across turns and enables conversational learning : facts stated in session 1 can be recalled in session 10 without re-stating them and without million-token context windows. The key insight was that the write rule is a differentiable operation on dense vectors, not a text-level retrieval step, and that training is necessary for the frozen backbone to discriminate useful memory entries from noise. The encoder–decoder setting, however, offered a natural injection point: the decoder’s cross-attention heads, pre-trained to attend to external encoder outputs, could be re-purposed to attend to memory entries with minimal architectural disruption. Decoder-only models lack this pathway. Their only attention mechanism is causal self-attention, which was pre-trained to attend to preceding tokens within the same sequence. Injecting persistent memory therefore requires a fundamentally different read strategy. 

Preprint. 

> arXiv:2603.22329v1 [cs.LG] 20 Mar 2026

We hypothesise, however, that persistent memory is most naturally aligned with decoder-only archi-tectures, despite the injection challenge. The key observation is that autoregressive generation is inherently sequential : each token step already reads from a KV cache that summarises all preceding context, and persistent memory is simply a generalisation of this cache to information that survives across sessions. Encoder–decoder models, by contrast, process context bidirectionally in the encoder and must bridge an architectural gap between the encoder representation and the decoder’s memory needs. Encoder-only models (BERT-style) lack generation entirely and process input in a single bidi-rectional pass with no temporal flow—the least natural setting for incremental memory accumulation. This motivates a conjectured ordering of architectural affinity: Decoder-only > Encoder–decoder > Encoder-only for persistent latent-space memory. Jeong [5] already established the middle case; the present paper tests the strongest case. This paper makes three contributions: 1. Architecture transfer. We adapt all six memory methods from Jeong [5] to a frozen decoder-only backbone (GPT-2), replacing cross-attention read paths with self-attention-compatible alterna-tives: KV prefix injection, parallel cross-attention insertion (Flamingo-style), and gated additive branches. 2. Inductive-bias dichotomy. At standard capacity ( 1×), only three methods with strong archi-tectural priors succeed (M.2 XAttn, M.4 Hebbian, M.6 Slot), while the other three fail. At 10 ×

capacity, all six methods converge, revealing that architectural bias determines efficiency under constrained capacity. 3. Two-metric evaluation. We evaluate persistent memory on both the absolute forgetting-curve protocol (retained-memory score) and a knowledge-accumulation metric ( ∆K), providing com-plementary views of memory retention and knowledge growth over 30 sessions. 

## 2 Related Work 

Persistent memory for LLMs. Application-level memory systems such as MemGPT [ 8] and MemoryBank [ 9 ] operate at the text level: facts are stored as natural-language strings and retrieved via search. These systems are architecture-agnostic by construction but add inference-time overhead proportional to memory size and cannot benefit from gradient-based training of the memory path-way. Our approach operates at the latent level: the memory bank P ∈ RnP ×d stores continuous representations, and both read and write are differentiable operations inside the forward pass. 

KV caching and context extension. Decoder-only models routinely cache key–value pairs from earlier tokens to avoid recomputation [ 11 ]. Several works extend this cache across sequences: Memorizing Transformers [ 12 ] retrieve from a growing external KV store, and ∞-former [ 13 ]compresses past KV pairs into a fixed-size summary. These methods cache raw activations; ours learns a compressed, trained representation that the frozen backbone can discriminate. The distinction is precisely what Jeong [5] established: without trained projections, accumulated cache states dilute attention rather than sharpen retrieval. 

Recurrent and compressive context extension. Transformer-XL [ 17 ] introduces segment-level recurrence that carries hidden states across fixed-length segments, extending the effective receptive field without recomputation. The Compressive Transformer [ 18 ] adds a second level of memory by compressing older activations into a lossy summary, trading exact recall for longer range. The Recurrent Memory Transformer [ 19 ] appends learned memory tokens that flow between segments. These methods extend context within a single document or session; our framework extends memory 

across sessions, storing persistent representations that accumulate over an arbitrarily long dialogue history. 

Learned external memory. Neural Turing Machines [ 20 ] and the Differentiable Neural Com-puter [ 21 ] introduced differentiable read/write access to an external memory matrix, trained end-to-end. Our memory bank P shares this differentiable read/write design, but differs in two respects: the transformer backbone is frozen and only the memory adapter is trained, and the memory persists across independent inference sessions rather than within a single forward pass. 2Parameter-efficient adaptation. Prefix tuning [ 14 ] prepends learnable soft tokens to each layer; LoRA [ 15 ] injects low-rank updates into attention projections. Both adapt a frozen model’s behaviour but are static after training—they do not accumulate new information at inference time. Our memory adapter combines a trained read/write pathway (like prefix tuning) with a persistent state that grows (like a KV cache), yielding a system that is both trained and dynamic. 

Attention-coupled latent memory. Jeong [6] introduced the A⊤AV W write-back operator for persistent lateralised memory banks and showed that inhibitory cross-talk enables functional lateralization—an insight that informs our understanding of why certain injection strategies succeed. Jeong [7] extended the framework with brain-region modules (thalamic gating, hippocampal lateral-ization, amygdaloid salience, prefrontal working memory), demonstrating that biological inductive biases shape memory dynamics even in small-scale models. Jeong [5] brought persistent memory to frozen encoder–decoder LLMs, establishing the forgetting-curve evaluation protocol that we adopt here. The present paper extends that framework to decoder-only models, completing the coverage of major architecture families. 

## 3 Problem Setting 

We formalise the decoder-only persistent-memory problem in three steps: first the stateless baseline that defines the control condition (§3.1), then the addition of a persistent memory bank that converts a stateless model into a stateful one (§3.2), and finally the architectural challenge specific to decoder-only models—the absence of a cross-attention pathway (§3.3). 

3.1 Stateless decoder-only baseline 

A frozen decoder-only model processes the current turn xt = ( x1 

> t

, . . . , x nt ) autoregressively. At each layer ℓ, the hidden state H(ℓ) ∈ Rn×d (sequence length n, model dimension d) is transformed by causal self-attention: 

Q(ℓ) = H(ℓ)W (ℓ) 

> Q

, K(ℓ) = H(ℓ)W (ℓ) 

> K

, V (ℓ) = H(ℓ)W (ℓ) 

> V

,A(ℓ) = softmax 

 Q(ℓ)K(ℓ)⊤

√dk

+ Mcausal 



, (2) where W (ℓ) 

> Q

, W (ℓ) 

> K

∈ Rd×dk , W (ℓ) 

> V

∈ Rd×dv , Q(ℓ), K (ℓ) ∈ Rn×dk , V (ℓ) ∈ Rn×dv , A(ℓ) ∈ Rn×n,and Mcausal ∈ Rn×n is the causal mask. All W matrices are frozen. The output Ht ∈ Rn×d at the final layer is the model’s only representation of the input; it is discarded after generation. Figure 1 illustrates the stateless pipeline.     

> xtDfrozen Hthead ˆyt
> ephemeral—discarded after each turn

Figure 1: Frozen decoder-only baseline. The hidden state Ht is consumed within the current turn and then discarded; no information persists across sessions. 

3.2 Adding persistent memory 

We augment the stateless system with a persistent memory bank Pt−1 ∈ RnP ×d that survives across turns: 

Ht = Dfrozen (xt; Pt−1), Pt = Write( Pt−1, H t), ˆyt = head( Ht). (3) The semicolon in Dfrozen (xt; Pt−1) indicates that Pt−1 is injected into the frozen forward pass through a method-specific read path. The Write operation updates memory from the current hidden state. The learned parameters θMem (read projections, write projections, gates) are the only trainable weights; all original model parameters remain frozen. 33.3 The decoder-only challenge: no cross-attention 

In encoder–decoder models, the decoder’s cross-attention heads provide a natural injection point: they were pre-trained to attend to external representations (encoder outputs), so re-purposing them for memory entries requires minimal architectural disruption. Decoder-only models have no such pathway. Their self-attention heads were pre-trained to attend exclusively to preceding tokens within the same sequence. Injecting external persistent state requires one of three strategies (illustrated in Figure 2): 1. KV prefix. Project P into additional key–value pairs and prepend them to each layer’s self-attention. The model “sees” memory as if it were earlier tokens. This is the most natural analog of how decoder-only models already process context. 2. Parallel cross-attention insertion. Insert new cross-attention layers alongside the existing self-attention (Flamingo-style). More expressive but heavier: each inserted layer adds a full set of Q,

K, V , O projections. 3. Gated additive branch. Compute a memory readout and add it to the hidden state through a learned gate, without modifying the self-attention computation itself.         

> (a) KV Prefix
> Pt−1KMem , V Mem Self-Attn H(ℓ)′
> prepend
> (b) Parallel XAttn
> Self-Attn
> Pt−1XAttn Mem
> +H(ℓ)′
> β
> (c) Gated Branch
> Self-Attn
> Pt−1XAttn Mem gt⊙
> +H(ℓ)′

Figure 2: Three decoder-only injection strategies. (a) KV prefix prepends memory-derived keys and values to the self-attention cache. (b) Parallel cross-attention inserts a new attention pathway alongside the existing self-attention. (c) Gated branch computes a memory readout and adds it through a learned content-dependent gate. Table 1 maps the six methods to their decoder-only read strategies. Table 1: Mapping of the six memory methods from the encoder–decoder setting [ 5 ] to the decoder-only setting. The write rule is identical; only the read injection point changes. 

Method Enc-Dec read path Decoder-only read path Strategy M.1 Prefix Encoder-input prefix Self-attention KV prefix KV prefix M.2 XAttn Parallel decoder XAttn Parallel cross-attention Inserted XAttn M.3 KV Ext Decoder KV extension Self-attention KV extension KV prefix M.4 Hebbian Decoder KV extension Self-attention KV extension KV prefix M.5 Gated Context-gated decoder Gated additive branch Gated branch M.6 Slot Decoder KV extension Self-attention KV extension KV prefix 

44 Adapting the Six Methods to Decoder-Only Models 

The write rule for all methods is architecture-agnostic: it takes the current hidden state Ht and the previous memory Pt−1 and produces Pt. We reproduce each write rule verbatim from Jeong [5] and describe only the decoder-only read adaptations below. 

4.1 Shared write rule 

For methods M.1–M.3, M.5, and M.6, the write rule is the attention-coupled update: 

Qw = HtW wQ , Kw = Pt−1W wK , Vw = HtW wV ,At = softmax 

 QwK⊤

> w

√d



, Pt = γ P t−1 + A⊤ 

> t

Vw. (4) Here W wQ , W wV ∈ Rd×d project the hidden state and W wK ∈ Rd×d projects memory, yielding 

Qw, V w ∈ Rn×d, Kw ∈ RnP ×d, and At ∈ Rn×nP . Values Vw are drawn from Ht (not from P ), ensuring new content enters memory regardless of P ’s current state. For M.4 (Hebbian), the write is an outer-product rule; for M.6 (Slot), a sparse top-k overwrite. These are unchanged from the encoder–decoder setting. 

4.2 M.1: Self-Attention KV Prefix 

Memory Pt−1 ∈ RnP ×d is projected into soft key–value pairs and prepended to every self-attention layer: 

K(ℓ)Mem = Pt−1 W (ℓ)

> K, Mem

, V (ℓ)Mem = Pt−1 W (ℓ)

> V, Mem

,

˜K(ℓ) = K(ℓ)Mem ; K(ℓ), ˜V (ℓ) = V (ℓ)Mem ; V (ℓ). (5) Here W (ℓ) 

> K, Mem

∈ Rd×dk and W (ℓ) 

> V, Mem

∈ Rd×dv are learnable projections, yielding K(ℓ)Mem ∈ RnP ×dk ,

V (ℓ)Mem ∈ RnP ×dv , and concatenated tensors ˜K(ℓ) ∈ R(nP +n)×dk , ˜V (ℓ) ∈ R(nP +n)×dv . The causal mask is extended: memory positions are visible to all input tokens (no causal restriction), while the original causal structure among input tokens is preserved. The query Q(ℓ) is unchanged. This is the most natural decoder-only injection: the model treats memory entries as if they were earlier tokens in the sequence. Figure 3 illustrates the KV prefix injection.           

> Pt−1KMem , V Mem [KMem ;K],[VMem ;V]Causal Self-Attn Ht
> A⊤
> tVwwrite
> Pt

Figure 3: M.1: Memory is projected into soft key–value pairs and prepended to the self-attention cache at every layer. The write rule updates P from Ht.

4.3 M.2: Inserted Parallel Cross-Attention 

A new cross-attention layer is inserted after each frozen self-attention block, attending from the hidden state to the memory bank: 

c(ℓ)Mem = XAttn (ℓ)Mem 

 H(ℓ), P t−1

, H(ℓ)′ = H(ℓ) + β(ℓ) c(ℓ)Mem , (6) where c(ℓ)Mem ∈ Rn×d is the cross-attention output and β(ℓ) ∈ R is a learnable scalar initialised to zero, so the model starts as the unmodified frozen baseline. This follows the Flamingo design [ 16 ]and is the only method that introduces a genuinely new attention pathway into the decoder-only model. Figure 4 shows the parallel insertion. 5Causal Self-Attn H(ℓ)

Pt−1 β(ℓ)· XAttn Mem (H, P )

H(ℓ)′ = H(ℓ) + β c Mem 

Self-attn path untouched ✓

Figure 4: M.2: A parallel cross-attention layer is inserted after each frozen self-attention block. The scaling factor β(ℓ) is initialised to zero for safe startup. 

4.4 M.3: Self-Attention KV Extension 

M.3 uses the same KV extension read path as M.1—projecting Pt−1 into key–value pairs and concatenating them with the frozen self-attention—but applies the attention-coupled write rule (Eq. 4) with per-layer projections. Each frozen layer ℓ maintains its own read projection pair: 

K(ℓ)ext = Pt−1 W (ℓ)

> K, ext

, V (ℓ)ext = Pt−1 W (ℓ)

> V, ext

, (7) where W (ℓ) 

> K, ext

∈ Rd×dk and W (ℓ) 

> V, ext

∈ Rd×dv are trainable, yielding K(ℓ)ext ∈ RnP ×dk and V (ℓ)ext ∈

RnP ×dv . These are concatenated with the frozen self-attention keys and values: 

˜K(ℓ) = K(ℓ)ext ; K(ℓ), ˜V (ℓ) = V (ℓ)ext ; V (ℓ), (8) producing ˜K(ℓ) ∈ R(nP +n)×dk and ˜V (ℓ) ∈ R(nP +n)×dv . The causal mask extension and query Q(ℓ)

are identical to M.1 (Eq. 5). The key difference from M.1 is that M.3’s write update is also applied per-layer: the attention-coupled rule P (ℓ) 

> t

= γP (ℓ) 

> t−1

+ A(ℓ)

> t
> ⊤

V (ℓ) 

> w

uses layer-specific projections, so each transformer layer can write to and read from its own view of the memory bank. M.1, by contrast, shares a single global write across all layers. Figure 5 illustrates the per-layer KV extension.               

> Pt−1K(ℓ)ext , V (ℓ)ext [Kext ;K],[Vext ;V]Causal Self-Attn Ht
> per-layer A(ℓ)⊤
> tV(ℓ)
> wwrite
> per-layer
> P(ℓ)
> t

Figure 5: M.3: Memory is projected into per-layer key–value pairs and concatenated with the frozen self-attention cache. Unlike M.1, both the read projections and the write update are layer-specific. 

4.5 M.4: Hebbian / Associative Memory 

The Hebbian matrix Mt ∈ Rdh×dh is queried by the current hidden state to produce a recall vector, which is then exposed to the decoder as additional KV entries: 

Rt = ( Ht WQ,H ) Mt−1, K(ℓ) 

> M

= Rt W (ℓ)

> K, Mem

, V (ℓ) 

> M

= Rt W (ℓ)

> V, Mem

, (9) where WQ,H ∈ Rd×dh projects the hidden state into the associative space, yielding Rt ∈ Rn×dh .The per-layer projections W (ℓ) 

> K, Mem

∈ Rdh×dk and W (ℓ) 

> V, Mem

∈ Rdh×dv produce K(ℓ) 

> M

∈ Rn×dk and 

V (ℓ) 

> M

∈ Rn×dv , concatenated to the self-attention keys and values as in M.1/M.3. Figure 6 illustrates the Hebbian read path. 

Ht

Mt−1

Rt = ( HtWQ,H ) Mt−1 [K; KM ], [V ; VM ] Self-Attn       

> Mt=γM +1
> n(HW K)⊤HW V

6Figure 6: M.4: The Hebbian matrix is queried by the current hidden state; the recalled vector is projected into additional KV pairs for self-attention. 

4.6 M.5: Gated Additive Branch 

A lightweight memory branch reads from P and contributes to the hidden state through a content-dependent gate: 

c(ℓ)Mem = XAttn (ℓ)Mem 

 H(ℓ), P t−1

,g(ℓ) 

> t

= σ W (ℓ) 

> g

[H(ℓ); c(ℓ)Mem ] + b(ℓ)

> g

,H(ℓ)′ = H(ℓ) + g(ℓ) 

> t

⊙ c(ℓ)Mem . (10) Here c(ℓ)Mem ∈ Rn×d is the memory readout, W (ℓ) 

> g

∈ Rd×2d maps the concatenation [H(ℓ); c(ℓ)Mem ] ∈

Rn×2d through a sigmoid to produce the gate g(ℓ) 

> t

∈ Rn×d, and b(ℓ) 

> g

∈ Rd is the gate bias. With 

b(ℓ) 

> g

< 0 at initialisation, the gate starts nearly closed (safe startup). Figure 7 illustrates the gated branch. 

Causal Self-Attn H(ℓ)

Pt−1 XAttn Mem (H, P ) gt ⊙ cMem 

H(ℓ)′

g → 0 ⇒ self-attn path unchanged ✓

Figure 7: M.5: A gated memory branch reads from P and contributes through a content-dependent gate. At initialisation bg < 0 keeps the gate nearly closed, preserving the frozen self-attention path. 

4.7 M.6: Slot-Based Sparse Write with KV Read 

The memory bank P ∈ RS×d is organised as S addressable slots. The write rule uses sparse top-k

addressing to selectively overwrite a small subset of slots each turn, preventing the dilution that affects dense write rules when capacity is limited. 

Addressing. A learnable address head computes a similarity score between each hidden-state token and every slot: 

aij = (Ht WA)i · (Pt−1 WS )j

√d , αj = max  

> i

aij , (11) where WA, W S ∈ Rd×d are learnable projections, aij is the affinity between token i and slot j, and 

αj ∈ R is the maximum affinity over all tokens for slot j. The top-k slots by αj are selected for writing. 

Sparse write. Let T = top -kj (αj ) be the index set of the k highest-scored slots. Each selected slot is updated with a gated blend of its previous content and the new value aggregated from the hidden state: 

P (j) 

> t

=

(

γ P (j) 

> t−1

+ (1 − γ) vj if j ∈ T ,P (j) 

> t−1

otherwise , (12) where vj = P 

> i

softmax( aij ) ( HtW wV )i ∈ Rd is the attention-weighted aggregation of hidden-state values into slot j, and γ is the shared decay factor. Only k of S slots change each turn, giving the write rule a discrete selection mechanism that prevents dilution. 

KV read. The read path exposes all S slots to the decoder via self-attention KV extension, identical to M.1/M.3: 

K(ℓ)Mem = Pt W (ℓ)

> K, Mem

, V (ℓ)Mem = Pt W (ℓ)

> V, Mem

, ˜K(ℓ) = [ K(ℓ)Mem ; K(ℓ)], ˜V (ℓ) = [ V (ℓ)Mem ; V (ℓ)].

(13) 7Figure 8 illustrates the slot-based design. 

Ht s1 s2 s3 s4 s5 s6 s7 s8

Pt−1: S slots 

Self-Attn   

> address + top-kwrite KV read all slots
> red = updated this turn

Figure 8: M.6: Memory is organised as S addressable slots. Only the top-k addressed slots are updated each turn (sparse write); all slots are read via self-attention KV extension. 

## 5 Training and Inference 

The training protocol mirrors Jeong [5] exactly. 

Type 1: Supervised learning. All adapter parameters θMem are trained while the decoder backbone remains frozen. Gradients flow through the frozen model (which acts as a fixed differentiable function) to reach θMem :

θMem ← θMem − η ∇θMem L Dfrozen (xt; Pt−1), y t

. (14) Write-side projections execute without gradients (detached) to prevent the computation graph from growing across the full conversation history. They act as fixed random maps that preserve pairwise distances (Johnson–Lindenstrauss property). 

Type 2: Conversational learning. After training, θMem is frozen but Pt continues to accumulate at inference time without gradients: Pt = Write( Pt−1, H t) with θMem fixed. Each new conversation enriches P , and the system becomes more informed with every turn—driven by ordinary dialogue rather than curated datasets. 

Implementation details. We use GPT-2 (124M parameters) as the frozen backbone in bfloat16. Memory adapters are trained with AdamW (learning rate 10 −4, weight decay 10 −2, linear warmup of 200 steps, gradient norm clipped at 1.0) for 10 epochs with batch size 4 and gradient accumulation 4 (effective batch 16). Shared memory hyperparameters: bank size nP = 64 , write decay γ = 0 .95 ,

dh = 256 (M.4), S = 64 slots with top-k = 8 writes (M.6)—all identical to the encoder–decoder setting. Truncated backpropagation uses a window of k = 8 turns. Early stopping halts training if validation loss does not improve for 3 consecutive epochs. Figure 9 contrasts the two phases. 

Frozen Dfrozen 

+ Memory adapter 

Type 1: Train θMem 

(backprop, offline) 

Type 2: Update Pt

(no grad, every turn)    

> ∇θL
> update θMem
> Ht
> update Pt

Figure 9: Two learning phases. Type 1 trains the memory adapter parameters offline by backpropaga-tion through the frozen decoder. Type 2 updates the persistent memory online at each conversation turn with all model weights frozen. 8Table 2 consolidates the six methods along key design dimensions for the decoder-only setting. Table 2: Six trained persistent-memory methods compared for decoder-only models. “Self-attn safe” means the original frozen self-attention route is preserved. “Memory cost” is per turn.                                               

> Method Injection strategy Self-attn safe New params Mem. cost Write mechanism
> 1Self-attention KV prefix KV prefix ✓∼2.4M const. A⊤
> tV
> 2Parallel cross-attention Inserted XAttn ✓∼4.1M const. A⊤
> tV
> 3Self-attention KV extension KV prefix ✓∼2.4M const. A⊤
> tV(per-layer) 4Hebbian / associative KV prefix ✓∼0.8M O(d2
> h)Hebbian outer prod. 5Gated additive branch Gated branch ✓∼4.7M const. gated XAttn 6Slot-based sparse write KV prefix ✓∼1.8M O(Sd )Top-koverwrite

## 6 Evaluation 

The preceding sections specify how each method reads and writes persistent memory; this section specifies what we measure and how we measure it. We adopt the absolute forgetting-curve protocol from Jeong [5] without modification, apply the same equal-input principle, and extend the evaluation to a cross-architecture comparison spanning encoder–decoder and decoder-only backbones. 

6.1 Forgetting-curve protocol 

We adopt the absolute forgetting-curve evaluation from Jeong [5] without modification. For a question q asked after T conversational turns with annotated evidence turns Eq , the evidence lag is: 

ℓq = T − min( Eq ). (15) The retained-memory score measures how much answer quality disappears when a method’s own persistent state is ablated: 

Rq (m) = max  0, F 1(ˆ yMem  

> q

(m), y q ) − F1(ˆ y0 

> q

(m), y q ). (16) The stateless baseline has Rq = 0 by construction. For decoder-only evaluation we use five lag buckets: [0 , 32) , [32 , 64) , [64 , 128) , [128 , 256) , and [256 , ∞) turns, averaged within each bucket, and smoothed by a weighted non-increasing isotonic fit (PAVA), following the five-bucket scheme from Jeong [5]. 

6.2 Equal-input principle 

Every condition—baseline and all six memory methods on each backbone—receives exactly the same input xt at each turn: the current conversational turn only. No method receives the full history. Any non-zero retained-memory score is attributable solely to the persistent state P .

6.3 Benchmark 

The primary benchmark is LoCoMo [ 10 ], a long-term conversational-memory dataset with explicit QA supervision and annotated evidence turns. 

6.4 Knowledge-accumulation protocol 

We complement the forgetting curve with a cumulative knowledge evaluation over LoCoMo’s 30 sessions. At each session boundary s, we measure the knowledge score Ks: the F1 improvement on factual questions about sessions 0, . . . , s relative to the stateless baseline. The overall metric 

∆K = K29 captures total accumulated knowledge at the final session. Positive ∆K means the model has learned facts from the conversation that the stateless baseline cannot access. 

6.5 Capacity conditions 

A central question is whether the inductive bias of a memory method or its raw capacity determines success. To disentangle the two, we evaluate every method under two capacity budgets that share all other hyperparameters (backbone, training schedule, decay γ). 9The standard ( 1×) setting uses the same hyperparameters as the encoder–decoder experiments of Jeong [5] : memory bank size nP = 64 , Hebbian dimension dh = 256 , slot count S = 64 , and top-k = 8 writes. This is a deliberately constrained regime: with 64 memory vectors the adapter must compress an entire conversation history into roughly 49 K parameters of persistent state. Any method that succeeds here must rely on a strong inductive bias to route the right information into the limited capacity. The large ( 10 ×) setting scales every capacity parameter by a factor of ten: nP = 640 , dh = 810 ,

S = 640 , top-k = 80 . If a method fails at 1× but succeeds at 10 ×, the failure is attributable to insufficient capacity rather than a fundamental incompatibility with the decoder-only injection path. Conversely, if a method already saturates at 1×, extra capacity yields diminishing returns—confirming that its inductive bias is already sufficient. Table 3 summarises the two conditions. Table 3: Memory capacity conditions. All other hyperparameters (backbone, learning rate, decay γ,training epochs) are shared. 

Parameter 1× 10 ×

Memory bank size nP 64 640 Hebbian dimension dh 256 810 Slot count S 64 640 Top-k writes 8 80 

## 7 Results 

We organise the empirical findings around four questions: which methods produce positive forgetting curves at 1× capacity (§7.1)? What separates the methods that succeed from those that fail (§7.2)? Does 10 × capacity bridge the gap (§7.3)? And does knowledge accumulate across sessions (§7.4)? 

7.1 Forgetting curves at 1× capacity 

Figure 10 shows the absolute forgetting curves for all six methods on GPT-2 at 1× capacity. Three methods produce substantial retained-memory scores: M.2 XAttn peaks at 17.8% for recent evidence and sustains 9.0% beyond 256 turns; M.4 Hebbian maintains a remarkably flat profile at 9.2–9.5% across all lag bins; M.6 Slot peaks at 17.2% but decays to 7.1% at longer lags. The remaining three methods—M.1 Prefix (0.02%), M.3 KV Extension (0.0%), and M.5 Gated (0.09–0.36%)—produce scores indistinguishable from the baseline.  

> 0–31
> 32 –63
> 64 –127
> 128 –255
> 256+ 0
> 5
> 10
> 15
> 20
> Evidence lag (turns in the past)
> Retained-memory score (%)
> GPT-2 ( 1×capacity)
> Baseline
> M.2 XAttn
> M.4 Hebbian
> M.6 Slot
> M.5 Gated
> M.1 Prefix
> M.3 KV Ext

Figure 10: Absolute forgetting curves on LoCoMo for the GPT-2 (124M) backbone at 1× capacity. Three methods (M.2, M.4, M.6) produce substantial retained-memory scores; three (M.1, M.3, M.5) are indistinguishable from the baseline. Higher and flatter curves indicate stronger memory. 10 7.2 The inductive-bias dichotomy 

The three successful methods share a common property: each injects memory through a mechanism with strong inductive bias for integrating external information into self-attention. • M.2 XAttn inserts a full parallel cross-attention pathway (Flamingo-style), the most expressive injection mechanism. • M.4 Hebbian computes an associative recall Rt = ( HtWQ) Mt−1 that explicitly routes content-addressed memory into new KV entries—a strong structural prior for pattern completion. • M.6 Slot uses sparse top-k addressing to overwrite specific memory slots, giving the write rule a discrete selection mechanism that prevents dilution. The three failing methods rely on weaker injection paths: M.1/M.3 use shared or per-layer KV prefix projection (equivalent to adding soft tokens), and M.5 filters through a content-dependent gate. At 

1× capacity these weaker priors cannot overcome the distributional gap between pre-trained token representations and persistent memory entries. Table 4 summarises the retained-memory scores and knowledge gains for both capacity conditions. Table 4: Retained-memory score (min across lag bins, %) and knowledge accumulation ( ∆K) for each method at 1× and 10 × capacity on GPT-2. Bold: best in column. Knowledge accumulation at 10 × is omitted: the forgetting-curve convergence already establishes the capacity-bridging result (§7.3).                                    

> 1×capacity 10 ×capacity Method Ret. mem. (%) ∆KRet. mem. (%) ∆K
> M.0 Baseline 0.00 5.57 0.00 5.57 M.1 Prefix 0.02 0.00 9.20 —M.2 XAttn 9.02 7.34 9.88 —M.3 KV Ext 0.00 0.00 9.69 —M.4 Hebbian 9.23 7.84 10.32 —M.5 Gated 0.09 0.17 7.62 —M.6 Slot 7.08 9.71 9.66 —

7.3 Capacity bridging: 1× vs. 10 ×

Figure 11 shows the forgetting curves at 10 × capacity. All six methods now produce substantial retained-memory scores (7.6–10.3%), and the three methods that failed at 1× match or approach the performance of the three that succeeded. M.1 Prefix jumps from 0.02% to 9.20%; M.3 KV Extension from 0.0% to 9.69%; M.5 Gated from 0.09% to 7.62%. This convergence demonstrates that the 1× failure is not a fundamental incompatibility but a capacity bottleneck: with sufficient memory dimensionality, even weak injection mechanisms can bridge the distributional gap between pre-trained representations and persistent memory entries. The strong-bias methods (M.2, M.4, M.6) achieve this with 10 × less capacity. 

7.4 Knowledge accumulation 

Figure 12 shows the cumulative knowledge score Ks across 30 sessions at 1× capacity. The same dichotomy appears: three methods accumulate knowledge well above the stateless baseline (∆K = 5 .57 ), while three produce near-zero knowledge gain. M.6 Slot achieves the highest 

∆K = 9 .71 , followed by M.4 Hebbian ( ∆K = 7 .84 ) and M.2 XAttn ( ∆K = 7 .34 ). M.1 Prefix and M.3 KV Extension produce ∆K = 0 .0; M.5 Gated reaches only ∆K = 0 .17 .Notably, the knowledge ranking differs from the forgetting-curve ranking: M.6 Slot leads on ∆K

despite having the lowest retained-memory score among the three successful methods. This suggests that slot-based sparse writes are particularly well suited for accumulating diverse factual knowledge, even though the readout fidelity per-fact is somewhat lower than for M.2 or M.4. 11 0–31 

> 32 –63
> 64 –127
> 128 –255
> 256+ 0
> 5
> 10
> 15
> 20
> Evidence lag (turns in the past)
> Retained-memory score (%)

GPT-2 ( 10 × capacity) 

> Baseline
> M.2 XAttn
> M.4 Hebbian
> M.6 Slot
> M.3 KV Ext
> M.1 Prefix
> M.5 Gated

Figure 11: Absolute forgetting curves at 10 × capacity. All six methods now produce substantial retained-memory scores. Methods that failed at 1× (M.1, M.3, M.5) converge to comparable performance with the strong-bias methods, demonstrating that the 1× gap is architectural, not fundamental.           

> 0510 15 20 25 29
> 0
> 5
> 10
> Session
> Knowledge score  Ks
> Knowledge accumulation ( 1×)
> Baseline ( ∆K= 5 .57 )
> M.6 Slot ( ∆K= 9 .71 )
> M.4 Hebbian ( ∆K= 7 .84 )
> M.2 XAttn ( ∆K= 7 .34 )

Figure 12: Cumulative knowledge score Ks over 30 sessions at 1× capacity. Successful methods (M.2, M.4, M.6) accumulate knowledge substantially above the baseline; failing methods (M.1, M.3, M.5, not shown for clarity) produce ∆K < 0.2.

## 8 Discussion 

The results confirm that persistent latent-space memory transfers from encoder–decoder to decoder-only models. We discuss four aspects: the architectural affinity hierarchy (§8.1), why the injection problem is harder yet the fit is more natural (§8.2), why the write rule transfers without modification (§8.3), and the current limitations of the study (§8.4). 

8.1 Architectural affinity for persistent memory 

We conjectured in §1 that persistent memory has a natural affinity ordering across transformer families: decoder-only > encoder–decoder > encoder-only. The reasoning rests on three properties. 

Sequential processing. Autoregressive decoders process input one token at a time, building a running KV cache that is conceptually a volatile session memory. Persistent memory generalises this cache across session boundaries—a minimal conceptual leap. Encoder–decoder models process context bidirectionally in the encoder, then read it once in the decoder; the temporal structure is less direct. Encoder-only models process everything in a single bidirectional pass with no notion of incremental accumulation. 12 Dominant deployment. Virtually all modern deployed LLMs (GPT-4, Claude, LLaMA, Gemini, Mistral) are decoder-only. Because persistent memory retrofits a frozen backbone, the practical impact is maximised when the paradigm aligns with the most widely used architecture. 

Generation capability. Persistent memory is useful only if the model can produce output condi-tioned on recalled information. Decoder-only models generate natively; encoder–decoder models generate through the decoder half; encoder-only models require a separate head or decoder to produce text. The more direct the generation path, the more cleanly memory recall integrates into output. 

8.2 The injection paradox: harder mechanics, better fit 

The encoder–decoder setting benefits from cross-attention heads that were pre-trained to integrate external representations. Decoder-only models must repurpose self-attention, which was trained to attend only to preceding tokens in the same sequence. The memory adapter must therefore learn a harder mapping: projecting persistent state into a format that self-attention treats as “legitimate earlier context” despite never having seen such entries during pre-training. This creates an apparent paradox: the architecture with the highest conjectured affinity for persistent memory presents the hardest injection problem. The fact that it works at all—and with comparable retained-memory scores—suggests that self-attention heads possess representational slack analogous to the cross-attention slack observed in encoder–decoder models. KV prefix injection exploits this slack directly: the frozen self-attention computes softmax( Q[KMem ; Kinput ]⊤)[ VMem ; Vinput ],treating memory entries as if they were earlier tokens in the sequence. 

8.3 The universal write rule 

The write rule Pt = γP t−1 + A⊤ 

> t

Vw is a content-addressed aggregation over the current hidden state, gated by a temporal decay factor. It is architecture-agnostic because it depends only on the existence of dense hidden states Ht ∈ Rn×d, which every transformer produces. The read path must adapt to the backbone’s attention structure, but the write path does not. This separation—universal write, architecture-specific read—is the central structural insight. It means that the memory bank P is a general-purpose persistent substrate that can be connected to any frozen backbone through a thin, architecture-specific adapter layer. 

8.4 Limitations 

All experiments use a single benchmark (LoCoMo) and a single training seed. Decoder-only eval-uation reformulates LoCoMo’s QA format for autoregressive generation, which may introduce format-specific variance. We do not test encoder-only models (BERT-style) or vision–language mod-els, which remain important future targets. The write-side projections are fixed random maps; learning them may improve performance but would require backpropagation through the full conversation history. 

## 9 Conclusion 

We have demonstrated that persistent latent-space memory, originally developed for frozen encoder– decoder models [ 5 ], transfers to the decoder-only setting despite the absence of cross-attention injection points. The central finding is an inductive-bias dichotomy: at constrained capacity ( 1×), only memory methods with strong architectural priors—parallel cross-attention (M.2), Hebbian associative recall (M.4), and slot-based sparse write (M.6)—succeed, while the remaining three methods require 10 × capacity to reach comparable performance. This reveals that the gap is one of efficiency rather than fundamental compatibility. The architectural decomposition into a universal write rule (shared across backbones) and architecture-specific read paths (KV prefix, inserted cross-attention, gated branch) provides a principled design template. Because the write rule depends only on the existence of dense hidden states, extending persistence to new architecture families requires only a thin read adapter for the frozen backbone. 13 Together with the encoder–decoder results of Jeong [5] and the brain-inspired memory modules of Jeong [6 , 7] , these findings establish persistent latent-space memory as a general paradigm spanning the major transformer families. Extending the framework to encoder-only and multimodal architectures, scaling to larger backbones, and learning the write projections end-to-end remain promising directions for future work. 

## Acknowledgements 

The author thanks Inha University in Tashkent for research support. This work reflects the author’s ongoing inquiry into nature and human cognition. 

## Reproducibility Statement 

All experiments use GPT-2 (124 M parameters) from the Hugging Face transformers library in bfloat16 precision. Memory adapter hyperparameters are reported in §5 and Table 3. The evaluation protocol follows Jeong [5] without modification; the benchmark is the publicly available LoCoMo dataset [ 10 ]. Training uses a single NVIDIA GPU with AdamW ( η = 10 −4). Code and trained adapter weights will be released upon publication. 

## Ethics and Broader Impact 

Persistent memory enables language models to accumulate user-specific information across sessions. This capability raises privacy considerations: remembered facts may include sensitive personal data. Practical deployments should provide users with mechanisms to inspect, edit, and delete stored memory entries, consistent with data-protection regulations. On the positive side, persistent memory reduces the need for repeatedly restating context, which may improve accessibility for users who struggle with extended text input. Our experiments use a publicly available benchmark and do not involve private user data. 

## References 

[1] Alec Radford, Jeffrey Wu, Rewon Child, David Luan, Dario Amodei, and Ilya Sutskever. Language models are unsupervised multitask learners. OpenAI Technical Report , 2019. [2] Hugo Touvron, Louis Martin, Kevin Stone, et al. Llama 2: Open foundation and fine-tuned chat models. arXiv preprint arXiv:2307.09288 , 2023. [3] Mojan Javaheripi, Sébastien Bubeck, et al. Phi-2: The surprising power of small language models. Microsoft Research Blog , 2023. [4] Albert Q. Jiang, Alexandre Sablayrolles, Arthur Mensch, et al. Mistral 7B. arXiv preprint arXiv:2310.06825 , 2023. [5] Hong Jeong. Trained persistent memory for frozen encoder–decoder LLMs: Six architectural methods. arXiv:2603.16413 [cs.LG] , 2026. https://arxiv.org/abs/2603.16413 

[6] Hong Jeong. Inhibitory cross-talk enables functional lateralization in attention-coupled latent memory. arXiv:2603.03355 [q-bio.NC] , 2026. https://arxiv.org/abs/2603.03355 

[7] Hong Jeong. A miniature brain transformer: Thalamic gating, hippocampal lateralization, amygdaloid salience, and prefrontal working memory in attention-coupled latent memory. 

arXiv:2603.07217 [q-bio.NC] , 2026. https://arxiv.org/abs/2603.07217 

[8] Charles Packer, Sarah Wooders, Kevin Lin, Vivian Fang, Shishir G. Patil, Ion Stoica, and Joseph E. Gonzalez. MemGPT: towards LLMs as operating systems. arXiv:2310.08560 , 2024. [9] Wanjun Zhong, Lianghong Guo, Qiqi Gao, He Ye, and Yanlin Wang. MemoryBank: enhancing large language models with long-term memory. In AAAI , 2024. 14 [10] Adyasha Maharana, Dong-Ho Lee, Sergey Tulyakov, Mohit Bansal, Francesco Barbieri, and Yuwei Fang. Evaluating very long-term conversational memory of LLM agents. In Proceedings of ACL , 2024. [11] Reiner Pope, Sholto Douglas, Aakanksha Chowdhery, et al. Efficiently scaling transformer inference. In MLSys , 2023. [12] Yuhuai Wu, Markus N. Rabe, DeLesley Hutchins, and Christian Szegedy. Memorizing trans-formers. In ICLR , 2022. [13] Pedro Henrique Martins, Zita Marinho, and André F.T. Martins. ∞-former: Infinite memory transformer. In ACL , 2022. [14] Xiang Lisa Li and Percy Liang. Prefix-tuning: Optimizing continuous prompts for generation. In ACL–IJCNLP , 2021. [15] Edward J. Hu, Yelong Shen, Phillip Wallis, Zeyuan Allen-Zhu, Yuanzhi Li, Shean Wang, Lu Wang, and Weizhu Chen. LoRA: low-rank adaptation of large language models. In ICLR ,2022. [16] Jean-Baptiste Alayrac, Jeff Donahue, Pauline Luc, Antoine Miech, et al. Flamingo: a visual language model for few-shot learning. In NeurIPS , 2022. [17] Zihang Dai, Zhilin Yang, Yiming Yang, Jaime Carbonell, Quoc V. Le, and Ruslan Salakhutdinov. Transformer-XL: Attentive language models beyond a fixed-length context. In ACL , 2019. [18] Jack W. Rae, Anna Potapenko, Siddhant M. Jayakumar, and Timothy P. Lillicrap. Compressive transformers for long-range sequence modelling. In ICLR , 2020. [19] Aydar Bulatov, Yuri Kuratov, Yermek Kapushev, and Mikhail S. Burtsev. Scaling transformer to 1M tokens and beyond with RMT. arXiv preprint arXiv:2304.11062 , 2023. [20] Alex Graves, Greg Wayne, and Ivo Danihelka. Neural Turing machines. arXiv preprint arXiv:1410.5401 , 2014. [21] Alex Graves, Greg Wayne, Malcolm Reynolds, Tim Harley, Ivo Danihelka, Agnieszka Grabska-Barwi´ nska, et al. Hybrid computing using a neural network with dynamic external memory. 

Nature , 538(7626):471–476, 2016. 15