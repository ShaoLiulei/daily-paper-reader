---
title: A transformer architecture alteration to incentivise externalised reasoning
title_zh: 一种旨在激励外部化推理的 Transformer 架构改进
authors: "Elizabeth Pavlova, Mariia Koroliuk, Karthik Viswanathan, Cameron Tice, Edward James Young, Puria Radmard"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21376v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 带有早退机制的 Transformer 架构改进
tldr: 本研究提出一种改进Transformer架构的方法，旨在通过减少内部冗余计算来激励模型进行外部化推理。研究者在中间层引入“早期退出”机制，并结合强化学习训练模型在预测简单标记时尽早退出。该方法能根据标记难度自适应调整计算量，从而限制模型利用内部激活进行非透明规划，迫使其将复杂逻辑通过外部推理步骤呈现，提高了推理过程的可解释性与效率。
selection_source: fresh_fetch
motivation: 旨在减少大模型在简单标记上的过度内部计算，从而激励其将复杂逻辑转向可观测的外部推理。
method: 在Transformer架构中引入早期退出机制，并利用强化学习在保持性能的前提下诱导模型尽早结束前向传播。
result: 初步实验表明，小型推理模型能够根据标记预测难度自适应地减少计算层数。
conclusion: 该方法能有效限制模型的内部隐式规划，将计算资源保留给真正需要的复杂推理任务。
---

## 摘要
我们提出了一种新的架构变更和训练后流水线，通过教导模型提前截断前向传播，使大语言模型（LLM）成为更冗长的推理者。我们在现有 Transformer 架构的中间层增加了早停（early-exit）机制，并训练模型在无需深度计算即可预测下一个 token 时，从较浅的层退出。在校准阶段之后，我们利用强化学习激励模型在保持任务性能的同时尽可能早地退出。我们为小型推理模型提供了初步实验结果，表明它们学会了在不同 token 之间自适应地减少计算量。我们预测，在合适的规模下应用该方法，可以最大限度地减少推理模型利用其内部激活进行非近视规划（non-myopic planning）时所能支配的冗余计算量，并将其仅保留给难以预测的 token。

## Abstract
We propose a new architectural change, and post-training pipeline, for making LLMs more verbose reasoners by teaching a model to truncate forward passes early. We augment an existing transformer architecture with an early-exit mechanism at intermediate layers and train the model to exit at shallower layers when the next token can be predicted without deep computation. After a calibration stage, we incentivise the model to exit as early as possible while maintaining task performance using reinforcement learning. We provide preliminary results to this effect for small reasoning models, showing that they learn to adaptively reduce computations across tokens. We predict that, applied at the right scale, our approach can minimise the amount of excess computation that reasoning models have at their disposal to perform non-myopic planning using their internal activations, reserving this only for difficult-to-predict tokens.

---

## 论文详细总结（自动生成）

以下是对论文《A Transformer Architecture Alteration to Incentivise Externalised Reasoning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在生成思维链（CoT）时，往往利用其内部激活进行“非近视规划”（non-myopic planning），即在预测当前 token 时已经在内部计算了后续多个步骤。
*   **研究背景**：这种内部计算会导致两个安全风险：
    1.  **不忠实性（Unfaithfulness）**：外部输出的 CoT 不能真实反映模型的实际推理逻辑。
    2.  **混淆性（Obfuscation）**：模型可能在内部进行恶意规划，而外部监控器无法察觉。
*   **研究目标**：通过架构改进，迫使模型在预测简单 token 时尽早退出计算，从而剥离其冗余的内部计算能力，激励模型将复杂的推理过程“外部化”到可观测的 CoT 中。

### 2. 方法论
该研究提出了一种**自适应早停架构**及配套的两阶段训练流水线：

*   **核心思想**：在 Transformer 的中间层（每 5 层）添加轻量级的“早停头”（Early-exit heads）。模型在每一层都会随机采样是否退出。一旦退出，残差流将直接跳过后续层进入输出层。
*   **关键技术细节**：
    *   **架构变更**：使用 LoRA 适配器调整现有权重，并添加标量逻辑值（Logit）输出的早停头。
    *   **第一阶段：自适应校准（Self-distillation SFT）**：
        *   将原始全深度模型作为教师，带早停头的模型作为学生。
        *   利用教师模型中间层与最终层预测结果的 **KL 散度**来确定目标退出概率。KL 散度越低，代表该层已具备预测能力，目标退出概率越高。
    *   **第二阶段：强化学习激励（RLOO）**：
        *   使用改进的 RLOO 算法，奖励函数设计为：$R_{total} = R_{task} - \lambda \cdot (\text{平均退出层索引}) - \beta \cdot D_{KL}$。
        *   **$\lambda$ 参数**：作为“激进程度”的调节旋钮，$\lambda$ 越高，模型越倾向于更早退出以节省计算量。

### 3. 实验设计
*   **实验对象**：小型推理模型，包括 DeepSeek-R1-Distill-Qwen-1.5B 和 Qwen3-4B。
*   **数据集/场景**：
    *   **GSM8K**：数学文字题推理。
    *   **Theory of Mind (ToM)**：心理理论推理任务（使用 ExploreToM 生成）。
*   **Benchmark 与对比**：
    *   以**原始全深度模型（Base Model）**为基准。
    *   对比了不同 **KL 因子**（0.25 到 4.0）对模型连贯性和计算节省量的影响。
*   **评估指标**：任务准确率（Accuracy）、回答连贯性（Coherence）、平均计算量（使用的层数比例）和总计算量（Token 数 × 平均层数）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练总时长。
*   **实现细节**：提到了使用了 LoRA 这种参数高效的微调方法，暗示了该方法在算力受限的情况下也具有可行性。

### 5. 实验数量与充分性
*   **实验规模**：属于**初步验证（Proof of Concept）**性质。
*   **实验组数**：
    *   在 1.5B 模型上验证了 SFT 校准的有效性。
    *   在 4B 模型上进行了 5 次 RL 训练运行（Step 0 到 380）。
    *   进行了针对 KL 因子的消融实验（5 种不同设置）。
*   **充分性评价**：实验在小规模模型上证明了机制的可行性，但缺乏在大规模模型（如 70B+）上的验证。实验设计相对客观，通过多维度指标（准确率 vs 计算量）展示了权衡过程。

### 6. 主要结论与发现
*   **计算量自适应**：模型学会了根据 token 难度调整深度。例如，在预测高度可预测的 token（如“walkie-talkie”中的“-talk”）时，模型会显著提前退出。
*   **性能保持**：通过 RL 训练，模型在减少约 5%-10% 平均计算量的同时，任务准确率反而有所提升（从 47% 提升至 55-60%），且保持了较好的连贯性。
*   **可调控性**：通过调节 $\lambda$ 和 KL 因子，可以在“推理外部化程度”与“生成质量”之间寻找平衡点。

### 7. 优点
*   **创新性**：不同于传统的为了推理加速的早停机制，本文首次将其作为一种**AI 安全工具**，用于强制模型外部化推理。
*   **随机性设计**：采用随机采样而非确定性阈值退出，使得模型能够通过强化学习（RL）进行端到端的优化。
*   **实用性**：提供了推理时的控制旋钮，允许安全人员根据需求调整监控强度。

### 8. 不足与局限
*   **核心假设未验证**：论文尚未正式验证该方法是否真的提高了 CoT 的**忠实性（Faithfulness）**，这留作了未来工作。
*   **模型规模限制**：实验仅限于 1.5B 和 4B 模型，尚不清楚在具有更强隐式规划能力的大模型上效果如何。
*   **任务局限**：主要集中在数学和心理推理，缺乏跨领域（如代码、通用对话）的泛化性验证。
*   **质量损失风险**：过于激进的早停（如 KL 因子过低）会导致模型连贯性大幅下降。

（完）
