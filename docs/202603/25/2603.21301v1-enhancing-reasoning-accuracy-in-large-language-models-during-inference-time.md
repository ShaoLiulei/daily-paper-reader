---
title: Enhancing reasoning accuracy in large language models during inference time
title_zh: 提升大语言模型在推理阶段的推理准确率
authors: "Vinay Sharma, Manish Jain"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21301v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 提升大语言模型推理准确率的推理时技术
tldr: "针对大语言模型在多步推理任务中可靠性不足的问题，本文系统评估了三种推理阶段增强策略：自一致性（随机采样）、双模型推理一致性及自我反思。研究发现，在CoT提示下，通过核采样和温度控制的自一致性方法效果最显著，准确率提升达9%-15%，而自我反思对非推理小模型提升有限。该研究为不同风险场景下的推理优化提供了实证指导。"
selection_source: fresh_fetch
motivation: 旨在不进行额外训练或微调的情况下，通过推理阶段的技术手段提升大语言模型在复杂推理任务中的准确性。
method: 在CoT提示基础上，对比评估了基于随机采样的自一致性、双模型独立输出比对以及模型自我批判修正三种策略。
result: "实验表明自一致性策略在准确率上比贪婪解码提升了9%至15%，而双模型法增强了可靠性，自我反思效果则不明显。"
conclusion: 自一致性是低成本提升推理能力的最佳选择，双模型适用于高可靠性场景，而自我反思在非推理模型上的有效性受限。
---

## 摘要
大语言模型（LLMs）通常表现出强大的语言能力，但在多步推理任务上仍然不够可靠，特别是在不进行额外训练或微调的情况下部署时。在这项工作中，我们研究了旨在提高 LLMs 推理准确率的推理时（inference-time）技术。我们系统地评估了三类推理时策略：(i) 通过随机解码实现的自一致性（self-consistency），即使用受控的温度（temperature）和核采样（nucleus sampling）对模型进行多次采样，并选择出现频率最高的最终答案；(ii) 双模型推理一致性（dual-model reasoning agreement），即比较两个独立模型的输出，并仅信任一致的推理路径；以及 (iii) 自反思（self-reflection），即模型对其自身的推理进行批判和修正。在所有评估的方法中，我们都采用了思维链（CoT）提示，以在生成最终答案之前引导出显式的中间推理步骤。在这项工作中，我们在相同的提示和验证设置下，对三种推理时策略进行了受控的对比评估。我们在 LLM 上的实验表明，结合核采样和受控温度值的自一致性方法带来了显著的提升，相比贪婪单次解码（greedy single-pass decoding），其准确率实现了 9% 到 15% 的绝对提升，非常适合低风险领域，能以极低的开销提供有意义的增益。双模型方法为模型的推理步骤提供了额外的确认，因此更适用于中等风险领域，在这些领域中，更高的可靠性证明了额外计算开销的合理性。自反思仅带来了微小的改进，这表明该方法在推理阶段对于较小的非推理模型效果有限。

## Abstract
Large Language Models (LLMs) often exhibit strong linguistic abilities while remaining unreliable on multi-step reasoning tasks, particularly when deployed without additional training or fine-tuning. In this work, we study inference-time techniques to improve the reasoning accuracy of LLMs. We systematically evaluate three classes of inference-time strategies: (i) self-consistency via stochastic decoding, where the model is sampled multiple times using controlled temperature and nucleus sampling and the most frequent final answer is selected; (ii) dual-model reasoning agreement, where outputs from two independent models are compared and only consistent reasoning traces are trusted; and (iii) self-reflection, where the model critiques and revises its own reasoning. Across all evaluated methods, we employ Chain-of-Thought (CoT) [1] prompting to elicit explicit intermediate reasoning steps before generating final answers. In this work, we provide a controlled comparative evaluation across three inference-time strategies under identical prompting and verification settings. Our experiments on LLM [2] show that self-consistency with nucleus sampling and controlled temperature value yields the substantial gains, achieving a 9% to 15% absolute improvement in accuracy over greedy single-pass decoding, well-suited for low-risk domains, offering meaningful gains with minimal overhead. The dual-model approach provides additional confirmation for model reasoning steps thus more appropriate for moderate-risk domains, where higher reliability justifies additional compute. Self-reflection offers only marginal improvements, suggesting limited effectiveness for smaller non-reasoning models at inference time.

---

## 论文详细总结（自动生成）

以下是对论文《Enhancing Reasoning Accuracy in Large Language Models During Inference Time》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：大语言模型（LLMs）虽然具备强大的语言生成能力，但在处理需要多步逻辑推理、一致性或结构化解决问题的任务时表现不稳定。
*   **研究背景**：现有的提升推理能力的方法（如架构修改、强化学习或指令微调）通常需要访问模型权重、大量训练数据或高昂的计算资源。本文旨在研究**推理阶段（Inference-time）**技术，即在不修改模型参数的前提下，仅通过提示词工程、解码策略和输出聚合来提高推理的可靠性。

### 2. 方法论
论文系统评估了三种基于思维链（CoT）提示的推理阶段策略：
*   **自一致性 + 受控随机采样（Self-Consistency + Stochastic Decoding）**：
    *   **核心思想**：对同一问题进行多次采样，通过多数投票选出最频繁的答案。
    *   **技术细节**：设置温度 $T=0.8$ 和核采样 $top\_p=0.9$ 以诱导推理路径的多样性；对每个问题采样 $n=6$ 次；使用低温度（$T=0.1$）模型提取最终答案，并由一个 LLM 评判器进行语义一致性投票（而非简单的字符串匹配）。
*   **双模型推理与跨模型验证（Dual-Model Reasoning）**：
    *   **核心思想**：利用两个架构和训练数据不同的独立模型共同解决问题。
    *   **技术细节**：两个模型独立生成 CoT 路径和答案；引入一个独立的验证模型（Verifier）对比两者的结论。只有当两个模型达成一致时才接受结果，否则标记为不可信。
*   **自我反思（Self-Reflection via Iterative Critique and Revision）**：
    *   **核心思想**：模型通过自我批判和修正来优化初始答案。
    *   **技术细节**：模型先生成初始方案，随后根据“反思提示词”分析逻辑漏洞或错误，最后根据批判意见生成修订后的最终答案。

### 3. 实验设计
*   **数据集**：使用 HuggingFace 上的 **Logical Reasoning Improvement Dataset (Open-Platypus)**。该数据集涵盖科学、数学、编程等多个领域，具有确定性且无歧义的标准答案。
*   **基准（Benchmark）**：贪婪解码（Greedy single-pass decoding）下的单次生成结果。
*   **对比设置**：在相同的 CoT 提示和验证环境下，对比上述三种策略相对于基准的准确率提升。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件配置（如 GPU 型号、数量）或具体的推理耗时/训练时长。由于研究聚焦于推理阶段技术，不涉及模型训练，其主要开销体现在多次调用 API 或模型推理的次数上（如自一致性需要 6 次采样）。

### 5. 实验数量与充分性
*   **实验规模**：论文对三种策略分别进行了对比实验，并针对自一致性策略对比了“随机采样”与“贪婪解码”的效果。
*   **客观性与公平性**：实验采用了统一的独立验证模型来评估结果与 Ground-truth 的一致性，减少了主观偏差。
*   **充分性评价**：实验覆盖了逻辑推理的主要维度，但采样次数（$n=6$）相对较小，且主要针对“较小的非推理模型”进行了测试，未展示在超大规模模型（如 GPT-4 或 Claude 3）上的表现差异。

### 6. 主要结论与发现
*   **自一致性效果最显著**：结合随机采样的自一致性方法比贪婪解码实现了 **9%–15% 的绝对准确率提升**（正确率从 56.2% 升至 64.9%）。
*   **双模型法提升可靠性**：虽然其接受率（47.4%）略低于单模型，但其结果与真实准确率高度一致，能有效过滤错误，适合高风险领域。
*   **自我反思增益有限**：在小型非推理模型上仅提升了约 **3.4%**（从 47.2% 升至 50.6%），表明该策略可能更依赖模型本身的固有能力。

### 7. 优点
*   **实战指导性强**：为不同风险等级的业务场景提供了具体的策略建议（如金融医疗用双模型，教育客服用自一致性）。
*   **低成本优化**：证明了无需微调即可通过解码策略显著提升推理性能。
*   **语义化投票**：在自一致性中采用 LLM 评判器进行语义对齐，比传统的字符串匹配更鲁棒。

### 8. 不足与局限
*   **模型范围局限**：实验主要针对较小的模型，自反思等策略在更强大的模型上可能表现不同，文中未予验证。
*   **成本权衡缺失**：虽然提到了双模型法成本更高，但未量化不同策略之间的 Token 消耗与延迟（Latency）对比。
*   **采样多样性不足**：自一致性的采样次数仅为 6 次，可能未充分挖掘随机采样的潜力。

（完）
