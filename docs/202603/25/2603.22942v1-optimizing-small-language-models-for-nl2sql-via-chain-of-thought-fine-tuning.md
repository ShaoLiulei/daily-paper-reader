---
title: Optimizing Small Language Models for NL2SQL via Chain-of-Thought Fine-Tuning
title_zh: 通过思维链微调优化用于 NL2SQL 的小语言模型
authors: "Anshul Solanki, Sanchit Latawa, Koushik Chakraborty, Navneet Kamboj"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22942v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 针对NL2SQL任务的小语言模型微调
tldr: "本研究探讨了如何通过微调优化小语言模型（如Qwen）在NL2SQL任务中的表现。研究发现，相比于在大模型上微调收益有限且易过拟合，在小模型上引入思维链（CoT）推理数据的微调能显著提升准确率（从36%提升至54.5%）。这证明了通过迁移推理模式，低成本的小模型也能接近生产级性能，在降低推理成本和延迟的同时，满足业务对准确性的核心需求。"
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在NL2SQL部署中推理成本高、延迟大，且在大模型上微调收益边际递减的问题。
method: 通过在微调数据集中引入显式的思维链（CoT）推理模式，将大模型的推理能力迁移至计算效率更高的小模型中。
result: "小模型在微调后准确率从36%提升至45%，结合CoT后进一步提升至54.5%，显著增强了其处理复杂查询的能力。"
conclusion: 证明了利用推理模式迁移可使小模型达到生产级性能，是实现高性价比NL2SQL解决方案的有效途径。
---

## 摘要
将自然语言转换为 SQL (NL2SQL) 仍然是企业实现数据民主化的关键瓶颈。尽管 Gemini 2.5 等大语言模型 (LLMs) 已展现出令人印象深刻的零样本能力，但其高昂的推理成本限制了大规模部署。本文探讨了在 NL2SQL 任务上微调大语言模型和小语言模型的有效性。研究揭示了一种反直觉的缩放现象：在标准数据集上微调大模型 (Gemini 2.5 Flash/Lite) 的收益微乎其微，且往往会导致在复杂查询上出现过拟合；相反，小模型 (Qwen) 则表现出显著的提升。微调将小模型的基准准确率从 36% 提高到 45%，而通过显式思维链 (CoT) 推理进一步丰富数据集后，准确率飙升至 54.5%（图 2）。虽然这一结果仍低于 Gemini 2.5 等大模型的准确率，但它实现了大幅降低成本、缩短推理延迟的业务目标，并达到了业务关键的性能准确率阈值。本文证明，通过迁移推理模式，可以使计算效率更高的小型模型达到接近生产级的性能。

## Abstract
Translating Natural Language to SQL (NL2SQL) remains a critical bottleneck for democratization of data in enterprises. Although Large Language Models (LLMs) like Gemini 2.5 and other LLMs have demonstrated impressive zero-shot capabilities, their high inference costs limit deployment at scale. This paper explores the efficacy of fine-tuning both large and small language models on NL2SQL tasks.   Our research reveals a counter-intuitive scaling phenomenon. Fine-tuning large models (Gemini 2.5 Flash/Lite) on standard datasets yields negligible returns, often leading to overfitting on complex queries. Conversely, small models (Qwen) show significant gains. Fine-tuning improved the small model baseline from 36% to 45%, and further enriching the dataset with explicit Chain-of-Thought (CoT) reasoning surged accuracy to 54.5%(Fig 2). While this is still lower than the accuracy of large models like Gemini 2.5 , it does serve the business goal of significant cost reduction, latency in inference time and also meeting the business critical performance accuracy threshold.This paper demonstrates that transferring reasoning patterns enables compute-efficient smaller models to approach production-grade performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Optimizing Small Language Models for NL2SQL via Chain-of-Thought Fine-Tuning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证 NL2SQL（自然语言转 SQL）任务准确率的同时，降低大型语言模型（LLM）带来的高昂推理成本和高延迟。
*   **研究动机**：
    *   企业对非技术人员查询数据库的需求日益增长，但 SOTA 模型（如 Gemini 2.5, GPT-4）虽然零样本能力强，但大规模部署成本太高。
    *   研究发现，直接对大模型进行标准微调往往收益递减甚至导致性能下降（过拟合）。
    *   探索是否可以通过“思维链（CoT）微调”将大模型的推理能力迁移到小模型（如 Qwen-7B）中，使其达到生产级可用的水平。

### 2. 论文提出的方法论
*   **核心思想**：通过显式的思维链（CoT）数据增强，教导小模型学习人类数据工程师编写 SQL 的逻辑步骤，而非简单的输入输出映射。
*   **关键技术细节**：
    *   **复杂度评分机制**：根据 SQL 结构（JOIN, GROUP BY, 嵌套查询等）对 Spider 数据集进行打分，筛选出 5500 条高质量数据，并调整分布（40% 困难，50% 中等，10% 简单），以增加学习密度。
    *   **CoT 数据构造**：在训练数据中加入五个推理步骤：
        1.  **查询分析**：识别用户意图和关键词。
        2.  **表选择**：从架构中筛选相关表。
        3.  **列选择**：确定所需的具体字段。
        4.  **逻辑与连接策略**：确定主外键关系及连接逻辑。
        5.  **自我验证**：检查语法和幻觉。
    *   **微调技术**：采用 **LoRA（低秩自适应）** 进行参数高效微调。
    *   **提示工程优化**：自动生成包含列名、数据类型和样本值的详细 Schema 描述，并加入自我修正指令。

### 3. 实验设计
*   **数据集**：使用 **Spider-1.0** 数据集。
*   **Benchmark**：构建了一个包含 600 条查询的基准测试集，严格遵循 4:5:1 的难易比例。
*   **对比方法**：
    *   **大模型基准**：Gemini 2.5 Pro, Flash, Flash-Lite（零样本 vs. 标准微调）。
    *   **小模型对比**：Qwen-7B-Chat（零样本 vs. 标准微调 vs. CoT 微调）。
*   **评估指标**：**执行准确率（Execution Accuracy）**。通过在影子数据库上运行生成的 SQL 并对比结果集，而非简单的文本匹配。

### 4. 资源与算力
*   **模型选择**：Google Gemini 系列 API 和 Qwen-7B-Chat。
*   **微调配置**：使用了 LoRA。具体参数包括：Epochs (3-5), Learning Rate Multiplier (1 to 5e-5), Adapter Rank (16-32)。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。考虑到使用了 LoRA 和 7B 规模的模型，预计在单张或少量消费级/企业级 GPU（如 A100 或 L40）上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对大模型和小模型分别进行了多组对比实验（共约 7 组主要配置）。
    *   训练集规模为 5500 条，验证/测试集为 600 条。
*   **充分性评价**：实验设计较为客观，通过对比“标准微调”与“CoT 微调”的差异，清晰地展示了推理路径的重要性。同时，对大模型微调后的性能退化进行了深入的错误分析（Error Analysis），增强了结论的说服力。

### 6. 主要结论与发现
*   **反直觉的缩放现象**：对 Gemini 2.5 等大模型进行标准微调（SFT）几乎没有收益，甚至在复杂查询上出现性能下降（如 Flash 从 62.5% 降至 57.5%），原因是发生了“表示崩溃（Representation Collapse）”，模型倾向于记忆模板而非动态推理。
*   **小模型潜力巨大**：Qwen-7B 在零样本下仅有 36.17% 准确率，标准微调提升至 45.33%，而 **CoT 微调将其提升至 54.5%**（绝对提升 18.33%）。
*   **语法错误减少**：CoT 微调显著降低了执行失败率（从 223 次降至 106 次），证明推理步骤有助于生成更准确的 SQL 语法。

### 7. 优点
*   **实用性强**：直接针对企业部署中的成本和延迟痛点，证明了小模型在特定任务上的生产可行性。
*   **推理迁移**：成功将大模型的逻辑思考过程（CoT）“蒸馏”到了小模型参数中，而不仅仅是结果。
*   **评估严谨**：采用执行准确率而非文本相似度，更符合真实业务场景。

### 8. 不足与局限
*   **数据集局限**：仅在 Spider 数据集上测试。现实中的企业数据库 Schema 往往更混乱、不规范，模型在处理模糊命名或非规范化表时的鲁棒性有待验证。
*   **Token 开销**：CoT 步骤虽然提高了准确率，但也增加了输出的 Token 数量，这在一定程度上抵消了小模型的部分速度优势。
*   **推理形式**：目前仍需显式输出文本推理过程。论文提到未来需研究“内化推理（Internalized Reasoning）”，即在潜空间完成推理而不增加输出长度。

（完）
