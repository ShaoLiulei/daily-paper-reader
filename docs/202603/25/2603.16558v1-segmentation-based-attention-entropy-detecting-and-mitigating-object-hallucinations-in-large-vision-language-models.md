---
title: "Segmentation-Based Attention Entropy: Detecting and Mitigating Object Hallucinations in Large Vision-Language Models"
title_zh: 基于分割的注意力熵：检测与缓解大视觉语言模型中的物体幻觉
authors: "Jiale Song, Jiaxin Luo, Xue-song Tang, Kuangrong Hao, Mingbo Zhao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16558v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 减轻生成式视觉语言模型中的幻觉
tldr: 针对大语言视觉模型（LVLM）中的物体幻觉问题，本文发现视觉模态内的异常注意力模式是其核心诱因。为此提出基于分割的注意力熵（SAE），利用语义分割在物体级语义空间量化视觉注意力不确定性。基于SAE，研究者设计了幻觉检测可靠性评分及推理时注意力调整方法，在无需额外训练的情况下显著降低了幻觉率，提升了模型在基准测试及四足机器人实测中的可靠性。
selection_source: fresh_fetch
motivation: 现有研究多关注文本先验对幻觉的影响，而本文旨在通过分析视觉模态内部的异常注意力模式来解决物体幻觉问题。
method: 提出SAE方法，利用语义分割量化物体级视觉注意力不确定性，并据此在推理阶段动态调整注意力以抑制幻觉。
result: 实验证明SAE在公开基准测试和真实四足机器人具身智能场景中均能显著减少物体幻觉，且无需任何额外训练成本。
conclusion: SAE为提升LVLM的视觉可靠性提供了一种高效、即插即用的方案，增强了模型在复杂感知与决策任务中的可信度。
---

## 摘要
大视觉语言模型（LVLMs）在许多多模态任务中表现出色，但物体幻觉严重削弱了其可靠性。大多数现有研究关注文本模态，将幻觉归因于过强的语言先验和不足的视觉接地（visual grounding）。与此不同，我们观察到视觉模态内的异常注意力模式也会导致物体幻觉。基于这一观察，我们提出了基于分割的注意力熵（SAE），利用语义分割在物体级语义空间中量化视觉注意力不确定性。基于 SAE，我们进一步设计了一个用于幻觉检测的可靠性评分，以及一种 SAE 引导的注意力调整方法，在推理阶段修改视觉注意力以缓解幻觉。我们在公开基准测试以及使用四足机器人的真实具身多模态场景中评估了我们的方法。实验结果表明，SAE 在不增加任何额外训练成本的情况下显著减少了物体幻觉，从而实现了更可靠的由 LVLM 驱动的感知与决策。

## Abstract
Large Vision-Language Models (LVLMs) achieve strong performance on many multimodal tasks, but object hallucinations severely undermine their reliability. Most existing studies focus on the text modality, attributing hallucinations to overly strong language priors and insufficient visual grounding. In contrast, we observe that abnormal attention patterns within the visual modality can also give rise to hallucinated objects. Building on this observation, we propose Segmentation-based Attention Entropy (SAE), which leverages semantic segmentation to quantify visual attention uncertainty in an object-level semantic space. Based on SAE, we further design a reliability score for hallucination detection and an SAE-guided attention adjustment method that modifies visual attention at inference time to mitigate hallucinations. We evaluate our approach on public benchmarks and in real embodied multimodal scenarios with quadruped robots. Experimental results show that SAE substantially reduces object hallucinations without any additional training cost, thereby enabling more trustworthy LVLM-driven perception and decision-making.

---

## 论文详细总结（自动生成）

以下是对论文《Segmentation-Based Attention Entropy: Detecting and Mitigating Object Hallucinations in Large Vision-Language Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：大视觉语言模型（LVLMs）虽然强大，但常出现“物体幻觉”（即描述图像中不存在的物体），严重影响其在医疗、自动驾驶等高风险领域的可靠性。
*   **核心发现**：现有研究多归因于语言先验过强，而本文发现**视觉模态内部的异常注意力模式**也是主因。当模型生成幻觉词汇时，其视觉注意力往往弥散在多个无关区域，而非集中在特定语义物体上。
*   **研究目标**：通过量化视觉注意力的不确定性，实现对物体幻觉的精准检测与实时缓解。

### 2. 方法论
论文提出了**基于分割的注意力熵（SAE）**，核心流程如下：
*   **语义分割预处理**：使用预训练模型（如 Mask2Former）对输入图像进行语义分割，将图像划分为不同的语义类别区域。
*   **注意力聚合**：在模型解码时，提取当前生成 Token 对所有视觉 Token 的注意力权重。根据分割结果，将这些权重按语义类别进行累加，得到类别级的注意力分布。
*   **SAE 计算**：对类别级分布计算归一化香农熵（Shannon Entropy）。SAE 越高，表示注意力越分散（不确定性高），越可能产生幻觉。
*   **幻觉检测（可靠性评分）**：结合跨模态注意力总量（Mass）与 SAE，定义可靠性评分 $R = M \times (1 - SAE)$。
*   **幻觉缓解（注意力调制）**：在推理阶段，利用 SAE 作为“门控”信号。对于 SAE 较高的注意力头（即不确定性高的头），动态调整其注意力 Logits，使其向全局一致的注意力图靠拢，从而强制模型“聚焦”于真实物体区域。

### 3. 实验设计
*   **模型对象**：主流开源模型 LLaVA-1.5-7B 和 LLaVA-1.5-13B。
*   **数据集**：从 COCO 2014 验证集中随机抽取 1,000 张图像进行评测。
*   **基准测试（Benchmarks）**：
    *   **检测任务**：对比了预测熵（PE）、最大 Softmax 概率（MSP）、VAR 等 6 种不确定性度量方法。
    *   **缓解任务**：对比了 Greedy、Beam Search、VCD、OPERA、PAI、VAR 等多种解码策略和干预方法。
*   **具身智能场景**：在四足机器人（Unitree Go1）上进行实测，验证其在真实导航任务中减少目标幻觉的效果。

### 4. 资源与算力
*   **训练成本**：该方法属于 **Training-free（无需训练）** 方法，不需要对 LVLM 进行微调或重新训练。
*   **算力细节**：文中未明确给出具体的 GPU 型号、数量或推理耗时。但由于涉及额外的语义分割模型（Mask2Former）和推理时的注意力矩阵提取，其推理开销会略高于标准的 Greedy Decoding。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 1,000 张图像的自动评估、消融实验、以及不同模型规模（7B/13B）的对比。
*   **充分性**：实验设计较为全面，不仅有传统的文本指标（CHAIR、P/R/F1），还有 ROC/PR 曲线分析，并延伸到了具身智能的实际应用。
*   **客观性**：对比了近年来（如 2024/2025 年）最新的幻觉缓解方法（如 VAR、VCD），证明了 SAE 在多个指标上的领先地位。

### 6. 主要结论与发现
*   **SAE 与幻觉强相关**：实验证实，真实物体的 SAE 显著低于幻觉物体，且这种差异在模型的**中间层**最为明显。
*   **显著降低幻觉率**：在 LLaVA-1.5-7B 上，SAE 引导的干预将 CHAIR 指标（幻觉率）降低了约 50%（从 47.9 降至 24.2）。
*   **提升决策可靠性**：在机器人导航中，SAE 能有效减少虚假目标的生成，使导航行为更符合指令逻辑，减少无效探索时间。

### 7. 优点
*   **即插即用**：无需重新训练模型，适用于任何基于 Transformer 架构的 LVLM。
*   **语义级解释性**：相比于 Token 级的分析，SAE 从物体语义层面解释注意力，更符合人类视觉感知逻辑。
*   **多场景验证**：不仅限于离线数据集，还通过物理机器人实验证明了实用价值。

### 8. 不足与局限
*   **依赖外部模型**：SAE 的效果高度依赖于语义分割模型的准确性。如果分割模型无法识别某些罕见物体，SAE 的计算可能失效。
*   **推理延迟**：在推理过程中实时提取注意力并计算熵，加上运行分割模型，会增加端到端的响应延迟。
*   **幻觉类型限制**：主要针对“物体存在性”幻觉，对于物体属性（颜色、形状）或物体间复杂关系的幻觉，其缓解效果尚待进一步验证。

（完）
