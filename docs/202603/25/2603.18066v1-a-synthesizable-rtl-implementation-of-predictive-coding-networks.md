---
title: A Synthesizable RTL Implementation of Predictive Coding Networks
title_zh: 预测编码网络的可综合 RTL 实现
authors: Timothy Oh
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18066v1"
tags: ["query:snn"]
score: 9.0
evidence: 硬件学习与神经核心的数字架构
tldr: 针对反向传播算法在硬件实现中面临的全局误差传播和内存依赖等挑战，本文提出了一种可综合的预测编码（PC）网络数字架构。该设计采用RTL实现，使每个神经元核心独立维护活动、预测误差和权重，仅通过硬连线与相邻层通信。该架构利用顺序MAC数据通路和固定有限状态机执行本地更新规则，支持监督学习与推理，为构建全分布式、在线学习的硬件系统提供了完整的数字基底。
selection_source: fresh_fetch
motivation: 克服反向传播算法在硬件实现中因全局误差传播和中心化内存导致的在线学习困难。
method: 提出一种基于可综合RTL的数字架构，通过固定本地更新规则和神经元钳位原语实现预测编码动力学。
result: 实现了一个无需任务特定指令、仅靠本地通信和固定状态机即可完成推理与学习的硬件系统。
conclusion: 该研究为预测编码在数字硬件上的直接实现提供了可行方案，是迈向全分布式在线学习硬件的重要一步。
---

## 摘要
反向传播推动了现代深度学习的发展，但由于全局误差传播、相位分离以及对集中式存储的严重依赖，难以实现为在线、全分布式的硬件学习系统。预测编码提供了一种替代方案，其中推理和学习源于相邻层之间的局部预测误差动力学。本文提出了一种数字架构，直接在硬件中实现离散时间预测编码更新。每个神经核心维护其自身的活动、预测误差和突触权重，且仅通过硬连线连接与相邻层通信。通过一种统一的单神经元钳位原语支持有监督学习和推理，该原语在强制执行边界条件的同时保持内部更新调度不变。该设计是一个确定性的、可综合的 RTL 基底，围绕串行 MAC 数据通路和固定有限状态调度构建。该系统并非在学习基底内部执行特定任务的指令序列，而是在固定的局部更新规则下演化，任务结构通过连接性、参数和边界条件施加。本工作的贡献不在于提出新的学习规则，而在于提供了一个完整的可综合数字基底，能够直接在硬件中执行预测编码学习动力学。

## Abstract
Backpropagation has enabled modern deep learning but is difficult to realize as an online, fully distributed hardware learning system due to global error propagation, phase separation, and heavy reliance on centralized memory. Predictive coding offers an alternative in which inference and learning arise from local prediction-error dynamics between adjacent layers. This paper presents a digital architecture that implements a discrete-time predictive coding update directly in hardware. Each neural core maintains its own activity, prediction error, and synaptic weights, and communicates only with adjacent layers through hardwired connections. Supervised learning and inference are supported via a uniform per-neuron clamping primitive that enforces boundary conditions while leaving the internal update schedule unchanged. The design is a deterministic, synthesizable RTL substrate built around a sequential MAC datapath and a fixed finite-state schedule. Rather than executing a task-specific instruction sequence inside the learning substrate, the system evolves under fixed local update rules, with task structure imposed through connectivity, parameters, and boundary conditions. The contribution of this work is not a new learning rule, but a complete synthesizable digital substrate that executes predictive-coding learning dynamics directly in hardware.

---

## 论文详细总结（自动生成）

### 论文总结：预测编码网络的可综合 RTL 实现

#### 1. 核心问题与研究动机
*   **核心问题**：现代深度学习依赖的反向传播（Backpropagation, BP）算法在硬件实现上存在三大挑战：全局误差传播（破坏局部性）、相位分离（前向/后向/更新阶段需严格同步）以及对中心化存储的重度依赖。
*   **研究动机**：预测编码（Predictive Coding, PC）提供了一种生物学启发式的替代方案。在 PC 中，推理和学习通过相邻层之间的局部预测误差动力学自发产生。本文旨在证明 PC 算法可以被直接映射到一个确定性的、可综合的数字电路基底（RTL）上，从而实现全分布式的在线硬件学习系统。

#### 2. 方法论：核心思想与技术细节
*   **核心思想**：将每个神经元实现为一个独立的“神经核心”（Neural Core），它自主维护状态、误差和权重，且仅与物理相邻的层通信。
*   **关键技术细节**：
    *   **神经核心架构**：每个核心包含标量状态 $x$、预测误差 $\epsilon$ 和局部突触权重向量 $\theta$。
    *   **有限状态机（FSM）调度**：每个时钟周期（Tick）执行固定的六阶段序列：
        1.  **PRED**：计算预测值 $\mu$。
        2.  **ERR**：计算局部预测误差 $\epsilon$。
        3.  **BACKSUM**：累加来自下层的反向误差项。
        4.  **BACKVEC**：向上层发送误差乘积。
        5.  **WUP**：根据局部赫布（Hebbian）规则更新权重。
        6.  **STATE**：更新神经元活动状态。
    *   **统一钳位原语**：通过 `x_set_en` 和 `x_obs` 信号实现边界条件控制。在监督学习中，输入层和输出层被“钳位”到观测值；在推理中，仅钳位输入层。
    *   **算术格式**：采用 IEEE-754 单精度浮点数（使用 HardFloat 库），确保计算的确定性和精度。
    *   **顺序数据通路**：为了节省面积，每个核心内部使用单个乘累加（MAC）单元循环处理所有突触输入。

#### 3. 实验设计
*   **实验场景**：
    1.  **教师-学生回归**：使用 2→4→3 结构的 ReLU 网络学习预定义的非线性函数。
    2.  **非线性回归**：使用 2→2→1 结构的 tanh 网络进行回归任务。
    3.  **架构缩放测试**：在不改变 RTL 逻辑的情况下，测试 2→4→3、4→8→4 和 8→16→8 三种不同规模的网络。
*   **Benchmark**：以均方误差（MSE）随训练轮数（Epoch）的变化曲线作为主要衡量指标。
*   **对比方法**：本文重点在于硬件实现的可行性验证，主要对比了不同激活函数（ReLU vs tanh）和不同网络规模下的收敛稳定性，未直接与 GPU 运行的 BP 算法进行性能对比。

#### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 训练时长或 FPGA 资源消耗数值。
*   **实现方式**：实验通过 **Verilator** 进行 RTL 仿真验证。作者提供了开源的 SystemVerilog 代码和仿真脚本，允许在通用 CPU 上复现硬件逻辑的演化过程。
*   **硬件成本模型**：文中给出了一个理论延迟模型，每个 Tick 的周期数 $C_{tick} \approx 3N + M + 4$（$N$ 为前向扇入，$M$ 为后向扇入），表明延迟随扇入线性增长。

#### 5. 实验数量与充分性
*   **实验数量**：共进行了 5 组主要实验（涵盖不同激活函数和 3 种网络规模）。
*   **充分性评价**：
    *   **功能验证充分**：实验成功证明了局部更新规则在数字硬件中可以实现端到端的监督学习。
    *   **客观性**：通过开源代码和 CSV 原始数据确保了结果的可复现性。
    *   **局限性**：实验规模较小（神经元数量在数十个量级），尚未在大规模数据集（如 ImageNet）或复杂视觉任务上进行验证。

#### 6. 主要结论与发现
*   **硬件可行性**：预测编码动力学可以完全通过固定的硬件 FSM 和局部通信实现，无需全局控制器或复杂的指令集。
*   **收敛特性**：在硬件离散时间更新下，网络表现出“两阶段”收敛特征——初期快速下降后进入缓慢优化的平台期。
*   **架构通用性**：同一套神经核心 RTL 代码可以无缝扩展到不同深度的网络，任务结构由连接关系和边界条件决定，而非软件指令。

#### 7. 优点
*   **全分布式设计**：消除了 BP 算法中的全局同步瓶颈，适合构建大规模并行神经形态硬件。
*   **可综合性**：采用标准 RTL 设计流程，可直接部署于 FPGA 或 ASIC。
*   **开源贡献**：提供了完整的硬件实现参考，对神经形态工程社区具有较高的实用价值。
*   **确定性**：相比模拟电路实现，数字 RTL 保证了计算的可重复性和易调试性。

#### 8. 不足与局限
*   **吞吐量瓶颈**：由于采用顺序 MAC 处理扇入，当神经元连接数增加时，单个 Tick 的延迟会显著增加。
*   **数值复杂性**：实现非线性激活函数（如 tanh）及其导数在数字电路中需要较大的面积开销。
*   **稳定性风险**：离散时间步长（$\gamma$）和有限精度可能导致某些配置下系统不稳定，需精细调参。
*   **任务复杂度**：目前仅验证了简单的回归任务，对于深度卷积网络或循环网络的适配性尚待研究。

（完）
