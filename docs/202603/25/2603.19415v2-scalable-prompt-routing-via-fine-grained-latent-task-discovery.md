---
title: Scalable Prompt Routing via Fine-Grained Latent Task Discovery
title_zh: 基于细粒度潜在任务发现的可扩展提示词路由
authors: "Yunyi Zhang, Soji Adeshina, Sheng Guan, Ashwin Ganesh, Zhen Han, Vassilis N. Ioannidis, Huzefa Rangwala, George Karypis"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19415v2"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型池的提示路由
tldr: 针对大模型路由中手动分类粒度粗、单一路由模型难以区分细微任务差异的问题，本文提出一种两阶段路由架构。该方法通过图聚类自动发现潜在的细粒度任务，并结合混合专家（MoE）架构进行任务感知的质量评估。实验表明，该方法在10个基准测试中超越了最强单模型，且成本降低了一半以上，实现了性能与成本的高效平衡。
selection_source: fresh_fetch
motivation: 现有的提示词路由方法难以应对模型池规模扩大带来的细粒度能力区分挑战，且手动定义的任务分类无法捕捉模型间的微妙差异。
method: 采用两阶段架构，首先通过图聚类自动发现潜在任务并训练分类器，随后利用带有任务特定预测头的混合专家模型进行精细化质量估算。
result: 在10个基准测试和11个前沿模型上的评估显示，该方法优于现有基准，并以不到一半的成本超越了最强单模型。
conclusion: 通过自动化细粒度任务发现与任务感知质量预测相结合，可以实现高效、可扩展且低成本的大模型动态路由。
---

## 摘要
提示词路由为每个查询从候选池中动态选择最合适的大语言模型，在控制成本的同时优化性能。随着模型池扩展到包含数十个性能差距较小的前沿模型，现有方法面临重大挑战：手动定义的任务分类法无法捕捉细粒度的能力差异，而单一路由模型难以区分不同任务之间的细微差别。我们提出了一种两阶段路由架构，通过自动化的细粒度任务发现和任务感知质量评估来解决这些局限性。第一阶段采用基于图的聚类来发现潜在任务类型，并训练一个分类器将提示词分配给发现的任务。第二阶段使用带有任务特定预测头的混合专家架构，进行专门的质量评估。在推理时，我们聚合两个阶段的预测结果，以平衡任务层面的稳定性和提示词层面的适应性。在包含 11 个前沿模型的 10 个基准测试上的评估结果显示，我们的方法始终优于现有基准，并在成本不到一半的情况下超越了最强的单个模型。

## Abstract
Prompt routing dynamically selects the most appropriate large language model from a pool of candidates for each query, optimizing performance while managing costs. As model pools scale to include dozens of frontier models with narrow performance gaps, existing approaches face significant challenges: manually defined task taxonomies cannot capture fine-grained capability distinctions, while monolithic routers struggle to differentiate subtle differences across diverse tasks. We propose a two-stage routing architecture that addresses these limitations through automated fine-grained task discovery and task-aware quality estimation. Our first stage employs graph-based clustering to discover latent task types and trains a classifier to assign prompts to discovered tasks. The second stage uses a mixture-of-experts architecture with task-specific prediction heads for specialized quality estimates. At inference, we aggregate predictions from both stages to balance task-level stability with prompt-specific adaptability. Evaluated on 10 benchmarks with 11 frontier models, our method consistently outperforms existing baselines and surpasses the strongest individual model while incurring less than half its cost.

---

## 论文详细总结（自动生成）

以下是对论文《Scalable Prompt Routing via Fine-Grained Latent Task Discovery》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）的爆发式增长，用户面临如何在数十个性能相近的前沿模型（如 Claude 4.5, DeepSeek-R1, Llama-4 等）中为特定查询选择最合适模型的问题。
*   **现有挑战**：手动定义的任务分类（如“数学”、“代码”）过于粗糙，无法捕捉模型间的微妙差异；而单一的路由模型（Monolithic Router）在面对海量且异构的任务时，难以精准预测每个模型的表现。
*   **核心目标**：开发一种可扩展的提示词路由架构，通过**自动发现细粒度潜在任务**并结合**任务感知质量评估**，在降低推理成本的同时提升整体性能。

### 2. 方法论：FineRouter 两阶段架构
论文提出了名为 **FineRouter** 的两阶段路由方法：

*   **第一阶段：潜在任务发现与匹配（Offline & Online）**
    *   **任务发现**：利用 LLM 生成提示词的简短描述，构建一个稀疏图。图中节点为提示词，边权重结合了**语义相似度**和**模型偏好相似度**（使用 RBO 秩相关系数衡量）。
    *   **聚类算法**：采用 Leiden 社区检测算法迭代发现细粒度的任务簇（如将“数学”细分为“符号代数”和“应用题”）。
    *   **候选筛选**：为每个任务簇自适应选择表现最好的模型子集。
    *   **分类器训练**：训练一个双线性匹配分类器，将新提示词映射到发现的任务类型上。

*   **第二阶段：任务感知动态路由（MoE 架构）**
    *   **混合专家（MoE）质量评估**：路由模型包含一个共享的提示词编码器和两类预测头（Adapters）。
    *   **通用预测头**：学习每个模型在全球数据上的基准表现。
    *   **任务特定预测头**：针对第一阶段发现的特定任务，仅在对应任务激活时训练和调用，提供精细化的质量预测。
    *   **推理聚合**：最终路由分数综合了第一阶段的任务级先验得分和第二阶段的提示词级预测得分。

### 3. 实验设计
*   **数据集/场景**：涵盖 10 个主流基准测试，包括问答（NQ, TriviaQA）、多轮推理（MMLU, ARC）、数学（GSM8K, MATH）和代码（HumanEval, MBPP）。
*   **候选模型池**：包含 **11 个前沿模型**，如 Claude-Sonnet-4.5、DeepSeek-R1、Llama-4-Maverick、Qwen3-235B 等。
*   **对比方法（Baselines）**：
    *   传统方法：kNN, MLP。
    *   先进路由算法：RouteLLM, RouterDC, GraphRouter, IPR（目前最强的质量预测路由）。
*   **评估指标**：任务特定指标（如准确率）和基于 Skywork-Reward-V2 的综合质量得分。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练时长**：整个流水线训练约需 **17 小时**（总计约 135 GPU 小时）。其中基础模型训练 6 小时，任务特定适配器微调 11 小时。
*   **推理效率**：由于采用了 MoE 架构，推理时的有效参数量保持恒定，计算开销极低。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同领域的基准测试上进行了验证，训练集包含约 27.9 万条样本，测试集约 3.5 万条。
*   **消融实验**：对比了“仅第一阶段”、“仅第二阶段”、“粗粒度分类 vs 细粒度发现”等多种变体，证明了自动化细粒度任务发现的必要性。
*   **客观性**：使用了最新的前沿模型作为候选池，而非较弱的开源模型，这更符合真实工业界的应用场景，实验结果具有较高的参考价值。

### 6. 主要结论与发现
*   **超越单模型**：FineRouter 的综合表现超过了模型池中任何单一模型（包括最强的 Claude-Sonnet-4.5）。
*   **成本优势**：在达到相同或更高性能的前提下，FineRouter 的推理成本**不到最强单模型的一半**。
*   **任务发现价值**：系统自动发现了 332 个细粒度任务，能够识别出如“电话区号查询”等手动分类难以覆盖的利基领域。
*   **路由分布**：路由决策分布均匀，充分利用了不同模型（Claude, DeepSeek, Llama）在不同细分领域的长处。

### 7. 优点
*   **自动化程度高**：无需人工设计复杂的任务分类法，能够随数据动态演进。
*   **精度与稳定性平衡**：通过两阶段聚合，既保留了任务层面的统计稳定性，又具备提示词层面的灵活性。
*   **可扩展性强**：MoE 架构允许在不显著增加推理延迟的情况下，扩展到更多的模型和任务类型。

### 8. 不足与局限
*   **依赖奖励模型**：路由器的训练高度依赖于奖励模型（Reward Model）的打分质量，若奖励模型存在偏见，路由结果也会受损。
*   **模态限制**：目前仅支持文本提示词，尚未扩展到多模态（图像、音频）路由场景。
*   **超参数敏感性**：图聚类中的 RBO 阈值、覆盖率阈值等参数需要针对不同数据集进行一定程度的调优。
*   **冷启动开销**：发现新任务需要收集所有候选模型对训练数据的响应，这在模型频繁更新时可能带来较高的初期数据采集成本。

（完）
