---
title: Reward Sharpness-Aware Fine-Tuning for Diffusion Models
title_zh: 扩散模型的奖励锐度感知微调
authors: "Kwanyoung Kim, Byeongsu Sim"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21175v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 扩散模型的基于人类反馈强化学习与奖励中心微调
tldr: 针对扩散模型在强化学习微调中出现的“奖励作弊”问题，本文提出了RSA-FT框架。研究发现该问题源于奖励模型梯度的不鲁棒性，特别是在奖励景观较陡峭时。RSA-FT通过对扩散模型参数和生成样本进行扰动，平滑奖励景观，从而在不重新训练奖励模型的情况下提高微调的鲁棒性和生成质量，为扩散模型与人类偏好对齐提供了更可靠的方案。
selection_source: fresh_fetch
motivation: 扩散模型在强化学习微调中容易出现奖励作弊现象，即奖励分值增加但图像质量反而下降，这与奖励景观的尖锐程度密切相关。
method: 提出RSA-FT框架，通过在微调过程中引入模型参数扰动和生成样本扰动，利用平滑后的奖励梯度来引导模型优化。
result: 实验结果显示，该方法能有效抑制奖励作弊，提升生成结果的感知质量，且两种扰动策略结合使用效果更佳。
conclusion: RSA-FT通过优化奖励景观的平滑度，为提升扩散模型强化学习对齐的稳定性和可靠性提供了一种简单且通用的解决方案。
---

## 摘要
基于人类反馈的强化学习 (RLHF) 已被证明在使大语言模型与人类偏好对齐方面非常有效，这启发了以奖励为中心的扩散强化学习 (RDRL) 的发展，以实现类似的对齐和可控性。虽然扩散模型可以生成高质量的输出，但 RDRL 仍然容易受到奖励作弊 (reward hacking) 的影响，即奖励分数增加但感知质量却没有相应提高。我们证明了这种脆弱性源于奖励模型梯度的非鲁棒性，特别是当相对于输入图像的奖励景观 (reward landscape) 较为锐利时。为了缓解这一问题，我们引入了一些方法，利用经过鲁棒化处理的奖励模型的梯度，而无需对其进行重新训练。具体而言，我们采用了来自平滑奖励模型的梯度，该模型是通过对扩散模型进行参数扰动以及对其生成的样本进行扰动而获得的。实验表明，每种方法都能独立地缓解奖励作弊并提高鲁棒性，而将它们结合使用则能放大这些益处。我们最终得到的框架 RSA-FT（奖励锐度感知微调）简单且具有广泛的兼容性，并能持续增强 RDRL 的可靠性。

## Abstract
Reinforcement learning from human feedback (RLHF) has proven effective in aligning large language models with human preferences, inspiring the development of reward-centric diffusion reinforcement learning (RDRL) to achieve similar alignment and controllability. While diffusion models can generate high-quality outputs, RDRL remains susceptible to reward hacking, where the reward score increases without corresponding improvements in perceptual quality. We demonstrate that this vulnerability arises from the non-robustness of reward model gradients, particularly when the reward landscape with respect to the input image is sharp. To mitigate this issue, we introduce methods that exploit gradients from a robustified reward model without requiring its retraining. Specifically, we employ gradients from a flattened reward model, obtained through parameter perturbations of the diffusion model and perturbations of its generated samples. Empirically, each method independently alleviates reward hacking and improves robustness, while their joint use amplifies these benefits. Our resulting framework, RSA-FT (Reward Sharpness-Aware Fine-Tuning), is simple, broadly compatible, and consistently enhances the reliability of RDRL.

---

## 论文详细总结（自动生成）

以下是对论文《Reward Sharpness-Aware Fine-Tuning for Diffusion Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然基于奖励模型的扩散强化学习（RDRL）在对齐人类偏好方面表现出色，但普遍面临“奖励作弊”（Reward Hacking）问题——即模型生成的图像在奖励模型（RM）中得分很高，但实际视觉质量或文本对齐度却在下降。
*   **核心发现**：作者发现奖励作弊本质上是一种“对抗攻击”。奖励模型在输入图像的局部景观（Landscape）中过于“尖锐”（Sharp），导致微调过程陷入那些奖励极高但缺乏泛化性的非鲁棒区域。
*   **整体含义**：本文旨在通过平滑奖励景观，在不重新训练奖励模型的前提下，提升扩散模型微调的鲁棒性和生成质量。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **RSA-FT（奖励锐度感知微调）** 框架。其核心逻辑是：与其直接最大化原始奖励，不如最大化局部邻域内的“最小奖励”（即最差情况奖励），从而迫使模型寻找平坦且鲁棒的奖励区域。
*   **关键技术细节**：
    *   **图像空间扰动（AT 风格）**：在生成的图像 $x_0$ 上施加微小扰动 $\delta$，寻找使奖励下降最快的方向。
    *   **参数空间扰动（SAM 风格）**：对扩散模型的权重 $\theta$ 施加扰动 $\epsilon$，寻找对权重变化敏感的区域。
    *   **联合优化（RSA-FT）**：算法在每一步训练中，先计算图像和权重的最差情况扰动，然后基于扰动后的状态计算梯度并更新模型参数。
*   **公式逻辑**：目标函数定义为 $\max_{\theta} \mathbb{E} [ \min_{\|\delta\|<\rho, \|\epsilon\|<\rho_w} r(x_0(\theta + \epsilon) + \delta) ]$。这种双重扰动机制结合了对抗训练（AT）和锐度感知最小化（SAM）的优点。

### 3. 实验设计
*   **数据集/场景**：使用了 **DrawBench** 和 **HPSv2** 基准测试集，涵盖了广泛的文本提示词。
*   **扩散模型底座（Backbones）**：包括 Stable Diffusion v1.5 (SD1.5)、SDXL、SD3 以及最新的 Flux.1-dev。
*   **对比框架（Baselines）**：集成了四种主流 RDRL 方法：ReFL、DRaFT-K、AlignProp 和 DRTune。
*   **评估指标**：
    *   **自动指标**：HPSv2.1、PickScore、ImageReward（用于检测是否发生奖励作弊）。
    *   **人工评估**：邀请 17 名独立评估者进行视觉偏好和文本对齐度测试。

### 4. 资源与算力
*   **硬件设备**：使用了 **NVIDIA H100 GPU**。
*   **算力分配**：
    *   SD1.5 实验：使用 2 张 H100，训练 2500 次迭代。
    *   SDXL / SD3 / Flux 实验：使用 4 张 H100，训练 2000 次迭代。
*   **超参数**：Batch Size 为 32（SD1.5）或 4（大模型），学习率统一设为 $2 \times 10^{-5}$ 或 $1 \times 10^{-5}$。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 512x512 到 1024x1024 分辨率的多种模型，跨越了 U-Net 和 Transformer (DiT) 两种架构。
*   **消融实验**：详细对比了仅使用图像扰动、仅使用参数扰动以及两者结合的效果，验证了双重扰动的协同作用。
*   **充分性评价**：实验设计非常充分且客观。作者不仅在目标奖励（HPSv2）上进行了测试，还通过第三方奖励模型（PickScore 等）验证了泛化性，有效证明了该方法确实抑制了奖励作弊而非仅仅是过拟合。

### 6. 主要结论与发现
*   **锐度与偏好的负相关性**：实验证实，奖励景观越尖锐，模型生成的图像与人类真实偏好的对齐度就越差。
*   **RSA-FT 的普适性**：该方法作为“即插即用”模块，能显著提升所有基准 RDRL 方法的性能。
*   **抑制奖励作弊**：在 HPSv2 分数提升的同时，PickScore 和 ImageReward 等辅助指标也同步提升，证明了生成质量的真实改进。

### 7. 优点
*   **无需重训奖励模型**：在不增加额外标注数据或重新训练 RM 的情况下，解决了 RDRL 的核心痛点。
*   **理论与直觉结合**：将对抗鲁棒性理论成功引入扩散模型微调领域，解释了奖励作弊的几何本质。
*   **架构无关性**：在 CNN (SD1.5) 和 Transformer (SD3/Flux) 架构上均表现出极强的鲁棒性。

### 8. 不足与局限
*   **计算开销**：由于需要计算两次梯度（一次用于寻找扰动，一次用于参数更新），训练时间相比原始 RDRL 会有所增加（这是 SAM 类方法的通病）。
*   **人工评估规模**：虽然包含人工评估，但 17 人的规模相对较小，可能存在一定的统计偏差。
*   **超参数敏感性**：虽然论文给出了推荐值，但扰动半径 $\rho$ 在不同奖励模型或任务下可能仍需微调以达到最优平衡。

（完）
