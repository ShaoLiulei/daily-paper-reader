---
title: A Latency Coding Framework for Deep Spiking Neural Networks with Ultra-Low Latency
title_zh: 一种具有超低延迟的深层脉冲神经网络延迟编码框架
authors: "Yi Lu, Jianhao Ding, Zhaofei Yu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23206v1"
tags: ["query:snn"]
score: 10.0
evidence: 使用BPTT的深度脉冲神经网络延迟编码框架
tldr: 本研究针对脉冲神经网络（SNN）中首个脉冲时间（TTFS）编码训练难、延迟高的问题，提出了一种深度延迟编码框架。通过引入延迟编码模块、放宽单脉冲约束及时间自适应决策损失函数，实现了基于BPTT的高效训练。该方法在保持超低推理延迟和高能效的同时，达到了SOTA准确率，并显著增强了模型对输入干扰的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的TTFS编码SNN缺乏高效训练手段，导致模型在深层网络中面临推理延迟高、性能受限及梯度消失等问题。
method: 构建了包含特征提取与直通估计器的延迟编码模块，放宽了中间层单脉冲限制，并设计了时间自适应决策损失函数以优化训练。
result: 该框架在极低推理延迟下实现了SOTA级别的分类准确率，同时展现出优异的能效比和抗干扰能力。
conclusion: 本研究验证了延迟编码在快速响应场景下的巨大潜力，为进一步开发SNN的时间维度学习能力提供了新思路。
---

## 摘要
脉冲神经网络 (SNN) 提供了一种受生物启发的计算范式，在高效能神经处理方面具有巨大潜力。在 SNN 的神经编码方案中，首个脉冲时间 (TTFS) 编码通过神经元首个脉冲的精确时间来编码信息，具有卓越的能量效率和生物合理性。尽管具有理论优势，现有的 TTFS 模型仍缺乏高效的训练方法，面临推理延迟高和性能受限的问题。在这项工作中，我们提出了一个全面的框架，通过采用随时间反向传播 (BPTT) 算法，实现了深层 TTFS 编码 SNN 的高效训练。我们将框架中广义的 TTFS 编码方法命名为延迟编码 (latency coding)。该框架包括：(1) 一个包含特征提取和直通估计器 (straight-through estimators) 的延迟编码 (LE) 模块，以解决直接强度-延迟映射中的严重信息丢失问题，并确保梯度流平滑；(2) 放宽了传统 TTFS 严格的单脉冲约束，允许中间层神经元多次发放脉冲，以缓解深层网络中的梯度消失问题；(3) 一种时间自适应决策 (TAD) 损失函数，根据样本相关的置信度动态加权监督信号，解决了延迟编码与标准交叉熵损失之间的不兼容性。实验结果表明，与现有的 TTFS 编码 SNN 相比，我们的方法在超低推理延迟和卓越能量效率的情况下实现了最先进的准确率。该框架还表现出对输入损坏更强的鲁棒性。我们的研究探讨了延迟编码在需要快速响应场景下的特性和潜力，为进一步挖掘 SNN 的时间学习能力提供了宝贵的见解。

## Abstract
Spiking neural networks (SNNs) offer a biologically inspired computing paradigm with significant potential for energy-efficient neural processing. Among neural coding schemes of SNNs, Time-To-First-Spike (TTFS) coding, which encodes information through the precise timing of a neuron's first spike, provides exceptional energy efficiency and biological plausibility. Despite its theoretical advantages, existing TTFS models lack efficient training methods, suffering from high inference latency and limited performance. In this work, we present a comprehensive framework, which enables the efficient training of deep TTFS-coded SNNs by employing backpropagation throuh time (BPTT) algorithm. We name the generalized TTFS coding method in our framework as latency coding. The framework includes: (1) a latency encoding (LE) module with feature extraction and straight-through estimators to address severe information loss in direct intensity-to-latency mapping and ensure smooth gradient flow; (2) relaxation of the strict single-spike constraint of traditional TTFS, allowing neurons of intermediate layers to fire multiple times to mitigating gradient vanishing in deep networks; (3) a temporal adaptive decision (TAD) loss function that dynamically weights supervision signals based on sample-dependent confidence, resolving the incompatibility between latency coding and standard cross-entropy loss. Experimental results demonstrate that our method achieves state-of-the-art accuracy in comparison to existing TTFS-coded SNNs with ultra-low inference latency and superior energy efficiency. The framework also demonstrates improved robustness against input corruptions. Our study investigates the characteristics and potential of latency coding in scenarios demanding rapid response, providing valuable insights for further exploiting the temporal learning capabilities of SNNs.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Latency Coding Framework for Deep Spiking Neural Networks with Ultra-Low Latency》的结构化深度分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：脉冲神经网络（SNN）中的“首个脉冲时间”（Time-To-First-Spike, TTFS）编码虽然具有极高的能效和生物合理性，但在深度学习语境下面临三大挑战：
    1.  **训练效率低下**：缺乏高效的直接训练方法，现有方法多依赖高延迟的 ANN-to-SNN 转换。
    2.  **性能与延迟矛盾**：传统 TTFS 在深层网络中容易出现梯度消失和信息衰减，且推理延迟往往高达数百甚至数千个时间步。
    3.  **信息丢失**：直接将像素强度映射为延迟会导致严重的量化误差和特征表达不足。
*   **研究动机**：探索如何将随时间反向传播（BPTT）算法引入 TTFS 编码，构建一个能在超低延迟（1-4个时间步）下实现高精度的深度 SNN 训练框架。

### 2. 方法论：核心思想与关键技术
该论文提出了一个名为“延迟编码”（Latency Coding）的通用框架，核心组件包括：
*   **延迟编码（LE）模块**：
    *   **特征提取**：不直接对原始像素编码，而是先通过卷积层提取特征，再经 Sigmoid 归一化。
    *   **强度-延迟映射**：将特征值 $x$ 映射为发放时间 $t_s = \lceil(1-x)T\rceil$。
    *   **直通估计器（STE）**：在反向传播中将不可微的编码过程视为恒等映射，确保梯度能够流向输入层。
*   **放宽单脉冲约束**：
    *   不同于传统 TTFS 严格限制每个神经元只发一次脉冲，该框架允许中间层神经元多次发放脉冲以缓解梯度消失，但输出层决策仍基于首个脉冲。
*   **膜电位辅助解码**：
    *   在离散时间步下，若多个输出神经元在同一最早时刻发放脉冲，则通过比较它们的**膜电位（Membrane Potential）**大小来消除歧义，选择电位最高者作为预测类别。
*   **时间自适应决策（TAD）损失函数**：
    *   **核心逻辑**：利用输出层 Logits 的 Shannon 熵计算瞬时置信度 $\lambda[t]$。
    *   **动态加权**：置信度越高的时间步，其交叉熵损失的权重越大。这迫使网络对简单样本早决策，对复杂样本则延迟决策以积累更多信息。

### 3. 实验设计
*   **数据集**：涵盖静态图像（CIFAR-10, CIFAR-100, Tiny-ImageNet）和神经形态数据集（CIFAR10-DVS）。
*   **基准模型（Backbone）**：VGG-11, VGG-16, SEW-ResNet-18。
*   **对比方法**：
    *   **TTFS 类**：T2FSNN, TSC-SNN, DTA-TTFS 等。
    *   **率编码（Rate Coding）类**：TET 等 SOTA 方法。
    *   **Transformer 类**：T-SpikeFormer。
*   **评估指标**：分类准确率（Accuracy）、推理延迟（Timesteps）、能效（SOPs, Energy Consumption）、鲁棒性（mCE）。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX 4090 GPU**。
*   **训练配置**：使用 AdamW 优化器，初始学习率 0.001，采用余弦退火调度，训练时长为 **200 epochs**。
*   **时间步设置**：训练时的最大时间步 $T$ 通常设为 2、4 或 6。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验验证，包括：
    1.  **主流数据集对比**：在四个数据集上验证了性能。
    2.  **能效分析**：基于 45nm CMOS 技术参数，对比了 ANN、率编码 SNN 与本方法的理论能耗。
    3.  **鲁棒性测试**：在 CIFAR-10/100-C（受损数据集）上测试了 15 种干扰下的表现。
    4.  **消融实验**：分别验证了 LE 模块、TAD 损失函数的有效性，并分析了时间步的可扩展性。
*   **评价**：实验设计非常充分且客观。通过与不同编码机制（率编码 vs 延迟编码）的深度对比，清晰地展示了该框架在延迟和能效上的压倒性优势。

### 6. 主要结论与发现
*   **超低延迟**：在 CIFAR-10 上仅需 **1.00 个平均时间步**即可达到 93.60% 的准确率，比现有 TTFS 方法快了两个数量级。
*   **高能效**：能耗仅为同等规模 ANN 的 **4.1% - 12.6%**，且显著低于率编码 SNN。
*   **强鲁棒性**：延迟编码 SNN 在面对图像模糊、天气干扰等结构性失真时，表现优于率编码 SNN。
*   **时间稀疏性与性能的平衡**：发现放宽中间层单脉冲约束虽然略微增加了脉冲数，但通过大幅缩短推理时间，最终实现了更低的整体功耗。

### 7. 优点（亮点）
*   **训练范式突破**：成功将 BPTT 应用于延迟编码，解决了深度 TTFS-SNN 难以直接训练的顽疾。
*   **自适应机制**：TAD 损失函数巧妙地模拟了“简单样本快思考，复杂样本慢思考”的生物逻辑。
*   **理论与实际结合**：不仅关注准确率，还深入分析了硬件层面的能耗（SOPs）和时间相似性矩阵，解释了鲁棒性的来源。

### 8. 不足与局限
*   **噪声敏感性**：实验发现延迟编码对**高斯噪声（Gaussian Noise）**等像素级统计噪声的抵抗力弱于率编码。
*   **性能微差**：虽然达到了 TTFS 类的 SOTA，但与最顶尖的率编码 SNN（带早期退出机制）相比，在某些复杂任务上仍存在极小的准确率差距。
*   **编码模块依赖**：LE 模块引入了额外的卷积层进行特征预处理，虽然提升了性能，但也略微增加了输入端的计算开销。

（完）
