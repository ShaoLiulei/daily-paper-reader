---
title: "Capability-Guided Compression: Toward Interpretability-Aware Budget Allocation for Large Language Models"
title_zh: 能力引导的压缩：面向大语言模型可解释性感知预算分配
authors: Rishaank Gupta
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16440v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型的可解释性感知压缩
tldr: 针对大语言模型压缩中忽略组件功能编码的“能力盲区”问题，本文提出能力引导压缩（CGC）框架。该框架利用稀疏自编码器（SAE）定义的“能力密度”指标，衡量组件的功能复杂性与冗余度，从而实现差异化的压缩预算分配。研究证明能力密度是独立于现有重要性指标的新信号，能预测组件性能的相位突变，为可解释性驱动的模型压缩奠定了理论基础。
selection_source: fresh_fetch
motivation: 现有压缩方法因缺乏对组件功能含义的理解，导致困惑度评估失效及模型性能出现不可预测的相位突变。
method: 提出CGC框架，利用SAE提取的特征广度、熵和一致性定义“能力密度”，并据此为不同Transformer组件分配差异化压缩预算。
result: 实验证实能力密度与Wanda等传统重要性指标统计独立，且能理论预测组件在压缩过程中的性能崩溃点。
conclusion: 该研究建立了能力感知压缩的理论框架，证明了可解释性信号在优化模型压缩策略中的独特价值。
---

## 摘要
大语言模型压缩在剪枝、量化和低秩分解方面取得了显著进展，但现有方法普遍存在一个根本性局限：压缩预算的分配并未考虑单个模型组件在功能上编码的内容。我们将此称为“能力盲视压缩”问题，并认为它是两个已有记录的失效现象的根源——即基于困惑度（PPL）的评估对推理能力损失的不敏感，以及 Ma 等人（2026）最近描述的模型性能突发相变。我们提出了能力引导压缩（CGC）框架，通过使用稀疏自编码器（SAE）衍生的能力密度图，在 Transformer 组件之间分配差异化的压缩预算。能力密度是一个正式定义的标量度量，结合了组件 SAE 特征激活分布的特征广度、激活熵和跨输入一致性。我们在理论上证明，具有较高能力密度的组件表现出较低的结构冗余，并在较低的压缩率下达到各自的相变点，从而为组件级相变预测提供了首个预压缩机制。在 GPT-2 Medium 上的实验证实，能力密度在统计上独立于 Wanda 重要性评分（Spearman rho = -0.054, n = 384 个注意力头），这确立了它是一种与所有现有重要性指标正交的全新压缩信号。我们报告了基于 PPL 的压缩比较的负面结果，并提供了一个原则性的诊断，指出 GPT-2 Medium 不足以作为验证完整 CGC 假设的测试平台。该理论框架、密度形式化方法以及正交性发现为能力感知压缩研究奠定了基础。

## Abstract
Large language model compression has made substantial progress through pruning, quantization, and low-rank decomposition, yet a fundamental limitation persists across all existing methods: compression budgets are allocated without any representation of what individual model components functionally encode. We term this the capability-blind compression problem and argue it is a root cause of two well-documented failures -- the insensitivity of perplexity-based evaluation to reasoning capability loss, and the abrupt phase transitions in model performance recently characterized by Ma et al. (2026). We propose Capability-Guided Compression (CGC), a framework that addresses this by using Sparse Autoencoder (SAE)-derived capability density maps to allocate differential compression budgets across transformer components. Capability density is a formally defined scalar measure combining the feature breadth, activation entropy, and cross-input consistency of a component's SAE feature activation distribution. We prove theoretically that components with higher capability density exhibit lower structural redundancy and reach their individual phase transition points at lower compression ratios, providing the first pre-compression mechanism for component-level phase transition prediction. Experiments on GPT-2 Medium confirm that capability density is statistically independent of Wanda importance scores (Spearman rho = -0.054, n = 384 heads), establishing it as a genuinely novel compression signal orthogonal to all existing importance metrics. We report a negative result on PPL-based compression comparison and provide a principled diagnosis identifying GPT-2 Medium as an insufficient test bed for the full CGC hypothesis. The theoretical framework, density formalism, and orthogonality finding constitute a foundation for capability-aware compression research.

---

## 论文详细总结（自动生成）

### 论文总结：能力引导的压缩 (Capability-Guided Compression, CGC)

#### 1. 核心问题与整体含义
论文针对大语言模型（LLM）压缩中的**“能力盲视压缩” (Capability-blind compression)** 问题展开研究。现有的压缩技术（如剪枝、量化）主要依赖权重大小或激活方差等统计指标来分配压缩预算，但这些指标无法反映组件（如注意力头）具体编码的功能（如推理、事实检索或语法解析）。这导致了两个后果：一是基于困惑度（PPL）的评估无法捕捉推理能力的丧失；二是模型在达到临界压缩阈值时会出现性能骤降的“相变”现象。本文旨在通过可解释性工具，将组件的功能重要性引入压缩决策过程。

#### 2. 方法论
核心思想是利用**稀疏自编码器 (SAE)** 提取的特征信息来定义**“能力密度” (Capability Density)**，并据此进行差异化的预算分配。
*   **能力密度 ($\delta^{(c)}$) 的定义**：由三个子维度构成的加权几何平均值：
    *   **特征广度 ($\beta^{(c)}$)**：SAE 字典中在校准数据上激活的特征数量。
    *   **特征多样性 ($H^{(c)}$)**：特征激活频率分布的香农熵。
    *   **跨输入一致性 ($\Psi^{(c)}$)**：不同语义类别输入下激活特征集的 Jaccard 相似度。
*   **核心算法 (CGC)**：
    *   **阶段 1 (CGC-L)**：根据能力密度图初始化各组件的保留比例，密度越高，保留越多。
    *   **阶段 2 (CGC-F)**：在进化搜索（基于 EvoPress）中加入“能力保护约束”，限制高密度组件的压缩上限，防止其越过各自的相变点。
*   **理论支撑**：论文证明了高能力密度的组件结构冗余较低，会更早达到性能崩溃的相变点，因此需要优先保护。

#### 3. 实验设计
*   **模型与数据集**：使用 **GPT-2 Medium (355M)** 作为实验模型；使用 **WikiText-103** 进行 SAE 训练、校准和困惑度（PPL）评估。
*   **Benchmark 与对比方法**：
    *   **Uniform**：均匀剪枝（基准）。
    *   **CGC-L**：本文提出的密度比例分配法。
    *   **Inverted**：反向分配（故意压缩高密度组件，作为负面参考）。
    *   **Wanda**：作为现有重要性指标的对比，验证信号的正交性。
*   **评估指标**：PPL、各组件消融后的 PPL 变化、Spearman 秩相关系数。

#### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA T4 GPU**（消费级算力）。
*   **训练细节**：为 GPT-2 Medium 的 384 个注意力头各训练了一个独立的 TopK-SAE。
*   **训练量**：每个 SAE 在 65,536 个 token 上训练了 5 个 epoch。论文指出这种训练规模相对较小（浅层训练）。

#### 5. 实验数量与充分性
*   **实验规模**：针对 384 个注意力头进行了详尽的密度映射、重要性相关性分析和消融实验。
*   **充分性评价**：
    *   **正交性分析充分**：成功证明了能力密度是一个与现有指标（如 Wanda）完全独立的新信号。
    *   **性能验证不足**：在 GPT-2 Medium 上的 PPL 实验结果为负面（CGC 表现未超过均匀剪枝）。作者对此进行了诚实的披露和深入的原则性诊断，认为 GPT-2 模型规模太小、功能分布太均匀，且 PPL 指标对能力损失不敏感。

#### 6. 主要结论与发现
*   **信号正交性**：能力密度与 Wanda 重要性得分的相关性极低 ($\rho = -0.054$)，证明它捕捉到了现有压缩指标忽略的维度。
*   **相变预测**：理论上建立了能力密度与组件级相变点的联系，为预压缩阶段的风险预测提供了工具。
*   **负面结果诊断**：在小模型上，由于组件功能高度冗余且缺乏复杂的推理电路，差异化分配预算的优势无法通过 PPL 体现。

#### 7. 优点
*   **理论创新**：首次将机械可解释性（SAE）与模型压缩预算分配直接挂钩，提出了“能力密度”这一形式化度量。
*   **科学诚实**：论文详尽报告并分析了负面实验结果，没有通过筛选数据来掩盖方法在小模型上的局限性。
*   **前瞻性**：为解决压缩中的“相变”和“推理能力丢失”问题提供了极具潜力的研究方向。

#### 8. 不足与局限
*   **实验模型过小**：GPT-2 Medium 无法代表现代大模型（如 Llama 3）的复杂功能结构，导致核心假设未能在性能指标上得到充分证实。
*   **评估指标局限**：依赖 PPL 而非推理基准测试（如 GSM8K），这与其“保护推理能力”的初衷存在脱节（受限于模型规模）。
*   **SAE 训练深度**：由于算力限制，SAE 的训练量远低于工业界标准，可能导致提取的特征不够纯净。
*   **应用范围**：目前主要针对结构化剪枝，尚未扩展到量化或 KV 缓存压缩等领域。

（完）
