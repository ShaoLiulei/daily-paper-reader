---
title: Privacy-Preserving Reinforcement Learning from Human Feedback via Decoupled Reward Modeling
title_zh: 通过解耦奖励建模实现隐私保护的基于人类反馈的强化学习
authors: "Young Hyun Cho, Will Wei Sun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22563v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 保护隐私的基于人类反馈的强化学习
tldr: 针对大语言模型RLHF微调阶段的隐私泄露风险，本文提出了一种解耦式差分隐私框架。该方法仅在奖励建模阶段施加差分隐私约束，并据此导出最终策略。研究不仅在理论上分析了隐私带来的次优性差距和极小极大下界，还在Gemma-2B-IT模型上通过实验证明，该方法在多种隐私预算下均优于现有基准，实现了更高效的隐私保护对齐。
selection_source: fresh_fetch
motivation: 解决RLHF过程中人类偏好数据可能包含敏感信息的问题，探索如何设计适配RLHF结构的差分隐私保护方案。
method: 提出一种将奖励建模与策略优化解耦的框架，仅在奖励学习阶段实施差分隐私，再利用私有奖励模型指导策略生成。
result: 理论推导出了隐私导致的额外误差项，且实验显示该方法在Anthropic HH-RLHF数据集上的表现优于传统DP基准方法。
conclusion: 通过解耦奖励建模施加隐私保护，可以在保证模型对齐质量的同时，为RLHF提供更强的隐私安全保障。
---

## 摘要
基于偏好的微调已成为训练大语言模型的重要组成部分，而这一阶段使用的数据可能包含敏感的用户信息。一个核心问题是如何设计一个非常适合基于人类反馈的强化学习（RLHF）独特结构的差分隐私流水线。我们提出了一个隐私保护框架，该框架仅在奖励学习阶段施加差分隐私，并从生成的私有奖励模型中推导出最终策略。在理论上，我们研究了次优性间隙（suboptimality gap），并证明了隐私保护在通常的非私有统计误差之外贡献了一个额外的加性项。我们还建立了极小极大下界，并表明主导项随样本量和隐私水平而变化，进而刻画了上界在对数因子意义下达到率最优（rate-optimal）的情形。在实证方面，合成实验证实了理论预测的缩放规律；在 Anthropic HH-RLHF 数据集上使用 Gemma-2B-IT 模型的实验表明，在各种隐私预算下，该方法比现有的差分隐私基准方法具有更强的私有对齐性能。

## Abstract
Preference-based fine-tuning has become an important component in training large language models, and the data used at this stage may contain sensitive user information. A central question is how to design a differentially private pipeline that is well suited to the distinct structure of reinforcement learning from human feedback. We propose a privacy-preserving framework that imposes differential privacy only on reward learning and derives the final policy from the resulting private reward model. Theoretically, we study the suboptimality gap and show that privacy contributes an additional additive term beyond the usual non-private statistical error. We also establish a minimax lower bound and show that the dominant term changes with sample size and privacy level, which in turn characterizes regimes in which the upper bound is rate-optimal up to logarithmic factors. Empirically, synthetic experiments confirm the scaling predicted by the theory, and experiments on the Anthropic HH-RLHF dataset using the Gemma-2B-IT model show stronger private alignment performance than existing differentially private baseline methods across privacy budgets.

---

## 论文详细总结（自动生成）

以下是对论文《Privacy-Preserving Reinforcement Learning from Human Feedback via Decoupled Reward Modeling》的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**大语言模型（LLM）对齐过程中的隐私保护问题**。在基于人类反馈的强化学习（RLHF）中，训练数据包含“提示词（Prompt）- 回答对 - 偏好标签”的元组。这些数据（尤其是Prompt）往往包含用户的敏感信息（如医疗、财务记录）。
*   **研究动机**：现有的差分隐私（DP）对齐方法（如DP-DPO或DP-PPO）通常直接在策略参数上施加隐私保护，这会导致梯度极不稳定、裁剪偏置（Clipping Bias）大，从而严重损害模型性能。
*   **核心目标**：设计一种既能提供元组级（Tuple-level）隐私保护，又能保持模型对齐效能的RLHF流水线。

### 2. 方法论：核心思想与技术细节
论文提出了一种**解耦式奖励建模（Decoupled Reward Modeling）框架**。
*   **核心思想**：将隐私预算全部投入到“奖励模型（RM）”的学习中，而将后续的“策略优化”视为对私有奖励模型的“后处理（Post-processing）”。根据DP的性质，对私有输出的后处理不会消耗额外的隐私预算。
*   **算法流程**：
    1.  **私有奖励学习**：在冻结的预训练模型特征上，使用DP-SGD训练一个线性奖励头。由于奖励建模的损失函数（如Bradley-Terry模型）通常比策略梯度更稳定，因此对隐私噪声的鲁棒性更强。
    2.  **策略导出（推理阶段）**：
        *   **精确推理**：若动作空间较小，直接根据私有奖励导出Gibbs分布策略。
        *   **Best-of-N (BoN) 采样**：针对LLM的大动作空间，从参考模型 $\pi_0$ 中采样 $N$ 个候选回答，利用私有奖励模型选出得分最高的一个。
*   **关键公式**：论文推导了次优性间隙（Suboptimality Gap）的上界，证明其由“非私有统计误差”和“隐私诱导误差”两部分加性组成，并给出了极小极大下界（Minimax Lower Bound）证明其在特定条件下的率最优性（Rate-optimality）。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据**：用于验证理论推导的缩放规律（样本量 $n$、维度 $d$、隐私预算 $\epsilon$ 的关系）。
    *   **真实LLM对齐**：使用 **Anthropic HH-RLHF** 数据集（包含4万条对话）。
*   **Benchmark与对比方法**：
    *   **DP-DPO**：直接在直接偏好优化目标上应用DP-SGD。
    *   **DP-RLHF**：将数据平分，一半用于私有奖励建模，另一半用于私有策略更新（类似PPO的离线变体）。
*   **评估模型**：以 `Gemma-2B-IT` 作为基础参考模型。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA A100 GPU**。
*   **软件实现**：利用 `Opacus` 库进行差分隐私会计（Privacy Accounting）。
*   **训练细节**：逻辑 Batch Size 为 64，物理 Microbatch 为 8（通过梯度累积实现），训练轮数为 2 轮。

### 5. 实验数量与充分性
*   **实验规模**：
    *   合成实验针对不同维度 $d \in \{3, 5, 7, 9\}$ 和隐私预算 $\epsilon$ 进行了 30 次独立重复实验，提供了 95% 置信区间。
    *   LLM实验针对 $\epsilon \in \{0.5, 1.0, 2.0\}$ 进行了 3 个随机种子的测试。
*   **充分性评价**：实验设计较为充分且客观。不仅对比了最终性能，还进行了消融分析（如 $\eta$ 参数的影响）、裁剪阈值敏感性分析，并提供了定性的案例展示（Best-of-N 随 $N$ 增加的效果变化），能够有力支撑论文结论。

### 6. 主要结论与发现
*   **性能优越性**：在相同的隐私预算下，该框架显著优于 DP-DPO 和 DP-RLHF。例如在 $\epsilon=0.5$ 的严格隐私下，该方法的奖励准确率接近 60%，而基准方法仅略高于随机猜测（约 52%）。
*   **稳定性**：解耦框架避免了在策略参数上直接加噪导致的“性能崩溃”，特别是在强正则化（小 $\eta$）场景下表现更稳健。
*   **理论验证**：实验结果完美契合理论预测的 $1/n$（统计）和 $1/(n^2\epsilon^2)$（隐私）的误差缩放趋势。

### 7. 优点
*   **架构巧妙**：利用 RLHF 的两阶段结构，将隐私保护压力从复杂的策略空间转移到简单的奖励空间。
*   **理论扎实**：提供了详尽的收敛性分析和极小极大下界证明，填补了私有 RLHF 理论研究的空白。
*   **实用性强**：BoN 采样不需要对策略进行昂贵的私有微调，降低了部署门槛。

### 8. 不足与局限
*   **表示能力受限**：为了保证隐私训练的稳定性，实验中仅训练了线性奖励头，这可能限制了模型捕捉复杂偏好的上限（依赖于 Backbone 的特征质量）。
*   **推理开销**：Best-of-N 策略在推理时需要多次采样，虽然不消耗隐私预算，但增加了计算延迟和成本。
*   **覆盖性假设**：理论分析依赖于参考策略对目标策略的“覆盖性假设”，在策略漂移过大时，理论保证可能失效。

（完）
