---
title: "VLGOR: Visual-Language Knowledge Guided Offline Reinforcement Learning for Generalizable Agents"
title_zh: VLGOR：面向可泛化智能体的视觉-语言知识引导的离线强化学习
authors: "Pengsen Liu, Maosen Zeng, Nan Tang, Kaiyuan Li, Jing-Cheng Pang, Yunan Liu, Yang Yu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22892v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 将大语言模型与强化学习结合以增强智能体泛化能力
tldr: "针对大语言模型在强化学习中缺乏物理环境感知、泛化能力受限的问题，本文提出VLGOR框架。该框架通过微调视觉语言模型（VLM），结合初始视觉观察和高层指令生成具有时空一致性的虚构交互轨迹，并利用反事实提示增强数据多样性，从而在离线强化学习中提升智能体对新任务的泛化能力。实验表明，VLGOR在机器人操作基准测试中显著优于基线，新任务成功率提升超过24%。"
selection_source: fresh_fetch
motivation: 现有的结合LLM的强化学习智能体因缺乏对物理环境的直接感知，难以理解环境动力学并泛化到未见过的任务。
method: 提出VLGOR框架，通过微调视觉语言模型生成受指令引导的虚构轨迹，并利用反事实提示增加离线训练数据的多样性。
result: "在机器人操作基准测试中，VLGOR在需要新策略的未见任务上表现优异，成功率比基线方法高出24%以上。"
conclusion: 整合视觉与语言知识生成的虚构数据能有效增强离线强化学习智能体的环境感知与泛化能力。
---

## 摘要
将大语言模型（LLMs）与强化学习（RL）相结合，使智能体能够更有效地理解语言指令以执行任务。然而，LLMs 通常缺乏对物理环境的直接感知，这限制了它们对环境动力学的理解以及对未知任务的泛化能力。为了解决这一局限性，我们提出了视觉-语言知识引导的离线强化学习（VLGOR），这是一个整合视觉和语言知识以生成想象轨迹（imaginary rollouts）的框架，从而丰富了交互数据。VLGOR 的核心前提是微调一个视觉-语言模型，以初始视觉观察和高层指令为条件预测未来的状态和动作，确保生成的轨迹在时间上连贯且在空间上合理。此外，我们采用反事实提示（counterfactual prompts）为离线强化学习训练生成更多样化的轨迹，使智能体能够获取有助于遵循语言指令的知识，同时基于视觉线索在环境中实现具身落地（grounding）。在机器人操纵基准测试上的实验表明，VLGOR 显著提高了需要新型最优策略的未知任务的性能，其成功率比基准方法高出 24% 以上。

## Abstract
Combining Large Language Models (LLMs) with Reinforcement Learning (RL) enables agents to interpret language instructions more effectively for task execution. However, LLMs typically lack direct perception of the physical environment, which limits their understanding of environmental dynamics and their ability to generalize to unseen tasks. To address this limitation, we propose Visual-Language Knowledge-Guided Offline Reinforcement Learning (VLGOR), a framework that integrates visual and language knowledge to generate imaginary rollouts, thereby enriching the interaction data. The core premise of VLGOR is to fine-tune a vision-language model to predict future states and actions conditioned on an initial visual observation and high-level instructions, ensuring that the generated rollouts remain temporally coherent and spatially plausible. Furthermore, we employ counterfactual prompts to produce more diverse rollouts for offline RL training, enabling the agent to acquire knowledge that facilitates following language instructions while grounding in environments based on visual cues. Experiments on robotic manipulation benchmarks demonstrate that VLGOR significantly improves performance on unseen tasks requiring novel optimal policies, achieving a success rate over 24% higher than the baseline methods.

---

## 论文详细总结（自动生成）

### 论文总结：VLGOR 视觉-语言知识引导的离线强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
开发能够执行多样化操纵任务的通用智能体是人工智能的核心目标。虽然离线强化学习（Offline RL）能从历史数据中学习，但受限于数据集的覆盖范围，智能体在面对未见过的任务（Unseen Tasks）时泛化能力较差。
*   **研究动机**：现有的方法尝试引入大语言模型（LLM）的语义知识，但 LLM 缺乏对物理环境的直接感知，生成的“想象轨迹”往往违反物理定律（如物体穿透、运动不合理）。
*   **核心问题**：如何结合视觉感知与语言理解，生成既符合语义指令又符合物理动力学约束的虚构交互数据，以增强离线强化学习智能体的泛化性能。

#### 2. 方法论
VLGOR 框架分为两个主要阶段：**轨迹生成（Rollout Generation）**和**技能获取（Skill Acquisition）**。
*   **核心思想**：利用微调后的视觉-语言模型（VLM）作为“世界模型”，根据初始视觉观察和高层指令生成高质量的虚构轨迹，从而扩充离线训练集。
*   **关键技术细节**：
    *   **VLM 微调与环境落地**：采用 BAGEL 模型作为骨干，通过 LoRA 适配器进行微调。模型输入初始图像 $s_0$ 和指令 $G$，预测未来的视觉状态 $\hat{s}_h$ 和动作 $\hat{a}_h$。
    *   **反事实提示（Counterfactual Prompts）**：为了增加数据多样性，将指令分解为背景 $t$、目标物体 $m$ 和行为模式 $b$。通过修改 $m$（反事实目标）或 $b$（反事实行为），生成原始数据中不存在的新任务轨迹。
    *   **离线强化学习**：将原始真实数据与 VLM 生成的虚构数据合并，使用行为克隆（BC）或保守 Q 学习（CQL）等算法训练目标条件策略 $\pi(a|s, G)$。

#### 3. 实验设计
*   **实验场景/数据集**：
    *   **CLEVR-Robot**：智能体操纵 5 个移动球体达到目标配置。
    *   **Meta-World**：使用 Sawyer 机械臂执行开门、拨动开关、拿起放置等任务。
*   **任务难度分级**：
    1.  **指令改写（Rephrasing）**：语义相同但表达不同。
    2.  **反事实任务（Counterfactual）**：修改了目标或行为的新任务。
    3.  **困难任务（Hard）**：目标与行为的复杂组合，与训练集差异巨大。
*   **对比方法（Benchmarks）**：
    *   基础算法：BC, CQL。
    *   增强方法：KALM（仅基于语言的轨迹生成）、COMBO（基于模型的离线 RL）、TD3、PRDC。

#### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA H800 80GB PCIe GPU**。
*   **训练时长**：VLM 微调阶段训练 20 个 epoch（Batch size 16）；离线 RL 阶段训练 50 个 epoch（使用 Adam 优化器）。
*   **数据规模**：离线数据集包含 10 万个轨迹对，VLM 微调使用了其中 1 万个样本。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流机器人基准测试上进行了评估，涵盖了三种难度的未见任务。
*   **消融实验**：针对 VLM 微调样本量、不同类型的反事实提示（仅目标、仅行为、两者结合）进行了详细的消融研究。
*   **客观性与公平性**：所有实验均使用 **3 个随机种子**取平均值，并提供了标准差（误差棒）。在对比实验中，保持了训练数据总量的恒定，确保了对比的公平性。

#### 6 主要结论与发现
*   **泛化性能显著提升**：VLGOR 在所有未见任务上均优于基线。在最困难的任务中，成功率比基线方法高出 **24% 以上**。
*   **物理一致性**：相比于仅依赖语言的 KALM，VLGOR 生成的轨迹在空间位置和物体交互上更真实，有效避免了物体重叠等物理错误。
*   **反事实数据的价值**：通过反事实提示生成的轨迹能显著增强智能体处理新目标和新行为的能力。

#### 7. 优点
*   **物理落地（Grounding）**：成功将视觉感知引入轨迹生成，解决了纯语言模型无法理解空间动力学的问题。
*   **数据增强效率高**：通过反事实提示，仅需少量真实数据即可合成大量高质量、多样化的训练样本。
*   **算法兼容性**：VLGOR 作为一个数据增强框架，可以与多种离线 RL 算法（BC, CQL, TD3 等）无缝结合。

#### 8. 不足与局限
*   **场景复杂度有限**：实验目前集中在相对简单的模拟机器人环境（CLEVR, Meta-World），在真实世界或更复杂的开放场景中的表现尚待验证。
*   **计算负担**：VLM 同时生成状态和动作，学习压力较大。论文提到未来可能需要采用“先预测状态、再推断动作”的渐进式策略。
*   **误差累积**：虽然比纯语言模型好，但长程轨迹生成仍可能存在微小的物理偏差，影响极高精度要求的控制任务。

（完）
