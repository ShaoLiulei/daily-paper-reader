---
title: Generative Artificial Intelligence Assisted Multi-modal Semantic Extraction for NOMA-based Image Transmissions
title_zh: 生成式人工智能辅助的面向NOMA图像传输的多模态语义提取
authors: "Songhan Zhao, Shimin Gong, Bo Gu, Hongyang Du, Xidong Mu, Zehui Xiong, Yuming Fang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21092v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于多模态语义提取和图像恢复的生成式人工智能模型
tldr: 本文研究了生成式人工智能（GAI）辅助的NOMA图像语义通信框架。通过跨模态语义特征提取与GAI图像恢复，结合NOMA技术优化传输效率。提出了一种重要性感知且模型驱动的近端策略优化（IM-PPO）框架，协同优化语义特征选择、波束成形及解码顺序，在降低传输延迟的同时显著提升了图像恢复精度。
selection_source: fresh_fetch
motivation: 旨在解决NOMA图像传输中语义提取与传输控制协同优化的问题，以平衡传输延迟与语义恢复精度。
method: 提出IM-PPO框架，通过量化保留高重要性语义特征并结合模型驱动方法优化传输控制变量。
result: 实验结果表明，联合调整语义特征选择与传输控制能显著提升图像恢复质量并降低传输延迟。
conclusion: 该框架通过有效利用模型信息和重要性感知机制，显著提高了语义通信系统的学习效率和整体性能。
---

## 摘要
本文研究了一种用于基于非正交多址接入（NOMA）图像传输的生成式人工智能（GAI）辅助语义通信框架。语义用户（SU）从原始图像中提取跨模态语义特征，随后利用GAI模型进行图像恢复。GAI增强了语义图像传输的泛化能力和恢复效果，而NOMA则根据业务需求为语义用户高效分配传输容量。因此，语义提取与传输控制共同影响语义恢复性能和传输开销。我们通过联合优化语义层的语义特征选择，以及传输层的接收波束赋形和NOMA解码顺序，最大化传输延迟与语义恢复准确度的加权性能。为了减少语义特征中潜在的冗余并提高优化效率，我们开发了一种重要性感知且模型驱动的近端策略优化（IM-PPO）框架。具体而言，我们量化并保留高重要性的语义特征以增强PPO的学习效率，同时利用基于模型的优化方法来调整传输控制变量。数值结果验证了语义特征选择与传输控制的联合调整显著提高了语义恢复准确度和传输延迟性能。此外，与基准方法相比，IM-PPO框架有效地利用了模型信息，提高了学习效率。

## Abstract
In this paper, we investigate a generative artificial intelligence (GAI)-assisted semantic communication framework for non-orthogonal multiple access (NOMA)-based image transmissions. Semantic users (SUs) extract cross-modal semantic features from the raw images, which are then used for image recovery by leveraging a GAI model. The GAI enhances the generalization and recovery of semantic image transmissions, while NOMA efficiently allocates transmission capacities to SUs based on their traffic demands. Thus, the semantic extraction and transmission control jointly affect both semantic recovery performance and transmission overhead. We maximize a weighted performance of transmission latency and semantic recovery accuracy by jointly optimizing the semantic feature selection at the semantic level, as well as the receive beamforming and NOMA decoding order at the transmission level. To reduce potential redundancy in semantic features and improve optimization efficiency, we develop an importance-aware and model-driven proximal policy optimization (IM-PPO) framework. Specifically, we quantify and retain high-importance semantic features to enhance the learning efficiency of PPO, while model-based optimization methods are used to adapt the transmission control variables. Numerical results validate that the joint adjustment of the semantic feature selection and the transmission control significantly improves the semantic recovery accuracy and the transmission latency performance. Moreover, the IM-PPO framework effectively leverages the model information to improve the learning efficiency compared to benchmark methods.

---

## 论文详细总结（自动生成）

以下是对论文《Generative Artificial Intelligence Assisted Multi-modal Semantic Extraction for NOMA-based Image Transmissions》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在物联网（IoT）环境下，针对多用户图像传输任务，平衡**语义恢复精度**与**传输延迟**之间的矛盾。
*   **研究动机**：
    *   传统的图像传输面临频谱稀缺和高延迟挑战。
    *   语义通信（SC）通过提取关键信息而非原始像素来降低流量，但现有模型泛化性差且难以适应动态网络。
    *   生成式人工智能（GAI，如Stable Diffusion）在图像恢复上表现卓越，但其“黑盒”特性使得与物理层传输控制（如NOMA）的联合优化变得极其困难。
    *   NOMA技术虽然能提高多用户接入效率，但其性能高度依赖于用户流量需求，而语义提取会动态改变这些需求，导致两者深度耦合。

### 2. 方法论
论文提出了一种**联合传输与语义控制（JTSC）方案**，并开发了**IM-PPO（重要性感知且模型驱动的近端策略优化）框架**：
*   **跨模态语义提取**：利用 BLIP 模型提取文本特征（高层意图），利用 UperNet 提取视觉分割图（底层结构），形成互补的语义表示。
*   **重要性感知剪枝**：
    *   利用 **Grad-CAM** 技术计算文本特征对原始图像区域的贡献度。
    *   构建**依赖矩阵**和**贡献矩阵**，量化语义特征的重要性。
    *   通过预设阈值过滤低重要性特征，显著缩小 PPO 算法的动作搜索空间。
*   **IM-PPO 框架（混合驱动）**：
    *   **模型驱动部分（传输控制）**：针对具有明确物理模型的变量（接收波束赋形、NOMA 解码顺序），采用广义特征值分解和二次变换（Quadratic Transform）等数学优化方法求解。
    *   **模型免驱动部分（语义选择）**：针对黑盒化的语义恢复过程，利用 PPO 强化学习算法从剪枝后的特征空间中选择最优特征子集。
*   **优化目标**：最小化感知相似度度量（LPIPS）与传输延迟的加权和。

### 3. 实验设计
*   **数据集**：使用计算机视觉领域的标准数据集 **COCO-2017**。
*   **核心模型**：采用 **Stable Diffusion v1-5** 作为接收端的 GAI 解码器。
*   **基准方法（Benchmarks）**：
    *   **算法对比**：M-PPO（无剪枝的模型驱动PPO）、Plain-PPO（全模型免驱动PPO）。
    *   **策略对比**：ALL-Selection（传输所有特征）、Random-Selection（随机选择特征）、Location-Decoding（基于距离的固定NOMA解码顺序）。
*   **评估指标**：LPIPS（感知损失，越小表示恢复质量越高）、传输延迟（Latency）、奖励收敛曲线。

### 4. 资源与算力
*   **文中说明**：论文提到了软件参数设置（学习率 $1 \times 10^{-4}$，Stable Diffusion v1-5 权重，NOMA 带宽 2MHz 等）。
*   **算力细节**：**文中未明确说明**具体的 GPU 型号、数量及训练总时长。仅从算法复杂度分析（$O$ 阶）角度讨论了计算开销。

### 5. 实验数量与充分性
*   **实验规模**：
    *   展示了算法的收敛性实验。
    *   进行了不同带宽（2-20 MHz）、不同用户数量（3-9个）、不同发射功率下的性能对比。
    *   提供了图像恢复效果的视觉对比图。
*   **充分性与客观性**：实验覆盖了通信环境的主要变量（带宽、功率、用户数），并进行了消融实验（对比 M-PPO 和 Plain-PPO），证明了“重要性剪枝”和“模型驱动”两个核心模块的有效性。对比基准涵盖了当前主流的简单策略，实验设计较为客观。

### 6. 主要结论与发现
*   **联合优化的必要性**：单纯增加语义特征虽能提升精度，但会导致 NOMA 系统延迟激增；JTSC 方案能找到最佳平衡点。
*   **IM-PPO 的优越性**：相比于纯强化学习（Plain-PPO），引入物理模型信息和语义剪枝可使收敛速度提升约 2-3 倍，且能避免陷入局部最优。
*   **语义冗余发现**：实验观察到，传输过多的语义特征有时反而会干扰 GAI 的生成过程（产生伪影），而精简的高重要性特征能产生更准确的恢复结果。

### 7. 优点
*   **架构创新**：将“黑盒”AI 优化与“白盒”通信理论（NOMA/波束赋形）有机结合，解决了复杂耦合问题的求解难题。
*   **剪枝机制科学**：利用 Grad-CAM 量化跨模态特征重要性，不仅提高了学习效率，还符合人类对图像语义的直观理解。
*   **实用性强**：方案考虑了动态网络环境，能够根据实时信道状态自适应调整语义提取粒度。

### 8. 不足与局限
*   **算力开销**：虽然传输延迟降低了，但在接收端运行 Stable Diffusion 等大模型进行图像恢复需要极高的计算资源，论文未深入探讨接收端的计算延迟。
*   **阈值敏感性**：重要性剪枝依赖于预设阈值（$\xi_t, \xi_v$），这些参数在不同场景下的通用性有待进一步验证。
*   **实时性挑战**：PPO 虽已收敛，但在高度瞬变的无线信道中，频繁的推理和优化可能带来额外的处理时延。

（完）
