---
title: "IPatch: A Multi-Resolution Transformer Architecture for Robust Time-Series Forecasting"
title_zh: IPatch：一种用于鲁棒时间序列预测的多分辨率 Transformer 架构
authors: "Aymane Harkati, Moncef Garouani, Olivier Teste, Julien Aligon, Mohamed Hamlich"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24207v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于时间序列预测的多分辨率Transformer架构
tldr: 针对多元时间序列预测中长短期依赖捕捉的难题，本文提出IPatch架构。该模型结合了点级（point-wise）和块级（patch-wise）表示，通过多分辨率Transformer同时建模细粒度细节与宏观上下文。在7个基准数据集上的实验证明，IPatch在预测精度、抗噪性和泛化能力上均优于单一表示的基准模型。
selection_source: fresh_fetch
motivation: 传统点级表示计算开销大且缺乏上下文，而块级表示虽高效却易丢失对复杂序列至关重要的细粒度细节。
method: 提出一种多分辨率Transformer架构，通过整合点级和块级Token来同时捕捉不同时间尺度的特征。
result: 在7个基准数据集上的实验结果显示，IPatch在多种预测周期下均显著提升了预测准确性和鲁棒性。
conclusion: 结合多分辨率表示是提升Transformer在时间序列预测任务中性能和泛化能力的有效途径。
---

## 摘要
由于需要同时捕捉短期波动和长程时间依赖关系，多元时间序列的准确预测仍然具有挑战性。基于 Transformer 的模型已成为一种强大的方法，但其性能在很大程度上取决于时间数据的表示。传统的逐点（point-wise）表示保留了单个时间步的信息，从而实现了细粒度建模，但其计算成本往往较高，且在建模更广泛的上下文依赖关系方面效果较差，限制了其在长序列上的可扩展性。逐块（patch-wise）表示将连续的时间步聚合为紧凑的 token，以提高效率并建模局部时间动态，但它们往往会丢弃细粒度的时间细节，而这些细节对于波动剧烈或复杂的时间序列的准确预测至关重要。我们提出了 IPatch，这是一种多分辨率 Transformer 架构，它集成了逐点和逐块 token，在多个分辨率上对时间信息进行建模。在 7 个基准数据集上的实验表明，与单一表示的基准模型相比，IPatch 在预测准确性、噪声鲁棒性以及跨各种预测周期的泛化能力方面均有一致的提升。

## Abstract
Accurate forecasting of multivariate time series remains challenging due to the need to capture both short-term fluctuations and long-range temporal dependencies. Transformer-based models have emerged as a powerful approach, but their performance depends critically on the representation of temporal data. Traditional point-wise representations preserve individual time-step information, enabling fine-grained modeling, yet they tend to be computationally expensive and less effective at modeling broader contextual dependencies, limiting their scalability to long sequences. Patch-wise representations aggregate consecutive steps into compact tokens to improve efficiency and model local temporal dynamics, but they often discard fine-grained temporal details that are critical for accurate predictions in volatile or complex time series. We propose IPatch, a multi-resolution Transformer architecture that integrates both point-wise and patch-wise tokens, modeling temporal information at multiple resolutions. Experiments on 7 benchmark datasets demonstrate that IPatch consistently improves forecasting accuracy, robustness to noise, and generalization across various prediction horizons compared to single-representation baselines.

---

## 论文详细总结（自动生成）

### IPatch：一种用于鲁棒时间序列预测的多分辨率 Transformer 架构论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对多元时间序列预测中**数据表示方式的权衡问题**展开研究。
*   **背景**：Transformer 在长程依赖建模上表现出色，但其性能受输入表示影响极大。
*   **核心矛盾**：
    *   **逐点表示（Point-wise）**：保留了细粒度细节，适合处理高波动数据，但计算开销大且难以捕捉宏观上下文。
    *   **逐块表示（Patch-wise）**：通过聚合连续时间步提高效率并捕捉局部上下文，但会不可避免地丢失细粒度的时间细节，在处理复杂或噪声数据时精度受损。
*   **研究动机**：作者发现简单的“重叠分块（Overlapping Patches）”策略对性能提升有限。因此，需要一种能够同时整合全局上下文和局部细粒度特征的统一架构。

#### 2. 方法论：核心思想与关键技术
IPatch 提出了一种**双流（Dual-Stream）多分辨率架构**，其核心流程如下：
*   **输入分块与编码**：将输入序列划分为均匀的块（Patches），并加入位置编码以保留时间顺序。
*   **双流并行处理**：
    1.  **块级注意力流（Patch-based Attention）**：使用标准 Transformer 编码器计算块与块之间的自注意力，旨在捕捉宏观的、长距离的时间依赖关系。
    2.  **块内逐点自相关流（Patch's Point-wise Autocorrelation）**：针对每个块内部，利用快速傅里叶变换（FFT）在频域分析周期性模式。
*   **关键技术细节**：
    *   **Fourier-KAN (FKAN)**：在自相关模块中，作者放弃了传统的 Softmax 激活，转而使用基于傅里叶基函数的 Kolmogorov-Arnold 网络（KAN），以增强对正负相关性和周期性的自适应控制。
    *   **特征融合**：将两个流的输出进行拼接（Concatenate），通过线性层投影至预测维度。

#### 3. 实验设计
*   **数据集**：涵盖 7 个主流基准数据集：ETT 系列（ETTh1, ETTh2, ETTm1, ETTm2）、Electricity（电力）、Weather（天气）和 ILI（流感样病例）。
*   **预测周期（Horizons）**：
    *   主流数据集：{96, 192, 336, 720}。
    *   ILI 数据集：{24, 36, 48, 60}。
*   **对比方法（Baselines）**：共 11 种 SOTA 模型，包括 PatchTST, TimeMixer, TimesNet, Crossformer, MICN, FiLM, DLinear, FEDformer, Stationary, Autoformer, Informer。
*   **评估指标**：均方误差（MSE）和平均绝对误差（MAE）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率分析**：文中通过图表展示了 IPatch 在 ILI 数据集上的训练时间（对数刻度）与 MSE 的平衡关系。结果表明，IPatch 的训练速度远快于 FEDformer 和 Crossformer 等复杂 Transformer 变体，且在精度上优于轻量级的 DLinear。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在 7 个数据集、4 种预测周期下进行了 28 组配置的对比。
    *   **消融实验**：对比了“仅分块”、“仅自相关”以及“不同激活函数（Softmax vs. Fourier）”的效果。
    *   **定性分析**：提供了多个数据集的预测曲线可视化对比。
*   **充分性评价**：实验设计非常充分。通过使用统一的 TFB 评测框架，确保了预处理、数据分割和超参数优化的公平性。涵盖了从高频工业数据到低频流行病数据的多种场景，验证了模型的普适性。

#### 6. 主要结论与发现
*   **性能领先**：IPatch 在 28 组实验中，MAE 指标有 26 组达到最优，MSE 指标有 21 组达到最优。
*   **互补性验证**：消融实验证明，分块机制和逐点自相关机制具有强互补性，结合后的模型在长短期预测中均表现更优。
*   **FKAN 的有效性**：使用 Fourier-KAN 替代 Softmax 激活，在长程预测（如 ETTh1 720步）中使 MSE 降低了约 21%。
*   **效率优势**：在保持高精度的同时，IPatch 维持了较低的计算延迟，实现了性能与效率的良好平衡。

#### 7. 优点与亮点
*   **多分辨率建模**：巧妙地解决了 Transformer 在时间序列中“精度”与“效率”的矛盾。
*   **引入 FKAN**：将最新的 KAN 网络架构与傅里叶变换结合，提升了模型对复杂周期性信号的表达能力。
*   **鲁棒性强**：在噪声较大、样本较少的 ILI 数据集上表现尤为突出，显示出极强的泛化能力。

#### 8. 不足与局限
*   **固定回溯窗口**：实验中统一使用 96 的回溯窗口，未深入探讨不同输入长度对多分辨率表示的影响。
*   **分块策略单一**：目前采用固定长度的分块，缺乏对数据自适应分块（如动态调整块大小）的探索。
*   **解释性待加强**：虽然模型性能优异，但对于“块级注意力”和“点级自相关”在不同类型数据中具体贡献的理论解释尚显薄弱。

（完）
