---
title: "Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition"
title_zh: 通过联邦学习优化多语言大语言模型：客户端语言构成的研究
authors: "Aleix Sant, Jordi Luque, Carlos Escolano"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24242v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型的多语言指令微调与联邦学习
tldr: 本研究探讨了在联邦学习环境下优化多语言大语言模型的策略。针对客户端语言分布不均和资源差异，扩展了FederatedScope-LLM框架，并提出局部动态早停机制（LDES-FL）以提升训练效率。通过实验分析了客户端语言组成对模型质量、公平性和成本的影响，发现增加客户端内的语言多样性有助于构建更强大且公平的全局模型，尤其对低资源语言提升显著。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习中多语言大模型因客户端语言分布异构和资源不均带来的训练效率与性能不平衡问题。
method: 扩展了FederatedScope-LLM框架以支持多语言指令微调，并引入了根据本地验证表现自动暂停或恢复训练的LDES-FL机制。
result: 实验表明，客户端内部语言越丰富，全局模型的性能和公平性越好，且低资源语言获益最大，但优化步骤也随之增加。
conclusion: 客户端语言组成是多语言联邦学习的核心设计变量，决定了模型在性能、公平性与训练效率之间的权衡。
---

## 摘要
多语言环境下大语言模型（LLMs）的联邦学习（FL）面临着重大挑战，这些挑战源于客户端之间异构的语言分布以及语言资源可用性的差异。为了应对这些挑战，我们扩展了 FederatedScope-LLM 框架，以支持 LLMs 的多语言指令微调实验。我们还引入了一种新型的客户端特定早停机制——局部动态早停（LDES-FL），该机制允许客户端根据客户端侧的验证性能暂停和恢复本地训练，从而提高训练效率和可持续性。通过一系列实验，我们研究了客户端语言构成（从完全单语言到日益多样化的多语言客户端）如何影响多语言质量、公平性和训练成本。单语言本地微调对于单语言专业化仍然最为有效，而联邦训练则更适合学习单一的平衡多语言模型。在联邦学习中，增加客户端内部的多语言性可以产生更强大、更公平的全局模型，缩小与集中式多语言微调的差距，并为低资源语言带来最大的收益，尽管这以更多的优化步骤为代价。总之，我们的研究结果表明，客户端语言构成是多语言联邦学习中的一个关键设计变量，它塑造了性能、公平性和效率。

## Abstract
Federated Learning (FL) of Large Language Models (LLMs) in multilingual environments presents significant challenges stemming from heterogeneous language distributions across clients and disparities in language resource availability. To address these challenges, we extended the FederatedScope-LLM framework to support multilingual instruction-tuning experiments with LLMs. We also introduced a novel client-specific early stopping mechanism, Local Dynamic Early Stopping (LDES-FL), which allows clients to pause and resume local training based on client-side validation performance, enhancing training efficiency and sustainability. Through a series of experiments, we studied how client language composition - from fully monolingual to increasingly multilingual clients - affects multilingual quality, fairness and training cost. Monolingual local fine-tuning remains the most effective for single-language specialization, whereas federated training is better suited to learning a single balanced multilingual model. In FL, increasing within-client multilinguality leads to stronger and fairer global models, narrows the gap to centralized multilingual fine-tuning, and yields the largest gains for lower-resource languages, albeit at the cost of more optimization steps. Overall, our results identify client language composition as a key design variable in multilingual FL, shaping performance, fairness and efficiency

---

## 论文详细总结（自动生成）

这是一份关于论文《Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在联邦学习（FL）框架下微调多语言大语言模型（LLMs）时，面临两大核心挑战：**数据异构性（Non-IID）**和**语言资源不均**。
*   **动机**：现实中，不同客户端拥有的语言数据分布极不均衡（例如，一个客户端可能只有一种语言），这会导致“客户端漂移（Client Drift）”，严重损害全局模型的收敛速度和泛化性能。
*   **核心问题**：客户端内部的语言构成（从纯单语言到混合多语言）如何影响联邦微调后模型的质量、多语言公平性以及训练成本？

### 2. 方法论
论文提出了两个主要贡献：
*   **多语言联邦框架扩展**：基于 `FederatedScope-LLM` 进行了扩展，支持多语言指令微调、灵活的 Prompt 集成以及语言感知的样本处理流水线。
*   **局部动态早停机制（LDES-FL）**：
    *   **核心思想**：允许客户端根据其私有验证集的损失值自主决定暂停或恢复训练。
    *   **流程**：客户端监控本地验证损失，若无提升则停止训练并向服务器发送当前最优模型；若后续下载的全局模型在本地验证集上表现有所提升，客户端会动态“复活”并重新参与训练。
    *   **技术手段**：采用 **LoRA**（低秩适配）进行参数高效微调，仅更新约 1%-2% 的参数，以降低通信和计算开销。

### 3. 实验设计
*   **模型**：使用 `salamandra-2b-instruct`（支持 35 种语言的 20 亿参数模型）。
*   **数据集**：多语言版本的 **Alpaca Cleaned**，涵盖 8 种语言：英语(en)、西班牙语(es)、德语(de)、加泰罗尼亚语(ca)、丹麦语(da)、塞尔维亚语(sr)、克罗地亚语(hr)和巴斯克语(eu)。
*   **对比场景（Benchmark）**：
    1.  **Base Model**：原始预训练模型。
    2.  **Local FT (Monolingual)**：每个客户端仅针对单一语言进行本地微调（不进行联邦）。
    3.  **Local FT (Multilingual)**：集中式微调，使用所有语言的汇总数据（作为性能上限）。
    4.  **FedAvg (不同语言构成)**：设置了 6 种异构级别，从 100% 单语言客户端到 15% 单语言/85% 多语言混合客户端。
*   **评估指标**：ROUGE-L 和 BERTScore，并以标准差（$\sigma$）衡量多语言公平性。

### 4. 资源与算力
*   **训练细节**：每个客户端每轮执行 160 个 mini-batch 步骤，微批次大小（micro-batch size）为 2，梯度累积 16 步，有效批次大小为 32。使用 OneBitAdam 优化器。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。但通过“归一化优化步骤”对比了成本，指出集中式多语言微调成本最高（1.83e5 步），而联邦学习根据配置不同，成本在集中式的 6% 到 41% 之间。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 种不同资源水平的语言（高、中、低资源），设计了 6 种不同的客户端语言组成比例，并对比了多种微调基准。
*   **充分性评价**：实验设计较为系统且客观。通过控制变量法（保持每个客户端总样本数一致）排除了样本量对结果的干扰，专注于“语言组成”这一变量。引入了统计显著性检验（Bootstrap test），增强了结论的说服力。

### 6. 主要结论与发现
*   **LDES-FL 的有效性**：相比标准联邦早停，LDES-FL 在保持性能的同时，减少了 22%-32% 的不必要计算。
*   **语言构成的权衡**：
    *   **单语言微调**最适合特定语言的专业化，但多语言泛化差。
    *   **联邦学习**中，客户端内部语言越多样（越接近 IID），全局模型的平均性能越好，多语言公平性越高。
*   **低资源语言获益最大**：增加客户端的多语言性对低资源语言（如巴斯克语、克罗地亚语）的提升最为显著（ROUGE-L 提升达 16.11%）。
*   **训练成本**：多语言程度越高，模型收敛所需的优化步骤越多（因为模型有更大的提升空间，不会过早陷入局部最优）。

### 7. 优点（亮点）
*   **机制创新**：LDES-FL 解决了联邦学习中客户端参与度的动态调整问题，兼顾了效率与性能。
*   **现实意义**：深入探讨了“语言异构性”这一实际部署中极具挑战的问题，为构建公平的多语言模型提供了指导。
*   **开源贡献**：扩展了 FederatedScope 框架并开源了代码和多语言数据集。

### 8. 不足与局限
*   **数据重叠风险**：实验使用的多语言 Alpaca 数据集并非严格的平行语料，不同客户端之间可能存在翻译后的内容重叠，这可能导致对“跨语言迁移”能力的评估存在偏差。
*   **模型规模限制**：实验仅在 2B 参数模型上进行，尚不清楚相关结论在更大规模（如 70B+）模型上是否完全成立。
*   **Prompt 依赖**：虽然翻译了 Prompt 模板，但 LLM 对不同语言 Prompt 的敏感度差异可能影响最终评分。

（完）
