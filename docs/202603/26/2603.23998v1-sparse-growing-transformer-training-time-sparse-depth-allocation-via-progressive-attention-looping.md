---
title: "Sparse Growing Transformer: Training-Time Sparse Depth Allocation via Progressive Attention Looping"
title_zh: 稀疏增长 Transformer：通过渐进式注意力循环实现训练时稀疏深度分配
authors: "Yao Chen, Yilong Chen, Yinqi Yang, Junyuan Shang, Zhenyu Zhang, Zefeng Zhang, Shuaiyi Nie, Shuohuan Wang, Yu Sun, Hua Wu, HaiFeng Wang, Tingwen Liu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23998v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: Transformer训练时的稀疏深度分配
tldr: "本研究针对Transformer在增加有效深度时存在的计算冗余问题，提出了稀疏增长Transformer（SGT）。SGT摒弃了传统的静态参数复用模式，转而采用一种从深层到浅层的渐进式结构增长策略。通过在具有高熵特征的注意力头上实施针对性的循环机制，SGT在训练过程中动态且稀疏地分配计算深度。实验证明，该方法在保持高性能的同时，将训练FLOPs的额外开销从16-20%显著降低至1-3%，为高效训练深层模型提供了新思路。"
selection_source: fresh_fetch
motivation: 传统的静态深度分配和均匀参数复用导致训练过程中存在严重的计算冗余，且未考虑层间成熟度的差异。
method: 引入SGT框架，通过在信息丰富的注意力头上实施从深层到浅层的渐进式注意力循环，实现训练时的稀疏深度分配。
result: "SGT在多个规模上均优于静态循环基准，并将相对于标准Transformer的训练FLOPs额外开销从16-20%降至1-3%。"
conclusion: 深度分配应是一个动态增长的结构化过程，通过针对性地增加关键参数的计算深度可显著提升训练效率和模型性能。
---

## 摘要
现有的增加 Transformer 有效深度的方法主要依赖于参数复用，通过递归执行来扩展计算。在这种范式下，网络结构在整个训练过程中保持静态，且额外的计算深度在参数层面被均匀地分配给整个区块。这种在训练时间和参数空间上的僵化导致了训练过程中大量的计算冗余。与此相反，我们认为训练期间的深度分配不应是静态预设，而应是一个渐进增长的结构化过程。我们的系统分析揭示了跨层级“由深到浅”的成熟轨迹，其中高熵注意力头在语义整合中起着至关重要的作用。受此启发，我们提出了稀疏增长 Transformer (SGT)。SGT 是一种训练时稀疏深度分配框架，它通过对信息丰富的注意力头进行针对性的注意力循环，将递归从深层逐渐扩展到浅层。随着训练的进行，该机制通过仅选择性地增加一小部分参数的深度来诱导结构稀疏性。在多个参数规模上的广泛实验表明，在同等设置下，SGT 始终优于训练时静态块级循环基准，同时将相对于标准 Transformer 骨干网络的额外训练 FLOPs 开销从约 16-20% 降低到仅 1-3%。

## Abstract
Existing approaches to increasing the effective depth of Transformers predominantly rely on parameter reuse, extending computation through recursive execution. Under this paradigm, the network structure remains static along the training timeline, and additional computational depth is uniformly assigned to entire blocks at the parameter level. This rigidity across training time and parameter space leads to substantial computational redundancy during training. In contrast, we argue that depth allocation during training should not be a static preset, but rather a progressively growing structural process. Our systematic analysis reveals a deep-to-shallow maturation trajectory across layers, where high-entropy attention heads play a crucial role in semantic integration. Motivated by this observation, we introduce the Sparse Growing Transformer (SGT). SGT is a training-time sparse depth allocation framework that progressively extends recurrence from deeper to shallower layers via targeted attention looping on informative heads. This mechanism induces structural sparsity by selectively increasing depth only for a small subset of parameters as training evolves. Extensive experiments across multiple parameter scales demonstrate that SGT consistently outperforms training-time static block-level looping baselines under comparable settings, while reducing the additional training FLOPs overhead from approximately 16--20% to only 1--3% relative to a standard Transformer backbone.

---

## 论文详细总结（自动生成）

以下是对论文《Sparse Growing Transformer: Training-Time Sparse Depth Allocation via Progressive Attention Looping》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型（LLM）在追求更深网络结构时面临的**计算冗余**和**训练开销**问题展开研究。
*   **研究动机**：虽然增加 Transformer 深度能显著提升推理能力，但简单堆叠层数会导致参数量和显存占用激增。现有的“递归/循环”架构（如 Block Looping）虽能解耦深度与参数量，但其在训练时采用静态拓扑且对整个区块（Block）均匀分配计算量，忽略了不同参数角色的差异，导致了严重的计算浪费。
*   **核心问题**：如何在训练过程中，以更细粒度、更动态的方式分配计算深度，从而在不显著增加训练成本的前提下提升模型性能？

### 2. 方法论：核心思想与关键技术
论文提出了**稀疏增长 Transformer (SGT)**，其核心思想是实现“训练时结构稀疏性”。
*   **核心思想**：深度分配不应是预设的静态结构，而应是一个随训练进程自发增长的过程。SGT 仅在信息量大的特定参数上增加计算深度。
*   **关键技术细节**：
    *   **熵引导的注意力循环 (Entropy-Guided Attention Looping)**：研究发现“高熵注意力头”是语义整合的关键枢纽。SGT 并不循环整个 Block，而是仅选择每层中熵值最高的 $h$ 个注意力头进行递归循环。
    *   **渐进式增长训练 (Progressive Growth Training)**：基于“深层先成熟，浅层后演化”的观察，SGT 采用**由深到浅 (Deep-to-Shallow)** 的增长策略。训练初期作为标准 Transformer，随后定期评估各层熵值，优先在已稳定的深层激活循环，再逐步扩展至浅层。
    *   **算法流程**：预热阶段（稳定熵信号） $\rightarrow$ 增长阶段（根据熵分布动态选择层和头，增加循环深度 $K$） $\rightarrow$ 固定阶段（冻结结构完成参数收敛）。

### 3. 实验设计
*   **数据集**：使用 C4 语料库的 20B token 子集进行从零预训练。
*   **模型规模**：涵盖 275M、573M 和 1.2B 三种参数规模。
*   **Benchmark**：在 9 个推理与知识任务上评估，包括 ARC-E、WinoGrande、SocialIQA、HellaSwag、OpenBookQA、CommonSenseQA、MMLU 等。
*   **对比方法**：
    1.  **Vanilla**：标准 Transformer 骨干网络。
    2.  **Block Loop**：静态的块级递归基准（循环整个 Transformer 块）。
    3.  **SGT (本文方法)**：不同循环深度（$K=1, 2, 3$）的变体。

### 4. 资源与算力
*   **硬件环境**：使用 8 台 NVIDIA A100 GPU (40GB 显存)。
*   **训练参数**：序列长度 4096，全局 Batch Size 1024，总训练步数 5035 步。
*   **算力开销**：论文明确对比了 FLOPs。Block Loop 增加了约 **16-20%** 的额外计算开销，而 SGT 仅增加了 **1-3%**。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个不同量级的模型上验证了结论的一致性，实验设计较为全面。
*   **消融实验**：针对循环组件（高熵头 vs 低熵头 vs 全头）、增长方向（由深到浅 vs 由浅到深）进行了详细对比。
*   **客观性**：通过长文本外推实验和注意力贡献流可视化，进一步验证了方法的泛化性和可解释性。整体实验设计遵循公平对比原则（相同训练数据、相同步数、对齐 FLOPs 后的性能对比）。

### 6. 主要结论与发现
*   **高效性**：SGT 在仅增加 1-3% 训练 FLOPs 的情况下，性能优于增加 16-20% 开销的 Block Loop 基准。
*   **功能分工**：高熵注意力头负责全局语义整合，对其进行循环能有效促进模型的“内在思考（Inner Thinking）”。
*   **演化规律**：模型层遵循由深到浅的成熟轨迹，顺应此轨迹的结构增长能显著提升训练稳定性。
*   **泛化能力**：SGT 在长文本外推任务上表现出更强的鲁棒性，减少了盲目循环带来的噪声。

### 7. 优点
*   **极高的计算效率**：通过细粒度的头级循环，极大地降低了递归架构的训练成本。
*   **生物学启发与理论支撑**：方法论受到神经科学启发，并提供了关于高熵递归收敛性的理论证明。
*   **强解释性**：通过可视化展示了循环如何使模型注意力从语法噪声转向任务关键 token。

### 8. 不足与局限
*   **模型规模限制**：实验最高仅达到 1.2B 参数，尚未在百亿（7B+）或千亿级超大规模模型上验证其扩展性。
*   **超参数敏感性**：虽然做了部分分析，但对于循环头数 $h$、激活层数 $L$ 等超参数的最优组合仍需更深入的探索。
*   **推理延迟**：尽管比 Block Loop 快，但相比 Vanilla 模型，递归机制在推理时仍会引入一定的额外延迟。

（完）
