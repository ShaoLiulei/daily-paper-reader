---
title: "KARMA: Knowledge-Action Regularized Multimodal Alignment for Personalized Search at Taobao"
title_zh: KARMA：面向淘宝个性化搜索的知识-行为正则化多模态对齐
authors: "Zhi Sun, Wenming Zhang, Yi Wei, Liren Yu, Zhixuan Zhang, Dan Ou, Haihong Tang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22779v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 针对个性化搜索微调大语言模型
tldr: "针对淘宝个性化搜索中大语言模型（LLM）微调导致的“知识-行为鸿沟”和语义坍缩问题，本文提出KARMA框架。该框架将语义重构作为训练正则项，在优化兴趣嵌入以进行检索的同时，通过历史条件生成和嵌入条件重构来保持LLM的语义解码能力。实验证明，KARMA有效缓解了语义坍缩，显著提升了淘宝搜索的召回、粗排和精排性能，并已成功上线带来0.5%的点击增长。"
selection_source: fresh_fetch
motivation: 直接在个性化任务上微调LLM会导致预训练语义知识与特定行为目标冲突，引发语义坍缩并限制模型的泛化表现。
method: 提出KARMA框架，利用语义重构作为正则化手段，通过历史条件生成和嵌入条件重构两个目标来约束兴趣嵌入的语义可恢复性。
result: "在淘宝搜索系统中，KARMA显著提升了点击率AUC和各阶段召回率，并在实际线上部署中实现了0.5%的商品点击量增长。"
conclusion: 通过在训练中引入语义可解码性约束，可以有效弥合知识与行为之间的差距，从而在工业级个性化搜索中充分释放LLM的语义优势。
---

## 摘要
大语言模型（LLMs）具备深厚的语义知识，使其成为为个性化搜索系统注入语义泛化能力的自然选择。然而，在实践中我们发现，直接在工业级个性化任务（如预测下一商品）上微调 LLM 往往效果欠佳。我们将这一瓶颈归因于关键的“知识-行为鸿沟”：即保留预训练语义知识与通过判别性目标对齐特定个性化行为之间的内在冲突。经验表明，仅针对行为的训练目标会导致语义坍缩（Semantic Collapse），例如出现“注意力汇聚”（attention sinks）。这种退化严重削弱了 LLM 的泛化能力，导致其无法为个性化搜索系统带来提升。我们提出了 KARMA（知识-行为正则化多模态对齐），这是一个将语义重构视为仅训练期正则化项的统一框架。KARMA 在优化用于检索的下一兴趣嵌入（行为）的同时，通过两个互补目标强制实现语义可解码性（知识）：(i) 基于历史条件的语义生成，将优化锚定在 LLM 原生的下一标记（next-token）分布上；(ii) 基于嵌入条件的语义重构，约束兴趣嵌入保持语义可恢复性。在淘宝搜索系统中，KARMA 缓解了语义坍缩（通过注意力汇聚分析验证），并提升了行为指标和语义保真度。在消融实验中，语义可解码性带来了高达 +22.5 的 HR@200 提升。借助 KARMA，我们在精排阶段实现了 +0.25 的 CTR AUC 提升，在粗排阶段实现了 +1.86 的 HR 提升，在召回阶段实现了 +2.51 的 HR 提升。KARMA 已在线部署，在精排阶段推理开销较低，带动商品点击量增长了 +0.5%。

## Abstract
Large Language Models (LLMs) are equipped with profound semantic knowledge, making them a natural choice for injecting semantic generalization into personalized search systems. However, in practice we find that directly fine-tuning LLMs on industrial personalized tasks (e.g. next item prediction) often yields suboptimal results. We attribute this bottleneck to a critical Knowledge--Action Gap: the inherent conflict between preserving pre-trained semantic knowledge and aligning with specific personalized actions by discriminative objectives. Empirically, action-only training objectives induce Semantic Collapse, such as attention ``sinks''. This degradation severely cripples the LLM's generalization, failing to bring improvements to personalized search systems.   We propose KARMA (Knowledge--Action Regularized Multimodal Alignment), a unified framework that treats semantic reconstruction as a train-only regularizer. KARMA optimizes a next-interest embedding for retrieval (Action) while enforcing semantic decodability (Knowledge) through two complementary objectives: (i) history-conditioned semantic generation, which anchors optimization to the LLM's native next-token distribution, and (ii) embedding-conditioned semantic reconstruction, which constrains the interest embedding to remain semantically recoverable.   On Taobao search system, KARMA mitigates semantic collapse (attention-sink analysis) and improves both action metrics and semantic fidelity. In ablations, semantic decodability yields up to +22.5 HR@200. With KARMA, we achieve +0.25 CTR AUC in ranking, +1.86 HR in pre-ranking and +2.51 HR in recalling. Deployed online with low inference overhead at ranking stage, KARMA drives +0.5% increase in Item Click.

---

## 论文详细总结（自动生成）

以下是对论文《KARMA: Knowledge-Action Regularized Multimodal Alignment for Personalized Search at Taobao》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在工业级个性化搜索中，直接微调大语言模型（LLM）进行“下一商品预测”往往效果不佳。
*   **研究动机**：作者发现存在**“知识-行为鸿沟”（Knowledge-Action Gap）**。当使用纯判别性目标（如点击行为对齐）训练时，模型会发生**“语义坍缩”（Semantic Collapse）**。
*   **现象观察**：在连续向量接口下，模型倾向于寻找捷径（Shortcut），将商品向量视为无意义的 ID。定性分析显示，注意力机制会出现“注意力汇聚”（Attention Sinks）现象，导致模型丧失预训练的语义泛化能力，在长尾查询和冷启动场景下表现糟糕。

### 2. 方法论
KARMA 提出了一种**知识-行为正则化**框架，核心思想是将语义重构作为“仅训练期”的正则项，确保模型在学习用户行为的同时保留语义可解码性。
*   **连续向量接口**：为了满足工业级延迟要求，将历史商品通过编码器压缩为单个 Embedding Token，输入 LLM 解码器生成“下一兴趣嵌入” $h_t$。
*   **双重解码任务（核心技术）**：
    1.  **历史条件语义生成（Task 1）**：根据用户历史行为序列，直接预测目标商品的文本 Token。这使模型锚定在 LLM 原生的 Token 分布上，防止遗忘。
    2.  **嵌入条件语义重构（Task 2）**：强制要求从生成的兴趣嵌入 $h_t$ 中能重构出目标商品的语义。这为 $h_t$ 创造了语义瓶颈，防止其坍缩为纯 ID 类的特征。
*   **多模态对齐**：引入扩散模型（Diffusion/Flow-matching）头，用于重构目标的视觉语义特征，提供更强的对齐信号。
*   **两阶段训练策略**：先进行语义预热（对齐连续向量与 LLM 空间），再进行行为与语义的联合训练。
*   **推理优化**：所有解码头仅在训练时存在，推理时直接使用 $h_t$ 进行向量检索，实现**零额外推理开销**。

### 3. 实验设计
*   **场景与数据**：在大规模电商平台（淘宝搜索）的匿名搜索日志上进行评估，包含用户历史交互、点击商品及负样本。
*   **评价指标**：
    *   **行为指标**：gAUC（精排）、HR@K（召回/粗排）。
    *   **语义指标**：JS@50（Jaccard 相似度），用于检测检索结果与真值之间的语义重合度，识别捷径学习。
*   **对比基准**：以仅使用行为监督（Pairwise-CE 损失）的 LLM 微调模型为 Baseline。
*   **消融实验**：分别验证 Task 1、Task 2 以及多模态重构对性能的贡献。

### 4. 资源与算力
*   **模型规格**：使用了 Qwen3-0.6B 作为用户解码器，Qwen3-1.7B（文本）和 Qwen3-VL-2B（多模态）作为商品编码器。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量及训练总时长。但考虑到其模型参数量（0.6B 到 2B 级别）和淘宝的业务规模，通常需要大规模 GPU 集群（如 A100/H800）支持。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了离线消融实验、注意力图可视化分析、不同生成目标（MSE vs. Diffusion）的对比。
*   **充分性**：实验设计较为全面。不仅有离线指标提升（HR@200 提升达 22.5），还进行了为期 14 天的线上 A/B 测试。
*   **客观性**：通过引入 JS@50 语义指标和注意力图分析，从定量和定性两个维度证明了“语义坍缩”的存在及 KARMA 的缓解作用，论证逻辑较为严密。

### 6. 主要结论与发现
*   **Task 2 是关键**：相比于 Task 1，基于嵌入条件的重构（Task 2）对缓解语义坍缩、提升检索召回率的作用更大。
*   **注意力修复**：KARMA 有效消除了“注意力汇聚”现象，使模型注意力分布更均匀，证明了语义信息的保留。
*   **扩散模型的局限性**：扩散模型适合作为语义重构的正则项，但在生成检索嵌入时表现不如简单的 MSE 目标。作者指出这是因为检索需要“均值寻求”（Mean-seeking）的稳定性，而扩散模型倾向于“众数寻求”（Mode-seeking）的样本多样性。
*   **业务价值**：线上部署后，在精排阶段实现了 +0.5% 的商品点击量增长。

### 7. 优点
*   **工业实用性极强**：通过“训练期正则、推理期移除”的设计，解决了 LLM 落地搜索系统时的延迟瓶颈。
*   **理论洞察深刻**：识别并定义了“知识-行为鸿沟”和“语义坍缩”问题，并提供了直观的注意力图证据。
*   **多模态融合**：巧妙地将视觉重构整合进正则化框架，增强了表征的鲁棒性。

### 8. 不足与局限
*   **算力细节缺失**：未披露具体的训练资源消耗，难以评估中小规模团队的复现成本。
*   **场景单一**：实验主要集中在淘宝搜索这一特定电商场景，在其他非电商类搜索（如网页搜索、文档检索）中的泛化效果有待验证。
*   **依赖预训练模型质量**：效果高度依赖于底层 Qwen3 等基座模型的预训练质量，若基座模型语义空间较差，正则化的效果可能会受限。

（完）
