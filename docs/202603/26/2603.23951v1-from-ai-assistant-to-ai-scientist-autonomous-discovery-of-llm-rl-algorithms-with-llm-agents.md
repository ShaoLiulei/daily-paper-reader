---
title: "From AI Assistant to AI Scientist: Autonomous Discovery of LLM-RL Algorithms with LLM Agents"
title_zh: 从 AI 助手到 AI 科学家：利用 LLM 智能体自主发现 LLM-RL 算法
authors: "Sirui Xia, Yikai Zhang, Aili Chen, Siye Wu, Siyu Yuan, Yanghua Xiao"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23951v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 自主发现语言模型的强化学习算法
tldr: 本研究针对LLM策略优化算法开发成本高、依赖人工的问题，提出了POISE框架。POISE是一个闭环自动化系统，利用LLM智能体在复杂的算法机制空间中进行搜索，并维护包含提案、代码、评估和反思的结构化存档。在数学推理任务中，POISE从GRPO算法出发，自动发现了如解析方差缩放等改进机制，显著提升了模型性能，展示了AI自主进行算法科学发现的巨大潜力。
selection_source: fresh_fetch
motivation: 传统的LLM策略优化算法发现过程高度依赖人工经验且成本昂贵，难以应对算法机制与训练动态之间复杂的耦合关系。
method: 提出POISE闭环框架，通过LLM智能体维护一个谱系化的结构化存档，实现从提案到执行、评估及反思的证据驱动式算法迭代。
result: "在数学推理实验中，POISE自动发现的算法将AIME25 pass@32准确率从26.7%大幅提升至43.3%，并发现了多种有效的改进机制。"
conclusion: 该研究证明了自动化发现高性能LLM强化学习算法的可行性，为实现从AI助手向AI科学家的转变迈出了重要一步。
---

## 摘要
发现改进的语言模型策略优化算法仍然是一个成本高昂的手动过程，需要反复进行机制层面的修改和验证。与简单的组合代码搜索不同，该问题需要在与训练动态紧密耦合的算法机制空间中进行搜索，同时在迭代过程中复用经验证据。我们提出了 POISE，这是一个用于自动发现语言模型策略优化算法的闭环框架。POISE 维护一个结构化的、具有谱系关联的存档，将提案、可执行实现、标准化评估和自然语言反思联系起来，以支持证据驱动的迭代。在以 GRPO 为起点的数学推理实验中，POISE 评估了 64 个候选算法，并发现了改进的机制，包括解析方差缩放（analytic-variance scaling）和有效性掩码（validity masking）。最佳变体将加权总分从 47.8 提高到 52.5（+4.6），并将 AIME25 pass@32 从 26.7% 提高到 43.3%，证明了自动策略优化发现的可行性，同时支持可解释的设计原则。

## Abstract
Discovering improved policy optimization algorithms for language models remains a costly manual process requiring repeated mechanism-level modification and validation. Unlike simple combinatorial code search, this problem requires searching over algorithmic mechanisms tightly coupled with training dynamics while reusing empirical evidence across iterations. We propose POISE, a closed-loop framework for automated discovery of policy optimization algorithms for language models. POISE maintains a structured, genealogically linked archive linking proposals, executable implementations, standardized evaluations, and natural-language reflections to support evidence-driven iteration. In mathematical reasoning experiments starting from GRPO, POISE evaluates 64 candidate algorithms and discovers improved mechanisms, including analytic-variance scaling and validity masking. The best variant improves weighted Overall from 47.8 to 52.5 (+4.6) and increases AIME25 pass@32 from 26.7% to 43.3%, demonstrating the feasibility of automated policy optimization discovery while supporting interpretable design principles.

---

## 论文详细总结（自动生成）

这篇论文由复旦大学数据科学重点实验室的研究团队发表，探讨了如何利用大语言模型（LLM）智能体自主发现并优化针对语言模型的强化学习（RL）算法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：目前为语言模型开发高效的策略优化算法（如 PPO, GRPO）高度依赖人工经验，过程极其耗时且昂贵。算法机制（如损失函数、优势估计、正则化）与训练动态高度耦合，微小的设计变化可能导致训练崩溃或性能大幅波动。
*   **核心问题**：能否构建一个闭环系统，让 LLM 智能体像科学家一样，通过观察实验证据、进行反思并迭代，自主发现超越现有基准的新型强化学习算法？
*   **整体含义**：该研究标志着从“AI 助手”向“AI 科学家”的转变，证明了在复杂的算法搜索空间中，AI 可以通过证据驱动的迭代实现科学发现。

### 2. 方法论：POISE 框架
POISE（Policy Optimization through Iterative Search and Evidence）是一个闭环自动化发现框架，其核心思想是**认识论进化搜索（Epistemic Evolutionary Search）**。

*   **结构化存档（Structured Archive）**：维护一个具有谱系关联的数据库，记录每个算法提案、可执行代码、标准化评估结果以及自然语言形式的反思（Reflections）。
*   **三个核心阶段**：
    1.  **提案生成（Phase I）**：利用获取函数 $s(d)$ 平衡当前性能与进化潜力（通过高斯过程估计后代增益），从存档中选择“父节点”，结合文献先验生成新算法提案。
    2.  **实现与验证（Phase II）**：将抽象提案转化为基于 `verl` 框架的可执行代码。通过共享接口和一致性检查，确保实验的可比性。
    3.  **反思分析与存档更新（Phase III）**：分析训练轨迹（如 KL 散度、奖励曲线、熵），将定量信号转化为机制层面的诊断报告，更新存档。
*   **关键技术细节**：框架不仅搜索超参数，更侧重于**机制创新**。例如，它发现了“解析方差缩放”（Analytic-variance scaling）来替代经验方差，以及“有效性掩码”（Validity masking）来过滤格式错误对奖励信号的干扰。

### 3. 实验设计
*   **任务场景**：专注于**数学推理**任务，这是目前 LLM 强化学习最具挑战性的领域之一。
*   **基准算法（Baseline）**：以 **GRPO**（DeepSeek 提出的大群体相对策略优化）为进化起点。
*   **数据集**：使用了 6 个标准化推理基准：AIME24、AIME25、AMC、MATH-500、Minerva 和 OlympiadBench。
*   **对比对象**：进化过程中产生的 63 个变体算法，以及针对特定约束（如长度压缩）的专项变体。
*   **模型配置**：策略模型使用 Qwen2.5-Math-1.5B，框架背后的推理引擎为 Gemini-1.5-Pro-preview。

### 4. 资源与算力
*   **硬件资源**：实验使用了 **8 张 80GB A100 GPU**。
*   **训练细节**：每个候选算法在 5000 个 MATH 训练子集上运行，设置了固定的超参数（学习率 1e-6，全局 Batch Size 256，Group Size 8，训练 8 个 Epoch）。
*   **推理配置**：使用 vLLM 进行推理，最大生成长度为 4096。

### 5. 实验数量与充分性
*   **实验规模**：共评估了 **64 个算法**（1 个基准 + 63 个进化变体）。
*   **充分性分析**：
    *   **多样性**：涵盖了核心信号建模、信号路由、失败侧控制、稳定性约束等多个机制家族。
    *   **客观性**：所有算法在完全相同的硬件、数据和评估协议下运行，确保了对比的公平性。
    *   **专项实验**：额外进行了“长度压缩”约束下的定向进化实验，验证了框架的可控性。
    *   **局限性**：虽然 64 组实验在 RL 领域已属不小的规模，但受限于算力，未能覆盖更大规模的模型（如 70B+）或更多样化的任务（如代码生成、对话）。

### 6. 主要结论与发现
*   **性能突破**：最强变体 **VM-AV-GRPO** 将加权总分从 47.8 提升至 **52.5 (+4.6)**。在极难的 AIME25 上，pass@32 从 26.7% 飙升至 **43.3%**。
*   **机制发现**：
    *   **解析方差缩放**：在稀疏奖励下，通过数学手段锚定方差，比经验方差更稳定地放大正确信号。
    *   **有效性掩码**：严格区分“格式错误”和“逻辑错误”，防止模型通过投机取巧获得奖励。
*   **可控进化**：通过自然语言指令，POISE 成功发现了能在保持准确率的同时减少 29.1% 响应长度的算法（DACE-GRPO）。
*   **设计原则**：总结出“信号解耦”、“条件归一化”和“正确性优先的效率塑造”等可解释的算法设计原则。

### 7. 优点
*   **闭环自动化**：实现了从想法到代码再到反思的全流程自动化，减少了人工干预。
*   **证据驱动**：不同于随机搜索，POISE 能够利用历史失败的“负面证据”来修正后续的搜索方向。
*   **谱系化分析**：通过谱系树展示了算法演进的过程，使得 AI 发现的机制具有高度的可解释性。

### 8. 不足与局限
*   **计算成本**：每个候选算法都需要完整的训练和评估流程，在大规模搜索时算力开销巨大。
*   **任务局限**：目前仅在数学推理任务上验证，其发现的机制是否能泛化到通用对话或创意写作尚待观察。
*   **因果解释风险**：虽然反思报告提供了机制层面的解释，但这些解释本质上是 LLM 生成的假设，而非严格的因果验证。
*   **模型依赖**：框架的性能高度依赖于底层 LLM 智能体（如 Gemini）的推理能力。

（完）
