---
title: Large Language Model Guided Incentive Aware Reward Design for Cooperative Multi-Agent Reinforcement Learning
title_zh: 大语言模型引导的协作多智能体强化学习激励感知奖励设计
authors: "Dogan Urgun, Gokhan Gungor"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24324v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: LLM引导的强化学习奖励设计
tldr: 针对协作多智能体强化学习中辅助奖励设计困难且易导致协调失调的问题，本文提出一种利用大语言模型（LLM）自动合成奖励程序的框架。该框架通过环境仪表信息生成可执行代码，并在形式化有效性约束下进行迭代搜索与评估。在Overcooked-AI环境下的实验表明，该方法能显著提升任务回报和协调效率，特别是在存在交互瓶颈的复杂场景中，有效减轻了人工设计负担并增强了智能体间的协作能力。
selection_source: fresh_fetch
motivation: 手动设计多智能体协作中的辅助奖励既费时又容易导致激励不一致，尤其是在任务反馈稀疏的情况下难以实现高效协调。
method: 提出一种自动化框架，利用LLM从环境信息中合成受约束的可执行奖励程序，并通过固定计算预算下的策略训练进行迭代优化。
result: 在多种Overcooked-AI布局中，该框架生成的奖励显著提高了任务完成率和回报，特别是在协调密集型任务中表现出更强的动作依赖性和信号一致性。
conclusion: 基于LLM的目标导向奖励程序搜索能够有效替代人工工程，在有限预算内产生与协作学习兼容的塑造信号。
---

## 摘要
为协作多智能体系统设计有效的辅助奖励仍然是一项具有挑战性的任务；激励不一致可能导致次优的协调，特别是在稀疏的任务反馈无法提供足够基础的情况下。本研究引入了一种自动化的奖励设计框架，利用大语言模型从环境检测中合成可执行的奖励程序。该过程将候选程序约束在正式的有效性范围内，并通过在固定计算预算下从头开始训练策略来评估其功效；选择完全取决于稀疏的任务回报。该框架在四个不同的 Overcooked-AI 布局中进行了评估，这些布局具有不同的走廊拥堵、交接依赖和结构不对称特征。迭代搜索生成一致地产生了更高的任务回报和交付数量，其中最显著的增益出现在以交互瓶颈为主的环境中。对合成的塑造组件进行的诊断分析表明，在协调密集型任务中，动作选择的相互依赖性增加，信号对齐得到改善。这些结果表明，寻找基于目标的奖励程序可以减轻人工工程的负担，同时产生与有限预算下的协作学习相兼容的塑造信号。

## Abstract
Designing effective auxiliary rewards for cooperative multi-agent systems remains a precarious task; misaligned incentives risk inducing suboptimal coordination, especially where sparse task feedback fails to provide sufficient grounding. This study introduces an automated reward design framework that leverages large language models to synthesize executable reward programs from environment instrumentation. The procedure constrains candidate programs within a formal validity envelope and evaluates their efficacy by training policies from scratch under a fixed computational budget; selection depends exclusively on the sparse task return. The framework is evaluated across four distinct Overcooked-AI layouts characterized by varied corridor congestion, handoff dependencies, and structural asymmetries. Iterative search generations consistently yield superior task returns and delivery counts, with the most pronounced gains occurring in environments dominated by interaction bottlenecks. Diagnostic analysis of the synthesized shaping components indicates increased interdependence in action selection and improved signal alignment in coordination-intensive tasks. These results demonstrate that the search for objectivegrounded reward programs can mitigate the burden of manual engineering while producing shaping signals compatible with cooperative learning under finite budgets.

---

## 论文详细总结（自动生成）

### 论文总结：大语言模型引导的协作多智能体强化学习激励感知奖励设计

#### 1. 核心问题与整体含义
在协作多智能体强化学习（MARL）中，**奖励设计（Reward Design）**是决定性能的关键瓶颈。由于环境任务奖励通常是稀疏且延迟的，开发者往往需要手动设计辅助奖励（Reward Shaping）来引导学习。然而，手动设计不仅耗时耗力，还容易导致“激励不一致”问题，即智能体可能会为了追求辅助奖励而偏离最终任务目标。本文旨在通过大语言模型（LLM）自动化这一过程，生成既能加速学习又能保持目标一致性的奖励程序。

#### 2. 方法论
论文提出了一种**目标导向的奖励搜索框架**，其核心流程如下：
*   **LLM 奖励生成**：利用 LLM（如 GPT 系列）根据环境的仪表信息（Instrumentation）和任务描述，合成可执行的 Python 奖励程序。
*   **有效性信封（Validity Envelope）**：对生成的代码进行约束，强制执行输入输出签名检查、确定性校验、输出值裁剪（防止数值爆炸）以及对缺失键值的鲁棒性处理。
*   **端到端评估**：使用生成的奖励函数，配合固定的 MARL 算法（MAPPO）从零开始训练策略。
*   **目标导向选择（Objective-Grounded Selection）**：这是该方法的关键。尽管训练使用了辅助奖励，但**筛选和晋级候选程序的唯一标准是原始的稀疏任务回报**。这确保了辅助信号不会误导智能体偏离核心目标。
*   **激励诊断反馈**：计算“收益不平衡度”、“激励对齐度”和“动作耦合度（NMI）”等指标，作为结构化反馈提供给 LLM，用于下一轮迭代生成。

#### 3. 实验设计
*   **实验场景**：使用了经典的协作基准测试 **Overcooked-AI**（煮汤游戏）。
*   **布局（Layouts）**：选择了四个具有代表性的地图：
    1.  *Cramped Room*（拥挤房间）：测试空间竞争。
    2.  *Forced Coordination*（强制协作）：测试物品交接。
    3.  *Coordination Ring*（协作环路）：测试同步导航和避让。
    4.  *Asymmetric Advantages*（非对称优势）：测试角色分工。
*   **Benchmark 与对比对象**：以标准的 **MAPPO（仅使用稀疏奖励）** 作为 Baseline，对比了经过第一代（Gen 1）和第二代（Gen 2）迭代搜索后生成的奖励程序性能。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或具体的训练时长。
*   **计算预算**：文中强调在“固定计算预算”下进行评估，即每个候选奖励程序训练的步数是统一的，以确保公平比较。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 种不同特性的地图上进行了实验，每代搜索包含多个候选程序（K 个），并进行了多轮迭代（G 代）。
*   **充分性评价**：实验设计较为充分，涵盖了协作学习中常见的几种瓶颈（如死锁、交接、分工）。通过展示从 Baseline 到 Gen 2 的性能单调增长，证明了迭代搜索的有效性。
*   **客观性**：采用从零训练（Training from scratch）和仅依据稀疏奖励进行评估的方式，保证了实验结果的客观性和公平性，排除了人为干预奖励权重的偏差。

#### 6. 主要结论与发现
*   **性能显著提升**：在所有布局中，LLM 生成的奖励均优于 Baseline。在最难的 *Coordination Ring* 中，交付数量从 0.15 提升到了 7.65（提升近 50 倍）。
*   **解决交互瓶颈**：生成的奖励程序能有效引导智能体完成中间步骤（如拿取食材、交接），从而突破了稀疏奖励下的探索困境。
*   **协作模式演变**：诊断分析显示，高性能奖励显著增加了智能体间的“动作耦合度”（NMI），意味着智能体学会了更紧密的相互适应，而非各自为战。
*   **激励对齐**：在需要频繁交接的场景中，LLM 倾向于生成能平衡两个智能体收益的奖励，减少了因一方“摆烂”导致的协作失败。

#### 7. 优点
*   **自动化与可解释性**：将奖励设计转化为代码生成，既减少了人工干预，又使奖励逻辑对人类开发者透明、可读。
*   **目标对齐机制**：通过“目标导向选择”，有效解决了强化学习中常见的奖励黑客（Reward Hacking）问题。
*   **闭环反馈**：引入了多维度的诊断指标（如 NMI、相关性等）作为 LLM 的 Prompt 反馈，使搜索过程具有方向性。

#### 8. 不足与局限
*   **计算开销**：由于每个候选奖励都需要从头训练一个完整的 RL 模型，在大规模环境或复杂任务中，这种搜索方式的计算成本极高。
*   **领域局限性**：实验仅限于 Overcooked-AI 这一种环境，尚未在更复杂（如 3D 环境或更多智能体数量）的场景中验证。
*   **LLM 依赖**：框架的上限取决于底层 LLM 的代码生成能力和对环境仪表信息的理解能力。
*   **智能体规模**：目前仅验证了双人协作（n=2），在多人（n>10）复杂博弈中的表现尚不明确。

（完）
