---
title: Hyperagents
title_zh: 超智能体 (Hyperagents)
authors: "Jenny Zhang, Bingchen Zhao, Wannan Yang, Jakob Foerster, Jeff Clune, Minqi Jiang, Sam Devlin, Tatiana Shavrina"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19461v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 自我改进的AI系统及其基础理论
tldr: 本文提出Hyperagents框架，旨在解决现有自我改进AI系统依赖固定机制的局限。该框架将任务代理与元代理整合为统一的可编辑程序，使改进机制本身也能被优化。通过扩展DGM提出的DGM-H系统，在多领域实现了性能的持续提升和元能力的跨任务迁移，展示了AI从搜索解法向优化“改进过程”的进化。
selection_source: fresh_fetch
motivation: 现有自我改进系统受限于手工设计的固定元机制，且任务性能与自我改进能力的提升往往仅在编程领域对齐，难以推广到通用任务。
method: 提出Hyperagents框架，将任务执行与自我修改逻辑整合为统一的可编辑程序，使系统能同时优化任务策略和改进自身的元算法。
result: DGM-H在多领域任务中优于无自我改进的基准模型，且其产生的元层级改进（如持久内存）能够跨领域迁移并持续累积。
conclusion: Hyperagents实现了真正的元认知自我进化，为开发能够持续优化自身改进机制的开放式AI系统提供了新路径。
---

## 摘要
自我改进的 AI 系统旨在通过学习改进自身的学习和问题解决过程，从而减少对人工工程的依赖。现有的自我改进方法依赖于固定的、手工设计的元级机制，这从根本上限制了此类系统的改进速度。Darwin Gödel Machine (DGM) 通过反复生成和评估自我修改的变体，展示了在编程领域的开放式自我改进。由于评估和自我修改都是编程任务，编程能力的提升可以转化为自我改进能力的提升。然而，这种对齐关系在编程领域之外通常并不成立。我们引入了超智能体 (hyperagents)，这是一种自指智能体，它将任务智能体（解决目标任务）和元智能体（修改自身和任务智能体）集成到一个单一的可编辑程序中。关键在于，元级修改过程本身也是可编辑的，从而实现了元认知自我修改，不仅改进了任务解决行为，还改进了生成未来改进的机制。我们通过扩展 DGM 创建了 DGM-Hyperagents (DGM-H) 来实例化这一框架，消除了任务性能与自我修改技能之间领域特定对齐的假设，从而可能支持在任何可计算任务上实现自我加速的进展。在不同领域中，DGM-H 随着时间的推移提高了性能，并优于没有自我改进或开放式探索的基准系统，以及先前的自我改进系统。此外，DGM-H 改进了其生成新智能体的过程（例如，持久化内存、性能跟踪），并且这些元级改进可以跨领域迁移并在多次运行中累积。DGM-Hyperagents 让我们一窥开放式 AI 系统，它们不仅在寻找更好的解决方案，还在不断改进其‘如何改进’的搜索过程。

## Abstract
Self-improving AI systems aim to reduce reliance on human engineering by learning to improve their own learning and problem-solving processes. Existing approaches to self-improvement rely on fixed, handcrafted meta-level mechanisms, fundamentally limiting how fast such systems can improve. The Darwin Gödel Machine (DGM) demonstrates open-ended self-improvement in coding by repeatedly generating and evaluating self-modified variants. Because both evaluation and self-modification are coding tasks, gains in coding ability can translate into gains in self-improvement ability. However, this alignment does not generally hold beyond coding domains. We introduce \textbf{hyperagents}, self-referential agents that integrate a task agent (which solves the target task) and a meta agent (which modifies itself and the task agent) into a single editable program. Crucially, the meta-level modification procedure is itself editable, enabling metacognitive self-modification, improving not only the task-solving behavior, but also the mechanism that generates future improvements. We instantiate this framework by extending DGM to create DGM-Hyperagents (DGM-H), eliminating the assumption of domain-specific alignment between task performance and self-modification skill to potentially support self-accelerating progress on any computable task. Across diverse domains, the DGM-H improves performance over time and outperforms baselines without self-improvement or open-ended exploration, as well as prior self-improving systems. Furthermore, the DGM-H improves the process by which it generates new agents (e.g., persistent memory, performance tracking), and these meta-level improvements transfer across domains and accumulate across runs. DGM-Hyperagents offer a glimpse of open-ended AI systems that do not merely search for better solutions, but continually improve their search for how to improve.

---

## 论文详细总结（自动生成）

### 论文总结：Hyperagents (超智能体)

#### 1. 核心问题与整体含义（研究动机和背景）
现有的自我改进 AI 系统（如 Darwin Gödel Machine, DGM）通常依赖于**固定且手工设计的元级（meta-level）机制**来生成改进指令。这导致了两个主要瓶颈：
*   **改进速度受限：** 由于元代理（负责改进的系统）本身不可修改，系统无法优化其“如何改进”的逻辑。
*   **领域局限性：** 在编程领域，任务能力与改进能力是对齐的（都是写代码）；但在非编程领域（如数学、机器人），提升任务表现并不等同于提升自我修改能力。
**研究动机：** 提出一种能够针对任何可计算任务进行自我加速改进的框架，使 AI 不仅能搜索更好的解，还能优化其“搜索改进”的过程。

#### 2. 方法论：核心思想与算法流程
论文提出了 **Hyperagents（超智能体）** 框架，并将其应用于 DGM 得到 **DGM-H**：
*   **核心思想：** 将“任务智能体”（解决目标任务）和“元智能体”（修改代码）整合为一个**统一的、可自指（Self-referential）且可编辑的程序**。
*   **元认知自我修改（Metacognitive Self-modification）：** 元智能体不仅修改任务逻辑，还被允许修改自身的代码。这意味着改进机制本身是进化的。
*   **算法流程：**
    1.  **初始化：** 从一个简单的初始超智能体（包含基础的任务和元逻辑）开始。
    2.  **开放式探索：** 维护一个超智能体存档（Archive）。
    3.  **父代选择：** 根据性能和多样性从存档中选择父代。
    4.  **自我修改：** 父代超智能体生成自身的修改版本（子代）。
    5.  **评估与迭代：** 在目标任务上评估子代，若表现优异则加入存档，循环往复。

#### 3. 实验设计：场景、Benchmark 与对比
实验涵盖了四个迥异的领域：
*   **编程（Coding）：** 使用 Polyglot 基准测试。
*   **论文评审（Paper Review）：** 预测学术论文的录用结果（基于 APRES 数据集）。
*   **机器人奖励设计（Robotics Reward Design）：** 在 Genesis 物理引擎中为四足机器人设计奖励函数。
*   **奥数级数学评分（Olympiad-level Math Grading）：** 使用 IMO-GradingBench 对数学证明进行评分。
*   **对比方法：**
    *   **DGM-H w/o self-improve：** 固定元代理，仅优化任务逻辑（类似 ADAS 方法）。
    *   **DGM-H w/o open-ended exploration：** 移除存档机制，仅保留最新版本。
    *   **原始 DGM / DGM-custom：** 针对编程优化的原始系统及其手工定制版。
    *   **静态基准：** 如 AI-Scientist-v2 的评审员、ProofAutoGrader 等。

#### 4. 资源与算力
*   **模型使用：** 实验主要调用了基础模型 API，包括 **Claude 3.5/4.5 Sonnet**（用于自我修改和机器人任务）、**GPT-4o**（用于论文评审）、**o3-mini/o4-mini**（用于编程和数学评分）。
*   **算力成本：** 论文提供了 Token 消耗估算。一个 100 次迭代的典型运行大约消耗 **3300 万（33M）Token** 用于自我修改，加上约 **50M Token** 用于任务评估，总计约 **88.6M Token**。
*   **硬件：** 未明确说明具体的 GPU 数量，因为主要依赖 API 调用，但实验在沙盒环境（Sandbox）中运行。

#### 5. 实验数量与充分性
*   **实验规模：** 每个实验设置运行 5 次，报告中位数及 95% 置信区间。
*   **充分性：** 实验设计非常全面，包含了：
    *   **消融实验：** 证明了“自我改进元机制”和“开放式探索”缺一不可。
    *   **跨领域迁移实验：** 验证了在论文评审中学习到的元能力（如性能跟踪、持久内存）能否加速数学评分任务的改进。
    *   **统计显著性：** 使用了 Wilcoxon 符号秩检验。
*   **客观性：** 实验设置了严格的隔离，测试集在训练过程中不可见。

#### 6. 主要结论与发现
*   **性能超越：** DGM-H 在所有领域均显著优于无自我改进或无存档的基准，且在非编程领域超越了手工设计的 DGM。
*   **元能力进化：** 系统自发演化出了**持久内存（Persistent Memory）**、**性能跟踪器（Performance Tracker）**和**偏见检测机制**，这些并非人工预设。
*   **跨领域迁移：** 在一个领域学到的“改进策略”可以迁移到新领域，显著提升新任务的改进效率（imp@50 指标显著提升）。
*   **累积效应：** 自我改进的效果可以跨运行累积，展示了通往“无限自我改进”的潜力。

#### 7. 优点
*   **通用性：** 突破了 DGM 仅限于编程领域的限制，实现了对任何可计算任务的通用自我改进。
*   **真正的元学习：** 允许 AI 修改其改进算法本身，解决了元层级无限递归的问题。
*   **涌现性：** 观察到了 AI 自发构建复杂软件工程工具（如日志系统、决策树）的行为。

#### 8. 不足与局限
*   **任务分布固定：** 虽然智能体在进化，但任务集是预设的，缺乏任务与智能体的协同进化（Co-evolution）。
*   **外层循环固定：** 尽管元代理可变，但最外层的“父代选择”和“评估协议”在主实验中仍是固定的（尽管附录尝试了修改选择机制）。
*   **成本高昂：** 依赖大量的高性能 LLM 调用，推理成本和时间成本较高。
*   **安全风险：** 开放式自我改进系统存在演化速度超过人类监管的潜在风险，尽管文中强调了沙盒和人工监督。

（完）
