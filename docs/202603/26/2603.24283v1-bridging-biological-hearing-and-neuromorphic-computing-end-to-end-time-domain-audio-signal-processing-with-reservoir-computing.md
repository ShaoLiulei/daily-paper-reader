---
title: "Bridging Biological Hearing and Neuromorphic Computing: End-to-End Time-Domain Audio Signal Processing with Reservoir Computing"
title_zh: 连接生物听觉与类脑计算：基于储备池计算的端到端时域音频信号处理
authors: "Rinku Sebastian, Simon O'Keefe, Martin Trefzer"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24283v1"
tags: ["query:snn"]
score: 9.0
evidence: 类脑计算与储备池计算
tldr: 本研究针对传统音频处理中MFCC提取计算量大的问题，提出一种结合生物启发与储备池计算（RC）的端到端时域处理框架。通过卷积操作替代复杂的频域转换，简化了特征提取流程，实现了高效、实时的语音分析，为嵌入式系统和神经形态计算提供了低功耗解决方案。
selection_source: fresh_fetch
motivation: 传统音频信号处理依赖复杂的时频转换，难以达到人类听觉系统的精度且在实时应用中效率受限。
method: 提出一种利用储备池计算和卷积操作在时域内直接提取MFCC特征的端到端音频处理框架。
result: 该方法在保持特征辨识度的同时，消除了复杂的频率变换，显著提升了音频处理的计算效率。
conclusion: 本研究成功桥接了生物启发特征提取与现代神经形态计算，为下一代低功耗语音识别系统提供了可扩展的方案。
---

## 摘要
尽管前沿技术取得了长足进步，但音频信号处理仍面临诸多挑战，且在精确度上不及人类语音处理系统。为应对这些挑战，我们提出了一种利用时域技术和储备池计算来简化音频信号处理的新方法。通过研究，我们利用训练难度显著降低的储备池计算机简化了处理流程，开发出一套实时音频信号处理系统。特征提取是语音信号处理的基础步骤，梅尔频率倒谱系数（MFCCs）因其与人类听觉的感知相关性而成为主流选择。然而，传统的 MFCC 提取依赖于计算密集型的时频变换，限制了其在实时应用中的效率。为此，我们提出了一种利用储备池计算来优化 MFCC 提取的新方法。通过用卷积运算替代传统的频域转换，我们在保持特征辨别力的同时，消除了对复杂变换的需求。我们展示了一个集成该方法的端到端音频处理框架，证明了其在高效实时语音分析方面的潜力。我们的研究结果有助于推动节能音频处理技术的发展，使其能够在嵌入式系统和语音驱动应用中实现无缝部署。这项工作填补了生物启发式特征提取与现代类脑计算之间的空白，为下一代语音识别系统提供了一种可扩展的解决方案。

## Abstract
Despite the advancements in cutting-edge technologies, audio signal processing continues to pose challenges and lacks the precision of a human speech processing system. To address these challenges, we propose a novel approach to simplify audio signal processing by leveraging time-domain techniques and reservoir computing. Through our research, we have developed a real-time audio signal processing system by simplifying audio signal processing through the utilization of reservoir computers, which are significantly easier to train.   Feature extraction is a fundamental step in speech signal processing, with Mel Frequency Cepstral Coefficients (MFCCs) being a dominant choice due to their perceptual relevance to human hearing. However, conventional MFCC extraction relies on computationally intensive time-frequency transformations, limiting efficiency in real-time applications. To address this, we propose a novel approach that leverages reservoir computing to streamline MFCC extraction. By replacing traditional frequency-domain conversions with convolution operations, we eliminate the need for complex transformations while maintaining feature discriminability. We present an end-to-end audio processing framework that integrates this method, demonstrating its potential for efficient and real-time speech analysis. Our results contribute to the advancement of energy-efficient audio processing technologies, enabling seamless deployment in embedded systems and voice-driven applications. This work bridges the gap between biologically inspired feature extraction and modern neuromorphic computing, offering a scalable solution for next-generation speech recognition systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bridging Biological Hearing and Neuromorphic Computing: End-to-End Time-Domain Audio Signal Processing with Reservoir Computing》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的音频信号处理（尤其是语音识别）高度依赖于**梅尔频率倒谱系数（MFCC）**的提取。然而，标准的 MFCC 提取过程涉及复杂的时频转换（如快速傅里叶变换 FFT），这不仅计算量巨大、延迟高，还会导致精确时间特征的不可逆丢失。
*   **研究背景**：在实时应用和嵌入式系统（如助听器、低功耗语音唤醒）中，传统的频域处理方法构成了计算瓶颈。
*   **研究动机**：受生物听觉系统启发，探索能否利用**储备池计算（Reservoir Computing, RC）**在纯时域内直接处理原始波形，从而简化流程、降低功耗并保持时间精度。

### 2. 方法论
*   **核心思想**：提出一种端到端的时域处理框架，利用 RC 的动力学特性替代传统的频域变换。
*   **关键技术细节**：
    *   **储备池计算（RC）架构**：采用固定随机权重的循环神经网络（储备池）作为非线性映射层，仅训练输出层的线性读出权重（使用岭回归算法）。
    *   **时域 MFCC 提取**：
        1.  **合成滤波器**：根据 Mel 尺度的频率参数，预先合成一组时域正弦波信号，模拟 Mel 滤波器组。
        2.  **卷积运算**：将原始音频信号与这些时域滤波器进行卷积，直接在时域获取等效的频谱特征。
        3.  **数据降维**：对卷积后的信号进行分窗，并使用**绝对最大池化（Absolute Max Pooling）**提取关键特征点，以匹配后续分类器的输入维度。
    *   **双储备池级联**：第一个储备池负责特征提取（模拟 MFCC），第二个储备池负责最终的分类任务（数字或说话者识别）。

### 3. 实验设计
*   **数据集**：
    *   **TI-46**：包含 8 名女性说话者朗读数字 0-9 的语音。
    *   **Audio-MNIST**：包含 60 名说话者朗读数字 0-9，共计 30,000 条样本。
*   **Benchmark（基准）**：
    *   **Exp 1**：使用 Matlab 标准函数提取的 MFCC 作为输入，测试 RC 的分类极限。
    *   **Exp 2**：使用论文提出的“时域 RC 特征提取器”生成的特征作为输入，测试端到端性能。
*   **对比方法**：
    *   类脑模型：LSM（液体状态机）、Liquid-SNN。
    *   深度学习模型：CNN、LSTM、VGG16、ResNet50、CapsNet 等。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或训练时长。
*   **模型规模**：论文强调了模型的极度轻量化。分类储备池仅需 **400 个神经元**，特征提取储备池仅需 **35 个神经元**。相比之下，对比的 CNN/LSTM 模型通常拥有 100 万至 2000 万个参数。

### 5. 实验数量与充分性
*   **实验规模**：针对两个数据集分别进行了数字识别和说话者识别实验。
*   **充分性评价**：
    *   采用了 **5 折交叉验证**，并针对不同的随机种子重复实验 **10 次**，通过箱线图展示结果，具有较好的统计说服力。
    *   对比了多种主流架构（从简单的逻辑回归到复杂的残差网络），对比维度较为全面。
    *   但实验主要集中在干净环境下的孤立词识别，缺乏噪声环境下的鲁棒性测试。

### 6. 主要结论与发现
*   **性能表现**：在 TI-46 数据集上，该方法达到了 **91.82%** 的准确率；在 Audio-MNIST 上达到 **84.81%**。
*   **能效优势**：RC 方法在神经元数量极少（数百个）的情况下，达到了与深度学习模型（数百万参数）相近的性能。
*   **可行性验证**：证明了通过时域卷积和 RC 动力学可以有效模拟 MFCC 特征，消除了对 FFT 等复杂频域变换的依赖。

### 7. 优点
*   **极低复杂度**：通过时域卷积简化了预处理流程，非常适合资源受限的边缘计算设备。
*   **训练简便**：RC 仅需训练输出层，避免了传统 RNN 训练中的梯度消失/爆炸问题。
*   **生物相似性**：更接近生物耳蜗的处理方式，保留了原始信号的时间精细结构。

### 8. 不足与局限
*   **准确率差距**：在较大规模的数据集（Audio-MNIST）上，其准确率（84.81%）仍明显低于经过深度优化的 CNN（96.4%）。
*   **降维损失**：目前采用的最大池化降维方法较为简单，可能导致部分关键声学信息的丢失。
*   **应用场景受限**：目前仅验证了孤立数字识别，对于连续语音识别或复杂背景噪声下的表现尚不明确。
*   **超参数敏感性**：RC 的性能高度依赖于储备池的谱半径、泄漏率等超参数，论文未详细讨论这些参数的自动化优化方法。

（完）
