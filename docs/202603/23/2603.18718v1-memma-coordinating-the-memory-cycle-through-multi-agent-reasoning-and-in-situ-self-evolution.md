---
title: "MemMA: Coordinating the Memory Cycle through Multi-Agent Reasoning and In-Situ Self-Evolution"
title_zh: MemMA：通过多智能体推理与原位自我演化协调记忆周期
authors: "Minhua Lin, Zhiwei Zhang, Hanqing Lu, Hui Liu, Xianfeng Tang, Qi He, Xiang Zhang, Suhang Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18718v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 记忆增强型大语言模型智能体与多智能体推理
tldr: MemMA是一个即插即用的多智能体框架，旨在解决长程交互中LLM记忆系统的孤立性问题。它通过Meta-Thinker在正向路径提供策略指导，协调记忆构建与检索；在反向路径引入原位自进化机制，通过合成探针QA对记忆进行验证与修复。实验证明MemMA在LoCoMo基准上显著优于现有方法，并能有效提升多种存储后端的性能。
selection_source: fresh_fetch
motivation: 现有记忆增强型LLM智能体在记忆构建、检索和利用上缺乏协同，导致策略盲目且缺乏有效的错误修复机制。
method: 提出MemMA框架，利用Meta-Thinker指导记忆管理与查询推理，并通过原位自进化机制实现记忆的自动验证与修复。
result: 在LoCoMo基准测试中，MemMA在多种LLM骨干网络和存储后端上均取得了超越基准的效果。
conclusion: MemMA通过多智能体协同和自进化机制，显著提升了LLM在长程交互任务中的记忆管理效率和准确性。
---

## 摘要
记忆增强型大语言模型（LLM）智能体通过维护外部记忆库来支持长程交互，但现有大多数系统将记忆的构建、检索和利用视为孤立的子程序。这带来了两个耦合的挑战：一是记忆周期前向路径上的策略盲目性，即构建和检索是由局部启发式方法而非显式策略推理驱动的；二是后向路径上的稀疏且延迟的监督，即下游任务的失败很少能转化为对记忆库的直接修复。为了应对这些挑战，我们提出了 MemMA，这是一个即插即用的多智能体框架，可在前向和后向路径上协调记忆周期。在前向路径上，元思考器（Meta-Thinker）生成结构化指导，在构建过程中引导记忆管理器（Memory Manager），并在迭代检索过程中指导查询推理器（Query Reasoner）。在后向路径上，MemMA 引入了原位自我演化的记忆构建机制，该机制在记忆最终确定前，通过合成探测问答对、验证当前记忆，并将失败转化为修复动作。在 LoCoMo 上的广泛实验表明，MemMA 在多个 LLM 主干网络上始终优于现有基准，并以即插即用的方式改进了三种不同的存储后端。我们的代码已在 https://github.com/ventr1c/memma 公开。

## Abstract
Memory-augmented LLM agents maintain external memory banks to support long-horizon interaction, yet most existing systems treat construction, retrieval, and utilization as isolated subroutines. This creates two coupled challenges: strategic blindness on the forward path of the memory cycle, where construction and retrieval are driven by local heuristics rather than explicit strategic reasoning, and sparse, delayed supervision on the backward path, where downstream failures rarely translate into direct repairs of the memory bank. To address these challenges, we propose MemMA, a plug-and-play multi-agent framework that coordinates the memory cycle along both the forward and backward paths. On the forward path, a Meta-Thinker produces structured guidance that steers a Memory Manager during construction and directs a Query Reasoner during iterative retrieval. On the backward path, MemMA introduces in-situ self-evolving memory construction, which synthesizes probe QA pairs, verifies the current memory, and converts failures into repair actions before the memory is finalized. Extensive experiments on LoCoMo show that MemMA consistently outperforms existing baselines across multiple LLM backbones and improves three different storage backends in a plug-and-play manner. Our code is publicly available at https://github.com/ventr1c/memma.

---

## 论文详细总结（自动生成）

### 论文总结：MemMA —— 通过多智能体推理与原位自我演化协调记忆周期

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）从短期对话转向长程（Long-horizon）任务，外部记忆库成为核心组件。然而，现有系统通常将记忆的**构建（Construction）**、**检索（Retrieval）**和**利用（Utilization）**视为孤立的步骤。这导致了两个主要挑战：
*   **前向路径的策略盲目性（Strategic Blindness）：** 记忆的写入和查询往往依赖局部启发式搜索，缺乏全局策略指导，导致冗余存储或检索漂移。
*   **后向路径的反馈稀疏且延迟（Sparse and Delayed Feedback）：** 只有在下游任务失败时才能发现记忆错误，此时很难追溯并修复早期的存储偏差。
**MemMA** 旨在通过多智能体协同和闭环反馈机制，将记忆操作转化为一个协调的“记忆周期”。

#### 2. 核心方法论
MemMA 采用“计划者-执行者”架构，在记忆周期的前向和后向路径上进行协调：

*   **前向路径：推理感知协调（Reasoning-Aware Coordination）**
    *   **Meta-Thinker（元思考器）：** 充当决策大脑，为记忆构建提供结构化指导（存什么、合并什么），并为检索提供“答案性诊断”（判断当前证据是否充分，若不充分则指出信息缺口）。
    *   **Memory Manager（记忆管理器）：** 根据 Meta-Thinker 的指导执行原子编辑（添加、更新、删除）。
    *   **Query Reasoner（查询推理器）：** 采用迭代的“精炼-探测”循环，根据诊断结果精准重写查询语句，而非盲目搜索。
*   **后向路径：原位自我演化（In-Situ Self-Evolution）**
    *   **探针生成（Probe Generation）：** 在每个会话结束后，自动合成针对该会话内容的探测问答对（QA Probes）。
    *   **原位验证与修复：** 立即用当前记忆回答这些探针。如果回答错误，系统会生成修复建议，并在记忆正式提交前进行语义整合（去重、合并或插入），从而实现即时的错误修正。

#### 3. 实验设计
*   **数据集：** 使用 **LoCoMo** 基准测试，这是一个专门针对长程对话记忆设计的挑战性数据集，包含多轮对话及推理型问答。
*   **评测指标：** Token 级别的 F1 分数、BLEU-1 (B1) 以及基于 LLM-as-a-Judge 的准确率 (ACC)。
*   **对比方法：** 
    *   **被动基准：** 全文本输入（Full Text）、朴素 RAG。
    *   **主动记忆系统：** LangMem、Mem0、A-Mem、LightMem。
*   **骨干网络：** 采用了 GPT-4o-mini 和 Claude-Haiku-4.5 作为智能体引擎。

#### 4. 资源与算力
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现细节：** 由于该框架主要基于 API 调用（GPT 和 Claude 系列模型）构建智能体工作流，其核心开销在于推理阶段的 Token 消耗，而非传统的模型训练算力。实验中使用了 `text-embedding-3-small` 进行向量化。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 LoCoMo 中的 10 个长对话实例，涉及近 2000 个 QA 对。
*   **消融实验：** 针对 Meta-Thinker 指导、迭代检索、自我演化修复等核心组件分别进行了消融，验证了各部分的必要性。
*   **灵活性验证：** 将 MemMA 作为插件应用于三种不同的存储后端（Single-Agent, A-Mem, LightMem），均观察到性能提升。
*   **充分性评价：** 实验设计较为全面，通过跨模型（GPT/Claude）和跨后端的测试，证明了该框架的鲁棒性和普适性，实验结果客观且具有说服力。

#### 6. 主要结论与发现
*   **协同效应显著：** MemMA 在所有指标上均优于现有基准，尤其在多跳推理（Multi-Hop）任务中提升巨大。
*   **自我演化的价值：** 原位修复机制能有效捕捉并修正构建阶段的遗漏，防止错误记忆在后续会话中累积。
*   **检索深度胜过广度：** 诊断引导的迭代检索比单纯增加检索数量（Top-K）更有效，能快速收敛并定位关键证据。
*   **即插即用：** MemMA 不依赖特定的存储架构，能显著增强现有记忆系统的性能。

#### 7. 优点与亮点
*   **闭环设计：** 首次系统性地解决了记忆周期中反馈延迟的问题，通过合成探针实现了“边构建边修复”。
*   **解耦架构：** 将高层策略推理（Meta-Thinker）与底层执行（Manager/Reasoner）分离，使系统更易于优化和扩展。
*   **即插即用：** 作为一个框架层，它可以轻松集成到现有的各种记忆增强型智能体中。

#### 8. 不足与局限
*   **成本开销：** 引入了多次 LLM 调用（生成指导、迭代检索、合成探针），在实际应用中会显著增加推理成本和延迟。
*   **会话依赖：** 后向路径依赖于清晰的“会话（Session）”边界划分，在无明显停顿的连续流式交互中可能难以应用。
*   **合成数据偏差：** 自我演化的效果高度依赖于合成探针的质量，如果生成的探针过于简单或存在偏差，修复效果会打折扣。

（完）
