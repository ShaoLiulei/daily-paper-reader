---
title: "CATFormer: When Continual Learning Meets Spiking Transformers With Dynamic Thresholds"
title_zh: CATFormer：当持续学习遇上具有动态阈值的脉冲 Transformer
authors: "Vaishnavi Nagabhushana, Kartikay Agrawal, Ayon Borthakur"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15184v1"
tags: ["query:snn"]
score: 10.0
evidence: 结合脉冲Transformer与动态阈值的持续学习
tldr: 针对脉冲神经网络（SNN）在类增量学习中面临的灾难性遗忘问题，本文提出了CATFormer框架。该框架的核心在于动态阈值LIF（DTLIF）神经元模型，通过上下文自适应阈值调节神经元兴奋性以保留旧知识，并结合门控动态头选择（G-DHS）机制实现任务无关的推理。实验证明，CATFormer在多个静态和神经形态数据集上均优于现有的无重放增量学习算法，为高效能、真实的类增量学习提供了理想的架构。
selection_source: fresh_fetch
motivation: 现有的脉冲神经网络在类增量学习中随着任务积累会出现严重的性能下滑和灾难性遗忘。
method: 引入了具有上下文自适应阈值的DTLIF神经元模型，并配合门控动态头选择机制进行任务无关推理。
result: 在CIFAR-10/100、Tiny-ImageNet及神经形态数据集上，CATFormer均优于现有的无重放类增量学习算法。
conclusion: CATFormer证明了调节神经元兴奋性是克服SNN遗忘问题的关键，是实现能效型类增量学习的理想方案。
---

## 摘要
尽管深度神经网络在受控环境中表现极佳，但在现实场景中却往往失效。在现实场景中，数据并非一次性提供，模型必须适应可能遵循也可能不遵循初始分布的新数据分布。在基于新数据的后续更新过程中，先前获得的知识会丢失，这种现象通常被称为灾难性遗忘。相比之下，大脑无论遇到多少任务，都可以在不发生此类灾难性遗忘的情况下进行学习。现有的用于类增量学习（CIL）的脉冲神经网络（SNN）随着任务的积累，性能会出现大幅下降。我们在此介绍了 CATFormer（上下文自适应阈值 Transformer），这是一个克服了上述限制的可扩展框架。我们观察到，防止 SNN 遗忘的关键不仅在于突触可塑性，还在于调节神经元的兴奋性。CATFormer 的核心是动态阈值漏电积分发放（DTLIF）神经元模型，该模型利用上下文自适应阈值作为知识保留的主要机制。这与用于任务无关推理的门控动态头选择（G-DHS）机制相结合。在静态（CIFAR-10/100/Tiny-ImageNet）和神经形态（CIFAR10-DVS/SHD）数据集上的广泛评估表明，CATFormer 在各种任务划分下均优于现有的无重放 CIL 算法，使其成为高效能、真类增量学习的理想架构。

## Abstract
Although deep neural networks perform extremely well in controlled environments, they fail in real-world scenarios where data isn't available all at once, and the model must adapt to a new data distribution that may or may not follow the initial distribution. Previously acquired knowledge is lost during subsequent updates based on new data. a phenomenon commonly known as catastrophic forgetting. In contrast, the brain can learn without such catastrophic forgetting, irrespective of the number of tasks it encounters. Existing spiking neural networks (SNNs) for class-incremental learning (CIL) suffer a sharp performance drop as tasks accumulate. We here introduce CATFormer (Context Adaptive Threshold Transformer), a scalable framework that overcomes this limitation. We observe that the key to preventing forgetting in SNNs lies not only in synaptic plasticity but also in modulating neuronal excitability. At the core of CATFormer is the Dynamic Threshold Leaky Integrate-and-Fire (DTLIF) neuron model, which leverages context-adaptive thresholds as the primary mechanism for knowledge retention. This is paired with a Gated Dynamic Head Selection (G-DHS) mechanism for task-agnostic inference. Extensive evaluation on both static (CIFAR-10/100/Tiny-ImageNet) and neuromorphic (CIFAR10-DVS/SHD) datasets reveals that CATFormer outperforms existing rehearsal-free CIL algorithms across various task splits, establishing it as an ideal architecture for energy-efficient, true-class incremental learning.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CATFormer** 的新型脉冲 Transformer 框架，旨在解决脉冲神经网络（SNN）在类增量学习（CIL）中的灾难性遗忘问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的深度学习模型（包括 SNN）在连续学习新任务时，会因为参数覆盖导致对旧知识的“灾难性遗忘”。
*   **研究背景**：虽然 SNN 具有高能效和生物合理性，但现有的 SNN 类增量学习方法在任务增加时性能下降严重。
*   **研究动机**：受大脑神经调节机制（如乙酰胆碱调节神经元兴奋性而非仅仅改变突触权重）的启发，作者试图通过调节“神经元发放阈值”而非仅仅依赖“突触可塑性”来实现知识的保留。

### 2. 核心方法论
*   **核心思想**：提出**上下文自适应阈值（Context Adaptive Threshold）**机制。在学习新任务时，冻结主干网络权重，仅通过学习特定任务的神经元阈值来适配新分布。
*   **关键技术细节**：
    *   **DTLIF 神经元模型**：在漏电积分发放（LIF）模型基础上，引入可学习的动态阈值 $\phi^{(k)}$。每个通道在不同任务下拥有独立的阈值，通过梯度下降进行优化。
    *   **两阶段训练协议**：
        1.  **任务 0**：联合训练主干网络（Backbone）、初始阈值和分类头。
        2.  **后续任务 $k$**：冻结主干网络，仅优化该任务特有的动态阈值和分类头。
    *   **门控动态头选择（G-DHS）**：为了实现任务无关（Task-agnostic）的推理，设计了一个轻量级的两层 MLP 门控网络。推理时，先通过基础特征预测任务 ID，再调用对应的阈值和分类头。

### 3. 实验设计
*   **数据集**：
    *   **静态图像**：CIFAR-10、CIFAR-100、Tiny-ImageNet（扩展至 100 个任务）。
    *   **神经形态（事件驱动）**：CIFAR10-DVS、SHD（音频脉冲数据集）。
*   **Benchmark 与对比方法**：
    *   **基准模型**：以 SpikFormer 为主干网络。
    *   **对比算法**：正则化方法（EWC, SI, MAS）、重放方法（iCaRL, DER++, RDER）以及 SNN 专用增量学习方法（DSD-SNN, ALADE-SNN）。
*   **实验场景**：涵盖了从 2 任务到 100 任务的不同粒度划分，验证长程连续学习能力。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **存储开销**：文中提到模型非常轻量，存储每个任务的阈值仅需约 64.2 KB（FP32 格式），在任务 $k > 0$ 时仅需更新约 1.4M 参数，远低于全量微调。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 CIFAR-100 上进行了 10、20、25、50 任务的划分实验。
    *   在 Tiny-ImageNet 上挑战了极端的 100 任务划分。
    *   **消融实验**：对比了固定阈值、替换 Attention 为恒等映射（SpikIdentityFormer）或随机映射、以及冻结 FFN 等变体。
*   **充分性评价**：实验设计较为充分，涵盖了静态与动态数据集，且对比了多种主流的连续学习范式。通过长序列任务（50-100 任务）的测试，证明了其在极端情况下的稳定性。

### 6. 主要结论与发现
*   **反向遗忘（Reverse Forgetting）现象**：在 CATFormer 中观察到，随着任务数量增加，平均准确率反而有所提升（例如 CIFAR-100 从 10 任务的 68.33% 升至 50 任务的 75.66%）。这说明更细粒度的任务划分有助于动态阈值精准捕捉特征。
*   **性能领先**：在无数据重放（Rehearsal-free）的约束下，CATFormer 在所有基准测试中均显著优于现有的 SNN 连续学习算法。
*   **参数高效**：通过仅学习阈值，极大地降低了连续学习过程中的计算和存储负担。

### 7. 优点（亮点）
*   **生物启发性强**：将神经调节中的“兴奋性调节”转化为“动态阈值学习”，具有较强的理论支撑。
*   **无需数据重放**：不存储旧数据，保护了隐私且节省了大量内存，适合资源受限的边缘设备。
*   **卓越的可扩展性**：在长任务序列（100 任务）中表现出极强的鲁棒性，打破了传统方法随任务增加性能骤降的魔咒。

### 8. 不足与局限
*   **推理延迟**：由于推理时需要先通过门控网络预测任务 ID，再进行二次前向传播（或切换阈值），可能会增加一定的推理延迟。
*   **门控依赖**：整体性能高度依赖门控网络预测任务 ID 的准确性，如果任务之间特征极其相似，门控错误可能导致分类失败。
*   **硬件实测缺失**：虽然理论上能效高，但论文未在真实的神经形态硬件（如 Loihi 2）上给出功耗和速度的实测数据。

（完）
