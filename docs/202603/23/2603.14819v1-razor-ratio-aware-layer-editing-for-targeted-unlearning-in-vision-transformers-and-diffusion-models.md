---
title: "RAZOR: Ratio-Aware Layer Editing for Targeted Unlearning in Vision Transformers and Diffusion Models"
title_zh: RAZOR：视觉 Transformer 和扩散模型中针对性遗忘的比例感知层编辑
authors: "Ravi Ranjan, Utkarsh Grover, Xiaomin Lin, Agoritsa Polyzou"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14819v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: Transformer骨干网络中的定向遗忘
tldr: 针对Transformer架构的视觉和扩散模型，本文提出RAZOR框架，旨在高效移除敏感信息。RAZOR通过识别对遗忘目标贡献最大且对保留知识影响最小的关键层和注意力头，采用正则化规则进行多层协同编辑。该方法在CLIP、Stable Diffusion等模型上实现了高精度、高效率的遗忘，且在量化环境下依然稳定，为模型安全合规提供了实用方案。
selection_source: fresh_fetch
motivation: 现有的模型遗忘技术在处理Transformer架构时，难以在高效移除敏感信息的同时保持模型整体性能。
method: 提出一种比例感知的轻量级框架，通过衡量各层对遗忘与保留的贡献比，动态选择并协同编辑关键层和注意力头。
result: 在身份、风格和物体擦除任务中，RAZOR在准确性、稳定性和运行速度上均优于现有方法，且支持量化模型。
conclusion: RAZOR为Transformer类视觉模型提供了一种实用、可扩展且高效的针对性遗忘解决方案。
---

## 摘要
基于 Transformer 的扩散模型和视觉语言模型取得了显著成功；然而，在不重新训练的情况下高效删除不良或敏感信息，仍然是模型安全和合规性面临的核心挑战。我们引入了 RAZOR（比例感知零/一步优化保留遗忘），这是一种轻量级、模型无关的遗忘框架，它将遗忘更新推广到 Transformer 骨干网络中协调的多层和多头编辑。RAZOR 通过衡量各层和注意力头对遗忘目标数据的贡献程度，同时保留有用知识，来识别最重要的层和注意力头。然后，它使用精心设计的正则化规则更新模型的这些部分，以避免损害整体性能。被编辑组件的集合逐渐扩大，确保了精确的遗忘，而不会出现过度编辑或损害无关能力的情况。我们在 CLIP、Stable Diffusion 和视觉语言模型 (VLM) 上评估了 RAZOR，使用了涵盖身份、风格和物体擦除任务的广泛采用的遗忘基准。我们的结果表明，即使在量化情况下，RAZOR 也能实现高度准确且稳定的遗忘。与之前的方法相比，该方法提供了更强的保留能力和更高的效率。值得注意的是，它的运行速度也明显快于传统技术。这些结果证明，RAZOR 是基于 Transformer 的视觉模型中实现安全、自适应遗忘的一种实用且可扩展的解决方案。

## Abstract
Transformer based diffusion and vision-language models have achieved remarkable success; yet, efficiently removing undesirable or sensitive information without retraining remains a central challenge for model safety and compliance. We introduce Ratio-Aware Zero/One-step Optimized Retentive unlearning (RAZOR), a lightweight, model-agnostic unlearning framework that generalizes forgetting updates to coordinated multi-layer and multi-head edits within transformer backbones. RAZOR identifies the most important layers and attention heads by measuring how much they contribute to forgetting the target data while preserving useful knowledge. Then, it updates these parts of the model using a carefully regularized rule to avoid harming overall performance. The set of edited components grows gradually, ensuring precise unlearning without over-editing or damaging unrelated capabilities. We evaluate RAZOR on CLIP, Stable Diffusion, and vision-language models (VLMs) using widely adopted unlearning benchmarks covering identity, style, and object erasure tasks. Our results show that RAZOR achieves highly accurate and stable forgetting, even under quantization. This approach offers stronger retention and better efficiency than prior methods. Notably, it also operates significant faster than conventional techniques. These results demonstrate that RAZOR is a practical and scalable solution for safe, adaptive unlearning in transformer-based vision models.

---

## 论文详细总结（自动生成）

### 论文总结：RAZOR —— 视觉 Transformer 与扩散模型中的比例感知层编辑针对性遗忘

#### 1. 核心问题与研究动机
随着大规模视觉语言模型（如 CLIP、LLaVA）和生成式模型（如 Stable Diffusion）的普及，模型在训练过程中不可避免地吸收了敏感、受版权保护或有害的信息。根据 GDPR 等法律法规，必须提供一种机制来移除这些特定知识。
*   **核心挑战**：传统的“从头训练”成本极高；现有的“机器遗忘（Machine Unlearning）”方法要么效率低下（需更新全量参数），要么精度不足（单层编辑过于脆弱，无法处理分布式存储的知识），或者在模型量化后遗忘效果会失效。
*   **研究目标**：开发一种轻量级、模型无关且对量化鲁棒的遗忘框架，在不损害模型整体性能的前提下，精确擦除特定目标。

#### 2. 方法论
RAZOR 提出了一种**比例感知（Ratio-Aware）**的多层协同编辑框架，其核心步骤如下：
*   **比例感知显著性评分（Ratio-Aware Saliency Score）**：通过计算每一层/注意力头对“遗忘损失”和“保留损失”的梯度，定义一个评分公式。该分数倾向于选择那些“遗忘压力大”且“与保留任务梯度正交（冲突小）”的层。
*   **三部分复合损失函数**：
    1.  **保留损失 ($L_{retain}$)**：确保模型在非目标数据上的性能（如 CLIP 的 InfoNCE 损失）。
    2.  **遗忘损失 ($L_{forget}$)**：通过梯度上升或余弦相似度惩罚，推开目标图像与文本的表示。
    3.  **失配正则化 ($L_{mismatch}$)**：控制编辑后的模型表示与原始模型之间的漂移，维持表示空间的稳定性。
*   **迭代精炼（Iterative Refinement）**：RAZOR 不一次性编辑所有层，而是从显著性最高的层开始，动态地增加被编辑层的数量，直到达到预设的遗忘阈值（通常在 6 次迭代内完成）。
*   **模型无关性**：该方法可直接应用于 CLIP（视觉/文本编码器）、Stable Diffusion（文本编码器）和 VLM（视觉编码器或投影层）。

#### 3. 实验设计
*   **数据集与场景**：
    *   **身份移除**：使用 CelebA 和 LAION-400M 筛选出的名人身份。
    *   **物体与风格擦除**：使用 UnlearnCanvas 基准测试。
    *   **通用能力评估**：使用 CIFAR-10、ImageNet-1K 验证模型在遗忘后的泛化能力。
    *   **多模态基准**：对 VLM 使用 MME、GQA 和 MMBench 进行综合评估。
*   **对比方法**：包括 SSD（选择性突触减弱）、SalUn（显著性遗忘）、SLUG（单层遗忘梯度）、ESD（擦除扩散）、LoTUS 等 8 种前沿方法。
*   **评估指标**：遗忘准确率 (M1/FA)、余弦相似度 (M2)、隐私泄露度 (M3)、保留准确率 (M4)、检索稳定性 (M5) 以及生成对齐指标 (UA/IRA/CRA)。

#### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA A100 (40 GB)** GPU 上进行。
*   **计算特性**：使用了混合精度推理和训练。
*   **效率表现**：RAZOR 的运行速度极快。例如在 Stable Diffusion v1.5 上，RAZOR 仅需 **78 秒**，而传统的 ESD 方法需要 6163 秒。其存储开销极小，仅需保存少量被修改层的权重（约 0.06 GB）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 CLIP (ViT-B/32, L/14)、Stable Diffusion (v1.5, v3) 和 LLaVA-1.6-8B 三类主流架构上进行了广泛测试。
*   **消融实验**：详细探讨了损失函数各组件（保留、遗忘、失配项）、层选择策略以及迭代过程对结果的影响。
*   **鲁棒性测试**：特别针对 **8-bit 和 4-bit 量化** 环境进行了测试，这是许多现有遗忘方法失效的场景。
*   **评价**：实验覆盖了从判别式到生成式模型，从基础分类到复杂多模态推理，实验设计客观且对比公平，样本量（如 4000 对图像-文本对/身份）具有统计意义。

#### 6. 主要结论与发现
*   **高效且精确**：RAZOR 在几乎所有任务中均达到了最低的遗忘准确率（接近 0%），同时保持了与原模型几乎一致的通用任务精度。
*   **量化稳定性**：RAZOR 是少数能在模型量化后依然保持遗忘效果的方法，这得益于其对关键层的精确锁定。
*   **多层优于单层**：相比于 SLUG（单层编辑），RAZOR 通过协同多层更新，解决了知识分布式存储带来的遗忘不彻底问题。
*   **帕累托最优**：通过比例因子 $\rho$，RAZOR 实现了遗忘强度与公用事业保留之间的最佳权衡。

#### 7. 优点
*   **普适性强**：一套框架通吃 Transformer 架构的各类视觉模型。
*   **极速运行**：相比微调类方法，RAZOR 实现了近乎“即时”的遗忘处理。
*   **理论支撑**：引入了梯度正交性和比例感知的显著性评分，比单纯依赖梯度大小的方法更科学。
*   **资源友好**：极低的显存和存储占用，适合部署在实际生产环境。

#### 8. 不足与局限
*   **架构限制**：目前主要针对 Transformer 骨干网络，对于非 Transformer 架构（如纯 CNN 或新型状态空间模型）的适用性尚未验证。
*   **对抗攻击风险**：虽然在量化下表现稳健，但论文未深入探讨在恶意对抗样本诱导下，已遗忘的知识是否会被重新激活。
*   **超参数敏感性**：比例因子 $\rho$ 和学习率对结果有一定影响，可能需要针对不同模型进行微调。

（完）
