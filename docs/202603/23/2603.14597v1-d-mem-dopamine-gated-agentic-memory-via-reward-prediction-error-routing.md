---
title: "D-MEM: Dopamine-Gated Agentic Memory via Reward Prediction Error Routing"
title_zh: D-MEM：通过奖励预测误差路由实现的多巴胺门控智能体记忆
authors: "Yuru Song, Qi Xin"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14597v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 受生物启发的大语言模型智能体记忆架构
tldr: "针对自主LLM智能体长期记忆系统存在的O(N^2)写入延迟和高昂Token成本问题，本文提出受生物启发的多巴胺门控记忆架构D-MEM。该系统通过奖励预测误差（RPE）路由机制，将短期交互与认知重构解耦。低RPE的常规输入进入快速缓存，而高RPE的惊喜或高价值输入则触发“多巴胺”信号，激活知识图谱演化。实验表明，D-MEM在降低80%成本的同时，显著提升了推理能力和抗噪性。"
selection_source: fresh_fetch
motivation: 现有的智能体记忆系统在处理长期交互时面临计算复杂度过高和Token消耗巨大的扩展性瓶颈。
method: 引入基于奖励预测误差的快慢路由系统，通过轻量级评论器筛选高价值信息以触发选择性的记忆重构。
result: "D-MEM在减少80%以上Token消耗的同时，消除了计算瓶颈，并在多步推理和抗干扰能力上优于基准模型。"
conclusion: 通过选择性地门控认知重构过程，D-MEM为构建高效、可扩展的终身学习智能体记忆提供了新方案。
---

## 摘要
自主大语言模型（LLM）智能体需要结构化的长期记忆，但目前的“追加与演进”系统（如 A-MEM）面临 O(N^2) 的写入延迟和过高的 token 成本。我们提出了 D-MEM（多巴胺门控智能体记忆），这是一种受生物学启发的架构，通过基于奖励预测误差（RPE）的快/慢路由系统，将短期交互与认知重构解耦。一个轻量级的评论家路由器（Critic Router）评估刺激的惊喜度（Surprise）和效用（Utility）。常规的、低 RPE 的输入会被绕过或缓存在 O(1) 的快速访问缓冲区中。相反，高 RPE 输入（如事实矛盾或偏好转变）会触发“多巴胺”信号，激活 O(N) 记忆演进流水线，以重塑智能体的知识图谱。为了在现实条件下评估性能，我们引入了 LoCoMo-Noise 基准测试，该基准在长期会话中注入受控的对话噪声。评估结果表明，D-MEM 减少了超过 80% 的 token 消耗，消除了 O(N^2) 瓶颈，并在多跳推理和对抗韧性方面优于基准模型。通过选择性地对认知重构进行门控，D-MEM 为终身智能体记忆提供了一个可扩展且具有成本效益的基础。

## Abstract
Autonomous LLM agents require structured long-term memory, yet current "append-and-evolve" systems like A-MEM face O(N^2) write-latency and excessive token costs. We introduce D-MEM (Dopamine-Gated Agentic Memory), a biologically inspired architecture that decouples short-term interaction from cognitive restructuring via a Fast/Slow routing system based on Reward Prediction Error (RPE). A lightweight Critic Router evaluates stimuli for Surprise and Utility. Routine, low-RPE inputs are bypassed or cached in an O(1) fast-access buffer. Conversely, high-RPE inputs, such as factual contradictions or preference shifts, trigger a "dopamine" signal, activating the O(N) memory evolution pipeline to reshape the agent's knowledge graph. To evaluate performance under realistic conditions, we introduce the LoCoMo-Noise benchmark, which injects controlled conversational noise into long-term sessions. Evaluations demonstrate that D-MEM reduces token consumption by over 80%, eliminates O(N^2) bottlenecks, and outperforms baselines in multi-hop reasoning and adversarial resilience. By selectively gating cognitive restructuring, D-MEM provides a scalable, cost-efficient foundation for lifelong agentic memory.

---

## 论文详细总结（自动生成）

### D-MEM: 基于奖励预测误差路由的多巴胺门控智能体记忆论文总结

#### 1. 核心问题与研究背景
*   **核心问题**：现有的自主大语言模型（LLM）智能体长期记忆框架（如 A-MEM）通常采用“同步追加并演化所有记忆”的模式。这意味着每一句对话（无论是否有价值）都会触发复杂的记忆构建和全局知识图谱更新，导致 **$O(N^2)$ 的计算复杂度**、高昂的 API Token 成本、严重的写入延迟，以及由于对话噪声（如寒暄、废话）导致的上下文窗口污染。
*   **研究背景**：智能体需要从长期交互中提取知识，但人类大脑并不会记录所有感官输入。受生物学启发，作者试图在智能体记忆中引入类似哺乳动物大脑中由多巴胺驱动的过滤机制，以解决扩展性瓶颈。

#### 2. 方法论
D-MEM 的核心思想是**将短期交互与长期认知重构解耦**，引入了一个受生物学启发的“快/慢路由系统”。
*   **智能体奖励预测误差 (Agentic RPE)**：借鉴中脑腹侧被盖区（VTA）的多巴胺释放机制，定义 RPE 为**语义惊喜度 (Surprise)** 和 **长期效用 (Utility)** 的乘积。
    *   **语义惊喜度**：通过计算当前输入与现有记忆嵌入向量的余弦距离，并进行 Z-score 标准化，以克服嵌入模型的各向异性问题。
    *   **长期效用**：使用轻量级 LLM 调用，将输入分类为瞬时（Transient）、短期（Short-term）或持久（Persistent）。
*   **三层分级路由机制**：
    1.  **SKIP (跳过)**：RPE 极低（如“谢谢”、“好的”），直接绕过记忆演进，存入 $O(1)$ 的**影子缓冲区 (Shadow Buffer)**。
    2.  **CONSTRUCT_ONLY (仅构建)**：RPE 中等，仅生成原子记忆节点并存入短期缓冲区，不触发复杂的图谱链接，复杂度为 $O(1)$。
    3.  **FULL_EVOLUTION (全演进)**：RPE 极高（如事实矛盾或偏好改变），触发“多巴胺释放”，执行 $O(N)$ 的深度记忆重构和知识图谱更新。
*   **零成本检索增强**：结合 BM25 稀疏检索与向量检索（RRF 融合），并利用影子缓冲区应对对抗性查询，防止“记忆缺失”。

#### 3. 实验设计
*   **数据集与基准测试**：
    *   **LoCoMo**：标准的长期记忆评估数据集。
    *   **LoCoMo-Noise (本文提出)**：针对现实场景设计的基准，在原始会话中注入了 75% 的受控噪声（包括填充语、状态更新、离题话），模拟真实的人机交互。
*   **对比方法**：包括 A-MEM (SOTA 同步演进框架)、MemGPT (操作系统式分页记忆)、MemoryBank (基于遗忘曲线)、ReadAgent 以及全上下文 (Full Context) 上限。
*   **评估指标**：F1 分数、BLEU-1，涵盖单跳、多跳、时间推理、开放域及对抗性任务。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：实验主要基于 **GPT-4o-mini** 作为骨干模型（Backbone），通过 API 调用实现。由于 D-MEM 侧重于推理侧的架构优化而非模型训练，其核心贡献在于减少 API Token 消耗和推理延迟。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在标准 LoCoMo 和高噪声 LoCoMo-Noise 上进行了全面对比。
    *   进行了详细的**消融实验**（对比了线性 RPE、严格剪枝、安全网机制等四个版本）。
    *   提供了 700 轮对话的 RPE 分解可视化和 UMAP 记忆流形分析。
*   **充分性评价**：实验设计较为充分，特别是针对“噪声韧性”的测试非常贴近实际应用。通过 UMAP 可视化证明了记忆空间的结构稳定性，增强了结果的说服力。

#### 6. 主要结论与发现
*   **极高的效率**：相比 A-MEM，D-MEM **减少了 80% 以上的 Token 消耗**，彻底消除了 $O(N^2)$ 的写入瓶颈。
*   **性能反超**：在极端噪声环境下（75% 噪声），D-MEM 的多跳推理能力显著优于 A-MEM（F1 提升约 15.7%），证明了“选择性记忆”能保持知识图谱的纯净度。
*   **抗干扰能力**：通过影子缓冲区机制，D-MEM 在对抗性查询（询问被跳过的废话内容）中表现稳健。

#### 7. 优点与亮点
*   **生物启发式创新**：成功将神经科学中的 RPE 和多巴胺门控概念转化为可计算的 LLM 路由逻辑。
*   **解决实际痛点**：直接针对长期记忆系统“越用越贵、越用越慢、越用越乱”的扩展性难题。
*   **鲁棒性设计**：引入 Z-score 归一化解决嵌入向量聚集问题，以及冷启动保护机制，体现了工程上的严谨性。

#### 8. 不足与局限
*   **单跳任务性能下降**：在标准数据集上，D-MEM 的单跳事实检索性能低于 A-MEM。这是因为过于激进的效用过滤可能误删了一些简单的孤立事实。
*   **阈值敏感性**：路由效果高度依赖于预设的 RPE 阈值（$\theta_{low}, \theta_{high}$），在不同场景下可能需要手动调优。
*   **计算开销转移**：虽然减少了演进开销，但每轮对话仍需一次轻量级的 LLM 调用来评估效用，对于极低延迟要求的场景仍有优化空间（如使用更小的蒸馏模型替代）。

（完）
