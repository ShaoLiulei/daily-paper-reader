---
title: A Family of LLMs Liberated from Static Vocabularies
title_zh: 一系列摆脱静态词汇表限制的大语言模型
authors: "Aleph Alpha, :, Adnen Abdessaied, Artur Baranowski, Lukas Balles, Michael Barlow, Fabien C. Y. Benureau, Felix Berkenkamp, Lukas Bluebaum, Bastian Boll, Thomas F. Burns, Björn Deiseroth, Constantin Eichenberg, David Friede, Pablo Iyu Guerrero, Ahmed Hammam, Bastian Harren, Johann Higl, Yasser Jadidi, Carina Kauf, Johannes Messner, Jan Hendrik Metzen, Max Meuer, Vedant Nanda, Pit Neitemeier, Koen Oostermeijer, Letitia Parcalabescu, Markus Pernpointner, Felix Reinfurt, Dylan Rodriquez, Grégory Schott, Philipp Siedler, Martin Simonovsky, Till Speicher, Volker Stampa, Stephan Wäldchen, Samuel Weinbach, Gregor Ziegltrum"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15953v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 用于大语言模型的分层自回归 Transformer 架构
tldr: 本研究针对传统大语言模型分词器词表固定、跨领域适应性差的问题，提出了一种名为层次化自回归变换器（HAT）的架构。该架构通过编码器将字节聚合为单词嵌入，经由主干网络处理后由解码器还原为字节，实现了无分词器的字节级建模。研究团队不仅从零预训练了7B模型，还成功将Llama 3.1系列模型转换为HAT架构。实验证明，该方法在提高文本压缩率和鲁棒性的同时，在多项基准测试中超越了原始Llama 3.1模型。
selection_source: fresh_fetch
motivation: 传统分词器存在词表庞大且固定、难以适应新领域或语言以及对拼写变化鲁棒性差等局限性。
method: 提出HAT架构，利用编码器将字节流聚合为单词嵌入并输入主干网络，最后通过解码器将其还原为字节。
result: 转换后的Llama 3.1 HAT模型和从零预训练的7B模型在英德双语及多项基准测试中表现优异，超越了原始模型。
conclusion: HAT架构成功摆脱了静态词表的限制，在提升模型灵活性和鲁棒性的同时保持了极高的性能水平。
---

## 摘要
分词（Tokenization）是当前大语言模型（LLMs）中自然语言处理的核心组件，使模型能够将原始文本转换为可处理的单元。尽管学习型分词器被广泛采用，但它们表现出显著的局限性，包括庞大且固定的词汇表大小，以及对新领域或语言的适应性较差。我们提出了一系列基于分层自回归 Transformer（HAT）架构的模型，参数量最高达 700 亿。在 HAT 中，编码器 Transformer 将字节聚合为词嵌入（word embeddings），然后将其输入到主干网络（一个经典的自回归 Transformer）中。主干网络的输出随后由解码器进行交叉注意力处理，并转换回字节。我们展示了可以通过将 Llama 3.1 8B 和 70B 模型转换为 HAT 架构来重用现有的预训练模型：Llama-3.1-8B-TFree-HAT 和 Llama-3.1-70B-TFree-HAT 是字节级模型，其编码器和解码器是从头开始训练的，但我们调整了预训练的 Llama 主干网络（即移除了嵌入矩阵和头部的 Transformer 块），以处理词嵌入而非原始 token。我们还提供了一个 7B 的 HAT 模型 Llama-TFree-HAT-Pretrained，它是在近 4 万亿个单词上完全从头开始训练的。HAT 架构通过减少所需的序列位置数量提高了文本压缩率，并增强了对词内变体（如拼写差异）的鲁棒性。通过预训练以及随后在英语和德语中进行的监督微调和直接偏好优化，我们展示了在两种语言中的强大能力，在大多数基准测试中都优于原始的 Llama 3.1。我们在 Hugging Face 上发布了我们的模型（包括 200 个预训练检查点）。

## Abstract
Tokenization is a central component of natural language processing in current large language models (LLMs), enabling models to convert raw text into processable units. Although learned tokenizers are widely adopted, they exhibit notable limitations, including their large, fixed vocabulary sizes and poor adaptability to new domains or languages. We present a family of models with up to 70 billion parameters based on the hierarchical autoregressive transformer (HAT) architecture. In HAT, an encoder transformer aggregates bytes into word embeddings and then feeds them to the backbone, a classical autoregressive transformer. The outputs of the backbone are then cross-attended by the decoder and converted back into bytes. We show that we can reuse available pre-trained models by converting the Llama 3.1 8B and 70B models into the HAT architecture: Llama-3.1-8B-TFree-HAT and Llama-3.1-70B-TFree-HAT are byte-level models whose encoder and decoder are trained from scratch, but where we adapt the pre-trained Llama backbone, i.e., the transformer blocks with the embedding matrix and head removed, to handle word embeddings instead of the original tokens. We also provide a 7B HAT model, Llama-TFree-HAT-Pretrained, trained entirely from scratch on nearly 4 trillion words. The HAT architecture improves text compression by reducing the number of required sequence positions and enhances robustness to intra-word variations, e.g., spelling differences. Through pre-training, as well as subsequent supervised fine-tuning and direct preference optimization in English and German, we show strong proficiency in both languages, improving on the original Llama 3.1 in most benchmarks. We release our models (including 200 pre-training checkpoints) on Hugging Face.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Family of LLMs Liberated from Static Vocabularies》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统大语言模型（LLM）极度依赖预定义的静态分词器（Tokenizer，如 BPE）。这种机制存在三大弊端：
    1.  **词表固定且庞大**：嵌入矩阵和输出头占据了大量参数（如 Llama 3.1 8B 中约 13% 的参数用于词表），且难以扩展新语言或专业术语。
    2.  **缺乏鲁棒性**：对拼写错误、大小写变化或字符级扰动非常敏感。
    3.  **跨语言不平等**：不同语言的压缩率差异巨大，导致非英语语种的推理成本更高。
*   **整体含义**：本研究旨在通过一种“无分词器”（Tokenizer-Free）的层次化架构，使模型直接处理原始字节流，同时保持大规模 Transformer 的计算效率，并证明这种架构可以成功继承现有预训练模型（如 Llama 3.1）的知识。

### 2. 论文提出的方法论
核心思想是采用**分层自回归 Transformer（HAT）**架构，将文本处理分为三个阶段：
*   **编码器（Encoder）**：字节级 Transformer。它接收原始 UTF-8 字节序列，使用局部因果注意力生成字节嵌入。
*   **单词分割与聚合（Word Splitting & Aggregation）**：利用基于规则的分割器（如 UAX#29）将字节流划分为变长的“单词”块。通过交叉注意力机制，用一个可学习的查询向量（Query）将每个单词块内的字节嵌入聚合为一个单一的“单词嵌入”。
*   **主干网络（Backbone）**：单词级 Transformer。这是模型的核心，处理由编码器提供的单词嵌入序列。它可以直接复用 Llama 3.1 等现有模型的 Transformer 块。
*   **解码器（Decoder）**：字节级 Transformer。它通过交叉注意力结合主干网络的输出，以自回归方式逐个预测单词内部的下一个字节。
*   **关键技术细节**：
    *   **HATification（HAT 化）**：将预训练好的 Llama 3.1 移除其原始嵌入层和输出头，仅保留中间块作为 Backbone，然后为其训练新的 Encoder 和 Decoder。
    *   **双 KV 缓存管理**：在推理时同时维护字节级和单词级的键值缓存，以支持高效生成。

### 3. 实验设计
*   **模型版本**：
    *   `Llama-TFree-HAT-Pretrained-7B`：从零开始预训练。
    *   `Llama-3.1-8B/70B-TFree-HAT`：基于 Llama 3.1 转换而来。
*   **数据集**：
    *   **预训练**：混合了英语（70%）、德语（7%）、数学（5%）和编程代码（18%）的精选语料。
    *   **微调**：约 200 万条 SFT 样本（含合成数据）及 DPO 偏好对齐数据。
*   **Benchmark（基准测试）**：
    *   **知识与推理**：MMLU, MMLU-Pro, GPQA, ARC, HellaSwag。
    *   **德语能力**：MMMLU, German ARC, Wino-X DE 等。
    *   **长文本与安全**：QuALITY, Ada-LEval, WinoGender。
*   **对比方法**：原始 Llama 3.1 (8B/70B) Base 及 Instruct 版本、AllenAI 的 Tülu 3 系列。

### 4. 资源与算力
*   **算力设施**：使用了 NVIDIA A100、H100 和 H200 GPU 集群。
*   **训练规模**：
    *   **7B 从头预训练**：在近 **4 万亿（4T）**个单词上训练。
    *   **8B 转换训练**：在 134B 单词上进行初步适配，随后进行 20B 单词的长文本适配。
    *   **70B 转换训练**：在 108B 单词上进行初步适配。
*   **环境影响**：强调基础设施 100% 使用可再生能源，PUE ≤ 1.2。

### 5. 实验数量与充分性
*   **实验广度**：涵盖了从基础模型（Base）、指令微调（SFT）到偏好对齐（DPO）的全流程实验。
*   **消融与对比**：对比了从零训练与“HAT 化”两种路径，并针对 8B 和 70B 两个量级进行了验证。
*   **客观性**：实验不仅关注学术指标，还引入了 MT-Bench 胜率测试和文本压缩率分析。
*   **充分性评价**：实验设计非常充分且严谨。作者不仅发布了最终模型，还公开了 **200 个预训练中间检查点**供社区研究学习动态，这在同类论文中极为罕见。

### 6. 主要结论与发现
*   **性能超越**：转换后的 HAT 模型在大多数英语和德语基准测试中优于原始 Llama 3.1。
*   **参数效率**：8B HAT 模型的总参数量比原始 Llama 3.1 8B **减少了 10% 以上**（约 8.4 亿参数），因为移除了庞大的词表嵌入层。
*   **更高压缩率**：HAT 架构在 Backbone 的每个序列位置上承载的字节数更多（如德语任务中压缩率提升近一倍），这意味着在相同上下文窗口内能处理更多信息。
*   **鲁棒性**：由于直接操作字节，模型对拼写变体和字符级噪声具有天然的抵抗力。

### 7. 优点
*   **架构创新**：成功实现了“无分词器”与“重用预训练权重”的结合，解决了字节级模型通常计算开销过大的问题。
*   **开源贡献极大**：不仅开源模型权重，还开源了评估框架、推理引擎（vLLM 适配）和 200 个训练中间检查点。
*   **多语言潜力**：展示了在德语等非英语语种上极高的压缩效率和性能表现。

### 8. 不足与局限
*   **推理吞吐量**：尽管在 vLLM 上做了优化，但由于分层架构导致的异步生成模式，目前的批处理吞吐量仍低于标准的 Tokenizer-based 模型。
*   **任务覆盖**：作者明确指出未针对代码生成和数学推理进行专门优化，因此在这些领域的表现可能不是最优。
*   **硬件适配**：分层架构对现有的 Flash Attention 等高性能算子的支持需要特殊修改，增加了工程实现的复杂性。

（完）
