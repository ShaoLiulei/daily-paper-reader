---
title: "S3T-Former: A Purely Spike-Driven State-Space Topology Transformer for Skeleton Action Recognition"
title_zh: S3T-Former：一种用于骨骼动作识别的纯脉冲驱动状态空间拓扑 Transformer
authors: "Naichuan Zheng, Hailun Xia, Zepeng Sun, Weiyi Li, Yujia Wang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18062v2"
tags: ["query:snn"]
score: 10.0
evidence: 纯脉冲驱动的状态空间拓扑Transformer (S3T-Former)
tldr: 针对骨骼动作识别中人工神经网络功耗高及现有脉冲神经网络（SNN）稀疏性不足的问题，本文提出了首个纯脉冲驱动的Transformer架构S3T-Former。该模型通过多流解剖脉冲嵌入将特征转化为稀疏事件流，并利用侧向脉冲拓扑路由和脉冲状态空间引擎实现高效的拓扑与长程时间建模。实验证明，该方法在保持高精度的同时显著降低了能耗，达到了能效动作识别的新最先进水平。
selection_source: fresh_fetch
motivation: 现有的骨骼动作识别SNN模型因依赖密集矩阵运算和非稀疏变换，破坏了脉冲神经网络固有的稀疏性且难以处理长程时间依赖。
method: 提出了S3T-Former架构，利用多流解剖脉冲嵌入、侧向脉冲拓扑路由和脉冲状态空间引擎实现纯脉冲驱动的特征提取与建模。
result: 在多个大规模数据集上，该模型在实现高精度识别的同时，理论能耗远低于传统人工神经网络，达到了新的SOTA水平。
conclusion: S3T-Former证明了纯脉冲驱动架构在兼顾高精度与低功耗骨骼动作识别方面的有效性，为边缘设备部署提供了新方案。
---

## 摘要
基于骨骼的动作识别对于多媒体应用至关重要，但其严重依赖高功耗的人工神经网络 (ANN)，限制了在资源受限的边缘设备上的部署。脉冲神经网络 (SNN) 提供了一种节能的替代方案；然而，现有的骨骼数据脉冲模型往往通过采用密集矩阵聚合、沉重的多模态融合模块或非稀疏的频域变换，损害了 SNN 固有的稀疏性。此外，它们还深受脉冲神经元短期遗忘问题的困扰。在本文中，我们提出了脉冲状态空间拓扑 Transformer (S3T-Former)，据我们所知，这是首个专门为节能骨骼动作识别设计的纯脉冲驱动 Transformer 架构。我们没有依赖沉重的融合开销，而是构建了一种多流解剖脉冲嵌入 (M-ASE)，作为广义运动学微分算子，将多模态骨骼特征优雅地转换为异构且高度稀疏的事件流。为了实现真正的拓扑和时间稀疏性，我们引入了用于按需条件脉冲传播的侧向脉冲拓扑路由 (LSTR)，以及一个脉冲状态空间 (S3) 引擎，用以系统地捕获长程时间动态，而无需非稀疏的频谱变通方案。在多个大规模数据集上的广泛实验表明，与经典的 ANN 相比，S3T-Former 在理论上降低能耗的同时，实现了极具竞争力的准确率，为节能神经形态动作识别树立了新的最先进水平。

## Abstract
Skeleton-based action recognition is crucial for multimedia applications but heavily relies on power-hungry Artificial Neural Networks (ANNs), limiting their deployment on resource-constrained edge devices. Spiking Neural Networks (SNNs) provide an energy-efficient alternative; however, existing spiking models for skeleton data often compromise the intrinsic sparsity of SNNs by resorting to dense matrix aggregations, heavy multimodal fusion modules, or non-sparse frequency domain transformations. Furthermore, they severely suffer from the short-term amnesia of spiking neurons. In this paper, we propose the Spiking State-Space Topology Transformer (S3T-Former), which, to the best of our knowledge, is the first purely spike-driven Transformer architecture specifically designed for energy-efficient skeleton action recognition. Rather than relying on heavy fusion overhead, we formulate a Multi-Stream Anatomical Spiking Embedding (M-ASE) that acts as a generalized kinematic differential operator, elegantly transforming multimodal skeleton features into heterogeneous, highly sparse event streams. To achieve true topological and temporal sparsity, we introduce Lateral Spiking Topology Routing (LSTR) for on-demand conditional spike propagation, and a Spiking State-Space (S3) Engine to systematically capture long-range temporal dynamics without non-sparse spectral workarounds. Extensive experiments on multiple large-scale datasets demonstrate that S3T-Former achieves highly competitive accuracy while theoretically reducing energy consumption compared to classic ANNs, establishing a new state-of-the-art for energy-efficient neuromorphic action recognition.

---

## 论文详细总结（自动生成）

以下是对论文《S3T-Former: A Purely Spike-Driven State-Space Topology Transformer for Skeleton Action Recognition》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
基于骨骼的动作识别在具身智能和扩展现实（XR）中具有重要意义。目前主流方法依赖人工神经网络（ANN），其密集的浮点乘加运算（MAC）导致高功耗，难以部署在电池驱动的边缘设备上。脉冲神经网络（SNN）通过离散脉冲通信，将高能耗的 MAC 替换为低能耗的累加（AC）运算，是理想的替代方案。
然而，现有的骨骼识别 SNN 模型存在三大瓶颈：
*   **感知能力弱**：难以捕捉复杂的运动学动态。
*   **伪稀疏性**：为了性能往往采用密集矩阵聚合或非稀疏变换，破坏了 SNN 的计算优势。
*   **短期遗忘**：脉冲神经元固有的指数衰减导致其难以处理长序列的时间因果关系。
**S3T-Former** 的提出旨在构建首个**纯脉冲驱动**的 Transformer 架构，在保持极高稀疏性的同时解决上述问题。

### 2. 方法论：核心思想与关键技术
S3T-Former 模仿生物视觉系统（如视网膜神经节细胞和 M/P 细胞的分工），设计了四个核心模块：

*   **多流解剖脉冲嵌入 (M-ASE)**：
    *   **核心思想**：将其视为广义运动学微分算子。
    *   **技术细节**：不仅提取原始坐标（零阶），还计算时间梯度（一阶时间导数，即速度）和空间梯度（一阶空间导数，即骨骼向量）。通过 LIF 神经元将这些多阶动态转化为稀疏的事件流，使模型能像生物视觉一样仅对“变化”做出反应。
*   **非对称时间梯度 QKV (ATG-QKV)**：
    *   **核心思想**：模拟视觉系统中 M 细胞（感知动态）和 P 细胞（感知静态结构）的分工。
    *   **技术细节**：强制查询（Query）和键（Key）仅在动作梯度上触发脉冲，而值（Value）保留静态结构特征。这种非对称设计极大地抑制了静态背景冗余，将脉冲发射率降低了 1-2 个数量级。
*   **侧向脉冲拓扑路由 (LSTR)**：
    *   **核心思想**：将 $O(V^2)$ 的密集注意力计算转化为基于解剖图的条件传播。
    *   **技术细节**：利用人体骨骼的物理连接作为先验，通过多头动态拓扑矩阵引导脉冲传播。在硬件实现上，这使空间聚合退化为极低功耗的条件稀疏加法（Zero-MAC）。
*   **脉冲状态空间 (S3) 引擎与 U-Readout**：
    *   **核心思想**：引入线性复杂度的状态空间模型（SSM）来治愈 SNN 的“短期遗忘症”。
    *   **技术细节**：构建非递归的时间记忆池，系统地累积历史拓扑特征。最后使用 **U-Readout**（不发放脉冲的 IF 积分器）直接利用末端膜电位进行分类，避免了传统脉冲计数带来的量化误差。

### 3. 实验设计
*   **数据集**：
    *   **NTU RGB+D 60**：包含 60 类动作，采用 Cross-Subject (X-Sub) 和 Cross-View (X-View) 协议。
    *   **NTU RGB+D 120**：目前规模最大、最困难的骨骼数据集，包含 120 类动作。
    *   **NW-UCLA**：用于验证跨视角识别能力。
*   **Benchmark 与对比方法**：
    *   **ANN 模型**：ST-GCN, 2S-AGCN, CTR-GCN, Hyperformer, SkateFormer 等。
    *   **SNN 模型**：Spikformer, Spike-driven Transformer V2, MK-SGN, Signal-SGN 等。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **4 张 NVIDIA Tesla V100 GPU** 进行分布式训练。
*   **训练配置**：采用 AdamW 优化器，初始学习率 0.01，训练 250 个 epoch，配合余弦退火调度和自动混合精度（AMP）技术。
*   **仿真设置**：脉冲仿真时间窗口 $T$ 设置为 8、16 或 32。

### 5. 实验数量与充分性
论文进行了非常详尽的实验验证：
*   **性能对比**：在三个数据集的多个协议下与 SOTA 方法进行了全面对比。
*   **消融实验**：逐步验证了 M-ASE、S3-Engine、LSTR、ATG-QKV 和 U-Readout 的增量贡献（Top-1 准确率从基准的 76.32% 提升至 83.80%）。
*   **可视化分析**：包括拓扑路由矩阵的可视化、层级脉冲热力图（展示“语义稀疏漏斗”效应）、t-SNE 特征分布以及输出置信度曲线。
*   **能效分析**：提供了基于 45nm CMOS 技术的理论能耗计算。
实验设计客观、公平，对比基准涵盖了最新的研究成果，充分证明了方法的有效性。

### 6. 主要结论与发现
*   **性能突破**：S3T-Former 刷新了 SNN 在骨骼识别领域的记录，在 NTU-120 数据集上比之前的 SOTA（Signal-SGN）高出 **6.38%**。
*   **超越部分 ANN**：在纯准确率上，S3T-Former 成功超越了经典的 ANN 模型（如 ST-GCN），打破了 SNN 性能必然弱于 ANN 的固有印象。
*   **极高能效**：在实现高精度的同时，其理论能耗**不足同等规模 ANN 的 10%**。
*   **语义聚焦**：可视化显示，随着网络加深，脉冲会自发地从全身物理运动聚焦到关键的语义端点（如踢球时的脚踝）。

### 7. 优点
*   **纯脉冲驱动**：核心计算完全由脉冲触发，不含任何隐藏的密集 MAC 运算，符合神经形态计算的本质。
*   **生物启发性强**：ATG-QKV 和 M-ASE 的设计具有明确的生物学对应关系，解释性强。
*   **解决长程依赖**：通过 S3 引擎成功克服了 SNN 处理长序列时的遗忘问题。
*   **端到端优化**：U-Readout 和 TET 损失函数的结合提高了训练的收敛性和信息保真度。

### 8. 不足与局限
*   **微小动作识别困难**：混淆矩阵显示，模型在区分“读书”、“写字”、“玩手机”等仅涉及手指/手腕微小位移的动作时表现较弱。这是因为激进的梯度稀疏化过滤掉了这些微弱的静态空间线索。
*   **硬件依赖**：虽然理论能效极高，但其优势需要在专门的神经形态硬件（如类脑芯片）上才能完全体现，在传统 GPU 上由于缺乏对稀疏 AC 运算的底层优化，推理速度可能并不占优。
*   **超参数敏感性**：如 LSTR 中的缩放因子 $\gamma$ 和 S3 引擎中的衰减因子 $\lambda$ 需要精细调优以平衡物理先验与数据驱动的灵活性。

（完）
