---
title: Semantic Token Clustering for Efficient Uncertainty Quantification in Large Language Models
title_zh: 大语言模型中高效不确定性量化的语义 Token 聚类
authors: "Qi Cao, Andrew Gambardella, Takeshi Kojima, Yutaka Matsuo, Yusuke Iwasawa"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20161v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型的不确定性量化算法
tldr: 大语言模型常出现不实或过度自信的输出，而现有的不确定性量化方法因依赖多次采样或辅助模型而计算开销巨大。本文提出语义标记聚类（STC），通过嵌入聚类和前缀匹配将标记划分为语义一致的簇，并利用簇的概率质量在单次生成中实现高效的不确定性量化。实验证明 STC 在显著降低计算成本的同时，达到了与最先进方法相当的性能。
selection_source: fresh_fetch
motivation: 针对现有不确定性量化方法因多次采样或依赖辅助模型而导致的计算效率低下问题。
method: 提出 STC 方法，利用嵌入聚类和前缀匹配将语义相近的标记成簇，并基于簇概率进行量化。
result: STC 在不增加额外模型负担的情况下，实现了与 SOTA 基准相当的性能并大幅减少了开销。
conclusion: 该研究为 LLM 提供了一种无需多次采样、高效且可靠的单次生成不确定性量化方案。
---

## 摘要
大语言模型（LLMs）在多种任务中展现了卓越的能力。然而，其输出的真实性无法得到保证，且其过度自信的倾向进一步限制了可靠性。不确定性量化为识别潜在不可靠输出提供了一种有前景的方法，但大多数现有方法依赖于重复采样或辅助模型，从而引入了巨大的计算开销。为了解决这些局限性，我们提出了语义 Token 聚类（STC），这是一种利用 LLMs 固有编码的语义信息的高效不确定性量化方法。具体而言，我们利用嵌入聚类和前缀匹配将 Token 分组为语义一致的簇，并基于相应语义簇上聚合的概率质量来量化不确定性。我们的方法仅需单次生成，且不依赖于辅助模型。实验结果表明，STC 在显著降低计算开销的同时，实现了与最先进基准模型相当的性能。

## Abstract
Large language models (LLMs) have demonstrated remarkable capabilities across diverse tasks. However, the truthfulness of their outputs is not guaranteed, and their tendency toward overconfidence further limits reliability. Uncertainty quantification offers a promising way to identify potentially unreliable outputs, but most existing methods rely on repeated sampling or auxiliary models, introducing substantial computational overhead. To address these limitations, we propose Semantic Token Clustering (STC), an efficient uncertainty quantification method that leverages the semantic information inherently encoded in LLMs. Specifically, we group tokens into semantically consistent clusters using embedding clustering and prefix matching, and quantify uncertainty based on the probability mass aggregated over the corresponding semantic cluster. Our approach requires only a single generation and does not depend on auxiliary models. Experimental results show that STC achieves performance comparable to state-of-the-art baselines while substantially reducing computational overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic Token Clustering for Efficient Uncertainty Quantification in Large Language Models》的结构化分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLMs）虽然强大，但常表现出“过度自信”并产生幻觉。现有的不确定性量化（UQ）方法存在严重的性能与效率权衡：
    *   **采样法**（如 Semantic Entropy）：需要多次生成，计算开销巨大。
    *   **辅助模型法**（如使用 NLI 模型）：依赖外部模型判断语义一致性，引入额外延迟。
*   **研究动机**：开发一种**单次生成（Single-generation）**、**无需辅助模型**且能**捕捉语义一致性**的高效不确定性量化方法。

### 2. 方法论：语义 Token 聚类 (STC)
STC 的核心思想是利用 LLM 内部已有的语义表示（Embedding）来识别语义相近的标记，从而在单次解码中聚合概率。
*   **预计算阶段（离线）**：
    *   将模型的输入嵌入（Input Embedding）和输出嵌入（LM Head 权重）拼接，形成统一的语义表示。
    *   使用**层次聚类（Agglomerative Clustering）**算法将词表中的所有 Token 划分为 $K$ 个语义簇（默认 $K=16,000$）。
*   **推理阶段（在线）**：
    *   在每个解码步骤 $i$，不仅考虑生成的 Token $y_i$ 的概率，还聚合与其属于同一语义簇的其他候选 Token 的概率。
    *   **前缀匹配（Prefix Matching）**：为了弥补分词器（Tokenizer）可能将同一单词切碎的问题，将与后续生成内容具有前缀一致性的 Token 也纳入聚合范围。
*   **量化公式**：
    *   计算步骤概率：$\hat{p}_c = \sum_{t \in \text{Cluster} \cup \text{Prefix}} p(t)$。
    *   序列不确定性得分：$S(x, y) = 1 - \prod_{i=1}^n \hat{p}_c(y_i)$。

### 3. 实验设计
*   **数据集**：TriviaQA (TQA)、Natural Questions (NQ)、WebQuestions (WQ)，涵盖了事实性问答场景。
*   **评估模型**：Llama-2-7B、Llama-3-8B、Mistral-7B-v0.3，以及 Qwen2.5 系列（3B, 7B, 14B）。
*   **基准方法（Baselines）**：
    *   **单次生成类**：Perplexity、tokenSAR、CCP（依赖 NLI 模型）。
    *   **采样类**：Predictive Entropy、Semantic Entropy、EigenScore、SAR 等。
    *   **提示类**：P(True)。
*   **评估指标**：AUROC（衡量区分正确与错误回答的能力）、PRR（预测拒绝率）。

### 4. 资源与算力
*   **硬件环境**：
    *   **GPU**：8 台 Nvidia A100 (40GB)，主要用于运行需要多次采样的基准方法。
    *   **CPU**：Intel Xeon Platinum 8368。
*   **STC 的优势**：STC 的推理过程**不需要 GPU**，可以在 CPU 上高效运行。
*   **预计算开销**：对于词表最大的 Qwen2.5-14B（约 15 万词条），聚类过程耗时约 33 分钟，且每个模型仅需执行一次。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个数据集和 6 个不同架构/规模的模型上进行了交叉验证，实验覆盖面广。
*   **消融实验**：分别验证了“嵌入聚类”和“前缀匹配”的贡献，证明两者具有互补性。
*   **敏感性分析**：针对聚类算法（K-means vs 层次聚类）、距离度量（欧氏 vs 余弦）、簇数量 $K$、嵌入类型等进行了详尽的参数测试。
*   **客观性**：使用 GPT-4.1 作为自动评测员来判定回答的正确性，避免了传统 ROUGE 指标在语义判断上的局限。

### 6. 主要结论与发现
*   **性能卓越**：STC 在所有数据集和模型上的表现均优于传统的 Perplexity，并达到了与最先进的采样法（如 Semantic Entropy）和辅助模型法（如 CCP）相当的水平。
*   **效率极高**：相比于依赖 NLI 模型的 CCP 方法，STC 将推理开销平均降低了 **98%**。
*   **单次生成优势**：在保持高 AUROC 的同时，避免了多次采样带来的延迟，非常适合低延迟应用。

### 7. 优点与亮点
*   **自包含性（Self-contained）**：完全依赖模型自身的内部表示，无需微调，无需外部模型。
*   **离线优化**：将计算密集的聚类过程移至离线，推理时仅需简单的查表和求和操作。
*   **鲁棒性**：对聚类超参数（如簇的数量）不敏感，表现出稳定的语义捕捉能力。

### 8. 不足与局限
*   **白盒限制**：该方法需要访问模型的 Logits 和 Embedding 层，因此**无法直接应用于闭源 API 模型**（如 GPT-4）。
*   **上下文无关性**：目前使用的是静态词表嵌入，未考虑 Token 在特定上下文中的语义变化（多义词问题），未来可能需要引入上下文感知的嵌入。
*   **校准问题**：论文未深入探讨不确定性得分的绝对校准（Calibration），即得分是否能直接对应错误概率的百分比。

（完）
