---
title: Language Model Maps for Prompt-Response Distributions via Log-Likelihood Vectors
title_zh: 基于对数似然向量的提示-响应分布语言模型地图
authors: "Yusuke Takase, Momose Oyama, Hidetoshi Shimodaira"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18593v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 通过对数似然向量比较语言模型分布
tldr: 本研究提出了一种通过提示-响应对的对数似然向量来表示语言模型的方法，并据此构建“模型地图”以比较其条件分布。在该空间中，模型间的距离近似于KL散度。实验证明，该地图能有效捕捉模型属性、任务性能以及提示修改带来的系统性偏移，并揭示了提示操作的近似加性组合性。此外，引入PMI向量可进一步减少无条件分布干扰，为分析输入相关的模型行为提供了新视角。
selection_source: fresh_fetch
motivation: 旨在解决如何直观且量化地比较不同语言模型在特定提示下的条件概率分布差异及其演化规律。
method: 利用提示-响应对的对数似然向量构建模型空间，并引入点互信息（PMI）向量来减少无条件分布的影响。
result: 实验显示该方法能有效反映模型家族、性能及提示词修改对模型行为的系统性影响，并展现了提示操作的近似加性组合特性。
conclusion: 该框架为理解、可视化及预测复杂语言模型在不同输入下的行为模式提供了一种强有力的分析工具。
---

## 摘要
我们提出了一种通过提示-响应对上的对数似然向量来表示语言模型的方法，并构建了用于比较其条件分布的模型地图。在这个空间中，模型之间的距离近似于相应条件分布之间的 KL 散度。对大量公开可用语言模型的实验表明，这些地图捕捉到了有意义的全局结构，包括与模型属性和任务性能的关系。该方法还捕捉到了由提示修改引起的系统性偏移及其近似的可加组合性，为分析和预测复合提示操作的效果提供了一种途径。我们进一步引入了逐点互信息 (PMI) 向量，以减少无条件分布的影响；在某些情况下，基于 PMI 的模型地图能更好地反映与训练数据相关的差异。总的来说，该框架支持对输入依赖型模型行为的分析。

## Abstract
We propose a method that represents language models by log-likelihood vectors over prompt-response pairs and constructs model maps for comparing their conditional distributions. In this space, distances between models approximate the KL divergence between the corresponding conditional distributions. Experiments on a large collection of publicly available language models show that the maps capture meaningful global structure, including relationships to model attributes and task performance. The method also captures systematic shifts induced by prompt modifications and their approximate additive compositionality, suggesting a way to analyze and predict the effects of composite prompt operations. We further introduce pointwise mutual information (PMI) vectors to reduce the influence of unconditional distributions; in some cases, PMI-based model maps better reflect training-data-related differences. Overall, the framework supports the analysis of input-dependent model behavior.

---

## 论文详细总结（自动生成）

这是一份关于论文《Language Model Maps for Prompt-Response Distributions via Log-Likelihood Vectors》的结构化深入分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）的爆发式增长，如何系统地比较不同模型在特定输入下的行为变得至关重要。现有的模型比较方法要么依赖内部权重（难以跨架构比较），要么依赖生成文本（受随机性和嵌入模型质量影响）。
*   **核心问题**：如何构建一个统一的数学空间，既能直观可视化大量模型的分布关系，又能量化模型在“提示-响应”（Prompt-Response）条件概率分布上的差异。
*   **整体含义**：本文将模型表示为高维空间中的向量，其坐标由模型对预设“提示-响应对”的对数似然值组成。这使得模型间的欧几里得距离能够直接对应于它们之间的 KL 散度（Kullback–Leibler Divergence），从而实现对模型行为的精确制图。

### 2. 方法论
*   **核心思想**：利用模型对固定数据集 $D = \{(x_s, y_s)\}_{s=1}^N$ 的条件对数似然值作为特征向量。
*   **关键技术细节**：
    *   **条件对数似然向量 ($\ell_i$)**：对于模型 $p_i$，向量的每个分量为 $\log p_i(y_s | x_s)$。
    *   **KL 散度近似**：证明了两个模型的中心化对数似然向量之间的平方欧几里得距离近似于它们条件分布之间的平均 KL 散度。
    *   **PMI 向量**：引入点互信息（Pointwise Mutual Information）向量，计算公式为 $\log p_i(y|x) - \log p_i(y)$。其目的是减去模型自身无条件生成偏好（即“口癖”或通用概率），突出提示词 $x$ 对响应 $y$ 的实际影响。
    *   **提示偏移（Prompt Shift）**：研究当提示词 $x$ 发生变化（如加入“Let's think step by step”）时，模型在向量空间中的位移。
*   **算法流程**：选择参考数据集 -> 计算各模型对数据集的对数似然 -> 向量中心化 -> 降维可视化（t-SNE/PCA） -> 分析距离与偏移。

### 3. 实验设计
*   **数据集**：
    *   **Tulu3 (10,000对)**：用于构建主地图和提示偏移分析。
    *   **Infinity-Instruct (10,000对)**：用于对比不同训练数据对模型位置的影响。
*   **对比对象与 Benchmark**：
    *   **模型规模**：涵盖 552 个公开模型（主要在 70B 参数以下），包括 Llama、Qwen、Mistral、Gemma 等家族。
    *   **验证基准**：使用蒙特卡洛（Monte Carlo）估计的精确 KL 散度作为金标准，并与基于文本嵌入的语义距离（Semantic Distance）进行对比。
*   **场景**：性能预测（Open LLM Leaderboard v2 分数）、架构分类、提示词修改（CoT、重复提示词）。

### 4. 资源与算力
*   **算力说明**：文中提到由于“计算资源限制”，将模型大小限制在 70B 以下。
*   **具体细节**：实验使用了 `bfloat16` 精度进行推理计算。虽然未明确给出具体的 GPU 型号和总耗时，但考虑到需要对 500 多个模型在 10,000-20,000 个样本上计算对数似然，这涉及大规模的推理集群作业。

### 5. 实验数量与充分性
*   **实验规模**：分析了 **552 个模型**，这是同类研究中样本量非常大的一次。
*   **实验维度**：
    *   全局结构分析（t-SNE 可视化）。
    *   提示偏移实验（4 种提示组合：Base, CoT, Repeat, Repeat+CoT）。
    *   PMI 向量对比实验。
    *   数学有效性验证（与 MC-KL 散度的相关性分析）。
*   **充分性评价**：实验设计非常系统，不仅有定性的可视化，还有定量的相关性验证（Spearman 相关系数达 0.94），客观性较强。

### 6. 主要结论与发现
*   **全局规律**：模型地图能清晰地按架构家族（如 Qwen 簇、Llama 簇）聚类，且空间分布与模型在排行榜上的性能表现有显著相关性。
*   **提示偏移的加性组合性**：发现提示词修改在向量空间中具有类似词向量（Word2Vec）的**加性性质**。例如，“重复提示词”产生的位移向量加上“CoT”产生的位移向量，近似等于“重复+CoT”产生的总位移向量。
*   **PMI 的作用**：PMI 向量能更敏感地捕捉模型对训练数据的依赖。当测试集与训练集匹配时，模型在 PMI 地图上的聚集度更高。
*   **距离有效性**：基于对数似然向量的距离比基于文本嵌入的语义距离更能稳定地反映模型间的概率分布差异。

### 7. 亮点
*   **数学严谨性**：建立了向量空间距离与信息论中 KL 散度的直接联系。
*   **发现加性规律**：首次揭示了复杂提示词操作在概率空间中的线性组合特性，这为预测复合提示词的效果提供了可能。
*   **无需生成**：该方法不需要模型实际生成文本，避免了解码策略（如 Temperature）带来的干扰。

### 8. 不足与局限
*   **闭源模型限制**：由于需要获取对数似然值（Log-Likelihood），该方法无法直接应用于 GPT-4、Claude 等仅提供 API 文本输出的闭源模型。
*   **数据集依赖**：模型地图的形态取决于所选的参考数据集 $D$，不同领域的数据集可能导致不同的模型关系视图。
*   **PMI 计算近似**：在计算 $p(y)$ 时采用了简化处理（空输入），可能与真实的边缘概率分布存在偏差。
*   **提示词范围**：实验仅测试了少数几种提示词修改，尚不确定更复杂的提示工程是否遵循同样的几何规律。

（完）
