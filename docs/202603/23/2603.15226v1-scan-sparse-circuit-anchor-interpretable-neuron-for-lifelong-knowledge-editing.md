---
title: "SCAN: Sparse Circuit Anchor Interpretable Neuron for Lifelong Knowledge Editing"
title_zh: SCAN：用于终身知识编辑的稀疏电路锚定可解释神经元
authors: "Yuhuan Liu, Haitian Zhong, Xinyuan Xia, Qiang Liu, Shu Wu, Liang Wang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15226v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于大语言模型终身知识编辑的稀疏电路编辑
tldr: "针对大语言模型在连续知识编辑中容易出现的灾难性遗忘和模型崩溃问题，本文提出了 SCAN 框架。该框架通过稀疏转码器构建知识电路，将编辑过程转化为机制感知的稀疏操作，从而实现精准干预。实验证明，SCAN 在进行 3,000 次连续编辑后仍能保持模型在 MMLU 和 GSM8K 等基准上的性能，显著优于传统密集编辑方法，有效解决了模型完整性受损的问题。"
selection_source: fresh_fetch
motivation: 传统的密集编辑范式将模型视为黑盒，其粗粒度的参数干预会不可避免地破坏已有知识，导致连续编辑时的模型崩溃。
method: 提出 SCAN 框架，利用稀疏转码器构建知识电路，将知识编辑转化为针对特定神经元的机制感知操纵。
result: "在多种模型和数据集上的实验表明，SCAN 在经历 3,000 次连续编辑后仍能保持优异性能，而现有方法则会出现严重的模型退化。"
conclusion: SCAN 通过稀疏电路干预有效解决了终身知识编辑中的遗忘与崩溃问题，证明了可解释性机制在模型维护中的重要价值。
---

## 摘要
大语言模型（LLMs）在连续知识编辑过程中经常遭受灾难性遗忘和崩溃。这种脆弱性源于目前盛行的稠密编辑范式，该范式将模型视为黑盒，并依赖于粗粒度的参数干预，这不可避免地会破坏已保留的知识。为了解决这一问题，我们提出了 SCAN（一种基于稀疏电路锚定神经元的稀疏编辑框架），它通过稀疏转码器（Sparse Transcoders）构建知识电路，将编辑转化为一种机制感知的操作。在 Gemma2、Qwen3 和 Llama3.1 上针对 CounterFact、ZsRE 和 WikiFactDiff 进行的实验表明，SCAN 取得了卓越的性能。即使在进行 3,000 次连续编辑后，SCAN 仍能在 MMLU 和 GSM8K 等基准测试上保持模型完整性，而现有的其他方法随着编辑次数的累积性能逐渐恶化，最终导致模型崩溃。

## Abstract
Large Language Models (LLMs) often suffer from catastrophic forgetting and collapse during sequential knowledge editing. This vulnerability stems from the prevailing dense editing paradigm, which treats models as black boxes and relies on coarse-grained parameter interventions that inevitably disrupt preserved knowledge. To address this, we propose SCAN (a sparse editing framework based on Sparse Circuit Anchored Neuron) which transforms editing into a mechanism-aware manipulation by constructing a knowledge circuit via Sparse Transcoders. Experiments on Gemma2, Qwen3, and Llama3.1 across CounterFact, ZsRE and WikiFactDiff demonstrate that SCAN achieves a superior performance, maintaining model integrity on benchmarks like MMLU and GSM8K even after 3,000 sequential edits, whereas other existing methods deteriorate progressively as editing accumulates, eventually resulting in model collapse.

---

## 论文详细总结（自动生成）

### 论文总结：SCAN —— 用于终身知识编辑的稀疏电路锚定可解释神经元

#### 1. 论文的核心问题与整体含义
*   **研究背景**：大语言模型（LLMs）预训练知识会过时，需要通过“模型编辑”进行更新。
*   **核心问题**：在**终身（连续）知识编辑**场景下，现有方法（如 MEMIT、AlphaEdit 等）普遍面临“灾难性遗忘”和“模型崩溃”的问题。
*   **根本原因**：目前的编辑范式多为“密集编辑”，将模型视为黑盒，干预范围覆盖了大量的参数块。由于 MLP 神经元具有“多义性”（Polysemantic），粗粒度的干预会不可避免地破坏与编辑目标无关的知识，随着编辑次数累积，最终导致模型整体能力丧失。

#### 2. 方法论
SCAN 框架的核心思想是将编辑过程从“黑盒密集干预”转变为“白盒机制感知操作”，通过**稀疏性**实现精准定位。
*   **稀疏转码器（Sparse Transcoder）**：引入辅助模块将 MLP 的隐藏状态投影到高度稀疏且具有“单语义性”的特征空间中，确保每个特征代表单一概念。
*   **归因图构建（Attribution Graph Construction）**：
    *   利用**泰勒展开的一阶项**近似计算特征节点间的因果影响分数（Attribution Score）。
    *   通过雅可比矩阵（Jacobian）实现跨层空间的向量方向对齐，量化上游特征对下游输出的贡献。
*   **总归因计算与剪枝**：
    *   不仅考虑直接影响，还通过公式 $B = (I - A)^{-1} - I$ 计算包含多步路径的**总归因矩阵**。
    *   利用累积阈值 $\tau$ 对图进行剪枝，识别出负责特定事实的最小化“知识电路”。
*   **稀疏编辑与注入**：
    *   仅针对电路中识别出的关键稀疏特征进行解码器权重微调。
    *   在推理时，通过计算 Jaccard 相似度触发特定的“转向向量”（Steering Vector）注入残差流，实现知识更新。

#### 3. 实验设计
*   **实验对象（LLMs）**：Gemma2-2B、Qwen3-8B、Llama3.1-8B-Instruct、Llama3.2-1B。
*   **数据集**：
    *   **知识编辑基准**：CounterFact（反事实）、ZsRE（问答）、WikiFactDiff（事实更新）。
    *   **通用能力基准**：MMLU、GSM8K、ARC、CommonsenseQA、OpenBookQA、SciQ。
*   **对比方法**：
    *   参数修改类：Fine-Tuning (FT)、MEMIT、RECT、AlphaEdit。
    *   参数保留类：GRACE、MELO。
*   **评估指标**：可靠性（Rel）、泛化性（Gen）、局部性（Loc）以及通用能力准确率。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：作者提到使用了公开可用的预训练转码器 Checkpoints（如 Gemma2-2B-transcoders 等），这意味着 SCAN 框架在应用时不需要从头训练转码器，主要开销在于归因图的计算和少量的参数微调。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 **1,000 次连续编辑**的系统性对比。
    *   在通用能力测试中，将编辑压力扩展到了 **3,000 次连续编辑**。
    *   涵盖了 4 种不同架构和规模的模型，以及 3 个主流编辑数据集。
*   **充分性与公平性**：实验设计较为充分。通过设置统一的 Batch Size（100）和采用 Harmonic Mean（调和平均值）来评估 Rel/Gen/Loc 的平衡性。此外，还进行了特征判别力的消融实验（如 Sub、No last 等规则对比），证明了其定位机制的有效性。

#### 6. 论文的主要结论与发现
*   **解决模型崩溃**：SCAN 在 3,000 次连续编辑后仍能保持 MMLU 等基准的性能，而 MEMIT 和 RECT 在 2,000 次左右即出现性能骤降（模型崩溃）。
*   **知识定位发现**：研究发现模型内部存在“主语到答案”的流水线结构。浅层主要负责表示主语身份，中深层则结合关系信息在最后一个 Token 处提取目标答案。
*   **主语锚定效应**：实验证明，与特定事实相关的特征高度集中在主语的最后一个 Token 位置，这为精准编辑提供了物理依据。

#### 7. 优点
*   **高精度与低副作用**：通过稀疏电路定位，极大地减少了对无关参数的扰动，有效缓解了灾难性遗忘。
*   **强解释性**：将模型编辑从“炼金”转向“手术”，提供了可视化的归因图，增强了模型更新的可控性和透明度。
*   **长程稳定性**：在终身学习场景下表现出极强的鲁棒性，是目前少数能支撑数千次连续编辑而不崩溃的方法之一。

#### 8. 不足与局限
*   **依赖预训练转码器**：SCAN 的效果高度依赖于 Sparse Transcoder 的质量。如果目标模型缺乏高质量的预训练转码器，该方法难以直接部署。
*   **计算开销**：构建完整的归因图并计算矩阵逆（或迭代求解）在超大规模模型上可能会带来额外的计算延迟。
*   **应用限制**：目前主要针对事实性知识（三元组）进行编辑，对于更复杂的逻辑推理或风格迁移类编辑的适用性尚待验证。

（完）
