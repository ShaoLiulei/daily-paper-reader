---
title: "Memori: A Persistent Memory Layer for Efficient, Context-Aware LLM Agents"
title_zh: Memori：一种用于高效、上下文感知型大语言模型智能体的持久化内存层
authors: "Luiz C. Borro, Luiz A. B. Macarini, Gordon Tindall, Michael Montero, Adam B. Struck"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19935v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于上下文感知LLM智能体的持久记忆层
tldr: "Memori 是一个与模型无关的持久化记忆层，旨在解决 LLM 智能体在多轮对话中因注入原始对话导致的成本高和性能下降问题。它通过将非结构化对话转化为语义三元组和摘要，实现了高效的上下文感知。在 LoCoMo 基准测试中，Memori 以极低的 Token 消耗实现了 81.95% 的准确率，显著降低了部署成本并提升了推理效率。"
selection_source: fresh_fetch
motivation: 现有的 LLM 记忆方案通常依赖于注入大量原始对话，导致 Token 成本高昂、性能退化且存在供应商锁定问题。
method: 开发了一种高级增强流水线，将非结构化对话转换为紧凑的语义三元组和对话摘要，从而实现精确的记忆检索和推理。
result: "在 LoCoMo 基准测试中达到 81.95% 的准确率，Token 消耗仅为完整上下文的 5%，比竞争方案节省了 67% 的成本。"
conclusion: 研究证明有效的 LLM 记忆应依赖于结构化的数据表示而非单纯扩大上下文窗口，这为低成本部署提供了新路径。
---

## 摘要
随着大语言模型（LLM）演变为自主智能体，API 层的持久化内存对于实现跨 LLM 和多会话交互的上下文感知行为至关重要。现有方法往往导致供应商锁定，并依赖于将大量原始对话注入提示词，从而导致高昂的 Token 成本和性能下降。我们推出了 Memori，这是一个与 LLM 无关的持久化内存层，它将内存视为一个数据结构化问题。其高级增强（Advanced Augmentation）流水线将非结构化对话转换为紧凑的语义三元组和对话摘要，从而实现精确检索和连贯推理。在 LoCoMo 基准测试中的评估结果显示，Memori 达到了 81.95% 的准确率，在每次查询仅使用 1,294 个 Token（约占全上下文的 5%）的情况下，性能优于现有的内存系统。这显著降低了成本，包括比竞争方法减少了 67% 的 Token 使用量，且与全上下文方法相比节省了 20 倍以上的成本。这些结果表明，LLM 智能体中有效的内存依赖于结构化表示而非更大的上下文窗口，从而实现了可扩展且具有成本效益的部署。

## Abstract
As large language models (LLMs) evolve into autonomous agents, persistent memory at the API layer is essential for enabling context-aware behavior across LLMs and multi-session interactions. Existing approaches force vendor lock-in and rely on injecting large volumes of raw conversation into prompts, leading to high token costs and degraded performance.   We introduce Memori, an LLM-agnostic persistent memory layer that treats memory as a data structuring problem. Its Advanced Augmentation pipeline converts unstructured dialogue into compact semantic triples and conversation summaries, enabling precise retrieval and coherent reasoning.   Evaluated on the LoCoMo benchmark, Memori achieves 81.95% accuracy, outperforming existing memory systems while using only 1,294 tokens per query (~5% of full context). This results in substantial cost reductions, including 67% fewer tokens than competing approaches and over 20x savings compared to full-context methods.   These results show that effective memory in LLM agents depends on structured representations instead of larger context windows, enabling scalable and cost-efficient deployment.

---

## 论文详细总结（自动生成）

### 论文总结：Memori —— 用于高效上下文感知型 LLM 智能体的持久化内存层

#### 1. 核心问题与研究动机
*   **研究背景**：随着大语言模型（LLM）向自主智能体演进，记忆能力成为其实现长期交互、个性化和复杂推理的核心支柱。由于模型参数无法在部署期间实时更新，智能体必须依赖外部存储系统。
*   **核心问题**：现有的记忆方案通常采用“原始对话注入”方式，即将大量历史记录直接塞入 Prompt。这导致了三大问题：
    1.  **高昂成本**：Token 消耗随对话轮数呈指数级增长。
    2.  **性能退化（Context Rot）**：长上下文会导致模型忽略关键信息或产生幻觉。
    3.  **供应商锁定**：记忆逻辑往往与特定模型 API 绑定，缺乏灵活性。
*   **研究目标**：开发一种与模型无关的、通过“数据结构化”而非“扩大窗口”来解决记忆问题的持久化层。

#### 2. 方法论与核心技术
Memori 的核心思想是将非结构化对话转化为**结构化知识表示**，其关键技术为“高级增强（Advanced Augmentation）”流水线：
*   **语义三元组生成（Semantic Triples）**：将原始对话拆解为原子知识单位（主语-谓语-宾语）。例如，从“我喜欢在早晨喝苦咖啡”提取出 `(用户, 偏好, 苦咖啡)`。这种方式极大地压缩了信息并去除了口语噪声。
*   **对话摘要（Conversation Summaries）**：三元组虽然精确但缺乏叙事流。系统同步生成高层级的对话摘要，捕捉用户的意图演变和任务背景。
*   **双层关联索引**：每个三元组都与其来源对话的摘要相链接。检索时，系统不仅能提取孤立的事实，还能还原该事实发生的背景故事。
*   **混合检索机制**：结合了基于向量嵌入（Gemma-300 模型）的余弦相似度搜索和基于关键词的 BM25 匹配，确保检索的查全率与查准率。

#### 3. 实验设计
*   **基准测试（Benchmark）**：使用 **LoCoMo (Long Conversation Memory)** 数据集，该测试专门用于评估智能体在多会话、长时记忆中的表现。
*   **评估维度**：涵盖四种推理类别：单跳推理（Single-Hop）、多跳推理（Multi-Hop）、时间推理（Temporal）和开放域推理（Open-Domain）。
*   **对比方法**：
    *   **Zep、LangMem、Mem0**：当前主流的开源/商业记忆框架。
    *   **Full-Context (Ceiling)**：将全部历史记录注入的“性能上限”方案。
*   **评估方式**：采用 LLM-as-a-Judge 模式，使用 GPT-4o-mini 作为裁判，评估生成答案的准确性。

#### 4. 资源与算力
*   **模型使用**：实验中生成答案和充当裁判的模型均为 **GPT-4o-mini**（文中标记为 GPT-4.1-mini）。
*   **嵌入模型**：使用 **Gemma-300** 进行向量化。
*   **算力细节**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或处理 LoCoMo 全量数据所需的具体时长。由于其定位是 API 层的中间件，重点在于推理时的 Token 效率而非模型训练算力。

#### 5. 实验数量与充分性
*   **实验规模**：针对 LoCoMo 数据集中的 1529 个问题进行了测试（不含对抗性类别）。
*   **充分性**：
    *   进行了 3 轮独立运行并取平均值（附带标准差分析），确保了结果的稳定性。
    *   对比了三种主流基线方案，并提供了“全上下文”作为参照，实验设计较为客观。
    *   **局限性**：消融实验部分相对薄弱，未深入探讨三元组与摘要各自对性能贡献的比例。

#### 6. 主要结论与发现
*   **性能领先**：Memori 在检索类系统中取得最高分（81.95%），优于 Zep (79.09%) 和 LangMem (78.05%)。
*   **极致高效**：Memori 每次查询仅需 **1,294 个 Token**，仅占全上下文方案（26,031 Token）的 **5%** 左右。
*   **成本优势**：相比 Zep 节省了 67% 的 Token 成本，相比全上下文方案节省了 20 倍以上的成本。
*   **结构胜过规模**：实验证明，记忆质量取决于数据的**结构化程度**，而非上下文窗口的大小。

#### 7. 优点
*   **高信噪比**：通过三元组提取，有效过滤了对话中的寒暄、自我纠正等噪声。
*   **解耦设计**：作为独立的内存层，可以轻松集成到任何 LLM 工作流中，避免了供应商锁定。
*   **推理增强**：通过将原子事实（三元组）与叙事背景（摘要）结合，在保持低成本的同时保留了复杂推理的能力。

#### 8. 不足与局限
*   **时间推理瓶颈**：在时间推理类别（80.37%）中略逊于 LangMem 和 Zep，说明单纯的三元组结构在处理状态随时间变化的动态逻辑时仍有提升空间。
*   **开放域挑战**：在缺乏明确检索锚点的开放式问题上表现一般（63.54%），这是所有基于检索的系统的共同弱点。
*   **处理开销**：虽然推理时 Token 减少了，但前期将对话转化为三元组和摘要的“预处理”过程会产生额外的 API 调用成本和延迟，文中未对此部分成本进行详细量化。

（完）
