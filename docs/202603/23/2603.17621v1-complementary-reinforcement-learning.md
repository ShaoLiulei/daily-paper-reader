---
title: Complementary Reinforcement Learning
title_zh: 互补强化学习
authors: "Dilxat Muhtar, Jiashun Liu, Wei Gao, Weixun Wang, Shaopan Xiong, Ju Huang, Siran Yang, Wenbo Su, Jiamang Wang, Ling Pan, Bo Zheng"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17621v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 基于历史经验的LLM智能体强化学习
tldr: "针对LLM智能体在强化学习中样本效率低且难以有效利用历史经验的问题，本文受神经科学启发提出Complementary RL。该方法通过在优化循环中实现经验提取器与策略执行器的协同演化，使提取器能根据对执行器成功的贡献度动态调整策略。实验表明，该方法在单任务中性能提升10%，并在多任务场景下展现出良好的扩展性，为经验驱动的学习提供了新范式。"
selection_source: fresh_fetch
motivation: 现有方法中提取的经验往往是静态的，无法随执行器能力的提升而同步演化，导致经验与策略之间出现失配。
method: 提出一种协同演化框架，执行器由稀疏奖励优化，而经验提取器则根据其提供的经验对执行器成功的实际贡献进行动态优化。
result: "在单任务场景下相比传统强化学习基准提升了10%的性能，并在多任务设置中表现出稳健的可扩展性。"
conclusion: Complementary RL 证明了经验提取与策略优化的同步演化能显著提升智能体的学习效率和任务表现。
---

## 摘要
强化学习（RL）已成为训练基于大语言模型（LLM）智能体的强大范式，但仍受限于低样本效率。这不仅源于稀疏的结果反馈，还源于智能体无法跨回合利用先验经验。虽然利用历史经验增强智能体提供了一种有前景的补救方案，但现有方法存在一个关键缺陷：从历史中提取的经验要么是静态存储的，要么无法与不断进步的执行器（actor）协同演化，导致经验与执行器不断发展的能力之间出现渐进式失配，从而在训练过程中降低了其效用。受神经科学中互补学习系统的启发，我们提出了互补强化学习（Complementary RL），旨在强化学习优化循环中实现经验提取器与策略执行器的无缝协同演化。具体而言，执行器通过基于结果的稀疏奖励进行优化，而经验提取器则根据其提取的经验是否能显著促进执行器的成功来进行优化，从而使其经验管理策略与执行器不断增长的能力保持同步演化。实验结果表明，Complementary RL 优于不从经验中学习的基于结果的智能体强化学习基准，在单任务场景中实现了 10% 的性能提升，并在多任务设置中展现出稳健的可扩展性。这些结果确立了 Complementary RL 作为高效经验驱动型智能体学习范式的地位。

## Abstract
Reinforcement Learning (RL) has emerged as a powerful paradigm for training LLM-based agents, yet remains limited by low sample efficiency, stemming not only from sparse outcome feedback but also from the agent's inability to leverage prior experience across episodes. While augmenting agents with historical experience offers a promising remedy, existing approaches suffer from a critical weakness: the experience distilled from history is either stored statically or fail to coevolve with the improving actor, causing a progressive misalignment between the experience and the actor's evolving capability that diminishes its utility over the course of training. Inspired by complementary learning systems in neuroscience, we present Complementary RL to achieve seamless co-evolution of an experience extractor and a policy actor within the RL optimization loop. Specifically, the actor is optimized via sparse outcome-based rewards, while the experience extractor is optimized according to whether its distilled experiences demonstrably contribute to the actor's success, thereby evolving its experience management strategy in lockstep with the actor's growing capabilities. Empirically, Complementary RL outperforms outcome-based agentic RL baselines that do not learn from experience, achieving 10% performance improvement in single-task scenarios and exhibits robust scalability in multi-task settings. These results establish Complementary RL as a paradigm for efficient experience-driven agent learning.

---

## 论文详细总结（自动生成）

以下是对论文《Complementary Reinforcement Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：基于大语言模型（LLM）的智能体在强化学习（RL）中面临**样本效率低下**的问题。这主要源于：1) 奖励信号稀疏且仅基于结果；2) 智能体无法有效利用跨回合的历史经验。
*   **研究动机**：现有的经验增强方法（如反射、经验池）通常将经验视为**静态资源**，或者使用非自适应的提取器。随着执行器（Actor）能力的提升，静态经验会与执行器当前水平产生**分布失配（Misalignment）**，导致经验在训练后期失效甚至产生负面干扰。
*   **整体含义**：受神经科学中“互补学习系统（CLS）”启发，论文提出了一种让**策略执行器（Actor）**与**经验提取器（Extractor）**在RL优化循环中**协同演化（Co-evolution）**的范式。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：构建一个双模型闭环系统。执行器负责与环境交互并根据经验改进策略；提取器负责从轨迹中提炼经验，并根据这些经验对执行器成功的“贡献度”来优化自身的提取策略。
*   **关键技术细节**：
    *   **协同演化架构**：执行器使用基于结果的奖励（如成功/失败）进行优化；提取器使用**CISPO算法**优化，其奖励取决于所提供的经验是否帮助执行器完成了任务。
    *   **Split GRPO（分组优势估计）**：为了防止执行器过度依赖外部经验而忽视自身能力内化，将采样轨迹分为“有经验引导”和“无经验引导”两组，并分别在组内进行优势归一化。
    *   **经验管理器（ExperienceManager）**：采用异步设计，包含**周期性合并（Periodic Merge）**机制以消除冗余和冲突，以及**Search-and-Ask**工具允许执行器在决策中途主动查询。
    *   **异步训练框架**：解耦了执行器的交互采样与提取器的经验蒸馏，通过中心化的内存管理确保大规模训练时不会引入额外的延迟。

### 3. 实验设计
*   **实验场景**：涵盖了四类具有代表性的开放式环境：
    *   **MiniHack**（网格导航与解谜）
    *   **WebShop**（在线购物交互）
    *   **ALFWorld**（具身智能家庭任务）
    *   **SWE-Bench**（软件工程代码修复）
*   **Benchmark 与对比方法**：
    *   **Baseline**：不使用经验的标准RL。
    *   **Offline Exp**：使用预先收集的静态经验。
    *   **Static Online Exp**：动态维护经验池但提取器不参与演化。
    *   **Exp Only**：仅优化提取器，固定执行器。
*   **评估指标**：成功率（Success Rate）、奖励分数、完成任务所需的平均动作数（效率）。

### 4. 资源与算力
*   **模型规格**：执行器主要使用 **Qwen2.5-7B-Instruct**，提取器使用 **Qwen3-4B-Thinking**（部分实验对比了 30B 版本），嵌入模型使用 Qwen3-Embedding-0.6B。
*   **算力说明**：论文**未明确列出**具体的 GPU 总数或总训练时长。但提到使用了分布式框架（Megatron 训练，vLLM 推理），并强调了其异步架构在 128 到 1024 的 Batch Size 下均能保持与 Baseline 持平的采样延迟（约 1 秒以内的搜索耗时）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了单任务（4个环境独立训练）和多任务（3任务及6任务混合）的大规模实验。
    *   针对核心组件（Split GRPO、Merge 机制、Search-and-Ask）进行了详尽的消融实验。
    *   探讨了提取器模型规模（4B vs 30B）对结果的影响。
*   **充分性评价**：实验设计较为**充分且客观**。通过跨领域的任务验证了泛化性，并通过延迟测试证明了工程上的实用性。对比基准涵盖了当前主流的经验利用范式，公平性较高。

### 6. 主要结论与发现
*   **协同演化的必要性**：静态经验在训练后期会成为瓶颈，而协同演化的 Complementary RL 在单任务中比 Baseline 提升了 **10%** 的性能。
*   **效率提升**：经验引导不仅提高了成功率，还显著减少了完成任务所需的动作步数（在 MiniHack 和 ALFWorld 中减少了 1.5-2 倍）。
*   **多任务扩展性**：在多任务混合训练中，该方法能有效提取跨任务的通用经验（如“如何处理陷入死循环”），性能提升约 **7-8%**。
*   **内化效应**：通过 Split GRPO，执行器在测试时不依赖经验（w/o exp）也能获得比 Baseline 更好的表现，说明经验被成功内化到了模型参数中。

### 7. 优点与亮点
*   **生物学启发**：借鉴互补学习系统理论，解决了经验与策略步调不一致的痛点。
*   **工程设计精巧**：异步双循环架构解决了 RL 训练中引入经验提取带来的计算阻塞问题。
*   **算法稳定性**：提出的 Split GRPO 和训练计数感知（Training-Count-Aware）重加权技术有效缓解了双模型 RL 训练中的不稳定性。

### 8. 不足与局限
*   **自蒸馏不稳定性**：尝试将经验引导的行为通过自蒸馏（Self-Distillation）直接监督训练执行器时，出现了训练崩溃，显示该方向仍需探索。
*   **复杂度较高**：系统涉及两个 RL 进程、一个嵌入模型和一个动态维护的数据库，维护成本和超参数调优难度高于标准 RL。
*   **经验质量依赖**：初期如果提取器生成的经验质量极差，可能会对执行器产生误导（虽然有 Actor-Critic 机制缓解，但会增加延迟）。

（完）
