---
title: "S3T-Former: A Purely Spike-Driven State-Space Topology Transformer for Skeleton Action Recognition"
title_zh: S3T-Former：一种用于骨骼动作识别的纯脉冲驱动状态空间拓扑 Transformer
authors: "Naichuan Zheng, Hailun Xia, Zepeng Sun, Weiyi Li, Yujia Wang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18062v1"
tags: ["query:snn"]
score: 10.0
evidence: 纯脉冲驱动的状态空间拓扑Transformer
tldr: 针对骨骼动作识别中人工神经网络功耗高及现有脉冲神经网络（SNN）稀疏性不足的问题，本文提出首个纯脉冲驱动的Transformer架构S3T-Former。通过多流解剖脉冲嵌入、侧向脉冲拓扑路由和脉冲状态空间引擎，该模型在保持高稀疏性的同时解决了长程依赖建模难题，实现了极低功耗下的高性能动作识别。
selection_source: fresh_fetch
motivation: 现有SNN模型在处理骨骼数据时常因密集矩阵运算或非稀疏变换而丧失稀疏性优势，且受限于脉冲神经元的短期记忆问题。
method: 设计了纯脉冲驱动的S3T-Former，利用M-ASE生成稀疏事件流，通过LSTR实现按需脉冲传播，并引入S3引擎捕捉长程时间动态。
result: 在多个大规模数据集上，该模型在保持高准确率的同时，理论能耗远低于传统人工神经网络。
conclusion: S3T-Former成功构建了高效的纯脉冲骨骼动作识别框架，为资源受限边缘设备上的神经形态应用提供了新方案。
---

## 摘要
基于骨骼的动作识别对多媒体应用至关重要，但其严重依赖高功耗的人工神经网络 (ANN)，限制了它们在资源受限的边缘设备上的部署。脉冲神经网络 (SNN) 提供了一种节能的替代方案；然而，现有的骨骼数据脉冲模型通常通过采用密集矩阵聚合、沉重的多模态融合模块或非稀疏的频域变换，损害了 SNN 固有的稀疏性。此外，它们还严重受困于脉冲神经元的短期遗忘问题。在本文中，我们提出了脉冲状态空间拓扑 Transformer (S3T-Former)，据我们所知，这是首个专门为节能骨骼动作识别设计的纯脉冲驱动 Transformer 架构。我们没有依赖沉重的融合开销，而是构建了一种多流解剖脉冲嵌入 (M-ASE)，它作为一个广义运动学微分算子，将多模态骨骼特征优雅地转换为异构且高度稀疏的事件流。为了实现真正的拓扑和时间稀疏性，我们引入了用于按需条件脉冲传播的侧向脉冲拓扑路由 (LSTR)，以及一个脉冲状态空间 (S3) 引擎，以便在不使用非稀疏频谱变通方法的情况下系统地捕获远程时间动态。在多个大规模数据集上的广泛实验表明，与经典 ANN 相比，S3T-Former 在理论上降低功耗的同时，实现了极具竞争力的准确率，为节能神经形态动作识别树立了新的标杆。

## Abstract
Skeleton-based action recognition is crucial for multimedia applications but heavily relies on power-hungry Artificial Neural Networks (ANNs), limiting their deployment on resource-constrained edge devices. Spiking Neural Networks (SNNs) provide an energy-efficient alternative; however, existing spiking models for skeleton data often compromise the intrinsic sparsity of SNNs by resorting to dense matrix aggregations, heavy multimodal fusion modules, or non-sparse frequency domain transformations. Furthermore, they severely suffer from the short-term amnesia of spiking neurons. In this paper, we propose the Spiking State-Space Topology Transformer (S3T-Former), which, to the best of our knowledge, is the first purely spike-driven Transformer architecture specifically designed for energy-efficient skeleton action recognition. Rather than relying on heavy fusion overhead, we formulate a Multi-Stream Anatomical Spiking Embedding (M-ASE) that acts as a generalized kinematic differential operator, elegantly transforming multimodal skeleton features into heterogeneous, highly sparse event streams. To achieve true topological and temporal sparsity, we introduce Lateral Spiking Topology Routing (LSTR) for on-demand conditional spike propagation, and a Spiking State-Space (S3) Engine to systematically capture long-range temporal dynamics without non-sparse spectral workarounds. Extensive experiments on multiple large-scale datasets demonstrate that S3T-Former achieves highly competitive accuracy while theoretically reducing energy consumption compared to classic ANNs, establishing a new state-of-the-art for energy-efficient neuromorphic action recognition.

---

## 论文详细总结（自动生成）

这是一份关于论文《S3T-Former: A Purely Spike-Driven State-Space Topology Transformer for Skeleton Action Recognition》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：基于骨骼的动作识别在多媒体应用中至关重要，但目前主流的人工神经网络（ANN）模型（如 GCN 和 Transformer）依赖高功耗的浮点乘累加（MAC）运算，难以部署在资源受限的边缘设备上。
*   **现有挑战**：
    *   **伪稀疏性**：现有的脉冲神经网络（SNN）模型在处理骨骼数据时，往往采用密集的矩阵聚合或非稀疏的频域变换，破坏了 SNN 原生的稀疏性优势。
    *   **短期遗忘**：SNN 核心的 LIF 神经元具有指数级衰减特性，难以捕捉长程的时间因果关系。
    *   **架构缺失**：缺乏一种完全由脉冲驱动、能高效处理骨骼拓扑结构的 Transformer 架构。

### 2. 方法论
S3T-Former 提出了一个纯脉冲驱动的架构，核心组件包括：
*   **多流解剖脉冲嵌入 (M-ASE)**：受视网膜神经节细胞启发，将其设计为广义运动学微分算子。它计算零阶（恒等）、一阶时间梯度和一阶空间梯度，将原始坐标转换为高度稀疏的事件流，最大化单模态的表征能力。
*   **非对称时间梯度 QKV (ATG-QKV)**：模拟视觉系统中的 M 细胞和 P 细胞。强制查询（Q）和键（K）仅在动态运动梯度上触发脉冲，而值（V）保留静态结构特征，从而极大地抑制了背景冗余。
*   **侧向脉冲拓扑路由 (LSTR)**：将 $O(N^2)$ 的密集注意力计算转化为基于人体解剖图的条件稀疏加法（零 MAC 运算），实现了按需的脉冲传播。
*   **脉冲状态空间 (S3) 引擎**：构建了一个线性复杂度的临时存储池，通过学习衰减因子来积累全局历史拓扑特征，解决了 SNN 的“短期遗忘”问题。
*   **U-Readout 机制**：使用非脉冲的积分发放（IF）神经元作为无损累加器，直接利用最终膜电位进行分类，避免了启发式脉冲计数带来的量化误差。

### 3. 实验设计
*   **数据集**：
    *   **NTU RGB+D 60**：包含 60 个类别，采用 Cross-Subject (X-Sub) 和 Cross-View (X-View) 协议。
    *   **NTU RGB+D 120**：目前规模最大、最具挑战性的数据集，采用 X-Sub 和 Cross-Setup (X-Set) 协议。
    *   **NW-UCLA**：包含 10 个日常动作类别。
*   **Benchmark 与对比方法**：
    *   **ANN 模型**：包括 ST-GCN, 2S-AGCN, CTR-GCN, Hyperformer, SkateFormer 等。
    *   **SNN 模型**：包括 Spikformer, Spike-driven Transformer, MK-SGN, Signal-SGN 等。

### 4. 资源与算力
*   **硬件环境**：实验在 **4 张 NVIDIA Tesla V100 GPU** 上进行。
*   **训练细节**：使用 AdamW 优化器，基础学习率为 0.01，权重衰减为 0.0005，训练 250 个 epoch，batch size 为 64。
*   **软件框架**：基于 PyTorch 和 SpikingJelly 库实现。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了详尽的性能对比，并针对 M-ASE、LSTR、S3 引擎和 U-Readout 等核心组件进行了系统的消融实验。
*   **客观性与公平性**：
    *   采用了标准的评估协议。
    *   在消融实验中，通过逐步添加模块展示了性能的稳步提升（从 76.32% 提升至 83.80%）。
    *   提供了详细的脉冲发放率分析和可视化结果，证明了模型的稀疏性和可解释性。

### 6. 主要结论与发现
*   **性能领先**：S3T-Former 在 SNN 领域达到了新的 SOTA。在 NTU-120 数据集上，其集成模型比之前的最优 SNN 模型（Signal-SGN）准确率高出 **6.38%**。
*   **超越部分 ANN**：在纯准确率上，S3T-Former 成功超越了经典的 ANN 模型（如 ST-GCN）。
*   **极高能效**：理论能耗分析表明，S3T-Former 的功耗不到同等规模 ANN 的 **10%**。
*   **长程建模能力**：S3 引擎有效地弥补了脉冲神经元的记忆缺陷，使其能够处理长序列动作。

### 7. 优点
*   **纯脉冲驱动**：实现了真正的零 MAC 运算（除嵌入层和输出层外），完全符合神经形态计算原则。
*   **生物启发式设计**：ATG-QKV 和 M-ASE 的设计具有很强的生物学解释性，有效地利用了动作数据的时空梯度。
*   **拓扑感知**：LSTR 巧妙地结合了人体解剖先验与数据驱动的路由，既保证了物理合理性又具备灵活性。
*   **高效的内存管理**：S3 引擎以线性复杂度实现了全局上下文建模。

### 8. 不足与局限
*   **微小动作识别困难**：由于模型追求极端的时空稀疏性，在识别如“读书”、“写字”、“玩手机”等仅涉及手指/手腕微小位移的动作时，容易因脉冲过稀疏而丢失关键特征（混淆矩阵显示这些类别的错误率较高）。
*   **稀疏性与分辨率的权衡**：在追求超低功耗的同时，如何保持对精细动作的高分辨率感知仍是一个挑战。
*   **应用限制**：目前主要针对骨骼数据，对于需要结合环境上下文或物体交互的复杂场景，可能需要额外的多模态扩展。

（完）
