---
title: Continually self-improving AI
title_zh: 持续自我改进的人工智能
authors: Zitong Yang
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18073v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 持续自我改进的AI与LLM微调
tldr: 本研究旨在突破AI受限于人类创造者的三大瓶颈：知识获取效率低、依赖有限的人类数据以及受限于人类设计的算法。论文提出了持续自我改进AI的框架，通过合成数据增强小语种知识吸收、利用模型自生成数据引导预训练以减少对人类数据的依赖，并利用测试时搜索优化学习算法配置。这些方法共同推动了AI超越人类数据和算法限制，实现自主进化。
selection_source: fresh_fetch
motivation: 现有AI系统在知识更新效率、数据来源及算法设计上过度依赖人类，限制了其能力的进一步提升。
method: 论文提出了合成数据多样化增强、模型自引导预训练以及在算法空间进行大规模搜索的三位一体方法。
result: 实验证明模型能从有限素材中高效学习，在无外部蒸馏下提升预训练能力，并发现优于人工设计的算法配置。
conclusion: 通过减少对人类干预的依赖，AI能够实现持续的自我改进，迈向更高级的自主智能。
---

## 摘要
现代基于语言模型的 AI 系统功能极其强大，但其能力在三个关键方面仍从根本上受到人类创造者的限制。首先，尽管可以通过微调更新模型权重，但在预训练后从小型专业语料库中获取新知识的数据效率仍然极低。其次，这些系统的训练严重依赖于历史上有限的人类生成数据。第三，用于训练 AI 模型的流水线受限于人类研究人员所能发现和探索的算法。本论文在克服这些固有局限性方面迈出了一小步，通过三个章节的内容旨在打破这些依赖，从而创建持续自我改进的 AI。首先，为克服知识获取中的数据效率障碍，我们提出了一种合成数据方法，将小型语料库多样化并放大为丰富的知识表示，使模型能够从有限的源材料中有效地更新其参数。其次，为减少对人类数据的依赖，我们证明了在给定固定数量的人类数据时，模型可以自我生成合成数据，以在不从任何现成的指令微调语言模型进行蒸馏的情况下，自举其基础预训练能力。最后，为了超越人为设计的训练范式，我们证明了通过在测试时对算法空间进行规模化搜索，AI 可以搜索比人类研究人员手动探索更广阔的学习算法配置空间。

## Abstract
Modern language model-based AI systems are remarkably powerful, yet their capabilities remain fundamentally capped by their human creators in three key ways. First, although a model's weights can be updated via fine-tuning, acquiring new knowledge from small, specialized corpora after pretraining remains highly data-inefficient. Second, the training of these systems relies heavily on finite, human-generated data from across history. Third, the pipelines used to train AI models are confined by the algorithms that human researchers can discover and explore. This thesis takes a small step toward overcoming these inherent limitations, presenting three chapters aimed at breaking these dependencies to create continually self-improving AI. First, to overcome this data-efficiency barrier in knowledge acquisition, we propose a synthetic data approach that diversifies and amplifies small corpora into rich knowledge representations, enabling a model to effectively update its parameters from limited source material. Second, to reduce reliance on human data, we show that given a fixed amount of such data, the model can self-generate synthetic data to bootstrap its fundamental pretraining capabilities without distillation from any off-the-shelf, instruction-tuned LM. Finally, to transcend human-engineered training paradigms, we demonstrate that by scaling search during test time over the space of algorithms, AI can search over a larger space of learning algorithm configurations than human researchers can explore manually.

---

## 论文详细总结（自动生成）

这篇论文是斯坦福大学 Zitong Yang 的博士论文，题为《持续自我改进的人工智能》（Continually self-improving AI）。以下是对该论文的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管现代大语言模型（LLM）极其强大，但其能力仍受限于人类创造者的三个瓶颈：
    1.  **知识获取效率低**：从小型专业语料库中学习新知识的效率远低于大规模预训练。
    2.  **数据依赖性**：训练严重依赖有限的、历史产生的人类数据，面临“数据枯竭”危机。
    3.  **算法局限性**：训练流水线受限于人类研究人员能想到的算法和超参数配置。
*   **核心目标**：探索如何构建一个在创建后能够自主、持续改进，且改进效果超越人类干预的 AI 系统。

### 2. 论文提出的方法论
论文通过三个核心章节分别应对上述挑战：
*   **合成持续预训练（EntiGraph）**：针对小规模专业语料，提出一种基于知识图谱启发的增强算法。首先从文档中提取实体，然后利用 LLM 分析实体间的复杂关系并生成多样化的合成文本，最后在这些放大的合成数据上进行持续预训练。
*   **合成自举预训练（SBP, Synthetic Bootstrapped Pretraining）**：在不依赖外部更强模型（无蒸馏）的前提下，通过识别预训练语料中的语义相似文档对，训练一个条件合成器。该合成器能根据现有文档生成相关的、具有新视角或体裁的合成文档，从而在固定的人类数据基础上“自举”出更强的预训练能力。
*   **执行引导的自动化研究（Automated AI Researcher）**：将 AI 研究抽象为一个搜索问题。构建了一个包含“实现者、调度器、执行工人”的自动化系统，让 LLM 生成研究想法并自动转化为代码执行，通过实验结果（如验证集损失或准确率）作为反馈，利用进化搜索算法在算法空间内进行测试时缩放（Test-time scaling）。

### 3. 实验设计
*   **数据集与场景**：
    *   **知识获取**：使用 QuALITY 数据集（265 本书/文章），评估闭卷 QA 和摘要生成能力。
    *   **预训练自举**：使用 DCLM 语料库（高达 1T token 级别），评估 OpenWebText2 困惑度及 ARC、MMLU 等 9 个通用基准。
    *   **自动化研究**：在 nanoGPT（预训练速度优化）和 GRPO（数学推理强化学习）两个 GPU 密集型环境中进行。
*   **Benchmark 与对比方法**：
    *   对比了直接在原始数据上训练（Raw CPT）、简单改写训练（Rephrase CPT）以及检索增强生成（RAG）。
    *   在预训练实验中，采用了“算力匹配”（Compute-matched）的对比方式，基准为重复训练（Repetition baseline），上限为拥有无限唯一数据的“先知”（Oracle）。

### 4. 资源与算力
论文涉及了极高规格的算力消耗：
*   **持续预训练**：在 8×H100 节点上运行，吞吐量约 6090 tokens/s。
*   **大规模预训练（SBP）**：
    *   200B token 规模实验：消耗约 1.1 万 v5p-TPU 小时。
    *   1T token 规模实验（3B 模型）：消耗约 5.9 万 v5p-TPU 小时。
    *   1T token 规模实验（6B 模型）：消耗约 26.5 万 v5p-TPU 小时。
*   **自动化研究**：GRPO 实验涉及并行调用 256 块 GPU，nanoGPT 实验涉及 1024 块 GPU 的并行执行队列。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 3B、6B 到 32B（s1-32B）不同参数规模的模型，训练步数达到万亿级 token。
*   **消融实验**：非常充分。包括合成数据比例、生成器强度（如用 Llama 3.1 8B 代替 GPT-4 生成数据以排除蒸馏影响）、数据去重策略、搜索算法（进化搜索 vs Best-of-N）等。
*   **客观性**：通过“算力匹配”确保了对比的公平性，即在相同的 FLOPs 预算下，证明合成数据优于简单的重复训练。

### 6. 论文的主要结论与发现
*   **EntiGraph 效果**：在小语种/专业语料上，QA 准确率随合成 token 数量呈对数线性增长，且能与 RAG 效果互补。
*   **SBP 潜力**：在数据受限的情况下，SBP 能弥补约 40%-60% 与“无限唯一数据”之间的性能差距，证明模型可以从现有数据的相关性中提取额外信号。
*   **预训练是瓶颈**：通过 s1-32B 实验发现，仅需 1000 条高质量推理数据即可让模型达到 o1 水平，说明推理能力主要源于预训练而非微调。
*   **自动化研究可行性**：AI 能够发现优于人类设计的超参数和算法改进（如在 GRPO 环境中准确率从 48% 提升至 69%）。

### 7. 优点
*   **理论与实践结合**：不仅有大规模工程实验，还通过贝叶斯层次概念模型和物理学类比（爱因斯坦场方程）为自改进提供了深刻的直觉解释。
*   **严谨的自改进定义**：明确排除了“从更强模型蒸馏”的作弊行为，真正探讨了 AI 如何在封闭系统中实现熵减和能力提升。
*   **开源贡献**：开源了 s1K 数据集、s1-32B 模型以及自动化研究环境。

### 8. 不足与局限
*   **幻觉风险**：尽管有接地（Grounding）策略，合成数据仍存在事实性错误风险，尤其在处理极高难度文本时。
*   **泛化性挑战**：自动化研究发现的算法改进在不同规模模型间的迁移性尚未得到完全验证。
*   **算力门槛**：论文提出的自改进路径（如 SBP 和大规模搜索）对算力要求极高，普通研究机构难以复现。
*   **应用限制**：目前主要集中在文本和推理领域，对于多模态或具身智能的自改进探讨较少。

（完）
