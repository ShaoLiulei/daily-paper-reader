---
title: "Fusian: Multi-LoRA Fusion for Fine-Grained Continuous MBTI Personality Control in Large Language Models"
title_zh: Fusian：大语言模型中用于细粒度连续 MBTI 人格控制的多 LoRA 融合
authors: "Zehao Chen, Rong Pan"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15405v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于大语言模型人格控制的有监督微调与 LoRA
tldr: 针对大语言模型在模拟人格时仅能处理离散类别而缺乏连续强度控制的问题，本文提出了Fusian框架。该框架通过“轨迹收集”捕获SFT过程中LoRA适配器的演化以映射特质流形，并利用强化学习训练策略网络动态计算融合权重。通过多LoRA融合机制，Fusian能根据数值目标精确调节MBTI人格特质强度。实验证明，该方法在控制精度上显著优于传统方法，实现了细粒度的连续人格模拟。
selection_source: fresh_fetch
motivation: 现有的人格控制方法通常将特质视为离散类别，缺乏在连续频谱上精确控制人格特质强度的能力。
method: Fusian通过收集SFT过程中的LoRA轨迹并结合强化学习训练的动态融合策略，实现了对多个适配器的加权集成。
result: 在Qwen模型上的实验结果显示，Fusian在对齐用户指定的特质强度方面表现出极高的精度，显著优于基准方法。
conclusion: Fusian框架成功实现了大语言模型中细粒度、连续的人格控制，为模拟复杂且多样化的人类行为提供了新途径。
---

## 摘要
大语言模型（LLMs）在模拟多样化的人类行为和人格方面展现出了令人印象深刻的能力。然而，现有的性格控制方法（包括提示工程和标准有监督微调 SFT）通常将人格特质视为离散类别（例如“外向”与“内向”），缺乏在连续光谱上精确控制特质强度的能力。在本文中，我们提出了 Fusian，这是一个用于大语言模型细粒度、连续人格控制的新型框架。Fusian 分两个阶段运行：(1) 轨迹采集（Trajectory Collection），通过保存一系列 LoRA 适配器来捕捉 SFT 过程中人格习得的动态演变，从而有效地映射特质的连续流形；(2) 基于强化学习（RL）的动态融合（RL-based Dynamic Fusion），利用强化学习训练策略网络，为这些冻结的适配器动态计算混合权重。通过从策略网络参数化的狄利克雷分布（Dirichlet distribution）中进行采样，Fusian 融合了多个适配器，使模型的输出与特定的数值目标强度保持一致。在 Qwen3-14B 模型上的实验表明，Fusian 在人格控制方面实现了高精度，在对齐用户指定的特质强度方面显著优于基准方法。

## Abstract
Large Language Models (LLMs) have demonstrated impressive capabilities in simulating diverse human behaviors and personalities. However, existing methods for personality control, which include prompt engineering and standard Supervised Fine-Tuning (SFT), typically treat personality traits as discrete categories (e.g., "Extroverted" vs. "Introverted"), lacking the ability to precisely control the intensity of a trait on a continuous spectrum. In this paper, we introduce Fusian, a novel framework for fine-grained, continuous personality control in LLMs. Fusian operates in two stages: (1) Trajectory Collection, where we capture the dynamic evolution of personality adoption during SFT by saving a sequence of LoRA adapters, effectively mapping the continuous manifold of a trait; and (2) RL-based Dynamic Fusion, where we train a policy network using Reinforcement Learning to dynamically compute mixing weights for these frozen adapters. By sampling from a Dirichlet distribution parameterized by the policy network, Fusian fuses multiple adapters to align the model's output with a specific numerical target intensity. Experiments on the Qwen3-14B model demonstrate that Fusian achieves high precision in personality control, significantly outperforming baseline methods in aligning with user-specified trait intensities.

---

## 论文详细总结（自动生成）

### 论文总结：Fusian —— 大语言模型中用于细粒度连续 MBTI 人格控制的多 LoRA 融合

#### 1. 核心问题与整体含义（研究动机和背景）
现有的大语言模型（LLM）人格控制方法（如提示工程或标准有监督微调 SFT）通常将人格特质视为**离散的类别**（例如：要么是“外向型”，要么是“内向型”）。然而，人类的人格在心理学上（如 MBTI 或大五人格）是**连续的频谱**，具有不同的强度。
*   **研究动机**：现有的离散控制缺乏灵活性，无法精确调节特质强度（如“78% 的感知型”）。
*   **核心挑战**：参数空间与语义强度之间存在非线性映射，简单的线性插值或数值提示词（Prompt）难以实现高精度的连续控制。

#### 2. 方法论：核心思想与关键技术
论文提出了 **Fusian** 框架，通过两个阶段实现连续人格控制：
*   **阶段 1：轨迹收集（Trajectory Collection）**
    *   在对特定人格特质进行 SFT 过程中，高频保存 LoRA 适配器（Adapter）检查点。
    *   使用标准 MBTI 测试对每个检查点进行评估，计算其“特质百分比”（Trait Percentage），从而构建一个描述人格演化的“连续流形”。
    *   **稳定基准选择**：利用滑动窗口方差过滤掉不稳定的检查点，并均匀采样 $N$ 个适配器作为基准集。
*   **阶段 2：基于强化学习的动态融合（RL-based Dynamic Fusion）**
    *   **策略网络**：训练一个 MLP 策略网络，输入目标强度，输出狄利克雷分布（Dirichlet distribution）的参数。
    *   **权重采样与融合**：从分布中采样混合权重 $w$，将多个冻结的 LoRA 适配器线性融合：$\Delta\theta_{fused} = \sum w_i \cdot \Delta\theta_i$。
    *   **强化学习优化**：使用 REINFORCE 算法，设计“激进型”奖励函数（指数惩罚偏差 + 精度奖金），使融合后的模型输出精确对齐目标数值。

#### 3. 实验设计
*   **基础模型**：Qwen3-14B。
*   **数据集**：使用 Personality Control Datasets，包含 MBTI 各维度的 SFT 训练对和标准心理测试验证集。
*   **评估指标**：平均绝对误差（MAE，越低越好）和皮尔逊相关系数（$r$，越高越好，衡量线性控制能力）。
*   **对比基准（Benchmarks）**：
    *   提示工程（Prompt Engineering）：包括 Qwen3-14B 和 GPT-5-mini。
    *   标准 LoRA：选择最接近目标的单个检查点。
    *   PISF：现有的人格控制基准方法。
    *   人格向量（P-Vector）：通过缩放差异向量进行模型合并的方法。

#### 4. 资源与算力
*   论文提到了训练细节（如 LoRA rank=8, AdamW 优化器, 20 个 Epoch 等），但**未明确说明具体的硬件资源**（如 GPU 型号、数量）以及总体的训练时长。

#### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了 MBTI 的四个主要维度（E/I, S/N, T/F, P/J），并在每个维度上进行了多组目标强度的测试。
*   **消融实验**：针对“动态融合”、“稳定基准选择”和“奖励函数设计”分别做了消融，验证了各组件的必要性。
*   **定性分析**：提供了“情感（Feeling）”维度的案例研究，展示了模型随强度增加从“逻辑分析”向“情感支持”的平滑过渡。
*   **评价**：实验设计较为客观，对比了当前最先进的模型（如 GPT-5-mini），证明了参数级控制优于提示词控制。

#### 6. 主要结论与发现
*   **高精度控制**：Fusian 在所有维度上的 MAE 显著低于基准方法（总体 MAE 为 6.79，而 GPT-5-mini 为 14.97）。
*   **强相关性**：皮尔逊系数达到 0.88，表明 Fusian 能够提供平滑、单调的控制“旋钮”。
*   **攻克难点**：在“直觉（N）”这一语义模糊、非线性强的维度上，Fusian 依然保持了 0.79 的高相关性，而其他方法在该维度大多失效。
*   **非线性特性**：实验证实了 SFT 轨迹并非完全单调，且在接近极端人格时存在“边际效用递减”现象。

#### 7. 优点
*   **连续性**：突破了离散人格分类的限制，实现了真正的细粒度数值控制。
*   **鲁棒性**：通过 RL 学习非线性映射，比简单的线性插值（Linear Interpolation）更精确。
*   **即插即用**：基于 LoRA 融合，不需要重新训练整个大模型，推理时动态加载权重。

#### 8. 不足与局限
*   **维度局限**：目前主要针对单一 MBTI 维度的控制，尚未实现多个特质（如同时控制外向度和理性度）的复杂组合控制。
*   **计算开销**：在 RL 训练阶段，需要频繁调用模型生成响应以计算奖励，离线训练成本较高。
*   **评估依赖**：控制精度高度依赖于 MBTI 测试题项的质量，可能存在心理测量学上的偏差风险。

（完）
