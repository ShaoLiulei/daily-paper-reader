---
title: "SFedHIFI: Fire Rate-Based Heterogeneous Information Fusion for Spiking Federated Learning"
title_zh: SFedHIFI：基于脉冲率的异构信息融合脉冲联邦学习
authors: "Ran Tao, Qiugang Zhan, Shantian Yang, Xiurui Xie, Qi Tian, Guisong Liu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14956v1"
tags: ["query:snn"]
score: 10.0
evidence: 用于脉冲神经网络的异构脉冲联邦学习框架
tldr: 针对脉冲联邦学习（SFL）中模型同质化要求限制资源受限客户端参与的问题，本文提出SFedHIFI框架。该框架利用基于脉冲发放率的异构信息融合，通过通道级矩阵分解为不同客户端部署自适应规模的SNN模型，并实现跨尺度的模型聚合。实验证明，该方法在保持高能效的同时，有效提升了异构SFL系统的性能，优于现有基准。
selection_source: fresh_fetch
motivation: 现有的脉冲联邦学习要求模型同质，导致计算资源有限的客户端无法参与，难以应对现实中的系统异构性。
method: 提出SFedHIFI框架，采用通道级矩阵分解部署自适应规模模型，并利用异构信息融合模块实现不同宽度模型间的跨尺度聚合。
result: 在三个公共数据集上的实验表明，SFedHIFI性能优于所有基准方法，且相比人工神经网络（ANN）联邦学习显著降低了能耗。
conclusion: SFedHIFI成功实现了高效的异构脉冲联邦学习，在资源受限环境下展现了极佳的灵活性和能效优势。
---

## 摘要
脉冲联邦学习（SFL）凭借脉冲神经网络（SNNs）的能效优势得到了广泛研究。然而，现有的 SFL 方法要求模型同构，并假设所有客户端都拥有充足的计算资源，导致一些资源受限的客户端被排除在外。为了应对现实场景中普遍存在的系统异构性，构建允许客户端根据本地资源自适应部署不同规模模型的异构 SFL 系统至关重要。为此，我们提出了 SFedHIFI，这是一种具有基于脉冲率的异构信息融合的新型脉冲联邦学习框架。具体而言，SFedHIFI 采用通道级矩阵分解，在异构资源的客户端上部署具有自适应复杂度的 SNN 模型。在此基础上，所提出的异构信息融合模块实现了不同宽度模型之间的跨尺度聚合，从而增强了对多样化本地知识的利用。在三个公共基准数据集上的广泛实验表明，SFedHIFI 能够有效实现异构 SFL，且性能始终优于所有三种基准方法。与基于人工神经网络（ANN）的联邦学习相比，它在仅损失极小精度的情况下实现了显著的节能。

## Abstract
Spiking Federated Learning (SFL) has been widely studied with the energy efficiency of Spiking Neural Networks (SNNs). However, existing SFL methods require model homogeneity and assume all clients have sufficient computational resources, resulting in the exclusion of some resource-constrained clients. To address the prevalent system heterogeneity in real-world scenarios, enabling heterogeneous SFL systems that allow clients to adaptively deploy models of different scales based on their local resources is crucial. To this end, we introduce SFedHIFI, a novel Spiking Federated Learning framework with Fire Rate-Based Heterogeneous Information Fusion. Specifically, SFedHIFI employs channel-wise matrix decomposition to deploy SNN models of adaptive complexity on clients with heterogeneous resources. Building on this, the proposed heterogeneous information fusion module enables cross-scale aggregation among models of different widths, thereby enhancing the utilization of diverse local knowledge. Extensive experiments on three public benchmarks demonstrate that SFedHIFI can effectively enable heterogeneous SFL, consistently outperforming all three baseline methods. Compared with ANN-based FL, it achieves significant energy savings with only a marginal trade-off in accuracy.

---

## 论文详细总结（自动生成）

以下是对论文《SFedHIFI: Fire Rate-Based Heterogeneous Information Fusion for Spiking Federated Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的脉冲联邦学习（SFL）通常要求所有客户端使用完全相同的模型架构（同质性）。然而，现实中的边缘设备计算资源差异巨大，强制同构会导致资源受限的设备无法参与，或者造成资源浪费。
*   **研究动机**：虽然人工神经网络（ANN）已有异构联邦学习方案，但脉冲神经网络（SNN）具有高度稀疏和二值化的特性，直接套用传统的模型剪枝或聚合方法会导致严重的严重信息丢失。
*   **整体含义**：本文旨在开发一种能够让不同客户端根据自身资源部署不同规模（宽度）SNN模型，并能有效融合这些异构模型知识的联邦学习框架。

### 2. 方法论：核心思想与关键技术
SFedHIFI 框架的核心由两个主要部分组成：
*   **通道级矩阵分解（Channel-wise Matrix Decomposition）**：
    *   将卷积层权重 $W$ 分解为统一的**共享基矩阵 $B$** 和客户端特定的**尺度因子矩阵 $M$**（即 $W \approx B \cdot M$）。
    *   $B$ 矩阵在所有客户端间共享，捕获全局通用特征；$M$ 矩阵根据客户端资源调整维度，实现模型宽度的自适应缩放。
*   **基于脉冲发放率的异构信息融合（HIFI 模块）**：
    *   **信息密度识别**：利用 SNN 的生物特性——脉冲发放率（Fire Rate）来衡量各层的信息密度。发放率高的层被认为含有更丰富的特征信息。
    *   **跨尺度融合**：在服务端，识别出信息密度最高的层，对这些层的 $M$ 矩阵进行 **Tucker 分解**，提取核心张量（Core Tensor）进行跨尺度聚合。
    *   **知识增强**：通过这种方式，小规模模型可以获得大规模模型的知识指导，而大规模模型也能整合来自不同尺度的多样化特征。
*   **局部优化**：引入正交正则化项约束 $B$ 矩阵，并采用时间高效训练（TET）损失函数来增强 SNN 的泛化能力。

### 3. 实验设计
*   **数据集**：Fashion-MNIST、CIFAR-10、CIFAR-100。
*   **数据分布场景**：包含 IID（独立同分布）和 Non-IID（非独立同分布，使用 Dirichlet 分布 $\alpha=0.5, 0.3$ 模拟）。
*   **模型规模**：设置了四种宽度尺度：0.25×、0.5×、0.75×、1.0×。
*   **对比基准（Benchmarks）**：
    *   **FedAVG、FedProx、FedNova**：这些方法在异构环境下被修改为仅在相同规模的模型间进行聚合（作为 Baseline）。
    *   **消融实验**：对比了无 HIFI 模块、固定层融合、随机层融合等变体。
    *   **能效对比**：与同等规模的 ANN 联邦学习进行精度与功耗对比。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 `Spikejelly` 框架，SNN 采用 LIF 神经元，时间步 $T=10$，客户端总数为 10 个，每轮随机选择 5 个参与。

### 5. 实验数量与充分性
*   **实验数量**：在 3 个数据集上分别测试了 3 种数据分布和 4 种模型尺度，并进行了详细的消融实验和能效分析，实验组数较为充沛。
*   **充分性与公平性**：
    *   对比了多种经典的联邦学习改进算法。
    *   通过 Non-IID 场景验证了算法在极端数据异构下的鲁棒性。
    *   实验设计考虑了 SNN 特有的能效指标，对比维度较为全面。

### 6. 主要结论与发现
*   **性能提升**：SFedHIFI 在所有数据集和尺度下均优于基准方法。在复杂的 CIFAR-100 和强 Non-IID 场景下，准确率提升尤为显著（最高提升约 19%-30%）。
*   **跨尺度增益**：证明了通过 HIFI 模块，小模型能显著从大模型的聚合知识中获益。
*   **能效优势**：相比 ANN 联邦学习，SFedHIFI 在 Fashion-MNIST 上实现了约 **43.74% 的能耗降低**，而精度损失不到 1%，证明了其在边缘计算场景的实用性。
*   **脉冲率相关性**：实验证实了脉冲发放率确实能有效反映 SNN 层的特征重要性，基于此的融合策略优于随机或固定层融合。

### 7. 优点（亮点）
*   **针对性强**：专门解决了 SNN 在异构联邦学习中因稀疏性导致的信息丢失问题。
*   **生物启发**：巧妙利用 SNN 固有的“脉冲发放率”作为启发式信号来指导模型聚合，具有较强的解释性。
*   **灵活性**：支持客户端根据硬件能力动态调整模型宽度，提高了 SFL 系统的包容性。

### 8. 不足与局限
*   **异构维度单一**：目前主要关注模型**宽度**（通道数）的异构，未深入探讨深度（层数）异构或异构神经元模型的影响。
*   **计算开销**：服务端进行的 Tucker 分解和跨尺度融合虽然提升了精度，但可能增加服务端的计算负担。
*   **实验规模**：客户端总数（10个）相对较小，在大规模分布式系统（如成千上万个节点）中的表现尚待验证。
*   **数据集局限**：主要集中在图像分类任务，未在 DVS（动态视觉传感器）等原生脉冲数据集上进行测试。

（完）
