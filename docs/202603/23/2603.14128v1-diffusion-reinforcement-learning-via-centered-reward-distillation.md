---
title: Diffusion Reinforcement Learning via Centered Reward Distillation
title_zh: 基于中心化奖励蒸馏的扩散强化学习
authors: "Yuanzhi Zhu, Xi Wang, Stéphane Lathuilière, Vicky Kalogeiton"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.14128v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 通过奖励蒸馏进行扩散强化学习
tldr: 针对扩散模型在精细化提示词遵循和文本渲染方面的不足，本文提出了一种名为中心化奖励蒸馏（CRD）的强化学习微调框架。该方法基于KL正则化奖励最大化，通过在提示词内进行中心化处理消除了难以计算的归一化常数。结合解耦采样器、KL锚定和自适应KL强度等技术，CRD在提升生成质量的同时有效抑制了奖励作弊，在GenEval和OCR任务中达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有的扩散模型强化学习微调方法存在内存开销大、梯度方差高以及容易出现分布偏移和奖励作弊等问题。
method: 提出CRD框架，利用提示词内中心化消除归一化常数，并引入解耦采样、KL锚定和自适应KL强度来稳定训练。
result: 在GenEval和OCR奖励任务上的实验表明，CRD实现了快速收敛，并在优化奖励的同时显著减少了奖励作弊现象。
conclusion: CRD为扩散模型的后训练提供了一种高效且稳健的强化学习方案，有效平衡了生成性能与训练稳定性。
---

## 摘要
扩散模型和流模型取得了最先进的（SOTA）生成性能，然而许多在实际中非常重要的行为，如细粒度的提示词忠实度、组合正确性和文本渲染，在得分匹配或流匹配预训练目标中的定义较为薄弱。使用外部黑盒奖励进行强化学习（RL）微调是一种自然的补救措施，但扩散强化学习通常较为脆弱。基于轨迹的方法会产生高内存成本和高方差的梯度估计；前向过程方法收敛更快，但可能会遭受分布偏移，从而导致奖励作弊（reward hacking）。在这项工作中，我们提出了中心化奖励蒸馏（Centered Reward Distillation, CRD），这是一个基于前向过程微调、由 KL 正则化奖励最大化推导出的扩散强化学习框架。核心见解是，在提示词内中心化（within-prompt centering）下，难以计算的归一化常数会被抵消，从而产生一个适定的奖励匹配目标。为了实现可靠的文本到图像微调，我们引入了显式控制分布偏移的技术：(i) 将采样器与移动参考解耦，以防止比例信号崩溃；(ii) 将 KL 锚定到受 CFG 引导的预训练模型，以控制长期偏移并与预训练模型的推理时语义保持一致；(iii) 奖励自适应 KL 强度，以在较大的 KL 正则化下加速早期学习，同时减少后期对奖励模型漏洞的利用。在 GenEval 和 OCR 奖励的文本到图像后训练实验表明，CRD 实现了具有竞争力的 SOTA 奖励优化结果，具有快速收敛和减少奖励作弊的特点，并在未见过的偏好指标上得到了验证。

## Abstract
Diffusion and flow models achieve State-Of-The-Art (SOTA) generative performance, yet many practically important behaviors such as fine-grained prompt fidelity, compositional correctness, and text rendering are weakly specified by score or flow matching pretraining objectives. Reinforcement Learning (RL) fine-tuning with external, black-box rewards is a natural remedy, but diffusion RL is often brittle. Trajectory-based methods incur high memory cost and high-variance gradient estimates; forward-process approaches converge faster but can suffer from distribution drift, and hence reward hacking. In this work, we present \textbf{Centered Reward Distillation (CRD)}, a diffusion RL framework derived from KL-regularized reward maximization built on forward-process-based fine-tuning. The key insight is that the intractable normalizing constant cancels under \emph{within-prompt centering}, yielding a well-posed reward-matching objective. To enable reliable text-to-image fine-tuning, we introduce techniques that explicitly control distribution drift: (\textit{i}) decoupling the sampler from the moving reference to prevent ratio-signal collapse, (\textit{ii}) KL anchoring to a CFG-guided pretrained model to control long-run drift and align with the inference-time semantics of the pre-trained model, and (\textit{iii}) reward-adaptive KL strength to accelerate early learning under large KL regularization while reducing late-stage exploitation of reward-model loopholes. Experiments on text-to-image post-training with \texttt{GenEval} and \texttt{OCR} rewards show that CRD achieves competitive SOTA reward optimization results with fast convergence and reduced reward hacking, as validated on unseen preference metrics.

---

## 论文详细总结（自动生成）

这是一份关于论文《Diffusion Reinforcement Learning via Centered Reward Distillation (CRD)》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
尽管扩散模型（Diffusion Models）在图像生成上表现优异，但在**细粒度提示词遵循（Prompt Fidelity）**、**组合正确性**和**文本渲染**方面仍存在不足。传统的预训练目标（得分匹配或流匹配）难以捕捉这些复杂的语义需求。
*   **现有方法的局限：** 
    *   **基于轨迹的方法（如 DPO、策略梯度）：** 内存开销巨大，梯度估计方差高。
    *   **前向过程方法（如 ReFL）：** 虽然收敛快，但容易产生**分布偏移（Distribution Drift）**，导致模型为了刷高奖励分数而牺牲图像质量（即**奖励作弊 Reward Hacking**）。
*   **研究目标：** 提出一种高效、稳健的强化学习（RL）微调框架，在提升生成质量的同时抑制奖励作弊。

### 2. 论文提出的方法论：CRD 框架
CRD 的核心是基于 **KL 正则化奖励最大化** 推导出的奖励匹配目标。
*   **核心思想：中心化奖励蒸馏（Within-prompt Centering）**
    *   在数学推导中，KL 正则化目标通常涉及一个难以计算的归一化常数（配分函数）。
    *   CRD 通过在同一个提示词（Prompt）生成的多个样本之间进行奖励中心化处理，使归一化常数在对数空间中被抵消，从而将 RL 问题转化为一个可处理的加权最小二乘回归问题。
*   **关键技术细节：**
    1.  **解耦采样器（Decoupled Sampler）：** 将采样过程与移动参考模型解耦，防止训练过程中比例信号坍缩，增强梯度稳定性。
    2.  **KL 锚定（KL Anchoring）：** 并非锚定到原始预训练模型，而是锚定到受 **CFG（分类器自由引导）** 引导的模型。这确保了微调后的模型与推理时的语义逻辑保持一致，控制长期偏移。
    3.  **奖励自适应 KL 强度（Reward-adaptive KL）：** 动态调整 KL 惩罚力度。在训练初期使用较小惩罚以加速学习，在后期加大惩罚以防止模型利用奖励模型的漏洞进行作弊。

### 3. 实验设计
*   **场景与任务：** 文本到图像（T2I）生成的后训练微调。
*   **数据集/Benchmark：**
    *   **GenEval：** 用于评估组合能力（如物体计数、属性绑定、位置关系）。
    *   **OCR 奖励任务：** 评估模型在图像中准确渲染文本的能力。
*   **对比方法：** 包括 ReFL、DRaFT、AlignProp 等当前主流的扩散模型 RL 微调基准方法。
*   **验证指标：** 除了目标奖励外，还使用了未参与训练的第三方偏好指标（如 PickScore、ImageReward）来验证泛化性和图像质量。

### 4. 资源与算力
*   **算力说明：** 论文摘要和元数据中未明确给出具体的 GPU 型号和总训练时长。但文中强调了该方法具有**快速收敛**和**低内存成本**的特性，暗示其比传统的基于轨迹的 RL 方法更节省资源。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 GenEval 和 OCR 两个具有代表性的挑战性任务。
*   **消融实验：** 论文对“解耦采样”、“KL 锚定”和“自适应 KL 强度”等核心组件进行了消融研究，验证了各模块对抑制奖励作弊和提升稳定性的贡献。
*   **客观性：** 通过引入“未见过的偏好指标”进行交叉验证，证明了得分的提升并非仅仅是过拟合于特定的奖励模型，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **性能领先：** CRD 在 GenEval 和 OCR 任务上达到了 SOTA 水平，显著优于之前的微调方法。
*   **稳定性强：** 通过中心化和 KL 锚定，CRD 有效解决了扩散 RL 中常见的训练不稳定问题。
*   **抑制作弊：** 实验证明，CRD 在优化奖励的同时，能更好地保持图像的自然度和审美质量，减少了因过度优化奖励函数而导致的图像畸变。

### 7. 优点
*   **理论优雅：** 通过简单的中心化技巧解决了复杂的归一化常数问题。
*   **实用性高：** 解决了 CFG 引导下的分布偏移问题，使微调模型在推理阶段表现更可靠。
*   **平衡性好：** 成功在“奖励提升”与“分布保持（图像质量）”之间找到了较好的平衡点。

### 8. 不足与局限
*   **依赖奖励模型：** 效果上限仍受限于外部黑盒奖励模型的准确性。如果奖励模型本身存在严重偏见，CRD 仍可能学习到错误的行为。
*   **计算开销：** 虽然比轨迹法省资源，但相比于简单的监督微调（SFT），RL 过程仍需要多次前向采样，计算成本依然存在。
*   **应用范围：** 目前主要验证了图像生成任务，在视频生成或 3D 生成等更复杂领域的适用性尚待验证。

（完）
