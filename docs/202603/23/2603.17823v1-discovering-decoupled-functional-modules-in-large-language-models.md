---
title: Discovering Decoupled Functional Modules in Large Language Models
title_zh: 发现大语言模型中的解耦功能模块
authors: "Yanke Yu, Jin Li, Ying Sun, Ping Li, Zhefeng Wang, Yi Zheng"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17823v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型的内部功能组织
tldr: 本研究旨在探索大语言模型（LLM）内部的功能模块组织。作者提出了无监督跨层模块发现框架（ULCMOD），通过新颖的目标函数和迭代解耦算法（IterD），将全模型神经元解耦为具有特定语义的功能模块，并同步识别输入样本的主题。实验证明，该方法能发现高质量、语义连贯且具有空间层次结构的模块，显著提升了模型的可解释性及下游任务表现，填补了LLM功能模块化研究的空白。
selection_source: fresh_fetch
motivation: 为了解决大语言模型内部功能组织方式不明、神经元功能耦合难以解释的问题。
method: 提出ULCMOD框架，通过迭代解耦算法在无监督下同时实现神经元模块化划分与输入主题发现。
result: 发现的模块展现出高度的语义连贯性、清晰的层级组织，并在多项下游任务中验证了其有效性。
conclusion: 该工作为LLM的功能模块解释提供了创新工具，填补了模型内部组织结构研究的空白。
---

## 摘要
理解大语言模型（LLMs）的内部功能组织对于提高其可信度和性能至关重要。然而，LLMs 如何将不同功能组织成模块在很大程度上仍未得到充分探索。为了填补这一空白，我们定义了功能模块发现问题，并提出了一个无监督 LLM 跨层模块发现（ULCMOD）框架，该框架在将整个 LLM 中的海量神经元解耦为模块的同时，能够发现与这些模块相关的输入样本主题。我们的框架引入了一种新颖的目标函数和一种高效的迭代解耦（IterD）算法。大量实验表明，我们的方法发现了高质量、解耦的模块，这些模块捕获了更具意义的语义信息，并在各种下游任务中取得了优异的性能。此外，定性分析表明，所发现的模块展现出语义连贯性，对应于可解释的专业化分工，并在 LLM 内部呈现出清晰的空间和层级组织。我们的工作为解释 LLMs 的功能模块提供了一种新工具，填补了 LLM 可解释性研究中的一项关键空白。

## Abstract
Understanding the internal functional organization of Large Language Models (LLMs) is crucial for improving their trustworthiness and performance. However, how LLMs organize different functions into modules remains highly unexplored. To bridge this gap, we formulate a functional module discovery problem and propose an Unsupervised LLM Cross-layer MOdule Discovery (ULCMOD) framework that simultaneously disentangles the large set of neurons in the entire LLM into modules while discovering the topics of input samples related to these modules. Our framework introduces a novel objective function and an efficient Iterative Decoupling (IterD) algorithm. Extensive experiments show that our method discovers high-quality, disentangled modules that capture more meaningful semantic information and achieve superior performance in various downstream tasks. Moreover, our qualitative analysis reveals that the discovered modules show semantic coherence, correspond to interpretable specializations, and a clear spatial and hierarchical organization within the LLM. Our work provides a novel tool for interpreting the functional modules of LLMs, filling a critical blank in LLM's interpretability research.

---

## 论文详细总结（自动生成）

### 论文总结：发现大语言模型中的解耦功能模块

#### 1. 论文的核心问题与整体含义（研究动机和背景）
尽管大语言模型（LLMs）表现出卓越的能力，但其内部功能组织机制仍不透明。现有研究虽识别出某些神经元与特定功能（如数学、编程）相关，但这些神经元集合往往高度重叠（耦合），难以清晰界定独立的功能模块。
本研究受人类大脑具有高度专业化且解耦的功能分区启发，旨在探索 LLM 内部是否存在类似的解耦模块。论文定义了**功能模块发现问题**，即寻找一组神经元和一组样本，使得该神经元组在对应样本组上具有高强度的协同激活，且不同模块之间尽可能独立。

#### 2. 论文提出的方法论
论文提出了 **ULCMOD（无监督 LLM 跨层模块发现）** 框架，核心思想是通过双向划分（神经元和样本）来识别功能模块。

*   **核心目标函数 $L(F)$**：
    *   由两部分组成：$L(F) = \xi(F) \cdot B(F)$。
    *   **激活模块化得分 $\xi(F)$**：衡量模块内部神经元与样本之间的平均激活强度，旨在最大化模块内的协同激活。
    *   **平衡得分 $B(F)$**：使用调和平均数惩罚过大或过小的模块，确保 $K$ 个模块在规模上相对均衡，避免产生无意义的碎片化模块。
*   **IterD（迭代解耦）算法**：
    *   **初始化**：使用 K-Means 等基础聚类方法产生初始划分。
    *   **步骤 1：优化神经元分配**：固定样本划分，遍历每个神经元，将其重新分配到能使 $L(F)$ 最大化的模块中。
    *   **步骤 2：优化样本分配**：固定更新后的神经元划分，遍历每个样本，将其重新分配到能使 $L(F)$ 最大化的模块中。
    *   **迭代**：重复上述两步直至收敛（即分配不再发生变化）。

#### 3. 实验设计
*   **模型对象**：Qwen2.5 系列（1.5B-Instruct, 3B-Instruct, 7B-Instruct）。
*   **数据集**：从 **Infinity-Instruct** 中筛选出 8,400 个样本，涵盖 7 个明确类别：编程（Coding）、数学（Math）、语言学（Linguistic）、知识（Knowledge）、翻译（Translation）、伦理（Ethical）和写作（Writing）。
*   **Benchmark 与对比方法**：
    *   对比了五种经典聚类算法：K-Means、Mini-Batch K-Means、层次聚类（Agglomerative）、谱聚类（Spectral Clustering）和谱协同聚类（Spectral Co-clustering）。
*   **评估指标**：
    *   目标函数值（$L, \xi, B$）。
    *   **功能模块信息量 $I(F)$**：利用发现的模块激活模式作为特征，训练线性分类器预测样本标签，准确率越高说明模块捕获的语义越准确。

#### 4. 资源与算力
论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）及算法运行的总时长。但考虑到涉及全量神经元（如 7B 模型）的激活提取与迭代优化，该过程通常需要较高的显存支持和计算开销。

#### 5. 实验数量与充分性
*   **实验规模**：在三种不同参数规模的模型上进行了验证，并设置了不同的模块数量 $K \in \{10, 15, 20\}$。
*   **充分性**：实验不仅包含了定量指标对比，还进行了大量的定性分析（如热力图可视化、神经元层级分布分析、利用 LLM 自动总结模块语义等）。
*   **客观性**：通过与多种基准聚类算法对比，并使用下游分类任务作为客观评价标准，证明了该方法的优越性。

#### 6. 论文的主要结论与发现
*   **IterD 算法的优越性**：在所有模型规模和 $K$ 值下，IterD 发现的模块在激活强度和规模平衡性上均显著优于传统聚类方法。
*   **功能连贯性与解耦性**：发现的模块具有明确的语义（如“算法编程”、“角色扮演”），且模块间干扰极小。
*   **功能层级结构**：随着 $K$ 值增大，模块呈现出“由粗到细”的演变（例如“编程”模块会细化为“代码调试”和“系统编程”）。
*   **空间分布规律**：
    *   **层级敏感性**：简单功能（如信息检索）集中在浅层；复杂推理（如数学、编程）集中在中深层。
    *   **语义邻近性**：语义相关的模块（如数学与科学）在神经元空间分布上更接近。

#### 7. 优点
*   **无监督性**：无需预定义标签即可自动发现 LLM 内部的功能组织结构。
*   **双向优化**：同时考虑神经元和样本的对应关系，比单向聚类更符合模型运行逻辑。
*   **可解释性强**：通过可视化和层级分析，为理解 LLM 的“大脑分区”提供了直观工具。
*   **填补空白**：针对神经元功能重叠问题提出了有效的解耦方案。

#### 8. 不足与局限
*   **层级限制**：研究主要集中在 FFN（前馈网络）层，虽然 FFN 存储了大量知识，但忽略了注意力机制（Attention）层在功能模块化中的作用。
*   **硬划分假设**：算法假设每个神经元/样本仅属于一个模块，这在处理具有多义性的神经元或复杂样本时可能过于简化。
*   **超参数依赖**：模块数量 $K$ 需要预先设定，如何自动确定最优的 $K$ 值仍是一个挑战。
*   **计算成本**：对于超大规模模型（如 70B+），全量神经元的迭代优化可能面临严重的扩展性问题。

（完）
