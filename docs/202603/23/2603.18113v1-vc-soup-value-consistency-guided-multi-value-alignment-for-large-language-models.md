---
title: "VC-Soup: Value-Consistency Guided Multi-Value Alignment for Large Language Models"
title_zh: VC-Soup：面向大语言模型的价值一致性引导的多价值对齐
authors: "Hefei Xu, Le Wu, Yu Wang, Min Hou, Han Wu, Zhen Zhang, Meng Wang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18113v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型对齐
tldr: 针对大语言模型在多价值对齐中面临的训练成本高和价值冲突导致性能下降的问题，本文提出VC-soup框架。该框架引入价值一致性指标对偏好数据进行过滤，剔除冲突样本，随后训练多个价值一致的策略模型并进行线性合并，最后通过帕累托过滤优化多价值平衡。VC-soup不仅降低了对齐成本，还显著提升了模型在处理冲突价值时的表现。
selection_source: fresh_fetch
motivation: 现有的多价值对齐方法在处理潜在冲突的价值观时，面临训练成本极高且对齐性能受损的局限性。
method: 提出一种基于价值一致性过滤和参数合并的框架，利用奖励差距向量的余弦相似度量化并筛选一致性数据。
result: 实验和理论分析表明，VC-soup能有效缓解价值冲突，在多价值对齐任务中持续优于现有基准方法。
conclusion: 通过价值一致性引导的数据筛选与模型合并，可以实现更高效且平衡的大语言模型多价值对齐。
---

## 摘要
随着大语言模型（LLMs）日益影响互联网上的内容生成、交互和决策，将其与人类价值观对齐已成为可信人工智能的核心目标。当对齐多个且可能存在冲突的人类价值观时，这一挑战变得更加突出。尽管最近的方法（如奖励重加权、基于提示的监督微调和模型合并）尝试解决多价值对齐问题，但它们仍面临两个主要局限性：（1）为每种价值组合训练单独的模型成本极高；（2）价值冲突会显著降低对齐性能。这些局限性使得在多种人类价值观之间实现理想的权衡变得困难。为了应对这些挑战，我们从数据中价值一致性的角度重新审视多价值对齐，并提出了 VC-soup，这是一个基于价值一致性学习的数据过滤和参数合并框架。我们首先设计了一种价值一致性指标，该指标基于每个偏好对的奖励差距向量与全 1 向量之间的余弦相似度，从而量化其跨价值的连贯性。然后，我们过滤掉每个价值数据集中低一致性的偏好对，并在剩余数据上进行训练，以获得平滑且价值一致的策略模型，从而更好地保持线性模式连接性（linear mode connectivity）。最后，我们线性组合这些策略，并在不同价值之间应用帕累托过滤（Pareto filtering），以获得具有平衡多价值性能的解决方案。大量的实验和理论分析表明，VC-soup 有效地缓解了冲突，并始终优于现有的多价值对齐方法。

## Abstract
As large language models (LLMs) increasingly shape content generation, interaction, and decision-making across the Web, aligning them with human values has become a central objective in trustworthy AI. This challenge becomes even more pronounced when aligning multiple, potentially conflicting human values. Although recent approaches, such as reward reweighting, prompt-based supervised fine-tuning, and model merging, attempt to tackle multi-value alignment, they still face two major limitations: (1) training separate models for each value combination is prohibitively expensive; (2) value conflicts substantially degrade alignment performance. These limitations make it difficult to achieve favorable trade-offs across diverse human values. To address these challenges, we revisit multi-value alignment from the perspective of value consistency in data and propose VC-soup, a data filtering and parameter merging framework grounded in value-consistent learning. We first design a value consistency metric based on the cosine similarity between the reward-gap vector of each preference pair and an all-ones vector, which quantifies its cross-value coherence. We then filter out low-consistency preference pairs in each value dataset and train on the remaining data to obtain smooth, value-consistent policy models that better preserve linear mode connectivity. Finally, we linearly combine these policies and apply Pareto filtering across values to obtain solutions with balanced multi-value performance. Extensive experiments and theoretical analysis demonstrate that VC-soup effectively mitigates conflicts and consistently outperforms existing multi-value alignment methods.

---

## 论文详细总结（自动生成）

以下是对论文《VC-Soup: Value-Consistency Guided Multi-Value Alignment for Large Language Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在大语言模型（LLM）中同时实现多个、甚至相互冲突的人类价值观（如“有用性”与“无害性”）的对齐。
*   **研究动机**：
    *   **训练成本高昂**：现有的多价值对齐方法通常需要为每种不同的偏好权重组合训练独立模型，计算开销巨大。
    *   **价值冲突导致性能下降**：不同价值观在同一提示词下可能存在冲突（例如，为了更有用而牺牲了安全性）。这种数据层面的不一致会导致模型参数更新方向冲突，使得简单的模型合并（Model Merging）产生严重的性能退化。

### 2. 方法论（VC-Soup 框架）
VC-Soup 提出了一种“数据过滤引导参数合并”的思路，核心步骤如下：
*   **价值一致性指标（VC Metric）**：
    1.  利用多个预训练的奖励模型（RM），计算每个偏好对在不同价值维度下的“奖励差距”（Reward Gap）。
    2.  对奖励差距进行标准化处理，构建多维奖励差距向量。
    3.  计算该向量与“全一向量”（代表所有价值同时提升的理想方向）之间的**余弦相似度**，作为该样本的一致性得分。
*   **一致性数据过滤**：设定阈值 $\tau$，仅保留那些在多个维度上表现一致（即同时对多个价值有益）的样本，构建“一致性子集”。
*   **训练价值一致向量（VC Vectors）**：在过滤后的子集上，使用直接偏好优化（DPO）为每个价值维度训练专门的参数增量（LoRA 权重）。
*   **参数合并与帕累托过滤**：
    1.  将不同价值的 VC 向量进行线性加权合并。
    2.  通过在验证集上搜索权重系数，利用帕累托最优（Pareto Optimality）原则筛选出在多价值权衡中表现最佳的一组模型。

### 3. 实验设计
*   **数据集**：
    *   **Anthropic-HH**：包含有用性（Helpfulness）和无害性（Harmlessness）两个维度。
    *   **BeaverTails**：侧重于有用性和安全性（Safety）。
    *   **Honest**：用于扩展到三价值（有用、无害、诚实）对齐实验。
*   **基准模型（Backbone）**：LLaMA2-7B。
*   **对比方法（Baselines）**：
    *   单价值 DPO。
    *   **SeqT**：顺序微调。
    *   **DPO-LW**：线性加权损失函数。
    *   **SOUP**：原始模型合并方法。
    *   **MODPO**：基于边际的多目标 DPO。
    *   **MVA**：基于价值解耦和外推的合并方法。
*   **评估指标**：奖励模型评分（Reward Scores）、GPT-4 胜率（Winrate）、帕累托前沿曲线。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA RTX 5880 Ada GPU**。
*   **训练细节**：采用 LoRA 微调（Anthropic-HH 秩为 64，BeaverTails 秩为 16），学习率为 1e-5，Batch Size 为 2。
*   **时长**：文中未明确给出总训练时长，但强调了由于只需训练一次 VC 向量即可通过合并获得多种配置，其效率远高于重复训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了双价值和三价值对齐场景，进行了大量的超参数（阈值 $\tau$ 和合并权重 $\lambda$）敏感性分析。
*   **消融实验**：对比了“仅合并”、“仅过滤”以及“过滤+合并”的效果，验证了各组件的必要性。
*   **分析深度**：通过计算不同层参数的 L2 距离和余弦相似度，从参数空间几何角度解释了为什么 VC 向量比普通向量更易于合并。
*   **客观性**：同时使用了自动化奖励模型和 GPT-4 作为第三方裁判，评估结果较为全面、客观。

### 6. 主要结论与发现
*   **性能领先**：VC-Soup 在所有测试数据集上的帕累托前沿均优于现有基准，能够实现更优的价值权衡。
*   **缓解冲突**：通过数据过滤，减少了训练过程中的梯度冲突，使得不同价值的模型在参数空间上更接近（L2 距离更小、方向更一致）。
*   **线性模式连接性（LMC）**：理论和实验均证明，价值一致性训练增强了模型的线性模式连接性，使得线性合并后的模型不会掉入高损失区域。
*   **数据效率**：即使使用更少（但更一致）的数据进行训练，VC-Soup 也能获得更好的对齐效果。

### 7. 优点
*   **设计简洁且科学**：将复杂的多目标优化问题简化为“高质量数据筛选 + 线性参数插值”，易于实现。
*   **理论支撑扎实**：结合了线性模式连接性理论，解释了数据一致性与模型可合并性之间的内在联系。
*   **灵活性高**：推理时无需重新训练，通过调整合并权重即可快速适配不同用户的个性化价值偏好。

### 8. 不足与局限
*   **超参数依赖**：一致性阈值 $\tau$ 的选择需要根据具体数据集进行调整，缺乏自动化的确定机制。
*   **奖励模型依赖**：数据过滤的效果高度依赖于初始奖励模型的准确性，如果 RM 本身存在偏见，过滤后的数据质量会受损。
*   **多样性牺牲**：极高的一致性要求可能会过滤掉大量样本，虽然提升了合并稳定性，但可能限制了模型在极端单一价值维度上的上限表现。

（完）
