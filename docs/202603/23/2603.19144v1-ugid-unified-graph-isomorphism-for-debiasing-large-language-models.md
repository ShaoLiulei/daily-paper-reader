---
title: "UGID: Unified Graph Isomorphism for Debiasing Large Language Models"
title_zh: UGID：用于大语言模型去偏的统一图同构框架
authors: "Zikang Ding, Junchi Yao, Junhao Li, Yi Zhang, Wenbo Jiang, Hongbo Liu, Lijie Hu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19144v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 针对Transformer架构大模型的去偏框架
tldr: 针对大语言模型中存在的社会偏见，本文提出UGID框架，从内部表示层面进行去偏。该方法将Transformer建模为结构化计算图，通过强制反事实输入下的图结构同构性，联合约束注意力路由和隐藏状态，有效防止偏见在架构组件间迁移。实验证明UGID在减少偏见的同时，能良好保持模型的通用能力和安全性。
selection_source: fresh_fetch
motivation: 现有的输出层或数据优化去偏方法无法彻底解决嵌入在模型内部表示中的社会偏见问题。
method: 将Transformer建模为计算图，通过强制反事实输入间的图结构同构性，联合约束注意力机制和隐藏表示。
result: 实验表明UGID在分布内和分布外设置下均能显著降低偏见，并有效减少内部结构差异。
conclusion: UGID是一种有效的内部表示级去偏框架，能在不损害模型通用性能和安全性的前提下实现行为对齐。
---

## 摘要
大语言模型（LLMs）表现出显著的社会偏见。输出层或基于数据优化的去偏方法无法完全解决这些偏见，且许多先前的工作表明偏见嵌入在内部表示中。我们提出了用于大语言模型去偏的统一图同构（UGID），这是一种针对大语言模型的内部表示级去偏框架，它将 Transformer 建模为一个结构化计算图，其中注意力机制定义了图的路由边，隐藏状态定义了图节点。具体而言，去偏被公式化为强制图结构在反事实输入之间保持不变性，仅允许在敏感属性上存在差异。UGID 联合约束偏见敏感区域中的注意力路由和隐藏表示，有效地防止了偏见在架构组件之间的迁移。为了在不降低通用能力的情况下实现有效的行为对齐，我们引入了针对敏感 Logit 的对数空间约束以及一种基于选择性锚点的目标函数，以保留定义性语义。在大语言模型上的广泛实验表明，UGID 在分布内和分布外设置下均能有效减少偏见，显著降低内部结构差异，并保持模型的安全性和效用。

## Abstract
Large language models (LLMs) exhibit pronounced social biases. Output-level or data-optimization--based debiasing methods cannot fully resolve these biases, and many prior works have shown that biases are embedded in internal representations. We propose \underline{U}nified \underline{G}raph \underline{I}somorphism for \underline{D}ebiasing large language models (\textit{\textbf{UGID}}), an internal-representation--level debiasing framework for large language models that models the Transformer as a structured computational graph, where attention mechanisms define the routing edges of the graph and hidden states define the graph nodes. Specifically, debiasing is formulated as enforcing invariance of the graph structure across counterfactual inputs, with differences allowed only on sensitive attributes. \textit{\textbf{UGID}} jointly constrains attention routing and hidden representations in bias-sensitive regions, effectively preventing bias migration across architectural components. To achieve effective behavioral alignment without degrading general capabilities, we introduce a log-space constraint on sensitive logits and a selective anchor-based objective to preserve definitional semantics. Extensive experiments on large language models demonstrate that \textit{\textbf{UGID}} effectively reduces bias under both in-distribution and out-of-distribution settings, significantly reduces internal structural discrepancies, and preserves model safety and utility.

---

## 论文详细总结（自动生成）

### UGID：用于大语言模型去偏的统一图同构框架 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）在训练过程中吸收并放大了社会偏见。研究发现，这些偏见不仅存在于输出层面，更深层地嵌入在模型的内部表示（如注意力机制和前馈网络 FFN）中。现有的去偏方法（如数据增强或推理时干预）往往无法彻底根除内部偏见，且存在“偏见迁移”现象：在大型模型中，仅约束注意力机制会导致偏见转移到 FFN 存储的语义知识中。本文旨在提出一种统一的结构化去偏框架，从模型内部计算图的拓扑结构入手，实现深层且稳健的去偏。

#### 2. 方法论：核心思想与关键技术
UGID 将 Transformer 的每一层建模为一个动态计算图 $G = (V, E)$，其中节点 $V$ 代表 Token 的隐藏状态，边 $E$ 代表注意力权重。其核心思想是通过**图同构（Graph Isomorphism）**约束，使模型在处理仅敏感属性（如“他”与“她”）不同的反事实输入对时，其内部推理拓扑和语义表示保持一致。

*   **关键技术细节：**
    *   **边对齐（Design I）：** 使用拉普拉斯谱约束（Laplacian Spectral Constraints）对齐注意力路由。通过计算拉普拉斯矩阵 $L=D-A$ 的特征值来捕捉拓扑结构，并引入“注意力汇（Attention Sink）”掩码和自适应显著性权重，聚焦于偏见核心区域。
    *   **节点对齐（Design II）：** 强制反事实上下文下的隐藏表示 $H$ 保持一致，防止偏见迁移至 FFN。
    *   **效用保持（Design III）：** 引入对数空间引导（Log-Space Guidance）防止梯度消失，利用 Top-K 稳定性锚定原始注意力模式，并使用掩码 KL 散度保留通用知识。
    *   **定义性锚点（Design IV）：** 采用选择性对齐策略，通过“锚点对”（如 King/Queen）确保模型保留必要的性别定义语义，避免过度去偏导致的概念抹除。

#### 3. 实验设计
*   **模型范围：** 涵盖 LLaMA-3 (8B/8B-Instruct)、Qwen-2/2.5 (3B/7B/14B) 和 Gemma-2 (2B) 等多个主流模型家族。
*   **数据集：** 
    *   **干预训练：** 构建了一个包含 10 对职业反事实对和 6 对定义性锚点对的 Few-Shot 数据集。
    *   **评估基准：** 使用 BBQ 和 CrowS-Pairs 评估刻板印象；使用 BOLD、RealToxicityPrompts (RTP) 和 HolisticBias 评估生成偏见。
*   **对比方法：** 包括 CDA（反事实数据增强）、KLAAD（注意力精炼法）和 Self-Debias（推理时自我去偏）。

#### 4. 资源与算力
*   **硬件：** 实验在单张 **NVIDIA A100 (80GB)** GPU 上完成。
*   **效率：** 论文强调了 UGID 的高效性。通过基于 PEFT（参数高效微调）的结构对齐，峰值显存从 19.00 GB 降至 **6.92 GB**。
*   **推理开销：** 推理延迟与原始模型一致（1.00x），无额外计算负担。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 **12 个不同规模和家族的模型变体**上进行了测试，涵盖了从 2B 到 14B 的参数区间。
*   **维度：** 进行了分布内（ID）和分布外（OOD）测试、消融实验、鲁棒性诊断以及机械可解释性验证（如 Logit Lens、激活修补 Activation Patching）。
*   **评价：** 实验设计非常充分且客观。通过引入 $\Delta Spec$（谱散度）和 $\Delta Hidden$（隐藏层漂移）等内部指标，从物理层面验证了去偏效果，而非仅仅依赖黑盒化的输出指标。

#### 6. 主要结论与发现
*   **偏见规模相关性：** 证实了偏见会随模型规模增大而发生内部迁移，单纯干预注意力对大模型效果有限。
*   **UGID 的优越性：** 在显著降低偏见（接近中性水平）的同时，UGID 几乎不损害模型的通用语言能力（PPL 保持稳定）和定义性常识（100% 安全得分）。
*   **结构稳定性：** UGID 成功解耦了偏见与推理拓扑之间的联系，使模型在面对不同提示词诱导时表现出极高的去偏稳定性。

#### 7. 优点
*   **理论新颖：** 首次从图同构和机械可解释性的视角统一处理注意力路由和 FFN 表示的去偏问题。
*   **帕累托最优：** 在去偏效果、模型效用和计算效率之间取得了极佳的平衡。
*   **可解释性强：** 提供了丰富的可视化证据（如注意力图对齐、因果追踪），证明了干预的有效性。

#### 8. 不足与局限
*   **锚点集规模：** 实验中使用的定义性锚点对（6 对）较少，可能无法完全覆盖所有复杂的性别语义边界。
*   **文化依赖性：** 偏见定义和锚点选择具有文化规范性，该框架在不同文化背景下的普适性仍需进一步验证。
*   **任务覆盖：** 虽然在通用基准上表现良好，但在极特定领域的专业任务中是否会产生细微的语义漂移尚不明确。

（完）
