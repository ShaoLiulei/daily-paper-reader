---
title: Mitigating the Bandwidth Wall via Data-Streaming System-Accelerator Co-Design
title_zh: 通过数据流式系统-加速器协同设计缓解带宽墙
authors: "Qunyou Liu, Marina Zapater, David Atienza"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19057v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: Transformer推理的系统-加速器协同设计
tldr: 针对Transformer推理中带宽受限的“带宽墙”问题，本文提出了一种系统与加速器协同设计方案。通过引入MatrixFlow加速器，利用分页流式数据流和计算与传输的显式重叠，结合Gem5-AcceSys全系统模拟器优化互连带宽。研究表明，采用分页流式处理和流水线重叠，而非增加片上SRAM，是提升Transformer推理效率的关键，在标准PCIe环境下即可达到接近HBM的性能。
selection_source: fresh_fetch
motivation: Transformer模型的推理性能主要受限于数据移动和互连带宽，而非单纯的算力，导致传统加速器难以发挥效能。
method: 提出MatrixFlow加速器，采用分页对齐的块矩阵乘法和流水线化DMA调度，并开发Gem5-AcceSys模拟器进行全系统协同设计。
result: "在BERT和ViT模型上，该方案比纯CPU快22倍，比现有加速器快5-8倍，且PCIe设计能达到HBM性能的80%。"
conclusion: 分页流式传输和计算传输重叠是解决现实系统约束下Transformer推理瓶颈的最有效手段。
---

## 摘要
Transformer 彻底改变了自然语言处理和计算机视觉领域的 AI，但其巨大的计算和内存需求给硬件加速带来了重大挑战。在实践中，端到端吞吐量通常受限于分页数据移动和互连带宽，而非原始的 MAC 计数。本研究提出了一种用于 Transformer 推理的统一系统-加速器协同设计方法，通过分页流式数据流以及计算与传输的显式重叠，共同优化矩阵加速器及其系统集成。在硬件方面，我们引入了 MatrixFlow，这是一种松耦合的 16x16 脉动阵列加速器，采用基于 4 KB 分块的页对齐分块矩阵乘法方法、约 20 KB 的小型片上缓冲区，以及 DMA、计算和 DMA 输出的流水线调度，以高效利用互连带宽。在系统方面，我们开发了 Gem5-AcceSys，它是 gem5 全系统模拟器的扩展，用于探索 PCIe 等标准互连以及包括直接内存（Direct Memory）、直接缓存（Direct Cache）和设备内存（Device Memory）模式在内的可配置内存层次结构，并考虑了 SMMU/TLB 效应。我们在多种数据类型和系统设置下，通过对 BERT 和 ViT 等代表性 Transformer 模型进行 gem5 仿真，评估了该协同设计。结果显示，与仅 CPU 的基准相比，端到端加速比高达 22 倍，与最先进的松耦合和紧耦合加速器相比，性能提升了 5 到 8 倍。我们进一步表明，标准的基于 PCIe 的主机内存设计可以达到片上 HBM 性能的约 80%。总之，在现实的系统约束下，分页流式传输和流水线重叠（而非大型本地 SRAM）是实现高效 Transformer 推理最有效的手段。

## Abstract
Transformers have revolutionized AI in natural language processing and computer vision, but their large computation and memory demands pose major challenges for hardware acceleration. In practice, end-to-end throughput is often limited by paged data movement and interconnect bandwidth rather than raw MAC count. This work proposes a unified system-accelerator co-design approach for transformer inference that jointly optimizes a matrix accelerator and its system integration through paged streaming dataflows and explicit overlap of compute and transfer. On the hardware side, we introduce MatrixFlow, a loosely coupled 16x16 systolic-array accelerator with a page-aligned block matrix multiplication method using 4 KB tiles, a small on-chip buffer of about 20 KB, and a pipelined schedule of DMA, compute, and DMA-out to utilize interconnect bandwidth efficiently. On the system side, we develop Gem5-AcceSys, an extension of the gem5 full-system simulator that explores standard interconnects such as PCIe and configurable memory hierarchies including Direct Memory, Direct Cache, and Device Memory modes with SMMU/TLB effects. We evaluate the co-design using gem5 simulations on representative transformer models including BERT and ViT across multiple data types and system setups. Results show up to 22x end-to-end speedup over a CPU-only baseline and 5x to 8x gains over state-of-the-art loosely and tightly coupled accelerators. We further show that a standard PCIe-based host-memory design can achieve about 80 percent of the performance of on-device HBM. Overall, paged streaming and pipeline overlap, rather than large local SRAMs, are the most effective levers for efficient transformer inference under realistic system constraints.

---

## 论文详细总结（自动生成）

以下是对论文《Mitigating the Bandwidth Wall via Data-Streaming System-Accelerator Co-Design》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：Transformer模型（如BERT、ViT）在推理时面临严重的“带宽墙”瓶颈。传统的硬件加速研究往往只关注提升算力（MAC数量），但在实际系统中，端到端性能受限于**分页数据移动（Paged Data Movement）**、**互连带宽（如PCIe）**以及**内存管理开销（如SMMU/TLB）**。
*   **研究背景**：现有的加速器模拟框架（如gem5-Aladdin等）通常简化了互连模型，忽略了操作系统驱动、地址转换和真实的DMA调度，导致设计出的加速器在集成到真实系统时效率低下。

### 2. 论文提出 Methodologies
论文提出了一个**系统-加速器协同设计（Co-Design）**方案，包含硬件、模拟平台和软件运行环境三个层面：
*   **MatrixFlow 加速器（硬件）**：
    *   采用 16×16 的脉动阵列（Systolic Array）。
    *   **极小片上缓存**：仅使用约 20 KB 的 SRAM（3个 4 KB 的分块缓冲区），远小于传统加速器（通常数百 KB）。
    *   **分页流式数据流**：将矩阵划分为 4 KB 的页对齐分块（Tiles），直接从主机内存流式传输数据。
*   **Gem5-AcceSys 模拟框架（系统）**：
    *   扩展了 gem5 全系统模拟器，增加了对 **PCIe 3.0-6.0**、**多通道 DMA** 和 **SMMU（IOMMU）** 的精确建模。
    *   支持三种访问模式：直接内存（DM）、直接缓存（DC）和设备内存（DevMem）。
*   **协同优化策略（算法/流程）**：
    *   **流水线重叠**：通过双缓冲技术，使 DMA 数据读取（A、B矩阵）、脉动阵列计算和 DMA 结果写回（C矩阵）在时间上完全重叠。
    *   **数据布局优化**：将矩阵 B 进行行条带化（Row-striped）存储，确保 DMA 传输是连续的长突发（Burst），减少 TLB 缺失和 PCIe 协议开销。

### 3. 实验设计
*   **数据集/模型**：
    *   **NLP**：BERT-Base, BERT-Large。
    *   **CV**：ViT-Base, ViT-Large, ViT-Huge。
    *   **微基准测试**：不同尺寸（64到2048）和精度（INT8/16/32, FP8/16/32）的矩阵乘法（GEMM）。
*   **Benchmark 与对比方法**：
    *   **CPU 基准**：单核 ARM CPU、多线程 CPU (OpenMP)、ARM Neon (SIMD)。
    *   **加速器对比**：**TiC-SAT**（紧耦合脉动阵列）、**SMAUG**（基于 gem5 的松耦合加速器框架）。
    *   **系统配置对比**：不同 PCIe 带宽（2GB/s 到 64GB/s）、不同内存技术（DDR3/4/5, HBM2, GDDR6）。

### 4. 资源与算力
*   **硬件合成**：使用 **TSMC 28nm** 工艺对 MatrixFlow 进行逻辑合成，以获取准确的面积、功耗和频率数据（1.0 GHz @ INT8）。
*   **模拟环境**：实验基于 gem5 全系统模拟，运行完整的 Linux 操作系统。
*   **算力说明**：文中未提及训练所需的 GPU 算力，因为该研究专注于**推理阶段的架构设计与模拟**。实验结果是通过周期精确（Cycle-accurate）的软件模拟获得的。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽实验，包括：
    *   不同数据精度对吞吐量的影响。
    *   PCIe 数据包大小（64B 到 4096B）对性能的敏感度分析。
    *   内存位置（主机 vs. 设备）与非 GEMM 算子占比的交叉分析。
    *   地址转换（SMMU）开销的定量评估。
*   **充分性评价**：实验设计非常充分且客观。它不仅展示了加速比，还深入分析了瓶颈转移（从计算转移到 I/O），并探讨了在不同非 GEMM 负载下，主机内存方案何时优于设备内存方案。

### 6. 论文的主要结论与发现
*   **性能提升**：MatrixFlow 在端到端推理上比单核 CPU 快 **22倍**，比 SOTA 紧耦合加速器快 **8倍**，比松耦合加速器快 **5倍**。
*   **带宽墙的缓解**：通过分页流式传输，标准的 PCIe 主机内存设计可以达到本地 HBM 内存性能的 **80%**，证明了高效的系统集成比昂贵的本地内存更具性价比。
*   **关键因素**：在现实系统约束下，**分页流式传输和流水线重叠**是提升效率的最有效手段，而非单纯增加片上 SRAM 缓存。
*   **非 GEMM 影响**：当模型中非 GEMM 算子（如 Softmax, LayerNorm）占比超过 35% 时，使用高速 PCIe 访问主机内存的方案反而优于将数据全部放在设备内存的方案。

### 7. 优点
*   **全系统视角**：不同于孤立研究加速器核心的论文，本文考虑了操作系统、驱动、DMA 和 IOMMU 等真实系统开销。
*   **面积效率极高**：通过协同设计，仅需 20 KB SRAM 即可维持高利用率，极大地节省了芯片面积和静态功耗。
*   **开源贡献**：开发的 Gem5-AcceSys 框架为社区提供了研究标准互连（PCIe）和地址转换开销的有力工具。

### 8. 不足与局限
*   **模拟与实测差距**：虽然 gem5 是周期精确的，但仍是模拟结果，尚未在真实硅片上验证物理层（PHY）的复杂性。
*   **算子覆盖**：主要优化了 GEMM 算子，虽然分析了非 GEMM 的影响，但非 GEMM 算子仍由主机 CPU 执行，可能在未来成为新的瓶颈。
*   **应用限制**：该设计高度依赖于 Transformer 的分块特性，对于稀疏性较高或数据流极不规则的模型，其分页流式优势可能减弱。

（完）
