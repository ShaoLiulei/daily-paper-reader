---
title: Linearized Bregman Iterations for Sparse Spiking Neural Networks
title_zh: 稀疏脉冲神经网络的线性化 Bregman 迭代
authors: "Daniel Windhager, Bernhard A. Moser, Michael Lunglmayr"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16462v1"
tags: ["query:snn"]
score: 10.0
evidence: 用于训练稀疏脉冲神经网络的优化器
tldr: "脉冲神经网络（SNN）虽具能效优势，但参数量庞大。本研究引入线性化Bregman迭代（LBI）作为SNN优化器，通过Bregman距离最小化和近端软阈值更新实现稀疏性。为提升收敛性，采用了结合Adam动量的AdaBreg变体。在SHD、SSC和PSMNIST数据集上的实验表明，该方法在保持与Adam相当精度的同时，减少了约50%的活跃参数，为高效神经形态学习提供了新途径。"
selection_source: fresh_fetch
motivation: 针对脉冲神经网络（SNN）参数量大、存储开销高的问题，寻求一种能有效诱导模型稀疏性的优化方法。
method: 提出使用线性化Bregman迭代（LBI）及其动量修正版本AdaBreg作为优化器，通过迭代软阈值更新实现参数稀疏化。
result: "在多个神经形态基准数据集上，该方法在不损失精度的前提下，成功将活跃参数数量降低了约50%。"
conclusion: 基于LBI的凸优化方法能有效平衡SNN的稀疏性与性能，是实现高效神经形态计算的有力工具。
---

## 摘要
脉冲神经网络 (SNNs) 为传统的常规人工神经网络 (ANNs) 提供了一种高能效的替代方案，但通常仍需要大量的参数。本研究引入线性化 Bregman 迭代 (LBI) 作为训练 SNNs 的优化器，通过迭代最小化 Bregman 距离和近端软阈值更新来强制实现稀疏性。为了提高收敛性和泛化能力，我们采用了 AdaBreg 优化器，这是一种经过动量和偏差修正的 Adam 的 Bregman 变体。在三个成熟的神经形态基准数据集（即 Spiking Heidelberg Digits (SHD)、Spiking Speech Commands (SSC) 和 Permuted Sequential MNIST (PSMNIST)）上的实验表明，基于 LBI 的优化在保持与 Adam 优化器训练的模型相当的准确率的同时，将活跃参数的数量减少了约 50%，证明了凸稀疏诱导方法在高效神经形态学习中的潜力。

## Abstract
Spiking Neural Networks (SNNs) offer an energy efficient alternative to conventional Artificial Neural Networks (ANNs) but typically still require a large number of parameters. This work introduces Linearized Bregman Iterations (LBI) as an optimizer for training SNNs, enforcing sparsity through iterative minimization of the Bregman distance and proximal soft thresholding updates. To improve convergence and generalization, we employ the AdaBreg optimizer, a momentum and bias corrected Bregman variant of Adam. Experiments on three established neuromorphic benchmarks, i.e. the Spiking Heidelberg Digits (SHD), the Spiking Speech Commands (SSC), and the Permuted Sequential MNIST (PSMNIST) datasets, show that LBI based optimization reduces the number of active parameters by about 50% while maintaining accuracy comparable to models trained with the Adam optimizer, demonstrating the potential of convex sparsity inducing methods for efficient neuromorphic learning.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何通过数学上的凸优化方法实现脉冲神经网络（SNN）的稀疏化，以下是对该研究的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：脉冲神经网络（SNN）因其生物启发特性具有极高的能效潜力，但在实际应用中往往参数量庞大，增加了存储和计算负担。
*   **核心问题**：现有的稀疏化方法多依赖于启发式的剪枝（Pruning），缺乏严谨的数学理论支撑。
*   **研究目标**：引入**线性化 Bregman 迭代（LBI）**算法，将其作为 SNN 的优化器，旨在训练过程中自动诱导权重的稀疏性，同时保持较高的分类准确率。

### 2. 方法论
*   **核心思想**：利用 Bregman 距离代替传统的代价函数最小化，通过处理非平滑的 $\ell_1$ 正则化项来诱导稀疏性。
*   **关键技术细节**：
    *   **影子变量（Shadow Variables）**：引入辅助变量 $v$ 来跟踪梯度的累积，而模型参数 $\theta$ 则通过对 $v$ 进行近端映射（Proximal Mapping）得到。
    *   **软阈值算子（Soft-thresholding）**：在更新过程中，通过近端算子将绝对值小于阈值 $\lambda$ 的权重直接置为零，从而实现精确的稀疏化。
    *   **AdaBreg 优化器**：这是本文的核心工具，它将 LBI 的稀疏诱导特性与 Adam 优化器的自适应矩估计（动量和偏差修正）相结合，解决了标准 LBI 在深度学习中收敛慢的问题。
*   **算法流程**：计算梯度 $\rightarrow$ 更新一阶/二阶矩 $\rightarrow$ 更新影子变量 $v$ $\rightarrow$ 应用软阈值函数更新参数 $\theta$。

### 3. 实验设计
*   **数据集**：选择了三个主流的神经形态基准数据集，涵盖了不同的时间复杂度：
    1.  **SHD (Spiking Heidelberg Digits)**：短促的事件化音频信号。
    2.  **SSC (Spiking Speech Commands)**：语音命令识别。
    3.  **PSMNIST (Permuted Sequential MNIST)**：长序列图像模式识别。
*   **Benchmark（基准）**：对比了 Queant 等人（2025年）提出的当前性能领先的 SNN 模型（使用 Adam 优化器训练）。
*   **对比维度**：分析了正则化参数 $\lambda$ 对稀疏度、准确率、收敛速度的影响，并对比了有无学习率调度器（Learning Rate Scheduler）的表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。
*   **实现框架**：提到该方法已集成到 PyTorch 框架中，只需将 `Adam` 替换为 `AdaBreg` 即可使用。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对三个数据集分别进行了多组实验。
    *   每组实验均使用了 3 个不同的随机种子运行并取平均值，以确保结果的可重复性。
    *   对超参数 $\lambda$ 进行了详细的消融研究（从 0 到 10 的不同取值）。
*   **充分性评价**：实验设计较为充分，涵盖了从前馈到循环（Recurrent）的多种 SNN 架构，验证了算法在不同任务下的鲁棒性。对比实验直接面向当前的最优模型（SOTA），具有较高的说服力。

### 6. 主要结论与发现
*   **显著的稀疏效果**：AdaBreg 优化器在训练过程中能使活跃参数（非零权重）数量**减少约 50%**。
*   **精度保持良好**：与全参数的 Adam 基准相比，稀疏化后的模型精度损失仅在 **0.5% 至 1.5%** 之间。
*   **正则化效应**：参数 $\lambda$ 不仅控制稀疏度，还起到正则化作用，在训练早期能使损失曲线更平滑、收敛更快。
*   **学习率敏感性**：LBI 方法对高学习率较为敏感，若配合学习率调度器使用不当，可能会导致训练发散。

### 7. 优点
*   **理论与实践结合**：将严谨的凸优化理论（Bregman 迭代）成功应用于复杂的非凸 SNN 训练中。
*   **易用性高**：作为优化器插件，不需要修改网络结构或进行复杂的后期剪枝操作。
*   **动态稀疏化**：在训练过程中实时产生稀疏结构，而非训练后再处理，有利于硬件感知的训练。

### 8. 不足与局限
*   **超参数依赖**：最优的 $\lambda$ 值在不同数据集上差异较大（如 SHD 需要高 $\lambda$，而 PSMNIST 需要低 $\lambda$），缺乏自动调参机制。
*   **精度微损**：虽然稀疏度很高，但在极端追求精度的场景下，与 Adam 相比仍存在微小的性能差距。
*   **硬件验证缺失**：论文虽然强调了对边缘 AI 硬件的意义，但未在真实的神经形态芯片（如 Loihi 或自研 FPGA）上测试实际的功耗下降和加速比。

（完）
