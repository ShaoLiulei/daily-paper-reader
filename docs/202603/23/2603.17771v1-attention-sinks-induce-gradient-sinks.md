---
title: Attention Sinks Induce Gradient Sinks
title_zh: 注意力汇聚诱导梯度汇聚
authors: "Yihong Chen, Quanming Yao"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17771v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: Transformer模型中的注意力汇聚与梯度汇聚
tldr: 本研究探讨了Transformer模型中“注意力汇”与“巨大激活值”之间的联系。通过分析反向传播，作者发现因果掩码会导致梯度在初始Token处高度集中，形成“梯度汇”。在RMSNorm架构中，巨大激活值是模型对这种局部梯度压力的自适应响应。作者提出了V-scale方法调整梯度，成功在保留注意力汇的同时抑制了巨大激活值，证明了梯度汇是连接两者的关键训练机制。
selection_source: fresh_fetch
motivation: 旨在探究Transformer中注意力汇与巨大激活值现象之间的内在联系及其在训练阶段的形成机制。
method: 从反向传播视角分析梯度分布，并提出V-scale方法来调整值路径的回传梯度以验证梯度压力的影响。
result: 发现因果掩码会诱导梯度汇，且V-scale能在保留注意力汇的情况下有效抑制巨大激活值的产生。
conclusion: 梯度汇是连接注意力汇与巨大激活值的关键训练中介，揭示了模型内部异常激活值的起源。
---

## 摘要
注意力汇聚（Attention sinks）和巨量激活（massive activations）是 Transformer 模型中反复出现且密切相关的现象。现有研究主要集中在前向传播过程，因此尚不清楚两者之间的联系是直接的，还是由某种训练时机制介导的。我们从反向传播的角度研究了这一问题。通过实证和理论分析，我们证明在因果掩码（causal mask）下，注意力汇聚会诱导显著的梯度集中，我们将其称为梯度汇聚（gradient sinks）。此外，在采用 RMSNorm 的 pre-norm 架构中，巨量激活可以被理解为训练过程中对这种局部梯度压力的自适应响应。为了验证这一假设，我们引入了 V-scale，这是一种调整值路径（value-path）反向传播梯度的修改方法。在预训练的 V-scale 模型中，注意力汇聚得以保留，而巨量激活则被抑制。这些结果支持了这样一种解释：梯度汇聚是连接注意力汇聚和巨量激活的关键训练时中介。

## Abstract
Attention sinks and massive activations are recurring and closely related phenomena in Transformer models. Existing studies have largely focused on the forward pass, making it unclear whether their connection is direct or mediated by a training-time mechanism. We study this question from the perspective of backpropagation. Empirically and theoretically, we show that under causal mask, attention sinks can induce pronounced gradient concentration, which we term gradient sinks. Furthermore, in pre-norm architectures with RMSNorm, massive activations can be understood as an adaptive response to this localized gradient pressure during training. To test this hypothesis, we introduce V-scale, a modification that adjusts value-path backpropagated gradients. In pretrained V-scale models, attention sinks are preserved whereas massive activations are suppressed. These results support the interpretation that gradient sink is a key training-time mediator linking attention sinks and massive activations.

---

## 论文详细总结（自动生成）

这篇论文由清华大学的研究团队撰写，深入探讨了 Transformer 模型中两个著名现象——**注意力汇聚（Attention Sinks, AS）**与**巨量激活（Massive Activations, MA）**之间的内在联系。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在大型语言模型（LLM）中，第一位 Token 往往会吸引极高的注意力权重（AS），同时其激活值也会变得异常巨大（MA）。现有研究多从前向传播角度解释 AS（如作为“无操作”选项），但对于**优化过程为何会学习出如此巨大的激活值**（尤其是在输入已标准化的 Pre-norm 架构中）缺乏深入解释。
*   **核心假设**：作者认为 AS 与 MA 的联系是由训练时的**反向传播机制**介导的。在因果掩码下，第一位 Token 会累积后续所有 Token 的梯度，形成“梯度汇聚（Gradient Sinks, GS）”。而 MA 实际上是模型为了缓解这种局部梯度压力，利用 RMSNorm 的特性产生的一种自适应响应。

### 2. 方法论
*   **核心思想**：通过理论分析和实证观察，揭示“注意力汇聚 $\rightarrow$ 梯度汇聚 $\rightarrow$ 巨量激活”的因果链条。
*   **关键技术细节**：
    *   **梯度汇聚（GS）定义**：在反向传播中，由于因果掩码，后续 Token 的梯度会通过注意力权重汇集到初始 Token 的 Key 和 Value 路径上。
    *   **RMSNorm 的梯度压缩特性**：理论证明 RMSNorm 的雅可比矩阵范数与输入激活值的范数成反比。这意味着激活值越大，回传到残差结构的梯度就被压缩得越厉害。
    *   **V-scale 机制**：为了验证因果关系，作者设计了一个“梯度阀门”。在 Value 路径聚合前引入变换 $\hat{v}_j = \phi(\|v_j\|_2^2) v_j$，其中 $\phi(r) = \frac{r}{r+C}$。
    *   **算法逻辑**：V-scale 在前向传播中对小范数 Token（通常是 Sink Token）进行收缩，但在反向传播中能显著衰减这些 Token 的梯度。如果 MA 是为了应对梯度压力，那么引入 V-scale 后，模型就不再需要通过产生 MA 来压缩梯度。

### 3. 实验设计
*   **数据集**：使用 **C4** 数据集进行从头预训练。
*   **模型配置**：采用类 Llama 架构（Pre-norm, RMSNorm, RoPE, SwiGLU），规模分别为 **0.1B** 和 **0.3B** 参数。
*   **Benchmark 与对比**：
    *   **Baseline**：标准的 Llama 架构模型。
    *   **对比对象**：引入 V-scale 修改后的模型。
    *   **观测指标**：注意力汇聚质量（Sink Mass/Rate）、激活值范数（残差层、MLP 输出、注意力输出）、Q/K/V 各路径的梯度范数。

### 4. 资源与算力
*   论文提到模型是从头开始预训练的（从 0 到 25k 步），并每 1000 步保存一次 Checkpoint 进行动态分析。
*   **具体算力说明**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。考虑到模型规模（0.1B-0.3B）和预训练性质，通常需要数个至数十个高性能 GPU（如 A100/H100）运行数日。

### 5. 实验数量与充分性
*   **实验规模**：针对两种不同规模的模型（0.1B 和 0.3B）进行了完整的预训练过程观测。
*   **分析维度**：包含了训练动态追踪、梯度重塑的散点分析、以及针对 V-scale 的消融对比。
*   **充分性评价**：实验设计较为科学，通过干预反向传播路径（V-scale）来验证因果假设，这比单纯的相关性观察更具说服力。虽然模型参数量较小，但对于揭示底层机制而言是充分且客观的。

### 6. 主要结论与发现
*   **梯度汇聚确实存在**：实证发现第一位 Token 的 Key 和 Value 路径梯度范数远高于其他位置，且 Value 路径梯度压力最大。
*   **MA 是梯度的“避雷针”**：实验证明 MA 出现的层与梯度压缩发生的层高度重合。模型通过增大激活值，利用 RMSNorm 降低了回传到深层的梯度强度，维持了训练稳定性。
*   **现象可解耦**：使用 V-scale 后，模型在保留 AS 结构的同时，MA 现象显著减弱。这证明 MA 并非 AS 形成的必要前向条件，而是训练压力的产物。

### 7. 优点
*   **视角独特**：首次从反向传播和梯度流的角度解释了 Transformer 中的异常激活值现象。
*   **理论与实证结合**：不仅有严谨的数学证明（定理 1 和定理 2），还有详尽的训练动态数据支持。
*   **干预实验巧妙**：V-scale 的设计非常精准，只针对梯度压力最大的路径进行微调，成功分离了 AS 和 MA。

### 8. 不足与局限
*   **模型规模限制**：实验仅在 0.3B 以下的小模型上验证，尚未在大规模（如 7B、70B）模型上确认该机制的普适性。
*   **架构覆盖面**：主要针对类 Llama 的 Pre-norm 架构，对于 Post-norm 或其他变体（如 MoE 架构）的适用性讨论较少。
*   **下游任务评估**：虽然 V-scale 抑制了 MA，但论文未详细评估这种抑制是否会对模型的长文本推理、逻辑表达等下游能力产生副作用。

（完）
