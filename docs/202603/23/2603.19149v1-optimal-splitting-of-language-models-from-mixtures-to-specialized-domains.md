---
title: Optimal Splitting of Language Models from Mixtures to Specialized Domains
title_zh: 语言模型从混合领域到专业领域的最佳拆分
authors: "Skyler Seto, Pierre Ablin, Anastasiia Filippova, Jiayuan Ye, Louis Bethune, Angelos Katharopoulos, David Grangier"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19149v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 缩放法则与预训练分配
tldr: 本研究探讨了语言模型从通用预训练到特定领域专业化的最优计算资源分配问题。针对多领域设置下的“拆分模型训练”，作者提出了一种基于缩放法则（Scaling Laws）的方法，用于预测不同模型规模、预训练数据量及专业化数据量下的损失。该方法能独立预训练多个模型，并精确计算预训练与持续预训练之间的最优资源分配，从而在常识知识和推理基准测试中显著提升了不同规模模型的性能。
selection_source: fresh_fetch
motivation: 旨在解决在多领域专业化训练中，如何最优地分配预训练与持续预训练之间的计算资源以达到最佳性能。
method: 提出了一种基于缩放法则的方法，通过模型规模、预训练和专业化数据量来预测损失并优化资源分配。
result: 该方法能够准确预测模型损失并外推至更大规模，在多种常识和推理任务中一致性地提升了模型表现。
conclusion: 通过科学分配计算预算，可以显著提高语言模型在特定领域的专业化效率和最终性能。
---

## 摘要
由于预训练数据的规模和多样性，语言模型在各种知识、语言和推理任务中取得了令人印象深刻的性能。标准的训练方案是一个两阶段范式：首先在全量语料库上进行预训练，然后在全量语料库中高质量、专业化的子集数据上进行专业化训练。在多领域设置下，这涉及在每个专业领域上对多个模型进行持续预训练，被称为拆分模型训练（split model training）。我们提出了一种在通用预训练语料库上独立预训练多个模型的方法，并利用缩放法则（scaling laws）确定预训练与持续预训练之间的最佳计算资源分配。我们的方法能够准确预测参数量为 N、预训练 Token 数为 D 且专业化 Token 数为 D' 的模型损失，并能外推至更大的模型规模和 Token 数量。应用于语言模型训练时，我们的方法在不同的模型规模和计算预算下，在常识知识和推理基准测试中均能一致地提升性能。

## Abstract
Language models achieve impressive performance on a variety of knowledge, language, and reasoning tasks due to the scale and diversity of pretraining data available. The standard training recipe is a two-stage paradigm: pretraining first on the full corpus of data followed by specialization on a subset of high quality, specialized data from the full corpus. In the multi-domain setting, this involves continued pretraining of multiple models on each specialized domain, referred to as split model training. We propose a method for pretraining multiple models independently over a general pretraining corpus, and determining the optimal compute allocation between pretraining and continued pretraining using scaling laws. Our approach accurately predicts the loss of a model of size N with D pretraining and D' specialization tokens, and extrapolates to larger model sizes and number of tokens. Applied to language model training, our approach improves performance consistently across common sense knowledge and reasoning benchmarks across different model sizes and compute budgets.

---

## 论文详细总结（自动生成）

这是一份关于论文《Optimal Splitting of Language Models from Mixtures to Specialized Domains》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：在多领域（如数学、代码、法律等）专业化训练中，如何最优地分配计算资源（Compute Allocation）？即：在将模型拆分为多个领域专家模型之前，应该进行多久的通用预训练（PT），以及随后进行多久的领域持续预训练（CPT）？
*   **研究背景**：当前的 LLM 训练通常采用“先通用预训练、后领域微调”的模式。在多领域场景下，简单的共享预训练可能无法在所有领域达到最优。论文探讨了“拆分模型训练”（Split Model Training）的效率，旨在通过科学的缩放法则（Scaling Laws）找到预训练与拆分的最佳平衡点。

### 2. 方法论：核心思想与技术细节
*   **三阶段流程**：
    1.  **聚类（Clustering）**：使用文档嵌入向量（如 Sentence-BERT）将预训练语料库 $D$ 划分为 $K$ 个互斥的领域集群 $D_1, \dots, D_K$。
    2.  **种子模型预训练（Seed PT）**：在全量语料上训练一个种子模型 $\theta'$，消耗 $D$ 个 Token。
    3.  **拆分持续预训练（Split CPT）**：复制 $K$ 个种子模型副本，每个副本在各自的领域集群 $D_k$ 上独立训练 $D'$ 个 Token。
*   **核心公式（缩放法则）**：
    论文提出了一种新的函数形式来预测损失 $L$：
    $$L(N, D, D_k) = E_0 + E_\Delta + A \cdot (D_k^{\alpha_1} + cD^{\alpha_2})^{-1} + B \cdot N^{-\kappa}$$
    其中 $N$ 是参数量，$D$ 是预训练 Token 数，$D_k$ 是领域特定 Token 数。该公式考虑了预训练对后续专业化训练的边际贡献。
*   **最优拆分点预测**：通过求导和优化算法，计算在总预算 $D_T = D + K \times D'$ 固定的情况下，使平均损失最小化的最优预训练时长 $t_s$。

### 3. 实验设计
*   **数据集**：
    *   **合成数据**：电话簿记忆实验（用于验证领域重叠对拆分点的影响）。
    *   **真实数据**：使用 **DCLM-Baseline** 语料库进行聚类（通常分为 16 个簇）；使用 **The Pile** 的子集（ArXiv, Github 等）验证领域泛化性。
*   **Benchmark（基准测试）**：
    *   8 个零样本（Zero-shot）QA 任务：ARC-E/C, HellaSwag, PIQA, SciQ, BoolQ, MMLU, Winogrande。
*   **对比方法**：
    *   **Full PT**：在全量数据上进行标准的单模型预训练。
    *   **CPT from Scratch**：不进行预训练，直接在各领域独立训练。
    *   **MoE（混合专家模型）**：对比了拆分训练与 MoE 架构的性能差异。

### 4. 资源与算力
*   **GPU 型号**：NVIDIA **H100**。
*   **训练时长/算力消耗**：
    *   1.3B 模型在 100B Token 上的训练约需 **1000 GPUh**。
    *   2.7B 模型在 100B Token 上的训练约需 **2200 GPUh**。
    *   部分对比实验（如 2.7B 规模的完整路径）总计消耗超过 **7000 GPUh**。
*   **模型规模**：涵盖了 100M, 125M, 350M, 760M, 1.3B, 2.7B 到 6.7B 等多个量级。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了数百次模型训练任务以拟合缩放法则。
*   **消融实验**：
    *   改变领域数量 $K$（4, 16, 64 个簇）。
    *   改变预训练 Token 的比例（从 0% 到 100%）。
    *   推理时的路由策略（全量路由 vs. 任务子集路由）。
*   **充分性评价**：实验设计非常系统，不仅在小模型上拟合规律，还成功在 2.7B 模型上进行了外推验证。通过合成数据和真实数据的双重验证，增强了结论的可信度。

### 6. 主要结论与发现
*   **最优拆分点存在**：预训练并非越久越好。对于特定预算，存在一个最优的 $t_s$，过早或过晚拆分都会导致性能下降。
*   **数据效率提升**：在最优分配下，拆分模型训练的性能远超全量预训练。在某些高计算预算场景下，标准预训练需要 **50 倍** 的数据量才能达到拆分训练的损失水平。
*   **以小博大**：使用最优拆分策略训练的 **2.7B 拆分模型**，其零样本 QA 准确率超过了推理成本高出一倍多的 **6.7B 密集模型**。
*   **模型越大，拆分越晚**：随着模型参数量 $N$ 的增加，最优拆分点 $t_s$ 会向后推移（即需要更久的通用预训练）。

### 7. 优点
*   **实用性强**：为工业界在大规模预训练中如何分配“通用”与“专业”算力提供了量化的指导方案。
*   **理论创新**：扩展了传统的 Chinchilla 缩放法则，使其能够处理两阶段训练和多模型拆分的复杂场景。
*   **硬件友好**：拆分后的专家模型可以异步、分布式地在非专用硬件上训练，降低了对超大规模集群的依赖。

### 8. 不足与局限
*   **路由依赖**：推理时依赖于路由器的准确性，如果输入文本的领域判定错误，性能会受损。
*   **存储成本**：虽然推理时激活参数量不变，但需要存储 $K$ 个模型的参数，对于端侧设备或显存受限场景压力较大。
*   **领域划分粒度**：聚类算法的质量直接影响拆分效果，论文主要讨论了基于语义的聚类，未深入探讨其他划分方式（如语言、难度等）。
*   **超大模型验证**：实验最高只到 2.7B/6.7B 规模，尚未在 70B 或更高级别的模型上验证该缩放法则的绝对准确性。

（完）
