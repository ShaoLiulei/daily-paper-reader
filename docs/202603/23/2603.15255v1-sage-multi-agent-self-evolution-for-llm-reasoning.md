---
title: "SAGE: Multi-Agent Self-Evolution for LLM Reasoning"
title_zh: SAGE：大语言模型推理的多智能体自我演化
authors: "Yulin Peng, Xinxin Zhu, Chenxing Wei, Nianbo Zeng, Leilei Wang, Ying Tiffany He, F. Richard Yu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15255v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型推理的多智能体自我演化
tldr: SAGE是一个多智能体自我进化框架，旨在提升大语言模型的推理能力。针对现有方法依赖人工标注或缺乏规划控制的问题，SAGE通过挑战者、规划者、执行者和评论者四个角色的协同演化，实现了从少量种子数据出发的闭环自我训练。该框架通过显式规划和质量过滤，在数学和代码生成任务上显著提升了模型性能，为实现自主进化的通用推理提供了新路径。
selection_source: fresh_fetch
motivation: 现有的强化学习推理方法过度依赖人工标注，且自我博弈缺乏显式规划和质量控制，导致长程推理不稳定。
method: 提出SAGE框架，通过挑战者生成任务、规划者制定步骤、执行者求解、评论者过滤，实现多智能体闭环自我进化。
result: "在Qwen-2.5-7B模型上，SAGE在LiveCodeBench和OlympiadBench上分别实现了8.9%和10.7%的性能提升。"
conclusion: SAGE通过多智能体协同和严格的质量控制，证明了闭环自我进化在提升大语言模型泛化推理能力方面的有效性。
---

## 摘要
带有可验证奖励的强化学习提升了大语言模型（LLMs）的推理能力，但许多方法仍依赖于大规模的人工标注数据集。虽然自我博弈（self-play）减少了这种依赖，但它往往缺乏显式的规划和强大的质量控制，限制了在长程多步推理中的稳定性。我们提出了 SAGE（通用推理演化的自我演化智能体），这是一个闭环框架，其中四个智能体：挑战者（Challenger）、规划者（Planner）、求解者（Solver）和评论者（Critic），仅使用少量的种子集，从共享的 LLM 骨干模型中共同演化。挑战者不断生成难度递增的任务；规划者将每个任务转换为结构化的多步计划；求解者遵循该计划生成答案，其正确性由外部验证器确定。评论者对生成的题目和计划进行评分和过滤，以防止课程漂移并保持训练信号的质量，从而实现稳定的自我训练。在数学和代码生成基准测试中，SAGE 在不同模型规模上均实现了持续的提升，使 Qwen-2.5-7B 模型在 LiveCodeBench 上提升了 8.9%，在 OlympiadBench 上提升了 10.7%。

## Abstract
Reinforcement learning with verifiable rewards improves reasoning in large language models (LLMs), but many methods still rely on large human-labeled datasets. While self-play reduces this dependency, it often lacks explicit planning and strong quality control, limiting stability in long-horizon multi-step reasoning. We present SAGE (Self-evolving Agents for Generalized reasoning Evolution), a closed-loop framework where four agents: Challenger, Planner, Solver, and Critic, co-evolve from a shared LLM backbone using only a small seed set. The Challenger continuously generates increasingly difficult tasks; the Planner converts each task into a structured multi-step plan; and the Solver follows the plan to produce an answer, whose correctness is determined by external verifiers. The Critic scores and filters both generated questions and plans to prevent curriculum drift and maintain training signal quality, enabling stable self-training. Across mathematics and code-generation benchmarks, SAGE delivers consistent gains across model scales, improving the Qwen-2.5-7B model by 8.9% on LiveCodeBench and 10.7% on OlympiadBench.

---

## 论文详细总结（自动生成）

以下是对论文《SAGE: Multi-Agent Self-Evolution for LLM Reasoning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然带有可验证奖励的强化学习（RLVR）能显著提升大语言模型（LLM）的推理能力，但现有方法高度依赖大规模人工标注数据。现有的“自我博弈”（Self-play）方法虽减少了数据依赖，但往往缺乏显式的规划能力和严格的质量控制，导致在处理长程、多步推理任务时表现不稳定，且容易出现“课程漂移”（训练题目质量下降）。
*   **研究背景**：旨在构建一个无需大规模外部标注、能够自主进化、具备显式规划能力且训练过程稳定的通用推理框架。

### 2. 方法论
SAGE 提出了一个由四个专门智能体组成的闭环自我进化框架，这些智能体共享同一个 LLM 骨干模型，通过不同的角色指令进行区分：
*   **核心思想**：通过“挑战者-规划者-求解者-评论者”四方的对抗与协作，实现从少量种子数据（约500条）开始的持续进化。
*   **关键技术细节**：
    *   **挑战者 (Challenger)**：负责生成新任务及其验证器。它与求解者形成对抗：当求解者在验证中失败时，挑战者获得“难度奖励”，从而推动课程向更难但可解的方向演化。
    *   **规划者 (Planner)**：将任务分解为结构化的多步计划，引导求解者进行逻辑推理。
    *   **求解者 (Solver)**：执行计划并给出最终答案，通过外部验证器（如数学答案匹配或代码单元测试）获得正确性奖励。
    *   **评论者 (Critic)**：作为质量闸门，对生成的题目和计划进行评分（1-10分）并过滤掉低质量内容，防止训练集退化。
*   **算法流程**：采用 **Task-Relative REINFORCE++** 算法。其核心公式是对不同角色的优势函数进行独立归一化（Per-role advantage normalization），以协调具有异构目标的多个智能体同步更新。

### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理**：分布内（ID）使用 GSM8K、MATH；分布外（OOD）使用 AIME 2024/2025、AMC 2023、OlympiadBench。
    *   **代码生成**：分布内使用 HumanEval+、MBPP+；分布外使用 LiveCodeBench (v1-v5)。
*   **基准模型 (Backbones)**：Qwen2.5-3B-Instruct、Qwen2.5-7B-Instruct、Qwen3-4B-Base。
*   **对比方法**：
    *   原始 Base 模型。
    *   **AZR (Absolute Zero Reasoning)**：一种基于代码执行验证的自我博弈方法。
    *   **MAE (Multi-Agent Evolve)**：一种多智能体协同进化框架。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练配置**：提到了使用 LoRA 微调（Rank=128），学习率为 3e-6，训练步数为 200 步。最大 Prompt 和 Response 长度均为 8192。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同规模和系列的骨干模型，在 2 大核心推理领域（数学、代码）的 10 多个基准测试上进行了验证。
*   **消融实验**：分别去掉了挑战者训练、求解者训练和评论者训练，验证了每个角色对最终性能的贡献。
*   **充分性评价**：实验设计较为充分，不仅关注 ID 性能，还重点评估了 OOD 泛化能力，并展示了训练过程中题目库规模的动态增长（从 500 条增长到 2 万条以上），体现了自动扩充数据的能力。

### 6. 主要结论与发现
*   **性能提升显著**：SAGE 在所有模型规模上均取得领先。在 Qwen-2.5-7B 上，LiveCodeBench 提升 8.9%，OlympiadBench 提升 10.7%。
*   **强泛化能力**：相比基准方法，SAGE 在 OOD 任务上的表现尤为突出，证明了多智能体协作生成的课程具有更好的通用性。
*   **训练稳定性**：评论者的质量过滤机制有效防止了自我训练中的奖励崩溃。实验发现训练在 100-140 步左右达到峰值，随后可能出现过度拟合自我生成课程的现象。

### 7. 优点
*   **闭环进化**：实现了从任务生成到评估的完整闭环，极大地降低了对人工标注的依赖。
*   **显式规划**：引入 Planner 角色，解决了传统 RL 训练中模型“跳步”或逻辑断层的问题。
*   **角色解耦**：通过 Task-Relative 归一化解决了多角色联合训练时的梯度失衡问题。

### 8. 不足与局限
*   **领域限制**：目前仅限于“可验证领域”（数学、代码），即结果必须有客观标准（Ground Truth 或测试用例），难以直接推广到主观创作或开放式对话任务。
*   **种子依赖**：虽然需求量小，但仍需约 500 条高质量种子数据来启动进化过程。
*   **过拟合风险**：训练动态分析显示，长期训练可能导致模型过度适应自我生成的题目分布，导致泛化性能在后期下降。

（完）
