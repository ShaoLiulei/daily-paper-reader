---
title: "S2Act: Simple Spiking Actor"
title_zh: S2Act：简单脉冲 Actor
authors: "Ugur Akcal, Seung Hyun Kim, Mikihisa Yuasa, Hamid Osooli, Jiarui Sun, Ribhav Sahu, Mattia Gazzola, Huy T. Tran, Girish Chowdhary"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15725v1"
tags: ["query:snn"]
score: 10.0
evidence: 用于移动机器人的脉冲神经网络策略
tldr: 针对移动机器人受限的功耗和计算资源，本文提出了一种轻量级脉冲神经网络（SNN）强化学习框架 S2Act。该方法通过将 LIF 神经元的响应曲线近似为 ReLU 激活函数，解决了 SNN 在复杂随机环境中的梯度消失和超参数敏感问题。实验在多智能体环境及 Intel Loihi 硬件上验证了其在任务性能和实时推理方面的优越性，为 SNN 策略的快速原型设计和高效部署提供了新方案。
selection_source: fresh_fetch
motivation: 现有的 SNN 强化学习方法在复杂随机环境中面临超参数敏感和梯度信号不一致的挑战。
method: 提出 S2Act 框架，通过将 LIF 神经元参数全局塑造成近似 ReLU 的响应，实现从速率模型到物理脉冲神经元的权重迁移。
result: 在多智能体捕获旗帜和停车任务中，S2Act 在任务表现和实时推理速度上均优于现有基准。
conclusion: S2Act 证明了通过简化 SNN 训练与部署流程，可以有效提升神经形态硬件在复杂机器人任务中的应用潜力。
---

## 摘要
脉冲神经网络 (SNN) 和受生物启发的学习机制在移动机器人领域极具吸引力，因为车载神经网络策略的大小和性能受限于功耗和计算预算。现有的 SNN 方法，如群体编码、奖励调制和混合人工神经网络 (ANN)-SNN 架构，已显示出良好的效果；然而，由于 SNN 对超参数的敏感性以及梯度信号的不一致，它们在复杂且高度随机的环境中面临挑战。为了应对这些挑战，我们提出了简单脉冲 Actor (S2Act)，这是一个计算轻量级的框架，通过三个步骤使用 SNN 部署强化学习 (RL) 策略：(1) 构建基于速率脉冲神经元近似网络的 Actor-Critic 模型；(2) 使用兼容的激活函数通过梯度训练网络；(3) 将训练好的权重转换为速率型漏电积分发放 (LIF) 神经元的物理参数，用于推理和部署。通过全局调整 LIF 神经元参数，使其基于速率的响应近似于 ReLU 激活，S2Act 有效地缓解了梯度消失问题，同时预先约束 LIF 响应曲线减少了对复杂的 SNN 特定超参数调优的依赖。我们在两个捕捉多机器人交互复杂性的多智能体随机环境（夺旗和停车）中演示了我们的方法，并使用英特尔的 Loihi 类脑硬件在物理 TurtleBot 平台上部署了训练好的策略。我们的实验结果表明，S2Act 在几乎所有考虑的场景中，在任务性能和实时推理方面均优于相关的基准模型，突显了其在基于 SNN 的 RL 策略的快速原型设计和高效现实部署方面的潜力。

## Abstract
Spiking neural networks (SNNs) and biologically-inspired learning mechanisms are attractive in mobile robotics, where the size and performance of onboard neural network policies are constrained by power and computational budgets. Existing SNN approaches, such as population coding, reward modulation, and hybrid artificial neural network (ANN)-SNN architectures, have shown promising results; however, they face challenges in complex, highly stochastic environments due to SNN sensitivity to hyperparameters and inconsistent gradient signals. To address these challenges, we propose simple spiking actor (S2Act), a computationally lightweight framework that deploys an RL policy using an SNN in three steps: (1) architect an actor-critic model based on an approximated network of rate-based spiking neurons, (2) train the network with gradients using compatible activation functions, and (3) transfer the trained weights into physical parameters of rate-based leaky integrate-and-fire (LIF) neurons for inference and deployment. By globally shaping LIF neuron parameters such that their rate-based responses approximate ReLU activations, S2Act effectively mitigates the vanishing gradient problem, while pre-constraining LIF response curves reduces reliance on complex SNN-specific hyperparameter tuning. We demonstrate our method in two multi-agent stochastic environments (capture-the-flag and parking) that capture the complexity of multi-robot interactions, and deploy our trained policies on physical TurtleBot platforms using Intel's Loihi neuromorphic hardware. Our experimental results show that S2Act outperforms relevant baselines in task performance and real-time inference in nearly all considered scenarios, highlighting its potential for rapid prototyping and efficient real-world deployment of SNN-based RL policies.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **S2Act (Simple Spiking Actor)** 的轻量级脉冲神经网络（SNN）强化学习框架，旨在解决 SNN 在复杂、随机的机器人任务中训练不稳定和部署困难的问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然 SNN 在移动机器人（功耗受限）中具有高能效潜力，但在强化学习（RL）中面临三大挑战：
    1.  **超参数敏感性**：复杂的非线性神经元模型需要大量调优。
    2.  **梯度消失**：脉冲动力学与反向传播不兼容，导致训练不稳定。
    3.  **环境随机性**：在多智能体或对抗性环境下，现有 SNN 方法（如群体编码或生物学习规则）表现不佳。
*   **研究背景**：现有的方法要么过于复杂（增加计算负担），要么在处理高度随机的现实任务时缺乏鲁棒性。

### 2. 核心方法论
S2Act 采用了一种**“训练时近似，部署时转换”**的策略，具体分为三个步骤：
*   **神经元塑形（Soft-ReLLIF）**：这是核心创新。研究者调整了漏电积分发放（LIF）神经元的物理参数，使其速率响应曲线（Rate-based response）近似于深度学习中常用的 **ReLU 激活函数**。
*   **离线训练**：在仿真环境中使用基于速率的近似模型构建 Actor-Critic 架构。利用 **PPO（近端策略优化）** 算法进行训练。由于激活函数类似于 ReLU，有效地缓解了梯度消失问题，且无需复杂的代理梯度（Surrogate Gradient）设计。
*   **权重迁移与部署**：训练完成后，将学习到的权重直接转换为物理 LIF 神经元的参数，部署到英特尔 **Loihi 类脑芯片**上进行实时脉冲推理。

### 3. 实验设计
*   **实验场景**：
    1.  **夺旗（Capture-the-Flag, CtF）**：离散状态/动作空间。包括 1v1（随机对手）和 2v2（防御型对手）两种模式，具有高度随机性和对抗性。
    2.  **自动停车（Parking）**：连续状态/动作空间。测试物理动力学下的控制能力。
    3.  **实机部署**：在物理 TurtleBot3 机器人上，通过 Loihi 芯片运行 2v2 夺旗任务。
*   **对比基准（Benchmarks）**：
    *   **PopSAN**：基于群体编码的脉冲 Actor 网络。
    *   **Hybrid SNN**：ANN 与 SNN 结合的混合方法。
    *   **RSNN**：循环脉冲神经网络。

### 4. 资源与算力
*   **训练算力**：使用 GPU 进行离线训练。
*   **训练时长**：S2Act 表现出极高的效率。在 1v1 CtF 中仅需 **2.4 小时**，2v2 CtF 需 **5.0 小时**，停车任务需 **4.9 小时**。相比之下，PopSAN 的训练时间通常是其 3 倍以上。
*   **推理算力**：部署在英特尔 **Loihi (Kapoho Bay)** 类脑硬件上。文中通过 USB 功率计测量了每帧推理能耗（EpI），S2Act 在 Loihi 上的能耗约为 0.033J/inf。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从离散到连续、从仿真到实机的多种任务。
*   **充分性**：每组训练曲线均基于 **3 个随机种子**的平均值，并带有标准差阴影。在测试阶段，每个模型进行了 **1000 次**独立回合的评估。
*   **客观性**：对比了多种主流 SNN-RL 变体，并提供了详细的成功率、碰撞率、能耗和推理延迟数据，实验设计较为全面且公平。

### 6. 主要结论与发现
*   **性能领先**：S2Act 在所有测试场景中的成功率均优于或持平于最强基准（PopSAN），且训练速度显著加快。
*   **Sim-to-Real 鲁棒性**：通过简单的神经活动缩放，S2Act 能够成功从仿真环境迁移到物理机器人和类脑硬件，实现了首个在多智能体对抗任务中的 SNN 在线芯片演示。
*   **参数效率**：S2Act 仅需两个隐藏层（每层 64 个神经元）即可完成复杂任务，证明了简化架构在 SNN 中的有效性。

### 7. 优点与亮点
*   **简洁高效**：通过将 LIF 近似为 ReLU，巧妙地利用了成熟的深度学习优化工具，避开了 SNN 训练难的痛点。
*   **硬件友好**：直接面向类脑芯片设计，充分考虑了 Loihi 的量化约束和物理特性。
*   **快速原型化**：极短的训练时间使其非常适合机器人应用的快速迭代。

### 8. 不足与局限
*   **速率编码延迟**：基于速率的编码（Rate-based）在提高精度的同时会增加推理延迟，因为需要一定的时间窗口来估计脉冲频率，这在极高频的连续控制中可能存在瓶颈。
*   **量化损失**：在 Loihi 芯片部署时，由于硬件位宽限制（量化效应），性能较仿真环境有轻微下降。
*   **环境假设**：目前假设环境是完全可观测的，未深入探讨在部分可观测（POMDP）或极度动态环境下的表现。

（完）
