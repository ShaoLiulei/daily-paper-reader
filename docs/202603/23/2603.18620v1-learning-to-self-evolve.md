---
title: Learning to Self-Evolve
title_zh: 学习自我演化
authors: "Xiaoyin Chen, Canwen Xu, Yite Wang, Boyi Liu, Zhewei Yao, Yuxiong He"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18620v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型自我进化的强化学习
tldr: 本研究提出“学习自我进化”（LSE）框架，通过强化学习训练大语言模型在测试时自主优化上下文。不同于以往依赖模型固有推理能力的方法，LSE将多步进化简化为单步RL目标，并结合树引导进化循环。实验表明，经过LSE训练的4B模型在Text-to-SQL和MMLU任务上超越了GPT-5等强模型驱动的策略及现有提示优化方法，证明了将自我进化视为可学习技能的有效性。
selection_source: fresh_fetch
motivation: 现有模型在测试时自我进化主要依赖其固有的推理能力，而缺乏针对该进化过程的显式训练。
method: 提出LSE框架，将多步进化问题简化为单步强化学习目标，并结合树引导的进化循环来优化上下文编辑。
result: 训练后的4B模型在Text-to-SQL和MMLU任务上表现优于GPT-5驱动的策略及TextGrad等提示优化方法。
conclusion: 研究证明了将自我进化视为一种可学习的技能，可以显著提升模型在测试时的性能及其跨模型的迁移能力。
---

## 摘要
我们介绍了“学习自我演化”（Learning to Self-Evolve, LSE），这是一个强化学习框架，旨在训练大语言模型（LLM）在测试阶段改进其自身的上下文。我们将 LSE 置于测试时自我演化的场景中，模型通过对已见问题的反馈迭代地优化其上下文，从而在处理新问题时表现得更好。现有方法完全依赖于模型的固有推理能力，从未针对该任务进行显式训练。LSE 将多步演化问题简化为单步强化学习（RL）目标，其中每次上下文编辑都根据下游性能的提升获得奖励。我们将该目标与树引导的演化循环相结合。在 Text-to-SQL 生成（BIRD）和通用问答（MMLU-Redux）任务上，使用 LSE 训练的 4B 参数模型优于由 GPT-5 和 Claude Sonnet 4.5 驱动的自我演化策略，也优于包括 GEPA 和 TextGrad 在内的提示优化方法，并且无需额外训练即可迁移并引导其他模型。我们的结果强调了将自我演化视为一种可学习技能的有效性。

## Abstract
We introduce Learning to Self-Evolve (LSE), a reinforcement learning framework that trains large language models (LLMs) to improve their own contexts at test time. We situate LSE in the setting of test-time self-evolution, where a model iteratively refines its context from feedback on seen problems to perform better on new ones. Existing approaches rely entirely on the inherent reasoning ability of the model and never explicitly train it for this task. LSE reduces the multi-step evolution problem to a single-step RL objective, where each context edit is rewarded by the improvement in downstream performance. We pair this objective with a tree-guided evolution loop. On Text-to-SQL generation (BIRD) and general question answering (MMLU-Redux), a 4B-parameter model trained with LSE outperforms self-evolving policies powered by GPT-5 and Claude Sonnet 4.5, as well as prompt optimization methods including GEPA and TextGrad, and transfers to guide other models without additional training. Our results highlight the effectiveness of treating self-evolution as a learnable skill.

---

## 论文详细总结（自动生成）

以下是对论文《Learning to Self-Evolve》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：人类具备从经验中学习并进化的能力（如棋手复盘、程序员熟悉代码库），但目前的 LLM 在部署后通常是静态的，无论解决多少问题，其策略保持不变，且每次上下文重置都会丢失积累的经验。
*   **核心问题**：现有的“测试时自我演化”（Test-time Self-evolution）方法（如提示词优化、自我反思等）完全依赖模型**固有的推理能力**，从未针对“如何自我改进”这一特定任务进行显式训练。
*   **整体含义**：本文提出将“自我演化”视为一种**可学习的技能**。通过强化学习（RL）显式训练模型，使其能够根据环境反馈（如执行结果、正确性信号）自主重写和优化自身的指令/上下文。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将复杂的跨回合演化过程简化为单步强化学习目标，并结合树搜索算法在测试时进行全局优化。
*   **关键技术细节**：
    *   **LSE 框架**：将自我演化建模为“上下文强盗问题”（Contextual Bandit）。模型接收当前上下文 $c_t$ 和性能摘要 $S_t$，生成新的上下文 $c_{t+1}$。
    *   **改进型奖励（Improvement-based Reward）**：这是 LSE 的核心创新。奖励不再是编辑后的绝对分数，而是**编辑后与编辑前性能的差值**（$r_{LSE} = \bar{R}(c_1) - \bar{R}(c_0)$）。这种设计消除了初始提示词质量带来的偏差，直接激励模型产生“正向改进”。
    *   **树引导演化（Tree-Guided Evolution）**：在测试时，不使用简单的线性链式更新，而是维护一个演化树。利用 **UCB（置信区间上界）** 算法选择最有潜力的节点进行扩展。这允许模型在发现当前路径效果不佳时进行“回溯”，避免陷入局部最优。
    *   **训练流程**：使用 Qwen3-4B-Instruct 作为基础模型，通过在训练集上生成的演化轨迹进行离线强化学习训练。

### 3. 实验设计
*   **数据集/场景**：
    *   **Text-to-SQL**：使用 **BIRD** 数据集，涵盖金融、毒理学、代码库等 5 个领域。
    *   **通用问答**：使用 **MMLU-Redux**（10 个学科）和 SuperGPQA。
*   **Benchmark 与对比方法**：
    *   **强模型基准**：使用 GPT-5 和 Claude Sonnet 4.5 作为自我演化策略的驱动引擎。
    *   **提示优化方法**：对比了 **GEPA**（反射式提示演化）和 **TextGrad**（基于文本梯度的优化）。
    *   **动作模型**：统一使用 Qwen3-4B-Instruct 执行下游任务，仅改变负责“演化”的策略模型。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 `verl` 强化学习框架。
*   **训练细节**：学习率为 $1 \times 10^{-5}$，每批次采样 32 个节点，每个节点生成 4 个 rollout，训练 4 个 epoch。
*   **具体硬件**：文中**未明确说明**具体的 GPU 型号（如 A100/H100）及总算力消耗时长。

### 5. 实验数量与充分性
*   **实验规模**：在 BIRD 的 5 个数据库和 MMLU-Redux 的 10 个学科上进行了广泛测试。
*   **消融实验**：
    *   对比了“改进型奖励”与“标准 GRPO 奖励”的效果。
    *   对比了“树搜索（UCB）”与“线性链”搜索策略的差异。
    *   验证了 LSE 策略在不同动作模型（如 Arctic-Text2SQL-R1-7B）上的**迁移能力**。
*   **充分性评价**：实验设计较为全面，涵盖了不同任务领域、不同模型规模以及多种先进基线，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **小模型胜大模型**：经过 LSE 训练的 **4B 参数模型**，在自我演化任务上的表现优于未经过针对性训练的 **GPT-5** 和 **Claude 4.5**。
*   **奖励设计的关键性**：基于性能提升（Delta）的奖励比绝对性能奖励更能提供清晰的训练信号，使模型学会真正的“改进”而非仅仅“维持”。
*   **搜索策略的重要性**：树搜索配合 UCB 能有效防止演化过程中的性能崩溃，使系统具备从错误编辑中恢复的能力。
*   **通用性与迁移性**：LSE 训练出的演化策略可以跨模型工作，能够有效引导比自身更强的专业化模型（如 R1 调优后的模型）进一步提升。

### 7. 优点：亮点与创新
*   **范式转变**：将提示词优化从“依赖模型涌现能力”转变为“显式强化学习训练”，证明了自我演化是一项可训练的元技能。
*   **高效性**：证明了在特定元任务上，通过高质量的 RL 训练，小模型可以超越闭源巨型模型。
*   **鲁棒性设计**：引入树搜索和 UCB 解决了测试时反馈噪声大、演化路径易偏移的问题。

### 8. 不足与局限
*   **训练与测试的简化**：训练采用单步（Single-step）目标，虽然降低了信用分配难度，但可能无法捕捉需要多步战略规划的复杂演化路径。
*   **领域局限性**：目前仍需针对特定领域（如 SQL 或 QA）分别训练演化策略，尚未实现完全通用的跨任务演化模型。
*   **演化范围限制**：目前仅限于修改指令（Instructions），未涉及工具调用、外部记忆库或模型参数的在线更新。
*   **环境规模**：实验环境相对受限，在大规模、长程演化场景下的稳定性仍待验证。

（完）
