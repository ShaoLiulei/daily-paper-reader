---
title: "DyJR: Preserving Diversity in Reinforcement Learning with Verifiable Rewards via Dynamic Jensen-Shannon Replay"
title_zh: DyJR：通过动态 Jensen-Shannon 回放保持具有可验证奖励的强化学习中的多样性
authors: "Long Li, Zhijian Zhou, Tianyi Wang, Weidi Xu, Zuming Huang, Wei Chu, Zhe Wang, Shirui Pan, Chao Qu, Yuan Qi"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16157v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于大语言模型推理的强化学习
tldr: 针对强化学习（如GRPO）在提升大模型推理能力时存在的样本效率低和多样性缺失问题，本文提出DyJR框架。该方法通过动态Jensen-Shannon回放机制，利用时间敏感动态缓冲区和JS散度正则化，在重用历史数据的同时防止模型过拟合与模式崩溃。实验证明DyJR在数学推理和Text-to-SQL任务上显著优于现有基线，有效提升了生成多样性并保持了训练效率。
selection_source: fresh_fetch
motivation: 现有的经验回放方法在提升强化学习效率时，往往因过度拟合准确样本而导致模型出现模式崩溃和多样性丧失。
method: 提出DyJR框架，利用FIFO动态缓冲区保留近期样本，并通过Jensen-Shannon散度正则化约束分布演进以维持多样性。
result: 在数学推理和Text-to-SQL基准测试中，DyJR的性能显著优于GRPO、RLEP等基线方法，且训练效率与原始GRPO相当。
conclusion: DyJR通过动态参考分布有效平衡了样本重用与生成多样性，证明了维持历史数据多样性对强化学习优化大模型推理的重要性。
---

## 摘要
虽然强化学习（RL）增强了大语言模型的推理能力，但像 GRPO 这样的同策略（on-policy）算法由于丢弃了过去的采样序列（rollouts），导致样本效率低下。现有的经验回放方法通过重用准确的样本进行直接策略更新来解决这一问题，但这通常会带来高昂的计算成本，并因过拟合导致模式崩溃（mode collapse）。我们认为，历史数据应优先用于维持多样性，而非仅仅是强化准确性。为此，我们提出了动态 Jensen-Shannon 回放（DyJR），这是一个简单而有效的正则化框架，利用来自近期轨迹的动态参考分布。DyJR 引入了两项创新：（1）时间敏感动态缓冲区，采用先进先出（FIFO）和自适应大小调整，仅保留时间上邻近的样本，从而与模型演化保持同步；（2）Jensen-Shannon 散度正则化，用分布约束取代直接的梯度更新，以防止多样性崩溃。在数学推理和 Text-to-SQL 基准测试上的实验表明，DyJR 显著优于 GRPO 以及 RLEP 和 Ex-GRPO 等基准方法，同时保持了与原始 GRPO 相当的训练效率。此外，从 Rank-$k$ Token 概率演化的角度来看，我们展示了 DyJR 增强了多样性并减轻了对 Rank-1 Token 的过度依赖，阐明了 DyJR 的特定子模块如何影响训练动态。

## Abstract
While Reinforcement Learning (RL) enhances Large Language Model reasoning, on-policy algorithms like GRPO are sample-inefficient as they discard past rollouts. Existing experience replay methods address this by reusing accurate samples for direct policy updates, but this often incurs high computational costs and causes mode collapse via overfitting. We argue that historical data should prioritize sustaining diversity rather than simply reinforcing accuracy. To this end, we propose Dynamic Jensen-Shannon Replay (DyJR), a simple yet effective regularization framework using a dynamic reference distribution from recent trajectories. DyJR introduces two innovations: (1) A Time-Sensitive Dynamic Buffer that uses FIFO and adaptive sizing to retain only temporally proximal samples, synchronizing with model evolution; and (2) Jensen-Shannon Divergence Regularization, which replaces direct gradient updates with a distributional constraint to prevent diversity collapse. Experiments on mathematical reasoning and Text-to-SQL benchmarks demonstrate that DyJR significantly outperforms GRPO as well as baselines such as RLEP and Ex-GRPO, while maintaining training efficiency comparable to the original GRPO. Furthermore, from the perspective of Rank-$k$ token probability evolution, we show that DyJR enhances diversity and mitigates over-reliance on Rank-1 tokens, elucidating how specific sub-modules of DyJR influence the training dynamics.

---

## 论文详细总结（自动生成）

### DyJR：通过动态 Jensen-Shannon 回放保持强化学习中的多样性

#### 1. 核心问题与整体含义（研究动机和背景）
在大型语言模型（LLM）的推理任务中，强化学习（RL）已成为提升逻辑能力的关键。然而，主流的同策略（on-policy）算法（如 GRPO）在每次更新后都会丢弃昂贵的采样数据，导致**样本效率低下**。虽然现有的“经验回放”（Experience Replay）方法尝试通过重用历史正确样本来解决此问题，但它们通常采用直接的策略梯度更新，这会导致模型**过度拟合特定路径**，引发**模式崩溃（Mode Collapse）**和**多样性丧失**。

本文的核心动机在于重新定义经验回放的角色：**历史数据的主要价值应在于维持生成的多样性，而非仅仅是强化准确性。**

#### 2. 方法论：核心思想与关键技术
论文提出了 **DyJR (Dynamic Jensen-Shannon Replay)** 框架，其核心思想是将历史成功轨迹作为动态分布锚点，通过正则化手段约束当前策略。

*   **时间敏感动态缓冲区 (Time-Sensitive Dynamic Buffer)：**
    *   **FIFO 机制：** 仅保留最近 $M$ 步内生成的正确样本（$r=1$），确保参考分布紧跟当前模型的演化。
    *   **自适应规模：** 在训练初期的“快速转变期”扩大缓冲区（如从 5% 提升至 20% 的采样率），捕捉高熵的探索模式；随着模型稳定，收缩缓冲区以节省资源。
    *   **置信度分层准入：** 优先选择组内正确率高的样本，确保参考分布的低方差。
*   **Jensen-Shannon (JS) 散度正则化：**
    *   **核心公式：** 弃用直接的梯度上升，改为最小化当前策略 $\pi_\theta$ 与缓冲区分布 $Q_B$ 之间的 JS 散度。
    *   **低方差估计器：** 由于自回归模型的混合分布难以直接计算，采用生成式估计器：$f(u_s) = u_s \log u_s - (u_s + 1) \log \frac{u_s + 1}{2}$，其中 $u_s$ 是当前策略与历史策略的概率比。
    *   **总损失函数：** $L_{total} = L_{GRPO} + \alpha_{JS} \cdot L_{JS}$。

#### 3. 实验设计
*   **任务场景：**
    1.  **长链数学推理：** 使用 Qwen3-4B-Base 模型，在 Reinforce-Ada-Hard 数据集上训练。
    2.  **SQL 代码生成：** 使用 Llama-3.1-8B-Instruct 模型，在 BIRD 数据集上训练。
*   **Benchmark：**
    *   数学：AIME25, AMC23, Beyond AIME, BRUMO25, HMMT25, Minerva。
    *   SQL：BIRD, Spider。
*   **对比方法：**
    *   基础基线：GRPO, DAPO。
    *   经验回放基线：RLEP, Ex-GRPO。
    *   散度变体：DPH-RL (静态 JS), Forward KL 变体。

#### 4. 资源与算力
*   **硬件设备：** 实验使用了 **32 张 NVIDIA A800 GPU**。
*   **训练效率：** 论文明确指出 DyJR 的 GPU 显存额外开销极低（**小于 1GB**），因为仅存储 Token 级别的 Log-probabilities。
*   **时间成本：** 由于回放批次远小于在线批次，DyJR 的训练吞吐量（Rollout 和 Update 阶段）与原始 GRPO 基本持平。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了两个不同领域（数学、SQL）、两种主流模型家族（Qwen、Llama）。
*   **消融实验：** 进行了详尽的消融研究，包括：
    *   不同散度度量（JS vs. Forward KL）的对比。
    *   正则化系数 $\alpha_{JS}$ 的敏感度分析。
    *   缓冲区最大寿命 $M$ 的影响。
    *   初期探索数据重要性的验证。
*   **深度分析：** 引入了 **Rank-$k$ Token 概率演化分析**，从概率分布层面客观展示了模型如何避免模式崩溃。实验设计严谨，对比基线涵盖了最新的同类研究，具有较高的说服力。

#### 6. 主要结论与发现
*   **性能提升：** DyJR 在数学任务上平均准确率达到 34.1%（比 GRPO 高 4.3%），在 SQL 任务上也显著提升了 Pass@1 和 Pass@16。
*   **多样性保护：** DyJR 有效减缓了 Rank-1 Token 概率的过快增长，使模型在训练后期仍能保持对 Rank-2/3 Token 的探索能力。
*   **早期数据价值：** 实验发现**训练初期的探索数据比后期的准确数据更重要**，因为它们包含了丰富的解题路径多样性。
*   **JS 散度的优越性：** 相比 Forward KL，JS 散度在处理非平稳的历史混合分布时更加稳健，避免了过度平滑。

#### 7. 优点
*   **理念创新：** 改变了经验回放“只为刷准”的固有思维，将其转化为“保护多样性”的手段。
*   **高效轻量：** 动态缓冲区和 FIFO 机制在提升性能的同时，几乎不增加计算和存储负担。
*   **可解释性强：** 通过 Rank-$k$ 概率分析，清晰地揭示了算法对模型内部训练动态的影响。

#### 8. 不足与局限
*   **任务局限：** 目前仅在具有**可验证奖励（Verifiable Rewards）**的任务（数学、SQL）上验证，对于奖励信号模糊或主观的 RLHF 场景（如创意写作、对话对齐）是否同样有效尚待研究。
*   **超参数依赖：** 正则化系数 $\alpha_{JS}$ 和缓冲区寿命 $M$ 对性能有显著影响，可能需要针对不同模型进行微调。
*   **模型规模：** 实验主要集中在 4B 和 8B 模型，尚未在大规模参数模型（如 70B+）上验证其扩展性。

（完）
