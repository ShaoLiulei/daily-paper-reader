---
title: "PoC: Performance-oriented Context Compression for Large Language Models via Performance Prediction"
title_zh: PoC：通过性能预测实现大语言模型面向性能的上下文压缩
authors: "Runsong Zhao, Shilei Liu, Jiwei Tang, Langming Liu, Haibin Chen, Weidong Zhang, Yujin Yuan, Tong Xiao, Jingbo Zhu, Wenbo Su, Bo Zheng"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19733v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型的上下文压缩
tldr: 针对大语言模型上下文压缩中性能下降不可预测的问题，本文提出了一种面向性能的上下文压缩（PoC）新范式。不同于传统指定压缩率的方法，PoC 允许用户设定性能底线，通过轻量级性能预测器自动寻找满足约束的最优压缩率。研究对比了上下文无关和上下文感知两种预测器，证明了上下文感知预测器在问答和摘要任务中能更精准地平衡压缩效率与模型性能，为可靠部署提供了新思路。
selection_source: fresh_fetch
motivation: 现有的上下文压缩方法通常指定固定的压缩率，导致模型性能下降难以预测，阻碍了其在实际场景中的可靠部署。
method: 提出 PoC 框架，利用轻量级性能预测器在压缩前预测性能，从而动态确定满足用户性能底线要求的最大压缩率。
result: 实验表明，上下文感知预测器在问答和摘要基准测试中具有更低的预测误差，且 PoC 在整体性能表现上优于传统方法。
conclusion: PoC 为大语言模型提供了一种更可靠、高效且感知性能的上下文压缩部署方案，实现了压缩效率与模型效果的精准权衡。
---

## 摘要
虽然上下文压缩可以通过缩短上下文来缓解大语言模型（LLM）日益增长的推理成本，但现有的指定目标压缩率或长度的方法会遭受不可预测的性能下降，从而阻碍了它们的可靠部署。我们引入了一种向面向性能的上下文压缩（PoC）的范式转变，在这种范式中，开发人员指定一个可接受的性能底线，而不是压缩率。PoC 采用轻量级性能预测器，在引导现成的压缩器之前，自动寻找满足该约束的最激进压缩率。我们设计并比较了两种预测器变体：一种简单的上下文无关预测器，以及一种考虑输入固有可压缩性的更复杂的上下文感知预测器。在问答和摘要基准测试中，上下文感知预测器始终比上下文无关预测器获得更低的性能预测误差，而由此产生的上下文感知 PoC 则获得了更优越的整体性能。我们的工作为 LLM 上下文压缩的更可靠、高效且感知性能的部署铺平了道路。

## Abstract
While context compression can mitigate the growing inference costs of Large Language Models (LLMs) by shortening contexts, existing methods that specify a target compression ratio or length suffer from unpredictable performance degradation, hindering their reliable deployment. We introduce a paradigm shift to Performance-oriented Context Compression (PoC), where developers specify an acceptable performance floor instead of a compression ratio. PoC employs a lightweight performance predictor to automatically find the most aggressive compression ratio that satisfies this constraint before steering an off-the-shelf compressor. We design and compare two predictor variants: a simple context-agnostic predictor and a more sophisticated context-aware one that considers the input's inherent compressibility. On both question-answering and summarization benchmarks, the context-aware predictor consistently achieves lower performance prediction error than the context-agnostic predictor, while the resulting context-aware PoC attains a superior overall performance. Our work paves the way for a more reliable, efficient, and performance-aware deployment of context compression for LLMs.

---

## 论文详细总结（自动生成）

以下是对论文《PoC: Performance-oriented Context Compression for Large Language Models via Performance Prediction》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的上下文压缩方法多为“预算导向（BoC）”，即用户需预设固定的压缩率或长度。然而，压缩率与任务性能之间的关系高度依赖于输入内容，导致性能下降具有不可预测性，这阻碍了压缩技术在实际生产环境中的可靠部署。
*   **研究动机**：为了解决“激进压缩导致性能崩溃”或“保守压缩浪费计算资源”的矛盾，作者提出将控制权从“压缩预算”转向“性能底线”，实现一种可控、可靠的上下文压缩范式。

### 2. 方法论
*   **核心思想**：引入“面向性能的上下文压缩（PoC）”框架。用户指定一个性能保留目标（如：保留原始性能的90%），系统通过轻量级预测器自动寻找能满足该目标的最小压缩率（即最激进的压缩）。
*   **关键技术细节**：
    *   **性能预测器**：设计了两种变体。
        *   *上下文无关（Context-agnostic）*：基于校准数据集拟合平均性能曲线。
        *   *上下文感知（Context-aware）*：基于剪枝后的轻量级小语言模型（SLM），通过单一前向传播并行预测多个压缩率下的性能保留率。
    *   **性能保留率（Performance Retention）**：定义为 `min(压缩后得分 / 原始得分, 1)`，将不同任务的原始指标（F1, ROUGE等）归一化到 [0, 1] 区间。
    *   **两阶段搜索算法**：
        1.  *粗粒度搜索*：在 [0, 1] 范围内均匀采样18个点，定位目标区间。
        2.  *细粒度搜索*：在目标区间内再次采样18个点，精准锁定最优压缩率 $r^*$。
    *   **执行压缩**：使用现成的压缩器（如 LLMLingua2）按计算出的 $r^*$ 执行硬压缩。

### 3. 实验设计
*   **数据集/场景**：
    *   **问答（QA）**：SearchQA, TriviaQA, Natural Questions, HotpotQA, SQuAD。
    *   **摘要（Summarization）**：GovReport, SummScreenFD。
*   **Benchmark与对比方法**：
    *   以“上下文无关预测器”作为基准。
    *   使用 Llama-3.1-8B-Instruct 作为阅读器（Reader LLM）。
    *   使用 LLMLingua2 作为底层压缩器。
*   **评估指标**：
    *   **PPE（性能预测误差）**：衡量预测器准确度。
    *   **P@R（Performance at Ratio）**：新提出的指标，计算性能-压缩率曲线下的面积（AUC），用于公平比较不同压缩策略的整体效率。

### 4. 资源与算力
*   **模型规格**：预测器基于 Llama-3.2-1B，但经过大幅剪枝，仅保留嵌入层和前2个 Transformer 层。
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 256，学习率 2e-5，采用 bfloat16 混合精度训练。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或总训练时长。但在推理延迟实验中提到，预测器在处理 512 token 块时仅增加 1.53ms 的延迟。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个主流长文本基准测试，涉及问答和摘要两大核心任务。
*   **充分性分析**：
    *   对比了不同预测器变体，验证了上下文感知的重要性。
    *   深入分析了不同任务（QA vs 摘要）的性能-压缩曲线特征（如 QA 的“悬崖式”下降和摘要的“平滑式”下降）。
    *   进行了延迟评估，证明了框架的实用性。
    *   **客观性**：通过 P@R 指标解决了自适应压缩方法难以直接对比的难题，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **预测准确性**：上下文感知预测器在所有数据集上的 PPE 均显著低于基准（最高降低 33.9%）。
*   **性能提升**：在相同压缩水平下，PoC 能获得更高的任务得分（P@R 显著提升），尤其在 TriviaQA 上表现惊人（F1@R 从 49.2 升至 67.5）。
*   **去噪效应**：发现 TriviaQA 在压缩率为 0.4 左右时性能反而优于不压缩，说明适度压缩能起到“去噪”作用。
*   **效率优势**：PoC 框架的总开销（预测+压缩）约为 29ms/块，相对于长文本推理带来的 14x-36x 加速，开销几乎可以忽略。

### 7. 优点
*   **范式创新**：首次提出以“性能”为输入参数的压缩框架，极大提升了 LLM 部署的可控性。
*   **轻量高效**：预测器设计精巧，支持并行预测，推理开销极低，适合在线场景。
*   **通用性强**：作为一个元框架（Meta-framework），可以适配任何现成的上下文压缩器。

### 8. 不足与局限
*   **性能天花板**：PoC 的效果受限于底层压缩器（如 LLMLingua2）的质量，它只能优化压缩率的选择，无法提升压缩算法本身的上限。
*   **数据依赖**：训练预测器需要收集特定任务和特定阅读器模型下的性能数据，迁移到新模型或新任务时可能需要重新校准。
*   **实验覆盖**：目前主要验证了硬压缩（Token 移除），尚未探讨在软压缩（Embedding 压缩）或 KV-Cache 压缩上的表现。

（完）
