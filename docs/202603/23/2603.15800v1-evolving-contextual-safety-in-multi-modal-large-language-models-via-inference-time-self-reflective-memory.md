---
title: Evolving Contextual Safety in Multi-Modal Large Language Models via Inference-Time Self-Reflective Memory
title_zh: 通过推理阶段自反思记忆演进多模态大语言模型的上下文安全性
authors: "Ce Zhang, Jinxi He, Junyi He, Katia Sycara, Yaqi Xie"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15800v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 多模态大语言模型的上下文安全性评估
tldr: 本研究针对多模态大语言模型（MLLM）在处理细微上下文差异时的安全性漏洞，提出了MM-SafetyBench++基准测试，通过对比相似但意图相反的安全与不安全场景来评估模型的上下文安全能力。同时，提出了一种无需训练的框架EchoSafe，利用自反思记忆库积累并检索过往交互中的安全见解，在推理阶段实现上下文感知推理和安全行为的持续演进。实验证明EchoSafe在多个安全基准上表现优异。
selection_source: fresh_fetch
motivation: 现有的多模态模型安全研究多关注显式违规防御，忽视了模型在区分看似相似但安全意图截然不同的上下文场景时的脆弱性。
method: 提出了包含对比样本的基准测试MM-SafetyBench++，以及通过自反思记忆库在推理时动态检索并整合安全经验的EchoSafe框架。
result: 实验结果显示，EchoSafe在多个多模态安全基准测试中均取得了领先的性能，显著提升了模型在复杂上下文中的安全判断力。
conclusion: 该研究通过引入上下文安全评估和推理时自反思记忆机制，为提升多模态大语言模型的鲁棒性和安全性提供了新的有效路径。
---

## 摘要
多模态大语言模型（MLLMs）在广泛的视觉推理任务中取得了显著性能，但其对安全风险的脆弱性仍然是一个紧迫的问题。虽然先前的研究主要集中在检测和拒绝显式不安全输入的越狱防御上，但此类方法往往忽略了上下文安全性，这要求模型能够区分看似相似但在安全意图上存在显著差异的场景之间的细微上下文区别。在这项工作中，我们提出了 MM-SafetyBench++，这是一个专为上下文安全性评估而精心设计的基准测试。具体而言，对于每个不安全的图文对，我们通过最小程度的修改构建了相应的安全对照组，这些修改在保留底层上下文含义的同时反转了用户意图，从而能够受控地评估模型是否可以根据上下文理解来调整其安全行为。此外，我们引入了 EchoSafe，这是一个无需训练的框架，它维护一个自反思记忆库，用于积累和检索先前交互中的安全见解。通过将相关的过去经验整合到当前的提示词中，EchoSafe 实现了上下文感知推理，并在推理过程中实现了安全行为的持续演进。在各种多模态安全基准测试上的广泛实验表明，EchoSafe 一致地取得了卓越的性能，为推进 MLLMs 的上下文安全性建立了一个强大的基准。所有基准数据和代码均可在 https://echosafe-mllm.github.io 获取。

## Abstract
Multi-modal Large Language Models (MLLMs) have achieved remarkable performance across a wide range of visual reasoning tasks, yet their vulnerability to safety risks remains a pressing concern. While prior research primarily focuses on jailbreak defenses that detect and refuse explicitly unsafe inputs, such approaches often overlook contextual safety, which requires models to distinguish subtle contextual differences between scenarios that may appear similar but diverge significantly in safety intent. In this work, we present MM-SafetyBench++, a carefully curated benchmark designed for contextual safety evaluation. Specifically, for each unsafe image-text pair, we construct a corresponding safe counterpart through minimal modifications that flip the user intent while preserving the underlying contextual meaning, enabling controlled evaluation of whether models can adapt their safety behaviors based on contextual understanding. Further, we introduce EchoSafe, a training-free framework that maintains a self-reflective memory bank to accumulate and retrieve safety insights from prior interactions. By integrating relevant past experiences into current prompts, EchoSafe enables context-aware reasoning and continual evolution of safety behavior during inference. Extensive experiments on various multi-modal safety benchmarks demonstrate that EchoSafe consistently achieves superior performance, establishing a strong baseline for advancing contextual safety in MLLMs. All benchmark data and code are available at https://echosafe-mllm.github.io.

---

## 论文详细总结（自动生成）

### 论文总结：通过推理阶段自反思记忆演进多模态大语言模型的上下文安全性

#### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于多模态大语言模型（MLLM）的**上下文安全性（Contextual Safety）**。
*   **研究动机**：现有的安全防御方法（如安全微调、输入过滤）往往表现出“过度防御”倾向，即模型会无差别地拒绝包含敏感关键词或物体的请求，而无法区分细微的上下文差异（例如：询问“如何用这把刀切菜”是安全的，而“如何用这把刀伤人”是不安全的）。
*   **核心问题**：现有的安全基准测试缺乏对这种细微意图区分能力的评估，且模型缺乏在推理过程中根据过往经验自我进化的能力。

#### 2. 方法论：核心思想与技术细节
论文提出了两个核心贡献：**MM-SafetyBench++** 基准测试和 **EchoSafe** 防御框架。

*   **MM-SafetyBench++**：
    *   **配对设计**：为每个不安全样本构建一个“安全对照组”。通过最小化修改（Minimal Modifications）翻转用户意图，但保留相同的视觉背景和上下文语义。
    *   **高质量合成**：利用先进的文生图模型（如 Qwen-Image）生成高保真图像，并包含 OCR 增强攻击模式（将恶意指令嵌入图像中）。
*   **EchoSafe 框架**（无需训练的推理阶段增强）：
    *   **自反思记忆库（Self-Reflective Memory）**：模型在每次交互后，会对自己的推理过程进行自反思，提取出抽象的、可泛化的“安全见解”（Safety Insights），并将其与当前上下文的嵌入向量（Embedding）一同存入记忆库。
    *   **检索与整合**：当新查询到来时，系统通过向量相似度检索最相关的 $k$ 条历史安全见解，将其作为上下文示例（In-context guidance）加入 Prompt 中。
    *   **持续演进**：随着交互增加，记忆库不断扩充，模型在推理阶段表现出类似“终身学习”的进化特征。

#### 3. 实验设计
*   **数据集/场景**：
    *   **安全基准**：MM-SafetyBench++（本文提出）、MM-SafetyBench、MSSBench、SIUO。涵盖非法活动、仇恨言论、恶意软件、身体伤害、欺诈、性内容 6 大类别。
    *   **通用能力基准**：MME、MMBench、ScienceQA、TextVQA（用于验证防御是否损害了模型的正常功能）。
*   **对比方法**：
    *   **基座模型**：GPT-5 (预研版)、Gemini-2.5、LLaVA-1.5、Qwen-2.5-VL 等。
    *   **防御基准**：FigStep、ECSO、AdaShield 以及安全微调方法（Post-hoc LoRA）。
*   **评估指标**：拒绝率（RR）、回答率（AR）、上下文正确率（CCR，RR 与 AR 的调和平均值）以及质量得分（QS，由 GPT-5-Mini 作为裁判评分）。

#### 4. 资源与算力
*   **硬件环境**：实验使用了 **8 张 NVIDIA RTX 6000 Ada (48GB)** GPU。
*   **训练/推理时长**：由于 EchoSafe 是**无需训练（Training-free）**的方法，主要开销在于推理时的向量检索和自反思生成。文中提到 EchoSafe 的推理时间约为基座模型的 1.33 倍，总 FLOPs 约为 1.69 倍。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个代表性开源模型（LLaVA-1.5, LLaVA-NeXT, Qwen-2.5-VL）上进行了详尽测试，并扩展到了 13B 和 32B 的更大参数规模。
*   **消融实验**：针对嵌入模型选择、记忆库大小（$k$ 值）、检索策略（随机 vs 相似度）、存储格式（原始 QA vs 抽象见解）均做了对比实验。
*   **客观性**：通过 GPT-5-Mini 与人类评估、Gemini 评估的一致性分析（相关系数 $\rho \approx 0.74$），证明了自动评分机制的可靠性。实验设计涵盖了安全与实用的平衡，避免了单一指标的偏见。

#### 6. 主要结论与发现
*   **防御有效性**：EchoSafe 在 MM-SafetyBench++ 上显著优于现有方法，CCR 指标提升巨大（例如在 Qwen-2.5-VL 上比 AdaShield 高出 16.8%）。
*   **解决过度防御**：相比于 AdaShield 等方法会导致安全样本回答率骤降，EchoSafe 能够保持极高的回答率和回答质量。
*   **知识迁移**：记忆库中的安全见解具有跨类别迁移能力，即使是不同类别的历史经验也能辅助当前的安全判断。
*   **性能无损**：在通用视觉问答任务上，EchoSafe 几乎没有造成性能下降，实现了安全与效用的双赢。

#### 7. 优点：亮点总结
*   **基准测试创新**：MM-SafetyBench++ 的“配对样本”设计填补了上下文安全评估的空白，能够精准识别模型的“盲目拒绝”行为。
*   **即插即用**：EchoSafe 不需要昂贵的重新训练或微调，适合快速部署于各种 MLLM 架构。
*   **自进化能力**：引入自反思机制，使模型能够从过去的成功或失败中学习，模拟了人类形成安全准则的过程。

#### 8. 不足与局限
*   **合成数据偏差**：基准测试中的图像主要由 AI 生成，虽然保真度高，但与真实世界中复杂、多变且带有文化偏见的自然场景仍有差距。
*   **推理延迟**：引入记忆检索和自反思步骤增加了推理延迟（约 33%），对于实时性要求极高的应用场景可能存在限制。
*   **记忆库维护**：长期运行下记忆库的冗余清理、冲突处理以及存储规模的扩展性在论文中未做极长周期的深入探讨。

（完）
