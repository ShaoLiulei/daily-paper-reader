---
title: "MorphSNN: Adaptive Graph Diffusion and Structural Plasticity for Spiking Neural Networks"
title_zh: MorphSNN：脉冲神经网络的自适应图扩散与结构可塑性
authors: "Yongsheng Huang, Peibo Duan, Yujie Wu, Kai Sun, Zhipeng Liu, Jiaxiang Liu, Guangyu Li, Changsheng Zhang, Bin Zhang, Mingkun Xu"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14285v1"
tags: ["query:snn"]
score: 10.0
evidence: 具有结构可塑性和图扩散的脉冲神经网络架构
tldr: 针对脉冲神经网络（SNN）宏观架构静态且缺乏侧向交互的问题，本文提出MorphSNN框架。该框架受生物非突触扩散和结构可塑性启发，引入图扩散（GD）机制实现高效的无向信号传播，并结合时空结构可塑性（STSP）实现实例级的动态拓扑重组。MorphSNN在多个数据集上达到SOTA性能，并凭借自进化拓扑在分布外（OOD）检测中表现优异。
selection_source: fresh_fetch
motivation: 旨在解决SNN中神经元动态性与宏观固定层级架构之间的矛盾，以增强网络在复杂环境下的适应能力。
method: 引入图扩散机制实现无向信号传播，并结合时空结构可塑性机制赋予网络动态重组拓扑的能力。
result: 在N-Caltech101等数据集上实现SOTA精度，并利用自进化拓扑特征在无辅助训练下实现了卓越的OOD检测。
conclusion: MorphSNN通过模拟生物脑的扩散与可塑性，成功克服了固定拓扑的局限，提升了SNN的性能与鲁棒性。
---

## 摘要
脉冲神经网络（SNNs）目前面临一个关键瓶颈：虽然单个神经元表现出动态的生物学特性，但其宏观架构仍局限于静态且分层的传统连接模式。这种神经元层级的动态性与网络层级的固定连接性之间的差异，消除了关键的类脑侧向交互，限制了其在多变环境中的适应能力。为解决这一问题，我们提出了 MorphSNN，这是一个受生物非突触扩散和结构可塑性启发的骨干框架。具体而言，我们引入了一种图扩散（GD）机制，以促进高效的无向信号传播，从而补充前馈分层结构。此外，它结合了时空结构可塑性（STSP）机制，赋予网络针对特定实例进行动态拓扑重组的能力，从而克服了固定拓扑的局限性。实验表明，MorphSNN 在静态和神经形态数据集上均达到了最先进的准确率；例如，在仅 5 个时间步的情况下，它在 N-Caltech101 上达到了 83.35% 的准确率。更重要的是，其自演化的拓扑结构可作为内在的分布指纹，无需辅助训练即可实现卓越的分布外（OOD）检测。代码可在 anonymous.4open.science/r/MorphSNN-B0BC 获取。

## Abstract
Spiking Neural Networks (SNNs) currently face a critical bottleneck: while individual neurons exhibit dynamic biological properties, their macro-scopic architectures remain confined within conventional connectivity patterns that are static and hierarchical. This discrepancy between neuron-level dynamics and network-level fixed connectivity eliminates critical brain-like lateral interactions, limiting adaptability in changing environments. To address this, we propose MorphSNN, a backbone framework inspired by biological non-synaptic diffusion and structural plasticity. Specifically, we introduce a Graph Diffusion (GD)mechanism to facilitate efficient undirected signal propagation, complementing the feedforward hierarchy. Furthermore, it incorporates a Spatio-Temporal Structural Plasticity (STSP) mechanism, endowing the network with the capability for instance-specific, dynamic topological reorganization, thereby overcoming the limitations of fixed topologies. Experiments demonstrate that MorphSNN achieves state-of-the-art accuracy on static and neuromorphic datasets; for instance, it reaches 83.35% accuracy on N-Caltech101 with only 5 timesteps. More importantly, its self-evolving topology functions as an intrinsic distribution fingerprint, enabling superior Out-of- Distribution (OOD) detection without auxiliary training. The code is available at anonymous.4open.science/r/MorphSNN-B0BC.

---

## 论文详细总结（自动生成）

### MorphSNN：脉冲神经网络的自适应图扩散与结构可塑性总结

#### 1. 核心问题与研究动机
脉冲神经网络（SNN）虽然在神经元层面具有丰富的动态特性，但在宏观架构上仍沿用人工神经网络（ANN）的**静态分层连接**模式。这种局限性导致了两个主要问题：
*   **静态性：** 模型部署后结构固定，难以适应复杂多变的实时环境。
*   **层级性：** 缺乏侧向交互，导致信号延迟和“特征绑定”问题（Feature Binding Problem）。
**研究动机：** 受生物脑中“非突触扩散”和“结构可塑性”启发，旨在构建一种能够根据输入实例动态调整拓扑结构、并实现全局信息高效扩散的 SNN 框架。

#### 2. 方法论：核心思想与关键技术
论文提出了 **MorphSNN** 框架，其核心由两个机制组成：
*   **图扩散（GD）机制：** 
    *   将信号传播建模为连续的、能量最小化的扩散过程。
    *   利用对称归一化的拉普拉斯算子实现无向信号传播，打破了传统前馈网络的单向限制。
    *   **理论保证：** 证明了 GD 相当于对狄利克雷能量（Dirichlet Energy）进行梯度下降，保证了信号传播的稳定性和单调收敛性，防止过度激活。
*   **时空结构可塑性（STSP）机制：**
    *   **动态重组：** 在每个推理时间步，根据神经元的即时状态动态更新邻接矩阵。
    *   **核心原理：** 结合了赫布可塑性（Hebbian plasticity，同频共振）、图注意力机制、稳态可塑性（归一化）和短期可塑性（STP，动量更新）。
    *   **拓扑稀疏化：** 采用动态 Top-k 剪枝，仅保留最相关的连接，并使用梯度掩码确保端到端可训练。

#### 3. 实验设计
*   **数据集：** 
    *   **神经形态数据集：** DVS-Gesture, CIFAR10-DVS, N-Caltech101, UCF101-DVS。
    *   **静态数据集：** CIFAR-10/100, ImageNet-1k。
    *   **OOD 检测：** 使用 DVS-Lip 作为 Near-OOD 场景。
*   **Benchmark 与对比方法：** 
    *   对比了 ResNet-19, Spikformer, Spikingformer, SEW-ResNet, CogniSNN 等 SOTA 模型。
    *   OOD 检测对比了 MSP, Energy-based, kNN, SCP 等方法。

#### 4. 资源与算力
*   **硬件设备：** 
    *   **ImageNet 实验：** 使用了 4 台 NVIDIA H100 GPU。
    *   **其他实验：** 使用单张 RTX 4090 GPU。
*   **训练时长：** 文中提到 Full 模式在 CIFAR-10 上单轮（Epoch）训练时间约为 121 秒，相比静态模式（72 秒）更长，主因是 STSP 需要在时间步上串行处理，限制了并行性。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从小型神经形态数据到大规模 ImageNet 的 7 个以上数据集。
*   **消融实验：** 针对 GD、STSP、对称性、节点数 $N$、扩散步数 $M$、剪枝 $k$ 等关键超参数进行了详尽的敏感性分析。
*   **鲁棒性测试：** 进行了椒盐噪声、泊松噪声和帧丢失三种干扰下的性能评估。
*   **评价：** 实验设计非常充分且客观，不仅关注精度，还深入分析了能量消耗、发射率和理论收敛性。

#### 6. 主要结论与发现
*   **性能领先：** 在 N-Caltech101 上仅用 5 个时间步达到 83.35% 的 SOTA 精度；在 ImageNet 上达到 75.04% 的 Top-1 精度。
*   **OOD 检测突破：** 发现自进化拓扑可作为“分布指纹”，提出的 DGP 方法在无需额外训练的情况下，OOD 检测性能（如 FPR95）显著优于传统方法。
*   **动态适应性：** STSP 在处理具有真实时间动态的数据（如 UCF101-DVS）时优势明显，而在处理由静态图像转换的 DVS 数据时，静态图扩散已足够。

#### 7. 优点与亮点
*   **生物启发性强：** 成功将非突触扩散和结构可塑性转化为可数学描述的深度学习模块。
*   **理论支撑：** 引入狄利克雷能量分析，为 SNN 的图扩散稳定性提供了数学证明。
*   **多功能性：** 模型不仅用于分类，其演化出的拓扑结构天然适合做分布外检测，具有极高的实用价值。
*   **能效比：** 尽管增加了动态计算，但由于操作在低维通道特征上且利用了脉冲稀疏性，整体能耗依然极低。

#### 8. 不足与局限
*   **计算串行化：** STSP 机制要求在推理时间步内进行实时拓扑更新，这导致无法像传统 SNN 那样在时间维度上进行大规模并行加速，增加了训练耗时。
*   **数据敏感性：** 实验发现 STSP 在某些缺乏真实动态（如通过扫视生成的 DVS 数据）的数据集上可能捕获伪相关性，导致性能略微下降。
*   **超参数依赖：** 动量系数 $\beta$ 和剪枝 $k$ 等参数对性能有一定影响，可能需要针对不同任务进行微调。

（完）
