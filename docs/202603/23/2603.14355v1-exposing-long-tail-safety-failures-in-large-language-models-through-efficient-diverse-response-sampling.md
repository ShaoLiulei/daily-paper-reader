---
title: Exposing Long-Tail Safety Failures in Large Language Models through Efficient Diverse Response Sampling
title_zh: 通过高效的多样化响应采样揭示大语言模型中的长尾安全失效
authors: "Suvadeep Hajra, Palash Nandi, Tanmoy Chakraborty"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14355v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 通过有监督微调和 RLHF 进行安全对齐
tldr: 本研究指出安全微调虽能提升大模型鲁棒性，但往往只是抑制而非消除了不安全行为，使其潜藏在输出分布的长尾中。不同于侧重输入提示词优化的传统方法，本文提出 PDPS 采样法，通过在输出空间进行多样化响应生成来暴露安全漏洞。PDPS 结合了随机采样与多样性感知选择，能以极低的计算成本高效挖掘语义多样的不安全输出，显著提升了红队测试的效率和覆盖面。
selection_source: fresh_fetch
motivation: 现有的安全微调技术未能彻底消除不安全行为，导致在特定提示词下仍存在难以被发现的长尾安全风险。
method: 提出渐进式多样化种群采样（PDPS），通过结合随机令牌采样与多样性感知选择，在固定提示词下高效筛选语义多样的响应。
result: "PDPS 仅需 8% 到 29% 的计算成本即可达到与大规模采样相当的攻击成功率，并在受限响应场景下比传统采样方法提升了 26% 到 40%。"
conclusion: 通过探索输出空间的多样性可以更系统地揭示大模型的长尾安全失效，为评估和增强模型安全性提供了高效的新工具。
---

## 摘要
通过监督微调和人类反馈强化学习进行的安全微调显著提高了大语言模型（LLMs）的鲁棒性。然而，这种方法通常只是抑制而非消除不安全行为，使得罕见但关键的失效隐藏在输出分布的长尾中。虽然大多数红队测试工作强调对抗性提示词搜索（输入空间优化），但我们展示了对于固定的安全关键提示词，通过多样化响应生成（输出空间探索）也可以系统地揭示安全失效，其中增加采样响应的数量和多样性可以将越狱成功率推向接近 1。为了高效地发现此类失效，我们提出了渐进式多样化种群采样（PDPS），它结合了随机词元级采样与多样性感知选择，以探索庞大的候选响应池并保留一个紧凑且语义多样的子集。在多个越狱基准测试和开源大语言模型上，PDPS 仅使用 8% 到 29% 的计算成本，就实现了与大规模独立同分布（IID）采样相当的攻击成功率。在有限响应设置下，它比 IID 采样和多样化束搜索（Diverse Beam Search）的成功率提高了 26% 到 40%。此外，PDPS 生成的响应表现出更高数量和更多样化的不安全输出，证明了其在揭示更广泛失效方面的有效性。

## Abstract
Safety tuning through supervised fine-tuning and reinforcement learning from human feedback has substantially improved the robustness of large language models (LLMs). However, it often suppresses rather than eliminates unsafe behaviors, leaving rare but critical failures hidden in the long tail of the output distribution. While most red-teaming work emphasizes adversarial prompt search (input-space optimization), we show that safety failures can also be systematically exposed through diverse response generation (output-space exploration) for a fixed safety-critical prompt, where increasing the number and diversity of sampled responses can drive jailbreak success rates close to unity. To efficiently uncover such failures, we propose Progressive Diverse Population Sampling (PDPS), which combines stochastic token-level sampling with diversity-aware selection to explore a large candidate pool of responses and retain a compact, semantically diverse subset. Across multiple jailbreak benchmarks and open-source LLMs, PDPS achieves attack success rates comparable to large-scale IID sampling while using only 8% to 29% of the computational cost. Under limited-response settings, it improves success rates by 26% to 40% over IID sampling and Diverse Beam Search. Furthermore, responses generated by PDPS exhibit both a higher number and greater diversity of unsafe outputs, demonstrating its effectiveness in uncovering a broader range of failures.

---

## 论文详细总结（自动生成）

以下是对论文《Exposing Long-Tail Safety Failures in Large Language Models through Efficient Diverse Response Sampling》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLMs）通过安全微调（SFT）和人类反馈强化学习（RLHF）显著提升了安全性，但这些方法往往只是**抑制**而非彻底**消除**了不安全行为。这些不安全响应潜藏在模型输出分布的“长尾”区域。
*   **核心问题**：现有的红队测试（Red-teaming）主要集中在“输入空间优化”（即寻找能绕过过滤器的对抗性提示词），而忽略了“输出空间探索”。本文旨在证明：即使对于固定的安全关键提示词，通过高效的多样化采样，也能系统地揭示模型潜藏的安全失效。

### 2. 方法论：渐进式多样化种群采样 (PDPS)
*   **核心思想**：PDPS 放弃了低效的大规模独立同分布（IID）采样，转而采用一种“扩展-选择”的迭代策略。其目标是在保持极低计算成本的同时，覆盖尽可能广的语义空间，从而捕捉到低概率的不安全响应。
*   **关键技术细节**：
    *   **初始化**：从固定的提示词开始，复制生成 $n$ 个初始候选。
    *   **迭代扩展（Expansion）**：在每一步中，利用随机采样（如高温度采样或核采样）为每个候选生成一小段（Block）新词元。
    *   **多样性感知选择（Selection）**：这是核心步骤。使用**质量-多样性优化（Quality-Diversity Optimization）**目标函数，从候选池中筛选出既具有高生成概率（质量）又在语义上互不相似（多样性）的子集。
    *   **算法流程**：通过解决一个“Max-Avg 多样化问题”（Max-Avg Diversification Problem），利用贪心算法在嵌入空间（Embedding Space）中最大化候选集之间的平均成对距离。
*   **公式逻辑**：目标函数 $J(A) = \frac{1}{n} \sum q(s) + \lambda \cdot h(A)$，其中 $q(s)$ 是质量度量（如词元概率），$h(A)$ 是多样性度量（成对距离），$\lambda$ 平衡两者。

### 3. 实验设计
*   **目标模型**：Llama-2-7B-Chat、Llama-2-13B-Chat、Qwen2.5-7B-Instruct、Qwen2.5-14B-Instruct。
*   **数据集（Benchmarks）**：使用了四个主流安全基准：HarmBench、JailbreakBench、AdvBench 和 MaliciousInstruct。
*   **对比方法**：
    *   **IID Sampling**：标准的独立同分布随机采样。
    *   **Diverse Beam Search (DBS)**：传统的多样化束搜索算法。
    *   **IID-1024**：作为性能上限的暴力采样（生成 1024 条完整响应）。
*   **评估指标**：攻击成功率（ASR）、响应的多样性指标（如 Self-BLEU、Distinct-n、BERTScore 差异等）。

### 4. 资源与算力
*   **硬件设备**：文中提到在 **NVIDIA A100 GPU** 上进行了实验，特别是在测试 Diverse Beam Search 的显存占用（OOM）时提到了该型号。
*   **算力消耗**：论文重点分析了计算效率。PDPS 仅需 **8% 到 29%** 的 IID-1024 采样时间即可达到相近的 ASR。
*   **未明确说明的信息**：未详细列出总的 GPU 运行小时数或实验所用的 GPU 总数量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 个不同架构和规模的模型。
    *   在 4 个不同的安全数据集上各随机抽取 100 个样本进行测试。
    *   设置了 16 响应和 64 响应两种受限生成任务。
*   **充分性评估**：实验设计较为充分。除了 ASR 对比，还进行了超参数敏感性分析（如温度 $\tau$、核采样 $p$、多样性权重 $\lambda$ 的影响）以及定性的语义聚类分析。通过可视化（UMAP）展示了安全与不安全响应在语义空间的分离，增强了说服力。

### 6. 主要结论与发现
*   **长尾风险真实存在**：增加采样数量和多样性会单调地提高越狱成功率，证明安全对齐并未根除风险。
*   **PDPS 的高效性**：在有限响应（如仅生成 16 或 64 条）的情况下，PDPS 的 ASR 比 IID 和 DBS 高出 **26% - 40%**。
*   **失效模式更丰富**：PDPS 不仅能找到更多不安全响应，且这些响应在语义上比基准方法发现的更具多样性，能覆盖更广泛的失效类型。
*   **语义分离假设**：实验证实，不安全响应通常在语义嵌入空间中形成与安全拒绝响应（Refusals）不同的簇。

### 7. 优点
*   **视角独特**：从“输出空间”而非传统的“输入提示词”角度切入红队测试，是对现有安全评估手段的重要补充。
*   **计算友好**：通过渐进式剪枝，极大地降低了发现罕见失效所需的推理开销，具有很强的工程实用价值。
*   **理论支撑**：引入了具有理论近似保证的组合优化算法（Max-Avg），使多样性筛选过程更具科学性。

### 8. 不足与局限
*   **嵌入表示的依赖**：PDPS 的效果高度依赖于模型内部隐藏状态作为语义嵌入的质量。如果模型内部表示无法有效区分安全/不安全语义，效果可能打折扣。
*   **GPU 利用率波动**：作者承认 PDPS 在不同阶段的批处理大小不同，导致 GPU 利用率在某些阶段（如初始阶段）不如纯 IID 采样优化得好。
*   **应用场景限制**：目前主要针对单轮安全查询，尚未探讨在多轮对话或更复杂的交互式攻击中的表现。
*   **法官模型偏差**：安全性评估依赖于外部分类器（如 HarmBench 验证器），可能存在分类器本身的误报或漏报风险。

（完）
