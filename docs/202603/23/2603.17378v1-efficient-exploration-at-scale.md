---
title: Efficient Exploration at Scale
title_zh: 大规模高效探索
authors: "Seyed Mohammad Asghari, Chris Chute, Vikranth Dwaracherla, Xiuyuan Lu, Mehdi Jafarnia, Victor Minden, Zheng Wen, Benjamin Van Roy"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17378v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 提高大语言模型 RLHF 的数据效率
tldr: 本研究提出一种在线学习算法，旨在显著提升从人类反馈中强化学习（RLHF）的数据效率。该方法通过增量更新奖励和语言模型，引入认知神经网络来建模奖励不确定性，并结合信息导向探索（IDS）和肯定性微调信号。实验表明，在 Gemma 模型上，该算法仅需不到 2 万条标签即可匹配传统离线 RLHF 使用 20 万条标签的性能，实现了 10 倍以上的数据效率提升，且预测在大规模应用下可达 1000 倍增益。
selection_source: fresh_fetch
motivation: 针对当前 RLHF 过程中标注数据需求量巨大且效率低下的问题，探索如何通过在线学习提高数据利用率。
method: 结合认知神经网络建模奖励不确定性，并利用信息导向探索（IDS）和增量式 REINFORCE 算法进行模型更新。
result: 在 Gemma 模型上实现了超过 10 倍的数据效率提升，并预测在更大规模下可实现 1000 倍的效率增益。
conclusion: 该研究首次证明了通过高效的探索机制，大规模语言模型的 RLHF 过程可以实现极大幅度的数据效率飞跃。
---

## 摘要
我们开发了一种在线学习算法，显著提高了基于人类反馈的强化学习（RLHF）的数据效率。我们的算法在接收到选择数据时增量更新奖励模型和语言模型。奖励模型拟合选择数据，而语言模型通过 REINFORCE 算法的一种变体进行更新，强化信号由奖励模型提供。几项特性实现了效率提升：在每个强化信号中加入微小的肯定性微调（affirmative nudge）、一个建模奖励不确定性的认识神经网络（epistemic neural network），以及信息导向探索（information-directed exploration）。使用 Gemma 大语言模型（LLMs），我们的算法仅用不到 2 万个标签就达到了在 20 万个标签上训练的离线 RLHF 的性能，代表了超过 10 倍的数据效率提升。根据我们的结果推断，我们预计在 100 万个标签上训练的算法将能与在 10 亿个标签上训练的离线 RLHF 相媲美。这代表了 1000 倍的提升。据我们所知，这是首个证明如此巨大的改进是可能的结果。

## Abstract
We develop an online learning algorithm that dramatically improves the data efficiency of reinforcement learning from human feedback (RLHF). Our algorithm incrementally updates reward and language models as choice data is received. The reward model is fit to the choice data, while the language model is updated by a variation of reinforce, with reinforcement signals provided by the reward model. Several features enable the efficiency gains: a small affirmative nudge added to each reinforcement signal, an epistemic neural network that models reward uncertainty, and information-directed exploration. With Gemma large language models (LLMs), our algorithm matches the performance of offline RLHF trained on 200K labels using fewer than 20K labels, representing more than a 10x gain in data efficiency. Extrapolating from our results, we expect our algorithm trained on 1M labels to match offline RLHF trained on 1B labels. This represents a 1,000x gain. To our knowledge, these are the first results to demonstrate that such large improvements are possible.

---

## 论文详细总结（自动生成）

这篇由 Google DeepMind 团队发表的论文《Efficient Exploration at Scale》探讨了如何通过高效的探索机制显著提升大语言模型（LLM）在人类反馈强化学习（RLHF）中的数据效率。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的 RLHF 方法（如离线训练）对人类标注数据的需求量极大，且随着数据规模增加，性能提升往往遭遇瓶颈（Scaling Law 效率低下）。
*   **研究动机**：获取高质量的人类反馈成本高昂。为了实现更安全、更强大的 AI，必须找到一种方法，能够通过选择“最具有信息量”的数据来加速学习过程，即实现高效的“在线探索”。

### 2. 方法论
该研究提出了一种在线学习算法，其核心由以下三个关键技术组成：
*   **在线增量更新（Online RLHF）**：不同于传统的先收集数据再训练的离线方式，该算法在接收到每一批次选择数据时，立即增量更新奖励模型（RM）和语言模型（LM）。
*   **肯定性微调（Affirmative Nudge）**：针对在线 RLHF 中常见的“性能崩溃（Tanking）”现象（即模型在训练后期性能突然下降），在 REINFORCE 算法的强化信号中加入一个微小的正常数 $\epsilon$。这能稳定训练，无需降低学习率。
*   **信息导向探索（Information-Directed Exploration）**：
    *   **认识神经网络（ENN）**：使用集成 MLP 头部（包含先验网络和差分网络）来建模奖励模型的不确定性。
    *   **IDS 策略**：从模型生成的多个候选回复中，选择那些令奖励模型预测方差最大的回复对提交给人类（或模拟器）进行标注。这种方法能优先解决模型最“困惑”的部分。
*   **算法流程**：语言模型生成回复 -> ENN 评估不确定性 -> 选择最具信息量的回复对获取反馈 -> 增量更新 RM -> 使用带 Nudge 的变体 REINFORCE 更新 LM。

### 3. 实验设计
*   **模型基础**：使用 **Gemma 9B** 作为基础策略模型。
*   **反馈模拟器**：使用基于 **Gemini 1.5 Pro** 训练的奖励模型来模拟人类行为，以确保反馈的复杂度和真实性。
*   **数据集**：使用 20.2 万个涵盖写作、代码、摘要、数学、科学等领域的多元化提示词（Prompts）。
*   **对比基准（Benchmarks）**：
    1.  **Offline RLHF**：传统的离线训练模式。
    2.  **Periodic RLHF**：定期重新收集数据并训练的准在线模式。
    3.  **Online RLHF**：不带探索机制的普通在线模式。
    4.  **Information-Directed Exploration**：本文提出的完整算法。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长（如 TFLOPS）。
*   **实验规模**：提到了模型参数量为 9B，训练 Prompt 规模为 20 万级，批次大小（Batch Size）为 64。

### 5. 实验数量与充分性
*   **实验规模**：进行了 20 万次选择数据的训练实验，并在 1000 个测试集和 1000 个评估集上进行了验证。
*   **消融实验**：针对“奖励模型必要性”、“肯定性微调的作用”以及“不同探索策略”进行了专门的对比实验。
*   **充分性评价**：实验设计较为客观，通过对数坐标轴展示了性能随数据量变化的 Scaling Law，并进行了长程外推预测。使用 Gemini 1.5 Pro 作为模拟器增加了实验的可信度，但若能有真实人类实验则更佳。

### 6. 主要结论与发现
*   **效率飞跃**：该算法仅需 **2 万条**标签即可达到离线 RLHF 使用 **20 万条**标签的性能，实现 **10 倍**以上的数据效率提升。
*   **外推预测**：根据 Scaling Law 曲线外推，该算法在 100 万条标签下的表现预计可匹配离线 RLHF 在 **10 亿条**标签下的表现，潜在增益达 **1000 倍**。
*   **稳定性**：Affirmative Nudge 有效解决了在线强化学习中的不稳定性问题。

### 7. 优点
*   **突破性效率**：首次在 LLM 规模上证明了通过主动探索可以实现量级上的数据节省。
*   **系统性视角**：不仅关注算法改进，还系统地研究了 RLHF 的扩展定律（Scaling Laws）。
*   **实用性**：提出的 ENN 架构对原模型参数量的增加极小（小于 5%），且 Nudge 机制简单易行。

### 8. 不足与局限
*   **模拟器偏差**：实验高度依赖 Gemini 1.5 Pro 作为模拟器，虽然其能力强大，但可能无法完全替代真实人类的主观偏好和多样性。
*   **外推假设**：1000 倍的增益是基于曲线拟合的外推结果，尚未在 10 亿量级的实际标注中得到验证。
*   **场景限制**：目前主要集中在单轮对话任务，尚未深入探讨多轮对话或复杂智能体（Agent）环境下的表现。

（完）
