---
title: An Efficient Heterogeneous Co-Design for Fine-Tuning on a Single GPU
title_zh: 一种用于单 GPU 微调的高效异构协同设计
authors: "Ruijia Yang, Zeyi Wen"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16428v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 在单个GPU上对123B+大模型进行高效微调
tldr: 针对大语言模型微调中显存需求极高的问题，本文提出 SlideFormer 系统，专为单 GPU 环境设计。通过轻量级异步引擎、异构内存管理和优化的 Triton 算子，实现了计算与 I/O 的高效重叠。该系统支持在单块 RTX 4090 上微调 123B+ 模型，显著提升了吞吐量并降低了显存占用，推动了大型模型微调的普及。
selection_source: fresh_fetch
motivation: 解决大语言模型微调过程中显存消耗巨大，导致普通单 GPU 无法承载超大规模模型训练的问题。
method: 提出 SlideFormer 系统，采用滑动窗口异步引擎、异构内存管理方案以及优化的 Triton 内核来减少峰值显存并重叠计算与 I/O。
result: 在单块 RTX 4090 上成功微调 123B+ 模型，吞吐量提升 1.40x 至 6.27x，且显存占用减半。
conclusion: SlideFormer 通过软硬件协同设计，极大地扩展了单 GPU 处理超大模型的能力，为低成本微调提供了高效解决方案。
---

## 摘要
大语言模型（LLMs）的微调已成为领域自适应的关键，但其内存密集型的特性超出了大多数 GPU 的能力。为了应对这一挑战并使 LLM 微调普及化，我们提出了 SlideFormer，这是一个专为单 GPU 环境设计的新颖系统。我们的创新点包括：(1) 一个轻量级的异步引擎，将 GPU 视为滑动窗口，并将 GPU 计算与 CPU 更新及多级 I/O 重叠；(2) 一种高效的异构内存管理方案，显著降低了峰值内存占用；(3) 优化了 Triton 内核以解决关键瓶颈，并集成了先进的 I/O。这种协同设计使得在单块 RTX 4090 上微调最新的 123B+ 模型成为可能，支持高达 8 倍的批处理大小和 6 倍的模型规模。在评估中，与基准测试相比，SlideFormer 实现了 1.40 倍至 6.27 倍的吞吐量提升，同时将 CPU/GPU 内存占用减少了约一半，在 NVIDIA 和 AMD GPU 上均能保持超过 95% 的峰值性能。

## Abstract
Fine-tuning Large Language Models (LLMs) has become essential for domain adaptation, but its memory-intensive property exceeds the capabilities of most GPUs. To address this challenge and democratize LLM fine-tuning, we present SlideFormer, a novel system designed for single-GPU environments. Our innovations are: (1) A lightweight asynchronous engine that treats the GPU as a sliding window and overlaps GPU computation with CPU updates and multi-tier I/O. (2) A highly efficient heterogeneous memory management scheme significantly reduces peak memory usage. (3) Optimized Triton kernels to solve key bottlenecks and integrated advanced I/O. This collaborative design enables fine-tuning of the latest 123B+ models on a single RTX 4090, supporting up to 8x larger batch sizes and 6x larger models. In evaluations, SlideFormer achieves 1.40x to 6.27x higher throughput while roughly halving CPU/GPU memory usage compared to baselines, sustaining >95% peak performance on both NVIDIA and AMD GPUs.

---

## 论文详细总结（自动生成）

以下是对论文《An Efficient Heterogeneous Co-Design for Fine-Tuning on a Single GPU》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）全参数微调对显存（VRAM）的需求极高（如 8B 模型微调需 >128GB VRAM），远超主流消费级 GPU（如 RTX 4090 的 24GB）的承载能力。
*   **研究动机**：
    *   **显存墙瓶颈**：尽管消费级 GPU 算力充足，但显存容量增长缓慢，限制了个人开发者和小型实验室进行大模型微调。
    *   **硬件趋势**：系统内存（DDR5）和存储（NVMe）的容量与速度增长迅速，为利用异构资源（GPU+CPU+RAM+NVMe）解决显存不足提供了可能。
    *   **现有方案不足**：ZeRO-Offload 等系统多为多 GPU 设计，在单卡环境下存在同步等待、内存碎片及 CPU 更新效率低等问题。

### 2. 方法论：SlideFormer 系统设计
SlideFormer 通过“软硬件协同设计”构建了三个核心支柱：
*   **层滑动异步引擎（Layer-Sliding Architecture）**：
    *   **核心思想**：将 GPU 视为一个滑动窗口，仅保留当前计算所需的层。
    *   **异步流水线**：采用轻量级线程（而非多进程）机制，实现 GPU 计算与 CPU 参数更新、梯度卸载（d2h）及参数预取（h2d）的完全重叠。
    *   **重叠条件**：通过数学建模确保 $T_{compute} \ge T_{transfer} + T_{update}$，从而消除异构资源间的闲置时间。
*   **高效异构内存管理**：
    *   **预分配 GPU 缓存队列**：消除运行时的内存申请与释放，防止显存碎片化。
    *   **共享 CPU 缓冲区**：在 CPU 端使用层共享的固定内存（Pinned Memory）处理梯度和类型转换，使 CPU 内存占用降低约 40%。
    *   **滑动激活值检查点**：将激活值异步卸载至 CPU 或 NVMe，仅在反向传播前预取。
*   **集成 I/O 与计算优化**：
    *   **GPUDirect Storage (GDS)**：率先在微调中集成 GDS 技术，使数据在 NVMe 和 GPU 间直接传输，绕过 CPU 缓冲，降低 CPU 负载。
    *   **融合 Triton 内核**：开发了融合线性交叉熵（Fused LCE）内核，解决了输出层 Logits 占用巨大显存的隐形瓶颈（减少 >80% 输出层显存）。

### 3. 实验设计
*   **实验场景**：单 GPU 环境下的全参数微调。
*   **数据集**：使用合成数据集以确保计算负载稳定和有效长度一致。
*   **对比基准（Benchmarks）**：
    *   **主流框架**：ZeRO-Offload, ZeRO-Infinity, ColossalAI (Gemini)。
    *   **学术原型**：LoHan (针对单卡优化的最新工作)。
*   **测试模型**：Llama-3.1 (8B), Qwen-2.5 (3B-72B), Mistral (24B-123B)。

### 4. 资源与算力
*   **硬件平台**：
    *   **消费级 PC**：NVIDIA RTX 4090 (24GB) 或 AMD RX 7900XT (20GB)，配 256GB DDR5 内存。
    *   **服务器**：NVIDIA A100 (80GB)，配 1024GB DDR4 内存。
*   **软件环境**：PyTorch 2.7.0, CUDA 12.5, Triton。
*   **训练时长**：文中未明确给出总训练时长，但通过吞吐量（Tokens/s）和 TFLOPS 衡量了瞬时性能。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3B 到 123B 不同参数规模的模型，并在 NVIDIA 和 AMD 两种架构的 GPU 上进行了验证。
*   **维度丰富性**：
    *   **吞吐量扩展性**：测试了不同 Batch Size 下的性能。
    *   **内存效率**：对比了 GPU 和 CPU 的峰值内存占用。
    *   **消融/分析实验**：详细分析了 NVMe 硬盘数量对性能的影响，以及不同卸载策略（激活值 vs 优化器状态）的权衡。
*   **客观性**：对比实验均在相同硬件和最新版本框架下进行，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **突破极限**：SlideFormer 实现了在单块 RTX 4090 上微调 **123B+** 规模的模型，而基准框架（如 ZeRO-Offload）在 14B 规模时即告失败。
*   **性能卓越**：吞吐量比现有基准提升 **1.40x 至 6.27x**，且在卸载模式下仍能保持 GPU 峰值性能的 **95%** 以上。
*   **资源节省**：GPU 显存占用减少 50% 以上，CPU 内存占用减少约 40%。
*   **平台通用性**：在 AMD GPU 上同样表现优异，证明了其基于 Triton 的内核优化具有良好的跨平台能力。

### 7. 优点与亮点
*   **协同设计深度高**：不仅是简单的内存卸载，而是从算子融合（LCE）、底层 I/O（GDS）到上层调度（层滑动）的全面优化。
*   **实用性极强**：直接支持 Llama-3.1 和 Qwen-2.5 等最新主流模型，解决了实际科研中“买不起集群”的痛点。
*   **解决了隐形瓶颈**：敏锐捕捉到大词表模型中 CrossEntropyLoss 的显存占用问题，并通过融合算子予以解决。

### 8. 不足与局限
*   **NVMe 依赖**：微调超大规模模型（如 123B）时高度依赖 NVMe 阵列的带宽，若硬盘速度慢，性能会显著下降（下降 30-50%）。
*   **CPU 内存门槛**：虽然降低了显存要求，但仍需要较大的 CPU 内存（如微调 72B 模型建议配 256GB RAM），对极低配电脑仍有挑战。
*   **单卡局限**：该设计专注于单 GPU 场景，未讨论如何扩展到多卡分布式环境下的异构协同。

（完）
