---
title: "FedPDPO: Federated Personalized Direct Preference Optimization for Large Language Model Alignment"
title_zh: FedPDPO：面向大语言模型对齐的联邦个性化直接偏好优化
authors: "Kewen Zhu, Liping Yi, Zhiming Zhao, Zhuang Qi, Han Yu, Qinghua Hu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19741v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 用于大模型对齐的联邦直接偏好优化
tldr: 针对联邦学习中非独立同分布（non-IID）偏好数据导致的大模型对齐性能下降问题，本文提出FedPDPO框架。该框架采用参数高效的LoRA架构，通过全局共享LoRA与个性化客户端头部、引入显式奖励头以及瓶颈适配器，有效解决了数据异构性并增强了隐式奖励的泛化能力。实验证明其在多项任务中达到SOTA水平，显著提升了对齐准确率。
selection_source: fresh_fetch
motivation: 传统的直接偏好优化（DPO）在联邦学习环境下难以应对高度非独立同分布的偏好数据，导致模型对齐效果和泛化能力严重下降。
method: 提出一种结合全局共享LoRA、个性化客户端头部、显式奖励头及瓶颈适配器的个性化联邦学习框架。
result: "在多个偏好数据集上的实验显示，该方法在域内和跨域联邦设置下均取得领先性能，平均准确率提升高达4.80%。"
conclusion: FedPDPO通过个性化策略有效缓解了联邦对齐中的异构性挑战，为隐私保护下的大模型偏好对齐提供了稳健的理论与实践方案。
---

## 摘要
在联邦学习（FL）中将大语言模型（LLMs）与人类偏好对齐面临挑战，原因在于偏好数据具有去中心化、隐私敏感且高度非独立同分布（non-IID）的特性。直接偏好优化（DPO）为人类反馈强化学习（RLHF）提供了一种高效的替代方案，但其在联邦学习中的直接应用在非独立同分布数据下会遭受严重的性能退化，且隐式奖励的泛化能力有限。为了弥补这一差距，我们提出了 FedPDPO（联邦个性化直接偏好优化），这是一个用于大语言模型偏好对齐的个性化联邦框架。该框架采用参数高效微调架构，每个客户端维护一个冻结的预训练大语言模型主干，并辅以低秩自适应（LoRA）适配器，从而实现通信高效的聚合。为了解决非独立同分布的异构性，我们设计了：（1）全局共享的 LoRA 适配器与个性化的客户端特定大语言模型头部；（2）一种带有客户端特定显式奖励头部的个性化 DPO 训练策略，以补充隐式奖励并进一步缓解非独立同分布异构性；以及（3）一个瓶颈适配器（bottleneck adapter）来平衡全局和局部特征。我们提供了理论分析，建立了其概率基础和合理性。在多个偏好数据集上的广泛实验证明了该方法具有最先进的性能，在联邦域内和跨域设置中实现了高达 4.80% 的平均准确率提升。

## Abstract
Aligning large language models (LLMs) with human preferences in federated learning (FL) is challenging due to decentralized, privacy-sensitive, and highly non-IID preference data. Direct Preference Optimization (DPO) offers an efficient alternative to reinforcement learning with human feedback (RLHF), but its direct application in FL suffers from severe performance degradation under non-IID data and limited generalization of implicit rewards. To bridge this gap, we propose FedPDPO (Federated Personalized Direct Preference Optimization), a personalized federated framework for preference alignment of LLMs. It adopts a parameter-efficient fine-tuning architecture where each client maintains a frozen pretrained LLM backbone augmented with a Low-Rank Adaptation (LoRA) adapter, enabling communication-efficient aggregation. To address non-IID heterogeneity, we devise (1) the globally shared LoRA adapter with the personalized client-specific LLM head. Moreover, we introduce (2) a personalized DPO training strategy with a client-specific explicit reward head to complement implicit rewards and further alleviate non-IID heterogeneity, and (3) a bottleneck adapter to balance global and local features. We provide theoretical analysis establishing the probabilistic foundation and soundness. Extensive experiments on multiple preference datasets demonstrate state-of-the-art performance, achieving up to 4.80% average accuracy improvements in federated intra-domain and cross-domain settings.

---

## 论文详细总结（自动生成）

以下是对论文《FedPDPO: Federated Personalized Direct Preference Optimization for Large Language Model Alignment》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLMs）的对齐通常依赖于人类偏好数据。然而，在现实场景中，这些数据往往分散在不同用户手中，具有高度的隐私敏感性。
*   **核心挑战**：
    1.  **非独立同分布（Non-IID）问题**：不同用户的偏好存在显著差异，传统的联邦学习（FL）直接应用直接偏好优化（DPO）会导致严重的性能退化。
    2.  **隐式奖励的局限性**：DPO虽然避开了强化学习中的奖励模型训练，但在异构数据下，其隐式奖励机制的泛化能力较弱，难以捕捉复杂的个性化偏好。
*   **研究目的**：提出一种个性化的联邦学习框架，在保护隐私的同时，实现高效且稳健的大模型偏好对齐。

### 2. 方法论
FedPDPO 结合了参数高效微调（PEFT）与个性化联邦学习策略，核心组件包括：
*   **架构设计**：采用 **LoRA（低秩自适应）** 适配器。每个客户端维护一个冻结的预训练模型主干，仅训练和上传 LoRA 参数，以降低通信开销。
*   **个性化策略**：
    *   **全局共享 LoRA + 个性化头部**：LoRA 层在全局范围内聚合以学习通用知识，而 LLM 的输出头部（Head）保持在客户端本地，用于捕捉特定用户的表达风格和偏好。
    *   **显式奖励头（Explicit Reward Head, ERH）**：在 DPO 的隐式奖励基础上，引入一个轻量级的 MLP 作为显式奖励头。它通过二元交叉熵损失进行训练，辅助模型更清晰地分辨“选中”与“拒绝”的回答。
    *   **瓶颈适配器（Bottleneck Adapter）**：在模型中嵌入瓶颈结构，用于平衡全局聚合的特征与局部提取的特征，增强模型对异构数据的适应力。
*   **算法流程**：客户端在本地利用 DPO 损失和显式奖励损失共同优化模型，随后将 LoRA 参数上传至服务器进行聚合，而个性化头部和奖励头保留在本地。

### 3. 实验设计
*   **数据集**：使用了多个主流偏好数据集，包括 **HH-RLHF**（Anthropic）和 **UltraFeedback** 等。
*   **实验场景**：
    *   **域内（Intra-domain）**：客户端数据来自同一分布的不同子集。
    *   **跨域（Cross-domain）**：客户端持有完全不同来源或类型的偏好数据，模拟极端的 Non-IID 情况。
*   **基准方法（Benchmarks）**：对比了 **FedAvg**（标准联邦）、**FedProx**（处理异构性）、**FedLoRA** 以及直接在联邦环境下运行的 **DPO**。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中未明确列出具体的 GPU 型号（如 A100 数量）或总训练时长。
*   **效率优化**：论文强调了使用 **LoRA** 技术，这表明该方法旨在中等算力条件下实现大模型的联邦微调，显著降低了对显存和带宽的需求。

### 5. 实验数量与充分性
*   **实验规模**：进行了广泛的实验，涵盖了多种数据集和 Non-IID 设定。
*   **消融实验**：对显式奖励头、个性化头部以及瓶颈适配器分别进行了消融研究，验证了各组件对提升对齐准确率的贡献。
*   **客观性**：通过平均准确率（Accuracy）提升（最高达 4.80%）这一量化指标，证明了其相对于现有 SOTA 方法的优势，实验设计较为全面且符合联邦学习的研究规范。

### 6. 主要结论与发现
*   **性能领先**：FedPDPO 在处理非独立同分布偏好数据时，性能显著优于传统的联邦对齐方法。
*   **个性化的必要性**：实验证明，保留本地个性化头部比全模型聚合更能有效应对用户间的偏好冲突。
*   **显式奖励的补充作用**：引入显式奖励头能有效缓解 DPO 在小样本或高异构数据下的训练不稳定问题，增强了模型的判别能力。

### 7. 优点
*   **理论支撑**：为个性化 DPO 提供了概率论基础和数学证明，增强了方法的说服力。
*   **架构优雅**：巧妙结合了 LoRA 的高效性和个性化头部的灵活性，解决了联邦学习中的“客户端漂移”问题。
*   **实用性强**：针对隐私敏感的偏好对齐场景，提供了一个可落地的技术框架。

### 8. 不足与局限
*   **存储开销**：虽然通信高效，但每个客户端需要维护本地的个性化头部和奖励头，对于存储空间极度受限的移动端设备可能存在挑战。
*   **超参数敏感性**：引入了多个组件（ERH、瓶颈适配器），可能需要更精细的超参数调优（如损失函数的权重平衡）。
*   **长文本表现**：实验主要集中在偏好选择的准确率上，对于生成长文本时的连贯性及多样性是否受个性化头部影响，仍需进一步探讨。

（完）
