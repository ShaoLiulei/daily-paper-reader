---
title: "SNCE: Geometry-Aware Supervision for Scalable Discrete Image Generation"
title_zh: SNCE：面向可扩展离散图像生成的几何感知监督
authors: "Shufan Li, Jiuxiang Gu, Kangning Liu, Zhe Lin, Aditya Grover, Jason Kuen"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15150v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 离散图像生成器的新型训练目标
tldr: 针对离散图像生成中扩大VQ码本导致训练困难的问题，本文提出SNCE训练目标。该方法摒弃了传统的硬独热编码监督，转而根据代码嵌入与真实图像嵌入的邻近度，在邻近Token集上构建软分类分布。这种几何感知监督促使模型捕捉量化嵌入空间中的语义结构。实验证明，SNCE在ImageNet生成和大规模文生图任务中显著加快了收敛速度并提升了生成质量。
selection_source: fresh_fetch
motivation: 扩大VQ码本规模虽能提升图像重建保真度，但会显著增加生成模型的优化难度和训练时长。
method: 提出随机邻域交叉熵（SNCE），利用嵌入空间中的几何邻近度构建软目标分布来监督模型训练。
result: 实验表明SNCE在ImageNet-256生成、大规模文生图及图像编辑任务中均实现了更快的收敛和更高的生成质量。
conclusion: SNCE通过引入几何感知监督，为大规模码本离散图像生成提供了一种高效且可扩展的优化方案。
---

## 摘要
离散图像生成的最新进展表明，扩大 VQ 码本规模能显著提高重建保真度。然而，使用大规模 VQ 码本训练生成模型仍然具有挑战性，通常需要更大的模型规模和更长的训练周期。在这项工作中，我们提出了随机邻域交叉熵最小化（SNCE），这是一种旨在解决大码本离散图像生成器优化挑战的新型训练目标。SNCE 并非使用硬 one-hot 目标来监督模型，而是在一组相邻 token 上构建软分类分布。分配给每个 token 的概率与其代码嵌入与真实图像嵌入之间的接近度成正比，从而鼓励模型在量化嵌入空间中捕捉具有语义意义的几何结构。我们在类别条件 ImageNet-256 生成、大规模文本到图像合成以及图像编辑任务上进行了广泛的实验。结果表明，与标准交叉熵目标相比，SNCE 显著提高了收敛速度和整体生成质量。

## Abstract
Recent advancements in discrete image generation showed that scaling the VQ codebook size significantly improves reconstruction fidelity. However, training generative models with a large VQ codebook remains challenging, typically requiring larger model size and a longer training schedule. In this work, we propose Stochastic Neighbor Cross Entropy Minimization (SNCE), a novel training objective designed to address the optimization challenges of large-codebook discrete image generators. Instead of supervising the model with a hard one-hot target, SNCE constructs a soft categorical distribution over a set of neighboring tokens. The probability assigned to each token is proportional to the proximity between its code embedding and the ground-truth image embedding, encouraging the model to capture semantically meaningful geometric structure in the quantized embedding space. We conduct extensive experiments across class-conditional ImageNet-256 generation, large-scale text-to-image synthesis, and image editing tasks. Results show that SNCE significantly improves convergence speed and overall generation quality compared to standard cross-entropy objectives.

---

## 论文详细总结（自动生成）

以下是对论文《SNCE: Geometry-Aware Supervision for Scalable Discrete Image Generation》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在离散图像生成（如自回归模型或离散扩散模型）中，扩大向量量化（VQ）码本（Codebook）规模能显著提升图像重建的保真度。
*   **核心问题**：传统的交叉熵（CE）损失使用“独热编码（One-hot）”作为监督目标，这在超大码本（如 10 万级以上 token）下会导致**码本稀疏性问题**。由于每个 token 出现的频率大幅降低，且 CE 损失无法区分“非正确但相近”的 token 与“完全错误”的 token，导致模型优化极其困难，收敛缓慢且需要海量数据。
*   **整体含义**：本文旨在引入一种“几何感知”的监督机制，利用 VQ 嵌入空间的距离信息来指导模型学习，从而解决大码本带来的优化瓶颈。

### 2. 方法论
*   **核心思想**：将硬性的 One-hot 目标替换为基于几何距离的**软分类分布**。即：如果一个 token 在嵌入空间中靠近真实值（Ground-truth），即使它不是最精确的那个，也应获得一定的正向监督信号。
*   **关键技术细节**：
    *   **随机邻域分布构建**：计算连续潜变量 $z_i$ 与码本中所有向量 $v_k$ 的距离 $d$。利用类似 Softmax 的公式生成概率分布 $q_k(z_i)$，其中概率与距离的负指数成正比。
    *   **SNCE 损失函数**：将生成的软分布 $q$ 作为目标，计算其与模型预测对数概率的交叉熵。
    *   **温度参数 $\tau$**：引入温度系数来控制分布的平滑程度。
*   **多维视角解读**：
    *   **分类 VAE 视角**：等同于在量化过程中引入随机采样，但方差更低、更稳定。
    *   **知识蒸馏视角**：将离散分词器（Tokenizer）视为一个带有连续性先验的“弱教师”，引导生成模型学习。
    *   **强化学习视角**：将 token 预测视为决策，SNCE 提供了比二元奖励更丰富的连续奖励信号。

### 3. 实验设计
*   **数据集与场景**：
    *   **ImageNet-256**：类别条件图像生成，验证小规模收敛性。
    *   **大规模文生图（T2I）**：使用 5000 万张图像（LAION, COYO 等）训练 10B 规模的模型。
    *   **图像编辑**：使用 200 万个编辑样本验证模型的指令遵循能力。
*   **Benchmark 与指标**：
    *   **保真度**：FID、HPSv3（人类偏好得分）。
    *   **对齐性**：GenEval（物体对齐）、DPG（细粒度对齐）。
    *   **编辑质量**：ImgEdit（使用 GPT-4o 作为评判官）。
*   **对比方法**：标准交叉熵（CE）、标签平滑（Label Smoothing）、以及 SDXL、SD3、Flux 等主流连续扩散模型。

### 4. 资源与算力
*   **模型规模**：主要实验基于 10B 参数的离散扩散模型（LaViDa-O 架构）。
*   **训练细节**：文生图任务训练了 200k 步，全局 Batch Size 为 1024。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号（如 H100/A100）和总算力消耗时长，但提到在 ImageNet 上训练了 100-300 个 Epoch，在大规模任务上进行了微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 342M 到 10B 不同参数规模的模型，码本大小从 1.6 万扩展到 26 万。
*   **消融实验**：详细探讨了温度参数 $\tau$ 的影响，并对比了 SNCE 与传统标签平滑（Label Smoothing）的效果。
*   **充分性评价**：实验设计较为全面，跨越了自回归（AR）和离散扩散（Discrete Diffusion）两种主流架构，证明了 SNCE 作为“即插即用”替代方案的普适性。对比基准涵盖了当前最先进的开源模型，结果具有较高的说服力。

### 6. 主要结论与发现
*   **收敛加速**：在相同训练时间内，SNCE 显著优于 CE，尤其是在处理超大码本时。
*   **质量提升**：在 ImageNet 上，SNCE 将 FID 从 5.44 降低至 3.42；在大规模文生图任务中，FID 降低了 3.67，HPSv3 显著提升。
*   **几何重要性**：实验证明，尊重嵌入空间的几何结构（即距离信息）对于离散生成模型的扩展至关重要。

### 7. 优点
*   **简单高效**：作为损失函数的改进，无需修改模型架构，计算开销极小。
*   **理论完备**：从 VAE、蒸馏和强化学习三个维度给出了严谨的数学解释。
*   **解决痛点**：直接针对离散模型在大码本下的“优化难”痛点，为构建下一代多模态统一模型铺平了道路。

### 8. 不足与局限
*   **超参数依赖**：温度参数 $\tau$ 对结果影响较大，不同码本可能需要重新调优。
*   **伪影问题**：尽管质量提升明显，但在极高分辨率下仍可能存在微小的人工痕迹（Artifacts）。
*   **基础模型偏差**：实验多基于 LaViDa-O 预训练模型，其固有的幻觉或社会偏见风险依然存在。
*   **算力细节缺失**：缺乏具体的算力资源消耗报告，不便于其他研究者评估复现成本。

（完）
