---
title: Spiking Layer-Adaptive Magnitude-based Pruning
title_zh: 脉冲层自适应幅值剪枝
authors: "Junqiao Wang, Zhehang Ye, Yuqi Ouyang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14946v1"
tags: ["query:snn"]
score: 10.0
evidence: 脉冲神经网络的理论指导剪枝方法
tldr: 脉冲神经网络（SNN）虽节能但受限于高连接密度。本文提出SLAMP，一种理论指导的层自适应剪枝框架，通过控制跨层和跨时间步的输出失真，解决了传统剪枝在SNN中忽略时间累积和膜电位稳定性的问题。该方法通过两阶段流程优化稀疏度分配，在保持精度的同时显著减少了连接数和脉冲操作，提升了SNN的部署效率。
selection_source: fresh_fetch
motivation: 传统剪枝方法在SNN中应用时，未考虑时间维度上的累积效应和非均匀贡献，导致性能严重下降。
method: 提出SLAMP框架，将稀疏度分配建模为受时间失真约束的优化问题，并推导出时间感知的层重要性评分。
result: 在CIFAR10/100及DVS数据集上，该方法在大幅减少连接和脉冲操作的同时，有效保持了模型精度。
conclusion: SLAMP为SNN提供了一种高效且具有理论支撑的剪枝方案，显著增强了其在资源受限设备上的可部署性。
---

## 摘要
脉冲神经网络 (SNNs) 提供了高能效的计算，但其部署受到密集连接和高昂脉冲操作成本的限制。现有的基于幅值的剪枝策略在直接应用于 SNNs 时，未能考虑到时间累积、非均匀时间步贡献以及膜稳定性，往往导致严重的性能下降。本文提出了脉冲层自适应幅值剪枝 (SLAMP)，这是一种理论指导的剪枝框架，通过显式控制跨层和跨时间步的最坏情况输出失真，将层自适应幅值剪枝推广到时域 SNNs。SLAMP 将稀疏度分配建模为一个受时间失真约束的优化问题，从而产生时间感知的层重要性评分，这些评分在单时间步极限下可退化为传统的层自适应剪枝。推导出了一个高效的两阶段过程，结合了时间评分估计、全局稀疏度分配以及带有重训练以恢复稳定性的幅值剪枝。在 CIFAR10、CIFAR100 和基于事件的 CIFAR10-DVS 数据集上的实验表明，SLAMP 在保持准确性的同时，实现了连接和脉冲操作的大幅减少，从而实现了高效且可部署的 SNN 推理。

## Abstract
Spiking Neural Networks (SNNs) provide energy-efficient computation but their deployment is constrained by dense connectivity and high spiking operation costs. Existing magnitude-based pruning strategies, when naively applied to SNNs, fail to account for temporal accumulation, non-uniform timestep contributions, and membrane stability, often leading to severe performance degradation. This paper proposes Spiking Layer-Adaptive Magnitude-based Pruning (SLAMP), a theory-guided pruning framework that generalizes layer-adaptive magnitude pruning to temporal SNNs by explicitly controlling worst-case output distortion across layers and timesteps. SLAMP formulates sparsity allocation as a temporal distortion-constrained optimization problem, yielding time-aware layer importance scores that reduce to conventional layer-adaptive pruning in single-timestep limit. An efficient two-stage procedure is derived, combining temporal score estimation, global sparsity allocation, and magnitude pruning with retraining for stability recovery. Experiments on CIFAR10, CIFAR100, and the event-based CIFAR10-DVS datasets demonstrate that SLAMP achieves substantial connectivity and spiking operation reductions while preserving accuracy, enabling efficient and deployable SNN inference.

---

## 论文详细总结（自动生成）

### 脉冲层自适应幅值剪枝 (SLAMP) 论文总结

#### 1. 核心问题与研究动机
*   **研究背景**：脉冲神经网络 (SNNs) 因其模拟生物大脑的事件驱动特性，在能效方面具有天然优势。然而，现代深层 SNN 往往存在严重的参数冗余，导致极高的脉冲操作 (SOPs) 成本，限制了其在资源受限设备上的部署。
*   **核心问题**：传统的基于幅值的剪枝 (MP) 或层自适应剪枝 (LAMP) 主要针对静态人工神经网络 (ANN)。直接将其应用于 SNN 时，由于忽略了**时间累积效应**、**不同时间步的非均匀贡献**以及**膜电位稳定性**，往往会导致严重的性能下降和时间保真度丢失。

#### 2. 方法论
*   **核心思想**：提出一种理论引导的框架 SLAMP，通过显式控制跨层和跨时间步的最坏情况输出失真，将层自适应剪枝推广到时域 SNN。
*   **关键技术细节**：
    *   **时间重要性评分 ($R_i$)**：不同于静态网络，SLAMP 定义了一个聚合突触在所有时间步内平方贡献的评分函数。它为频繁激活的预突触神经元相关的权重分配更高的重要性。
    *   **时间失真约束优化**：将稀疏度分配建模为一个优化问题，旨在全局稀疏度预算下，最小化由剪枝引起的输出扰动上限。
    *   **理论泛化**：证明了该方法在单时间步 ($T=1$) 极限下可退化为传统的 LAMP 算法。
*   **算法流程**：
    1.  **预训练/微调**：在稳定基准上训练 SNN，获取可靠的时间活动统计数据。
    2.  **迭代剪枝**：根据计算出的时间重要性评分，识别并移除不重要的连接。
    3.  **稳定性恢复**：通过重训练（Fine-tuning）恢复膜电位动态平衡，补偿剪枝带来的精度损失。

#### 3. 实验设计
*   **数据集**：
    *   静态图像分类：**CIFAR10**、**CIFAR100**。
    *   神经形态/事件驱动数据：**CIFAR10-DVS**（通过动态视觉传感器转换的事件流）。
*   **Benchmark 与对比方法**：
    *   对比了多种 SNN 剪枝基准，包括 **ADMM**、**Grad R**、**ESLSNN**、**STDS** 和 **UPR**。
*   **模型架构**：采用 Spiking-ResNet19（用于 CIFAR）和 Spiking VGGNet（用于 DVS 数据集）。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号、数量或总训练/剪枝耗时。
*   **训练配置**：提到静态数据集时间步 $T=2$，DVS 数据集 $T=5$；初始训练 300 epoch，每轮迭代剪枝 15% 的连接，随后进行 15 epoch 的微调。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从静态到动态的三大主流数据集，进行了多轮迭代剪枝实验。
*   **消融实验**：专门分析了“有无微调”对准确率和膜电位方差的影响，验证了稳定性恢复的必要性。
*   **超参数分析**：对剪枝频率 ($f$) 和学习率 ($\eta_0$) 进行了鲁棒性测试。
*   **评价**：实验设计较为全面，对比了当前主流的 SNN 压缩技术，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **高稀疏度下的高性能**：SLAMP 在 CIFAR10 上保留 40% 连接时，准确率反而提升了 1.23%；在 CIFAR100 上也实现了正向的准确率增益。
*   **能效提升**：显著减少了连接数和 SOPs（脉冲操作数），例如在 CIFAR10 上将 SOPs 降低至 112.35M。
*   **时间保真度**：通过控制失真，SLAMP 能够比传统方法更好地保持 SNN 的时域表达能力，降低膜电位波动的方差。

#### 7. 优点
*   **理论支撑强**：并非纯启发式方法，而是基于输出失真界限推导出的重要性评分。
*   **自适应性**：能够自动在各层间分配稀疏度，无需手动调整每层的剪枝比例。
*   **多场景适用**：在处理具有复杂时间动态的事件驱动数据（DVS）时表现稳健。

#### 8. 不足与局限
*   **硬件细节缺失**：缺乏具体的算力消耗和推理延迟（Latency）的实测数据。
*   **极端稀疏度挑战**：虽然在 40% 密度下表现优异，但在极低密度（如 <10%）时，准确率仍会出现不可避免的骤降。
*   **模型多样性**：实验主要集中在 ResNet 和 VGG 结构，尚未在更先进的 SNN 架构（如 Spiking Transformer）上进行验证。

（完）
