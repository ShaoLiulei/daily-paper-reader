---
title: "ViT-AdaLA: Adapting Vision Transformers with Linear Attention"
title_zh: ViT-AdaLA：利用线性注意力适配视觉 Transformer
authors: "Yifan Li, Seunghyun Yoon, Viet Dac Lai, Franck Dernoncourt, Jason Kuen, Yu Kong, Trung Bui"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16063v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 利用线性注意力和有监督微调适配视觉 Transformer
tldr: 针对视觉Transformer（ViT）二次复杂度带来的扩展性挑战，本文提出ViT-AdaLA框架，旨在将预训练视觉基础模型（VFM）的知识高效迁移至线性注意力架构。该框架通过注意力对齐、特征对齐和监督微调三个阶段，解决了线性化过程中误差累积和训练成本高的问题。实验表明，该方法在分类和分割任务中表现优异，为构建高效视觉模型提供了新路径。
selection_source: fresh_fetch
motivation: 旨在解决ViT二次复杂度导致的计算瓶颈，并克服现有线性注意力方法在知识迁移和训练效率上的不足。
method: 采用三阶段策略，通过对齐线性与Softmax注意力、对齐最终层特征以及下游任务微调，实现知识的有效传递。
result: 在多项视觉任务中，ViT-AdaLA 的性能和泛化能力均显著超过了现有的最先进线性注意力模型。
conclusion: 该研究证明了通过多阶段对齐策略，可以成功将预训练大模型的先验知识适配到低复杂度的线性注意力ViT中。
---

## 摘要
基于视觉 Transformer (ViT) 的视觉基础模型 (VFM) 在各种视觉任务中取得了卓越的性能，但受限于二次复杂度，这限制了其对长序列的可扩展性。现有的 ViT 线性注意力方法通常需要从头开始训练，消耗大量计算资源，而为大语言模型解码器开发的线性化方法在迁移到 ViT 时表现不佳。为了应对这些挑战，我们提出了 ViT-AdaLA，这是一个旨在将先验知识从 VFM 有效适配并迁移到线性注意力 ViT 的新颖框架。ViT-AdaLA 包含三个阶段：注意力对齐、特征对齐和有监督微调。在注意力对齐阶段，我们将每个模块中的原始线性注意力与原始的基于 softmax 的注意力进行对齐，以近似 softmax 注意力的行为。然而，残差近似误差不可避免地会在层间累积。我们通过微调线性化后的 ViT，使其顶层特征与冻结的 softmax VFM 教师模型对齐，从而缓解这一问题。最后，通过有监督微调将适配后的先验知识迁移到下游任务。在分类和分割任务上的广泛实验证明了 ViT-AdaLA 相比于各种最先进的线性注意力对应方案的有效性和通用性。

## Abstract
Vision Transformers (ViTs) based vision foundation models (VFMs) have achieved remarkable performance across diverse vision tasks, but suffer from quadratic complexity that limits scalability to long sequences. Existing linear attention approaches for ViTs are typically trained from scratch, requiring substantial computational resources, while linearization-based methods developed for large language model decoders do not transfer well to ViTs. To address these challenges, we propose ViT-AdaLA, a novel framework for effectively adapting and transferring prior knowledge from VFMs to linear attention ViTs. ViT-AdaLA consists of three stages: attention alignment, feature alignment, and supervised fine-tuning. In the attention alignment stage, we align vanilla linear attention with the original softmax-based attention in each block to approximate the behavior of softmax attention. However, residual approximation errors inevitably accumulate across layers. We mitigate this by fine-tuning the linearized ViT to align its final-layer features with a frozen softmax VFM teacher. Finally, the adapted prior knowledge is transferred to downstream tasks through supervised fine-tuning. Extensive experiments on classification and segmentation tasks demonstrate the effectiveness and generality of ViT-AdaLA over various state-of-the-art linear attention counterpart.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ViT-AdaLA** 的新框架，旨在通过线性注意力机制（Linear Attention）对预训练的视觉 Transformer（ViT）进行高效适配，从而解决标准 ViT 在处理长序列（高分辨率图像）时的二次复杂度瓶颈。

以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：视觉基础模型（VFM）如 DINOv2 和 CLIP 表现卓越，但其核心的 Softmax 注意力机制计算复杂度随 Token 数量（序列长度）呈二次方增长（$O(N^2)$），限制了其在超高分辨率图像或长视频任务中的应用。
*   **现有挑战**：
    *   **从头训练成本高**：现有的线性注意力模型通常需要大规模预训练才能获得强大的先验知识。
    *   **迁移困难**：现有的线性化方法（如 LoLCATS）多针对大语言模型（LLM）的解码器设计，直接应用于 ViT 编码器时，由于误差在空间和层级上的累积，会导致性能大幅下降。
*   **核心目标**：开发一种能够继承现有预训练 VFM 强大先验知识，同时将其复杂度降低为线性的适配框架。

### 2. 方法论：ViT-AdaLA 框架
ViT-AdaLA 采用三阶段对齐策略，将 Softmax 注意力的知识逐步迁移到线性注意力模型中：
*   **阶段 1：注意力对齐（Attention Alignment）**
    *   **核心思想**：在冻结原始模型的情况下，为每个 Transformer 块引入线性注意力模块。
    *   **技术细节**：使用核函数技巧（Kernel Trick，默认采用 $\text{ELU}(x)+1$）重组计算顺序。通过 MSE 损失函数，使线性注意力的输出尽可能接近原始 Softmax 注意力的输出。此阶段仅微调查询（Q）、键（K）、值（V）的投影矩阵。
*   **阶段 2：特征对齐（Feature Alignment）**
    *   **核心思想**：解决层间残差误差累积问题。
    *   **技术细节**：用阶段 1 训练好的线性注意力替换所有 Softmax 模块，然后微调整个线性化 ViT，使其最终层的输出特征与冻结的 Softmax 教师模型特征对齐。
*   **阶段 3：有监督微调（Supervised Fine-tuning）**
    *   **核心思想**：将适配后的模型迁移至下游任务。
    *   **技术细节**：添加任务特定的头部（如分类头或 Mask2Former 分割头），在目标数据集上进行全模型微调。

### 3. 实验设计
*   **数据集**：
    *   **预训练/对齐**：COCO（阶段 1）、ImageNet-22K（阶段 2）。
    *   **下游任务**：ImageNet-1K（分类）、ADE20K（语义分割）、Cityscapes（高分辨率分割）。
*   **视觉基础模型（VFM）**：DINOv2-L、CLIP-L、SigLIP-L、IN1K-pretrained ViT-L。
*   **对比基准（Benchmarks）**：对比了 7 种主流线性注意力方法，包括 Hedgehog、LoLCATS、Linformer、Performer、Cosformer、Nystromformer 和 Monarch Attention。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练时长**：
    *   **阶段 1**：在 COCO 上训练 4 个 Epoch，每 Epoch 约 4-6 分钟。
    *   **阶段 2**：在 ImageNet-22K 上训练 10-30 个 Epoch，每 Epoch 约 **11 小时**。
    *   **推理效率**：在 1024 分辨率下，相比 Softmax 模型，显存占用降低约 58%，吞吐量提升约 125%。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从分类到密集预测（分割）的多种任务，并在 4 种不同的 VFM 架构上验证了通用性。
*   **消融实验**：详细探讨了阶段 1 和阶段 2 的必要性、不同激活函数（ReLU, Softplus, ELU+1）的影响、以及分辨率的可扩展性。
*   **客观性**：作者重新实现了所有对比基准，并在相同的硬件和超参数条件下进行测试，确保了对比的公平性。实验组数较多，数据支撑充分。

### 6. 主要结论与发现
*   **性能保持**：ViT-AdaLA 在分类任务中能保持原始 Softmax 模型 99% 以上的精度，在分割任务中也显著优于其他线性化方法。
*   **特征对齐的关键性**：对于视觉任务，仅做注意力对齐是不够的，最终层特征对齐（阶段 2）是继承 VFM 先验知识的核心。
*   **分辨率扩展性**：即使在 512 分辨率下训练，模型也能直接扩展到 1024 或 1536 分辨率，且性能随分辨率提升而增长，证明了线性注意力的优势。
*   **收敛加速**：阶段 1 的初始化能显著加快阶段 2 的收敛速度。

### 7. 优点与亮点
*   **架构无关性**：该框架不依赖特定的线性注意力设计，具有很强的通用性。
*   **高效继承**：无需从头预训练，通过相对较短的对齐过程即可获得大模型的特征提取能力。
*   **高分辨率友好**：解决了 ViT 处理大尺寸图像时的显存溢出问题，为高分辨率视觉应用提供了可行方案。

### 8. 不足与局限
*   **分割任务仍有差距**：尽管优于其他线性方法，但在 ADE20K 等复杂分割任务上，与原始 Softmax 模型相比仍有约 1-2% 的 mIoU 差距。
*   **低分辨率下的开销**：在线性注意力中，当序列长度 $N$ 小于特征维度 $D$ 时（如 $256 \times 256$ 图像），其计算效率反而可能低于高度优化的 Softmax 注意力（如 FlashAttention）。
*   **阶段 2 成本**：虽然比从头训练快，但阶段 2 在 ImageNet-22K 上的对齐仍需要数十小时的 H100 算力，对普通开发者仍有门槛。

（完）
