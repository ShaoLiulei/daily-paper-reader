---
title: "NeuronSpark: A Spiking Neural Network Language Model with Selective State Space Dynamics"
title_zh: NeuronSpark：一种具有选择性状态空间动力学的脉冲神经网络语言模型
authors: Zhengzheng Tang
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16148v1"
tags: ["query:snn"]
score: 10.0
evidence: 用于大规模语言建模的脉冲神经网络架构
tldr: 本研究探讨了纯脉冲神经网络（SNN）在不依赖Transformer蒸馏的情况下，能否从随机初始化学习大规模语言建模。作者提出了NeuronSpark，一个拥有9亿参数的SNN语言模型。该模型结合了选择性状态空间动力学、漏电流层间通信、PonderNet自适应时间步及多种稳定技术。在有限的预训练和微调下，模型展现了初步的多轮对话能力，证明了纯SNN架构在大规模端到端语言建模中的可行性。
selection_source: fresh_fetch
motivation: 探索纯脉冲神经网络（SNN）是否能在不借助Transformer蒸馏的情况下，从零开始实现大规模语言建模。
method: 提出了NeuronSpark模型，结合了选择性状态空间动力学、漏电流通信、自适应时间步以及多种训练稳定化技术。
result: 0.9B参数的模型在14亿token预训练后达到3.6的损失值，并在微调后展现出初步的多轮对话能力。
conclusion: 实验结果验证了在大规模参数下，使用纯SNN架构进行端到端语言建模的技术可行性。
---

## 摘要
我们探讨了纯脉冲骨干网络是否可以在不依赖 Transformer 蒸馏的情况下，从随机初始化开始学习大规模语言建模。我们介绍了 NeuronSpark，这是一个拥有 9 亿参数的脉冲神经网络（SNN）语言模型，采用下文预测（next-token prediction）和代理梯度（surrogate gradients）进行训练。该模型结合了选择性状态空间脉冲动力学、漏电流层间通信、PonderNet 自适应时间步、融合 Triton PLIF 核以及稳定化技术（残差中心化、侧抑制归一化和自然梯度补偿）。在受限的预算下（约 14 亿预训练 token 和 6500 步有监督微调 SFT），NeuronSpark-0.9B 的预训练损失达到 3.6，并在 SFT 后展现出初步的多轮对话行为。这些结果支持了在此规模下使用纯 SNN 架构进行端到端语言建模的可行性。

## Abstract
We ask whether a pure spiking backbone can learn large-scale language modeling from random initialization, without Transformer distillation. We introduce NeuronSpark, a 0.9B-parameter SNN language model trained with next-token prediction and surrogate gradients. The model combines selective state-space spiking dynamics, leakage-current inter-layer communication, PonderNet adaptive timesteps, fused Triton PLIF kernels, and stabilization techniques (residual centering, lateral-inhibition normalization, and natural-gradient compensation). Under a constrained budget (about 1.4B pretraining tokens and 6.5K SFT steps), NeuronSpark-0.9B reaches 3.6 pretraining loss and shows early multi-turn dialogue behavior after SFT. These results support the feasibility of end-to-end language modeling with a pure SNN architecture at this scale.

---

## 论文详细总结（自动生成）

### NeuronSpark：具有选择性状态空间动力学的脉冲神经网络语言模型总结

#### 1. 核心问题与研究动机
论文探讨的核心问题是：**纯脉冲神经网络（SNN）能否在不依赖 Transformer 知识蒸馏的情况下，仅通过随机初始化和标准的下文预测任务，学习大规模语言建模？**
背景在于，尽管 Transformer 在 NLP 领域取得巨大成功，但其二次方复杂度的注意力和高能耗引起了关注。SNN 作为“第三代神经网络”，具有高能效和生物合理性，但在语言建模规模上一直进展缓慢。现有研究多依赖预训练 Transformer 的蒸馏，或规模较小（<216M），且往往在关键阶段保留非脉冲组件。

#### 2. 核心方法论
论文提出了 **NeuronSpark** 架构（0.9B 参数），其核心思想是将 SNN 的动力学与选择性状态空间模型（SSM）相结合：
*   **SNN-SSM 对偶性**：将参数化漏电积分发放（PLIF）神经元的膜电位动力学公式化为选择性 SSM。其中，衰减率 $\beta$、输入增益 $\alpha$ 和发放阈值 $V_{th}$ 均变为输入相关的动态门控机制，类似于 Mamba 的选择机制。
*   **漏电流层间通信**：不同于传统 SNN 使用二进制脉冲（0/1）进行层间通信，NeuronSpark 使用浮点数形式的“漏电流激活信号” $(1-\beta) \cdot V_{post}$。这解决了二进制通信的表达能力瓶颈，并能根据神经元的动力学特性自动进行时间尺度加权。
*   **PonderNet 自适应时间步**：在每个子层引入 PonderNet，允许模型根据 token 的复杂程度动态调整 SNN 的计算步数（$1$ 到 $K_{max}$ 步），实现计算资源的自适应分配。
*   **训练稳定化技术**：
    *   **残差中心化**：在残差相加前减去均值，消除深层网络中的直流漂移。
    *   **侧抑制归一化**：模拟生物皮层的侧抑制机制（等效于 RMSNorm）。
    *   **自然梯度补偿**：针对调制参数的梯度饱和与跨层不均问题进行两阶段补偿。
*   **高效实现**：开发了 Triton 融合 PLIF 算子，将前向/后向传播（含代理梯度计算）封装在单个内核中，显著提升训练效率。

#### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 Seq-Monkey 通用语料库（约 100 亿 token），实际训练使用了约 14 亿 token。
    *   **微调（SFT）**：使用 BelleGroup 的 3.5M 中文指令微调数据集，实际训练了 6500 步（约 4.2 万样本）。
*   **Benchmark 与对比**：
    *   对比了现有的 SNN 语言模型，如 SpkBERT-110M、SpkGPT-216M。
    *   进行了 9 组架构消融实验，验证不同稳定化组件对损失函数收敛的影响。
    *   进行了生物解释性分析，研究模型在不同词性、不同深度下的计算分配逻辑。

#### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA RTX 4090 GPU**。
*   **训练时长/规模**：预训练进行了 8.5 万步（约 1.4B tokens），SFT 进行了 6500 步。
*   **吞吐量**：在 8 张 4090 上达到约 960 tokens/sec。

#### 5. 实验数量与充分性
*   **实验规模**：模型参数量达到 0.9B，是目前已知的最大纯 SNN 语言模型之一。
*   **消融实验**：作者测试了 9 种变体（如移除自然梯度补偿、改变 $\alpha$ 边界等），结果显示只有最终的 NeuronSpark 架构能稳定收敛至 3.5 左右的损失值，其余变体均在 7.0 以上停滞或发散。
*   **充分性评价**：实验在架构验证上非常充分，但在数据规模上受限于算力（仅使用了 14% 的预训练数据）。缺乏与同等规模 Transformer 模型的直接定量基准测试（如 C-Eval, CMMLU）。

#### 6. 主要结论与发现
*   **可行性确认**：证明了纯 SNN 架构可以从随机初始化开始，在 0.9B 规模下实现非平凡的语言生成和多轮对话能力。
*   **生物一致性**：模型自发学习到了生物学特征：
    1.  **结构驱动计算**：计算资源分配与语法角色（如标点、虚词步数少，实词步数多）相关，而非预测难度。
    2.  **层级深度依赖**：深层网络在 SNNBlock 中分配更多时间步，用于复杂的上下文整合。
    3.  **多时间尺度特化**：神经元自发分化为快速响应和长时记忆两类群体。
*   **结构先于语义**：模型在有限训练下获得了流利的语法生成能力，但尚未获得逻辑推理和算术能力。

#### 7. 优点
*   **理论创新**：成功建立了 SNN 与选择性 SSM 之间的形式化对偶关系。
*   **端到端纯粹性**：实现了从输入到输出核心堆栈的全脉冲/类脉冲动力学，不依赖蒸馏。
*   **工程优化**：Triton 算子的引入解决了 SNN 串行扫描导致的训练缓慢问题。
*   **解释性强**：通过 PonderNet 和 $\beta$ 分布分析，提供了丰富的神经科学视角解释。

#### 8. 不足与局限
*   **数据与算力受限**：训练 token 数远未达到模型饱和点，导致模型缺乏推理能力。
*   **评估指标缺失**：缺乏在标准 LLM 评测集上的定量得分，难以评估其相对于 Transformer 的竞争力。
*   **应用限制**：目前仅支持中文，且存在重复生成等小模型常见问题。
*   **能效验证缺失**：虽然 SNN 理论上节能，但论文未在神经形态硬件（如 Loihi）上进行实际功耗测量。

（完）
