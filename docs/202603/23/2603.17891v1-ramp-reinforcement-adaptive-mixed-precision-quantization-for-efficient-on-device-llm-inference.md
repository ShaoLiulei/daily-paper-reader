---
title: "RAMP: Reinforcement Adaptive Mixed Precision Quantization for Efficient On Device LLM Inference"
title_zh: RAMP：面向高效端侧大语言模型推理的强化自适应混合精度量化
authors: "Arpit Singh Gautam, Saurabh Jha"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17891v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 高效大模型推理的混合精度量化
tldr: 针对大语言模型在资源受限设备上的部署，本文提出RAMP框架，利用强化学习（SAC）实现自适应混合精度量化。通过学习每层的最优位宽分配并结合Scale Folding预处理技术，RAMP在保持模型性能的同时显著降低了存储需求。该方法具有极强的泛化能力，支持跨模型规模和家族的零样本迁移，并在多种硬件上实现了高效推理。
selection_source: fresh_fetch
motivation: 传统的均匀位宽量化方法无法在模型精度与推理效率之间达到最优平衡，且难以处理激活值中的离群点。
method: 采用基于Soft Actor-Critic的强化学习框架，结合11维层特征嵌入和Scale Folding预处理技术，自动搜索每层的最优位宽。
result: 在Llama 2 7B上以更小的体积实现了优于4位AWQ和GPTQ的精度，且训练好的策略可直接应用于Llama 2 13B和Mistral 7B。
conclusion: RAMP证明了量化敏感性主要由架构决定，通过自适应混合精度和跨模型迁移，显著提升了边缘设备上LLM的部署效率。
---

## 摘要
训练后量化对于在资源受限的硬件上部署大语言模型（LLMs）至关重要，然而目前最先进的方法对各层强制执行统一的位宽，导致精度与效率的权衡并非最优。我们提出了 RAMP（强化自适应混合精度），这是一个离策（off-policy）Soft Actor Critic 框架，通过学习逐层位宽分配，在全局比特预算下最小化困惑度。该策略基于激活统计量、权重属性和结构描述符的 11 维嵌入进行条件化，实现了跨模型家族和规模的零样本迁移。为了实现稳定的 4 比特以下量化，我们引入了尺度折叠（Scale Folding），这是一种通过逐通道缩放和归一化层补偿将激活离群值迁移至权重的预处理技术。一种具有非对称惩罚和预算悬崖的质量优先奖励机制驱动了模型的快速收敛。在 Llama 2 7B 上，RAMP 在 3.68GB（有效位宽 3.65 bits）下实现了 5.54 的困惑度，在模型大小上优于统一 4 比特 AWQ（3.90 GB 下为 5.60）和 GPTQ 约 6%，在质量上提升了 1% 至 3%。关键的是，仅在 Llama 2 7B 上训练的策略可以零样本泛化至 Llama 2 13B 和 Mistral 7B，且表现往往优于针对特定目标的训练，这支持了量化敏感性主要由架构决定的假设。HALO 流水线将分配方案导出为 GGUF 格式，以便在 CPU、GPU 和边缘设备上进行无算子（kernel-free）推理，保留了 FP16 常识推理性能的 99.5%。

## Abstract
Post training quantization is essential for deploying large language models (LLMs) on resource constrained hardware, yet state of the art methods enforce uniform bit widths across layers, yielding suboptimal accuracy efficiency trade offs. We present RAMP (Reinforcement Adaptive Mixed Precision), an off policy Soft Actor Critic framework that learns per layer bit width assignments to minimize perplexity under a global bit budget. The policy conditions on an 11 dimensional embedding of activation statistics, weight properties, and structural descriptors, enabling zero shot transfer across model families and scales. To enable stable sub 4 bit quantization, we introduce Scale Folding, a preconditioning technique that migrates activation outliers into weights via per channel scaling and normalization layer compensation. A quality prioritized reward with asymmetric penalties and budget cliffs drives rapid convergence. On Llama 2 7B, RAMP achieves 5.54 perplexity at 3.68GB (3.65 effective bits), outperforming uniform 4 bit AWQ (5.60 at 3.90 GB) and GPTQ by 6% in size and 1% to3% in quality. Critically, a policy trained only on Llama 2 7B generalizes zero shot to Llama 2 13B and Mistral 7B, often surpassing target specific training, supporting the hypothesis that quantization sensitivity is primarily architectural. The HALO pipeline exports allocations to GGUF format for kernel free inference on CPUs, GPUs, and edge devices, retaining 99.5% of FP16 commonsense reasoning performance.

---

## 论文详细总结（自动生成）

以下是对论文《RAMP: Reinforcement Adaptive Mixed-Precision Quantization for Efficient On-Device LLM Inference》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）部署面临“内存墙”瓶颈，现有的训练后量化（PTQ）方法（如GPTQ、AWQ）通常采用**均匀位宽（Uniform Bit-width）**分配，忽略了模型不同层对量化敏感性的差异，导致精度与效率的权衡并非最优。
*   **研究动机**：
    *   **灵活性不足**：均匀量化在鲁棒层浪费比特，在敏感层分配不足。
    *   **迁移性差**：现有混合精度搜索通常需要针对每个模型重新进行昂贵的优化。
    *   **推理开销**：非标准的混合精度模式会导致算子碎片化，降低实际推理速度。
    *   **离群值挑战**：激活值中的极端离群点使得 4-bit 以下的量化极易导致模型崩溃。

### 2. 方法论
RAMP 框架将量化位宽分配建模为一个**马尔可夫决策过程（MDP）**，通过强化学习自动寻找最优策略。
*   **核心思想**：利用 **Soft Actor-Critic (SAC)** 算法学习一个策略，根据层的结构和统计特征，逐层分配位宽（3, 4, 5, 6 bits），以在满足全局比特预算的前提下最小化困惑度（Perplexity）。
*   **关键技术细节**：
    1.  **11维层嵌入（State Space）**：策略输入包括激活统计量（最大值、重要性）、权重统计量（均值、标准差）、结构描述符（深度、维度、层类型）及上下文特征。通过归一化处理，使策略具备跨模型规模的迁移能力。
    2.  **质量优先奖励函数**：设计了非对称奖励机制，对困惑度上升给予重罚，并设置“预算悬崖（Budget Cliff）”惩罚，强制模型在预算内优化。
    3.  **尺度折叠（Scale Folding）**：在量化前通过逐通道缩放，将激活值中的离群点迁移到权重中，并补偿归一化层，从而稳定 3-bit 等低位宽量化。
    4.  **HALO 推理流水线**：将学习到的分配方案映射到标准的 GGUF 格式，利用 `llama.cpp` 的成熟算子实现跨平台（CPU/GPU/边缘端）的高效推理，避免了自定义算子的开发。

### 3. 实验设计
*   **实验对象**：Llama-2 (7B, 13B), Llama-3 (8B), Mistral-7B。
*   **数据集**：
    *   **校准与评估**：WikiText-2（用于计算困惑度 PPL）。
    *   **下游任务**：PIQA, HellaSwag, WinoGrande, ARC（用于评估常识推理能力）。
*   **对比基准（Benchmarks）**：
    *   **均匀量化**：RTN, GPTQ, AWQ, SmoothQuant, QuIP#, LRQ。
    *   **混合精度/稀疏**：SqueezeLLM, Q4_K_M (GGUF标准模式)。

### 4. 资源与算力
*   **硬件平台**：使用了 NVIDIA RTX PRO 5000 Blackwell 和 A100 GPU。
*   **训练时长**：
    *   SAC 策略训练在 Llama-2-7B 上约需 **6 小时**（约 120-150 个 Episode 即可收敛）。
    *   相比之下，使用 PPO 算法需要 48 小时，证明了 SAC 的高采样效率。
    *   校准和特征提取仅需约 2 分钟。

### 5. 实验数量与充分性
*   **实验规模**：进行了 **50 多组** 核心实验，涵盖了不同模型家族、不同规模以及零样本迁移测试。
*   **充分性**：
    *   包含详尽的**消融实验**（针对 Scale Folding、奖励设计、强化学习算法选择等）。
    *   进行了**零样本迁移实验**：在 7B 模型上训练的策略直接应用于 13B 和 Mistral 模型，验证了策略的泛化性。
    *   实验设计客观，通过多次运行取平均值，并对比了模型大小、困惑度、下游任务准确率和推理延迟等多个维度。

### 6. 主要结论与发现
*   **帕累托最优**：RAMP 在 Llama-2-7B 上以 3.68GB 的体积实现了 5.54 PPL，比 4-bit AWQ 体积缩小 6%，质量提升 1.1%。
*   **强大的迁移性**：在 Llama-2-7B 上训练的策略在 Llama-2-13B 和 Mistral-7B 上表现甚至优于直接在目标模型上训练的结果，证明量化敏感性主要由**架构结构**决定。
*   **性能保持**：在大幅压缩的情况下，RAMP 保留了 FP16 基准模型 99.5% 的常识推理性能。
*   **硬件友好**：通过 HALO 流水线，在 RTX 3090 上实现了比 FP16 快 3.05 倍的推理速度。

### 7. 优点与亮点
*   **首个可迁移策略**：打破了以往混合精度量化需要逐模型搜索的限制，实现了“一次训练，到处应用”。
*   **Scale Folding 技术**：有效解决了低比特量化下的离群点问题，使 3-bit 量化在生产环境中变得实用。
*   **生产级部署**：直接对接 GGUF 格式，无需编写复杂的自定义 CUDA 内核即可在多种硬件上运行。
*   **高效率**：利用离策强化学习（SAC）显著降低了搜索位宽分配方案的时间成本。

### 8. 不足与局限
*   **架构覆盖有限**：目前仅在 Decoder-only 的 Transformer 架构（Llama, Mistral）上验证，尚未测试 Encoder-Decoder 或 MoE 架构。
*   **位宽粒度限制**：目前仅支持离散的位宽（3, 4, 5, 6 bits），不支持分数位宽（如 3.5-bit）或更细粒度的通道级分配。
*   **静态分配**：位宽分配在推理前固定，尚未实现根据输入动态调整位宽的策略。
*   **校准依赖**：虽然策略可迁移，但每个新模型仍需进行简短的校准过程以提取特征嵌入。

（完）
