---
title: "ASAP: Attention-Shift-Aware Pruning for Efficient LVLM Inference"
title_zh: ASAP：面向高效 LVLM 推理的注意力偏移感知剪枝
authors: "Surendra Pathak, Bo Han"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14549v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 高效LVLM推理与注意力偏移感知剪枝
tldr: "针对LVLM处理高分辨率图像时计算开销大的问题，本文提出ASAP，一种无需训练且兼容KV缓存的剪枝方案。ASAP通过动态双向软注意力掩码缓解“注意力偏移”，并利用加权软合并减少语义冗余。该方法在保持LLaVA-NeXT-7B约99%性能的同时，减少了约80%的计算量，实现了高效推理。"
selection_source: fresh_fetch
motivation: 现有的Token减少策略忽略了LVLM中导致Token评分偏差的“注意力偏移”现象以及Token间的语义冗余。
method: 提出ASAP方案，结合动态双向软注意力掩码以精准筛选Token，并采用加权软合并技术融合语义相似的视觉块。
result: "在LLaVA-NeXT-7B上实现了近乎无损的压缩，保留了99.02%的性能并减少了约80%的计算FLOPs。"
conclusion: ASAP为LVLM提供了一种高效、无需训练且兼容现有缓存机制的推理加速新路径。
---

## 摘要
尽管大型视觉语言模型 (LVLM) 展示了卓越的多模态能力，但处理高分辨率视觉 token 的二次方计算成本仍然是一个关键瓶颈。虽然最近的 token 减少策略试图加速推理，但这些方法未能充分利用注意力值，且无法解决 token 冗余问题。更关键的是，它们忽视了 LVLM 中固有的“注意力偏移”现象，这种现象会使 token 的注意力分数产生偏差。在这项工作中，我们提出了 ASAP，这是一种新型的、无需训练且兼容 KV 缓存的剪枝方案，全面解决了这些局限性。首先，我们通过利用动态双向软注意力掩码来缓解注意力偏移，确保选择真正具有信息量的 token，而非基于朴素注意力的选择。其次，我们认为 token 集合中的高语义冗余会降低性能。因此，我们引入了一个加权软合并组件，用于合并语义相似的 token，仅为后续层保留特征最密集的视觉 patch。ASAP 实现了几乎无损的视觉上下文压缩，在保留 LLaVA-NeXT-7B 原始性能 99.02% 的同时，大幅削减了约 80% 的计算 FLOPs。

## Abstract
While Large Vision-Language Models (LVLMs) demonstrate exceptional multi-modal capabilities, the quadratic computational cost of processing high-resolution visual tokens remains a critical bottleneck. Though recent token reduction strategies attempt to accelerate inference, such methods inadequately exploit attention values and fail to address token redundancy. More critically, they overlook the ``attention shift'' phenomenon inherent in LVLMs, which skews token attention scores. In this work, we propose ASAP, a novel training-free, KV-Cache-compatible pruning recipe that comprehensively addresses these limitations. First, we mitigate the attention shift by utilizing a dynamic bidirectional soft attention mask, ensuring the selection of genuinely informative tokens rather than naive attention-based selection. Second, we posit that high semantic redundancy within the token set degrades performance. We therefore introduce a weighted soft merging component that merges semantically similar tokens, preserving only the most feature-dense visual patches for subsequent layers. ASAP achieves virtually lossless compression of visual context, retaining 99.02% of the original LLaVA-NeXT-7B performance while aggressively slashing computational FLOPs by ~80%.

---

## 论文详细总结（自动生成）

这是一份关于论文《ASAP: Attention-Shift-Aware Pruning for Efficient LVLM Inference》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：大型视觉语言模型（LVLM）在处理高分辨率图像时会产生海量的视觉 Token（如 LLaVA-NeXT 约 2304 个），导致计算开销随序列长度呈二次方增长，严重制约推理效率。
*   **研究动机**：
    *   **注意力偏移（Attention Shift）**：作者发现由于旋转位置编码（RoPE）的距离衰减特性，模型倾向于给序列后端的 Token 分配更高权重，导致前端视觉 Token 被错误地剪枝。
    *   **语义冗余**：图像中存在大量重复或低信息密度的区域（如天空、背景），现有方法未能有效压缩这些冗余。
    *   **兼容性需求**：现有的剪枝方案往往需要额外训练，或者不兼容 KV 缓存（KV-Cache），难以直接应用于多轮对话。

### 2. 论文提出的方法论：ASAP 框架
ASAP 是一种**无需训练（Training-free）**、**即插即用**且**兼容 KV 缓存**的剪枝方案，包含三个核心组件：
*   **显著性引导的双向注意力（SG-BiMask）**：
    *   **核心思想**：打破标准的因果掩码（Causal Mask）限制，允许视觉 Token 之间进行双向信息流动。
    *   **技术细节**：通过计算预 Softmax 的对齐矩阵来提取结构显著性，并引入一个动态的软双向掩码 $M_{bidir}$。该掩码根据 Token 的显著性得分调整权重，让早期的视觉 Token 能够“看到”后期的 Token，从而抵消 RoPE 带来的位置偏见。
*   **显著性加权合并（Salience-Weighted Consolidation）**：
    *   **核心思想**：在保留显著 Token 的基础上，通过特征相似度合并语义冗余的 Token。
    *   **技术细节**：利用余弦相似度识别冗余，并根据显著性得分进行加权融合，确保融合后的特征由最具结构意义的 Patch 主导，避免特征稀释。
*   **预算重新分配（Budget Reallocation/Salvage）**：
    *   **核心思想**：合并操作会腾出预设的 Token 预算位，ASAP 会从之前被剪掉的 Token 池中“打捞”出显著性次高的 Token 填补空位，最大化信息保留。

### 3. 实验设计
*   **模型对象**：LLaVA-1.5-13B、LLaVA-NeXT-7B (LLaVA-1.6)、InternVL 2.5-8B。
*   **数据集/Benchmark**：涵盖了 VQAv2、GQA、ScienceQA、TextVQA、MME、MMBench (CN)、MMVet、POPE 等 9 个主流基准，测试维度包括视觉问答、OCR、推理、幻觉检测等。
*   **对比方法**：FastV、LLaVA-PruMerge+、SparseVLM、DivPrune、VisPruner 等最先进的无需训练的剪枝基线。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及数量。
*   **效率指标**：作者重点分析了理论 FLOPs、KV 缓存存储占用（MB）、GPU 显存占用（GB）以及 CUDA 推理延迟（ms）。
*   **训练时长**：由于该方法是 **Training-free（无需训练）** 的，因此不存在训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个不同架构的 LVLM 上进行了验证。
    *   针对每个模型设置了不同的 Token 保留预算（如保留 128、192、640、960 个 Token 等）。
    *   进行了详细的消融实验，验证了双向注意力模块和合并模块的各自贡献。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了准确率，还深入分析了系统层面的效率（显存、延迟），并展示了将 ASAP 组件集成到其他框架（如 FastV）中的通用性。

### 6. 论文的主要结论与发现
*   **性能近乎无损**：在 LLaVA-NeXT-7B 上，ASAP 在减少约 **80% 计算量（FLOPs）** 的情况下，保留了 **99.02%** 的原始性能。
*   **超越基准**：在某些任务（如 MMBench）上，ASAP 的表现甚至超过了未剪枝的原始模型（Supra-vanilla performance），这表明剪枝可能起到了去除噪声的作用。
*   **成功解决偏移**：定性分析证明，ASAP 能够捕捉到被传统方法忽略的背景关键目标（如远处的车辆）。

### 7. 优点（亮点）
*   **理论深刻**：系统性地指出了 RoPE 导致的“注意力偏移”是 LVLM 剪枝失效的核心原因，并给出了数学解释。
*   **架构兼容**：完全兼容 KV 缓存，这意味着它在实际生产环境的多轮对话中具有极高的实用价值。
*   **三阶段协同**：剪枝、合并、打捞三个步骤环环相扣，在固定预算下最大化了语义密度。

### 8. 不足与局限
*   **超参数依赖**：引入了如 $\lambda_{max}$（最大前向可见度）等超参数，可能需要针对不同模型进行微调。
*   **推理开销**：虽然大幅减少了 FLOPs，但动态选择和合并逻辑引入了约 **2ms** 的额外 CUDA 延迟（尽管相对于节省的时间可以忽略不计）。
*   **适用范围**：主要针对基于 Transformer 架构且使用 RoPE 的模型，对于非 Transformer 架构的适用性未讨论。

（完）
