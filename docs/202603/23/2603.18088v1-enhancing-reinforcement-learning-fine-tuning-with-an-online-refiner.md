---
title: Enhancing Reinforcement Learning Fine-Tuning with an Online Refiner
title_zh: 通过在线精炼器增强强化学习微调
authors: "Hao Ma, Zhiqiang Pu, Yang Liu, Xiaolin Ai"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18088v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 带有动态约束的强化学习微调
tldr: 针对强化学习微调（RFT）中固定约束限制模型性能的问题，本文提出了一种动态约束方法。该方法利用参考模型作为在线精炼器，对微调模型的输出进行最小化错误修正，并以此作为监督信号。这种机制能根据输出质量自动调节约束强度，在防止模型退化的同时保留了探索空间。实验证明，该方法在对话和代码生成任务上优于KL正则化，实现了更高的任务奖励和更强的训练稳定性。
selection_source: fresh_fetch
motivation: 传统的固定约束在防止模型退化的同时，会因限制探索空间而与优化目标产生冲突。
method: 引入参考模型作为在线精炼器，对微调模型的输出进行实时修正，并利用监督微调损失引导模型学习这些改进后的输出。
result: 在对话和代码生成实验中，动态约束方法在任务奖励和训练稳定性方面均显著优于KL正则化和无约束基线。
conclusion: 动态约束机制通过根据模型输出质量自动调整干预强度，有效平衡了强化学习微调中的性能探索与输出稳定性。
---

## 摘要
约束对于稳定强化学习微调 (RFT) 和防止退化输出至关重要，但它们与优化目标本质上存在冲突，因为更强的约束限制了微调模型发现更好解决方案的能力。我们提出了“动态约束”，通过适应微调模型不断演进的能力来解决这种紧张关系，其核心见解是约束仅应在出现退化输出时进行干预。我们通过使用参考模型作为“在线精炼器”来实现这一点，该精炼器接收来自微调模型的响应，并生成一个最小修正版本，在修复错误的同时逐字保留正确内容。随后，通过监督微调损失训练该微调模型以产生精炼后的输出。这种机制产生了一种能够根据输出质量自动加强或放松的约束。在对话和代码生成方面的实验表明，动态约束优于 KL 正则化和无约束基线，在保持训练稳定性的同时实现了显著更高的任务奖励。

## Abstract
Constraints are essential for stabilizing reinforcement learning fine-tuning (RFT) and preventing degenerate outputs, yet they inherently conflict with the optimization objective because stronger constraints limit the ability of a fine-tuned model to discover better solutions. We propose \textit{dynamic constraints} that resolve this tension by adapting to the evolving capabilities of the fine-tuned model based on the insight that constraints should only intervene when degenerate outputs occur. We implement this by using a reference model as an \textit{online refiner} that takes the response from the fine-tuned model and generates a minimally corrected version which preserves correct content verbatim while fixing errors. A supervised fine-tuning loss then trains the fine-tuned model to produce the refined output. This mechanism yields a constraint that automatically strengthens or relaxes based on output quality. Experiments on dialogue and code generation show that dynamic constraints outperform both KL regularization and unconstrained baselines, achieving substantially higher task rewards while maintaining training stability.

---

## 论文详细总结（自动生成）

以下是对论文《Enhancing Reinforcement Learning Fine-Tuning with an Online Refiner》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在强化学习微调（RFT）中，**训练稳定性**与**性能上限**之间存在固有矛盾。
*   **研究背景**：为了防止模型在强化学习过程中出现“灾难性遗忘”或“分布崩溃”（生成无意义内容），通常会引入 KL 散度作为**静态约束**，将微调模型锚定在初始参考模型附近。
*   **动机**：静态 KL 约束虽然稳定了训练，但也限制了模型探索更优解的能力。作者认为，约束不应是死板的距离限制，而应只在模型输出出现退化（错误）时才进行干预。

### 2. 方法论
*   **核心思想**：提出**动态约束（Dynamic Constraint）**机制。利用参考模型作为一个“在线精炼器（Online Refiner）”，实时修正微调模型的输出，并以此作为动态目标引导训练。
*   **关键技术细节**：
    1.  **在线精炼（Online Refining）**：微调模型 $\pi_\theta$ 生成响应 $a$ 后，将其输入给参考模型 $\pi_0$。精炼器被要求：如果 $a$ 正确则逐字保留，如果错误则进行最小化修正，生成 $a'$。
    2.  **自适应目标**：不同于固定参考分布，动态约束将 $\pi_\theta$ 推向一个随其能力演进的、更高质量的目标 $a'$。
    3.  **损失函数**：采用序列级交叉熵损失（类似 SFT 损失），让 $\pi_\theta$ 学习精炼后的响应 $a'$。
    4.  **过滤机制**：仅当精炼后的响应 $a'$ 的奖励值高于原始响应 $a$ 时，才应用该约束，确保引导信号始终是正向的。
*   **算法流程**：在 RFT 迭代中，采样 -> 精炼 -> 过滤 -> 计算 RL 奖励 + 动态约束损失 -> 更新参数。

### 3. 实验设计
*   **数据集/场景**：
    *   **对话生成**：使用 `Prompt-Collection-v0.1`（包含 UltraFeedback 等子集）。
    *   **代码生成**：使用 `APPS` 数据集进行训练。
*   **Benchmark（测试集）**：在代码任务上使用了 `HumanEval`、`HumanEval+`、`MBPP` 和 `MBPP++`。
*   **对比方法**：
    *   **Static**：带标准 KL 正则化的 PPO。
    *   **w/o Constraint**：不带任何约束的纯 RL 优化。
    *   **DAPO**：目前最先进的（SOTA）强化学习微调算法。
*   **基础模型**：Llama-3.2-3B-Instruct 和 Qwen2.5-Coder-1.5B-Instruct。

### 4. 资源与算力
*   **硬件环境**：使用了 8 张 **NVIDIA A6000 (48GB)** GPU。
*   **训练时长**：
    *   在代码生成任务中，DAPO 基线耗时约 17.8 小时。
    *   **Dynamic 方法耗时约 26.3 小时**（由于引入了在线精炼步骤，计算开销增加了约 48%）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了通用对话和专业代码两大领域，进行了完整的训练动力学分析。
*   **消融实验**：针对约束系数 $\eta$ 的大小以及“过滤机制”的必要性进行了专门的消融研究。
*   **充分性评价**：实验设计较为充分。作者不仅展示了最终的 Benchmark 分数，还通过 KL 散度曲线、奖励增长曲线和 Token 级交叉熵热力图深入分析了动态约束的工作原理。对比实验涵盖了从基础 PPO 到最新的 DAPO 算法，具有较强的说服力。

### 6. 主要结论与发现
*   **突破性能天花板**：动态约束允许模型在远离初始分布（高 KL）的同时保持输出的连贯性，从而获得比静态约束显著更高的任务奖励。
*   **训练稳定性**：相比无约束方法，动态约束能有效防止分布崩溃；相比静态约束，它能持续推动模型进步而不陷入平台期。
*   **代码能力提升**：在 Python 代码生成基准测试中，该方法相比 DAPO 实现了平均 **30.8%** 的相对性能提升。

### 7. 优点（亮点）
*   **理念创新**：将“约束”从一种惩罚项转变为一种“按需干预”的引导机制，解决了 RFT 中探索与稳定的矛盾。
*   **自适应性**：约束强度随模型表现自动调节——表现好时约束弱，表现差时约束强。
*   **即插即用**：该动态约束可以作为附加项轻松集成到 PPO、DAPO 等现有 RFT 框架中。

### 8. 不足与局限
*   **计算开销**：引入在线精炼器显著增加了训练时的推理负担（约 50% 的额外耗时）。
*   **精炼器依赖**：动态约束的效果高度依赖于参考模型（精炼器）的修正能力。如果参考模型本身无法发现或修正微调模型的错误，该机制的效果会打折扣。
*   **长文本挑战**：对于极长序列的推理任务，保持全局结构的一致性进行精炼可能存在困难（作者在 Limitations 中提到未来需探索分块精炼）。

（完）
