---
title: Instruction-Free Tuning of Large Vision Language Models for Medical Instruction Following
title_zh: 面向医疗指令遵循的大视觉语言模型无指令微调
authors: "Myeongkyun Kang, Soopil Kim, Xiaoxiao Li, Sang Hyun Park"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19482v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大型视觉语言模型的免指令微调
tldr: 针对医疗领域缺乏高质量指令数据集的问题，本文提出一种无需指令的微调方法（Instruction-Free Tuning）。该方法仅利用图像-描述对，通过引入动量代理指令和响应打乱策略，在不依赖手工指令的情况下，使大视觉语言模型（LVLM）能够灵活响应领域特定指令。实验表明，该方法在多个医疗视觉问答任务上达到了SOTA水平，显著提升了微调效率。
selection_source: fresh_fetch
motivation: 医疗领域构建大规模高质量指令数据集极具挑战，限制了LVLM在专业领域的微调效果。
method: 提出利用动量代理指令替代手工指令，并结合响应打乱策略来优化仅基于图像-描述对的微调过程。
result: 在SKINCON、WBCAtt、CBIS和MIMIC-CXR等多个医疗数据集的多选题视觉问答任务中取得了最先进的准确率。
conclusion: 该方法有效降低了医疗LVLM微调对专家指令的依赖，在保持指令遵循能力的同时显著提升了模型性能。
---

## 摘要
大视觉语言模型（LVLMs）在广泛的任务中展现出了令人印象深刻的性能。这些能力主要源于视觉指令微调，即在由精心策划的“图像-指令-输出”三元组组成的数据集上对模型进行微调。然而，在医疗领域，由于需要专业的专家知识，构建大规模、高质量的指令数据集尤其具有挑战性。为了解决这一问题，我们提出了一种无指令微调方法，该方法减少了对手工指令的依赖，仅利用“图像-描述”对进行微调。具体而言，我们引入了动量代理指令作为精心策划的文本指令的替代方案，这在保留预训练 LVLM 指令遵循能力的同时，促进了在推理过程中保持有效的参数更新。因此，即使在微调过程中缺乏显式指令，微调后的 LVLM 也能灵活地响应特定领域的指令。此外，我们结合了响应打乱策略，以减轻模型对先前词汇的过度依赖，从而实现更有效的微调。我们的方法在 SKINCON、WBCAtt、CBIS 和 MIMIC-CXR 数据集的选择题视觉问答任务上达到了最先进的准确率，显著提高了 LVLMs 在医疗领域的微调效率。

## Abstract
Large vision language models (LVLMs) have demonstrated impressive performance across a wide range of tasks. These capabilities largely stem from visual instruction tuning, which fine-tunes models on datasets consisting of curated image-instruction-output triplets. However, in the medical domain, constructing large-scale, high-quality instruction datasets is particularly challenging due to the need for specialized expert knowledge. To address this issue, we propose an instruction-free tuning approach that reduces reliance on handcrafted instructions, leveraging only image-description pairs for fine-tuning. Specifically, we introduce a momentum proxy instruction as a replacement for curated text instructions, which preserves the instruction-following capability of the pre-trained LVLM while promoting updates to parameters that remain valid during inference. Consequently, the fine-tuned LVLM can flexibly respond to domain-specific instructions, even though explicit instructions are absent during fine-tuning. Additionally, we incorporate a response shuffling strategy to mitigate the model's over-reliance on previous words, facilitating more effective fine-tuning. Our approach achieves state-of-the-art accuracy on multiple-choice visual question answering tasks across SKINCON, WBCAtt, CBIS, and MIMIC-CXR datasets, significantly enhancing the fine-tuning efficiency of LVLMs in medical domains.

---

## 论文详细总结（自动生成）

以下是对论文《Instruction-Free Tuning of Large Vision Language Models for Medical Instruction Following》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在医疗领域，构建高质量的“图像-指令-输出”三元组（Instruction Tuning 数据集）极具挑战，因为这需要昂贵的专家知识。现有的自动生成方法（如利用 LLM 将放射报告转化为对话）容易产生领域知识偏差、幻觉以及严重的句子间依赖（即模型过度依赖文本上下文而忽略视觉特征）。
*   **研究背景**：大型视觉语言模型（LVLM）的成功高度依赖指令微调，但在缺乏大规模指令数据的医疗专业领域，模型往往表现出“指令依赖性”（仅对微调过的特定指令有效）或“图像依赖性”（生成固定格式响应而忽略指令）。

### 2. 方法论：核心思想与关键技术
论文提出了一种**无指令微调（Instruction-Free Tuning）**框架，核心思想是仅利用现有的“图像-描述”对（如放射报告）进行微调，而无需手工编写指令。
*   **动量代理指令（Momentum Proxy Instruction）**：
    *   引入一组可学习的连续向量 $t$ 作为“代理指令”，替代显式的文本指令。
    *   为了防止模型对特定代理指令过拟合，采用**指数移动平均（EMA）**更新策略，生成动量代理指令 $\bar{t}$：$\bar{t} \leftarrow \alpha\bar{t} + (1 - \alpha)t$。
    *   在推理阶段，丢弃代理指令，直接使用自然语言指令（如“请描述这张图”），模型仍能保持良好的指令遵循能力。
*   **响应打乱策略（Response Shuffling）**：
    *   针对医疗报告中存在的强因果依赖（如从“发现”直接推导“结论”），在微调时随机打乱描述文本的句子顺序。
    *   这迫使模型减少对前文词汇的过度依赖，转而更多地关注视觉特征。
*   **算法流程**：先进行预热阶段优化代理指令 $t$，随后在正式微调阶段同时更新视觉编码器参数和动量代理指令。

### 3. 实验设计
*   **数据集**：涵盖了四个具有代表性的医疗领域：
    1.  **SKINCON**：皮肤病学数据集（48种临床属性）。
    2.  **WBCAtt**：白细胞显微图像（11种形态属性）。
    3.  **CBIS**：乳腺钼靶影像（病理和异常类型）。
    4.  **MIMIC-CXR**：胸部 X 光报告（放射学发现和印象）。
*   **Benchmark**：将训练集转化为医疗报告格式进行微调，将验证/测试集转化为**多选题视觉问答（Multiple-choice VQA）**任务进行评估。
*   **对比方法**：
    *   **零样本/预训练模型**：LLaMA-3.2-Vision、Qwen2.5-VL、MedGemma、PubMedVision。
    *   **微调基准**：使用传统文本指令（如 "Describe this scan"）微调的上述模型，以及 BLIP-2。

### 4. 资源与算力
*   **模型架构**：基于 LLaMA-3.2-11B-Vision-Instruct 构建。
*   **框架**：使用 PyTorch 的 `torchtune` 微调库。
*   **超参数**：输入分辨率 560x560，Batch Size 为 2，使用 AdamW 优化器。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量，但提到微调过程包含 1 个 Epoch 的预热和 5 个 Epoch 的正式微调。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同模态的医疗数据集上进行了广泛测试，每组实验均运行 3 次并取平均值。
*   **消融实验**：
    *   对比了固定代理指令（Fix）与持续更新（Update）的效果。
    *   研究了动量系数 $\alpha$ 和代理指令向量数量 $N$ 的敏感度。
    *   验证了响应打乱策略在不同分隔符下的表现。
*   **充分性评价**：实验设计较为充分，不仅有定量准确率对比，还通过定性分析展示了模型在处理复杂指令时的灵活性，客观地证明了该方法在缓解“指令依赖”方面的有效性。

### 6. 主要结论与发现
*   **性能领先**：提出的 InstFree 方法在所有四个数据集上均显著优于现有的医疗 LVLM 和通用 LVLM 的微调结果。
*   **指令遵循能力更强**：相比于使用固定文本指令微调的模型，InstFree 在面对未见过的指令时表现更稳健，不会退化为生成固定格式的报告。
*   **视觉特征利用率提升**：响应打乱策略有效降低了模型对文本上下文的过度依赖，提高了对图像细节（如细胞形状、病变特征）的识别准确率。

### 7. 优点
*   **高效性**：完全摆脱了对专家标注指令的依赖，直接利用现有的医疗影像报告数据。
*   **通用性**：动量代理指令机制使得模型在微调后仍能保持预训练时的对话灵活性，解决了专业领域微调常见的“灾难性遗忘”或“模式僵化”问题。
*   **鲁棒性**：通过 EMA 更新和响应打乱，增强了模型对噪声数据和复杂关联文本的鲁棒性。

### 8. 不足与局限
*   **分隔符依赖**：响应打乱策略依赖于对报告文本的正确分割（如使用逗号或句号），对于格式极度混乱的原始医疗记录，可能需要额外的预处理。
*   **推理开销**：虽然微调效率高，但模型基础架构（11B参数）对于某些边缘医疗设备可能仍然过大。
*   **应用限制**：论文主要关注 VQA 任务的准确率，对于生成长篇、逻辑严密的医疗报告的质量提升效果探讨相对较少。

（完）
