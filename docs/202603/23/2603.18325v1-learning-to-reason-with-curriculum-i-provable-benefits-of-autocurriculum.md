---
title: "Learning to Reason with Curriculum I: Provable Benefits of Autocurriculum"
title_zh: 课程学习推理 I：自动课程的可证明收益
authors: "Nived Rajaraman, Audrey Huang, Miro Dudik, Robert Schapire, Dylan J. Foster, Akshay Krishnamurthy"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18325v1"
tags: ["query:ai-llm"]
score: 10.0
evidence: 用于监督微调和强化学习的自动课程学习
tldr: 本研究针对思维链（CoT）推理模型训练成本高昂的问题，提出了“自动课程学习”（Autocurriculum）框架。该方法通过让模型根据自身表现动态选择训练题目，实现了在监督微调（SFT）中指数级减少演示数据需求，并在强化学习（RL）中显著降低计算成本。研究证明，这种基于自适应数据选择的策略无需对提示词分布做特定假设，即可大幅提升推理能力的训练效率。
selection_source: fresh_fetch
motivation: 思维链推理模型的训练涉及大量人工或合成推理轨迹，导致数据和计算成本极高，限制了其进一步发展。
method: 提出自动课程学习方法，利用模型当前性能来决定训练重点，通过自适应数据选择优化SFT和RL过程。
result: 理论证明该方法在SFT中能指数级减少所需演示，在RL中能使计算成本几乎独立于参考模型的质量。
conclusion: 自动课程学习通过借鉴提升法和反例学习技术，为高效训练推理模型提供了一种具有理论优势且通用的算法设计方案。
---

## 摘要
思维链（Chain-of-thought）推理，即语言模型在给出最终回答前通过生成思考标记（thinking tokens）来消耗额外的计算量，已显著提升了模型能力。然而，训练这些推理模型的成本在数据和计算方面都极其高昂，因为它涉及从人类或合成生成器中收集长路径的推理行为轨迹，并进一步通过强化学习对模型进行后训练。这些成本是根本性的，还是可以通过更好的算法设计来降低？我们证明了自动课程（autocurriculum）——即模型利用自身表现来决定训练应关注哪些问题——在监督微调（SFT）和强化学习（RL）方面均能可证明地改进标准训练方案。对于 SFT，我们展示了自动课程通过将教师监督集中在当前模型难以处理的提示词（prompts）上，所需的推理示例比非自适应微调呈指数级减少。对于 RL 微调，自动课程将计算成本与参考模型的质量解耦，将后者降低为几乎与目标准确率无关的预热（burn-in）成本。这些改进纯粹源于自适应数据选择，借鉴了提升法（boosting）和反例学习（learning from counterexamples）的经典技术，且无需对提示词的分布或难度做出任何假设。

## Abstract
Chain-of-thought reasoning, where language models expend additional computation by producing thinking tokens prior to final responses, has driven significant advances in model capabilities. However, training these reasoning models is extremely costly in terms of both data and compute, as it involves collecting long traces of reasoning behavior from humans or synthetic generators and further post-training the model via reinforcement learning. Are these costs fundamental, or can they be reduced through better algorithmic design? We show that autocurriculum, where the model uses its own performance to decide which problems to focus training on, provably improves upon standard training recipes for both supervised fine-tuning (SFT) and reinforcement learning (RL). For SFT, we show that autocurriculum requires exponentially fewer reasoning demonstrations than non-adaptive fine-tuning, by focusing teacher supervision on prompts where the current model struggles. For RL fine-tuning, autocurriculum decouples the computational cost from the quality of the reference model, reducing the latter to a burn-in cost that is nearly independent of the target accuracy. These improvements arise purely from adaptive data selection, drawing on classical techniques from boosting and learning from counterexamples, and requiring no assumption on the distribution or difficulty of prompts.

---

## 论文详细总结（自动生成）

### 论文总结：Learning to Reason with Curriculum I: Provable Benefits of Autocurriculum

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了如何降低训练具有推理能力（如思维链 CoT）的语言模型的极高成本。目前，训练推理模型主要依赖两种高成本路径：
*   **SFT（监督微调）：** 需要大量高质量、长路径的人类或合成推理轨迹。
*   **RL（强化学习）：** 虽然不需要推理轨迹，但需要海量的模型采样（Rollouts）来通过验证器（Verifier）获取奖励。

研究的核心问题是：**“自动课程学习（Autocurriculum）”——即模型根据自身表现动态选择训练题目——是否能从理论上证明其在降低数据和计算成本方面的优越性？**

#### 2. 方法论：核心思想与算法流程
论文提出了基于“提升法（Boosting）”和“反例学习（Learning from Counterexamples）”思想的自动课程框架：

*   **核心思想：** 利用廉价的“结果验证器（Outcome Verifier）”来识别模型当前的薄弱环节，仅针对模型出错的提示词（Prompts）请求昂贵的“推理轨迹（CoT）”监督或进行强化学习更新。
*   **关键算法 - AutoTune (用于 SFT)：**
    1.  **迭代训练：** 算法分多个阶段运行，维护一个模型集合。
    2.  **自适应采样：** 在每一轮中，使用验证器检查当前模型集合在提示词上的表现。
    3.  **过滤与查询：** 仅对模型集合预测错误的提示词请求教师模型的 CoT 演示。
    4.  **弱学习器集成：** 每一轮训练一个新的“弱学习器”来修正之前的错误，最终通过多数投票或集成方式形成强推理模型。
*   **关键算法 - AutoTune.RL (用于 RL)：**
    *   将上述逻辑应用于强化学习，通过拒绝采样（Rejection Sampling）从参考模型中获取正确轨迹，但重点放在当前模型表现不佳的分布上。

#### 3. 实验设计与对比基准
**注意：这是一篇纯理论研究论文，其“实验”体现为数学证明和算法复杂度分析。**
*   **对比基准（Baseline）：** 主要对比了 Joshi 等人 (2025) 提出的非自适应（Non-adaptive）训练方法。
*   **场景设定：** 设定在自回归语言模型框架下，考虑了确定性模型类和随机性模型类。
*   **评估指标：** 样本复杂度（所需 CoT 演示的数量）和计算复杂度（生成的总轨迹数量）。

#### 4. 资源与算力
*   由于本文属于**理论计算学习理论（COLT）**范畴，文中**未提及**具体的 GPU 型号、数量或实际训练时长。
*   论文侧重于算法的**渐近复杂度分析**（如 $O(\cdot)$ 符号表示），而非实证性能。

#### 5. 实验数量与充分性
*   **理论完备性：** 论文通过严密的数学推导证明了在不同设定下的界限（Upper Bounds）。
*   **覆盖范围：** 涵盖了确定性策略、随机性策略、SFT 场景以及带参考模型的 RL 场景。
*   **客观性：** 证明过程不依赖于提示词的特定分布或问题的难度假设，具有很强的通用性。然而，作为理论文章，它缺乏在大规模真实 LLM（如 Llama 或 GPT）上的实证验证。

#### 6. 主要结论与发现
*   **SFT 的指数级优化：** 自动课程学习可以将所需的教师 CoT 演示数量从 $e^{\Theta(d/\epsilon)}$ 降低到 $e^{O(d)}$。这意味着监督成本几乎与目标准确率（$1-\epsilon$）解耦，实现了指数级的效率提升。
*   **RL 的成本解耦：** 在强化学习中，自动课程学习将计算成本与参考模型的质量（覆盖率 $C_{seq}$）解耦。计算成本从乘法关系（$C_{seq}/\epsilon$）变为加法关系（$C_{seq} + 1/\epsilon$），显著降低了预热后的训练负担。
*   **计算可行性：** 证明了即使在端到端反馈（仅结果对错）难以学习的情况下，少量的自适应 CoT 监督即可恢复计算上的可处理性。

#### 7. 优点：亮点与创新
*   **理论突破：** 首次为推理模型的自动课程学习提供了可证明的收益保证，解释了为什么动态数据选择在实践中有效。
*   **无需分布假设：** 与传统的积极学习（Active Learning）不同，该方法不需要对假设空间或数据分布做严格限制。
*   **跨场景适用：** 统一了 SFT 和 RL 两种主流后训练范式的优化逻辑。

#### 8. 不足与局限
*   **完美验证器假设：** 理论推导假设存在一个完美的“结果验证器”（如数学题答案检查或代码单元测试），但在开放域对话或主观推理中，这种验证器难以获得。
*   **唯一答案限制：** 部分证明（如定理 3.3）假设每个提示词有唯一的正确答案，这限制了其在多解问题上的应用。
*   **缺乏实证：** 论文未在真实的大模型训练流程中测试算法，理论上的指数级节省在实际神经网络优化（非 ERM Oracle）中可能受到梯度下降动力学的影响。
*   **模型类假设：** 复杂度分析依赖于 Natarajan 维度等经典学习理论指标，对于现代深度神经网络的参数化表示可能过于抽象。

（完）
