---
title: Do VLMs Need Vision Transformers? Evaluating State Space Models as Vision Encoders
title_zh: VLM 是否需要 Vision Transformer？评估状态空间模型作为视觉编码器的性能
authors: "Shang-Jui Ray Kuo, Paola Cascante-Bonilla"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19209v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 评估状态空间模型作为视觉语言模型中的视觉编码器
tldr: 本研究探讨了在视觉语言模型（VLM）中，状态空间模型（SSM）作为视觉编码器替代传统 Transformer 的潜力。通过在受控环境下系统评估 SSM 与 ViT 骨干网络，发现 SSM 在视觉问答和定位任务中表现优异，且在较小模型规模下仍具竞争力。研究还揭示了 ImageNet 准确率与 VLM 性能的不一致性，并提出了提升定位稳定性的策略，证明了 SSM 是构建高效 VLM 的强力选择。
selection_source: fresh_fetch
motivation: 探究状态空间模型（SSM）是否能作为视觉语言模型中视觉 Transformer（ViT）的高效替代方案。
method: 在统一的实验条件下，对比评估了 SSM 和 ViT 骨干网络在 VQA 和定位任务中的表现，并分析了密集任务预训练的影响。
result: SSM 骨干网络在多项任务中表现出最强性能，尤其在小规模模型下极具竞争力，同时发现视觉模型在定位任务中存在不稳定性。
conclusion: SSM 是视觉编码器的强力替代者，结合所提的稳定化策略可显著提升 VLM 的鲁棒性与性能。
---

## 摘要
大规模视觉-语言模型（VLM）通常使用冻结的视觉主干网络，其图像特征通过轻量级连接器映射到大语言模型中。虽然基于 Transformer 的编码器是标准的视觉主干，但我们探究状态空间模型（SSM）视觉主干是否可以成为一种强有力的替代方案。我们在受控设置下系统地评估了用于 VLM 的 SSM 视觉主干。在匹配的 ImageNet-1K 初始化下，SSM 主干在视觉问答（VQA）以及定位/接地（grounding/localization）方面均实现了最强的综合性能。我们进一步通过检测或分割训练对 SSM 和 ViT 系列主干进行适配，发现密集任务微调通常能提升各系列模型的性能；在适配后，SSM 主干在模型规模显著减小的情况下仍保持竞争力。我们还观察到：(i) 更高的 ImageNet 准确率或更大的主干网络并不一定能可靠地转化为更好的 VLM 性能；(ii) 某些视觉主干在定位任务中表现不稳定。基于这些发现，我们提出了稳定化策略，提高了这两个主干系列的鲁棒性，并强调 SSM 主干是 VLM 中基于 Transformer 的视觉编码器的强力替代方案。

## Abstract
Large vision--language models (VLMs) often use a frozen vision backbone, whose image features are mapped into a large language model through a lightweight connector. While transformer-based encoders are the standard visual backbone, we ask whether state space model (SSM) vision backbones can be a strong alternative. We systematically evaluate SSM vision backbones for VLMs in a controlled setting. Under matched ImageNet-1K initialization, the SSM backbone achieves the strongest overall performance across both VQA and grounding/localization. We further adapt both SSM and ViT-family backbones with detection or segmentation training and find that dense-task tuning generally improves performance across families; after this adaptation, the SSM backbone remains competitive while operating at a substantially smaller model scale. We further observe that (i) higher ImageNet accuracy or larger backbones do not reliably translate into better VLM performance, and (ii) some visual backbones are unstable in localization. Based on these findings, we propose stabilization strategies that improve robustness for both backbone families and highlight SSM backbones as a strong alternative to transformer-based vision encoders in VLMs.

---

## 论文详细总结（自动生成）

这篇论文对视觉语言模型（VLM）中视觉编码器的架构选择进行了深入探讨，以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在视觉语言模型（VLM）中，Vision Transformer (ViT) 是否是视觉编码器的唯一或最佳选择？状态空间模型（SSM，如 Mamba 系列）能否作为更高效、性能更强的替代方案？
*   **研究动机**：目前的 VLM 大多默认使用 ViT。然而，ViT 在处理高分辨率图像时计算开销巨大，且其全局注意力机制在捕捉精细空间信息（用于定位任务）方面可能并非最优。SSM 在密集预测任务中表现出良好的空间建模能力和线性缩放特性，因此作者希望通过受控实验验证 SSM 在 VLM 中的潜力。

### 2. 核心方法论
*   **核心思想**：采用“受控骨干网络替换”（Controlled Backbone Swaps）的方法。在保持大语言模型（Vicuna-7B）、连接器（Connector）和训练方案完全一致的前提下，仅更换视觉编码器。
*   **关键技术细节**：
    *   **模型架构**：遵循 LLaVA 风格，由视觉编码器、轻量级连接器（默认 2 层 MLP）和解码器语言模型组成。
    *   **对比家族**：包括纯 SSM（VMamba）、纯 Transformer（ViT）、层次化混合架构（MaxViT）和 SSM-Transformer 混合架构（MambaVision）。
    *   **密集任务适配**：研究了经过目标检测（Detection）或语义分割（Segmentation）预训练的骨干网络对 VLM 性能的影响。
    *   **稳定化策略**：针对发现的“定位崩溃”现象，提出了增加连接器容量（改为 3 层 MLP）和统一输入几何形状（使用 512x512 正方形输入）的方案。

### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **训练**：使用 LLaVA-v1.5 的 665K 多模态指令微调数据集。
    *   **评估（VQA 任务）**：VQA-v2, GQA, VizWiz, TextVQA, POPE, TallyQA。
    *   **评估（定位任务）**：RefCOCO, RefCOCO+, RefCOCOg, OCID-Ref。
*   **对比方法**：在 ImageNet-1K (IN1K) 预训练的相同基准下，对比了不同规模（Tiny, Small, Base, Large）的 ViT、VMamba、MaxViT 和 MambaVision。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 × NVIDIA H200 GPU**。
*   **训练细节**：采用全分片数据并行（FSDP）技术，混合精度（BF16），训练 1 个 epoch，全局 Batch Size 为 128。
*   **推理分析**：论文还对不同分辨率下的单 GPU 推理延迟和显存占用进行了详细分析。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   数十种不同架构和规模的骨干网络替换实验。
    *   针对特征提取阶段（Stage 3 vs Stage 4）的消融实验。
    *   针对预训练目标（分类 vs 检测 vs 分割）的对比实验。
    *   针对连接器容量和图像几何形状的稳定化实验。
    *   线性探测（Linear Probing）实验以分析特征质量。
*   **充分性评价**：实验设计非常严谨，通过严格控制变量（相同的优化器、随机种子、数据顺序等）确保了对比的客观性和公平性。

### 6. 主要结论与发现
*   **SSM 表现优异**：在匹配的 IN1K 预训练设置下，VMamba 在定位任务上显著优于 ViT，且在 VQA 任务上保持竞争力。
*   **指标不一致性**：ImageNet 的分类准确率并不是 VLM 性能的可靠预测指标。有时更大的模型或更高的分类精度反而导致 VLM 性能下降（过拟合于分类任务而丢失空间信息）。
*   **定位崩溃（Localization Collapse）**：某些检测预训练的高分辨率模型在接入 VLM 时会出现定位能力骤降。
*   **接口稳定性**：通过增加连接器层数和调整输入分辨率为正方形，可以有效修复定位崩溃，显著提升模型鲁棒性。
*   **效率优势**：VMamba 在处理高分辨率图像时，视觉阶段的 GPU 延迟增长比 ViT 更平缓。

### 7. 优点与亮点
*   **受控实验**：通过严格的变量控制，消除了训练方案不同带来的干扰，真正揭示了架构本身的影响。
*   **深入诊断**：不仅报告了性能，还深入分析了“定位崩溃”的原因，并给出了切实可行的工程稳定化建议。
*   **空间偏置**：证明了 SSM 的 2D 扫描机制天然带有更强的空间归纳偏置，有利于多模态理解。

### 8. 不足与局限
*   **预训练数据差异**：目前缺乏像 CLIP 或 SigLIP 那样在大规模对比学习数据上预训练的 SSM 模型，因此在与最顶尖的商业/开源 VLM 对比时，SSM 仍受限于预训练目标的差异。
*   **模型覆盖**：主要关注了 VMamba，虽然对比了 Vim，但对其他新兴 SSM 变体的覆盖有限。
*   **语言模型固定**：实验主要基于 Vicuna-7B，尚未验证在更大规模或不同架构的 LLM 上这些结论是否完全一致。

（完）
