---
title: Context Bootstrapped Reinforcement Learning
title_zh: 上下文引导强化学习
authors: "Saaket Agashe, Jayanth Srinivasa, Gaowen Liu, Ramana Kompella, Xin Eric Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18953v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 基于可验证奖励的强化学习
tldr: 针对可验证奖励强化学习（RLVR）中探索效率低下的问题，本文提出上下文引导强化学习（CBRL）。该方法在训练初期通过在提示词中随机加入少量示例来引导模型探索，并随训练进程逐渐减少示例比例至零。实验证明，CBRL能有效帮助模型内化推理模式，在多种推理任务和特定领域编程语言上显著提升了成功率和探索效率。
selection_source: fresh_fetch
motivation: 现有的RLVR方法在面对需要新颖推理或领域知识的任务时，常因难以生成成功样本而导致探索效率低下。
method: 提出CBRL方法，在训练提示词中动态加入少量示例，并采用从高到零的退火策略引导模型自主学习推理模式。
result: 在多个推理任务和特定编程语言Q上的实验显示，CBRL显著提高了模型的成功率和探索效率，且具有算法无关性。
conclusion: CBRL通过课程学习的方式成功解决了RLVR的冷启动问题，使模型能够在无需外部示例的情况下掌握复杂的推理能力。
---

## 摘要
基于可验证奖励的强化学习 (RLVR) 面临探索效率低下的问题，模型难以生成成功的采样轨迹，导致学习信号极少。对于需要获取新颖推理模式或特定领域知识的任务，这一挑战尤为严峻。为了解决这一问题，我们提出了上下文引导强化学习 (CBRL)，通过在训练提示词中随机预置少样本示例来增强 RLVR 训练。注入概率遵循一种课程学习方案：初始阶段保持高概率以引导早期探索，随后逐渐退火至零，使模型最终必须在没有辅助的情况下取得成功。这迫使策略内化示例中的推理模式，而非在测试时依赖它们。我们在两个模型系列和五个 Reasoning Gym 任务上验证了 CBRL。结果表明，CBRL 能够持续提高成功率，提供更好的探索效率，且具有算法无关性。我们进一步证明了 CBRL 在 Q 语言上的实际应用价值，Q 是一种与主流语言规范显著不同的特定领域编程语言。

## Abstract
Reinforcement Learning from Verifiable Rewards (RLVR) suffers from exploration inefficiency, where models struggle to generate successful rollouts, resulting in minimal learning signal. This challenge is particularly severe for tasks that require the acquisition of novel reasoning patterns or domain-specific knowledge. To address this, we propose Context Bootstrapped Reinforcement Learning (CBRL), which augments RLVR training by stochastically prepending few-shot demonstrations to training prompts. The injection probability follows a curriculum that starts high to bootstrap early exploration, then anneals to zero so the model must ultimately succeed without assistance. This forces the policy to internalize reasoning patterns from the demonstrations rather than relying on them at test time. We validate CBRL across two model families and five Reasoning Gym tasks. Our results demonstrate that CBRL consistently improves success rate, provides better exploration efficiency, and is algorithm-agnostic. We further demonstrate CBRL's practical applicability on Q, a domain-specific programming language that diverges significantly from mainstream language conventions.

---

## 论文详细总结（自动生成）

以下是对论文《Context Bootstrapped Reinforcement Learning》（上下文引导强化学习）的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：基于可验证奖励的强化学习（RLVR，如 DeepSeek-R1 所采用的范式）在处理复杂推理或冷门领域知识时，面临严重的**探索效率低下**问题。如果模型初始能力不足，无法生成正确的答案（Rollout），则无法获得奖励信号，导致训练难以起步或收敛极慢。
*   **核心问题**：如何在不改变强化学习本质、不增加推理成本的前提下，引导模型在训练初期突破“零奖励”困境，并最终实现独立推理。

### 2. 论文提出的方法论：CBRL
*   **核心思想**：利用大语言模型的**上下文学习（In-Context Learning, ICL）**能力作为“临时脚手架”。在训练过程中，随机在提示词前加入少量正确示例（Few-shot），引导模型生成正确轨迹，从而获得学习信号。
*   **关键技术细节**：
    *   **少样本示例库（Few-Shot Bank）**：为每个任务构建一个包含问题、推理过程和答案的示例库。
    *   **随机上下文注入**：在每个训练步骤中，以概率 $p_i$ 决定是否在当前问题前拼接 $k$ 个示例。
    *   **课程退火策略（Curriculum Schedule）**：注入概率 $p_i$ 随训练进程线性退火（例如从 0.5 降至 0）。
*   **算法流程**：
    1.  从任务库抽取小批量问题。
    2.  根据当前步数的概率 $p_i$ 进行伯努利采样，决定是否注入示例。
    3.  模型生成回复并根据环境反馈（如代码执行结果、数学答案）获得奖励。
    4.  使用策略梯度算法（如 GRPO 或 RLOO）更新模型参数。
    5.  随着训练进行，示例出现的频率降低，迫使模型将示例中的推理模式“内化”到参数中。

### 3. 实验设计
*   **数据集/场景**：
    *   **Reasoning Gym**：包含 ARC-1D（模式识别）、矩阵操作、单词排序、逆向拼写、24点游戏（Puzzle-24）五个合成推理任务。
    *   **Q Programming**：针对金融领域特定语言 Q 语言的代码生成任务。该语言语法独特（如从右向左计算），预训练数据极少。
*   **Benchmark 与对比方法**：
    *   **基准（Baseline）**：预训练模型的零样本和少样本表现。
    *   **对比算法**：标准的 GRPO（群体相对策略优化）和 RLOO（REINFORCE Leave-One-Out）。
    *   **对比维度**：成功率（Success Rate）、测试通过率（Pass@1）、训练奖励曲线。

### 4. 资源与算力
*   **Reasoning Gym 实验**：使用 4 张 **NVIDIA A6000** GPU，采用 FSDP（完全分片数据并行）策略。
*   **Q 编程实验**：使用 4 张 **NVIDIA GH200** GPU。
*   **训练规模**：Reasoning Gym 任务通常训练 500 个 Step，Batch Size 为 32。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 个模型系列（Qwen2.5-3B, Llama-3.2-3B）、5 个推理任务和 1 个真实编程领域，共 10 组模型-环境组合。
*   **消融实验**：对初始注入概率 $p_{start}$ 进行了详细消融（测试了从 0 到 1.0 的不同取值），并验证了在不同 RL 算法（GRPO vs RLOO）下的通用性。
*   **充分性评价**：实验设计较为充分，通过多随机种子（3-5个）取平均值，并提供了定性的思维链（CoT）对比分析，客观地展示了模型从“模仿示例”到“自主推理”的转变。

### 6. 论文的主要结论与发现
*   **性能提升**：CBRL 在所有测试任务上均优于标准 GRPO。在 Q 编程任务中，成功率从 5.0% 提升至 **26.3%**。
*   **解决冷启动**：CBRL 在训练早期能获得显著更高的平均奖励，有效解决了探索瓶颈。
*   **能力内化**：当注入概率退火至零后，模型性能并未崩溃，说明模型确实学到了推理逻辑，而非简单依赖上下文示例。
*   **算法无关性**：CBRL 作为一个数据增强层，可以无缝集成到 GRPO 和 RLOO 等多种策略梯度算法中。

### 7. 优点
*   **简单高效**：不需要复杂的离线数据收集或昂贵的教师模型蒸馏，仅通过修改输入分布即可实现。
*   **零推理成本**：由于训练结束时注入概率为零，模型在推理阶段不需要任何 Few-shot 示例，不增加 Token 消耗。
*   **鲁棒性强**：在不同架构（Qwen/Llama）和不同任务（合成推理/真实编程）上表现一致。

### 8. 不足与局限
*   **示例库依赖**：仍需要为每个任务准备少量（如 20-50 个）高质量的正确示例及其推理轨迹。
*   **退火策略单一**：目前采用固定的线性退火，可能不是最优的；对于不同难度的任务，可能需要动态调整注入概率。
*   **任务局限性**：实验主要集中在短程推理和特定编程语言，对于超长程决策或多步骤智能体任务的有效性尚待验证。
*   **失败案例**：在 RLOO 算法下的某些特定任务（如 ARC-1D）中，CBRL 出现了性能轻微下降，暗示示例的选择与任务结构的匹配度至关重要。

（完）
