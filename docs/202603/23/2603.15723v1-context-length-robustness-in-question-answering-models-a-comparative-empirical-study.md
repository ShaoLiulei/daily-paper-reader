---
title: "Context-Length Robustness in Question Answering Models: A Comparative Empirical Study"
title_zh: 问答模型中的上下文长度鲁棒性：一项比较实证研究
authors: "Trishita Dhara, Siddhesh Sheth"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15723v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 大语言模型上下文长度鲁棒性的实证研究
tldr: 本研究针对大语言模型在长且多噪背景下的鲁棒性进行了实证研究。通过在SQuAD和HotpotQA基准上系统性增加无关上下文，评估模型在保持答案信号不变时的准确率变化。研究发现，随着上下文增长，模型性能普遍下降，且多跳推理任务（HotpotQA）的性能衰减程度几乎是单跨度提取任务（SQuAD）的两倍，揭示了任务类型对上下文长度鲁棒性的显著影响。
selection_source: fresh_fetch
motivation: 旨在探究大语言模型在处理包含大量无关信息的长文本时，其问答性能如何随上下文长度增加而变化。
method: 通过在SQuAD和HotpotQA任务中系统性地增加无关上下文并保持答案信息不变，来隔离上下文长度对模型准确率的影响。
result: 实验表明模型性能随上下文增长而下降，且多跳推理任务的准确率跌幅远大于单跨度提取任务。
conclusion: 多跳推理对上下文稀释更为敏感，建议在评估模型可靠性（如RAG应用）时应明确考察其上下文长度鲁棒性。
---

## 摘要
大语言模型越来越多地被部署在相关信息嵌入长且多噪声上下文的场景中。尽管如此，在不同的问答任务中，模型对不断增长的上下文长度的鲁棒性仍未得到充分理解。在这项工作中，我们利用两个广泛使用的基准测试（SQuAD 和 HotpotQA），对大语言模型的上下文长度鲁棒性进行了受控实证研究。我们通过系统地增加无关上下文的数量，同时保留包含答案的信号，来评估模型准确率随总上下文长度变化的情况。这使我们能够将上下文长度的影响与任务难度的变化隔离开来。我们的结果显示，随着上下文长度的增加，性能会出现一致的下降，且与单跨度抽取任务相比，多跳推理任务的性能下降幅度明显更大。特别是，在等效的上下文扩展下，HotpotQA 的准确率下降几乎是 SQuAD 的两倍。这些发现强调了鲁棒性在不同任务间的差异，并表明多跳推理特别容易受到上下文稀释的影响。我们认为，在评估模型可靠性时，应明确评估上下文长度鲁棒性，特别是对于涉及长文档或检索增强生成的应用。

## Abstract
Large language models are increasingly deployed in settings where relevant information is embedded within long and noisy contexts. Despite this, robustness to growing context length remains poorly understood across different question answering tasks. In this work, we present a controlled empirical study of context-length robustness in large language models using two widely used benchmarks: SQuAD and HotpotQA.   We evaluate model accuracy as a function of total context length by systematically increasing the amount of irrelevant context while preserving the answer-bearing signal. This allows us to isolate the effect of context length from changes in task difficulty. Our results show a consistent degradation in performance as context length increases, with substantially larger drops observed on multi-hop reasoning tasks compared to single-span extraction tasks. In particular, HotpotQA exhibits nearly twice the accuracy degradation of SQuAD under equivalent context expansions.   These findings highlight task-dependent differences in robustness and suggest that multi-hop reasoning is especially vulnerable to context dilution. We argue that context-length robustness should be evaluated explicitly when assessing model reliability, especially for applications involving long documents or retrieval-augmented generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Context-Length Robustness in Question Answering Models: A Comparative Empirical Study》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）在检索增强生成（RAG）和长文档分析中的广泛应用，模型经常需要在包含大量无关信息的长上下文中提取关键信息。
*   **核心问题**：当答案信息（信号）保持不变，而仅仅通过增加无关文本（噪声）来扩展上下文长度时，模型的性能会如何变化？这种鲁棒性在不同类型的任务（如简单提取 vs. 复杂推理）中是否存在差异？
*   **整体含义**：论文旨在通过受控实验，量化上下文长度对模型准确率的独立影响，揭示“上下文稀释”现象对多步推理任务的严重干扰。

### 2. 论文提出的方法论
*   **核心思想**：采用“信号+噪声”的受控构造法。将包含答案的文本块（Signal）固定，通过填充无关的干扰文本（Distractors）来精确控制总长度。
*   **关键技术细节**：
    *   **信号提取**：从原始上下文中提取包含正确答案的 256 标记（tokens）窗口。
    *   **干扰项注入**：从同数据集的其他实例中采样 128 标记的文本块作为干扰项，并排除包含答案字符串的块以防泄露。
    *   **长度控制**：设定四个目标长度梯度：256、512、1024、2048 标记。
*   **数学建模**：
    *   定义准确率函数 $Acc_M(L)$ 为长度 $L$ 的函数。
    *   提出**信噪比视角**：假设 Transformer 的注意力分配与信号占比成正比。对于需要 $k$ 步推理的任务，成功概率 $p_k(L)$ 随长度 $L$ 的增加呈幂律衰减（即 $L$ 越大，多跳推理失败概率指数级上升）。

### 3. 实验设计
*   **数据集**：
    *   **SQuAD**：代表单跨度提取任务（Single-span extraction），侧重局部证据查找。
    *   **HotpotQA**：代表多跳推理任务（Multi-hop reasoning），需要整合多个文档的信息。
*   **评估模型**：对比了不同规模的指令微调模型：**GPT-4.1** 和 **GPT-4.1-mini**。
*   **Benchmark 与指标**：使用验证集的子集（n=200），采用**精确匹配（Exact Match, EM）**作为核心评价指标。
*   **对比方式**：在相同模型、相同问题下，对比不同上下文长度（256 到 2048）的性能跌幅。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **原因**：该研究主要基于 API 调用（GPT-4 系列模型）进行推理评估，而非模型训练或微调，因此算力消耗主要体现为 API 请求成本和推理延迟。

### 5. 实验数量与充分性
*   **实验规模**：每个数据集抽取 200 个实例，涵盖 2 个模型、2 个数据集、4 种长度，总计进行了约 1600 次模型查询。
*   **充分性与客观性**：
    *   **客观性**：通过固定随机种子和确定性解码（Temperature=0）确保了结果的可重复性。
    *   **公平性**：对所有模型使用相同的提示词和上下文构造逻辑。
    *   **统计学支持**：引入了 95% 自举置信区间（Bootstrap Confidence Intervals）来证明性能下降趋势在统计上是显著的，而非随机波动。

### 6. 论文的主要结论与发现
*   **性能普遍下降**：随着上下文长度增加，所有模型在所有任务上的表现均呈下降趋势。
*   **任务依赖性显著**：多跳推理（HotpotQA）对长度增加极其敏感，准确率跌幅高达 **24.5% - 27%**；而单步提取（SQuAD）表现稳健，跌幅仅为 **2.5% - 6.5%**。
*   **规模并非万能药**：虽然大模型（GPT-4.1）的绝对准确率高于小模型（GPT-4.1-mini），但两者在面对长上下文时的**性能衰减趋势几乎一致**，说明增加参数量并不能消除上下文稀释带来的结构性问题。

### 7. 优点（亮点）
*   **变量控制严谨**：通过固定 256 标记的信号窗口，成功隔离了“任务难度”和“上下文长度”两个变量，证明了性能下降纯粹是由长度/噪声引起的。
*   **理论解释简洁**：提出的信噪比与多跳推理成功率的数学关联，为观察到的实验现象提供了直观的机械论解释。
*   **实用性强**：研究结果对 RAG 系统的设计有直接指导意义，强调了减少无关上下文对复杂推理的重要性。

### 8. 不足与局限
*   **长度覆盖有限**：实验最高仅测试到 2048 标记，未涉及目前 LLM 宣称的 100k+ 甚至百万级别的超长上下文。
*   **模型多样性不足**：仅测试了 GPT-4 系列，未涵盖 Claude、Gemini 或 Llama 等其他架构或开源模型。
*   **噪声类型单一**：干扰项仅为无关文本，未考虑更具挑战性的“反事实信息”或“矛盾信息”等对抗性噪声。
*   **样本量较小**：每组实验仅 200 个样本，虽然有置信区间支撑，但在捕捉长尾错误分布方面可能存在局限。

（完）
