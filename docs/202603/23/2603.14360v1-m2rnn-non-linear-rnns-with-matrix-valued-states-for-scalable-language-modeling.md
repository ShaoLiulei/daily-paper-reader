---
title: "M$^2$RNN: Non-Linear RNNs with Matrix-Valued States for Scalable Language Modeling"
title_zh: M$^2$RNN：具有矩阵值状态的非线性 RNN，用于可扩展语言建模
authors: "Mayank Mishra, Shawn Tan, Ion Stoica, Joseph Gonzalez, Tri Dao"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14360v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 用于可扩展语言建模的非线性循环神经网络架构
tldr: 针对 Transformer 在实体追踪等复杂任务上的表达能力限制，本文提出 M$^2$RNN。该架构采用矩阵值隐藏状态和非线性状态转移，通过扩展状态规模并利用张量核心提升效率。实验表明，M$^2$RNN 在大规模语言建模和长文本泛化中表现优异，尤其在混合架构中以更小的状态规模超越了现有 SOTA 模型，证明了非线性 RNN 在可扩展语言模型中的潜力。
selection_source: fresh_fetch
motivation: 旨在克服 Transformer 在处理实体追踪和代码执行等高复杂度任务时表达能力受限的问题。
method: 引入具有矩阵值隐藏状态和非线性状态转移机制的 M$^2$RNN 架构，并优化了张量核心的利用效率。
result: 在 7B MoE 混合模型中，M$^2$RNN 以更小的状态规模取得了更优的困惑度，并在 LongBench 长文本评测中领先 SOTA 架构达 8 分。
conclusion: 非线性 RNN 层是构建高效、可扩展且具备强长文本泛化能力的语言模型的重要基石。
---

## 摘要
Transformer 具有高度并行性，但受限于 TC$^0$ 复杂度类的计算，排除了诸如实体追踪和代码执行等经证明需要更强表达能力的任务。受此限制启发，我们重新审视了用于语言建模的非线性循环神经网络 (RNN)，并引入了矩阵到矩阵 RNN (M$^2$RNN)：一种具有矩阵值隐藏状态和强表达能力非线性状态转移的架构。我们证明了非线性 RNN 的语言建模性能受其状态大小的限制。我们还展示了状态大小扩展机制如何实现对张量核心 (tensor cores) 的高效利用。实验表明，M$^2$RNN 在训练中未见过的序列长度上实现了完美的状态追踪泛化。这些优势也转化到了大规模语言建模中。在循环层与注意力层交替的混合设置中，Hybrid M$^2$RNN 在 7B MoE 模型上比等效的 Gated DeltaNet 混合模型在困惑度上领先 0.4-0.5 个点，同时循环层使用的状态大小缩小了 3 倍。值得注意的是，在现有混合架构中即使仅将一个循环层替换为 M$^2$RNN，也能获得与 Hybrid M$^2$RNN 相当的准确率提升，且对训练吞吐量的影响极小。此外，带有一个 M$^2$RNN 层的 Hybrid Gated DeltaNet 模型还实现了卓越的长文本泛化能力，在 LongBench 上比最先进的混合线性注意力架构高出多达 8 个点。总之，这些结果确立了非线性 RNN 层作为构建高效且可扩展语言模型的一种极具吸引力的基础组件。

## Abstract
Transformers are highly parallel but are limited to computations in the TC$^0$ complexity class, excluding tasks such as entity tracking and code execution that provably require greater expressive power. Motivated by this limitation, we revisit non-linear Recurrent Neural Networks (RNNs) for language modeling and introduce Matrix-to-Matrix RNN (M$^2$RNN): an architecture with matrix-valued hidden states and expressive non-linear state transitions. We demonstrate that the language modeling performance of non-linear RNNs is limited by their state size. We also demonstrate how the state size expansion mechanism enables efficient use of tensor cores. Empirically, M$^2$RNN achieves perfect state tracking generalization at sequence lengths not seen during training. These benefits also translate to large-scale language modeling. In hybrid settings that interleave recurrent layers with attention, Hybrid M$^2$RNN outperforms equivalent Gated DeltaNet hybrids by $0.4$-$0.5$ perplexity points on a 7B MoE model, while using $3\times$ smaller state sizes for the recurrent layers. Notably, replacing even a single recurrent layer with M$^2$RNN in an existing hybrid architecture yields accuracy gains comparable to Hybrid M$^2$RNN with minimal impact on training throughput. Further, the Hybrid Gated DeltaNet models with a single M$^2$RNN layer also achieve superior long-context generalization, outperforming state-of-the-art hybrid linear attention architectures by up to $8$ points on LongBench. Together, these results establish non-linear RNN layers as a compelling building block for efficient and scalable language models.

---

## 论文详细总结（自动生成）

以下是对论文《M$^2$RNN: Non-Linear RNNs with Matrix-Valued States for Scalable Language Modeling》的结构化深度总结：

### 1. 研究动机与背景
*   **核心问题**：Transformer 架构虽然在并行化和检索方面表现卓越，但在计算复杂度上被限制在 $TC^0$ 类，无法处理需要更强表达能力（如 $NC^1$ 类）的任务，如复杂的实体追踪、代码执行和置换组合。
*   **现有技术的局限**：
    *   **线性 RNN/SSM**（如 Mamba-2）：虽然推理高效且可并行训练，但表达能力受限，且在长文本检索任务中表现较弱。
    *   **传统非线性 RNN**（如 LSTM/GRU）：虽然表达能力强，但由于隐藏状态仅为向量（Vector-valued），导致语言建模性能差、检索能力弱，且由于无法利用并行扫描算法，训练效率极低。
*   **研究目标**：设计一种既具备非线性 RNN 的强表达能力，又能通过矩阵值状态（Matrix-valued states）扩展容量，并能高效利用现代硬件（张量核心）的新型架构。

### 2. 方法论：M$^2$RNN 架构
*   **核心思想**：引入“矩阵到矩阵”的非线性循环机制。将隐藏状态从向量扩展为矩阵 $H_t \in \mathbb{R}^{K \times V}$，并结合非线性激活函数和外积更新规则。
*   **关键技术细节**：
    *   **循环公式**：
        1.  更新项：$Z_t = \tanh(H_{t-1}W + k_t v_t^\top)$，其中 $W$ 是学习的状态转移矩阵。
        2.  门控状态更新：$H_t = f_t H_{t-1} + (1 - f_t)Z_t$，其中 $f_t$ 是独立于前一状态的遗忘门（便于并行预计算）。
        3.  读取输出：$y_t = H_t^\top q_t + w_r \odot v_t$。
    *   **状态扩展机制**：借鉴线性注意力的外积形式（$k_t v_t^\top$），在不显著增加参数量的情况下极大扩展了状态容量。
    *   **硬件优化**：
        *   **张量核心利用**：矩阵循环形式天然适合 GPU 的 WMMA/WGMMA 指令，无需像向量 RNN 那样为了对齐而进行填充（Padding），避免了算力浪费。
        *   **定制内核**：使用 Triton 开发了前向和后向算子，通过重计算（Recomputation）降低显存占用。
    *   **混合架构**：提出将 M$^2$RNN 层与注意力层、MLP 层交替组合（如 1 层注意力 + 7 层循环层）。

### 3. 实验设计
*   **模型规模**：
    *   **410M 稠密模型**（24 层）。
    *   **7B 混合专家模型 (MoE)**（40 层，激活参数 1.1B）。
*   **数据集**：使用 Nemotron-CC-v2 的高质量子集，训练 100B tokens。
*   **对比基准 (Baselines)**：
    *   **同质架构**：Mamba-2、Gated DeltaNet、传统 RNN、GRU。
    *   **混合架构**：Transformer++、Hybrid Mamba-2、Hybrid Gated DeltaNet。
*   **评测任务**：
    *   **基础能力**：LM Eval Harness（HellaSwag, ARC 等 10+ 任务）、Wikitext 困惑度。
    *   **长文本与检索**：RULER（大海捞针变体）、LongBench（长文本摘要、代码等）。
    *   **状态追踪**：$S_3$ 置换群组合任务。

### 4. 资源与算力
*   **硬件设备**：
    *   410M 模型：使用 **32 张 NVIDIA H100 GPU**。
    *   7B MoE 模型：使用 **64 张 NVIDIA H100 GPU**。
*   **训练细节**：上下文长度 4096，使用 AdamW 优化器，混合精度（BF16 训练，FP32 累积）。
*   **吞吐量优化**：集成了 FlashAttention-3 和 SonicMoE。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础状态追踪合成任务到 7B 规模的真实语言建模。
*   **消融实验**：深入探讨了状态大小（矩阵 vs 向量）对性能的影响，以及遗忘门的作用。
*   **公平性**：所有对比模型使用相同的训练数据、相同的 Token 顺序和一致的超参数（如学习率、Batch Size），确保了结果的客观性。
*   **充分性**：不仅对比了最终精度，还详细测试了不同上下文长度下的训练吞吐量和推理泛化能力。

### 6. 主要结论与发现
*   **状态容量是关键**：非线性 RNN 过去表现差并非因为“非线性”，而是因为状态太小。M$^2$RNN 通过矩阵状态解决了这一问题。
*   **卓越的泛化性**：在 $S_3$ 任务中，M$^2$RNN 实现了对训练时未见长度的完美泛化，远超线性 RNN。
*   **混合架构的最优选**：在 7B MoE 规模下，Hybrid M$^2$RNN 在困惑度上优于 Hybrid Gated DeltaNet 0.5 个点，且状态大小仅为后者的 1/3。
*   **“画龙点睛”效应**：在现有混合模型中，**仅需替换一个循环层为 M$^2$RNN**，即可获得显著的准确率提升（LongBench 提升高达 8 分），且吞吐量损失仅约 6%。

### 7. 优点
*   **理论与实践结合**：从计算复杂度理论出发，解决了 Transformer 和线性 RNN 的表达能力缺陷。
*   **硬件友好性**：通过矩阵化设计，解决了传统 RNN 在现代 GPU 上因 Padding 导致的效率低下问题。
*   **系统级创新**：提供了完整的分布式训练方案，包括两种针对张量并行（TP）的优化策略（拓扑感知型和拓扑独立型）。

### 8. 不足与局限
*   **计算开销**：尽管优化了硬件利用率，但非线性循环的计算量（FLOPs）仍高于线性 RNN，导致同质架构下的训练速度较慢。
*   **内核优化空间**：作者提到目前的 Triton 实现仍有优化余地，尚未达到线性 RNN 算子的极致性能。
*   **超长文本挑战**：虽然线性复杂度在理论上支持无限长度，但在极长序列下的非线性梯度稳定性仍需在大规模生产环境中进一步验证。

（完）
