---
title: "CRAFT: Aligning Diffusion Models with Fine-Tuning Is Easier Than You Think"
title_zh: CRAFT：通过微调对齐扩散模型比你想象的更简单
authors: "Zening Sun, Zhengpeng Xie, Lichen Bai, Shitong Shao, Shuo Yang, Zeke Xie"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18991v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 对齐扩散模型的微调范式
tldr: CRAFT是一种轻量级扩散模型微调范式，旨在解决现有对齐方法（如SFT和DPO）对高质量数据依赖性强且计算效率低的问题。该方法通过复合奖励过滤（CRF）筛选高质量一致性数据，并结合增强版SFT进行训练。实验证明，仅需100个样本，CRAFT在性能上即可超越主流偏好优化方法，且收敛速度提升11-220倍，实现了极高的数据与计算效率。
selection_source: fresh_fetch
motivation: 现有的扩散模型对齐方法面临高质量数据获取成本高、偏好数据集质量不一以及计算效率低下的挑战。
method: 提出CRAFT范式，利用复合奖励过滤（CRF）构建高质量训练集，并执行增强型有监督微调（SFT）。
result: 仅使用100个样本，CRAFT的性能便优于使用数千个样本的SOTA偏好优化方法，且收敛速度快11-220倍。
conclusion: CRAFT证明了通过精选少量高质量数据进行微调，可以比复杂的偏好优化更高效、更有效地实现扩散模型对齐。
---

## 摘要
扩散模型的对齐在生成高质量、符合人类偏好的图像方面取得了显著突破。现有技术，如监督微调（SFT）和 DPO 式偏好优化，已成为微调扩散模型的原则性工具。然而，SFT 依赖于获取成本高昂的高质量图像，而 DPO 式方法则依赖于大规模偏好数据集，这些数据集的质量往往不一致。除了数据依赖性外，这些方法还受到计算效率低下的进一步限制。为了应对这两个挑战，我们提出了复合奖励辅助微调（CRAFT），这是一种轻量级且强大的微调范式，在保持计算效率的同时显著减少了所需的训练数据。它首先利用复合奖励过滤（CRF）技术构建高质量且一致的训练数据集，然后执行 SFT 的增强变体。我们还在理论上证明了 CRAFT 实际上优化了基于组的强化学习的下界，在基于选择数据的 SFT 与强化学习之间建立了原则性的联系。我们广泛的实证结果表明，仅使用 100 个样本的 CRAFT 就能轻松超越使用数千个偏好对样本的最新 SOTA 偏好优化方法。此外，CRAFT 的收敛速度甚至比基准偏好优化方法快 11-220 倍，突显了其极高的效率。

## Abstract
Aligning Diffusion models has achieved remarkable breakthroughs in generating high-quality, human preference-aligned images. Existing techniques, such as supervised fine-tuning (SFT) and DPO-style preference optimization, have become principled tools for fine-tuning diffusion models. However, SFT relies on high-quality images that are costly to obtain, while DPO-style methods depend on large-scale preference datasets, which are often inconsistent in quality. Beyond data dependency, these methods are further constrained by computational inefficiency. To address these two challenges, we propose Composite Reward Assisted Fine-Tuning (CRAFT), a lightweight yet powerful fine-tuning paradigm that requires significantly reduced training data while maintaining computational efficiency. It first leverages a Composite Reward Filtering (CRF) technique to construct a high-quality and consistent training dataset and then perform an enhanced variant of SFT. We also theoretically prove that CRAFT actually optimizes the lower bound of group-based reinforcement learning, establishing a principled connection between SFT with selected data and reinforcement learning. Our extensive empirical results demonstrate that CRAFT with only 100 samples can easily outperform recent SOTA preference optimization methods with thousands of preference-paired samples. Moreover, CRAFT can even achieve 11-220$\times$ faster convergences than the baseline preference optimization methods, highlighting its extremely high efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《CRAFT: Aligning Diffusion Models with Fine-Tuning Is Easier Than You Think》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何高效地将文本生成图像（T2I）扩散模型与人类偏好（美学、指令遵循等）对齐。
*   **研究背景**：
    *   **现有方法局限性**：有监督微调（SFT）依赖昂贵的高质量人工标注数据；DPO 类方法依赖大规模且质量参差不齐的偏好对数据集；在线强化学习方法（如 SPO）则因频繁的在线采样和评估导致计算开销巨大。
    *   **研究动机**：探索一种既能降低数据依赖，又能减少计算成本的轻量级对齐范式。

### 2. 方法论：CRAFT 框架
CRAFT（Composite Reward Assisted Fine-Tuning）的核心思想是通过**复合奖励过滤**筛选极少量高质量数据，并结合**加权 SFT** 进行训练。
*   **数据构建与过滤（CRF）**：
    1.  **提示词增强**：使用大语言模型（Qwen-Plus）将原始提示词改写为更详细的描述性变体。
    2.  **图像生成**：利用基础模型生成多组候选图像。
    3.  **复合奖励过滤**：整合 HPS v2.1、PickScore 和 AES 三种奖励模型，通过严格的过滤规则（如 Ihpa，要求所有指标均优于原始提示词生成的图像）筛选出最符合偏好的样本。
*   **加权 SFT 微调**：
    *   计算每个样本的“组内优势值”（Normalized Advantage），即该样本奖励值减去组内平均值并除以标准差。
    *   将该优势值作为梯度权重，对扩散模型进行微调。
*   **理论支撑**：论文从数学上证明了这种基于优势值加权的 SFT 实际上是在优化**组内强化学习（Group-based RL）的下界**，为 SFT 与强化学习之间建立了理论联系。

### 3. 实验设计
*   **基础模型**：Stable Diffusion v1.5 (SD1.5) 和 Stable Diffusion XL (SDXL)。
*   **测试基准（Benchmarks）**：HPDv2（3200提示词）、Parti-Prompts（1632提示词）、Pick-a-Pic 验证集（500提示词）以及 GenEval（评估组合能力）。
*   **对比方法**：包括 Diffusion-DPO、Diffusion-KTO、MaPO、SmPO 和 SPO 等主流对齐算法。
*   **评估指标**：HPS v2.1、AES（美学评分）、ImageReward（图像质量）、MPS（多维人类偏好评分）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练时长**：
    *   **SDXL**：微调阶段仅需约 **4.0 GPU 小时**。
    *   **SD1.5**：微调阶段仅需约 **1.9 GPU 小时**。
*   **效率对比**：在 SDXL 上，CRAFT 比 SPO 快 15.8 倍，比 Diffusion-DPO 快 159.5 倍；在 SD1.5 上，比 Diff-KTO 快 218.7 倍。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础模型到大规模模型的验证，并在多个独立测试集上进行了测试。
*   **消融实验**：
    *   **数据筛选策略**：对比了全量数据、低分数据、随机数据和 Top-100 数据，证明了“少而精”的数据（仅 100 样本）效果最好。
    *   **奖励模型组合**：验证了不同奖励模型组合对最终性能的影响。
*   **充分性与公平性**：实验设置了严格的对比条件，使用了未参与训练的第三方指标（如 MPS、ImageReward）来评估泛化能力，并进行了人工评测（User Study），结果具有较高的客观性。

### 6. 主要结论与发现
*   **对齐并不需要大数据**：仅需 **100 个** 精选的高质量样本，CRAFT 就能在多个指标上超越使用数千甚至数十万样本的 SOTA 方法。
*   **极高的计算效率**：CRAFT 实现了 11 到 220 倍的收敛加速，显著降低了对齐扩散模型的门槛。
*   **强大的泛化与组合能力**：在 GenEval 等复杂指令遵循测试中，CRAFT 表现优异，证明其不仅提升了美学，还增强了模型对颜色、位置、数量等属性的理解。

### 7. 优点与亮点
*   **简单高效**：摒弃了复杂的偏好对构建和在线采样，回归到加权 SFT，易于实现。
*   **理论与实证结合**：不仅有显著的实验提升，还提供了 RL 下界优化的理论解释。
*   **数据利用率极高**：通过 CRF 策略实现了极低样本量下的高性能对齐。

### 8. 不足与局限
*   **离线限制**：CRAFT 本质上是离线微调，依赖于预先构建的静态数据集，可能无法像在线 RL 那样持续探索模型的生成上限。
*   **图像级过滤**：目前的过滤是在图像层面进行的，未考虑扩散模型初始噪声对质量的影响。
*   **模态局限**：目前仅在文本转图像（T2I）任务上验证，尚未扩展到视频（T2V）或 3D 生成等更复杂的时空对齐任务。

（完）
