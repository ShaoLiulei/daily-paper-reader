---
title: "SpikeCLR: Contrastive Self-Supervised Learning for Few-Shot Event-Based Vision using Spiking Neural Networks"
title_zh: SpikeCLR：基于脉冲神经网络的小样本事件视觉对比自监督学习
authors: "Maxime Vaillant, Axel Carlier, Lai Xing Ng, Christophe Hurter, Benoit R. Cottereau"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16338v1"
tags: ["query:snn"]
score: 10.0
evidence: 用于事件视觉和自监督学习的脉冲神经网络
tldr: 针对事件相机数据标注稀缺的问题，本文提出SpikeCLR框架，首次将对比自监督学习引入脉冲神经网络（SNN）。通过设计针对事件流的空间、时间和极性增强策略，该方法在无标注数据上学习鲁棒的视觉表征，显著提升了SNN在少样本和半监督视觉任务中的性能，为低功耗神经形态视觉应用提供了高效的表征学习方案。
selection_source: fresh_fetch
motivation: 旨在解决事件相机数据大规模标注困难的问题，以充分释放脉冲神经网络在神经形态硬件上的低功耗潜力。
method: 提出一种基于对比学习的SNN自监督框架，利用代理梯度训练并结合了专门针对事件流特性的时空及极性数据增强技术。
result: 在CIFAR10-DVS等多个基准测试中，该方法在少样本和半监督场景下均优于监督学习，并证明了时空增强对学习不变性的重要性。
conclusion: 自监督预训练能有效提升SNN在标签匮乏环境下的特征表达能力，为构建高效的事件驱动视觉模型提供了新路径。
---

## 摘要
事件视觉传感器在高速感知方面具有显著优势，包括微秒级的时间分辨率、高动态范围和低功耗。当与脉冲神经网络（SNNs）结合时，它们可以部署在类脑硬件上，从而在嵌入式系统中实现高能效应用。然而，由于有效训练此类模型所需的大规模标记数据集稀缺，这一潜力受到了严重限制。在这项工作中，我们介绍了 SpikeCLR，这是一个对比自监督学习框架，使 SNN 能够从无标记的事件数据中学习鲁棒的视觉表示。我们利用代理梯度训练将先前的基于帧的方法适配到脉冲领域，并引入了一套利用空间、时间和极性变换的事件特定增强方法。通过在 CIFAR10-DVS、N-Caltech101、N-MNIST 和 DVS-Gesture 基准测试上的广泛实验，我们证明了在低数据量情况下，自监督预训练结合随后的微调优于监督学习，在小样本和半监督设置中取得了持续的提升。我们的消融研究表明，结合空间和时间增强对于学习事件数据中有效的时空不变性至关重要。我们进一步展示了所学习的表示可以跨数据集迁移，为在标签稀缺环境下构建强大的基于事件的模型做出了贡献。

## Abstract
Event-based vision sensors provide significant advantages for high-speed perception, including microsecond temporal resolution, high dynamic range, and low power consumption. When combined with Spiking Neural Networks (SNNs), they can be deployed on neuromorphic hardware, enabling energy-efficient applications on embedded systems. However, this potential is severely limited by the scarcity of large-scale labeled datasets required to effectively train such models. In this work, we introduce SpikeCLR, a contrastive self-supervised learning framework that enables SNNs to learn robust visual representations from unlabeled event data. We adapt prior frame-based methods to the spiking domain using surrogate gradient training and introduce a suite of event-specific augmentations that leverage spatial, temporal, and polarity transformations. Through extensive experiments on CIFAR10-DVS, N-Caltech101, N-MNIST, and DVS-Gesture benchmarks, we demonstrate that self-supervised pretraining with subsequent fine-tuning outperforms supervised learning in low-data regimes, achieving consistent gains in few-shot and semi-supervised settings. Our ablation studies reveal that combining spatial and temporal augmentations is critical for learning effective spatio-temporal invariances in event data. We further show that learned representations transfer across datasets, contributing to efforts for powerful event-based models in label-scarce settings.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **SpikeCLR**，这是一个专门为脉冲神经网络（SNN）设计的对比自监督学习框架，旨在解决事件相机（Event-based Vision）数据标注成本高、大规模标签数据集稀缺的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：事件相机具有高时间分辨率、高动态范围和低功耗等优势，与脉冲神经网络（SNN）结合是实现高效能嵌入式视觉的理想方案。然而，SNN 的训练高度依赖大规模标注数据，而事件数据的标注极其困难且耗时。
*   **核心问题**：如何在无标注的情况下，利用事件流的固有结构（时空特性和极性）让 SNN 学习到鲁棒的视觉表征，从而在标签极少的“小样本（Few-shot）”场景下依然保持高性能。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：借鉴 SimCLR 的对比学习框架，通过最大化同一样本不同增强视图之间的相似度，同时最小化不同样本之间的相似度，来训练 SNN 编码器。
*   **关键技术细节**：
    *   **输入表示**：将异步事件流转换为归一化的事件直方图（Event Histograms），保留 $T$ 个时间步和正负双极性通道。
    *   **SNN 架构**：采用 SEW-ResNet-18 作为主干网络，利用 **LIF 脉冲神经元**处理时序信息。
    *   **训练机制**：使用**代理梯度（Surrogate Gradient）**方法解决脉冲函数不可微的问题，并通过随时间反向传播（BPTT）进行优化。
    *   **事件特定增强策略**：
        *   **空间增强**：随机裁剪、水平翻转、循环滚动（Rolling）。
        *   **极性增强**：模拟传感器增益变化，对正负极性通道进行全局缩放和均值偏移。
        *   **时间增强**：从原始事件流中随机采样不同的时间窗口（Temporal Crop），捕捉不同的运动相位。
    *   **损失函数**：采用 NT-Xent 损失。论文还探索了“时间 NT-Xent”，即在每个时间步独立计算损失以增强学习信号。

### 3. 实验设计
*   **数据集**：
    *   **静态衍生数据集**（由静态图像重录制）：CIFAR10-DVS, N-Caltech101, N-MNIST。
    *   **动态事件数据集**（真实运动）：DVS-Gesture。
*   **评估协议**：
    *   **线性探测（Linear Probing）**：冻结编码器，仅训练线性分类头，测试表征的线性可分性。
    *   **微调（Fine-tuning）**：全网络端到端微调。
    *   **小样本场景**：测试每类仅有 $k \in \{1, 10, 20, 50\}$ 个标签时的性能。
*   **对比方法**：完全监督学习（从头训练）、EventDrop、NDA（神经形态数据增强）等。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 `SpikingJelly` 框架和 `Tonic` 库进行数据处理。实验中涉及了 11.2M 参数的 ResNet-18 和 1.4M 参数的轻量化版本。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流基准数据集，进行了大量的消融实验（针对增强策略、损失函数聚合方式、骨干网络架构）。
*   **充分性与客观性**：
    *   为了保证结果的鲁棒性，小样本实验在 10 个随机划分的子集上取平均值。
    *   对比了多种 SNN 架构（ResNet, VGG, 可分离卷积），证明了方法的普适性。
    *   进行了跨数据集的**迁移学习**实验，验证了表征的泛化能力。整体实验设计严谨、客观。

### 6. 主要结论与发现
*   **自监督优势**：在所有测试数据集中，SpikeCLR 预训练在小样本场景下均显著优于传统的监督学习。
*   **增强策略的重要性**：**时间增强和极性增强**对事件数据至关重要，其贡献超过了传统的空间增强。
*   **时间维度利用**：在每个时间步独立计算对比损失（Temporal NT-Xent）比仅对时间平均后的特征计算损失效果更好。
*   **可扩展性**：随着无标注预训练数据量的增加，下游任务的性能持续提升。

### 7. 优点（亮点）
*   **首次系统性研究**：这是首个在小样本标签稀缺场景下系统评估 SNN 对比自监督学习的工作。
*   **适配性强**：设计的增强策略充分考虑了事件相机的物理特性（如极性和异步时序）。
*   **高效性**：证明了即使是参数量极小的 SNN 也能通过该方法获得强大的特征提取能力，适合神经形态硬件部署。

### 8. 不足与局限
*   **内存瓶颈**：SimCLR 风格的对比学习依赖大 Batch Size，而 SNN 带有时间维度，这会导致训练时的显存压力巨大，限制了更长序列的处理。
*   **任务局限**：目前仅在分类任务上进行了验证，尚未扩展到目标检测、深度估计或光流计算等回归任务。
*   **表示形式**：虽然使用了 SNN，但输入仍依赖于网格化的直方图表示，未能完全脱离“帧”的概念来实现纯异步的自监督学习。

（完）
