---
title: Fine-tuning MLLMs Without Forgetting Is Easier Than You Think
title_zh: 不遗忘地微调多模态大语言模型（MLLM）比你想象的要简单
authors: "He Li, Yuhui Zhang, Xiaohan Wang, Kaifeng Lyu, Serena Yeung-Levy"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14493v1"
tags: ["query:ai-llm"]
score: 9.0
evidence: 多模态大语言模型微调与灾难性遗忘
tldr: 本研究探讨了如何缓解多模态大语言模型（MLLM）在微调时的灾难性遗忘。研究者通过2x2实验框架评估了模型在不同分布图像和文本下的表现，发现简单的正则化（如限制参数量、低学习率）和数据混合训练策略足以应对遗忘问题。该方法在持续学习任务中也优于复杂的现有方法，证明了MLLM本身具有较强的鲁棒性，为保持模型通用能力提供了实用的微调指南。
selection_source: fresh_fetch
motivation: 旨在寻找一种简单有效的方法来解决多模态大语言模型在特定任务微调时丧失通用能力的灾难性遗忘问题。
method: 采用限制可训练参数、降低学习率的正则化手段，并结合多任务数据混合训练策略。
result: 实验表明简单调整即可在处理分布外数据时保持性能，且在持续学习中表现优于复杂的辅助机制。
conclusion: 研究挑战了现有关于MLLM脆弱性的假设，证明通过基础的微调策略优化即可实现鲁棒的知识保留。
---

## 摘要
本文证明，对多模态大语言模型（MLLM）的微调方案进行简单调整，就足以缓解灾难性遗忘。在视觉问答任务上，我们设计了一个 2x2 实验框架，用于评估模型在分布内（in-distribution）和分布外（out-of-distribution）图像及文本输入下的性能。我们的结果表明，适当的正则化（例如限制可训练参数的数量或采用较低的学习率）在处理分布外图像时能有效防止遗忘。然而，我们发现在分布内图像和分布外文本的设置下存在一种独特的遗忘形式。我们将这种遗忘归因于特定任务的过拟合，并通过引入一种结合了数据集和任务的数据混合训练策略来解决这一问题。最后，我们证明该方法可以自然地扩展到持续学习，其性能优于现有的具有复杂辅助机制的方法。总的来说，我们的发现通过强调 MLLM 固有的鲁棒性，并为在保留其通用能力的同时进行适配提供了实用指南，从而挑战了现有的普遍假设。

## Abstract
The paper demonstrate that simple adjustments of the fine-tuning recipes of multimodal large language models (MLLM) are sufficient to mitigate catastrophic forgetting. On visual question answering, we design a 2x2 experimental framework to assess model performance across in-distribution and out-of-distribution image and text inputs. Our results show that appropriate regularization, such as constraining the number of trainable parameters or adopting a low learning rate, effectively prevents forgetting when dealing with out-of-distribution images. However, we uncover a distinct form of forgetting in settings with in-distribution images and out-of-distribution text. We attribute this forgetting as task-specific overfitting and address this issue by introducing a data-hybrid training strategy that combines datasets and tasks. Finally, we demonstrate that this approach naturally extends to continual learning, outperforming existing methods with complex auxiliary mechanisms. In general, our findings challenge the prevailing assumptions by highlighting the inherent robustness of MLLMs and providing practical guidelines for adapting them while preserving their general capabilities.

---

## 论文详细总结（自动生成）

这篇论文对多模态大语言模型（MLLM）在微调过程中的“灾难性遗忘”现象进行了深入研究，挑战了“必须使用复杂算法才能保留通用能力”的传统观点。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当 MLLM 为了适配特定下游任务（如医疗、机器人、工业检测）进行微调时，往往会丧失预训练阶段获得的通用推理能力（即灾难性遗忘）。
*   **研究动机**：现有解决遗忘的方法通常极其复杂（如参数隔离、复杂的正则化、重放缓冲区等），增加了架构和训练开销。作者试图探究：**是否通过简单的训练策略调整，就能在不牺牲专业任务性能的前提下保留通用能力？**

### 2. 论文提出的方法论
论文提出了一套简单但系统的微调准则（Recipe）：
*   **2×2 评估框架**：将分布偏移分解为两个维度：**图像（ID/OOD）**和**文本/任务（ID/OOD）**。这有助于精准定位遗忘发生的具体场景。
*   **简单正则化（Simple Regularization）**：
    *   **低学习率**：使用极低的学习率（如 $1e-6$）进行全参数微调。
    *   **参数高效微调（PEFT）**：采用 LoRA 等技术限制可训练参数量。
*   **数据混合策略（Data-Hybrid Training）**：针对“分布内图像+分布外文本”导致的过拟合（即模型学会了忽略指令直接输出标签），通过在微调数据中混入少量通用指令数据（如 LLaVA-665K）来强制模型关注 Prompt 指令。
*   **持续学习策略**：提出 **IncLoRA**（增量 LoRA 合并）和 **SeqFull**（顺序全参数微调），在不使用复杂路由机制的情况下处理多任务流。

### 3. 实验设计
*   **基础模型**：Qwen2.5-VL (3B/7B)、LLaVA-1.5-7B。
*   **微调任务**：ImageNet-VQA（基于 ImageNet 图像构建的多轮选择题）。
*   **评估数据集**：
    *   **ID 图像/ID 文本**：ImageNet 验证集、ImageNetV2。
    *   **OOD 图像/ID 文本**：Flowers102、Caltech101、Stanford Cars。
    *   **ID 图像/OOD 文本**：ImageWikiQA（针对 ImageNet 图像询问外部知识）。
    *   **OOD 图像/OOD 文本**：MMMU、VMCBench。
*   **特殊领域**：BSCCM（显微镜图像）、PitVis（外科手术视频）。
*   **持续学习 Benchmark**：MLLM-CL（涵盖遥感、医疗、自动驾驶、科学、金融五个领域）。
*   **对比方法**：对比了 MoELoRA、O-LoRA、L2P、ModalPrompt、HiDe-LLaVA、MR-LoRA 等多种专门针对持续学习设计的复杂 SOTA 方法。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号和数量。
*   **训练细节**：提到了使用 LLaMA-Factory 框架，Batch Size 为 16 或 32，训练步数通常为 80,000 步（约 1 个 Epoch），学习率在 $1e-4$ 到 $1e-6$ 之间波动。

### 5. 实验数量与充分性
*   **实验规模**：实验非常充分。作者不仅在主流模型（Qwen, LLaVA）上验证，还跨越了从自然图像到极端的专业领域（医疗、手术、遥感）。
*   **消融实验**：针对学习率、可训练模块（Backbone vs. Projector vs. Vision Encoder）、数据混合比例、数据量大小（从 0.25% 到 100%）均做了详尽的消融研究。
*   **客观性**：通过 2x2 矩阵量化了不同类型的遗忘，区分了“知识擦除”与“指令忽略（任务过拟合）”，实验设计逻辑严密且公平。

### 6. 主要结论与发现
1.  **遗忘并非不可避免**：只要使用低学习率或 LoRA，模型在提升专业任务性能的同时，几乎不会在 OOD 图像上发生遗忘。
2.  **不存在必然的权衡**：专业化（Specialization）和通用性保留（Preservation）可以兼得。
3.  **识别出“任务特定过拟合”**：在 ID 图像上出现的性能下降并非知识丢失，而是模型学会了“偷懒”，即看到熟悉的图像就直接预测类别而忽略了 Prompt。
4.  **简单胜过复杂**：在持续学习任务中，简单的顺序微调（SeqFull）或增量 LoRA 在无重放（No-replay）场景下表现优于所有复杂的 SOTA 算法。

### 7. 优点
*   **实用性极强**：为开发者提供了直接可用的微调指南（低学习率 + 数据混合）。
*   **视角独特**：通过 2x2 矩阵揭示了 MLLM 遗忘的本质是“指令忽略”而非单纯的“参数覆盖”。
*   **挑战权威**：有力地反驳了 MLLM 适配必须依赖复杂架构的假设，简化了研究路径。

### 8. 不足与局限
*   **任务类型局限**：实验主要集中在多选题（Multiple-choice）形式的 VQA，对于开放式生成（Open-ended generation）或长文本推理的遗忘情况探讨较少。
*   **模型架构覆盖**：虽然测试了 Qwen 和 LLaVA，但未涵盖基于交叉注意力（Cross-attention）架构的模型（如 Flamingo 风格）或纯 Encoder-based 模型。
*   **算力细节缺失**：缺乏具体的资源消耗报告，难以评估简单方法在超大规模模型上的扩展性成本。

（完）
